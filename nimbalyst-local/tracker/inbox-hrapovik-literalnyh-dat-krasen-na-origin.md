---
trackerStatus:
  type: inbox
title: "Храповик литеральных дат КРАСЕН на origin/main: два файла вошли в класс, база расти не может"
status: new
created: 2026-09-07
---

## Что случилось

`spa_core/tests/test_frozen_date_ratchet.py::test_no_new_file_joins_the_frozen_date_class` краснеет на ЧИСТОМ `origin/main` (sha 64516493f) — измерено дифференциально на контрольном дереве, пришпиленном к этому sha, без единой своей правки.

В класс литеральных дат вошли два файла, доставленные предыдущими циклами:

- `spa_core/tests/test_cio_failure_modes.py`
- `spa_core/tests/test_decision_reproducibility.py`

## Почему это нельзя чинить прицепом

Правило `.claude/rules/deployment.md` (раздел о времени в тестах) прямо запрещает гасить падение дописыванием файла в `frozen_date_baseline.json`: база может ТОЛЬКО уменьшаться. Чинить надо фикстуру — перевести проверку на инъекцию часов (приём №1) либо на относительные отметки (`_freshness.ts`), а если литеральная дата и есть предмет проверки — поставить `# FROZEN-DATE-OK: <причина>`, причём причину `injected-clock` теперь СВЕРЯЕТ с кодом AST-сторож `test_injected_clock_claim`.

Это отдельная работа с отдельным замером, поэтому цикл #512 её НЕ сделал молча (инвариант #16) и не приписал к своей правке.

## Критерий «сделано»

`test_no_new_file_joins_the_frozen_date_class` зелёный на `origin/main`, при этом `frozen_date_baseline.json` НЕ вырос (сверить число строк до и после), и `test_injected_clock_claim` подтверждает причину у каждого нового маркера.

## Что дальше

Красный храповик — бомба замедленного действия того самого класса, ради которого он написан: пока он красен, следующий файл войдёт в класс незамеченным.
