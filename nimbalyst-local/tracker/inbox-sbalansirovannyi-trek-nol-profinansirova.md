---
trackerStatus:
  type: inbox
title: "Сбалансированный трек: ноль профинансированных дней из 40 при 920 отработанных циклах"
status: new
created: 2026-08-08
---

## Что случилось и почему это важно

Мандат владельца — три сопоставимых paper-трека по $100k (Conservative / Balanced / Aggressive).
Замер цикла #171 (2026-08-09): **у Balanced нет ни одного профинансированного дня.**

| Книга | Строк истории | Строк с equity > 0 | Циклов отработано | Начало |
|---|---|---|---|---|
| `hy_paper_trading.json` (рукав B) | 40 | **0** | 920 | 2026-06-30 |
| `lp_paper_trading.json` (рукав C) | 41 | **0** | 931 | 2026-06-22 |

Позиций — ноль, сделок — ноль, `daily_yield_usd` — ноль в КАЖДОЙ строке, включая сегодняшнюю.
Агенты при этом живы и исправно крутят циклы: работа имитируется.

Для сравнения, Aggressive — настоящий: 27–39 форвардных дней, капитал двигался
(`pendle_yt_susde` 108 670.74, `lrt_neutral` 101 113.54), одна книга остановлена kill-switch.

## Что уже сделано и чего не хватает

Замок самозасева был снят вчера (`e1ae173a4`, 08.08 21:17Z): условие «денег не было НИКОГДА».
Шапки файлов действительно профинансированы ($66 666.67 + $33 333.33). **Но строка истории за
2026-08-08 осталась нулевой**, потому что запись за дату делается ОДИН раз:

```python
existing_dates = {entry.get("date") for entry in state.get("daily_history", [])}
if today not in existing_dates:      # spa_core/paper_trading/hy_cycle.py
```

Строка за 08.08 была создана ранним прогоном ещё до засева, и вечерний прогон (21:35Z) её
уже не переписал. То есть дефект, вероятно, САМ рассосётся со следующего UTC-дня — но это
предположение, а не измерение.

## Что сделать

1. Проверить фактом, а не рассуждением: после первого прогона 09.08 в `daily_history` обязана
   появиться строка с `equity > 0` и `positions_count > 0` у ОБЕИХ книг.
2. Если не появилась — разбирать: засев в шапке есть, а строка нулевая ⇒ расходятся шапка и
   история (класс «одно число, два вердикта»).
3. Сторож: трек, у которого N дней подряд `equity == 0` при живых циклах, обязан КРИЧАТЬ.
   Сейчас 47 дней простоя не заметил никто — ни один сторож не спрашивал «а книга-то не пуста?».
   Это тот же класс fail-OPEN: каждый честно отвечал на свой вопрос.
4. Пока книга пуста — на публичном сайте писать «идёт paper-трек» про Balanced НЕЛЬЗЯ
   (инвариант #8). Цикл #171 по этой причине отказался публиковать половину уже одобренной
   владельцем правки; см. карточку `owner-decision-sait-avtonomnaya-pravka-zadela-owner-gat-3`.

## Ставки выросли: утверждение уже НА ПУБЛИЧНОМ САЙТЕ

2026-08-09 00:30 (местного) параллельная сессия с одобрения владельца опубликовала
`landing/src/pages/packages.astro` (коммит `39adb9d6f`) со статусом
`Balanced · идёт paper-трек · без live-аллокации`. Замер выше сделан ДО этого и утверждению
противоречит: у книги ноль профинансированных дней.

Решение владельца не отменяется и сайт этим циклом не трогается — это его поверхность.
Но проверка перестала быть внутренней гигиеной: **публичная страница уже утверждает то, чего
данные пока не подтверждают** (инвариант #8, evidence-level). Поэтому пункт 1 ниже — срочный,
и закрывать его надо ФАКТОМ.

## Как понять, что готово

`hy_paper_trading.json` и `lp_paper_trading.json` имеют хотя бы одну строку с `equity > 0`,
и это показано прогоном. Плюс сторож, который краснеет на пустой книге при живых циклах —
с положительным контролем на этих самых 47 днях.

---

## Замер по пункту 1 — ФАКТОМ, цикл #208 (2026-08-13)

Карточка требовала закрыть пункт 1 фактом, а не рассуждением. Закрываю: **предположение
«само рассосётся со следующего UTC-дня» подтвердилось наполовину.**

| | `hy_paper_trading.json` (B) | `lp_paper_trading.json` (C) |
|---|---|---|
| строк истории | 45 | 46 |
| строк с `equity > 0` | **5** (с 09.08, было 0) | **5** (с 09.08, было 0) |
| строк с `positions_count > 0` | **0 из 45** | **0 из 46** |
| `positions` в шапке | 0 | 0 |
| `daily_yield_usd` за 13.08 | 14,6247 | 7,7698 |

**Критерий приёмки этой карточки требовал `equity > 0` И `positions_count > 0` у ОБЕИХ книг.
Выполнена только первая половина.** Деньги в книгах есть, цифра каждый день растёт — и при
этом не открыто ни одной позиции.

**Почему цифра растёт без позиций** (`spa_core/paper_trading/hy_cycle.py`, блок «accrue ONE
day of carry yield»): пока `regime == ENTER`, книге начисляется `daily_yield(equity,
hy_target_apy_pct())` — средняя ставка полосы на ВСЮ сумму, независимо от того, держит ли
книга хоть что-нибудь. В ту же строку истории пишется `positions_count = len(positions)`,
то есть ноль. Комментарий рядом гласит «No yield invented», и формально это так — ставка
настоящая, — но начисляется она на кэш, а не на портфель.

Это меняет диагноз, а не снимает его: раньше книга была ПУСТА, теперь она **фантомна**.
Дефект того же класса, что и весь наш улов последних недель: каждый слой честно отвечает на
свой вопрос («деньги есть?» — да), и ни один не отвечает на нужный («а держим ли мы хоть
что-то?»).

**Что из этого следует и куда ушло:**

1. Публичное утверждение (пункт 4 этой карточки) — вопрос владельцу
   `owner-decision-sbalansirovannyi-tir-na-saite-idet-paper` (`needs-owner`, отправлен):
   на сайте про Balanced написано «идёт paper-трек», а книга ничего не держит. Сам не
   трогаю — числа и статусы тиров owner-gated.
2. Пункты 2 и 3 (позиции в книге + сторож «трек с нулевыми позициями при живых циклах
   обязан краснеть») остаются открытыми в ЭТОЙ карточке. Сторож нужен именно с
   положительным контролем на этих самых строках: 45 строк подряд с нулём позиций не
   заметил никто.

---

## Замер по пунктам 2–3 — цикл 2026-08-18: причина названа, и она не в фильтрах

Пункт 2 карточки предполагал расхождение «шапка ↔ история». Замер показывает причину на
уровень глубже, и она снимает саму постановку «книга не проходит фильтры».

**В коде рукавов B и C нет шага выбора протоколов.** Машинный разбор (AST) обоих циклов —
полный список ключей состояния, в которые они вообще пишут:

```
spa_core/paper_trading/hy_cycle.py
  state-key writes: ['LLM_FORBIDDEN','cycles_completed','drawdown_pct','equity',
                     'last_cycle_at','note','peak_equity','regime','seed_equity']
  writes to 'positions': []
spa_core/paper_trading/lp_cycle.py
  state-key writes: ['LLM_FORBIDDEN','cycles_completed','equity','il_drawdown_pct',
                     'last_cycle_at','note','peak_equity','seed_equity']
  writes to 'positions': []
```

`positions` читается (`len(state.get("positions", []))` → в историю) и не пишется НИКОГДА.
Писателей у обоих файлов ровно два, оба внутри самих циклов (`save_hy_state` ×3,
`save_lp_state` ×3) — больше в `spa_core/` и `scripts/` никто в эти книги не пишет.
Импорты обоих циклов: `pathlib`, `json`, `os`, `clock`, `regime_gate`, `sleeve_yield`.
Ни адаптеров, ни `ADAPTER_REGISTRY`, ни `RiskPolicy`, ни аллокатора.

**Разложение отказов по причинам (пункт 3) — все нули, и это не опечатка:**

| Причина отказа | Протоколов отсеяно |
|---|---|
| нет живого TVL (порог $5 млн) | **0** |
| гейт доказанности APY | **0** |
| классовые гейты (advisory / RESEARCH_ONLY / тир) | **0** |
| потолки концентрации | **0** |
| **рассмотрено кандидатов всего** | **0** |

`policy_hy.evaluate_protocol` и `policy_lp.evaluate_lp_position` вызываются во всём
не-тестовом коде РОВНО по одному разу — из чек-листов go-live (CHECK-HY-004 / CHECK-LP-003),
с литеральными выдуманными аргументами (`Pendle_test`, TVL $1 млрд; `USDC_USDT`, TVL $180 млн),
и только чтобы убедиться, что функция не выбрасывает исключение. Торговый цикл их не зовёт.

**Ответ на вопрос «рынок не даёт кандидатов или наши измерения не работают»: ни то, ни другое.**
Не построена сама покупка. Это третий ответ, и он дешевле в диагностике, но дороже в починке.

**Рост цифры воспроизведён формулой, без единого входа о позициях** (подтверждает: доход —
функция остатка и средней ставки, портфель в нём не участвует):

```
HY seed 66666.67 @ 8.0% , 5 accrual days -> equity 66739.76, day-5 yield 14.6247 (карточка: 14.6247)
LP seed 33333.33 @ 8.5% , 5 accrual days -> equity 33372.16, day-5 yield  7.7698 (карточка:  7.7698)
```

**Пункт 3 (сторож) — подтверждён фактом, что сторожа нет и пустота проходит go-live.**
Прогон обоих чек-листов на синтетической книге ровно того вида, что в проде:

```
--- GoLive-HY на ПУСТОЙ книге (positions=[], 45 строк) ---
  CHECK-HY-001   PASS     value=45 thr=14
  CHECK-HY-002   PASS     value=45 thr=7
  CHECK-HY-003   PASS     value='0.00%' thr='-8.00%'
--- GoLive-LP на ПУСТОЙ книге (positions=[], 46 строк) ---
  CHECK-LP-001   PASS     46 дней в daily_history
  CHECK-LP-002   PASS     Worst IL drawdown: 0.00% — в пределах нормы
  CHECK-LP-006   PASS     Нет открытых позиций — требование выполнено
```

`CHECK-LP-006` («все позиции delta-neutral») на пустой книге отвечает «требование выполнено».
Ни один из 12 чеков обоих списков не спрашивает «держит ли книга хоть что-нибудь».

**Куда ушло.** Постройка шага выбора и блокирующая проверка «книга не пуста» — money-path и
owner-gated, сам не трогаю: карточка `owner-decision-sbalansirovannyi-paket-ne-pokupaet-niche`
(`needs-owner`, три варианта с измеренной ценой). Пункты 2 и 3 этой карточки остаются
открытыми до ответа владельца.

**Где замер неполон, честно.** Числа 45/46 строк перепроверить на живых данных из контейнера
НЕЛЬЗЯ: `data/hy_paper_trading.json` здесь — git-снимок от 22.06 (0 строк истории, `regime:
EXIT`), `data/lp_paper_trading.json` — 1 строка, `data/apy_ranking.json` — от 21.06. Живая
книга лежит только на Маке. Поэтому подтверждение числового замера карточки — косвенное
(точное воспроизведение обоих `daily_yield_usd` формулой), а структурный вывод «позиций не
может быть НИ ОДНОЙ» — прямой и от данных не зависит: он следует из кода.
