---
trackerStatus:
  type: inbox
title: Расширить опрашиваемый набор адаптеров шагами (второй шаг варианта A, после ADR-138)
status: in-progress
source: nimbalyst
created: 2026-08-26
---

## Что случилось

Первый шаг варианта A выполнен (ADR-138): аллокатор теперь **осознанно отбирает лучшие 8**
строго ПОСЛЕ проверок доходности и живого размера пула, и книга из 27 проходных протоколов
проходит гейт целиком вместо ухода в аварийный фолбэк.

Второй шаг — расширение опрашиваемого набора — по решению владельца делается **отдельно,
шагами, с замером в песочнице на каждом шаге**.

Сегодня цикл опрашивает **8** протоколов (`POLLED_ADAPTERS`,
`spa_core/orchestrator/adapter_orchestrator.py`), канонический реестр (`ADAPTER_REGISTRY`)
знает **36**.

## Что надо сделать

1. Расширять `POLLED_ADAPTERS` **шагами** (не разом до 36). После каждого шага — прогон в
   песочнице и замер: сколько протоколов проходит все проверки, сколько финансируется, что
   говорит `policy_enforcer` о готовой раскладке.
2. Никаких прогонов против живого `data/` — только одноразовое дерево
   (`.claude/rules/deployment.md`, п. 4).
3. Вернуть храповик «покрытие вне Ethereum ≥ 1» — он снимался, пока опрос был из одних
   L1-протоколов.
4. Проверять на каждом шаге, что аварийная книга НЕ включается: её включение теперь означает
   не переполнение по счёту (это закрыто), а другую причину — и её надо назвать, а не
   пережить.

## Как понять, что готово

`POLLED_ADAPTERS` расширен, на каждом шаге есть записанный замер, храповик покрытия вне
Ethereum снова живой, и предложение аллокатора на расширенном наборе проходит
`policy_enforcer` без нарушений.

## Почему это отдельная задача

Менять состав опрашиваемых протоколов — значит менять, какие протоколы вообще рассматриваются
деньгами. Владелец разделил это на два шага сознательно: пока аллокатор не умел отбирать
лучших, «больше кандидатов» буквально означало «хуже книга». Теперь умеет — но расширение всё
равно идёт с замером, а не одним движением.


---

## ШАГ 1 ИСПОЛНЕН — 2026-08-29

**Сначала замер, и он опроверг общее убеждение.** Считалось, что «все живые кандидаты сидят на
Ethereum». Неверно: снимок оркестратора знал **8** протоколов (все L1), а `adapter_status.json`
знал **34**, из них **7 живых вне Ethereum**. Книгу держала на одной цепочке не рыночная
реальность, а СОСТАВ ОПРОСА.

**Замер в песочнице** (`run_orchestrator(write=False)` → аллокатор), 8 против 10:

| | было | стало |
|---|---|---|
| протоколов в книге | 6 | **8** |
| кэш | **10 %** | **0 %** |
| ожидаемая APY | 3.47 % | **3.87 %** |
| защитные тримы | `chain_caps: 0.10` | **пусто** |

Потолок «одна цепочка ≤ 90 %» больше не связывает — то есть исчезает ровно тот лимит, который
делал перераздачу ADR-160 бесполезной. ALLOC-002 отработал осознанным отбором лучших восьми
(ADR-138), а не обвалом в аварийный фолбэк, как при откате 08.08.

**Взяты двое:** `aave_v3_base` (T2, живой TVL $23.8M) и `morpho_blue_base` (T2, живой TVL
$448.9M, APY 4.44 %).

**`aave_arbitrum` НЕ взят сознательно:** отдаёт `tvl_source="static"` — литерал $1.2 млрд, а
порог TVL проверяется только живым числом. В песочнице он забирал 24.3 % книги, опираясь на
константу. Цена отказа названа: с ним APY была бы 4.04 %, без него 3.87 %. Карточка на живой
фид — `inbox-aave-arbitrum-otdaet-staticheskii-tvl`.

**Храповик «покрытие вне Ethereum ≥ 1» ВОЗВРАЩЁН** (инв. 16, обоснование в теле теста и в
журнале): условие, названное в его же тексте, выполнено — осознанный отбор приземлён и замером
подтверждён. Положительный контроль: при снятии двух новых адаптеров храповик краснеет.

**Следующий шаг** — по тому же правилу: живой фид для `aave_arbitrum`, затем отдельный шаг
расширения с новым замером.

---

## Замер кандидата `spark_susds` (цикл #495, 2026-09-05) — кандидат ОТКЛОНЁН

Пришло из соседней карточки `inbox-ozhivit-fidy-vne-ethereum-put-k-snyatiyu` (шаг (3),
ADR-236). Карточку эту НЕ беру — только прикладываю замер к её порядку «шагами, с замером
в песочнице».

**Почему кандидат вообще возник.** Владелец решением ADR-232 (05.09, вариант А) объявил,
что `spark_susds` для нас — кредитный рынок, и пул запинён. Замер показал, что это решение
**невидимо гейту**: заморозку ADR-053 применяет `_apply_risk_policy_gate` по снимку
ОРКЕСТРАТОРА, а `spark_susds` там отсутствует. При живом пине (`tvl_source: live`,
$242.5 млн) принудительная заявка на $10 000 отклоняется как
`TVL unverified (missing) — fail-closed`. Ключ непригоден к финансированию по построению,
пока его не опрашивают.

**Замер опроса** (добросовестная симуляция: в снимок оркестратора добавлен ключ ровно с
наблюдёнными числами — T1, 2.3185 пп, $242.5 млн, `tvl_source: live`):

| | размещено | кэш | книга |
|---|---|---|---|
| без опроса `spark_susds` | $84 453.48 | 15.55 % | 7 позиций |
| **с опросом** | **$84 453.48** | **15.55 %** | те же 7 позиций |

**Разницы нет ни на цент.** ALLOC-002 (ADR-138) отсекает ключ в числе худших восьми:
при честной ставке 2.32 пп он не проходит в лучшие восемь. Проходных стало 16 при пределе 8.

**Вывод для этой карточки:** `spark_susds` в опрашиваемый набор брать НЕ НУЖНО — это
измерено, а не предположено. Расширение ради него стоило бы одного сетевого опроса за цикл
и не двигало бы ни доллара.

**Инструмент для следующих кандидатов:** `scripts/measure_pin_placement_effect.py`
(дифференциально, один снимок фида, песочница; фид не ответил ⇒ `unmeasured`, не ноль).
