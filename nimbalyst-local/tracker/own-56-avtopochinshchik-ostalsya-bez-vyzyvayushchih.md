---
trackerStatus:
  type: owner-decision
title: "Автопочинщик кода остался без вызывающих: убрать совсем или оставить лежать"
status: ingested
created: 2026-08-17
owner_choice: 2
owner_answered_at: 2026-08-21T09:39:14.776512+00:00
owner_answer_via: telegram
owner_answered_by: 258651137
---

## Что случилось и почему это важно

Ты решил списать сторожа `telegram_watcher` — он забирал твои команды из Телеграма себе
(карточка own-55). Сделано. Но у той петли было две половины: сторож её **запускал**, а
саму починку делал второй модуль — `spa_core/devtools/auto_fixer.py`. Он просит Claude
переписать прод-код, применяет правку, прогоняет тесты и пушит на GitHub. После списания
сторожа звать его в боевом коде стало **некому** — а сам он остался целиком, вместе со
своей точкой запуска: одна строка `python3 -m spa_core.devtools.auto_fixer` — и петля,
которую ты только что закрыл, снова открыта. Ровно этот класс («модуль без вызывающего =
заряженное ружьё») мы уже ловили на `bot_commands`.

Твоя формулировка была про петлю целиком: «авто-починка кода по тексту из чата — самая
рискованная петля в системе». Но твоё решение названо поимённо про сторожа, поэтому сам
модуль я НЕ трогал: удалять боевой код по догадке о твоём намерении — не моё право.

## Что от тебя нужно

Выбери:

1. **Убрать совсем** (моя рекомендация): удалить модуль и его тесты. Тогда LLM-правки
   прод-кода в системе не остаётся вовсе, и запустить её будет нечем. Ничего не сломается:
   вызывающих у неё сейчас ноль.
2. **Оставить, но обезоружить**: снять `__main__` (точку запуска) и оставить функции как
   библиотеку — на случай, если ты захочешь вернуть авто-починку под ручным присмотром.
3. **Оставить как есть** — тогда я записываю решение вслух в ADR, и «кто-то переписал файл
   и запушил» перестаёт быть загадкой: у этого будет названная причина.

## Как понять, что готово

`spa_core/devtools/auto_fixer.py` либо удалён, либо в нём нет `if __name__ == "__main__"`,
либо в `docs/decisions/` есть запись, что он оставлен сознательно.

## Что будет после

Получив ответ: вариант 1 — удаляю модуль и тесты, снимаю исключение
`ORPHANED_DOORS_KNOWN_GAP` из сторожа `spa_core/tests/test_one_telegram_poller.py` (он
специально краснеет в день закрытия вопроса, чтобы закрытие не прошло молча); вариант 2 —
снимаю точку запуска и завожу вызывающего под твоим контролем; вариант 3 — пишу ADR и
оставляю сторожа с названным исключением.

---

## 🚚 Перенесено на `main` циклом #324 (20.08→21.08)

Карточка родилась на ветке `origin/claude/work-status-check-xfnbew` (`6025edc8a`) и на `main` её
не было ВООБЩЕ. Перенос — по решению владельца 20.08 19:12:45Z (вариант 1, ADR-100):
разобрать карточки ветки СОДЕРЖАНИЕМ, потом ветку удалить
(задача `inbox-razobrat-52-kartochki-s-vetki-work-statu`).

**С чем сверялось на `main`:** совпадений на `main` НЕТ (искал по `auto_fixer` и «автопочин» — ноль карточек).

**Почему всё-таки перенесена:** **ПРОВЕРЕНО НА `main` циклом #324 — модуль ЖИВ:** `spa_core/devtools/auto_fixer.py` (26 КБ) на
месте, и `spa_core/monitoring/telegram_watcher.py` тоже на месте — то есть списание сторожа,
о котором карточка говорит «сделано», на `main` НЕ доставлено (оно было на ветке `6025edc8a`).
Читать карточку надо с этой поправкой: петля «LLM переписывает прод-код и пушит» на `main`
закрыта НЕ наполовину, а не закрыта вовсе. Предшественница `own-55` тоже осталась на ветке.

**Владельцу пачкой НЕ отправлялась** (ADR-084: штатное и решённое не звонит владельцу) — `notify`
по ней не запускался. Код и тесты ветки не переносились: так записано в решении владельца.

---

## Решение владельца

**Вариант 2** — Оставить, но обезоружить

_Ответ владельца получен 2026-08-21T09:39:14.776512+00:00 (telegram). Карточка закрыта самим владельцем, не агентом (инвариант #14)._

---

## 📥 Инжест циклом #331 (2026-08-21)

**ADR-106** — ДОСТАВЛЕНО. У `auto_fixer` нет ни `main()`, ни `__main__`; библиотека цела (вариант 2, не 1). Замер остатка: `telegram_watcher` жив и зовёт починку внутрипроцессно, его plist в репозитории есть, но агент НЕ загружен. Судьба сторожа (твой ответ 15.08 потерялся с закрытой веткой) переспрошена: `owner-decision-peresprashivayu-sudba-storozha-telegram`.
