---
trackerStatus:
  type: inbox
title: "Текст инварианта 10 (Sky/sUSDS = 0 %) живёт как действующий ещё в docs/"
status: new
source: nimbalyst
created: 2026-08-23
---

## Что случилось и почему это важно

Решением владельца 23.08 (вариант 1, ADR-126) правило «Sky/sUSDS = 0 % до подтверждённой
задержки GSM Pause Delay ≥ 48 часов» снято как **исполненное** — условие выполнено 5 августа
и записано ADR-065. Правка сделана ровно в двух файлах, которые решение и называло: главный
файл правил `CLAUDE.md` и правило про адаптеры `.claude/rules/adapters.md`.

Тот же текст как действующий запрет остался ещё в исследовательских и аудиторских документах:
`docs/06_spa_core_invariants.md`, `docs/44_research_first_20_strategies.md`,
`docs/SOURCE_INTEGRATION_GUIDE.md`, `docs/EXECUTION_SAFETY_AUDIT_20260619.md`,
`docs/AGENT_REGISTRY.md`, `docs/DEFI_STRATEGY_RESEARCH_2025.md` и др. (замер — `grep`,
цикл #356).

Расширять решение владельца молча нельзя — поэтому остаток НАЗВАН карточкой, а не подчищен
заодно. Цена остатка ниже, чем у `CLAUDE.md`: эти файлы не читают все агенты в начале работы.
Но ровно тот же класс — документ, описывающий исполненное условие как запрет, — уже дал одну
ложную CRITICAL 16.08.

## Что надо сделать

Пройти по перечисленным файлам и в каждом заменить формулировку действующего запрета ссылкой
на ADR-065 (как сделано в `CLAUDE.md`), НЕ трогая исторические документы, где дата является
предметом: аудит от 2026-06-19 описывает состояние на свою дату и должен его описывать.
Отличить «живой справочник» от «снимка истории» — часть задачи, а не мелочь.

## Критерий приёмки

`grep -rn "Sky/sUSDS = 0"` по `docs/` даёт только исторические снимки, каждый с датой в
заголовке; ни один живой справочник не описывает запрет как действующий.

---

## ➕ Замер с ветки (цикл #375, пачка 4 — карточка `inbox-konstitutsiya-protivorechit-prinyatomu-a` НЕ перенесена как повтор)

На ветке `origin/claude/work-status-check-xfnbew` тот же предмет разобран карточкой
`inbox-konstitutsiya-protivorechit-prinyatomu-a` (`blocked`, 16.08). Дубля не завожу — её замер
приложен сюда, иначе он уйдёт вместе с веткой:

* **цена расхождения ИЗМЕРЕНА, а не гипотетична:** агент триажа 16.08 объявил КРИТИЧНЫМ
  нарушением живую строку кода — ровно потому, что читал инвариант 10 как действующий. Ложная
  «критичная находка» из текста конституции, а не из кода;
* сверка 17.08 своим чтением: `CLAUDE.md:93` и `.claude/rules/adapters.md:29` объявляют инвариант
  действующим, а `docs/decisions/ADR-065-sky-susds-promoted-to-t1.md` — **Accepted, 05.08**,
  решение владельца «повысить до T1, долю решает аллокатор», задержка наблюдена 172 800 с =
  48.00 ч кворумом независимых RPC;
* вопрос владельцу заведён и на `main` уже приехал: `owner-decision-pravilo-sky-susds-0-otmeneno-tvoim-zhe-r`
  (перенесена пачкой 2, статус `ingested`) — три варианта: снять как исполненный со ссылкой на
  ADR (рекомендация) · переформулировать в потолок доли · оставить ноль и отменить ADR-065;
* **отдельно и не смешивать:** вторая половина исходной карточки-родителя
  (`agent-safe-fallback-bypasses-adapter-gates` — «аварийный портфель обходит гейты адаптеров»)
  к тексту инварианта отношения не имеет.

Перемерено мной на `main` `d4e4fc18d`: обе строки конституции на месте (`CLAUDE.md`, раздел
инвариантов, п. 10; `.claude/rules/adapters.md`, строка про Sky/sUSDS) — расхождение живо.
