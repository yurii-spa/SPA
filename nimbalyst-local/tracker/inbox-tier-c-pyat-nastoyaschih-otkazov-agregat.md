---
trackerStatus:
  type: inbox
title: "Tier-C: пять настоящих отказов агрегатора — два чинятся, три требуют фактов, которых нет"
status: new
source: nimbalyst
created: 2026-08-06
claimed_by: cycle-87477
claimed_at: 2026-08-06T18:43:13Z
---

## Находка

Цикл #136 разобрал группу «64 failed» Tier-C. 59 из них оказались не отказами
модулей, а неспособностью агрегатора построить их доменный вход (переклассифицированы
в `unchecked` с названной причиной). **Осталось пять НАСТОЯЩИХ отказов** — эти
модули контекст принять МОГУТ и всё равно падают каждый прогон:

| модуль | исключение |
|---|---|
| `protocol_defi_interest_rate_kink_proximity_analyzer` | `TypeError: analyze() got an unexpected keyword argument 'context'` |
| `protocol_defi_validator_slashing_exposure_analyzer` | `TypeError: analyze() got an unexpected keyword argument 'context'` |
| `protocol_adoption_scorer` | `KeyError: 'unique_users_30d'` |
| `defi_protocol_flash_loan_attack_surface_analyzer` | `ValueError: Missing required keys: ['protocol_name', 'tvl_usd', 'single_block_borrowable_usd', …]` |
| `protocol_defi_protocol_maturity_score_analyzer` | `ValueError: Missing required fields: ['audit_count', 'bug_bounty_usd', 'chain_count', 'github_commits_90d', …]` |

## Два разных диагноза, не смешивать

**(а) Двое первых — дефект обвязки, чинится.** Диагноз снят с живого объекта:

```
signature: analyze(token: 'dict | None' = None, **kwargs: 'Any') -> 'dict'
source:    return analyze(token, config=self._config, **kwargs)
```

Классовая обёртка принимает контекст в `**kwargs` (поэтому `bind` проходит) и
пробрасывает его в модульную функцию `analyze`, которая аргумента `context` не
знает. Контекст при этом НЕ доезжает до `token`, ради которого всё писалось.
То есть обвязку начали и не довели: параметр есть, маршрут не проложен.
Починка — направить контекст в `token`, как у остальных обвязанных модулей.

**(б) Трое остальных — нехватка ФАКТОВ, и чинить их «дописав ключи» ЗАПРЕЩЕНО.**
`unique_users_30d`, `audit_count`, `bug_bounty_usd`, `single_block_borrowable_usd`
— это ровно тот класс входов, которые циклы #133/#134 измерили и отклонили:
их нет в `_protocol_facts`, и дописать их значит СОЧИНИТЬ вход. Правильный
исход для этой тройки — честная разметка (как `unsourced` в Tier-B), а не
выдуманные числа. См. `spa_core/analytics/_protocol_key_coverage.py`.

## Acceptance

- для (а): контекст доезжает до `token`, модуль перестаёт падать ИЛИ получает
  честный не-ok статус по доменной причине; тест-репродукция краснеет на
  сегодняшнем коде;
- для (б): решение записано (разметка/списание), ни одного нового ключа в
  `_protocol_facts` без источника факта;
- радиус измерен до правки, контроль в обе стороны, инв. #16.

## Радиус

Tier-C advisory, в hard-гейт RiskPolicy не попадает. Но `signal_aggregator`
общий для Tier-A/B/C — правка обвязки конкретных модулей радиус не расширяет,
правка адаптера расширяет (урок #136: аннотация не гарантия, Tier-A ловится
`test_tier_a_protocol_context.py`).

*Родительская карточка: `inbox-tier-c-171-iz-180-modulei-ne-otvechayut`. Цикл #136.*

---

## Замер цикла #137: диагноз (а) ОПРОВЕРГНУТ — эти двое тоже из группы (б)

Карточка относила `protocol_defi_interest_rate_kink_proximity_analyzer` и
`protocol_defi_validator_slashing_exposure_analyzer` к «двум чинящимся»:
направить контекст в `token`, и модуль заработает. **Дефект обвязки описан
верно** — классовая обёртка принимает контекст в `**kwargs` (поэтому `bind`
проходит) и пробрасывает его в модульную `analyze`, которая аргумента `context`
не знает; воспроизведено дословно, оба дают
`TypeError: analyze() got an unexpected keyword argument 'context'`.

**Но починка, которую предлагала карточка, ухудшила бы систему, и это измерено
ДО правки.** Профиль подан этим движкам в их собственной форме (`fn(profile)`,
6 пробных протоколов, `RecordingProfile` считает прочитанные и недоданные ключи):

| модуль | что вышло бы | покрытие | недостающее |
|---|---|---|---|
| `..._validator_slashing_exposure_analyzer` | **0.0 на всех шести протоколах** (BLIND) | 0.27 | `num_validators`, `annual_downtime_slash_prob`, `annual_correlated_slash_prob`, `downtime_penalty_pct`, `correlated_penalty_pct`, `operator_concentration_pct`, `restaking_layers` |
| `..._interest_rate_kink_proximity_analyzer` | различает (12.5/0/50/…), но UNCOVERED | 0.30 | `kink_utilization_pct`, `base_rate_pct`, `slope1_pct`, `slope2_pct`, `reserve_factor_pct`, `available_liquidity_usd` |

То есть проводка дала бы: у первого — **константу «риска слашинга нет»** на
каждом протоколе (успокаивающая константа на оси риска, худший вид), у второго —
«близость к кинку», посчитанную относительно **выдуманного кинка 80 %**, потому
что `kink_utilization_pct` молча дефолтится. Ровно тот класс входов, который
#133/#134/#135 измерили и отклонили. Оба, таким образом, принадлежат группе
**(б) — нехватка ФАКТОВ**, а не (а).

Сегодня оба числятся `failed` и в `avg_score` не входят, то есть ни одно
опубликованное число от этого не страдает; страдает ЯРЛЫК — «failed» отправляет
следующего исполнителя чинить код, в котором чинить нечего (ровно урок #136).

**Почему этого не было видно раньше.** Инструмент пригодности
(`audit_tier_c_wiring_feasibility.py`) звал ТОЛЬКО `list`-образные движки, а
`dict`-образные получали `SHAPE_NOT_PROBED` — то есть все пять модулей этой
карточки инструмент структурно не мог измерить. Расширено циклом #137:
из 18 `dict`-модулей Tier-C **wirable = 0** (10 BLIND, 6 RAISES, 1 UNCOVERED,
1 NO_SCORE) — вывод #133 «Tier-C wirable = 0» устоял и на форме, которой он не
видел. Трое «требующих фактов» теперь тоже получают вердикт с ПОИМЁННЫМ списком
недостающего (`unique_users_30d`; `audit_count`, `chain_count`,
`github_commits_90d`, …; `single_block_borrowable_usd`, `price_oracle_type`, …).

## Что остаётся (карточка НЕ закрыта)

Проводить нечего — это измерено. Открытым остаётся **ярлык**: `run_tier_c` не
потребляет разметку покрытия (её потребляет только `run_tier_b`), поэтому пять
модулей продолжают числиться `failed`/`RAISES` вместо честного «нечем сорсить,
вот чего не хватает». Это правка прод-пути аггрегатора с радиусом на все 180
модулей Tier-C, и после урока #136 (аннотация — не гарантия; первая версия
погасила рабочий Tier-A модуль) она делается отдельно, с приёмкой в обе стороны,
а не хвостом к починке инструмента.

*Цикл #137. Замер воспроизводится в sandbox: `python3 scripts/audit_tier_c_wiring_feasibility.py --tier C --out /tmp/feas_c.json`.*

---

## Перемер цикла #138: замер #137 подтверждён точь-в-точь, но у него нашлось продолжение на прод-пути

Сессия #137 (`pid76115`) умерла между «сделано» и пушем — четвёртый раз за
неделю. Её отчёт на веру НЕ принят, а перемерен на двух деревьях с приколотой
sha (`adc7bb2bf`). **Воспроизвелось дословно, ни одно число не разошлось:**

- Tier-C `SHAPE_NOT_PROBED` 41 → 23 = ровно **18** новых измеренных, все
  `call_shape=dict`; переходы 10 BLIND / 6 RAISES / 1 UNCOVERED / 1 NO_SCORE;
  `wirable=0` и до, и после — вывод #133 устоял на форме, которой не видел;
- ни один ранее измеренный модуль вердикта не сменил (проверено поимённо),
  листовое плечо не тронуто: 69 `list`-модулей, 0 изменений записи;
- `..._validator_slashing_exposure_analyzer` → BLIND, покрытие 0.2727, 0.0 на
  всех шести протоколах; `..._interest_rate_kink_proximity_analyzer` →
  UNCOVERED, покрытие 0.30, `kink_utilization_pct` в недостающих. Вывод #137
  «оба принадлежат группе (б), а не (а)» подтверждён.

**Чего #137 не досмотрел — и это оказалось важнее самой починки.** Тот же
инструмент производит `spa_core/analytics/_protocol_key_coverage.py` —
разметку, которую `run_tier_b` потребляет НА ПРОД-ПУТИ. Слепота к `dict`-форме
означала, что разметку 20 модулей (#134/#135) считал инструмент, структурно не
видевший **186 из 479** модулей Tier-B. Перегенерация починенным инструментом:

| | было | стало |
|---|---|---|
| разметка `UNSOURCED_MODULES` | 20 | **35** (+15, все `dict`-образные) |
| `module_status.ok` | 104 | **94** |
| `unsourced` | 20 | **30** |
| confidence aave_v3 | 0.2129 | **0.1921** |
| composite aave_v3 | 33.82 | 35.70 |

Диф разметки **строго аддитивен**: удалено 0, изменено 0 прежних записей
(проверено программно) — направление fail-CLOSED, модули только выбывают из
composite. Среди добавленных: `protocol_defi_yield_bearing_stablecoin_risk_analyzer`
публикует различающийся риск депега (60/60/70/60/70/60), не получая ни
`peg_asset`, ни `days_since_depeg_event` (покрытие 0.1667); и
`protocol_defi_position_health_monitor` — монитор здоровья позиции без
`health_factor` (покрытие 0.1667).

Класс — ровно тот же, что вёл всю неделю (#29/#31/#35–#40, #136): сторож честно
отвечал на СВОЙ вопрос («различается ли не тем ЛИСТОВОЙ движок») и читался как
ответ на нужный.

## Что остаётся открытым (карточка НЕ закрыта)

Ярлык Tier-C: `run_tier_c` по-прежнему не потребляет разметку покрытия, и пять
модулей этой карточки продолжают числиться `failed`/`RAISES` вместо честного
«нечем сорсить, вот чего не хватает». Радиус — все 180 модулей Tier-C; после
урока #136 делается отдельно, с приёмкой в обе стороны.

*Цикл #138. Отдельная находка про сам критерий WIRABLE →
`inbox-verdikt-wirable-poddelyvaem-22-iz-23-pri`.*

---

## Перемер цикла #142 (2026-08-17): пятёрка подтверждена поимённо, ярлык по-прежнему открыт

**Мёртвый захват.** Frontmatter карточки несёт `claimed_by: cycle-87477`,
`claimed_at: 2026-08-06T18:43:13Z` при `status: new` — захват стоит 11 суток, а
сессия мертва. Карточка была ЗАПЕРТА: исполнитель, читающий захват как «кто-то
уже делает», обходил её. Захват сам не снимается; я его НЕ трогал (мутировать
чужой захват по своему усмотрению — не моё решение), работу сделал. Родственная
находка: `agent-card-file-in-ownership-locks-a-card-it-doesnt-claim`.

### Пятёрка воспроизведена дословно

Базовая линия совпала с #136 байт-в-байт: `modules=180 counts={'blind_constant':
9, 'unchecked': 162, 'dormant': 4, 'failed': 5}`, и множество `failed` ПОИМЁННО
равно таблице карточки — те же пять модулей, те же тексты исключений.

*(Побочно замерено: если из дерева выпадает
`spa_core.utils.live_paths.sandboxed_state_path`, двое аналитиков падают
`ImportError` и счётчик становится `failed=7, blind_constant=8`. Счётчик этой
карточки чувствителен к целостности дерева, а не только к модулям, — при
перемере базовую линию надо сверять, а не принимать на веру.)*

### Диагноз (б) подтверждён третьим независимым способом

Инструмент пригодности после правки #142 — контекст-путь стал измеримым, см.
`inbox-25-modulei-poluchili-vechnyi-verdikt-pok` — на Tier-C дал **0 изменений
вердиктов**: `BLIND 44 · NO_ENTRY 70 · RAISES 24 · SHAPE_NOT_PROBED 23 ·
NO_SCORE 16 · UNCOVERED 3 · wirable 0`, до и после. Вывод #133/#137/#138
«Tier-C wirable = 0» устоял и на плече, которого у него раньше не было.
Проводить нечего — теперь это измерено тремя разными вопросами.

Отдельно проверено, что вердикт `blind_constant` на Tier-C означает именно
слепоту, а не потерянную коэрцию (класс `protocol_insurance_scorer`): все девять
константных модулей берут score ОБЪЯВЛЕННЫМ ключом из `_SCORE_KEYS`, и весь их
результат константен, кроме `timestamp` и эха имени. Разбор — в родительской
карточке `inbox-tier-c-171-iz-180-modulei-ne-otvechayut`.

### Что ОСТАЁТСЯ открытым (карточка НЕ закрыта)

Ярлык. `run_tier_c` по-прежнему не потребляет разметку покрытия: ветка
`UNSOURCED_MODULES` есть ровно в `run_tier_b`, а `_tier_c_pass` зовёт все 180
модулей без фильтра (`spa_core/analytics/signal_aggregator.py`). Пять модулей
продолжают числиться `failed`/`RAISES` вместо честного «нечем сорсить, вот чего
не хватает», и следующий исполнитель снова пойдёт чинить код, в котором чинить
нечего.

Почему не сделано в этой итерации: (а) разметки покрытия для Tier-C НЕ
СУЩЕСТВУЕТ — `--emit-markup` поддержан только для Tier B, и завести её значит
добавить в прод-путь второй производный файл; (б) радиус — все 180 модулей
Tier-C; (в) урок #136 (аннотация не гарантия; первая версия разделения погасила
рабочий Tier-A модуль) требует приёмки в обе стороны. Это своя итерация, как
карточка и написала в #137 и #138.

*Цикл #142.*

---

## Цикл #143 (2026-08-17): ярлык ЗАКРЫТ — разметка Tier-C заведена и потребляется

Открытым в этой карточке оставался ровно ярлык: `run_tier_c` не потреблял разметки
покрытия, потому что её **не существовало** (`--emit-markup` был Tier-B-only), и пять
модулей числились `failed` — приглашая следующего исполнителя чинить код, в котором
чинить нечего. Сделано.

### Базовая линия сверена, а не принята на веру

Оба замера воспроизведены в sandbox-копии дерева (живое `data/` не тронуто) и совпали
с #142 дословно:

```
audit_protocol_blindness.py --tier C
  modules=180 counts={'blind_constant': 9, 'unchecked': 162, 'dormant': 4, 'failed': 5}
audit_tier_c_wiring_feasibility.py --tier C
  counts={'BLIND': 44, 'NO_ENTRY': 70, 'RAISES': 24, 'SHAPE_NOT_PROBED': 23,
          'NO_SCORE': 16, 'UNCOVERED': 3}   wirable=0
```

Множество `failed` поимённо равно таблице карточки — те же пять.

### Разметка сшита из ДВУХ отчётов, и это не украшение

Ни один из двух аудитов сам по себе не отвечает на нужный вопрос — ровно класс
#29/#31/#35–#40. Пригодность говорит «чего не хватает», слепота — «даёт ли агрегатор от
модуля число СЕГОДНЯ». Потребляемый набор = пересечение:

1. `blindness == "failed"` ⇒ числа от модуля нет ⇒ не звать его **численно инертно**;
2. недостающие факты названы поимённо (замер покрытия либо список полей из текста
   исключения; обрезанный многоточием список НЕ принимается — назвать «и ещё что-то»
   это не назвать).

Оба условия обязательны, fail-CLOSED. Правило намеренно узкое: широкое («все BLIND»)
погасило бы девять модулей, дающих сегодня публикуемый `avg_score`, то есть разметка
молча изменила бы опубликованное число под видом починки ярлыка. Это решение владельца,
и оно вынесено карточкой `owner-decision-spisat-180-fonovyh-modulei-tier-c-ili-ch`.

Набор вышел равен пятёрке этой карточки — 5 из 5, ни одного лишнего:

| модуль | покрытие | чего не хватает |
|---|---|---|
| `defi_protocol_flash_loan_attack_surface_analyzer` | 0.10 | 9 полей (`single_block_borrowable_usd`, `price_oracle_type`, `audit_count`, …) |
| `protocol_adoption_scorer` | 0.50 | `unique_users_30d` |
| `protocol_defi_interest_rate_kink_proximity_analyzer` | 0.30 | 7 полей (`kink_utilization_pct`, `slope1_pct`, …) |
| `protocol_defi_protocol_maturity_score_analyzer` | не измерено | 11 полей, названных самим движком в исключении |
| `protocol_defi_validator_slashing_exposure_analyzer` | 0.2727 | 8 полей (`num_validators`, `annual_downtime_slash_prob`, …) |

### Что изменилось в проде и что НЕ изменилось

`_tier_c_pass` не исполняет помеченный модуль и пишет `unsourced` с поимённым списком
недостающего вместо `failed`. **Ни `modules_ok`, ни `avg_score` не двигаются** — это не
обещание, а тест в обе стороны: `test_marking_a_scoreless_module_changes_nothing_numerically`
(с пометкой и без — байт-в-байт одно и то же) и отрицательный контроль
`test_marking_a_scoring_module_WOULD_change_the_number`, который воспроизводит аварию
#136 (первая версия разделения погасила рабочий модуль Tier-A) и показывает её цену.
Контрольный (`silent`) прогон статус по-прежнему не пишет.

Радиус остался на пяти названных модулях, а не на 180: `module_count` в `_meta` прежний,
диспозиция знает про все 180 (ничего не удалено).

### Мёртвый захват — по-прежнему висит

`claimed_by: cycle-87477` / `claimed_at` 11-суточной давности при `status: new` я, как и
#142, НЕ трогал: мутировать чужой захват — не решение исполнителя. Карточка
`agent-card-file-in-ownership-locks-a-card-it-doesnt-claim`.

*Цикл #143. Файлы: `spa_core/analytics/_tier_c_key_coverage.py` (сгенерирован),
`scripts/audit_tier_c_wiring_feasibility.py`, `spa_core/analytics/signal_aggregator.py`,
`spa_core/tests/test_tier_c_unsourced_markup.py` (23 теста).*

---

## Цикл #145 (2026-08-17): проверка «а не потерянная ли это коэрсия» — 0 из 5; побочно вскрыт ложный `miscoerced`

Сегодня в агрегаторе появился статус `miscoerced` (`_protocol_blindness.MISCOERCED_MODULES`):
вердикт «слеп» ставился по коэрсированному score, и один модуль был помечен слепым
несправедливо. Вопрос к этой карточке: не того ли класса часть «настоящих отказов».

### Ответ: НИ ОДИН из пяти, и это структурно, а не по совпадению

`_coerce_score` вызывается в `signal_aggregator.py:455` — ПОСЛЕ возврата `_invoke`.
Все пятеро бросают исключение внутри `_invoke` (строка 389), оно всплывает в `except`
на 461 и даёт `failed`. Коэрсировать нечего: результата нет. Замер поимённо, с точкой
разрыва:

| модуль | где рвётся | что именно | вердикт |
|---|---|---|---|
| `protocol_defi_interest_rate_kink_proximity_analyzer` | `…kink_proximity_analyzer.py:755` `return analyze(token, config=self._config, **kwargs)` | `TypeError: analyze() got an unexpected keyword argument 'context'` | дефект обвязки, но отказ ПО ДЕЛУ (см. ниже) |
| `protocol_defi_validator_slashing_exposure_analyzer` | `…slashing_exposure_analyzer.py:780` (та же строка делегирования) | тот же `TypeError` | то же |
| `protocol_adoption_scorer` | `protocol_adoption_scorer.py:53` `float(protocol_data["unique_users_30d"])` | `KeyError: 'unique_users_30d'` | отказ по нехватке факта |
| `defi_protocol_flash_loan_attack_surface_analyzer` | `…flash_loan_attack_surface_analyzer.py:265` (`_validate`) | `ValueError: Missing required keys: [10 полей]` | fail-CLOSED по проекту |
| `protocol_defi_protocol_maturity_score_analyzer` | `…protocol_maturity_score_analyzer.py:94` (`_validate_input`) | `ValueError: Missing required fields: [13 полей]` | fail-CLOSED по проекту |

Закреплено тестом `TestRaisingModuleIsNeverCalledMiscoerced` — утверждение держит проверка,
а не прочтение кода.

### Диагноз (а) опровергнут в ТРЕТИЙ раз, теперь свежим замером

Обвязка у обоих — не «начата и не доведена», а generic-заглушка: `**kwargs` там ради
делегирования, никакого `_pf.generic_profile_for` (как у обвязанных модулей) в ней нет.
Что дала бы проводка контекста в `token`/`position`, перемерено сегодня на шести
протоколах через `generic_profile_for`:

* `slashing_risk_score = 0.0` на ВСЕХ шести — успокаивающая константа «риска слашинга нет»
  (`num_validators`, `annual_downtime_slash_prob`, `…_penalty_pct` молча дефолтятся в 0);
* `kink_proximity_score` = 12.5 / 0 / 50 / 12.5 / 50 / 12.5 — различает, но
  `kink_utilization_pct = 80.0` ОДИНАКОВ у всех: это дефолт движка, а не факт, то есть
  «близость к кинку» считается относительно выдуманного кинка.

Проводка превратила бы честный отказ в тихий успех. НЕ сделано — и не должно быть сделано.
Все пять остаются в `UNSOURCED_MODULES`; изменений в прод-пути агрегатора нет.

### Что ПОЧИНЕНО: ложный `miscoerced` на грубых часах

Прогон сегодняшним (miscoerced-осведомлённым) инструментом дал на Tier-C
`counts={'blind_constant': 8, …, 'failed': 5, 'miscoerced': 1}` — то есть на один
`blind_constant` меньше, чем базовая линия #142/#143. Виновник —
`defi_protocol_sandwich_attack_exposure_analyzer`, и его `differing_paths` = `['.timestamp']`.
Результат модуля константен для любого протокола во всём, кроме собственной отметки времени.

Отсев дрожи в аудите построен на повторе одного протокола и предполагает, что часы дрожат.
Отметка секундной точности (`2026-08-17T22:43:34Z`) внутри одной секунды у обоих повторов
ОДИНАКОВА → путь признан стабильным, а trio- и wide-прогон разнесены секундами → путь
«различается». Слепой модуль получал ярлык «зряч, чините коэрсию», причём вердикт зависел от
того, в какую секунду лёг прогон. Существующий сторож `test_clock_field_is_not_a_measurement`
это не ловил: его фикстура штампует микросекунды и потому дрожит всегда — проверка не видела
настоящей поломки.

Починка (`scripts/audit_protocol_blindness.py`): листовое ЗНАЧЕНИЕ-отметка СОБСТВЕННЫХ часов
(ISO-строка в пределах `CLOCK_WINDOW_S = 300 c` от момента снятия отпечатка) маскируется, как
маскируется эхо имени протокола. Маска намеренно узкая: только строки и только «сейчас» —
дата аудита протокола или срок разлока отстоят от «сейчас» на месяцы и остаются РАЗЛИЧИЕМ;
число не маскируется вовсе (эпоха неотличима от измерения без угадывания). Часы — вход
(`_fingerprint(..., now=)`), а не окружение.

После починки Tier-C воспроизводит базовую линию #142/#143 дословно:
`modules=180 counts={'blind_constant': 9, 'unchecked': 162, 'dormant': 4, 'failed': 5}`,
`miscoerced=0`.

**Прод-разметка НЕ перегенерировалась и не изменилась.** Маска умеет только СНИМАТЬ различие,
то есть `MISCOERCED_MODULES` может лишь сжаться; в файле ровно одна запись
(`defi_cross_chain_yield_comparator`), её различия — `.detail.gross_apy_pct` и родня, не
отметки времени. Перепроверено адресным прогоном этого модуля починенным инструментом: вердикт
`miscoerced`, пути те же.

### Контроль в обе стороны (мутационный)

* снять маску часов (`_is_wall_clock_now → False`) → краснеют 3 теста, включая
  `test_coarse_clock_field_is_not_a_measurement`; `test_domain_date_stays_a_measurement` зелёный;
* расширить маску до любой ISO-строки (`→ True`) → краснеет `test_domain_date_stays_a_measurement`
  (дата аудита перестала быть измерением) и обе границы окна; грубые часы зелёные.

*Цикл #145. Файлы: `scripts/audit_protocol_blindness.py`,
`spa_core/tests/test_protocol_blindness_miscoercion.py` (13 тестов). Мёртвый захват
`claimed_by: cycle-87477` не трогал — как #142 и #143.*
