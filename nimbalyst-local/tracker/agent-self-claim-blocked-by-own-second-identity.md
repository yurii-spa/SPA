---
trackerStatus:
  type: agent-task
title: Сессия не может взять СВОЮ карточку, если объявилась под двумя идентификаторами — каждый блокирует другой
status: done
priority: medium
source: session-2026-08-01-cycle67
created: 2026-08-01
---

## Как найдено

Найдено догфудом цикла #67: задача цикла была другая
(`agent-fresh-weak-mention-deadlocks-queue`), а этот дефект вскрылся при попытке взять
собственную только что созданную карточку.

## Что измерено (воспроизводится дословно)

`SPA_SESSION_ID` в ИНТЕРАКТИВНОМ прогоне не выставлен (его ставит только обёртка
`scripts/agent_orchestrator.sh`), поэтому `log_session_change.py` записывает идентификатор
ОДНОКРАТНОГО CLI-процесса. Первое объявление ушло под `pid72203`, второе — под `cycle67`
(после того как я выставил `SPA_SESSION_ID` вручную). Обе записи — мои, обе несут СИЛЬНЫЙ
признак (поле `card:`), обе свежие. Дальше:

```
$ check_card_claim.py claim <карточка> --session cycle67
ОТКАЗ: вердикт `claimed` — карточка не взята.
  - [свежий] pid72203 … поле `card:` в объявлении [сильный признак]     ← это тоже Я

$ check_card_claim.py claim <карточка> --session pid72203
ОТКАЗ: вердикт `claimed` — карточка не взята.
  - [свежий] cycle67  … поле `card:` в объявлении [сильный признак]     ← и это Я
```

Круговая блокировка: `self_session` — одно значение, поэтому «своей» признаётся ровно одна из
двух записей, а вторая читается как чужой захват. Выхода флагом нет: `claim` не имеет `--force`
(в отличие от `release`), а снять чужое объявление нельзя по построению.

**Радиус.** Бьёт по КАЖДОЙ интерактивной сессии, где `SPA_SESSION_ID` не выставлен заранее, —
достаточно объявиться дважды под разными id. Автономные циклы под обёрткой не затронуты (у них
один `SPA_SESSION_ID` на весь цикл). Это остаточная форма `agent-durable-session-id` (закрыта):
там чинили «активность не измерить», здесь — «идентичность не одна».

**Чем это НЕ является.** Работа не теряется и владение не пропадает: объявление в общем журнале
— авторитетный механизм (цикл #54/#56), и цикл #67 отработал под ним нормально. Не пишется
только `claimed_by` во frontmatter ⇒ карточка не попадает в раздел `🔒 ЗАНЯТЫ СЕССИЯМИ` доски.

## Гипотезы решения (ни одна не реализована)

1. **Множественная идентичность:** `--session` принимает несколько значений (или `check` считает
   «своими» все id, встречающиеся в записях с тем же `session_pid`). Требует, чтобы у записей был
   общий якорь — сегодня у pid-однократных записей его нет.
2. **Не плодить идентичности:** обёртка/протокол требуют выставить `SPA_SESSION_ID` ДО первого
   объявления, а `log_session_change.py` предупреждает, если его нет. Дёшево, но не чинит уже
   сделанные записи.
3. **`claim --force <причина>`** с обязательной записью причины в журнал — симметрично `release
   --force`. Риск: даёт способ обойти настоящую находку, поэтому причина обязана быть в журнале.

## Acceptance criteria

- сессия, объявившаяся под двумя своими id, может взять свою карточку, и это пиннится тестом;
- чужой свежий сильный захват по-прежнему НЕ обходится (положительный контроль);
- инвариант #16: ни один существующий ассерт не ослаблен молча.

**Не входит:** RiskPolicy / kill-switch / живой трек / launchd / `landing/**`.

## Независимое подтверждение (цикл #70, 2026-08-01) — воспроизвелось само, без попытки

Цикл #70 поднимал сироту #67 и наткнулся на этот же дефект в ЧИСТОМ виде, не воспроизводя его
намеренно: захват и снятие захвата — **две разные CLI-команды**, а идентификатор сессии в
интерактивном прогоне выводится из pid однократного процесса. Дословно:

```
$ check_card_claim.py claim   agent-fresh-weak-mention-deadlocks-queue
взята: … → pid15267
$ check_card_claim.py release agent-fresh-weak-mention-deadlocks-queue
ОТКАЗ: карточку держит pid15267, а не pid17106; снять чужой захват можно только с --force
```

Обе строки — одна и та же сессия с разницей в минуты. **Радиус шире, чем описано выше:** двух
объявлений не требуется, достаточно двух вызовов инструмента — то есть штатной пары
`claim` … `release`, которую предписывает протокол. Обход существует (`release --force`) и был
применён, но он же снимает и НАСТОЯЩИЙ чужой захват, поэтому «просто пользоваться `--force`» —
не решение, а обесценивание проверки. Гипотеза 2 (выставлять `SPA_SESSION_ID` до первого вызова)
этот случай закрывает целиком и остаётся самой дешёвой.

---

## ✅ ЗАКРЫТО циклом #72 (2026-08-01)

**Что сделано.** Личность сессии перестала быть ЯРЛЫКОМ и стала ИЗМЕРЕНИЕМ. «Моя ли это
запись» решает совпадение долгоживящего процесса — пары (`session_pid`, `session_pid_start`),
которую `log_session_change.durable_process` пишет только после того, как процесс подтверждён,
вместе с его временем старта. Две записи с одинаковым якорем написаны одним процессом, поэтому
их ярлыки — мои, сколько бы их ни было. Новое в `scripts/check_card_claim.py`: `anchor_of`
(пара из записи; **обе** части обязательны), `measure_self_anchor` (свой якорь тем же кодом,
что пишет его в журнал, — без близнеца арифметики), `self_identities` (все мои ярлыки).
Применено в ТРЁХ местах сразу, потому что дефект один: захват (`_classify`), пересечение по
файлам и `claim`/`release` (иначе собственное второе объявление, не блокируя как захват,
блокировало бы как пересечение по файлам — «починить одного близнеца из двух»).

**Выбрана гипотеза 1, а не 3.** `claim --force` (гипотеза 3) отвергнута: она даёт способ
обойти НАСТОЯЩУЮ находку, то есть чинит симптом ценой самой проверки. Гипотеза 2 (не плодить
идентичности) — **уже реализована на origin циклом #63** (`agent_orchestrator.sh` экспортирует
`SPA_SESSION_PID=$$` и `SPA_SESSION_ID`), это проверено, а не предположено, и поэтому здесь НЕ
делалась повторно. Но она не сработала ни разу: **запущенная на маке копия обёртки отстаёт от
GitHub на 345 коммитов** (`git log -1 -- scripts/agent_orchestrator.sh`: хост `1e52fffef`
против origin `d1cd39215`), в окружении живого процесса `SPA_SESSION_*` нет — это ровно та
причина, по которой ярлыки циклов #67–#70 не содержали pid. Отдельная карточка владельца об
отставшей копии уже открыта (`owner-decision-rabochaya-kopiya-proekta-na-make-otstala`).
Профилактика усилена дешёвой частью: `log_session_change.py` теперь ПРЕДУПРЕЖДАЕТ на stderr,
когда долгоживущий процесс не объявлен (раньше молчал).

**Acceptance criteria — все три закрыты:**

- [x] сессия под двумя своими ярлыками берёт свою карточку — пиннится
      `test_my_own_second_label_is_not_a_claim`, `test_claim_takes_over_from_my_other_label`,
      `test_release_recognises_the_claim_of_my_other_label` (последний — дословный случай #70:
      взял `pid15267`, снял `pid17106`);
- [x] чужой свежий сильный захват НЕ обходится — 6 положительных контролей: чужой якорь ·
      **тот же pid с другим временем старта** (переиспользованный номер — не я) · запись без
      якоря · половина якоря (`session_pid` без старта) · `claim` не перезаписывает чужого
      держателя · `release` по-прежнему требует `--force`;
- [x] инвариант #16 — механически: диф теста **262 добавления / 2 удаления**, обе удалённые
      строки — сигнатура и вызов тестового помощника `run()` (добавлен параметр `self_anchor`,
      умолчание `None` делает существующие проверки СТРОЖЕ — герметичными относительно
      окружения). Ни один ассерт не тронут.

**Проверка.** Целевой файл **113 passed** (91 → 113, ровно +22); на чистом `origin/main` новый
класс **22/22 красных**; **8 мутационных контролей — каждый краснит РОВНО свою проверку**
(первая версия контроля M7 не сработала: подставной читатель журнала не вызывался, потому что
файла не существовало, — починен тест, а не ассерт); смежные сюиты (журнал объявлений, шаг 0a,
очередь) **193 passed**; `--collect-only` 91 209 → 91 231 = **ровно +22**; mypy на обоих
изменённых файлах чисто и до, и после; `lint_llm_forbidden` 163/0.

**Радиус измерен на ЖИВЫХ 113 карточках, а не предположен:** без объявленного долгоживящего
процесса вердикт, список захватов и пересечений **побайтово совпадает с origin — 0 расхождений
на 113 карточках** (то есть для всего существующего журнала поведение не изменилось); с моим
якорем сдвинулась **ровно одна строка** — моё же второе объявление перестало быть захватом моей
же карточки. **Живой догфуд на настоящем журнале:** объявился вторым ярлыком без
`SPA_SESSION_ID` (`pid48394`, тот же процесс `pid41721`) — код origin читает эту запись как
чужой захват с ПОДТВЕРЖДЁННО ЖИВОЙ сессией (жёсткий блок), код #72 опознаёт её как свою и
называет основание в отчёте.

**НЕ делал:** RiskPolicy / kill-switch / пороги / гейты / живой трек / launchd / деплой не
трогал; `landing/**` в цикле не было ⇒ `safe_site_push.py` не требовался; `data/**` не
публиковал; обёртку `agent_orchestrator.sh` не менял (её правка уже на origin).

**Найдено сверх задачи → карточка `agent-card-file-in-ownership-locks-a-card-it-doesnt-claim`:**
шаг 0b заблокировал эту самую карточку объявлением цикла #70, у которого в поле `card:` стоит
ДРУГАЯ карточка — он лишь дописал сюда раздел «Независимое подтверждение». Молча не чинил:
это сужение СИЛЬНОГО признака, а у владельца уже открыт вопрос ровно про такое сужение
(`owner-decision-otchet-o-zanyatosti-kartochki-bolshe-ne-sc`).
