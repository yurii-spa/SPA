---
trackerStatus:
  type: owner-decision
title: "Твой Obsidian показывает состояние проекта недельной давности — одна минута на починку"
status: needs-owner
source: agent
created: 2026-09-09
tags: [obsidian, docs, adr-152, mirror]
---

## Что случилось и почему это важно

Ты читаешь состояние проекта из папки `~/Documents/SPA_Claude/docs/`. Замер 09.09: файл
`docs/STATE.md` в этой папке — **от 2 сентября** и расходится с актуальным на **100 строк**;
всего между этой папкой и актуальной версией различаются **253 файла**.

Причина штатная: работа уезжает на GitHub напрямую, а эта папка обновляется только в части кода —
документы туда не возятся сознательно (иначе синхронизация затирала бы локальные правки).

При этом **актуальная копия уже есть на диске** и обновляется сама каждые полчаса:
`~/Documents/SPA_mirror`. Замер: её `STATE.md` — сегодняшний, 04:28.

## Что от тебя нужно

- **Вариант 1 (рекомендация агента) — переключить Obsidian на зеркало.**
  Шаг 1: в Obsidian открыть хранилище `~/Documents/SPA_mirror` вместо `~/Documents/SPA_Claude`.
  Шаг 2: сказать мне «переключил».
  Почему так: ноль изменений в системе, читаешь свежее, и зеркало нельзя случайно испортить —
  оно только читается.
  Оговорка: заметки, которые ты сам писал внутри `SPA_Claude/docs/`, останутся в старой папке;
  скажи, если нужно перенести — агент перенесёт их в зеркало-совместимое место.
- **Вариант 2 — возить документы в рабочую папку.**
  Агент дополняет синхронизацию тремя каталогами (`docs/STATE.md`, `docs/journal/`,
  `docs/decisions/`). Риск назван: если ты правишь эти файлы руками в этой папке, синхронизация
  их затрёт. Именно поэтому документы там и не возятся.
- **Вариант 3 — оставить как есть.**
  Тогда при чтении состояния держи в голове: то, что ты видишь, старше недели.

## Как понять, что готово

Ты нажал вариант 1, 2 или 3.

## Что будет после

Вариант 1 — ничего делать не нужно, агент только запишет решение. Вариант 2 — правка скрипта
синхронизации с приёмкой и тестом, что локальные правки не теряются. Вариант 3 — агент добавит
в утренний отчёт строку «состояние в твоей папке старше N дней», чтобы это не забывалось.
