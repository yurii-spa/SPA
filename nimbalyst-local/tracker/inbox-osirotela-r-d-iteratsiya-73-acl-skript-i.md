---
trackerStatus:
  type: inbox
title: "Осиротела R&D-итерация #73 ACL: скрипт и тест лежат в /private/tmp/spa_rnd73, замеров и записи реестра нет — сессия умерла ПОСРЕДИ работы"
status: done
source: nimbalyst
created: 2026-08-23
---

## Что измерено (цикл #351, финальный шаг 0a, 2026-08-23T02:16Z)

Сессия `rnd-83461` объявила R&D-итерацию **#73 ACL** («закон стоимости непрерывного сайзера по
уровню агрегации» — ПРЯМОЙ заказ записи #71/#72, названный в `docs/STATE.md`) и **#74**
(ex-ante предиктор стоимости), после чего умерла. `ps -p 83461` — мёртв. Шаг 0a показал её
разделом «🕳 осиротело, но окно не истекло».

**Что физически есть** (`/private/tmp/spa_rnd73`, дерево живо):

| Путь | Размер | Время правки |
|---|---|---|
| `scripts/edge_aggregation_cost_law.py` | 45 279 Б | 04:15 (локальн.) |
| `spa_core/tests/test_edge_aggregation_cost_law.py` | 26 284 Б | 04:14 (локальн.) |

**Чего НЕТ, и это главное:** сессия умерла ПОСРЕДИ работы, а не между работой и пушем. Файлы
правлены в 04:14–04:15, объявление сделано в 04:02 — то есть последняя запись легла за минуту-две
до смерти. В дереве `spa_rnd73` **нет ни записи реестра** (`docs/DYNAMIC_LEVERAGE_GUARDIAN.md`),
**ни журнала**, **ни результатов прогона**. Это не «доделанное, не доставленное» — это
недоделанное.

## Почему я не поднял это сам

Подъём здесь — не перенос байтов, а ПОВТОРЕНИЕ R&D-итерации: прогнать замер, вынести вердикт,
завести запись реестра. Отчёту мёртвой сессии верить нельзя по протоколу, а отчёта и нет вовсе.
Цикл #351 уже вёл две задачи и доставил обе; браться третьей за чужую незаконченную работу без
её замеров — ровно тот способ, которым в проект попадают числа, которых никто не мерил.

## Что сделать

1. Прогнать `scripts/edge_aggregation_cost_law.py` СВОИМ прогоном (не веря отчёту, которого нет).
2. Прогнать `spa_core/tests/test_edge_aggregation_cost_law.py`; сверить с контролем на чистом
   `origin/main` того же sha.
3. Вынести вердикт по идее #73 (и #74, если она в том же файле) и завести запись реестра в
   `docs/DYNAMIC_LEVERAGE_GUARDIAN.md` — иначе скрипт попадёт под храповик неподключённых.
4. Если вердикт отрицательный — записать ЭТО (отрицательный результат #58–#72 — норма ветки).

## Как понять, что готово

Либо запись реестра существует и скрипт на `origin/main`, либо в журнале написано, почему работа
списана. Дерево `/private/tmp/spa_rnd73` снимается только квитанцией уборщика, не раньше.

---

## Разобрано циклом #353 (2026-08-23) — работа поднята, ОДНО утверждение опровергнуто

**Карточка была права по факту и неточна по диагнозу.** Она измеряла дерево `spa_rnd73` в 02:16Z
и заключила «это недоделанное»: нет записи реестра, нет журнала, нет результатов прогона. К тому
моменту так и было. Но после неё в том же дереве работала ВТОРАЯ сессия (`pid95478`, объявление
02:21Z: «R&D #73/#74 ЖИВА и ЗАВЕРШЕНА»), и она успела дописать и запись реестра (242 строки), и
тест-файл (26 284 → 27 130 Б, правка 04:22 против 04:14 в замере карточки). Она тоже умерла до
пуша. Диагноз в карточке — гипотеза, а не факт; рецидив урока #175/#235.

**Отчёту ни одной из двух мёртвых сессий не верил — всё перемерено своим прогоном:**

1. `scripts/edge_aggregation_cost_law.py` прогнан на ЖИВОЙ панели
   (`SPA_PANEL_DIR=<прод>/data/aggressive_lab`; без него панель не git-tracked и прогон молча
   уехал бы на фикстуру). Все опубликованные числа воспроизвелись: лестница 71.7 → 96.6 →
   268.9 → 592.2 бп/год, residency 1.1 → 3.0 → 13.3 → 48.1 %, контроль «без `eth_directional`
   клин ИНЕРТЕН» — 0.0 % в полосе, 0.0 бп.
2. `spa_core/tests/test_edge_aggregation_cost_law.py`: **43 passed как поднято**, 46 после
   моих трёх. Пропуски честные и названные: без `SPA_PANEL_DIR` три теста на живой панели
   пропускаются с причиной в тексте, а не молчат (40 passed, 3 skipped).
3. **Приёмка мутациями, а не прогоном:** снятие масштабирования издержек по доле капитала
   (`cost/len(groups)` → `cost`) красит 5 тестов, включая парити с опубликованными ячейками #71;
   ослепление liveness к флагу `killed` красит оба теста живости. Тесты — не украшение.

**Вердикт по идеям вынесен и записан в реестр** (`docs/DYNAMIC_LEVERAGE_GUARDIAN.md`):
**#73 — частично** (лестница агрегации заполнена и механизм подтверждён числом; правилом «до
такого-то размера корзины можно» НЕ стала — у агрегации два противоположных отказа без середины);
**#74 — честный негатив** (ex-ante прокси ρ=0.771 проигрывает бесплатному контролю «размер
корзины» ρ=0.869, и знак его ошибки меняется с формой ряда). Отрицательный результат — норма
этой ветки.

**Что СВЕРХ поднятой работы (и ради чего стоило не верить на слово):**

- Черновая карточка владельцу `own-2026-08-23-mertvye-knigi-v-issledovatelskoi-paneli` из того же
  дерева **НЕ доставлена**: шаг 1a даёт вердикт DONE — вопрос уже задан карточкой `own-54` и
  **отвечен владельцем 19.08 (вариант 1)**. Доставка была бы вторым «нужно твоё решение» на
  отвеченный вопрос, да ещё с рекомендацией варианта 2 против выбранного — ровно шторм повторов
  из ADR-084.
- Запись реестра ссылалась на эту карточку как на заведённую — **утверждение исправлено в самой
  записи**, а не оставлено висеть.
- Измерена ПРИЧИНА смерти книг, которой не было ни у `own-54`, ни у первой редакции записи:
  трёх из четырёх убила настоящая просадка при ЖИВОМ фиде (−29.7 / −19.6 / −5.4 % в день kill'а),
  фид пропал ровно у `lp_eth_stable`, **возвратов в строй ноль у всех четырёх**. Отсюда:
  принятая владельцем починка фидов достаёт до 1 книги из 4 и не оживляет ни одной, потому что
  узкое место — отсутствующее re-arm-правило лаборатории. Дописано в
  `agent-ozhivit-chetyre-mertvye-knigi-paneli`, +3 теста, +3 мутации.

**Дерево `/private/tmp/spa_rnd73` не снимаю** — уборщиком с квитанцией, как велит карточка.
