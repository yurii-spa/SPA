---
trackerStatus:
  type: inbox
title: "AI1 (книга владельца): три внедрения доставлены — экономика цеха, паспорта агентов, стандарт отчёта; циклам — заполнить паспорта и подключить генератор"
status: new
source: cloud-сессия владельца (мандат 2026-08-20; разбор книги AI1 Гамзатова)
created: 2026-08-20
priority: high
---

## Доставлено этим коммитом

1. **Экономика цеха** (AI1 гл.19) — `spa_core/monitoring/fleet_economics.py`:
   циклы/коммиты за 24ч по git-истории + оценка стоимости
   (`SPA_COST_PER_CYCLE_USD`, owner-tunable). Расход подписки перестаёт быть
   невидимым (инцидент 52 цикла/сутки 08.08 заметили случайно).
2. **Паспорта агентов** (гл.3/24) — `spa_core/monitoring/agent_passports.py`:
   полнота блока `passport {goal, quality_metric, escalation}` в
   `architecture/manifest.json`; «агент без паспорта = непонятный бот».
3. **Стандарт дневного отчёта** (гл.10) — в дайджесте новая секция «⚙️ Цех /
   🪪 Паспорта» + блок «📋 Стандарт отчёта»: отчёт САМ называет отсутствующие
   обязательные блоки (в т.ч. «3 трека» — живой вопрос владельца 19.08 теперь
   кричит из отчёта каждый день до закрытия).

Приёмка: +11 тестов (offline, git/манифест инжектируются); дайджест-набор
672 = 672 против чистого origin — ноль регрессий.

## Циклам (под мандатом)

1. Заполнить `passport` всем агентам манифеста (владелец увидит прогресс в
   дайджесте X/38); цель и метрику брать из docstring агента/ADR, эскалацию —
   из push_policy.
2. `fleet_economics --run` + `agent_passports --run` — в ежедневный цикл (иначе
   дайджест считает inline, что тоже работает, но артефакт даёт историю).
3. Владельцу задать `SPA_COST_PER_CYCLE_USD` в env цикла (средняя цена
   headless-сессии) — до этого строка честно пишет «стоимость не оценена».
