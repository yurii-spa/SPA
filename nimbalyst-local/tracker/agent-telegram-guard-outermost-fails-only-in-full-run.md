---
trackerStatus:
  type: agent-task
title: Последнее падение на main — страж сети требует ПУСТОЙ журнал попыток, а к его очереди накоплено 2267 чужих записей
status: backlog
priority: high
source: session-2026-08-03-cycle96
created: 2026-08-03
---

## Почему карточка заводится именно сейчас

Цикл #95 этот дефект **точно диагностировал**, но карточку на него не завёл — он записал разбор
в тело уже закрытой карточки `agent-task-ci-lite-krasnyi-s-02-08-mypy-na-allocato` и в журнал
`2026-W32`. Пока падений было три, оно тонуло в списке. **Цикл #96 убрал два других**
(`agent-spark-apy-tests-assert-a-fallback-adr-063-removed`), и теперь это —
**ЕДИНСТВЕННОЕ** падение полного среза, то есть единственная причина, по которой `SPA Tests`
и `SPA CI` на `main` остаются красными. У единственного блокера должна быть своя карточка.

## Что измерено (цикл #96, два полных прогона по 15 мин 41 с)

```
чистый origin/main (контроль /tmp/spa_wt_c96_ctl):
  3 failed, 91158 passed, 576 skipped, 1 error
  FAILED …test_e2e_integration…::test_spark_get_apy_pct_positive       ← починено #96
  FAILED …test_e2e_integration…::test_spark_get_apy_pct_returns_float  ← починено #96
  FAILED …test_no_live_network_in_tests…::test_telegram_guard_stays_outermost

worktree #96:
  1 failed, 91162 passed, 576 skipped, 1 error
  FAILED …test_no_live_network_in_tests…::test_telegram_guard_stays_outermost
```

**В изоляции тест ЗЕЛЁНЫЙ:** `pytest spa_core/tests/test_no_live_network_in_tests.py -q`
→ `15 passed`. Краснеет он **только** в полном прогоне ⇒ это зависимость от состояния соседей,
а не дефект самого файла.

Механизм (замер цикла #95, здесь подтверждён воспроизведением на контроле): тест требует
`_live_net.attempts() == []` — то есть ПУСТОЙ глобальный журнал сетевых попыток. К моменту его
запуска в полном срезе там накоплено **2267** записей от более ранних тестов; первая —
`urlopen https://yields.llama.fi/pools`.

## Почему это не «просто почистить журнал перед тестом»

Осторожно: тест — **страж** (инвариант «тесты не ходят в живую сеть», карточки
`agent-tests-send-live-telegram-alerts`, `agent-gap-monitor-test-still-reaches-live-telegram`,
`agent-tests-do-live-network-io`). Обнулить журнал в `setUp`, чтобы «покрасить зелёным», —
это ослабить стража: 2267 записей означают, что **живые сетевые попытки в наборе реально есть**,
и страж как раз про это. Инвариант #16 применяется в полную силу.

Развилка (решать замером, не вкусом):
* либо утверждение стража «журнал пуст» относится к ЕГО области, и тогда он должен мерить
  дельту в своём окне (`attempts()` до/после), а не глобальный кумулятив — тогда правка честная,
  и она НЕ прячет чужие сетевые походы;
* либо утверждение верное и глобальное, и тогда красный тест указывает на 2267 настоящих
  нарушений — их надо назвать (кто именно ходит в сеть) и чинить их, а не тест.

## Acceptance criteria

- измерено, КТО делает эти 2267 попыток (сгруппировать `attempts()` по тесту/URL) — список в теле
  карточки, а не «много разных»;
- выбранная ветка развилки обоснована замером; если правится тест — он обязан **краснеть**,
  когда в его собственном окне действительно происходит сетевой вызов (положительный контроль
  мутацией, а не на слово);
- ни один существующий ассерт не ослаблен молча (инв. #16) + запись в `docs/journal/<неделя>.md`;
- проверка полным срезом `spa_core/tests/` с контролем на чистом `origin/main` (дельта названа).

**Не входит:** RiskPolicy / kill-switch / живой трек / launchd / `landing/**`.
