---
trackerStatus:
  type: agent-task
title: После починки протокольной слепоты аналитики 4 теста tier-сигналов красные на main — CI снова красный
status: backlog
priority: high
source: cycle-89
created: 2026-08-02
---

**Найдено попутно** (цикл #89 гонял `tests/` перед пушем восстановления общей памяти; задача цикла
была другая). **Не чинил здесь же намеренно:** причинный коммит приземлился ~30 минут назад, его
сессия может быть ещё жива — а сегодня уже дважды две сессии независимо делали одну работу
(#87/#88 и #89/параллельная). Карточка вместо третьего столкновения.

## Что измерено

На **чистом чекауте `origin/main` `19666683a`** (никаких своих правок, `git status` пуст):

```
python3 -m pytest tests/test_signal_aggregator.py -q
4 failed, 8 passed

FAILED tests/test_signal_aggregator.py::test_tier_a_blocks_high_risk_protocol
FAILED tests/test_signal_aggregator.py::test_tier_a_warns_on_mid_risk
FAILED tests/test_signal_aggregator.py::test_tier_b_increases_multiplier_for_low_risk
FAILED tests/test_signal_aggregator.py::test_tier_b_high_risk_lowers_multiplier
```

Характерное падение: `assert 1.0 < 1.0` (`tests/test_signal_aggregator.py:129`) — то есть
множитель Tier-B перестал двигаться и всегда равен нейтральной единице.

**Причина — сам смысл починки, а не её дефект.** Коммит `19666683a` («Tier-B protocol-blindness
differential audit») установил, что все 150 «ok»-модулей Tier-B выдавали одинаковый скор даже для
НЕСУЩЕСТВУЮЩЕГО контрольного протокола, и теперь слепые модули исключаются из композита и из
знаменателя уверенности ⇒ Tier-B честно нейтрален (50.0 / 1.0 / conf 0). Тесты же написаны под
СТАРОЕ поведение: они требуют, чтобы множитель отклонялся от 1.0 в зависимости от риска.

## Почему это не «просто поправить тесты»

Инвариант #16: молча ослаблять/переписывать проверку нельзя. Здесь ровно та развилка, ради которой
он существует:

- если Tier-B честно нейтрален **навсегда** (слепые модули исключены, живых не осталось) — тесты
  проверяют поведение, которого больше нет по решению, и их надо ПЕРЕПИСАТЬ под новый контракт
  («слепой модуль не двигает множитель») с явным обоснованием в теле правки + записью в журнал;
- если нейтральность **временная** (пока модули не станут протокол-зависимыми) — тесты правы, а
  красный CI честно сигналит, что аналитический слой сейчас ничего не измеряет, и правильный ход —
  пометить их `xfail` с причиной и сроком, а не переписать.

Выбор между этими двумя — за автором `19666683a` (у него измерения) либо за владельцем.

## Что сделать

1. Автору `19666683a`: решить, нейтральность постоянная или временная, и привести тесты в
   соответствие с обоснованием (инвариант #16 — обязательна запись в `docs/journal/<неделя>.md`).
2. Проверить положительным контролем: после правки мутация «вернуть слепому модулю влияние на
   композит» обязана краснить ровно эти проверки.

## Критерий «сделано»

`tests/test_signal_aggregator.py` зелёный на чистом чекауте `origin/main`, CI на `main` зелёный,
и в журнале записано, КАКОЙ контракт Tier-B теперь проверяется и почему.

Смежное: `docs/ANALYTICS_PROTOCOL_BLINDNESS.md`, память
`analytics-signal-layer-protocol-blind` (advisory-слой, money-path не задет — ни один из этих
тестов не гейтит исполнение).
