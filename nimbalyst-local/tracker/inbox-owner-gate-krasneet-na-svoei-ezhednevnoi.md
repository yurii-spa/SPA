---
trackerStatus:
  type: inbox
title: "owner-gate краснеет на СВОЕЙ ежедневной автоматике: сдвиг снимка трека на один цент = owner-gated"
status: done
source: nimbalyst
created: 2026-08-13
claimed_by: cycle-299-pid65591
claimed_at: 2026-08-19T06:05:24Z
---

## Что измерено (цикл #221, 2026-08-13)

Воркфлоу **`owner-gate`** на `main` покраснел на коммите `575e504a7`
(`chore(site-custodian): auto-deploy fresh track_snapshot after daily cycle`):

```
[B] landing/src/data/track_snapshot.json:0 snapshot.number () — end_equity: 100863.44 → 100863.45
[B] landing/src/data/track_snapshot.json:0 snapshot.number () — nav_usd:    100863.44 → 100863.45
RESULT: GATED — 2 owner-gated change(s) → route to owner card.   (exit 2)
```

То есть красноту вызвал сдвиг публикуемого числа на **ОДИН ЦЕНТ**, сделанный собственной
ежедневной автоматикой Site Custodian (ADR-YL-011). Предыдущие прогоны `owner-gate` были зелёные
(`0da42d81`, `25dc02ac`), так что это не давняя болезнь, а свежий рецидив.

**Почему это важно, а не косметика.** Снимок трека обновляется КАЖДЫЙ день после цикла, и любое
изменение равновесия книги двигает `end_equity`/`nav_usd`. Значит `owner-gate` будет краснеть на
`main` каждый день — а сторож, который краснеет каждый день на честной работе, перестаёт что-либо
означать ровно к тому моменту, когда покраснеет на настоящем нарушении. Это уже знакомый нам класс
«сторож зовёт владельца на успешно отработавшую автоматику»: гасить надо МАРШРУТ, а не проверку.

**Чего я НЕ делал.** Проверку не ослаблял и не выключал (инв. #16), число на сайте не трогал.
Сам факт, что коммит уже лежит в `main`, означает, что гейт здесь работает ПОСТ-ФАКТУМ (жалуется
в CI), а не как предпусковой замок — но это **гипотеза о канале доставки custodian**, отдельным
замером в этот цикл не проверенная.

## Что сделать

1. Измерить, каким путём Site Custodian публикует снимок: идёт ли он через `safe_site_push.py`
   (тогда почему гейт не завернул его в карточку ДО пуша) или мимо.
2. Развести два разных события: «автоматика обновила уже одобренное число» и «кто-то ввёл НОВОЕ
   число/нейминг/legal». Первое — штатный ежедневный шум, второе — настоящая тревога.
3. Решение о том, что именно из чисел трека вправе уезжать без владельца, — **owner-gated**
   (ADR-078 прямо оставляет числа/тиры/legal владельцу). Техническую часть (разделение классов,
   маршрут) можно готовить автономно, включать — с ответом владельца.
4. Закрепить положительным контролем в обе стороны: ежедневный цент — не тревога; подмена
   наименования тира / нового APY — тревога.

## Границы

Числа доходности, нейминг тиров и legal на сайте не трогать. Проверку не глушить.

Найдено попутно циклом #221 (карточка `inbox-test-agent-template-code-sync-krasneet-t`).

---

## Исполнено циклом #299 (2026-08-19)

**П.1 карточки ИЗМЕРЕН, и гипотеза карточки не подтвердилась.** Custodian публикует снимок
**ЧЕРЕЗ `safe_site_push.py`** (`deploy_site_snapshot.py:127`), и гейт его не завернул **потому
что там он ЧЕСТНО прошёл**: в рабочем режиме освобождение вычисляется — регенерация из живого
канона совпадает ⇒ EXEMPT ⇒ пуш законен. Вопрос карточки «или мимо?» снят: мимо не идёт.

**Настоящая причина — в другом месте, и она одна строка.** `check_owner_gate.py:498`:

```python
exempt = _snapshot_is_custodian_equivalent(repo) if diff_mode != "git-range" else False
```

`git-range` — это РОВНО тот режим, которым гейт запускает CI. То есть в облаке освобождение
не вычислялось **вовсе**, литералом. И вычислить его там действительно нельзя, но не потому,
что «нет `data/`» (как говорил докстринг — 3 из 4 входов генератора git-tracked), а потому что
**канон на origin ЗАМОРОЖЕН**: замер 2026-08-19 — регенерация даёт `as_of=2026-07-04` против
`as_of=2026-08-19` у снимка. Диагноз в докстринге был верен по выводу и неверен по причине.

**Цена, замерена по 209 прогонам с 2026-07-15:** 109 красных, **106 (97,2 %) — свой же
`chore(site-custodian)`**, ~10 красных в сутки. Информативных красных ТРИ, и одна из них
(08.08, `packages: статусы тиров — добавлен факт «идёт paper-трек»`) — ровно тот Balanced,
который позже стал вопросом владельца о книге с нулём позиций. Сторож её ПОЙМАЛ; в потоке
10 красных в день её никто не прочитал.

**Что сделано (п.2 и п.4), без ослабления:**
- Три исхода вместо двух: `_snapshot_custodian_equivalence` → `True` доказано custodian ·
  `False` доказано НЕ custodian · `None` **нечем измерить, причина названа**. Раньше и голый
  `except`, и `git-range` возвращали `False` — «не смог измерить» приходило как «доказал
  нарушение» (класс #201).
- `None` освобождения НЕ даёт: **код возврата 2 не смягчён**, на сайт ничего нового не уезжает.
  Находки помечены `exemption_unmeasured`, счёт `unmeasured_count` отдельно от `gated_count`.
- `_snapshot_self_consistency` — маршрут, который В ОБЛАКЕ ИЗМЕРИМ: пересчёт показываемых чисел
  из **собственных `bars`** снимка арифметикой самого генератора (импортом, не второй копией).
  Пока **только измерение, не гейт** — включение owner-gated (п.3 карточки).
- Попутно: в `git-range` снимок читался из РАБОЧЕГО ДЕРЕВА, а не из судимого коммита (в CI
  совпадает, локально — нет). Приведено к тому же виду, что у `tier_bands.json` рядом.

**Приёмка:** +17 тестов (50 в файле), контроль на чистом `origin` того же sha — 17 красных
(честно: 12 из них по `AttributeError` = новизна API, поведенческих 2, поэтому поведение
держат **5 мутаций**, каждая красит свою цель; ключевая M2 — «разрешить НЕ ИЗМЕРЕНО
освобождать» — ловится тестом о несмягчённом коде возврата). Модуль после мутаций сверен
байт-в-байт. Полный прогон — в журнале W34.

**Инв. #16:** один существующий тест изменён НАМЕРЕННО
(`test_custodian_equivalence_false_without_data`) — он закреплял ровно схлопывание «нечем
измерить» в «нарушение». Проверка **усилена**: утверждаются оба конца — вердикт стал `None` с
названной причиной И освобождение по-прежнему не выдаётся. Обоснование в теле теста и в журнале.

**П.3 — ушёл владельцу:** `owner-decision-storozh-chisel-saita-krasneet-10-raz-v-den`
(три варианта + рекомендация, подкреплённая замером на реальной истории снимка).

---

## Доставлено циклом #300 (2026-08-19) — ПОДЪЁМ осиротевшей работы

Цикл #299 умер до пуша (`pid65591` мёртв, шаг 0b: `stale`, шаг 0a: недоставленное дерево
`/tmp/spa_c299`). Работа поднята, **отчёту мёртвой сессии не верил — всё перемерено своим
прогоном.** Что подтвердилось и что нет:

- **Подтвердилось:** зашитый `False` в `git-range` (`check_owner_gate.py:498`) · канон на origin
  заморожен (`as_of=2026-07-04` против снимка `2026-08-19`) · публикация идёт через
  `safe_site_push.py` · 109 красных, **106 (97,2 %) — свой же `chore(site-custodian)`».
  Прогонов теперь **221**, а не 209 (счёт вырос за сутки) — цифра в карточке владельца и в
  докстринге исправлена на мою.
- **НЕ подтвердилось — главная цифра рекомендации владельцу.** #299 писал «79 переходов:
  77 тишины / 2 красных». Мой прогон по ВСЕЙ истории снимка (199 коммитов) даёт
  **107 переходов с изменением числа: 91 тишины / 16 красных**. Все 16 двигали `packages.*`
  или `gates_passed` — ровно то, что сверке не выводимо и обязано звать владельца. Отличие в
  пользу осторожности, вывод не меняет, но чужое число как своё в карточку владельцу не ушло.
- **Сквозная приёмка на ЖИВЫХ данных, которой у #299 не было:** починенный гейт прогнан на
  реальном красном диапазоне CI (`bc22e0f38..6442ba987`) — те же 4 находки теперь помечены
  `[НЕ ИЗМЕРЕНО]` с названной причиной, сверка с собственными `bars` — СОГЛАСНЫ, **код
  возврата 2 сохранён** (проверен отдельно). Прогнаны и все ТРИ информативных красных:
  первый (`39adb9d6f`) — класс E, снятие honesty-токена, к снимку отношения не имеет и
  пометки не получает; два других двигали `packages.conservative.apy_pct` — то есть честный
  предел сверки, закреплённый тестом, не академический, а именно тот, что различает шум и дело.
- **Единственная правка сверх поднятого:** цифры замеров приведены к моим.
