---
trackerStatus:
  type: owner-decision
title: "Тюнер зеркалит политику БЕЗ запасов — решение владельца 26.08 (cloud, кнопка), ОТМЕНЯЕТ «запас оставить» от 25.08; реализация доставлена"
status: ingested
source: cloud-сессия владельца (вопрос с вариантами и ценой задан явно; выбран «Зеркалить политику (Рекомендую)»)
created: 2026-08-26
priority: high
related: ADR-136, ADR-060 §1.4, inbox-task-portfolio-cio-dynamic-capital-alloc
status_trail:
  - "2026-08-26T20:38:00.738102+00:00 needs-owner -> ingested · queue.set_status · cycle-36510"
---

## Решение владельца — ПРИНЯТО, зафиксировать owner-путём

26.08 владельцу задан вопрос «убрать двойную страховку тюнера поверх
RiskPolicy?» с тремя вариантами и последствиями. Ответ кнопкой:
**«Зеркалить политику»**. Это СОЗНАТЕЛЬНО отменяет его же решение 25.08
«запас оставить» — последнее решение побеждает, разворот назван вслух.

## Реализация (в этом же коммите, тестами)

`TunerConstraints` (дефолты) и `_DEFAULT_CONSTRAINTS` ребалансера → один
источник правды, живое зеркало RiskConfig: t1_min 0.55→0 (пола в политике нет),
t2_max 0.35/0.45→0.50 (ADR-019), конверт per_protocol 0.25→0.40 (per-тир min()
даёт РОВНО 40/20), max_protocols 6/7→8 (ALLOC-002), cash 0.07→0.05. Формула
min() сохранена — вернуть запас можно одним полем. Ожидание: +0.5–1% к
blended (снят принудительный перекос в 2.5–3.5%-е T1).

Приёмка: +6 тестов паритета (читают ЖИВОЙ RiskConfig — не эхо); 3 пина старых
запасов ИЗМЕНЕНЫ НАМЕРЕННО с обоснованием в каждом (семантика сохранена, суд по
источнику правды; исторические сценарии воспроизводятся явным запасом);
28-файловый набор: 697 passed у меня против 691 на чистом origin = ровно +6,
0 failed с обеих сторон.

## Циклу

1. Оформить ADR (номер через распределитель) со ссылкой на этот след; карточку
   закрыть owner-путём.
2. Живой шаг: сравнить книгу до/после на хосте (SHADOW ADR-060 уже пишет
   вердикты) — дельту blended APY доложить владельцу в дайджесте.

## Инжест (цикл #388, 2026-08-26)

- ADR оформлен: [`ADR-144`](../../docs/decisions/ADR-144-tuner-mirrors-policy-margins-removed.md),
  строка добавлена в `docs/decisions/INDEX.md`.
- Реализация СВЕРЕНА на `origin/main`, а не принята на слово: коммит `b6f426f88`,
  `spa_core/tuner/allocation_tuner.py` — `t1_min=0.0`, `t2_max=_P_T2_TOTAL`,
  `per_protocol_max=_P_T1_CONC`, `max_protocols=_P_MAX_PROTOCOLS`, `cash_min=_P_CASH_MIN`,
  формула `min()` на месте.
- Статус переведён `needs-owner → ingested` (инв. #14: `owner-done` ставит ТОЛЬКО владелец).
- **НЕ сделано этим циклом и не потеряно:** живой шаг п.2 — сравнить книгу до/после на хосте и
  доложить дельту blended APY в дайджесте. Вынесено строкой в `docs/STATE.md`.
- **Сказано вслух, а не проглочено:** `set-status` предупредил, что машинно проверяемого следа
  ответа (`owner_choice` / `owner_answered_at`) нет НИ В ОДНОЙ копии карточки. Причина названа в
  самой карточке: решение принято в cloud-сессии владельца, а не кнопкой бота, поэтому след не
  прошёл через очередь. Закрываю по ЧЕЛОВЕЧЕСКОМУ следу (текст решения + разворот назван вслух +
  доставленная реализация `b6f426f88`), но отмечаю: у cloud-пути ответа владельца нет квитанции,
  которую умеет читать `owner_decision_pending`. Это отдельный дефект пути, не этой карточки.
