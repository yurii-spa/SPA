---
trackerStatus:
  type: inbox
title: "Вопрос с ДВУМЯ решениями нечем ответить с телефона: кнопок нет по построению, а он висит девять дней"
status: new
source: nimbalyst
created: 2026-08-19
---

## Что измерено (цикл #309, 19.08, задание владельца «пришли заново все открытые вопросы с кнопками»)

Все 13 открытых вопросов пересланы заново и ДОШЛИ (проверено у двери, `ok=True` в
`data/alert_history.json`, ни одного подавленного). Кнопки уехали у **11 из 13**. Оставшиеся
два — не поломка отправки, а два РАЗНЫХ отказа разбора, и оба сегодня честны, но дороги:

| карточка | почему без кнопок |
|---|---|
| `own-rnd-killswitch-rearm-policy-missing` | карточка задаёт **ДВА независимых решения** — А (политика возврата после жёсткой остановки: А1/А2/А3) и Б (смысл мягкой ступени: Б1/Б2). `parse_options` видит две «семьи» номеров и отказывается: смешать их в одном ряду кнопок значит закрыть карточку ПЕРВЫМ нажатием и молча похоронить второй вопрос. |
| `own-33-plist-marker-for-cycle-origin` | вариантов в тексте нет вовсе — «разреши…» и «альтернатива — ничего не менять» прозой. Выдумывать владельцу выбор запрещено (ADR-075). |

Отказ в обоих случаях **правильный** — чинить надо не разбор, а форму вопроса.

## Почему это стоит закрыть, а не оставить

`own-rnd-killswitch-rearm-policy-missing` — `priority: high`, риск-контур: политики возврата
после жёсткой остановки у нас **нет вообще** (ADR-034/048 её не содержат). Владелец физически
не может ответить на неё с телефона — только словами в чат, а по замеру карточки
`agent-porucheniya-bez-vybora-nechem-otvetit` на такие вопросы он не отвечает НИКОГДА:
молчание тут неотличимо от «не увидел». Вопрос висит с 10.08 — девять дней.

## Что предлагается (НЕ сделано — это меняет очередь владельца)

**Вариант 1 (рекомендация).** Разделить карточку с двумя решениями на ДВЕ карточки
(«политика возврата» и «смысл мягкой ступени»), каждая со своим перечнем вариантов. Тогда обе
получают кнопки штатным путём, разбор не трогается ни одной строкой, инвариант «не смешивать
два вопроса» соблюдён по построению. Цена — две карточки вместо одной в очереди.

**Вариант 2.** Научить `record_choice` многовопросным карточкам (ответ вида «А1+Б1»).
Существенно дороже: сегодня он знает только варианты, ВЫЧИТАННЫЕ из карточки, и закрывает
карточку одним выбором. Трогает путь записи ответа владельца — рядом с инвариантом #14.

**Вариант 3.** Оставить как есть и признать честно: карточка с двумя решениями отвечается
только словами.

Сессия #309 сама не выбирала: деление чужой карточки-вопроса меняет то, что видит владелец,
а разбор ответа — money-adjacent путь. Правило «одна задача за итерацию» соблюдено.

## Как понять, что готово

Ни один открытый вопрос владельцу не остаётся без способа ответить нажатием — замер тот же:
`parse_options` по всем `needs-owner` карточкам даёт непустой список у каждой.

## Смежное, уже закрытое этим же циклом

Журнал пушей утверждал доставку того, что заслон потока мог уронить молча
(`register_push` пишет `buttons: true` ДО отправки). Исход отправки теперь записывается
(`mark_send_outcome`), и недоехавшее решение попадает в штатный ремонт `heal_buttonless`,
для которого раньше было невидимо навсегда. +7 тестов, 6 краснеют на чистом origin.
