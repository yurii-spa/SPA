---
trackerStatus:
  type: agent-task
title: 19 тестов spa_core/tests красны на main (предсуществующие, никем не отслеживались)
status: done
source: session-2026-07-29-cycle33
created: 2026-07-29
---

**Что найдено (цикл #33).** Полный прогон `spa_core/tests/ tests/` на чистом `origin/main`:
**102 634 passed, 20 failed, 642 skipped**. Карточка цикла #31/#32 разбирала только каталог
`tests/` — эти падения живут в `spa_core/tests/` и до сих пор не были описаны ни одной карточкой.
Все 20 воспроизведены на чистом чекауте без чужих правок (не регрессия).

Из 20 один уже адресован владельцу (`test_evidence_seeded::test_real_days_present` →
`owner-decision-dannye-treka-v-git-fail-dokazatelstv-zam.md`). Остальные 19:

| Файл | Тесты | Первая гипотеза (проверять!) |
|---|---|---|
| `test_eth_signer.py` | 8 | нет опциональной зависимости `eth_account` — вероятно, гейт по зависимости |
| `test_rates_desk_calibration.py` | 3 | детерминизм/robust-центр свипа |
| `test_deploy_gate_exit78.py` | 2 | fail-closed проверки деплой-гейта |
| `test_portfolio_capacity.py` | 1 | identity «каждое семейство выше пола» |
| `test_rates_desk_carry_validation.py` | 1 | детерминизм прогона |
| `test_rates_desk_surface_expansion.py` | 1 | refusal 100% на toxic после расширения |
| `test_tier1_backtest.py` | 1 | gate-eligible ⊆ validated |
| `test_tier1_e2e.py` | 1 | предсуществующая data-зависимость (известна с цикла #32) |
| `test_tier1_regime.py` | 1 | атомарность/форма отчёта |

**Почему это важно.** Красный тест — сигнал, а не помеха (инвариант #16). Часть этих имён
(`rates_desk` refusal, `tier1_backtest` gate-eligible, `deploy_gate` fail-closed) звучит как
проверки честности/отказа — то есть ровно тот класс, где молчаливое падение опаснее всего.
Тяжёлый `ci.yml` гоняет `tests/`, поэтому в CI эти падения не видны вовсе.

**Acceptance criteria:**
1. По каждому из 19 определён класс: (а) отсутствие опциональной зависимости → явный
   `skipUnless(<условие>, "<почему>")`, (б) устаревший ассерт после чужого рефактора →
   правка по существу с обоснованием в теле + журнал, (в) **настоящий баг** → фикс прод-кода
   с тестом, красным до фикса.
2. Ничего не ослаблено молча (инвариант #16); каждое намеренное изменение теста — с
   обоснованием в теле и записью в `docs/journal/<неделя>.md`.
3. Всё, что упирается в risk-логику / kill-switch / живой трек / данные в git — НЕ чинить,
   а завести карточку `needs-owner`.
4. Брать порциями (по файлу-два за заход), не big-bang.

---

## Ход работы — заход 1 (цикл #34, 2026-07-29)

**Закрыто 18 из 19.** Ни один не оказался «просто устаревшим ассертом»; гипотезы карточки в двух
местах были неверны (проверять их и дальше, а не доверять):

| Файл | Тестов | Что оказалось на самом деле |
|---|---|---|
| `test_eth_signer.py` | 8 | НЕ отсутствие `eth_account`, а недоведённый рефактор WS-5.1: арминг-проверка переехала внутрь `sign_transaction`, соседние сюиты обновили — этот файл забыли. Корректность подписи не проверялась НИГДЕ. Вооружение через `monkeypatch.setenv` как у соседей; свойство «без арминга — отказ» остаётся за `test_execution_guard_coverage.py` |
| `test_tier1_regime.py` | 1 (+2 новых) | **Настоящий баг прод-кода:** `regime_summary` на пустой истории отдавал меньший набор ключей → `build_report` падал `KeyError: 'n_transitions'`. Плюс тест был псевдо-герметичным (патчил `_DATA`, а данные идут через `oos_mod.load_protocol_series`) |
| `test_portfolio_capacity.py` | 1 | Допуск выведен из публикационного округления (`net_apy_pct` до 4 знаков при полной точности `Decimal` внутри): старая граница 1¢ падала на 2–3¢, когда модель была ПРАВА |
| `test_rates_desk_calibration.py` | 3 | Гейт по артефакту вместо «потому что CI»; набор расширен до полного замыкания (PT + funding + prices) — гейт по одному файлу оставался полуправдой |
| `test_rates_desk_surface_expansion.py` | 1 | Проверка refusal-first против РЕАЛЬНОЙ истории (ничего не инжектит) → гейт по артефакту |
| `test_rates_desk_carry_validation.py` | 1 | Тест инжектировал только половину входа (PT — да, funding — нет). Фикстуру сознательно НЕ достраивал: funding кормит риск-вход гейта, синтетика сделала бы гейт мягче реальности |
| `test_deploy_gate_exit78.py` | 2 | Не дефект гейта: `check_agent_before_deploy.sh` жёстко пинит `REPO_ROOT`, тест пишет plist в свой чекаут. Скип теперь вычитывается ИЗ САМОГО гейта. Сам гейт не трогал (деплой-инструмент) |
| `test_tier1_backtest.py` | 1 | Тест сравнивал ДВА РАЗНЫХ входа: `build_gate` читает снимок `data/tier1_verdict.json`, а ожидание выводилось из свежего `evaluate()`. Переведён на вердикт, который гейт реально прочитал, + усилен (eligible ∪ blocked покрывают доску точно и не пересекаются) |

**Проверка:** чистый чекаут `origin/main` — 132 passed / 11 skipped / 0 failed; с подложенными
артефактами те же файлы — **79 passed / 0 skipped** (т.е. это гейт по данным, а не маска);
2 теста deploy-гейта прогнаны в каноническом чекауте (2 passed, файл восстановлен побайтово).

**Осталось на заход 2:**
1. `test_tier1_e2e.py::test_status_health_invariant` — предсуществующее падение (идентично со
   стэшенным дифом). Файл сознательно построен «против реальных текущих данных»; в нём же живёт
   `test_gate_eligible_equals_validated` с тем же дефектом live-пересчёт-против-снимка.
2. **Сигнал (не баг-фикс, решать отдельно):** `tier1/gate.py::build_gate` публикует пригодность к
   paper из снимка вердикта БЕЗ отметки возраста; при отсутствии файла `_load` глотает исключение и
   отчёт выглядит как «0 заблокировано, всё в порядке». Класс тот же, что RISKWIRE (цикл #29).
   Радиус измерен: продакшн-потребителей `is_eligible` нет, `tier1_gate.json` читают только
   отчёты ⇒ advisory. Отдельным заходом.

---

## Ход работы — заход 2 (цикл #35, 2026-07-29/30) · 19/19 ЗАКРЫТО

**Остаток закрыт, но не тем, чем выглядел.** `test_tier1_e2e::test_status_health_invariant` был не
«data-зависимостью», а **третьим за неделю случаем fail-OPEN класса #29/#31**: сводка Tier-1
докладывала `health: "OK"` о проверках, которых не делала.

| Что | Было | Стало |
|---|---|---|
| `status.py::_load` | `{}` и для «прочитал пусто», и для «не смог прочитать» | `None`, если вход нечитаем (нет файла / битый JSON / не-объект) |
| детекторы | `== "DIVERGENT"` / `== "ISSUES"`, всё прочее ⇒ `OK` | три состояния: сработал / чисто / **не измерено** → `unchecked` + причина вербатим |
| `health` | `"OK"`, если `problems` пуст | `"OK"` только если пусты И `problems`, И `unchecked` |
| алерт | молчал, когда ничего не проверено | `unchecked` дублируется в `problems` ⇒ Telegram это скажет |
| фикстура `verdict` в e2e | свежий `evaluate()` против модулей, читающих СНИМОК | `verdict_snapshot` — файл, который модули реально читают |
| скип теста | `skipif(GITHUB_ACTIONS)` «runs locally» (локально ПАДАЛ) | гейт по артефакту `data/tier1_packages.json` + усиление |

**Живая достижимость доказана:** `data_integrity.audit()` при своём исключении отдаёт третий статус
`NO_DATA` (`data_integrity.py:32`) — не `ISSUES` ⇒ аудит целостности упал, а `/api/tier1/status`
публикует «OK». Плюс `run_backtest_tier1.sh` под `set +e` не останавливается на упавшем шаге.

**Дефект был во ВСЁМ e2e-файле, а не в одном тесте:** `_VERDICT` есть у gate / packages /
correlation / monte_carlo / var / attribution / benchmark / status — все читают снимок, пересчитывает
только `evaluator`. В смешанном состоянии (снимок есть, серии отсутствуют) файл давал **4 падения**,
ни одно не про интеграцию. Все кросс-модульные ожидания переведены на снимок.

**Проверка:** +27 герметичных тестов (`test_tier1_status_honesty.py`, **23 красных до фикса**);
чистый чекаут `origin/main` — **65 passed / 3 skipped / 0 failed**; с артефактами те же файлы —
**68 passed / 0 skipped** (гейт по данным, не маска); на КОПИИ полных живых данных — 68 passed;
смежный срез 233 passed (полные данные) / 226 passed + 7 skipped (чистый чекаут); mypy чисто;
md5 живого трека не изменился.

**Осталось как СИГНАЛ (не баг, решать отдельно — карточка закрывается, сигнал переписан в journal):**
1. Ни один модуль Tier-1 не публикует возраст `tier1_verdict.json` — «кто обязан обновлять снимок»
   это вопрос владельца/ADR, не правка теста.
2. `gate.is_eligible` fail-OPEN → `True` при отсутствии файла — ЯВНО задокументированное решение
   («don't block ops on a missing Tier-1 run»), менять молча нельзя; тестом не пиннил (пиннить =
   благословить).

**Итог карточки: все 19 тестов закрыты (18 в заходе 1, 1 в заходе 2) → `done`.**
