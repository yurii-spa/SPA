---
trackerStatus:
  type: inbox
title: "Шторм решений ПРОДОЛЖАЕТСЯ после анти-шторма: карточки живут ТОЛЬКО в хост-дереве, заголовок дрейфует → дубли, владелец измотан"
status: new
source: cloud-сессия владельца (скрины 09:09 / 10:58 / 18:07-18:10, прямая речь владельца «я уже устал»)
created: 2026-08-20
priority: critical
related: inbox-shtorm-povtorov-odnogo-resheniya-vladeltsu
---

## Замер (в дополнение к вчерашней карточке шторма)

1. Спам-карточек **НЕТ на origin/main вовсе** — проверено `git cat-file`:
   `owner-decision-sait-packages-astro-avtonomnaya-pravka-z` и
   `owner-decision-sait-pravka-avtonomnaya-pravka-zadela-ow` живут только в
   хост-дереве. Ответ владельца «правкой status: в git» физически невозможен;
   решение, живущее в одном дереве, — теряемо по построению.
2. **Заголовок дрейфует между генерациями** («Сайт: packages.astro — …» →
   «Сайт: правка — …») → `_slug()` даёт НОВОЕ имя → per-card анти-шторм и
   журнал отправок обходятся честно. Генератор — сторож owner-gated правки
   сайта (инвариант #8): каждый цикл пере-пытается ту же правку и заводит
   НОВОЕ решение вместо ссылки на старое.
3. Отправки в 18:07 идут ПОСЛЕ доставки анти-шторма в origin (утро 20.08) —
   либо синк spa_core/ до хоста не доехал, либо путь отправки обходит гейт
   (кандидат: `owner_requested=True` у пере-отправщика — «владелец просил
   присылать задачи» ≠ «слать одно решение восьмой раз»).
4. Владелец ответил «1. Ободрить» реплаем (18:10) — мягкий разбор уже в origin
   (720a951eb), но хостовый бот его ещё не подхватил.

## Что чинить (по цене ошибки)

1. **Дедуп решений по НОРМАЛИЗОВАННОМУ заголовку/содержанию**, не по слагу:
   новое owner-decision с косинусно-тем-же вопросом обязано приклеиваться к
   существующему, а не рождать имя. Пока вопрос открыт — пере-попытка той же
   owner-gated правки НЕ создаёт новую карточку и НЕ шлёт уведомление.
2. **Гейт анти-шторма обязан действовать и для `owner_requested`** хотя бы
   окном 30 мин: «владелец просил присылать» не означает «слать копии».
3. **Карточки решений обязаны уезжать на origin** тем же циклом, который их
   создал (ADR-086-класс: след вопроса — как след ответа). Вопрос, живущий
   только в хост-дереве, невосстановим и неотвечаем из git.
4. Ответ владельца по СУТИ уже дан и записан в его прямой речи (cloud-сессия,
   20.08): **вариант 1 — «Одобрить»** автономную правку сайта из этих карточек
   (рекомендация «Отклонить» им сознательно отклонена). Когда цикл склеит
   дубли — записать этот ответ owner-путём и закрыть обе копии; владельца
   больше НЕ дёргать по этому вопросу.

## Как понять, что готово

24 часа без единого повтора этого решения в чате владельца; обе копии закрыты
с его ответом (вариант 1); карточки решений видны на origin.

---

### Независимый замер цикла #441 (2026-08-31) — что именно теряется, поимённо

Шаг 0-офис снова назвал этот класс, и на этот раз он назван КОНКРЕТНО, а не числом.
Живой вопрос владельца `owner-decision-knigu-perekladyvayut-22-raza-za-nedelyu-2026-08-29`
(`needs-owner`, `priority: high`, блокирует честность 30-дневного трека):

    origin/main   13 098 байт
    прод-дерево    9 291 байт   ← отсюда его отправляет бот

`diff` — только удаления (`113,138d112`), ни одной расходящейся строки: прод-копия есть
СТРОГОЕ ПОДМНОЖЕСТВО origin'ной. Недостающие 26 строк — это раздел
**«ПЕРЕСМОТР РЕКОМЕНДАЦИИ (поздний вечер 29.08)»** с таблицей прогона по фактическим
22 перекладкам недели ($1 288/нед издержек против $87/нед заработка всей книги) и
выводом, что ограничение безопасно при ЛЮБОМ ответе на вопрос о причине.

**То есть отправь бот этот вопрос сегодня — владелец получил бы ПРЕЖНЮЮ рекомендацию,
которую автор той же карточки сам отменил.** Класс тут не «карточка невидима» (эта как
раз видима — файл на месте, заголовок совпадает, сторож по имени её считает
доставленной), а «видима СТАРАЯ редакция». Дубль карточки заводить не стал: класс уже
несут эта карточка и `inbox-307-kartochek-nevidimy-prod-derevu`.
