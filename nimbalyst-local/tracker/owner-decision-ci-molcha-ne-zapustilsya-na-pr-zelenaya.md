---
trackerStatus:
  type: owner-decision
title: CI молча не запустился на PR — зелёная галочка означала «не проверяли»
status: owner-done
source: nimbalyst
created: 2026-08-26
status_trail:
  - "2026-08-26T20:34:03.051680+00:00 needs-owner -> owner-done · queue.set_status/closed_by:agent/evidence:варианты А+Б исполнены (ADR-145); критерий карточки проверен положительным контролем: head c7586a5 с одной галочкой Cloudflare даёт no_runs и код 1; test_pr_ci_liveness.py 20 passed / 11 subtests"
---

## Что случилось и почему это важно

26.08 у PR #46 событие `pull_request` **не создало ни одного прогона GitHub Actions**. Не «прогон
упал», а «прогона не было вовсе» — на странице PR стояла одна зелёная галочка Cloudflare, и PR
выглядел проверенным.

Замер: у КАЖДОГО предыдущего head этой ветки прогон `ci.yml` есть (`7543ba7`, `91f9c07`,
`91eddc7`, `a4fd5f9`, `f1a5ef1` …), у `c7586a5` — **нет вообще**. `paths-ignore` ни при чём: в
диффе пять `.py`-файлов. Actions при этом жив — ручной `workflow_dispatch` создал прогон сразу.

Это ровно тот класс, который мы весь день чиним у чисел и тестов: **«не измерено» стало
неотличимо от «прошло»**, и неотличимо в безопасную сторону. Только здесь не тест молчит, а весь
CI, и заметить это можно лишь сверкой «а прогон-то был?».

## Что от тебя нужно

Решить, чем закрывать класс. Варианты:

- **Вариант А (рекомендую) — сторож «у этого head есть прогон».** Проверка, которая для текущего
  head PR спрашивает у Actions API список прогонов и краснеет, если их НОЛЬ. Дёшево, ловит
  ровно этот случай и любой будущий. Цена: ~полдня, нужен доступ к API из CI.
- **Вариант Б — добавить `workflow_dispatch` тем четырём workflow, у которых его нет**
  (`generated-docs-integrity`, `spa-lint`, `numbers-lint`, `owner-gate`). Это не ЛОВИТ проблему,
  а лишь даёт руками добрать проверки, когда она уже случилась. Полезно как дополнение к А,
  само по себе — нет.
- **Вариант В — ничего не делать**, полагаясь на то, что случай единичный. Тогда это должно
  быть выбрано и записано, а не получиться молчанием.

## Как понять, что готово

На выбранном варианте: PR, у head которого нет ни одного прогона, **виден как непроверенный**
(красным или явной пометкой), а не как зелёный.

## Что будет после

Вариант А — пишу сторожа с положительным контролем (искусственный head без прогонов обязан его
покрасить). Вариант Б — правлю четыре файла workflow. Вариант В — записываю решение и закрываю.

_Причина, по которой событие не сработало, НЕ установлена: из API её не видно. Замер выше —
всё, что удалось узнать. Найдено при доставке ADR-146 (PR #46)._


---

## ✅ Закрыто агентом 2026-08-26 — варианты А + Б (ADR-145)

Владелец: «**почини это тоже сам**». Взят вариант А (сторож на отсутствие прогона) **и** вариант
Б (ручной вход недостающим workflow) — в карточке они были альтернативами, но Б без А не ловит
проблему, а А без Б оставляет её без законного обхода.

**Критерий приёмки карточки** («PR, у head которого нет ни одного прогона, ВИДЕН как
непроверенный, а не как зелёный») **ПРОВЕРЕН** положительным контролем, воспроизводящим аварию
дословно: head `c7586a5`, единственная проверка — «Cloudflare Pages» ⇒ вердикт `no_runs`,
код возврата 1.

**Главное свойство решения:** сторож НЕ живёт на событии, которое сторожит. Вход — `schedule`
(ежечасно) + ручной; `pull_request` НЕТ, и это закреплено отдельным тестом. Иначе не сработало
событие ⇒ не запустился и сторож.

**Три исхода, а не два:** `checked` (0) · `no_runs` (1) · `unchecked` (2). Живой замер из
контейнера без токена: API отвечает 403, сторож печатает `unchecked` и возвращает 2, а не 0.
Сторонние галочки прогоном НЕ считаются (это сердце аварии), а КРАСНЫЙ прогон считается —
вопрос сторожа «был?», а не «зелёный?».

**Вариант Б исполнен:** `workflow_dispatch` добавлен `generated-docs-integrity`, `spa-lint`,
`numbers-lint`, `owner-gate`. Сторожит это не список в голове, а тест, перебирающий ВСЕ файлы
`.github/workflows/*.yml`: любой будущий workflow с `pull_request:` без ручного входа покраснеет
сам.

**Чего это решение НЕ делает:** причина несработавшего события так и НЕ установлена — из API её
не видно, и выдумывать её я не стал. Сторож делает исход видимым, что ценно независимо от
причины, но причиной не является.

Приёмка: `spa_core/tests/test_pr_ci_liveness.py` — 20 тестов + 11 subtests;
`test_unwired_scripts_ratchet` — 14 passed (новый скрипт признан подключённым).
