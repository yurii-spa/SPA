---
trackerStatus:
  type: inbox
title: "Шаг 0a повторяет одни и те же 6 находок каждый цикл: осадок из мёртвых /tmp-worktree'ов"
status: new
created: 2026-08-14
---

## Что измерено (цикл #224, 2026-08-14)

`scripts/check_undelivered_work.py` выдал **6 находок «НЕ ДОСТАВЛЕНО»**. Все шесть указывают
на `/tmp`-worktree'ы циклов #210 / #213 / #220, которые до сих пор лежат на диске и числятся
в `git worktree list`:

- `spa_core/monitoring/house_view_gap.py`
- `spa_core/investment_os/health.py`
- `nimbalyst-local/tracker/inbox-storozh-rashozhdenii-sudit-po-staromu-sn.md`
- `docs/STATE.md`
- `docs/journal/2026-W33.md`
- `nimbalyst-local/tracker/_BOARD.md`

**Работа при этом ДОСТАВЛЕНА:** #220 подняли циклом #222, #221 — коммит `ca028000`
(записано в `docs/STATE.md`). То есть сторож прав по своему контракту (содержимого этих
рабочих деревьев в истории `origin` нет), но отвечает не на тот вопрос, который читает
оркестратор: «есть ли потерянная работа?» — потерянной работы нет.

`git worktree list` на момент замера — **30+ каталогов**, старейшие от цикла #189.

## Почему это не косметика

Шаг 0a обязателен КАЖДЫЙ цикл и обязан читаться. Шесть постоянных ложных строк — это тот
самый механизм, которым сторожа глохнут: следующая сессия учится пролистывать раздел
«НЕ ДОСТАВЛЕНО», и настоящая находка проедет вместе с осадком. Ровно этот класс уже чинили
(#146–#176, «шаг 0a морил шумом»).

## Чего я НЕ сделал и почему

Каталоги **не удалял**. Признак, по которому сторож отличает потерянную работу, — это и есть
«содержимого нет в истории origin»; молча стирать деревья, подходящие под такое описание,
значит уничтожать именно то, ради чего сторож существует. Нужна процедура, а не жест.

## Что сделать

1. Разобрать, чем эти шесть отличий являются на самом деле: промежуточные состояния уже
   доставленной работы или что-то, чего на `origin` нет вовсе. Пофайлово, замером.
2. Дать снятию worktree'ов **правило**: когда каталог можно убрать (доставлено · подтверждено
   пофайлово · сессия мертва) и кто это делает. Без правила осадок вернётся.
3. Не гасить сам сторож и не сужать его: чинить причину (мёртвые деревья), а не проверку.

## Границы

`scripts/check_undelivered_work.py` не ослаблять. RiskPolicy, kill-switch, живой трек,
деплой агентов — не сюда.

Родитель: цикл #224.
