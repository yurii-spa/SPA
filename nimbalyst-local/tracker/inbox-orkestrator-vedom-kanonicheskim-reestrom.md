---
trackerStatus:
  type: inbox
title: Оркестратор ведом каноническим реестром + ALLOC-002-осознанный отбор (иначе больше кандидатов = хуже книга)
status: new
source: nimbalyst
created: 2026-08-08
priority: high
---

Замер 08.08 (sandbox): подключение morpho_blue_base + fluid_fusdc довело снимок до 10 адаптеров ⇒ аллокатор выдал >8 протоколов ⇒ сработал pre-diff collapse _compliant_target ⇒ книга ушла в детерминированный safe fallback: pendle (17.92% live) ВЫПАЛ, ожидаемая доходность 6.03% → 3.51%, появился spark_susds. Расширение откачено в тот же день (нормализация ключа снимка оставлена — она полезна и безвредна). ПРАВИЛЬНЫЙ порядок: (1) сделать отбор лучших N ОСОЗНАННЫМ в самом аллокаторе (ранжирование по risk-adjusted APY с уважением ALLOC-002 ≤8), чтобы расширение множества кандидатов улучшало книгу, а не роняло её в fallback; (2) только ПОСЛЕ этого вести оркестратор от канонического ADAPTER_REGISTRY (36) с фильтром живости; (3) вернуть храповик «покрытие вне Ethereum ≥1». Класс дефекта: «источник ≠ предмет наблюдения» + «больше входов без отбора = деградация». Money-path: sandbox-замер обязателен на каждом шаге.

---

## Волна 0 триажа, 16.08 — что эта карточка теперь покрывает

Схлопнута сюда (кластер К11 `docs/BACKLOG_TRIAGE_2026-08-16.md`, переведена в `done`, с диска
не удалена):

- **`inbox-alloc-002-otbor-top-8-po-vesu-bednit-kni`** — «топ-8 по весу беднит книгу,
  отбирать ПОСЛЕ evidence/TVL-гейта». Её замер 08.08 (sandbox): отбор лучших 8 по
  risk-adjusted весу ДО потолков дал кэш **15.8 % и APY 4.73** против **10 % / 6.03** без
  него — в топ-8 попадают кандидаты, которые затем не проходят TVL/evidence-гейт
  (`aave_v3_base`, `morpho_blue_base` со static TVL), их доли уходят в кэш, а отсечённые
  живые (`compound_v3`, `euler`) в книгу уже не возвращаются.

Это и есть шаг (1) настоящей карточки — «сделать отбор лучших N ОСОЗНАННЫМ в самом
аллокаторе». Порядок остаётся: сначала отсев по доказанности (live TVL+APY, ADR-053/061),
потом отбор лучших N среди ПРОХОДНЫХ, потом потолки; и только ПОСЛЕ этого вести оркестратор
от канонического `ADAPTER_REGISTRY`. Money-path: sandbox-замер до/после, `pre_cutover_gate`.
