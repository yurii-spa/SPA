---
trackerStatus:
  type: agent
title: "Одиннадцать сторожей с точкой входа, которых не зовёт никто — подключить или вывести"
status: backlog
source: систематический замер 2026-08-29 (после находки про apy_spike_monitor)
created: 2026-08-29
priority: high
domain: надзор (директива владельца «90 % должно РАБОТАТЬ»)
---

## Почему искал

`spa_core/alerts/apy_spike_monitor.py` написан, порог верный (`compound_v3` > 8 %),
показание 25.08 было **9.2007 %** — он бы сработал. Его не звал никто, и всплеск
нашёлся неделей позже вручную, при разборе перекладок книги.

Храповик неподключённых у нас есть, но он покрывает `scripts/`. Модули `spa_core/`
не проверял никто.

## Замер

Из **72** модулей со своей точкой входа в `alerts/`, `monitoring/`, `governance/`
**11** не упомянуты нигде — ни в plist, ни в обёртке, ни в другом модуле, ни в манифесте:

| Модуль | Что, судя по имени, сторожит |
|---|---|
| `alerts/apy_spike_monitor.py` | всплески доходности (порог верный, срабатывание пропущено) |
| `alerts/adaptive_monitor.py` | адаптивный мониторинг |
| `monitoring/data_freshness_monitor.py` | свежесть данных |
| `monitoring/unified_gas_monitor.py` | газ, сводный |
| `monitoring/arbitrum_gas_monitor.py` | газ Arbitrum |
| `monitoring/optimism_gas_monitor.py` | газ Optimism |
| `monitoring/golive_checker_lp.py` | готовность go-live для LP |
| `monitoring/slo_proposal.py` | предложения по срокам годности |
| `monitoring/stalled_run_diagnosis.py` | диагностика зависших прогонов |
| `monitoring/findings_bridge_c125.py` | мост находок (вариант c125) |
| `governance/cpa_governance_watcher.py` | наблюдатель за governance |

Три из одиннадцати — про **газ**, и это рядом с находкой дня: стоимость перекладок
считается по константам без даты замера (`docs/cost_model_provenance.md`), а три
написанных газовых монитора не подключены.

## Что сделать с каждым

По директиве владельца «90 % должно РАБОТАТЬ»: **подключить или вывести карточкой**.
Третьего состояния — «лежит и ждёт» — быть не должно.

Осторожно с теми, кто шлёт в Telegram: подключение — это новый источник тревог,
решение владельца (прецедент `own-37`). У `apy_spike_monitor` уже своя карточка.

## Что уже стоит

`spa_core/tests/test_unwired_guard_modules_ratchet.py` — перепись закреплена,
список может только сокращаться. Три мутации краснеют.

Две ошибки в собственном замере, обе пойманы до публикации:

1. первая редакция искала только точечный путь (`spa_core.alerts.x`) и объявила
   сиротами `morning_digest` и `portfolio_health`, которые вызываются обёрткой
   и plist'ом — 18 «сирот» вместо 11;
2. вторая склеивала дерево в одну строку, и модуль «ссылался» сам на себя через
   собственную докстроку.

Заодно тест ускорен с **85 секунд до 1.06** — 72 запуска `grep` заменены одним
проходом. Медленный тест рано или поздно отключают, а это ровно тот класс,
который он ловит.
