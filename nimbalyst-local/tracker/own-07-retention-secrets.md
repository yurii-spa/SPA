---
trackerStatus:
  type: owner-decision
title: Включить письма-подтверждения для подписки — добавить два ключа на сервер
status: ingested
priority: medium
owner: yuriycooleshov@gmail.com
blocks: Подписка «следить за кошельком» (возврат пользователей)
created: 2026-07-15
legacy_id: Q-OWN-07
---

> **ИНЖЕСТ 2026-08-02: письмо ДОШЛО — исходная задача выполнена владельцем.**
> Настоящая причина оказалась третьей (ни «ключ есть, но не в том окружении», ни поломка
> кода): `RESEND_API_KEY` в Railway **отсутствовал вовсе**; окружение в проекте одно —
> production. Владелец создал ключ в Resend и добавил переменную; после деплоя
> подтверждающее письмо реально пришло. Первый тест сессии показал пустой лог Resend
> только потому, что пришёлся на момент сборки.
> Остаток НЕ на владельце по этой карточке: домен для отправки — `own-26`; честный статус
> отправки в ответе API и эфемерное хранилище подписчиков — `agent-checkup-waitlist-fail-open-ok-true`.
> `needs-owner` → `ingested`; в `owner-done` переводит только владелец (инвариант 14).

> **ПРОВЕРКА ВЛАДЕЛЬЦЕМ 2026-07-23 (Путь A, end-to-end): ключи добавлены, но письма НЕ уходят на проде.**
> Тест `POST /api/waitlist {email: yuriycooleshov@gmail.com}` → `{"ok":true,"position":1}` (HTTP 200) —
> запись создана, НО письмо-подтверждение в Gmail (вкл. спам, за час) НЕ пришло. `position:1` = waitlist
> пуст. Диагноз: **тот же класс, что `ETHERSCAN_API_KEY` (карта `checkup-approvals-dark`)** — ключ
> «добавлен», но не доходит до работающего сервиса, ИЛИ код чекапа молча глотает ошибку отправки
> (`ok:true` даже без письма).
>
> **НАСТОЯЩАЯ ЗАДАЧА (переформулирована):**
> 1. Railway → `defi-checkup` → Variables: убедиться, что `RESEND_API_KEY` + `WALLET_REF_SALT` есть
>    в ЗАДЕПЛОЕННОМ environment (не staging) и в правильном сервисе → **Redeploy**.
> 2. Чекап-сервис (отдельный репо) должен НЕ возвращать `ok:true`, если письмо не отправилось —
>    fail честно + залогировать ошибку Resend (сейчас `ok:true` маскирует поломку). Класс: fail-OPEN.
> 3. Повторный тест: подписка тестовым email → письмо реально приходит.
>
> Статус остаётся `needs-owner` (нужно действие на Railway + правка чекапа). В `owner-done` — только владелец.

## Что случилось и почему это важно
На сайте проверки кошелька есть подписка («следить за кошельком» / список ожидания) — это главный
крючок, который превращает разовую проверку в привычку возвращаться. Чтобы человеку уходило
письмо-подтверждение подписки, сервису нужны два ключа: `RESEND_API_KEY` (сервис отправки писем
Resend) и `WALLET_REF_SALT` (секретная строка для безопасных реферальных ссылок). Без них подписка
не подтверждается письмом.

## Что от тебя нужно
Шаг 1. Открой Railway → сервис `defi-checkup` → раздел Variables (переменные окружения).
Шаг 2. Добавь `RESEND_API_KEY` (возьми в своём аккаунте Resend) и `WALLET_REF_SALT` (любая длинная
случайная строка — придумай один раз, например 32+ символов).
Шаг 3. Нажми Redeploy.

## Как понять, что готово
Подпишись на сайте тестовым email — на почту придёт письмо-подтверждение подписки.

## Что будет после
Я проверю, что письма реально уходят, и закрою карточку. Подписка-возврат заработает как канал
удержания пользователей.
