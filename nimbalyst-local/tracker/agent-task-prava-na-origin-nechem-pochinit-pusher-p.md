---
trackerStatus:
  type: agent-task
title: "Права на origin нечем починить: пушер по построению сохраняет режим remote, а правило требует чинить режим на origin"
status: backlog
created: 2026-08-07
priority: medium
domain: доставка / права файлов
source: "цикл #156 (подъём осиротевшей работы #153)"
---

## Что нашли

`scripts/agent_morning_digest.sh` лежит на origin с режимом `100644` (не исполняемый), а в
рабочем дереве прода — `755`. Разошлись именно права, содержимое совпадает.

Само по себе это сейчас не гасит агента: метка `com.spa.morning_digest` в `RETIRED_LABELS`,
в `launchctl list` её нет, а живой близнец `com.spa.work_digest` запускается через
`/bin/bash <путь>` — при таком вызове x-бит не требуется. Опасность отложенная: правило
`.claude/rules/deployment.md` п.3 прямо говорит «режим `100644` у скрипта, который запускает
launchd, = агент мёртв (exit 126), и это не видно ни по одному пульсу», и оживление метки
(или переход на прямой `ProgramArguments[0]`) делает эти права аварией.

## Почему это не чинится обычным путём

Починить надо **на origin** (правило п.3: «Если на origin режим неверный — чинить на origin,
а не выставлять бит руками после каждого деплоя»). Но наш инструмент доставки этого не умеет
**по построению**: `push_to_github.py::tree_entry_mode` для уже существующего на remote пути
возвращает **его собственный режим** — специально, чтобы пуш никогда молча не снял x-бит.
Обратная сторона той же логики: пуш никогда не может x-бит и **добавить**. `chmod +x` в
worktree перед пушем ничего не меняет — режим берётся с remote, а не с диска.

Это ровно наш знакомый класс: сторож честно отвечает на СВОЙ вопрос («не снять случайно») и
читается как ответ на нужный («режим — часть доставки»).

## Что сделать

1. Разово выправить режим на origin (git-путь: `git update-index --chmod=+x` + обычный пуш
   веткой, либо явный tree-режим через API) — это операция над деревом доставки, поэтому
   отдельной задачей, а не побочным эффектом чужого пуша.
2. Дать `push_to_github.py` **явный** способ поднять x-бит (флаг вида `--exec <path>`), не
   ослабляя защиту «не снимать молча»: по умолчанию поведение прежнее, изменение режима —
   только по явному указанию и с печатью «режим 644 → 755» в отчёте пуша.
3. Закрепить тестом в обе стороны: без флага режим remote сохраняется (в т.ч. x-бит не
   пропадает), с флагом — поднимается.

## Как понять, что готово

`git ls-tree origin/main scripts/agent_morning_digest.sh` показывает `100755`, и в
`spa_core/tests/test_digest_wrapper_honest_exit.py` карта `EXEC_REQUIRED` может стать
`{"work": True, "morning": True}` без единого красного теста.

## Откуда взялось

Цикл #156, подъём осиротевшей работы #153 (ADR-070 п.11). Тест `test_wrapper_exists_and_is_
executable[morning]` краснел на чистом checkout из origin и был бы зелёным в хост-дереве —
измерено, а не предположено.


---

## СВЕРКА 2026-08-17 — НЕ ЗАКРЫТА: инструмент есть, режим на origin прежний

Критерий карточки состоит из ДВУХ измеримых половин. Сделана одна.

### ✅ Пункты 2 и 3 — инструмент и тесты доставлены

`push_to_github.py` получил явный `--exec <path>`:
`EXEC_MODE = "100755"` (стр. 61); разбор аргументов с отказом на неизвестный и на
неоднозначный путь (стр. 1270–1317, «этого файла нет в наборе доставки» /
«подходит больше одного файла»); правило режима (стр. 1435–1460) — «x-бит
НИКОГДА не снимается: `--exec` умеет только 644 → 755»; печать «режим 644 → 755»
в отчёте пуша (стр. 1629–1633); проверка аргументов ДО сети (стр. 1658).
Отдельно закрыт случай «содержимое совпадает с remote, а ехать надо за режимом»
(стр. 1547–1573).

Тесты в обе стороны: `spa_core/tests/test_push_exec_mode_on_origin.py`.

```
$ python3 -m pytest spa_core/tests/test_push_exec_mode_on_origin.py \
                   spa_core/tests/test_digest_wrapper_honest_exit.py -q
41 passed in 1.19s
```

### ❌ Пункт 1 и оба условия приёмки — НЕ выполнены

```
$ git ls-tree origin/main scripts/agent_morning_digest.sh
100644 blob fc022211ee193fda41a3beed1d6c44814a2696cc	scripts/agent_morning_digest.sh
```

Приёмка требует `100755`. Режим на origin **не выправлен** — и не мог быть
выправлен автономно: пуш агентам запрещён, а разовая правка режима есть операция
над деревом доставки (действие владельца, `.claude/rules/deployment.md` п.6).

Вторая половина приёмки — тоже нет:

```
spa_core/tests/test_digest_wrapper_honest_exit.py:55
EXEC_REQUIRED = {"work": True, "morning": False}
```

Приёмка требует `{"work": True, "morning": True}` без единого красного теста.

**Вывод:** наличие `--exec` — это средство, а не результат. Карточка закрывается
одним действием владельца: разовый пуш `--exec scripts/agent_morning_digest.sh`
(или `git update-index --chmod=+x` + пуш веткой), затем `morning: True` в
`EXEC_REQUIRED` и прогон файла.
