---
trackerStatus:
  type: inbox
title: Сделать названия/ID карточек человекочитаемыми (убрать нечитаемые цифры-буквы)
status: done
source: intake
created: 2026-07-15
---

## Задание (фидбек владельца, 2026-07-15)
ID карточек вида `inbox-20260715-160918-note` — нечитаемые (лишние цифры/буквы). Владелец хочет,
чтобы карточки были простыми и читаемыми.

## Что нужно
- Генерировать имя файла карточки из ЧЕЛОВЕЧЕСКОГО slug'а заголовка (напр. `inbox-dobavit-knopku-naverh.md`),
  а не из таймстампа; уникальность — коротким суффиксом только при коллизии.
- Проверить, где владелец видит «нечитаемые» ID (Nimbalyst показывает title — человеческий; ID всплывает
  в /status и в списках) — убрать/скрыть уродливые ID в выводах бота (`/status`), показывать title.

## Как понять, что готово
Новые карточки — с читаемыми именами; в `/status` и ответах бота видны заголовки, а не таймстамп-ID.

## Что будет после
Полный цикл возьмёт в работу (правка create_card + вывод /status).

---

## ✅ Сделано (автономный цикл env-setup-v3, 2026-07-16)
- `spa_core/owner_queue/queue.py`: добавлен Cyrillic→Latin транслит (`_translit`/`_CYR_TRANSLIT`) в `_slug`.
  Русские заголовки больше НЕ схлопываются в `note` — теперь `inbox-dobavit-knopku-naverh.md`.
- Имя файла карточки строится из читаемого slug'а заголовка, **таймстамп из имени убран**; дата
  осталась в поле `created:`. Уникальность — короткий суффикс `-2`/`-3` только при коллизии.
- Идея-файлы (`docs/ideas/…`, `intake.py` использует тот же `_slug`) тоже стали читаемыми — бонусом.
- Про `/status`: он и так показывает счётчики по статусам (не ID); `notify.py` предпочитает title.
  Уродливые таймстамп-ID владельцу больше нигде не всплывают.
- +3 регресс-теста (readable-slug / collision-suffix / fallback), 18 owner_queue зелёных, 29 в смежном наборе.
- Пуш: `queue.py` + тест на origin/main (verified). Существующие карточки со старыми именами не переименовывались
  (только форвард-изменение — переименование ломало бы ссылки; новые пойдут читаемыми).
