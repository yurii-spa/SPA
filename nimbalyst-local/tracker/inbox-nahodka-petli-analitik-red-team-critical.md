---
trackerStatus:
  type: inbox
title: "Находка петли: аналитик red_team: CRITICAL — требует реакции (карточка/решение), не п"
status: new
source: nimbalyst
created: 2026-08-10
finding_key: "gap:analyst_red:red_team"
---

Находка петли ADR-066 (house_view_gap, WARN, подтверждена 2 прогонами подряд):

аналитик red_team: CRITICAL — требует реакции (карточка/решение), не пролистывания

Сделано = находка исчезает из отчёта источника при следующем прогоне (мост закроет карточку сам).

_finding_key: `gap:analyst_red:red_team` · ADR-066_

---

## Сверка 2026-08-18 — храповик заведён, петля НЕ закрыта, остаток назван числом

Проверено своим прогоном, не по отчёту.

**Заведено:** `spa_core/tests/test_red_team_finding_loop.py` (коммит `b6c97996b`) —
**3 теста, зелёные** (`pytest spa_core/tests/test_red_team_finding_loop.py -q` → 3 passed):
настоящая критика симуляции атак доходит до канала карточек · ослепший аналитик побайтово
отличим от спокойного рынка · эхо собственной остановки не может приехать безымянным.
Тесты ничего не ослабляют — закрепляют сегодняшнее поведение (инвариант 16).

**Собственный замер канала `red_team → house_view_gap → карточка`** (четыре состояния,
`RedTeamAgent.analyze()` + `compute_gaps(now=)`, песочница, живое `data/` не тронуто):

| состояние | posture | posture_reason | находок в сверке |
|---|---|---|---|
| эхо нашей остановки | CRITICAL | `kill_switch_already_active` | **1** (доходит) |
| **наблюдённая угроза** | THREATS_PRESENT | `threats_present` | **0** (НЕ доходит) |
| критика симуляции атак | CRITICAL | `attack_surface_critical` | **1** |
| спокойный рынок | NO_THREAT_OBSERVED | — | 0 |

**Остаток одним числом: 1 из 2 настоящих тревог (`threats_present`) до канала карточек не
доезжает вовсе**, а собственное эхо остановки доезжает каждый цикл. Это асимметрия лестницы
постур, а не дефект храповика: закреплять кривое = узаконить его, поэтому храповик её
СПЕЦИАЛЬНО не пинит.

**Чего НЕ было сделано, вопреки формулировке в шапке теста** («вынесена решением владельцу»):
карточки владельцу на эту асимметрию в трекере не существовало — заведена этой сверкой,
`own-red-team-nablyudennaya-ugroza-ne-doezzhaet` (`needs-owner`). Статус этой карточки остаётся
`new`: её закрывает мост ADR-066, когда находка исчезнет из отчёта источника.
