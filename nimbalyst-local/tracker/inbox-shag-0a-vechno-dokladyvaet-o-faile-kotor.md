---
trackerStatus:
  type: inbox
title: "Шаг 0a вечно докладывает о файле, которого не существовало ни минуты: R&D-сессия переименовала свой результат на лету"
status: done
source: nimbalyst
created: 2026-08-15
---

## Что измерено (цикл #239, 2026-08-15)

Шаг 0a (`scripts/check_undelivered_work.py`) докладывает как «НЕ ДОСТАВЛЕНО» два файла,
которых **не существовало ни одной минуты ни в одном дереве**:

- `scripts/edge_risk_shape_budget.py`
- `spa_core/tests/test_edge_risk_shape_budget.py`

Отчёт сторожа про них честен и по-своему точен: «на origin/main файла нет; локально файла
тоже нет». Но вывод, который из этого читает оркестратор («есть потерянная работа, подними
её»), — ложный: поднимать нечего.

**Механизм — измерен, не предположен.** R&D-сессия `pid16782` (2026-08-15T02:02:28Z)
объявила владение файлами ДО работы, как требует протокол, и назвала идею реестра
**#55 RSB (Risk-Shape Budget)**. По ходу работы гипотеза сменилась: доставлена запись
реестра **#55 CSF (Cash-Sleeve Frontier)**, файлы `scripts/edge_cash_sleeve_frontier.py` +
`spa_core/tests/test_edge_cash_sleeve_frontier.py`. Сессия умерла до пуша, объявление
осталось. Имена в объявлении и имена результата разошлись — и это НОРМАЛЬНАЯ работа
исследовательского слоя: объявление пишется до того, как ответ известен.

**Почему само не рассосётся.** Находка снимается ровно одним событием — файл появляется на
`origin/main` **под тем же именем**. У сканера нет верхнего горизонта: он читает журнал
целиком (845 записей, старейшая 2026-07-11). Значит эти два имени будут в отчёте шага 0a
**каждый цикл до конца жизни журнала**, и каждый следующий цикл заново потратит время,
чтобы выяснить то же самое, что выяснил этот.

## Почему это не косметика

Это класс «сторож отвечает не на тот вопрос» в его дорогой форме: находка НЕ ложна по
своему контракту (объявленного на origin действительно нет) и при этом НЕ снимаема
никаким действием, кроме создания файла-пустышки с нужным именем — то есть подлога.
Отчёт шага 0a — первое, что читает каждый цикл; нечистая его часть обесценивает чистую
(см. соседний класс `inbox-shag-0a-povtoryaet-odni-i-te-zhe-6-nahod`, там причина ДРУГАЯ —
живой осадок из мёртвых `/tmp`-worktree'ов, содержимое которых не попало в историю; здесь
файла не было вовсе). **Похоже на ту карточку — проверить обе вместе.**

## Что НЕ надо делать (границы, названы заранее)

- **Не вводить горизонт по времени** («старше N суток — не находка»): именно окно
  ожидания уже однажды съело реальную потерю (#233), а класс «работа лежит в /tmp неделю»
  живой. Возраст — не признак ложности.
- **Не давать сессии право снять свою находку задним числом**: это fail-OPEN внутри
  fail-CLOSED-сторожа, ровно тот дефект, который чинили в #226.
- Не создавать файл-пустышку, чтобы «покрасить» отчёт.

## Направление (гипотеза, не решение)

У объявления нет способа сказать «результат вышел под другим именем». Кандидат —
**явная связка**: закрывающее объявление (`--card-state done`) уже перечисляет фактически
доставленные файлы; если сторож при разборе видит, что ТА ЖЕ сессия позже объявила
закрытие с непустым списком, попавшим на origin, то расхождение имён — это
**переименование, а не потеря**, и должно докладываться отдельной строкой («объявлено X,
доставлено Y — сверьте») вместо «НЕ ДОСТАВЛЕНО». Для `pid16782` это бы не помогло:
сессия умерла до закрывающего объявления. Значит нужна и вторая половина — правило
«объявляй результат при смене гипотезы», либо признание, что у R&D-объявления имена
предварительны by design, и такие сессии помечаются флагом.

**Мерить до правки:** сколько сейчас находок шага 0a относится к именам, которых не было
никогда, против имён, которые есть в другом дереве. Число решает, стоит ли чинить вообще.

## Как понять, что готово

Шаг 0a про такой случай печатает суждение, отличающее «переименовано» от «потеряно»,
и это закреплено тестом в ОБЕ стороны: настоящая потеря по-прежнему находка (положительный
контроль), переименование — отдельная строка. Ни один существующий тест не ослаблен.

## Контекст

- Работу `pid16782` (#54 ETS + #55 CSF) поднимали **четыре цикла подряд, и все умерли до
  пуша**: #239 (`pid84190`), #240 (`pid99441`) — оба перемерили числа и не доставили ничего;
  доставил её цикл **#241**, числа реестра перемерены им ЧЕТВЁРТЫЙ раз своим прогоном
  (совпали полностью). Строка «доставил цикл #240» в первой редакции этой карточки была
  **неверна**: её писал сам #240 авансом, до пуша, которого не случилось — ровно тот случай,
  ради которого протокол запрещает верить отчёту сессии на слово. Сама эта карточка тоже
  четырежды оставалась недоставленной.
- `scripts/edge_risk_shape_budget.py` доставлять НЕ надо: идея под этим именем не
  существует, её место заняла #55 CSF.

---

## Закрыто циклом #243 (2026-08-15, worktree от `origin/main` 6921981b3)

**Сначала замер, как требовала карточка** («число решает, стоит ли чинить вообще»). Весь
журнал, 852 записи, 37 живых рабочих деревьев:

| Что | Сколько |
|---|---|
| находок `ABSENT` всего | **42** |
| из них — имени не существовало НИГДЕ | **38** |
| настоящая недоставленная работа | **2** (`scripts/edge_criterion_consensus.py` + тест, лежат в `/tmp/spa_wt_rnd49`) |
| путь жил на origin и был удалён | 1 |
| не измерено | 1 |

То есть карточка недооценила класс в 19 раз: не два файла, а **38 из 42 — девять десятых
раздела «НЕ ДОСТАВЛЕНО»**. Ровно тот механизм, которым сторожа глохнут: следующая сессия
учится пролистывать раздел целиком, и настоящая пара находок (единственная, где ЕСТЬ что
поднимать) уезжает вместе с осадком.

**Что сделано.** У находки `ABSENT` спрашивается третий вопрос — существовало ли имя хоть
где-нибудь (`never_existed`): история базового ref для этого пути · файловая система КАЖДОГО
живого рабочего дерева. Молчат оба ⇒ отдельный раздел «🏷 ОБЪЯВЛЕНО, НО НЕ СУЩЕСТВУЕТ НИГДЕ»
с вердиктом **«поднимать нечего»**. Вторая половина, тоже из карточки: подсказка «эта же
сессия объявляла и ДОСТАВИЛА …», закрывающее объявление (`--card-state done`) читается первым.
На живых данных она отвечает дословно на вопрос карточки:

- `spa_core/tests/test_check_card_claim.py` (объявила `cycle-53284`) → доставлено
  `spa_core/tests/test_card_claim_guard.py` — переименование видно глазом;
- `scripts/edge_risk_shape_budget.py` (объявила `pid16782`) → доставлено
  `scripts/edge_event_time_scoring.py` + тест, то есть #55 RSB → #55 CSF, как и говорила карточка.

**Все три границы карточки соблюдены и проверены тестами:** горизонта по времени НЕ введено ·
права снять свою находку задним числом у сессии не появилось · файлов-пустышек нет.
**Код возврата НЕ смягчён:** «нигде» по-прежнему даёт 1, а не 0 — пути к «✅ всё доставлено»
через этот класс не заведено (мутация M3 это краснит).

**Сверх карточки — измерено и названо.** 5 из 38 «путей» содержат пробел или запятую:
объявление слепило несколько файлов в ОДНУ строку, и такого файла не существует по
построению. Теперь это сказано в самой находке, а не отправляет искать. Побочно усилена и
настоящая находка: раз деревья всё равно опрошены, она НАЗЫВАЕТ дерево, где лежат байты
(`… но он ЛЕЖИТ в: /private/tmp/spa_wt_rnd49 — это настоящая недоставленная работа, её надо
поднять`), вместо прежнего «на origin нет; локально тоже нет».

**Живой результат:** раздел «НЕ ДОСТАВЛЕНО» по всему журналу 42 → **4**, и обе настоящие
потери в нём теперь видны, а не тонут.

**Приёмка.** +12 тестов, 87 в наборе. Контроль на чистом `origin/main` того же sha
(`6921981b3`): **12 краснеют**, ключевые — на ПОВЕДЕНИИ (`assert rep["findings"] == []` против
непустого списка), а не на отсутствии ключа; 1 объявленный обратный контроль
(`test_lost_work_lying_in_a_worktree_is_still_a_finding`) зелёный на ОБОИХ деревьях намеренно —
им и доказывается, что настоящая потеря не переехала в «поднимать нечего».
4 мутации, каждая краснит свою цель: деревья не опрашиваются → 20 (в т.ч. соседние наборы) ·
неизмеренная история сворачивается в «нигде» (fail-OPEN) → 1 · «нигде» не влияет на код
возврата → 2 · подсказка собирает НЕдоставленное → 2.

**ИНВ. #16.** Один существующий тест изменён НАМЕРЕННО:
`test_declared_file_missing_everywhere_says_so_verbatim` → `…_is_judged_nowhere_not_undelivered`.
Вход не изменился ни на байт; изменился ожидаемый ВЕРДИКТ — прежняя редакция утверждала ровно
то поведение, которое эта карточка называет дефектом, и проверяла его слабо (подстрока
«локально» в тексте). Проверка УСИЛЕНА: утверждаются место записи, вердикт, ненулевой код
возврата и отсутствие записи в `findings` — то есть что находка не исчезла в тишину.
Обоснование в теле теста и в журнале W33.

**Не тронуто:** RiskPolicy, kill-switch, живой трек, `data/`, флот, `landing/`, деплой.
