---
trackerStatus:
  type: inbox
title: Храповик считает упоминание в докстринге за проводку — и это лишь одна из трёх слепот
status: done
source: nimbalyst
created: 2026-08-14
claimed_by: pid66130
claimed_at: 2026-08-16T03:49:44Z
---

> **ЗАКРЫТА 17.08 (цикл #278): посылка карточки УСТАРЕЛА, вот доказательство.**
> Все три слепоты закрыты (#255 сделал пункты 1, 2, 4; сведение двух реализаций
> 17.08 добавило строгий суд однофамильца, границы слова с ОБЕИХ сторон и разбор
> протокольных команд). Пункт 3 живёт отдельной карточкой
> `inbox-sem-skriptov-vskrytyh-strogim-skanerom-r` и здесь не дублируется.
>
> Замер на песочнице, живой код `spa_core/tests/_unwired.py::scripts_without_caller`,
> шесть подставных пирамидок — по одной на каждый класс плюс обратные контроли:
>
> | подставная «проводка» | класс карточки | сирота? | ожидалось |
> |---|---|---|---|
> | имя только в докстринге чужого модуля | 1 | **да** | да |
> | имя только в `#`-комментарии | 1 (#227) | **да** | да |
> | однофамилец вне `scripts/` упоминает сам себя | 2 | **да** | да |
> | имя — подстрока `dfb_<имя>.py` | 3 | **да** | да |
> | `subprocess.run([… "scripts/<имя>.py"])` | обратный контроль | нет | нет |
> | голый `import <имя>` по `sys.path` | обратный контроль | нет | нет |
>
> Расхождений ноль. Обратные контроли здесь обязательны: сканер, объявляющий
> сиротой ВСЁ, дал бы ту же зелень — и это была бы ложная сирота, ровно тот исход,
> ради предотвращения которого карточка и предписывала порядок «сперва научить
> видеть, потом отнимать». Код не менялся: честное доказательство, что работа уже
> сделана, дешевле выдуманной работы поверх неё.

## Что нашли

Цикл #227 сделал сканер неподключённых скриптов (`spa_core/tests/_unwired.py`)
**комментарио-слепым**: упоминание имени скрипта в комментарии больше не считается
вызовом. Измеряя эффект, замерил и **обратную сторону — и она шире, чем ожидалось.**

Слепота к комментариям стоила **одного** скрипта (`daily_paper_report`, списан тем же
циклом). Но «подключёнными» ещё **восемь** держит не вызов, а одна из **трёх разных**
слепот. Их нельзя чинить одной правкой — у каждой своя цена ошибки.

## Три класса, замер 14.08 на дереве `881893a3d`

**1. Докстринг = комментарий по последствиям (5 из 8).** Прозаическое упоминание
`scripts/<имя>.py` внутри `"""..."""` неотличимо от вызова, потому что литералы
сканер сохраняет НАМЕРЕННО: в литерале живёт настоящий вызов
(`subprocess.run(["python3", "scripts/x.py"])`). Кто держится за докстринг:

| скрипт | держит |
|---|---|
| `audit_tier_c_wiring_feasibility` | шапка сгенерированного `spa_core/analytics/_protocol_key_coverage.py` |
| `smoke_test_flagship` | шапка `scripts/smoke.py` («в отличие от …») |
| `run_stress_tests` | докстринг `spa_core/stress/stress_engine.py` |
| `perf_budget` | шапка `scripts/dfb_perf_budget.py` (подстрочная коллизия: `perf_budget` ⊂ `dfb_perf_budget`) |
| `check_tracker_drift` | докстринги `spa_core/owner_queue/queue.py`, `tracker_status_sentinel.py` |

**2. Однофамилец в другом каталоге (2 из 8).** Файл с ТЕМ ЖЕ именем вне `scripts/`
упоминает сам себя — и этого хватает, чтобы скрипт числился вызванным:

- `scripts/day30_review.py` ← держит `spa_core/riskwire/day30_review.py`;
- `scripts/ots_anchor.py` ← держит `spa_core/audit/ots_anchor.py`.

Ни один из двух модулей скрипт не зовёт. Это не докстринг и правкой из класса 1
не лечится: нужен запрет считать проводкой самоупоминание однофамильца.

**3. Подстрочная коллизия (внутри классов 1–2).** Имя скрипта — подстрока другого
имени: `perf_budget` ⊂ `dfb_perf_budget.py`, `run_backtest` ⊂ `run_backtest_real.py`
и `run_backtest_tier1.sh`. У `run_backtest` при этом есть и НАСТОЯЩИЙ вызов
(`scripts/backtest_s44.py`: `from scripts.run_backtest import …`) — то есть коллизия
маскирует не всегда пустоту, и рубить по ней вслепую нельзя.

## Почему это НЕ починено тем же циклом — и почему торопиться опаснее

Сделать сканер докстринго-слепым в лоб = получить **ложную сироту**. Живой пример:
`check_tracker_drift` действительно подключён — `scripts/orchestrator_queue.py:198`
делает `import check_tracker_drift as drift`, — но сканер ищет только `<имя>.py` и
`scripts.<имя>`, а **голый `import <имя>` по `sys.path` он не видит вовсе**. Убери
докстринги — и живой, ежедневно исполняемый скрипт объявится мёртвым.

Значит порядок обязан быть такой: сперва научить сканер ВИДЕТЬ все формы проводки
(голый импорт, `runpy`, `-m scripts.<имя>`), и только потом отнимать у него
доказательства слабее вызова. Иначе храповик начнёт краснеть на честной работе — тот
самый исход, который в этом проекте уже признан хуже пропуска (страж номеров ADR, #183).

## Что сделать

1. Расширить набор форм проводки и закрепить каждую тестом: `import <имя>`,
   `python3 -m scripts.<имя>`, `runpy.run_path`, вызов через обёртку.
2. Только после этого — докстринго-слепота (`ast`: докстринг Module/Class/Func —
   прозa; прочие литералы ОСТАВИТЬ) + запрет самоупоминания однофамильца.
3. Разобрать вскрывшиеся скрипты ПОШТУЧНО: подключить или списать. **В базу
   `unwired_scripts_baseline.json` не дописывать** — это запрещено самим храповиком.
4. Положительные контроли в обе стороны: докстринг → сирота; голый импорт → не сирота;
   однофамилец → сирота. Мутация каждой — красит свой тест.

## Границы

Только тестовый слой. RiskPolicy, kill-switch, живой трек, флот и деплой не затрагиваются.
Починка делает храповика СТРОЖЕ (подопечных станет больше), ослабления в ней нет.

## Как понять, что готово

Голый импорт виден как проводка · докстринг и самоупоминание однофамильца — нет ·
восемь скриптов разобраны поштучно · база не выросла · возврат любой из трёх правок
красит поимённый тест.

---

## Что сделано циклом #255 (16.08) — пункты 1, 2, 4

Порядок карточки исполнен буквально: **сперва научить видеть, потом отнимать**.

1. **Формы проводки** вынесены в `wiring_patterns()` и расширены: голый `import <имя>` /
   `from <имя> import` по `sys.path` (форма, которой сканер не видел ВОВСЕ),
   `-m scripts.<имя>`, `runpy.run_path`, плюс **границы слова с обеих сторон** —
   `perf_budget` больше не ловится хвостом `dfb_perf_budget.py`, `scripts.run_backtest` —
   началом `scripts.run_backtest_real`.
2. **Только после этого** отняты доказательства слабее вызова: докстринг Module/Class/Func
   вырезается по `ast` (прочие литералы СОХРАНЕНЫ — в них живёт настоящий вызов), а у
   однофамильца засчитываются лишь формы, которые нельзя написать про себя
   (`scripts/<имя>.py`, `scripts.<имя>`).
   **Ложная сирота, о которой предупреждала карточка, не случилась:** `check_tracker_drift`
   держится докстрингами, зовут его голым импортом — проверено отдельно, он остался
   подключённым.
4. **Положительные контроли в обе стороны** — `spa_core/tests/test_unwired_wiring_forms.py`
   (+14 проверок). Пять мутаций, каждая красит СВОИ тесты поимённо: снять докстринги — 5,
   снять границы — 2, снять голый import — 3, снять правило однофамильца — 1, вернуть
   байтовую колонку — 1. `test_a_docstring_mention_still_counts_KNOWN_GAP` не удалён, а
   **развёрнут** в противоположную сторону (инв. #16, по предписанию его собственного
   докстринга).

**Собственный тест поймал настоящую ошибку правки:** колонки `ast` — в БАЙТАХ utf-8, а
докстринги здесь кириллические; затирание по байтовой колонке уезжало на строку вперёд и
съедало `from scripts.x import` СЛЕДОМ за докстрингом — то есть ровно ту аварию (живой
скрипт объявлен сиротой), ради предотвращения которой карточка и предписывала порядок.

## Что осталось — пункт 3, вынесен в `inbox-sem-skriptov-vskrytyh-strogim-skanerom-r`

Замер сошёлся с карточкой не полностью: вскрылось **семь** имён, а не восемь
(`edge_*`-скрипты из класса R&D остались под правилом реестра, цикл #214).

**Пункт «база не выросла» НЕ исполнен, и это названо, а не обойдено.** Разбор «подключить
или списать» автономному заходу недоступен: мёртвых по содержанию среди семи нет
(`reap_stale_worktrees` протокол требует каждый цикл, `day30_review` — обёртка над живым
модулем, `perf_budget` — гейт money-path), а настоящая проводка `ots_anchor` означает
установку агента launchd — **деплой, запрещённый мандатом**. Поэтому рост базы сделан
максимально ВИДИМЫМ вместо тихого: отдельный раздел `revealed_by_stricter_detector`,
у каждого имени назван файл, который его держал, раздел сторожат те же три проверки
храповика плюс две новые (`test_the_two_sections_do_not_overlap` не даёт переложить имя
вместо решения, `test_every_revealed_name_says_WHAT_used_to_hide_it` — дописать имя без
измеренной причины).
