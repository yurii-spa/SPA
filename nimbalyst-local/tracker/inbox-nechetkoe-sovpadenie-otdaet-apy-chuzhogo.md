---
trackerStatus:
  type: inbox
title: "Нечёткое совпадение отдаёт APY ЧУЖОГО актива: USDC.e ранжирует капитал как USDC — и платит БОЛЬШЕ настоящего"
status: done
source: nimbalyst
created: 2026-08-09
claimed_by: cycle-77790
claimed_at: 2026-08-09T23:01:54Z
---

## Что нашлось (цикл #186, при закреплении пинов ADR-076.1)

Разрешение пула двухступенчатое: сначала пин по UUID, затем нечёткий hint
(project + chain + symbol, «побеждает больший TVL»). Контракт про TVL честный и проверен
тестами: **только пин** имеет право на `tvl_source: "live"`, hint оставляет `static`.

Но **APY hint отдаёт всегда** — и вот чего никто не проверял: hint матчит symbol
ПОДСТРОКОЙ, поэтому пул ДРУГОГО актива на той же цепочке ему подходит. Замер живого фида
2026-08-09 (те самые ключи, которые я закреплял):

| ключ | пул, который вернёт hint без пина | актив | APY |
|---|---|---|---|
| `aave_arbitrum` | `7aab7b0f…` $146k | **USDC.e** (мостовой, `0xFF97…5CC8`) | **4.17 %** |
| `aave_v3_polygon` | `37b04faa…` $532k | **USDC.e** (`0x2791…4174`) | **3.18 %** |
| `aave_v3_base` | `974b8732…` $12.4M | **SYRUPUSDC** (symbol СОДЕРЖИТ «USDC») | 0 % |

Обратите внимание: у двух из трёх доходность ВЫШЕ настоящей (4.17 % против 2.36 %,
3.18 % против 2.92 %). То есть подмена не выглядит как поломка — она выглядит как удача.
Сегодня hint выбирает правильный пул только потому, что настоящий крупнее; это не свойство
правила, а свойство текущих чисел. Пины, поставленные циклом #186, закрывают вопрос для
четвёрки Aave — но у 15+ ключей пина нет вовсе, и они ранжируются именно так.

## Почему это важно, а не «и так гейт поймает»

Гейт доказанности (ADR-061) и floor стоят на TVL — а TVL у hint честно `static`, так что
свежий капитал такой пул не возьмёт. Но APY используется НЕ только для входа: рейтинг,
`house_view`, отчёты офиса, триггер ребаланса по улучшению доходности (ADR-060) читают
`live_apy`. Иначе говоря, чужой актив может не получить денег напрямую, но способен
сдвинуть решение о том, откуда деньги ЗАБРАТЬ.

## Что сделать

1. Сузить hint по идентичности актива, а не по строке: сверять `underlyingTokens` с
   ожидаемым адресом (нативный USDC цепочки) — данные уже есть в каждой записи фида.
2. Нет однозначного совпадения ⇒ `None` и названная причина (fail-CLOSED), а не «ближайшее».
3. Тест в обе стороны + положительный контроль: подсунуть USDC.e-пул с более высоким APY —
   должен быть отказ, а не подстановка. Фикстуры уже написаны и лежат готовыми в
   `spa_core/tests/test_feed_pins_aave_non_ethereum.py` (`_ARBITRUM_BRIDGED`, `_POLYGON_LEGACY`,
   `_BASE_SYRUP`) — это реальные записи скана, не выдумка.
4. Отдельно посчитать, у скольких ключей сейчас `pool_match: "hint"`, и назвать их поимённо.

## Границы

Пороги RiskPolicy и лимиты НЕ трогать. Только stdlib, FakeFeed в тестах, адаптеры read-only.
Ужесточение может ОБНУЛИТЬ несколько APY — это допустимый и правильный исход
(`.claude/rules/adapters.md`: нет данных ⇒ `None`, а не выдумка); каждый обнулённый ключ назвать.

Смежное: `agent-find-feeds-for-seven-protocols` (backlog) · цикл #186.

---

## Сделано — цикл #188 (2026-08-10)

**Замер, которого требовал пункт 4.** Нечётко разрешаются ровно **6 ключей**, все на Ethereum,
поимённо: `aave_v3`, `compound_v3`, `euler_v2`, `morpho_blue`, `spark_susds`, `yearn_v3`.
Остальные 16 идут пином, 12 не разрешаются вовсе.

**Пункт 1 — сделано.** `_CANONICAL_UNDERLYING`: hint обязан совпасть по `underlyingTokens`
с объявленным активом. Ключ таблицы — (цепочка, символ) самого hint, а не адаптер, поэтому
новый ключ наследует проверку, а не подключает её отдельно (opt-in — это fail-OPEN).

**Пункт 2 — сделано.** Нет объявленного актива для (цепочка, символ) ⇒ hint отказывает целиком;
причина названа в новом поле `pool_match_refused`. Мульти-токен (LP) отказывается, а не берётся
по первому токену: `CRVUSDCWBTCWETH` держит USDC первым.

**Пункт 3 — сделано.** 16 тестов в `spa_core/tests/test_feed_hint_asset_identity.py`, фикстуры
дословные из живого скана. На чистом `origin/main` файл краснеет: `aave_v3` публикует
**4.7934 %** чужого актива (`0xD4fa…D23E`), при увеличенном самозванце — **9.9 %**.
Отдельное плечо на РЕГИСТР адресов: yearn отдаёт нижний, aave — смешанный.

**Обнулённых ключей — НОЛЬ.** Все шесть по-прежнему разрешаются в те же пулы; ужесточение
сегодня не стоит ни одного APY. Контроль на чистом `origin/main` подтвердил, что расхождения
в песочнице (`stusd`/`wusdm`/`pendle_pt_susde`) — отсутствие carry-forward, а не правка.

**Найдено попутно (не чинил, отдельные карточки):**
1. `morpho_blue` нечётко разрешается в `931ea9be…` — ТОТ ЖЕ пул, что закреплён за
   `morpho_steakhouse`. Два ключа книги на одном пуле — скрытая концентрация, которой
   per-protocol cap не видит; `test_no_two_keys_share_a_pool` сверяет только таблицу пинов.
2. Фикстура `morpho_steakhouse` в `test_adapter_status_generator.py` несла адрес контракта
   вместо UUID, из-за чего два теста «exact UUID match» годами шли через hint. Исправлено
   здесь же (обоснование — журнал W32, инвариант #16).

Границы соблюдены: пороги RiskPolicy и лимиты не тронуты, адаптеры остались read-only,
только stdlib, ни один тест не обращается к сети.
