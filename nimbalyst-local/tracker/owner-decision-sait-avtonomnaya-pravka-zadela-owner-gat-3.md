---
trackerStatus:
  type: owner-decision
title: "Сайт: автономная правка задела owner-gated область — нужно решение"
status: ingested
source: orchestrator
created: 2026-08-08
approves: landing/src/pages/packages.astro
---

## Что случилось и почему это важно
Автономный оркестратор хотел изменить публичный сайт, но правка задевает owner-gated область (числа доходности / нейминг тиров / legal / solicitation). Такое не уезжает в live само — только с твоего одобрения (инвариант #8).

## Что от тебя нужно
Посмотри изменение и выбери:

1. **Одобрить** — правка уезжает в live как есть.
2. **Отклонить (рекомендую)** — оркестратор не трогает эту область; owner-gated поверхность остаётся неизменной, пока ты не решишь иначе.
3. **Отложить** — оставить карточку открытой и вернуться к ней позже.

Что именно меняется:
- Файлы: landing/src/pages/packages.astro
- Коммит-сообщение оркестратора: packages: Aggressive — добавлен ФАКТ «идёт paper-трек» (33 форвардных дня, книги живые). Honesty-токен 'refused for live (by design)' СОХРАНЁН дословно; чисел доходности не добавлено. Balanced НЕ трогаю: у книги 0 профинансированных дней из 40
- Что зафлагано owner-gate линтером:
  - [E] landing/src/pages/packages.astro · honesty.token.removed · status_en: 'RESEARCH · refused for live (by design)', status_ru: 'ИССЛЕДОВАНИЕ · отказан для live (осознанно)',

## Как понять, что готово
Ты нажал кнопку в Телеграме (или написал в карточке «одобряю» / «отклоняю»); при одобрении оркестратор запушит с трейлером `Owner-Approved: <id-карточки>`.

## Что будет после
Одобришь → изменение уезжает в live /dashboard и на сайт. Отклонишь → оркестратор не трогает эту область.

---

## Дополнение оркестратора (цикл #171, 2026-08-09)

### Твоё одобрение от 22:00 исполнить было НЕЧЕМ — теперь механизм работает

Ты одобрил такую же правку вчера в 22:00 (карточка `...owner-gat-2`, вариант 1, телеграм).
Исполнить это штатным путём оказалось невозможно: обход гейта по трейлеру `Owner-Approved:`
был порван в **четырёх** местах подряд, и каждое молчало.

| # | Где | Что было |
|---|---|---|
| 1 | `check_owner_gate._approved_scope` | опечатка `card_type=` вместо `tracker_type=` (починено 08.08 по твоему «варианту А») |
| 2 | `_parse_approves` | список читался одной строкой (починено 08.08 там же) |
| 3 | `_approved_scope` | читал поле `card.frontmatter`, которого у карточки НЕТ (есть `fields`) ⇒ scope всегда пустой |
| 4 | шаблон трейлера | принимал только `own-…`, а гейт сам заводит карточки `owner-decision-…` ⇒ ни одна МАШИННАЯ карточка не подходила |
| 5 | `safe_site_push` | вообще не писал в карточку поле `approves:` — одобрять было нечего |

Я чинил **3, 4, 5** — это доделка того же механизма, который ты уже разрешил чинить
(вариант А). Направление защиты не изменилось: обход по-прежнему требует карточку в статусе
`owner-done` и разрешает **ровно те файлы**, которые гейт заблокировал и показал тебе выше.
Сам себе агент `owner-done` поставить не может (инвариант #14). 10 тестов, 4 мутации краснят
каждая свою цель.

**Эта карточка — первая, у которой одобрение реально сработает** (в шапке появилось поле
`approves:`). Нажми «Одобрить» — и ближайший цикл довезёт правку в live.

### Что я НЕ стал публиковать и почему

Правка, которую ты одобрил вчера, меняла статус ДВУХ тиров. Я проверил факты по данным:

- **Aggressive — правда.** Форвардный трек идёт: 27–39 дней по книгам, капитал живой
  (`pendle_yt_susde` 108 670, `lrt_neutral` 101 113, `lp_eth_stable` остановлен kill-switch).
  Это в правке осталось.
- **Balanced — НЕПРАВДА на сегодня.** Книга пуста: **0 профинансированных дней из 40**
  (`hy_paper_trading.json`) и **0 из 41** (`lp_paper_trading.json`) — ни одной строки с
  equity > 0 с 22.06, при 920 и 931 отработанных циклах. Вчерашний засев ($66 666 + $33 333)
  записан в шапку файла, но строка за 08.08 уже была создана ДО него и не переписывается
  (`if today not in existing_dates` — строка на дату пишется один раз).

Написать на публичном сайте «идёт paper-трек» про пустую книгу — это ровно то, что запрещает
инвариант #8. Поэтому Balanced я оставил как есть. Отдельная карточка про сам простой книги:
`inbox-sbalansirovannyi-trek-nol-profinansirova`.

**Что будет после:** одобришь — уедет только честная половина (Aggressive). Про Balanced
вернусь, когда у книги появятся реальные профинансированные дни (ожидаемо со следующего
UTC-дня).

---

## Снято: владелец решил это в интерактиве (цикл #171, позже той же ночью)

Пока этот цикл готовил карточку, ПАРАЛЛЕЛЬНАЯ сессия задала тот же вопрос владельцу вживую и
получила «да, обновить». Правка уехала в live коммитом `39adb9d6f` (2026-08-09 00:30 местного).
Спрашивать второй раз не о чем — карточка закрыта как отработанная.

**Но опубликовано БОЛЬШЕ, чем этот цикл считал честным.** Уехали статусы ОБОИХ тиров, включая
`Balanced · идёт paper-трек · без live-аллокации`. По данным на момент публикации у книги
Balanced **0 профинансированных дней из 40** и **0 из 41** — замер в
`inbox-sbalansirovannyi-trek-nol-profinansirova`. Решение владельца я не отменяю и сайт не
трогаю: это его поверхность и его выбор. Но факт зафиксирован, и проверить его надо фактом —
со следующего UTC-дня у книги обязаны появиться строки с `equity > 0`.
