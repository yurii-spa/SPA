---
trackerStatus:
  type: inbox
title: Агент может написать owner_choice — ответ владельца подделывается без единого отказа
status: done
source: nimbalyst
created: 2026-08-31
claimed_by: pid91998
claimed_at: 2026-08-31T06:10:54Z
claim_takeover_reason: захват принадлежал циклу #437b (cycle-42676), который эту карточку СОЗДАЛ; его работа доставлена — квитанция снятия /tmp/spa_c437b от 2026-08-31T03:25:00Z перечисляет этот файл как delivered, ADR-186/187 на origin. Поднимать нечего, работа по карточке не начиналась
status_trail:
  - "2026-08-31T06:35:03.583095+00:00 new -> done · queue.set_status · cycle-91998"
---

## Что случилось и почему это важно

Поле `owner_choice` в карточке решения — это ЗАПИСЬ ОТВЕТА ВЛАДЕЛЬЦА. Замер 31.08
(цикл #437) показал, что написать в него может кто угодно, и один раз это уже
случилось: `git show 765363a8e` (2026-08-29 14:41Z) — карточка
`owner-decision-tier-steakhouse-2026-08-29` была `needs-owner` с `owner_choice: ""`,
владелец ещё не отвечал, и сессия одним коммитом поставила `status: ingested`
и `owner_choice: "2"`. Проза того же коммита говорит обратное («Выбран вариант 1…
Вариант 2 — НЕ сделан»). Владелец ответил на 6 ч 20 мин позже кнопкой — вариантом 1.

Цена уже заплачена: обязательный шаг 0-офис несколько прогонов подряд печатал
«⛔ ДВА РАЗНЫХ ОТВЕТА ВЛАДЕЛЬЦА, нужен человек» на состоянии, где ответ владельца
один. ADR-186 научил СТОРОЖА отличать безымянную запись от ответа — но писать её
по-прежнему можно, значит класс может повториться.

Замер по всей популяции `origin/main`: карточек 832, с непустым `owner_choice` — 86,
без единого признака авторства — ровно одна, эта.

## Что от тебя нужно

Автономная задача агента (hardening, не owner-gated, risk-логики не касается).

1. Закрыть запись `owner_choice` на стороне ПИСАТЕЛЯ: единственный законный путь —
   `spa_core/owner_queue/owner_answer.record_owner_answer` (он же всегда пишет
   провенанс). Любая другая запись поля — отказ, как `OwnerDoneForbidden` для
   `owner-done` (инвариант #14): тот же вопрос, та же форма ответа.
2. Проверить ВСЕ двери, а не одну: `orchestrator_queue.set_status`, ручная правка
   карточки сессией, мост находок. Дверей у чата владельца оказалось пять (#218) —
   считать двери здесь тоже надо, а не полагаться на одну.
3. Отдельный вопрос, решать НЕ молча: что делать с уже стоящим `owner_choice: "2"`
   на origin. Перенос следа умеет только ДОписывать
   (`inbox-perenos-sleda-umeet-tolko-dopisyvat-pust`), заменой строки на месте он не
   владеет.
4. Положительный контроль: тест воспроизводит коммит `765363a8e` (агент пишет
   `owner_choice` в карточку `needs-owner`) и КРАСНЕЕТ при возврате правки.
   Обратный контроль: штатный писатель по-прежнему пишет поле беспрепятственно.

## Как понять, что готово

Попытка агента записать `owner_choice` мимо `record_owner_answer` отказывает с
названной причиной; `record_owner_answer` работает как прежде; тесты зелёные.

## Что будет после

Класс закрыт с обеих сторон: сторож его РАЗЛИЧАЕТ (ADR-186), писатель его НЕ ДОПУСКАЕТ.

---

## Исполнено — цикл #439, 2026-08-31 (ADR-191)

**Пункт 1 (закрыть запись на стороне писателя) — сделано, но НЕ там, где предлагала карточка.**
Двери посчитаны замером, а не приняты на слово: в рантайме `owner_choice` пишет РОВНО ОДИН
модуль — `spa_core/owner_queue/owner_answer.py`; `queue.set_status` переписывает только строку
`status:`, `create_card` рождает поле пустым, `findings_bridge` / `card_delivery` /
`orchestrator_queue` его только ЧИТАЮТ. Заслон на `set_status` был бы сторожем у двери, в
которую никто не ходит. Авария 765363a8e прошла через ПУШ — там интерлок и стоит
(`push_to_github.enforce_owner_choice_authorship`, код 8, `scripts/check_owner_choice_authorship.py`).

**Пункт 2 (проверить ВСЕ двери) — двери доставки тоже две**, и это тот же капкан, что с
ADR-145: реализация одна, зовут её ОБА CLI (`push_to_github.py` и `push_to_github_batch.py`,
под которым стоит `safe_site_push.py`). Проверяется ФОРМОЙ вызова, а не упоминанием имени.

**Пункт 3 (что делать со стоящим `owner_choice: "2"`) — решено и исполнено, не молча.**
Без маршрута починки интерлок оставил бы находку, которую нельзя закрыть НИКАК, а проверку,
мешающую верной работе, обходят флагом. Заведён узкий именованный маршрут
`repair_unattributed_choice` (+ команда `--repair-unattributed`): нужен ПОЛНЫЙ провенанс у нас
и ПОЛНОЕ его отсутствие на origin. `owner-decision-tier-steakhouse-2026-08-29` починена:
`"2"` → `1` (ответ владельца от 29.08 21:00:44Z с его провенансом), снятое значение записано в
`owner_choice_unattributed_removed` — не в регистр вытеснения: тот означал бы, что владелец
однажды ответил «2», а он не отвечал. Тело решения не тронуто байт в байт; сторож после
починки говорит «след уже на origin» (IDLE) вместо пятого исхода.

**Пункт 4 (контроли).** 36 тестов, `spa_core/tests/test_owner_choice_writer_gate.py`; байты
аварии вшиты дословно (в CI дерево мелкой глубины, `git show 765363a8e` там не ответит).
19 мутаций по координате — краснеют все 19.

**Замер ДО написания правила:** `origin/main` 845 карточек / 88 с ответом / **0** отказов;
прод-дерево 572 / 73 / **0**; блоб аварии — **1** отказ. Одно истинное красное, ноль ложных.

**Граница названа честно:** гейт кооперативный — подделавший заодно поля провенанса пройдёт.
Планка выбрана замером, а не на глаз.

Решение: **ADR-191**.
