---
trackerStatus:
  type: agent
title: Сетевая икота фида может эвакуировать книгу в кэш — нужна политика устаревания вместо «есть/нет»
status: done
source: session-2026-08-04 (найдено при ремонте прода; приоритет №1 в docs/STRATEGY_NEXT_2026-08.md)
created: 2026-08-04
priority: critical
domain: money-path (гейт доказанности ADR-061 + фиды)
---

> **ЗАКРЫТО 2026-08-08 — проверено решающим экспериментом, а не по статусу.**
>
> Симуляция ПОЛНОСТЬЮ молчащего фида (`_fetch_pools` возвращает пустоту):
>
> ```
> наблюдается: 25 из 34 — перенос последних наблюдений сработал
> КНИГА: pendle 20k · morpho_blue_base 20k · maple 10k · morpho_steakhouse 40k · compound_v3 5k
> кэш: 5.0 %  ⇒  книга УДЕРЖАНА, эвакуации нет
> ```
>
> Требование карточки выполнено: наблюдение живёт 36 часов от момента, когда было СДЕЛАНО
> (`live_apy_as_of`, не от записи файла), одна неудачная загрузка не превращается в движение
> капитала, а «фид не ответил» отличается от «протокол не наблюдаем» полем `live_apy_fresh`.
>
> Ничего не чинил: работающее не трогают. Карточка висела открытой, вероятно закрыта работой,
> которую не отметили — проверил фактом, а не по статусу.


## Что случилось (реальный случай, не гипотеза)

2026-08-04 в 15:14 UTC `adapter_status_generator` **один раз** не достучался до DeFiLlama
(таймаут 5 с). Результат: `live_apy: null` у **всех 34 адаптеров**, `live_count: 0`.

Гейт доказанности (ADR-061) при этом отработал ровно как задумано — не пустил в книгу ни один
непроверенный протокол. Но это значит, что **один неудачный HTTP-запрос = эвакуация книги в кэш**.
В тот раз повезло: цикл в этот момент не шёл, и повторная генерация вернула 14 протоколов.

## Почему это опаснее, чем выглядит

Сегодняшняя логика бинарна: наблюдение есть сейчас или его нет. Между «протокол перестал быть
наблюдаемым» и «сеть моргнула» разницы не делается — а это разные события с разными правильными
реакциями. Fail-CLOSED защищает от вранья, но в бинарном виде превращает сетевую икоту в
движение капитала.

## РЕШЕНИЕ ВЛАДЕЛЬЦА ПОЛУЧЕНО (2026-08-29) — вариант 1, ADR-167

Вторую ступень делать **дословно как записано**: наблюдение старше **168 ч** ⇒
де-риск-канал, держимая позиция принудительно сокращается. Число 168 ч сохраняется.

Рамки из ADR-167, которые нельзя нарушить при реализации:

- порог 36 ч для новой аллокации **не трогать**;
- сокращение — штатным ребалансом с уважением к потолкам, **не** forced-sell;
- де-риск-канал **не спрашивает экономику**: «ход не окупается» не отменяет
  сокращение по слепоте — экономика влияет на ТЕМП, не на факт;
- **массовая слепота ≠ рынок.** Если ненаблюдаемыми стали ВСЕ протоколы разом,
  это симптом нашей поломки (урок 04.08: одна сетевая икота обнулила `live_apy`
  у 34 адаптеров) — поднимать тревогу, а не эвакуировать книгу;
- срабатывание считать по ВОЗРАСТУ наблюдения, а не по «последний запрос не удался»;
- каждое срабатывание обязано назвать протокол, возраст и сокращаемую сумму.

Приёмка: положительный контроль, краснеющий на коде без де-риск-канала, плюс
отдельная проверка, что массовая слепота эвакуацию НЕ запускает.

## Что нужно

1. **Наблюдение живёт N часов.** У значения есть `as_of`; протокол остаётся наблюдаемым, пока
   возраст ≤ `EVIDENCE_MAX_AGE_H` (ADR-060 §3 предлагает 36 ч в paper). Одна неудачная попытка
   не должна обнулять вчерашнее наблюдение.
2. **Повторы с отступом** в производителе + честный таймаут (сейчас 5 с на весь DeFiLlama).
3. **Отдельный сигнал «фид не отвечает» ≠ «протокол не наблюдаем».** Первое — инцидент
   инфраструктуры (алерт, не движение капитала); второе — законное основание не финансировать.
4. **Защита от массового обнуления:** если доля наблюдаемых протоколов падает более чем на X %
   за один цикл — это признак отказа фида, а не рынка ⇒ держать предыдущую книгу и кричать,
   а не перекладывать капитал.

## Как понять, что готово

Тест: производитель получает сетевую ошибку ⇒ вчерашние наблюдения остаются валидными в пределах
окна, книга не меняется, поднимается алерт «фид недоступен». Тест: наблюдение старше окна ⇒
протокол честно выходит из свежей аллокации.

## Связанные

ADR-061 (гейт доказанности) · ADR-060 §L0 (окна `EVIDENCE_MAX_AGE_H` / `EVIDENCE_HARD_AGE_H`) ·
`docs/STRATEGY_NEXT_2026-08.md` п.2.2 · `agent-gsm-hours-producer.md`
