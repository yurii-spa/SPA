---
trackerStatus:
  type: owner-decision
title: "Решение по алерту: ⚠️ SPA 7-Day Checkpoint FAILED: 1 check(s)"
status: needs-owner
source: telegram-alert-button
created: 2026-08-08
alert_id: 0cac55af
alert_kind: problem
---

## Что случилось и почему это важно

Пришёл алерт (проблема), и ты нажал «Нужно моё решение». Значит, дальше без тебя двигаться нельзя — сам алерт ниже дословно.

```
⚠️ SPA 7-Day Checkpoint FAILED: 1 check(s)
  • gap_check: Gap in paper_evidence: 2026-06-21 → 2026-06-30 (9 days)

=== SPA 7-Day Checkpoint (2026-06-19) ===
Days tracked:   50/30
Gap-free:       ❌ NO — Gap in paper_evidence: 2026-06-21 → 2026-06-30 (9 days)
Equity:         $100,791.39 (+0.791%)
APY (7d):       5.7%
PROMOTE ready:  — (no strategy >= 1.0 yet)
Kill-switch:    ✅ NOT triggered
GoLive status:  ✅ PASS
⚠️  S7 Sharpe=0.00 < 0.8 (warning)
```

## Что от тебя нужно

Ответь прямо в этой карточке, что делать: починить (и как), оставить как есть, или это ожидаемо и тревога лишняя. Если проблема касается risk-логики, kill-switch, порогов или живого трека — агент не имеет права решать это сам, поэтому и спрашивает.

## Как понять, что готово

В карточке есть твой ответ, и ты перевёл её в `owner-done`.

## Что будет после

Оркестратор прочитает ответ, заведёт из него задачу (или закроет вопрос), выпишет ADR, если решение архитектурное, и переведёт карточку в `ingested`.

_Заведено кнопкой под алертом (`alert_actions`), алерт `0cac55af` от 2026-08-08T13:08:39.935669+00:00._

---

## Измеренный диагноз (цикл #166, 2026-08-08) — добавлен агентом, решение по-прежнему за тобой

Проверил, ничего не меняя (только чтение). **Твой трек цел.** Тревога говорит правду о другом.

**Коротко:** «дырка в 9 дней» — не в треке, а во ВТОРОЙ, вспомогательной книге. Чекпойнт судит
о непрерывности трека по ней, а не по канону, и поэтому пугает не тем.

| Что смотрели | Дней | Разрывы |
|---|---|---|
| **Канон — `data/equity_curve_daily.json`** | 46 evidenced (от якоря 2026-06-22) | только 19.07 и 27.07 — честные отказы `no_live_data`, известны, by design |
| Вспомогательная — `data/paper_evidence.json` | 50 записей (от 2026-06-10) | **06-21 → 06-30 (нет 8 дней)** + те же 19.07 и 27.07 |

Дни 2026-06-22…06-29 в каноне на месте все до одного — это первые дни после якоря, и они целы.
Расходятся не дни, а два реестра: `scripts/checkpoint_7day.py::check_gaps` читает
`paper_evidence.json`, отсюда и «Days tracked: 50/30» вместо канонических 46.

## Что от тебя нужно — выбрать одно

1. **Чекпойнт начинает судить по канону** (`equity_curve_daily.json`) — та же книга, по которой
   считается go-live. Тревога перестаёт звенеть на пустом месте, а настоящий разрыв трека
   по-прежнему поймает. Вторая книга остаётся как есть.
2. **Достроить `paper_evidence.json`** до канона за 06-22…06-29 (в репозитории для этого есть
   `scripts/reconcile_paper_evidence.py`). Это **запись в данные трека**, поэтому агент сам
   этого не делает ни при каких условиях.
3. **Оставить как есть** — тогда алерт «7-Day Checkpoint FAILED» будет приходить каждый раз,
   и через месяц на него перестанут смотреть.

**Рекомендация агента — вариант 1** ⭐. Дефект в том, ЧТО ИМЕННО меряет сторож, а не в данных:
он назван «gap in track», но читает не тот реестр. Вариант 1 чинит именно это и не трогает
ни одной цифры трека. Вариант 2 можно сделать потом отдельно — он про порядок во второй книге,
а не про правду о треке.

## Как понять, что готово

Ты ответил в этой карточке и перевёл её в `owner-done`.

## Что будет после

Вариант 1 — правка `checkpoint_7day.py` + тест в обе стороны (на каноне с настоящим разрывом
краснеет, на нынешнем каноне зелен), ADR при необходимости. Вариант 2 — отдельная карточка,
касается данных трека, поэтому только с твоего явного разрешения и с резервом до правки.
