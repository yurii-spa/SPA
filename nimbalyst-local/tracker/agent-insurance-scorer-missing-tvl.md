---
trackerStatus:
  type: agent
title: Красный тест: страховой скоринг не отказывает при отсутствующем TVL
status: done
source: session-2026-08-05 (найдено попутно, предсуществующее)
created: 2026-08-05
priority: medium
domain: risk-scoring (advisory-слой)
---

## Факт

`spa_core/tests/test_protocol_insurance_scorer.py::TestValidation::test_missing_tvl_usd` — **красный**.
Тест ожидает `ValueError` при отсутствующем `tvl_usd`; исключение не поднимается.

Проверено, что падение **предсуществующее**: воспроизводится в чистом дереве без каких-либо правок
этой сессии. Умышленно не тронут (правило 16 — тест не ослаблять молча).

## Почему это тот же класс

Ровно то же, что ADR-064: величина, которой нет, обрабатывается как приемлемая. Здесь последствие
мягче (слой advisory, капитал не двигает), но направление отказа то же — fail-OPEN там, где
задумывался fail-CLOSED.

## ЗАКРЫТО 2026-08-05 — исправлено выше по потоку

На свежем origin файл зелёный: `test_protocol_insurance_scorer.py` — 83 теста пройдено.
Красный наблюдался только в рабочем дереве со старой базой. Своего вмешательства не
потребовалось; тест не трогали.

## Что предлагалось

1. Понять, что изменилось: перестал ли валидатор проверять поле, или тест описывает контракт,
   который сознательно отменили.
2. Если контракт в силе — починить код, а не тест.
3. Если контракт отменяли осознанно — обоснование в теле изменения + запись в
   `docs/journal/<неделя>.md` (правило 16), и только тогда правка теста.
