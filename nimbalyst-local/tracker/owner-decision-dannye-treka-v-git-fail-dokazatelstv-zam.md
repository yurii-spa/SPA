---
trackerStatus:
  type: owner-decision
title: "Данные трека в git: файл доказательств заморожен на 21.06, файла бэктест-гейта нет вовсе — реши, что коммитить"
status: needs-owner
source: nimbalyst
created: 2026-07-29
priority: medium
---

## Что случилось и почему это важно

Разбирал красные тесты (карточка `agent-ci-data-dependent-red-tests`, цикл #32) и упёрся в две
вещи, которые решать не мне — это данные о треке, а их я трогать не имею права.

1. **В git лежит замороженный файл доказательств трека.** `data/paper_evidence.json` в
   репозитории содержит **12 дней, последний — 21.06**. На твоём Маке в этом же файле —
   **40 дней, последний 29.07**. То есть публичная/репозиторная копия доказательств отстала
   на пять недель: живой цикл пишет её локально, но в git она не уезжает.
   Прямое следствие: проверка «в треке не меньше 13 честных дней»
   (`tests/test_evidence_seeded.py::test_real_days_present`) на чистой копии репозитория
   **красная** — и она права, файл в git действительно куцый. Я её НЕ трогал (менять порог =
   молча ослабить проверку доказательств, запрещено).

2. **Файла результата бэктест-гейта в git нет вообще.**
   `data/backtest/pre_paper_backtest_gate.json` (у тебя локально `status=PASS`) не
   закоммичен — он подпадает под общее правило «`data/**/*.json` не в git». Из-за этого
   **20 проверок** (в т.ч. сторож «оценка гейтов не проваливается ниже 16/20») на чистой копии
   и в CI не выполняются вообще — теперь они честно помечены «пропущено: нет такого-то файла»
   (раньше было написано «пропущено, потому что это CI», что скрывало причину).
   Живой рабочий смысл: этот сторож регрессий **никогда не срабатывает в CI** — он проверяет
   только твою машину.

Похожая тема уже есть в карточке `agent-ci-data-dependent-red-tests` (она про отсутствующие
файлы). Эта — про то, ЧТО из данных вообще должно лежать в git; поэтому отдельно и тебе.

## Что от тебя нужно

Выбери по каждому пункту.

**Пункт 1 — устаревшие доказательства трека в git:**
- **A (рекомендую).** Разрешить агенту регулярно коммитить `data/paper_evidence.json` вместе
  с дневным циклом (как уже коммитится `data/equity_curve_daily.json`), — но **только после**
  того, как ты закроешь открытую карточку про выдуманные 10.115 %
  (`owner-decision-v-zhurnalah-dohodnosti-zapisany-vydumann`): иначе в git уедут и пять
  выдуманных записей. Порядок: сначала решение по выдуманным записям, потом синхронизация.
- **B.** Оставить как есть (в git — фоссилия от 21.06). Тогда я снимаю проверку «≥13 дней» с
  чистого чекаута отдельной пометкой «этот файл в git заведомо устаревший» — но тогда никакой
  автоматической защиты от потери дней трека в git не остаётся.
- **C.** Вообще убрать `data/paper_evidence.json` из git (пусть живёт только на хосте) и
  переписать проверку на живой файл. Самый честный вариант «в репозитории нет доказательств
  вовсе», но публичная проверяемость трека из репозитория пропадает.

**Пункт 2 — результат бэктест-гейта:**
- **A (рекомендую).** Закоммитить `data/backtest/pre_paper_backtest_gate.json` (это статичный
  результат гейта, не текущее состояние портфеля) — тогда 20 проверок, включая сторож
  «≥16/20», начнут реально работать в CI.
- **B.** Оставить вне git — тогда согласиться, что сторож гейтов проверяет только твою машину,
  и в CI его нет.

## Как понять, что готово

Ты написал в карточке «пункт 1 — вариант X, пункт 2 — вариант Y» и перевёл её в Owner Done.

## Что будет после

По пункту 1 (A) — после твоего решения по выдуманным записям добавлю синхронизацию файла
доказательств в дневной цикл и верну проверку «≥13 дней» в зелёное честным путём; (B) —
помечу проверку с явной причиной; (C) — перепишу её на живой файл и уберу файл из git.
По пункту 2 (A) — закоммичу файл гейта, и 20 проверок начнут выполняться в CI (проверю по
результату прогона Actions); (B) — запишу в журнал, что сторож гейтов вне CI осознанно.
