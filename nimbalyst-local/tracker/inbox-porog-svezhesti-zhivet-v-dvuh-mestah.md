---
type: inbox
title: "Порог свежести артефакта живёт в ДВУХ местах и они не пересекаются — расхождение никем не проверяется"
status: in-progress
created: 2026-08-28
priority: high
source: ADR-158
claimed_by: cycle-42991
claimed_at: 2026-08-31T17:59:54Z
status_trail:
  - "2026-08-31T18:07:32.514541+00:00 new -> in-progress · queue.set_status · cycle-42991"
---

# Порог свежести живёт в двух местах, и они не пересекаются

Найдено 28.08 при работе над ADR-158.

## Замер

Ответ на вопрос «через сколько часов файл агента считается протухшим» хранится в двух
независимых местах:

| где | сколько агентов | как называется |
|---|---|---|
| `architecture/manifest.json` | 19 производителей | `produces[].slo_hours` |
| `spa_core/monitoring/uptime_monitor.py` | 16 агентов | `AGENT_OUTPUT_FILES[label] = (файл, max_age_seconds)` |

**Поправка к первой редакции этой карточки (28.08, вечер).** Здесь стояло «пересечение — НОЛЬ».
Это было посчитано по ПАРАМ «агент + файл» и потому неверно как утверждение о пересечении:
по агентам оно НЕ пустое. Поставленная сверка нашла расхождение в первый же прогон —
`com.spa.daily_cycle` (карточка `inbox-zhivost-dnevnogo-tsikla-suditsya-po-chuzhomu-failu`):
два дома называют продуктом одного агента РАЗНЫЕ файлы.

Ошибка ровно того класса, что и вся эта задача: посчитал не то, что назвал. Сверка,
поставленная вместо переезда, окупилась в первый же запуск.

Остальное в силе: сверки между домами не было ни одной, и как только кому-нибудь пропишут
`slo_hours` агенту под наблюдением `uptime_monitor`, пороги разойдутся молча.

Это ровно тот класс, на котором система горела не раз: одно и то же знание в двух домах.

## Что важнее — правило, по которому назначен второй набор

Над картой в `uptime_monitor.py` написано:

> max_age is derived from the agent's schedule with generous slack (≈ 2–3× the run interval)
> so a single missed tick doesn't flap.

То есть **механическое правило «2–3× интервала расписания» уже действует в проде для 16 агентов.**
При ответе на `own-35` владелец это же правило отклонил для манифеста — обоснованно (число без
автора), но решение принималось без знания, что такой набор уже существует и работает.

Это не отменяет ADR-158. Это факт, который должен быть на столе у двух ролей, когда они начнут
назначать сроки: у 16 агентов срок уже назначен, и назначен ИНАЧЕ.

## Что сделать

1. Показать владельцу: правило, которое он отклонил, живёт в проде для 16 агентов (эта карточка).
2. Решить, где ЕДИНСТВЕННЫЙ дом порога свежести. Вариант по умолчанию — манифест (он уже
   машиночитаемая конституция флота), а `uptime_monitor` читает оттуда, а не держит свою копию.
3. До сведения — поставить сверку: порог для одного агента в двух местах обязан совпадать,
   расхождение = находка. Сверка дешевле переезда и ловит проблему сразу.

## Чего НЕ делать

Не переносить числа молча. 16 порогов писал человек с объявленным правилом; перенос без сверки
превратит осознанные значения в «унаследованные», и спросить будет уже не у кого.

---

## Цикл #444 (2026-08-31): п.3 исполнен ПО-НАСТОЯЩЕМУ — ADR-199

Пункт 3 («до сведения — поставить сверку») числился сделанным с 28.08. Сверка стояла,
но была ослепляема ровно на своём же правиле.

**Замер.** «Зелёный вердикт на пустом множестве — сторож, который не может сработать» —
это правило из шапки самого модуля, и исполнялось оно только для случая «пересечение пусто
ЦЕЛИКОМ». По каждому агенту стояло безмолвное `continue`: у метки монитора срок есть,
манифест не дал ни одного — агент не попадал НИ В ОДИН исход. Печаталось «сошлись все
сопоставимые пороги» — правда о МЕНЬШЕМ множестве, чем звучало: **14 из 16**.

Кто терялся:

| метка | монитор | манифест |
|---|---|---|
| `com.spa.autopush` | живость по `logs/auto_push.log`, окно 4.5 ч | `intent: active`, `curation: partial`, `produces: []` |
| `com.spa.checkpoint-7day` | живость по `logs/checkpoint_7day.log`, окно 192 ч | `intent: retired`, `produces: []` |

Первый — острый край: это ровно `different_artifact`, ради которого модуль написан,
отброшенный до того, как его можно было увидеть.

**Сделано (ADR-199):** поагентный исход `uncompared` с причиной и с `intent`/`curation`;
тождество учёта `compared + не сверено == меток монитора со сроком` (немой пропуск невозможен
по построению); итоговая строка называет размер множества; пробел печатается шагом 0-офис
строкой `[ПОРОГ НЕ СВЕРЕН]`. Пробел сознательно НЕ стал находкой — как и требует эта карточка.
10 тестов, каждый краснеет на коде до правки.

**Пункты 1 и 2 карточки НЕ исполнены и остаются открытыми** — они не мои:
п.1 (показать владельцу, что отклонённое им правило «2–3× интервала» уже живёт в проде для
16 агентов) и п.2 (решить, где ЕДИНСТВЕННЫЙ дом порога) — вопросы владельца, а не агента.
Карточка остаётся `in-progress` под ними, закрывать её нельзя.

### Уточнение того же цикла #444: КУДА адресованы пп.1–2 — перемерено, нового вопроса владельцу НЕ создаю

Выше я написал «пп.1 и 2 — вопросы владельца». Перемерил, прежде чем заводить карточку
`needs-owner`, и адресат оказался другим у обоих пунктов:

* **П.1 (показать владельцу, что отклонённое правило «2–3× интервала» уже живёт в проде для
  16 агентов)** — это не решение, а ВХОД для тех, кто назначает сроки. Он записан туда, где
  его прочитают: карточка `agent-dve-roli-rasstavlyayut-sroki-godnosti` (`in-progress`,
  исполняет ADR-158) — вместе с конкретным следующим кандидатом `com.spa.autopush` и с
  оговоркой, что ADR-158 это не отменяет и переспрашивать владельца не о чем (ADR-084).
* **П.2 (где ЕДИНСТВЕННЫЙ дом порога)** — на него ответ уже дан: выбрана СВЕРКА вместо
  переезда, и сверка стоит и работает. **Честно про слабое место:** этот ответ живёт ровно в
  одном месте — в шапке `spa_core/monitoring/freshness_threshold_parity.py` («Выбор владельца
  28.08»), — и ADR под ним нет. Утверждать по нему как по решению нельзя, выдавать его за
  открытый вопрос — тоже неверно: сверка построена, принята и работает третий день. Пробел
  архивный, а не решенческий, и он НАЗВАН здесь, а не молча закрыт.

Карточка остаётся открытой под этим архивным пробелом и под п.1 до назначения срока
`com.spa.autopush`. Заводить владельцу вопрос при очереди, где 4 прежних до него физически
не доезжают, значило бы добавить шума вместо сигнала.
