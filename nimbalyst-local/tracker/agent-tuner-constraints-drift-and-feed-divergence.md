---
trackerStatus:
  type: agent
title: Constraints тюнера не зеркалят RiskPolicy + расхождение фидов по pendle (D5+D6)
status: backlog
source: session-2026-08-02 (W4 дизайн-шаг, побочные находки)
created: 2026-08-02
priority: medium
domain: money-path (значения RiskPolicy НЕ меняем)
---

## Что случилось

- **D5 — `portfolio_rebalancer._DEFAULT_CONSTRAINTS` не зеркалит RiskPolicy v1.0:** `t1_min=0.55`
  (такого требования в политике нет — ложный T1-floor убран ещё 2026-07-08), `per_protocol_max=0.25`
  (в политике T1 = 40 %), `t2_max=0.45` (в политике 50 %). Ограничения строже политики, то есть
  безопасны, но означают, что «оптимум» тюнера — не оптимум политики. Замер 2026-08-02: предложение
  тюнера `expected_apy 4.65 %` при 41 % кэша, то есть **хуже** текущей книги. Комментарии в файле
  (строки 56–61) описывают уже несуществующие значения enforcer'а.
  ⇒ В ADR-060 зафиксировано: источник оптимума для yield-триггера — `optimized_yield` аллокатора,
  НЕ `AllocationTuner`. Эта карточка — про приведение тюнера в соответствие либо про честную
  пометку его как advisory-инструмента.
- **D6 — расхождение фидов по `pendle` (20 % книги).** Снимок оркестратора (06:00 UTC): `apy 13.9419,
  live_data true`. `data/adapter_status.json` (15:01 UTC): `apy 8.0, live_apy null`. Уточнение:
  это не противоречие двух наблюдений — один производитель получил живое чтение, второй не получил
  и подставил литерал 8.0. Но потребитель этого не различает, а инвариант 2 требует fail-CLOSED при
  расхождении фидов. Нужен детерминированный приоритет источников и явная запись, какой источник
  победил и почему.

## Что нужно

1. D5: либо выровнять `_DEFAULT_CONSTRAINTS` по `RiskConfig` (единый источник, без хардкода), либо
   явно пометить тюнер как advisory и убрать его из money-path решений. Комментарии 56–61 — переписать.
2. D6: единый детерминированный порядок источников APY + запись победившего источника в
   `positions_detail` (частично уже есть) и в новый `allocation_rationale.json` (ADR-060 L3).

## Как понять, что готово

Тест: constraints тюнера читаются из `RiskConfig` (или тюнер не влияет на money-path); для каждого
протокола в книге видно, какой источник дал использованное число и почему он победил.

---

## ➕ Замер с ветки `work-status-check-xfnbew` (приложен циклом #324)

Карточка ветки `own-2026-08-18-zapas-rebalansera-strozhe-politiki` (`6025edc8a`) описывает ЭТОТ же дефект, поэтому на `main` она
не переносилась (решение владельца 20.08, вариант 1: дубли не тащим). Но её замер на `main`
не записан нигде и вместе с веткой был бы потерян — поэтому приложен сюда:

По пункту **D5**: на ветке тюнер был сведён к одному источнику порогов (RiskPolicy), второй копии
чисел в нём не осталось, пороги политики не менялись ни на цифру. **Эта починка на `main` НЕ
доставлена** — код ветки не переносился по решению владельца, поэтому D5 на `main` остаётся
открытым, а карточка ветки фиксирует, что решение уже найдено и проверено.
Отдельный остаток, названный там же: у `portfolio_rebalancer` есть СВОЙ набор ограничений строже
политики (на протокол 25 % против 40 %, T2 45 % против 50 %, кэш 7 % против 5 %, книга 7 против 8,
плюс `t1_min=0.55`, которого в политике нет вовсе) — вопрос владельцу, оставлять ли этот запас.


---

## D6 — перемерен и получил ИЗМЕРИТЕЛЬ (цикл #389, 2026-08-27). D5 остаётся открытым.

Карточка НЕ закрывается: сделана половина (D6), D5 (констрейнты тюнера) не тронут.

**D6 жив 25 дней спустя ДОСЛОВНО.** Замер 26.08 20:47Z — два артефакта ОДНОГО цикла,
разрыв 0,6 с: `adapter_status` 8,0 % / tier 2 / static-$500M против `orchestrator`
13,9673 % / **T3** / live-$6,15M. Уточнение этой карточки («не противоречие двух наблюдений,
а литерал вместо неполученного чтения») подтвердилось и **сохранено как отдельный род
находки** в стороже — именно чтобы починка не поехала в «выбрать число».

Сверх карточки измерено: **тир расходится тоже** (T2 против T3 — это два разных потолка
концентрации на один капитал), и **живое число выдаётся в обход собственного порога
адаптера** (`_classify_tier` возвращает `None` для всех трёх eligible-рынков Pendle при
пороге T3 $20 млн; `get_markets()` отдаёт 0 рынков, а `get_yield_info()` делает
`_classify_tier(tvl) or "T3"` и отдаёт «14,39 %, live»).

Доставлено: **ADR-147**, сторож `spa_core/monitoring/adapter_feed_divergence.py` — читается
обязательным шагом 0-офис каждого цикла (21 тест, 6 мутаций приёмки, 2 из них — мутации
проводки). Пункт 2 «Что нужно» этой карточки («детерминированный порядок источников +
запись победителя») — money-path, отдан владельцу карточкой
`owner-decision-pyataya-chast-deneg-stoit-na-chisle-o-ko`.

---

## Дополнение цикла #408 (28.08, замер шага 0-офис)

D6 жив и предъявлен сторожем `adapter_feed_divergence` (отчёт 27.08 23:30Z), числа сместились,
утверждение то же: оркестратор наблюдает `pendle` **13.8534 пп**, `adapter_status` предъявляет
**литерал 8.0 пп** (разница 5.8534 пп). Сторож формулирует это ровно как карточка два месяца
назад: «это НЕ спор двух наблюдений — вторая сторона не наблюдала ничего; починка — дать ей фид,
а не выбрать число».

**Новое в показании (в исходной карточке этого нет): стороны кладут `pendle` в РАЗНЫЕ ТИРЫ** —
`adapter_status` в **T2**, снимок оркестратора в **T3**. Тир — это потолок концентрации, а не
подпись: два ответа означают два разных потолка на один и тот же капитал (T2 — 20% на протокол,
T3 — своя ветка). То есть расхождение фидов протекло из «какая доходность» в «сколько этого
вообще можно держать», и приёмка карточки обязана покрыть обе оси, а не только APY.

Отдельной карточки не заводил намеренно (шаг 1a протокола): предмет, файлы и причина те же —
дубль расщепил бы одну починку на две очереди.
