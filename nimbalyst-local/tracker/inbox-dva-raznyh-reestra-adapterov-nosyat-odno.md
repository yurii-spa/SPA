---
trackerStatus:
  type: inbox
title: "Два разных реестра адаптеров носят ОДНО имя ADAPTER_REGISTRY: 36 кортежей против 22-dict, пересечение 13, aave_v3 против aave_usdc"
status: done
created: 2026-08-12
claimed_by: cycle-275-pid12688
claimed_at: 2026-08-17T12:06:46Z
---

## Что нашли

Под одним именем `ADAPTER_REGISTRY` в репозитории живут **два разных объекта разной формы
и с разными именами протоколов**, и ответ на вопрос «есть ли у протокола адаптер?» зависит
от того, каким импортом его задали:

| Импорт | Форма | Записей | Как зовут крупнейшую позицию книги |
|---|---|---|---|
| `from spa_core.adapters import ADAPTER_REGISTRY` | список кортежей `(имя, тир, класс)` | **36** | `aave_v3` |
| `from spa_core.adapters.registry import ADAPTER_REGISTRY` | dict `{id: метаданные}` | **22** | `aave_usdc` |
| `spa_core.ADAPTER_REGISTRY` (верхний уровень) | это второй (dict), с падением в `{}` | 22 | `aave_usdc` |

Замер 2026-08-12 (цикл #206): пересечение имён — **13**. Только в первом: `aave_v3`,
`moonwell_base`, `fluid_arbitrum`, `pendle_pt_usdc`, `stusd` и ещё 18. Только во втором:
`aave_usdc`, `compound_usdc`, `notional_v3`, `gmx_btc_perp` и ещё 5.

## Почему это дороже, чем дубль

1. **Тихий разный ответ.** Потребитель не выбирает реестр осознанно — он пишет
   «импортировать ADAPTER_REGISTRY» и получает тот, чьё имя короче набрать. Ошибиться
   формой нельзя громко: обе формы итерируются без исключения, и обе дают непустой
   результат. Ровно на этом сгорел `house_view_gap` (цикл #206): он читал форму как
   имена и три месяца объявлял достижимые возможности недостижимыми.
2. **Имя одной и той же позиции разное.** `aave_v3` (40 % книги) в dict-реестре
   отсутствует ВОВСЕ — там она `aave_usdc`. Любая сверка «книга ↔ реестр», случайно
   взявшая dict, честно доложит, что крупнейшая позиция книги не зарегистрирована.
3. **Инвариант `CLAUDE.md` («Реестр — `ADAPTER_REGISTRY` в `spa_core/adapters/__init__.py`»)
   и `.claude/rules/adapters.md` знают только ПЕРВЫЙ.** Проверка количества из правила
   (`len(ADAPTER_REGISTRY)`) на втором импорте вернёт 22 и будет выглядеть правдой.

## Кто уже читает какой

- `spa_core/monitoring/house_view_gap.py` — первый (починен в #206, читает форму, а не предполагает её).
- `spa_core/alerts/governance_watcher.py::whitelisted_protocol_keys` — первый, и **уже написан
  устойчиво к обеим формам** (dict → `.keys()`, иначе элемент-кортеж → `entry[0]`); образец.
- `spa_core/compliance/audit_report_generator.py` — первый, распаковкой `for key, tier, _cls`
  (сломается на dict-форме, но громко).
- `spa_core/backtesting/paper_day1_checklist.py` — ВТОРОЙ (`len` → 22 в отчёте дня-1).
- `spa_core/agents/protocol_research_agent.py` — не импортирует вовсе, а **сканирует текст файла**
  регуляркой; третий способ ответить на тот же вопрос.

## Что сделать (предложение, не сделано)

Свести к одному источнику правды, НЕ трогая money-path одним движением:
1. Назвать канон (по инварианту `CLAUDE.md` — реестр из `spa_core/adapters/__init__.py`, 36 записей).
2. Второй реестр либо переименовать (`ADAPTER_METADATA`), либо сделать производным от канона;
   расхождение имён (`aave_v3` ↔ `aave_usdc`) разрешить таблицей соответствия **в одном месте**.
3. Храповик: ни один новый потребитель не читает `ADAPTER_REGISTRY` из второго модуля без
   явного «мне нужны метаданные, а не имена».
4. Положительный контроль обязателен: тест, краснеющий на возврате второго импорта туда,
   где ждут имена протоколов.

## Границы

RiskPolicy, kill-switch, живой трек и пороги НЕ трогать. Состав реестра НЕ менять: тут вопрос
только о том, что два разных ответа не могут носить одно имя. Промоушен/демоушен протоколов —
отдельное owner-gated решение.

## Как понять, что готово

`grep` по дереву не находит двух разных объектов с именем `ADAPTER_REGISTRY`; проверка количества
из `.claude/rules/adapters.md` даёт одно и то же число любым разрешённым импортом; тест краснеет
на возврате расхождения.

---

## ЗАКРЫТО — цикл #274, 2026-08-17

### Объектов оказалось ТРИ, а не два

Карточка называла два. Перемер AST по всему дереву на `dd203d8fd` нашёл **три** модульных
определения имени `ADAPTER_REGISTRY`:

| Файл | Форма | Записей (рантайм) | Крупнейшая позиция книги |
|---|---|---|---|
| `spa_core/adapters/__init__.py` | список кортежей | **36** | `aave_v3` |
| `spa_core/adapters/registry.py` | dict метаданных | **22** | `aave_usdc` (`aave_v3` НЕТ вовсе) |
| `spa_core/orchestrator/adapter_orchestrator.py` | список кортежей | **8** | что цикл РЕАЛЬНО опрашивает |

Третий — не безобидный: это набор, который money-path опрашивает каждый цикл. Его существование
под общим именем уже вынуждало потребителей защищаться псевдонимом вручную
(`spa_core/tests/test_orchestrator_registry_coverage.py`: `import ADAPTER_REGISTRY as ORCH`) —
то есть цену путаницы платили, но имя не трогали.

### Что сделано

1. **Одно имя — один объект.** `registry.py` → `ADAPTER_METADATA`; `adapter_orchestrator.py` →
   `POLLED_ADAPTERS`; канон `ADAPTER_REGISTRY` остался ровно один — там, где его называет
   инвариант `CLAUDE.md`. Псевдонимов совместимости НАМЕРЕННО не оставлено: алиас — это ровно
   то же «два ответа под одним именем», только с отсрочкой.
2. **Публичная поверхность перестала врать.** `spa_core.ADAPTER_REGISTRY` отдавал dict (22,
   `aave_usdc`) — то есть НЕ тот объект, который инвариант называет реестром. Теперь отдаёт
   канон; dict публикуется рядом под своим именем (`spa_core.ADAPTER_METADATA`, оба в `__all__`).
3. **Состав не тронут ни у одного из трёх** (граница карточки соблюдена): 36 / 22 / 8 до и после.
4. **Храповик** `spa_core/tests/test_adapter_registry_single_name.py` — AST по дереву: новое
   определение имени краснеет. Сканер — по координатам AST, а не по тексту: упоминание в
   комментарии/докстринге и присваивание внутри функции определением не считаются (урок #227,
   закреплён обратным контролем).

### Приёмка

Положительный контроль на чистом `origin/main` (тот же sha `dd203d8fd`): храповик даёт
**6 failed / 3 passed**, ключевое падение — поведенческое: `AssertionError: 3 != 1` на
«определено ровно один раз». Оба контроля самого сканера (дубль ловится · упоминание не ловится)
на origin ЗЕЛЁНЫЕ — они проверяют сканер, а не дерево, и краснеть там не должны.

### Намеренная правка теста (инв. #16)

`tests/test_spa_public_api.py::T-022` утверждал «`spa_core.ADAPTER_REGISTRY` — это dict», то есть
**закреплял сам дефект**. Проверка не снята и не сужена, а расширена: теперь утверждается форма
и ТОЖДЕСТВО канона (`assertIs`) плюс отдельная проверка на `ADAPTER_METADATA`, про который раньше
не утверждалось ничего. Обоснование продублировано в докстринге теста и в журнале W34.

### Что НЕ чинил — отдельной карточкой

`spa_core/agents/protocol_research_agent.py` карточка НАЗЫВАЛА, но не мерила. Замер: его разбор
возвращает **`[]`** — парсер написан под dict-форму (`… and "{" in stripped`), а нацелен на файл,
где реестр списочный, поэтому «активных адаптеров» у исследователя кандидатов НОЛЬ, молча.
Это fail-OPEN в исследовательском слое и отдельное поведение агента ⇒ карточка
`inbox-issledovatel-kandidatov-schitaet-chto-ak`, а не попутная правка.

RiskPolicy, kill-switch, живой трек `data/equity_curve_daily.json`, `data/`, флот, деплой и
числа на сайте — не тронуты.

---

## ПОДНЯТО И ДОСТАВЛЕНО — цикл #275, 2026-08-17

Цикл #274 (`pid98896`) умер между работой и пушем: на `origin/main` не было ничего, работа лежала
в `/private/tmp/spa_c274`. Шаг 0a назвал её осиротевшей (`durable_process_gone`), шаг 0b — `stale`.
Перенесено в своё дерево патчем от `dd203d8fd` (расхождения с `origin/main` по этим путям нет),
**`data/` и `spa.db` из чужого дерева НЕ брал** (правило доставки, п. 4). Отчёту мёртвой сессии
не верил — и правильно: **её работа НЕ была зелёной**.

### Что нашёл перемером (чего #274 не измерил)

1. **Три теста краснели.** `tests/test_cross_chain_yield.py` (T-22/24/25) патчит реестр по имени
   строкой — `patch("spa_core.adapters.registry.ADAPTER_REGISTRY", …)`. После развода имён атрибута
   больше нет, и `mock` падает `AttributeError` ДО утверждений. Замер до починки: `3 failed,
   27 passed`. Починено переводом цели патча на `ADAPTER_METADATA` — проверка не ослаблена и не
   сужена, сменилось имя патчимого символа (инв. #16 соблюдён, запись в журнале W34).
2. **Храповик краснел бы на ПРОДЕ — на верном состоянии.** Сканер шёл по всему дереву, а прод-дерево
   держит рабочие копии ВНУТРИ себя (`.claude/worktrees/*`, 9 штук). Замер тем же сканером по
   `~/Documents/SPA_Claude`: **33 определения** вместо одного ⇒ гарантированный ложный отказ, а
   ложный красный живёт ровно до первого «выключите его». Теперь пропускаются вложенные чекауты
   по признаку СВОЕГО `.git` (каталог у клона, файл-указатель у worktree), а не по имени каталога —
   имена деревьев случайны. Перемер после починки: прод **3** (там ещё старый код — это ЧЕСТНЫЙ
   красный, он и должен гореть до автосинка), моё дерево **1**. Плюс `root.resolve()`: на macOS
   `/tmp` против `/private/tmp` ронял `relative_to`.
3. **Документированная команда врала.** `ERROR_CODE_REFERENCE.md` (обе копии, корень и `docs/`)
   в разделе «что делать при `RegistryError`» предлагал импорт, которого больше нет. Заменён на
   две строки — по одной на каждый объект, с числом записей.
4. **Ещё два потребителя, которых НЕ ВИДИТ ни один grep по имени.** `spa_core/tests/test_risk_scores_regen.py`
   берёт набор опроса через ПСЕВДОНИМ модуля (`… as orch` → `orch.ADAPTER_REGISTRY`). Поиск по
   `adapter_orchestrator.ADAPTER_REGISTRY` такую форму не находит по построению. Нашёл их ТОЛЬКО
   полный прогон: `2 failed` у меня против нуля у контроля. Починено на `orch.POLLED_ADAPTERS`,
   и из находки вырос ещё один сторож — `test_no_compat_aliases_left_behind`: ни один из трёх
   модулей не смеет носить чужое имя как атрибут (алиас совместимости = та же авария с отсрочкой).

### Приёмка #275 (своими прогонами, не отчётом)

- новый положительный контроль в самом храповике: синтетическое дерево с вложенным клоном И
  вложенным worktree обязано дать РОВНО одно определение (без починки — три);
- полный прогон командой CI в своём дереве и контроль на чистом `origin/main` с ТЕМ ЖЕ sha
  `6e79f4f3e`: контроль **108 305 passed / 0 failed**, я — **108 315 passed / 0 failed**, разница
  ровно **+10** (9 тестов храповика + расщеплённый надвое T-022). База зелёная целиком, значит
  списать своё падение на «оно и так красное» было НЕ на что;
- `_read_active_adapters_from_init() -> []` перемерено своим прогоном — карточка
  `inbox-issledovatel-kandidatov-schitaet-chto-ak` доставляется вместе с этой.

Границы соблюдены: состав всех трёх наборов (36 / 22 / 8) не тронут, RiskPolicy, kill-switch,
живой трек, `data/`, флот и сайт — ни строкой.
