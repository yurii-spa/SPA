---
trackerStatus:
  type: inbox
title: "задача- разобраться \"Сторож расхождения флота не запускался 21 день — его никто…"
status: done
source: telegram
created: 2026-08-05
---

## Задание (из Telegram)

задача- разобраться "Сторож расхождения флота не запускался 21 день — его никто не зовёт
➡️ Выбрать, что делаем со сторожем. Трогать launchd (ставить/снимать агентов) мне запрещено
📄 nimbalyst-local/tracker/owner-decision-storozh-rashozhdeniya-flota-ne-zapuskals.md
Ответь: переведи карточку Needs Owner → Owner Done (в Nimbalyst или правкой status:)."

---

## Разбор (цикл #120, 2026-08-05) — вердикт: УЖЕ СДЕЛАНО, дубль не завожу

Это пересланный текст уведомления по карточке
`owner-decision-storozh-rashozhdeniya-flota-ne-zapuskals.md`. Инженерная половина
закрыта СЕГОДНЯ, за несколько часов до этого задания, циклом #119 — проверял, а не
поверил отчёту:

- **Расписание есть.** `scripts/fleet_parity_check.py` вызывается шагом внутри дневного
  цикла (`scripts/run_daily_paper_cycle.sh`, non-fatal), не отдельным агентом — чтобы не
  трогать launchd, что автономной сессии запрещено. Проверено на `origin/main`
  (`05a507910`), строки на месте.
- **Замер свежий и настоящий:** `data/fleet_parity.json` от 05.08 12:32Z,
  `status: DRIFT`, объявлено установщиком 56, файлов на диске 82.
- **Видно в здоровье системы:** `data/agent_health.json` → `system_issues`:
  «fleet parity DRIFT (22 orphan-plist, 1 declared-not-running)». Раньше там 597 часов
  висело «fleet parity stale» — теперь строка означает настоящее сегодняшнее расхождение.

**Почему проверка «не проходила»:** она не ломалась. Её было **некому звать** — ни plist,
ни строки в цикле; скрипт позвали руками один раз 11 июля и больше не звали.

**Что осталось и почему я это не делаю:** состав флота. 15 работающих агентов установщик
не знает, ещё 7 файлов лежат мёртвым грузом. Дописать их в установщик или снять — это
установка/снятие агентов, только твоё решение. Вопрос уже задан **в существующей карточке**
`owner-decision-storozh-rashozhdeniya-flota-ne-zapuskals.md` (раздел «Обновление 2026-08-05»),
поэтому второй карточки не завожу — источник правды один.

Инженерная карточка: `agent-fleet-parity-guard-never-scheduled` (blocked, ждёт того же ответа).

Закрываю как `done` (§6.4).
