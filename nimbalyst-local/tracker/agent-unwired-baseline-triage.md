---
type: agent-task
status: done
created: 2026-08-09
status_trail:
  - "2026-08-24T20:50:30.178876+00:00 backlog -> done · queue.set_status · cycle-67844"
---

# Разбор базы храповика: 24 скрипта не упомянуты НИГДЕ — и это не значит «мусор»

## Что случилось и почему это важно

Храповик (09.08) зафиксировал **88** скриптов с точкой входа, у которых не найдено
вызывающего. Первый разбор — объективным признаком: упоминается ли скрипт хоть где-то в
`docs/`, карточках, `CLAUDE.md`, `README`.

- **64** упоминаются → скорее всего запускают руками, это нормально;
- **24** не упомянуты нигде.

**Ожидание было «24 мёртвых». Оно не подтвердилось.** Вот что в этом списке:

| скрипт | почему настораживает |
|---|---|
| `send_milestone_alert` | отправка вехи владельцу — молчащий канал уже был дефектом (kill-switch) |
| `reconcile_paper_evidence` | сверка доказательной базы трека |
| `run_auto_promoter` | продвижение стратегий |
| `evaluate_shadow_trigger` | теневые триггеры |
| `mutation_gate` | проверка качества тестов мутациями |
| `generate_evidence_report` | отчёт о доказательствах |

Это не похоже на мусор. Это похоже на **следующие случаи того же класса**, ради которого
храповик и ставился: код написан, никто не зовёт, никто не заметил.

Остальные из 24 действительно выглядят разовыми: `seed_test_fixtures`, `preflight_day1`,
`verify_phase2_migration`, `baseanalytics_migration_summary`, `s11_backtest_90d`.

## Что нужно сделать

**Не удалять по списку.** Разобрать шесть настораживающих поштучно, и по каждому ответить
ровно на один вопрос: **эта функция сейчас выполняется чем-то другим или не выполняется
вовсе?**

Порядок по цене ошибки: `send_milestone_alert` → `reconcile_paper_evidence` →
`run_auto_promoter` → остальные.

Разовые — в `attic/` с пометкой, а не удалять: они документируют, как систему поднимали.

## Как понять, что готово

По каждому из шести есть ответ в карточке: «выполняется вот этим» либо «не выполняется,
завожу подключение». База храповика уменьшилась на разобранное.

## Что будет после

Либо найдём ещё несколько молчащих функций (и тогда храповик окупился в первый же день),
либо убедимся, что их работу делает кто-то другой — и это тоже знание, которого сейчас нет.


## Разобран 1 из 6 · `send_milestone_alert` — ЛОЖНАЯ ТРЕВОГА (2026-08-09)

**Вопрос:** функция выполняется чем-то другим или не выполняется вовсе?
**Ответ: выполняется.**

Цепочка проверена до конца, а не до первого совпадения имени:

1. `spa_core/reporting/alert_on_milestone.py` — действующий отправитель вех;
2. `scripts/agent_telegram_milestone.sh` — обёртка, которая его зовёт;
3. `com.spa.telegram_milestone` — **есть в флоте, код выхода 0**.

Найденный `scripts/send_milestone_alert.py` — разовый скрипт MP-417 под конкретное
историческое событие («S7 пробил барьер 10 % APY»), а не постоянная функция.

**Почему проверял до конца.** Наличие второго файла с похожим именем ничего не доказывает:
ровно так выглядели бы и семь предыдущих дефектов этого класса — код есть, зовущего нет.
Достаточным ответом было только присутствие живого агента в флоте.

**Вывод:** безопасно вывести в `attic/` с пометкой. Не удалять — скрипт документирует, как
отмечалась веха.

⚠️ **Физический перенос не сделан:** он требует УДАЛЕНИЯ файла из `scripts/`, а это операция,
которую я не проверял в пушере. Делать вслепую нельзя — незавершённый перенос оставит скрипт
и в `attic/`, и в `scripts/`, то есть удвоит путаницу вместо того, чтобы её убрать.
Следующая сессия: сначала убедиться, что доставка умеет удалять, затем перенести и убрать имя
из базы храповика (тест на призраков в базе поймает рассинхрон).

**Осталось разобрать 5:** `reconcile_paper_evidence` → `run_auto_promoter` →
`mutation_gate` → `evaluate_shadow_trigger` → `generate_evidence_report`.


## Разобран 3 из 6 · `run_auto_promoter` — ЛОЖНАЯ ТРЕВОГА (2026-08-09)

**Вопрос:** функция выполняется чем-то другим или не выполняется вовсе?
**Ответ: выполняется, и это подтверждено данными, а не чтением кода.**

`data/promotion_report.json` обновлён **сегодня** (возраст 0.0 дня). Значит продвижение
оценивается — но не этим скриптом.

Настоящие производители: `spa_core/reporting/auto_promoter.py`,
`spa_core/paper_trading/promotion_engine.py`, через `cycle_reporting` в дневном цикле.
Отдельного агента в флоте нет и не нужно: работа идёт внутри цикла.

**Свежесть артефакта — более сильный довод, чем найденный модуль.** Модуль может
существовать и не вызываться (так выглядели все семь дефектов этого класса). Свежий файл
означает, что кто-то реально отработал сегодня.

`scripts/run_auto_promoter.py` (ADR-029) — более ранняя, отдельно стоящая версия той же
оценки. Безопасно в `attic/`, вместе с `send_milestone_alert`.

## Счёт по разбору: 3 из 6

| скрипт | вердикт |
|---|---|
| `send_milestone_alert` | ложная тревога — живой агент в флоте |
| `reconcile_paper_evidence` | **НАСТОЯЩАЯ находка** → карточка `own-32` |
| `run_auto_promoter` | ложная тревога — артефакт свежий сегодня |

Осталось: `mutation_gate`, `evaluate_shadow_trigger`, `generate_evidence_report`.

**Наблюдение о методе.** Две ложные тревоги из трёх — и обе опознаны по РАЗНЫМ признакам:
живой агент в флоте и свежий артефакт. Ни один из них не заменяет другой, и ни один не
следует из чтения кода. Единственная настоящая находка обнаружилась там, где docstring
уверенно утверждал обратное.


## Разобран 4 из 6 · `mutation_gate` — НАСТОЯЩАЯ находка, ПОДКЛЮЧЕНА (2026-08-09)

**Вопрос:** функция выполняется чем-то другим или не выполняется вовсе?
**Ответ: не выполняется вовсе.** Ни агента, ни шага в CI, ни артефакта.

Это мета-проверка (WS-6.3): она ломает money-path нарочно и требует, чтобы тесты
покраснели. Обычные наборы доказывают, что код верен на пробованных входах; эта —
что тесты **поймают настоящую регрессию**.

### Проверено запуском, а не рассуждением

**14 мутантов, 14 убито, 0 выживших, 92 секунды.** Среди убитых — подмены, от которых
зависят деньги: проверка сохранения NAV принудительно в True; обход защиты от нечисловой
оценки; допуск на пыль раздут до $1T; сломан инвариант «kill-switch спрашивают первым»;
блокирующий гейт симуляции понижен до необязательного.

**Хорошая новость: тесты money-path действительно имеют зубы — теперь это измерено.**
Я весь день опирался на зелёные прогоны, и до этого замера у меня не было права так делать.

### Что сделано

Шаг `Mutation gate (money-path meta-check)` добавлен в `ci.yml` между модульными и
интеграционными тестами. 92 секунды, детерминирован. Красный шаг означает ровно одно:
money-path можно сломать, и тесты этого не заметят.

**Восьмой случай класса «код есть, никто не зовёт» — и самый обидный:** самая
доказательная проверка в наборе существовала, работала и молчала.

## Счёт: 4 из 6

| скрипт | вердикт |
|---|---|
| `send_milestone_alert` | ложная — живой агент в флоте |
| `reconcile_paper_evidence` | **находка** → `own-32` (расхождение записей) |
| `run_auto_promoter` | ложная — артефакт свежий сегодня |
| `mutation_gate` | **находка** → подключён к CI |

Осталось: `evaluate_shadow_trigger`, `generate_evidence_report`.


## Разобраны 5 и 6 · РАЗБОР ЗАВЕРШЁН (2026-08-09)

### `evaluate_shadow_trigger` — ложная тревога

CLI-обёртка над `spa_core.paper_trading.shadow_trigger_eval`.
`data/shadow_trigger_evaluation.json` обновлён **сегодня** — модуль работает в цикле,
обёртка нужна лишь для ручного запуска.

### `generate_evidence_report` — находка: мёртвая ЦЕПОЧКА

Отчёт о доказательствах за 30 дней (MP-441). Вызывающего нет.

Рядом ещё два производителя отчётов — `scripts/weekly_evidence_report.py` и
`spa_core/alerts/daily_evidence_report.py`. У них **единственный** вызывающий:
`scripts/run_daily_simulation.py` — который **сам лежит в этой же базе** несвязанных.

**То есть мёртв не скрипт, а цепочка целиком, вместе с корнем.** Это качественно хуже
одиночного случая: три производителя отчётов о доказательной базе не работают, и обнаружить
это чтением любого из них по отдельности было нельзя — каждый выглядит вызываемым.

Ставки ниже, чем у `mutation_gate`: это отчёты, а не гейты, капитал они не двигают. Но
доказательная база — то, на что мы ссылаемся, говоря «трек честный», и три её отчёта
молчат.

**Не чиню сам:** сначала надо решить, нужны ли эти отчёты вообще, или их работу давно
делает `SYSTEM_BRIEFING` / дневной дайджест. Оживлять три молчащих отчёта, не проверив
этого, — значит завести три новых источника шума.

## ИТОГ РАЗБОРА: 6 из 6

| скрипт | вердикт | чем доказано |
|---|---|---|
| `send_milestone_alert` | ложная | живой агент в флоте |
| `reconcile_paper_evidence` | **находка** → `own-32` | расхождение в живых данных |
| `run_auto_promoter` | ложная | артефакт свежий сегодня |
| `mutation_gate` | **находка** → подключён к CI | запуск: 14/14 убито |
| `evaluate_shadow_trigger` | ложная | артефакт свежий сегодня |
| `generate_evidence_report` | **находка** → нужно решение | мёртвая цепочка с корнем |

**Три ложные тревоги, три настоящие находки.** Соотношение важно для будущих разборов:
половина «подозрительных» скриптов оказывается нормой, поэтому удалять по списку было бы
ошибкой — и поэтому же проверять каждый по отдельности необходимо.

**Ни один вердикт не получен чтением кода.** Живой агент, свежий артефакт, запуск,
сравнение чисел. Единственный раз, когда код говорил о себе сам (`reconcile_paper_evidence`:
«это разовый ремонт»), он говорил неправду.

---

## ✅ ЗАКРЫТА циклом #373 (2026-08-24) — оба оставшихся хвоста разобраны замером

Разбор 6/6 был закончен ещё 09.08, но карточка осталась `backlog`, потому что в ней висели
два незакрытых хвоста. Оба закрыты сегодня — каждый ЗАМЕРОМ, а не рассуждением.

### Хвост 1 · «доставка не умеет удалять» — ЛОЖНЫЙ ЗАСЛОН

09.08 перенос двух ложных тревог (`send_milestone_alert`, `run_auto_promoter`) в `attic/` был
отложен формулировкой «требует УДАЛЕНИЯ файла из `scripts/`, а это операция, которую я не
проверял в пушере». Замер сегодня: **инструмент удаления существует и лежит рядом** —
`scripts/github_delete.py` (2818 байт, на `origin/main`). Пушер действительно не умеет удалять
— и именно поэтому для удаления есть отдельный инструмент.

Заслон снят, но **перенос я не делаю** и называю почему: это две операции подряд (добавить в
`attic/` + удалить из `scripts/`), между которыми дерево остаётся в промежуточном состоянии, а
цена ошибки — ровно та путаница, ради устранения которой перенос и затевался. Это отдельная
задача с собственной приёмкой (`deployment_acceptance` до и после, `.claude/rules/deployment.md`),
а не хвост чужой. Зафиксировано как знание: **следующей сессии искать инструмент не нужно, он
называется `scripts/github_delete.py`.**

### Хвост 2 · `generate_evidence_report` — отложенный вопрос ИЗМЕРЕН и ушёл владельцу

09.08 оживление трёх молчащих отчётов было отложено верным вопросом: «нужны ли они вообще, или
их работу давно делает `SYSTEM_BRIEFING` / дневной дайджест?» Ответ сегодня:

| отчёт | последний результат | вызывающий |
|---|---|---|
| `scripts/generate_evidence_report.py` → `docs/evidence_report_30d.txt` | **29.06** (56 дней) | нет |
| `scripts/weekly_evidence_report.py` → `data/weekly_evidence/` | **20.06**, ровно один файл `2026-W24` | только `run_daily_simulation.py` |
| `spa_core/alerts/daily_evidence_report.py` → Телеграм | отправок нет | нет |

Корень `scripts/run_daily_simulation.py` подтверждён мёртвым: во всём дереве его имя встречают
только `scripts/run_health_check.py` и два теста — ни агента, ни шага цикла.

**Вопрос ушёл владельцу карточкой** `owner-decision-tri-otcheta-o-dokazatelnoi-baze-treka-mo`
(оживление = новый агент + новый канал сообщений, автономно запрещено). Три варианта с
рекомендацией; дублей нет — Шаг 1a по всей памяти дал NEW.

### СВЕРХ карточки · пока отчёт молчал, он сломался внутри

Отложив оживление, 09.08 никто не посмотрел, что именно оживлять. Посмотрел сегодня — запустил
в песочнице на копии живых данных. **Секция 2 («турнир стратегий») печатала пустое имя, `N/A` и
`unknown` в КАЖДОЙ строке при ЛЮБЫХ настоящих данных.**

`data/tournament_ranking.json` пишут ДВА производителя, и они расходятся втрое:

* `MultiStrategyRunner.export_results` → ключ `strategies`, поля `strategy_id`/`net_apy`/
  `is_active`/`days_running`, где **`net_apy` — ДОЛЯ** (производитель говорит это дословно:
  «0.042 = 4.2%»). Это то, что лежит в живом файле.
* `TournamentEvaluator.save_ranking` → ключ `ranking`, а имя и статус **вложены в `metrics`**,
  и там `realized_apy_pct` — **ПРОЦЕНТ**.

Читатель брал верхнеуровневые `name` / `apy_target` / `status` / `apy_realized` — ключи,
которых не пишет НИ ОДИН из двух. Сверка 28.06 (WS2.2) починила дрейф ключей в секции 1 и
оставила секцию 2 на старой схеме; не заметили ровно потому, что у генератора нет вызывающего —
то есть **дефект породил тот же класс, ради которого эта карточка и заведена**.

Починено и закреплено: единицы разрешаются **по КЛЮЧУ, а не по величине числа**. Наивная
починка (подставить `net_apy` в процентный слот) напечатала бы стратегию с 4.2 % как «0.0%» —
занижение в 100 раз; на это стоит отдельный положительный контроль.

**Почему соседний набор был зелёный всю аварию:** фикстура `MINIMAL_TOURNAMENT` в
`tests/test_evidence_report.py` — написанная от руки ТРЕТЬЯ схема, которую тоже не пишет ни
один производитель. Тест сверял копию с копией. Новый файл
`tests/test_evidence_report_tournament_schema.py` строит документы САМИМИ производителями
(`export_results`, `StrategyResult.to_dict`) — 9 тестов, 7 из них краснеют на неисправленном
модуле, 2 оставшихся — намеренные стражи от обратной регрессии (пустой документ; доказанные
дни имеют приоритет над турниром). Старую фикстуру НЕ трогал (инв. #16): она проверяет
законную третью форму, а её слепота названа здесь.

**Итог: 6 из 6 разобрано, оба хвоста закрыты, отложенное решение у владельца. Карточка `done`.**
