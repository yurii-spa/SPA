---
trackerStatus:
  type: inbox
title: Свести две реализации перезаполнения бюджета в одну (ADR-072 vs версия параллельной сессии)
status: done
source: nimbalyst
created: 2026-08-08
priority: high
---

Решение владельца 09.08 (вариант 1): освободившийся после страховок бюджет перезаполняется внутри тех же потолков. Реализаций ДВЕ: (а) ADR-072 redistribute_freed_budget в risk_gate.py — в проде, chain-aware, повторный проход гейта, cap_bound-именование, 11 тестов, замер кэш 25%→10%; (б) версия параллельной сессии из карточки owner-decision-posle-strahovki-dengi-ostayutsya-sirotam. Задача: сверить, оставить ОДНУ (базой — прод-версию), вторую удалить, тесты объединить, sandbox-замер до/после, pre_cutover_gate. Два механизма одного назначения = будущий конфликт в money-path.

## Замер cloud-сессии 20.08: второй реализации на origin НЕТ

`grep -rn "def.*redistribute|refill"` по всему origin/main: единственная
реализация — `risk_gate.redistribute_freed_budget` (прод, ADR-072). «Версия
параллельной сессии» жила только в /tmp-worktree мёртвой сессии на хосте и на
origin не доехала. Сводить нечего; карточку можно закрывать как
«дубль не существует», либо переформулировать в «проверить, что мёртвое дерево
не несло ничего сверх прод-версии, и снять его».

---

## Закрытие (аудит очереди, 26.08)

Замер cloud-сессии 20.08 (см. `inbox-cio-etap1-diagnostika-gotova-ispolnyat-g1-g4.md`, G2): второй реализации на origin/main нет и не было — она жила только в /tmp-worktree мёртвой сессии на хосте. Сводить нечего — карточка закрывается как решённая по факту (мнимая проблема).
