---
trackerStatus:
  type: inbox
title: Свести две реализации перезаполнения бюджета в одну (ADR-072 vs версия параллельной сессии)
status: done
source: nimbalyst
created: 2026-08-08
priority: high
---

Решение владельца 09.08 (вариант 1): освободившийся после страховок бюджет перезаполняется внутри тех же потолков. Реализаций ДВЕ: (а) ADR-072 redistribute_freed_budget в risk_gate.py — в проде, chain-aware, повторный проход гейта, cap_bound-именование, 11 тестов, замер кэш 25%→10%; (б) версия параллельной сессии из карточки owner-decision-posle-strahovki-dengi-ostayutsya-sirotam. Задача: сверить, оставить ОДНУ (базой — прод-версию), вторую удалить, тесты объединить, sandbox-замер до/после, pre_cutover_gate. Два механизма одного назначения = будущий конфликт в money-path.

## Закрыто 16.08

**Схлопнуто 16.08 в `inbox-adr-072-ne-srabotal-trim-proishodit-v-al`: то же самое другими
словами** (кластер К12 триажа `docs/BACKLOG_TRIAGE_2026-08-16.md`).

Обе карточки об одном механизме — перезаполнении бюджета, срезанного защитными тримами
(`redistribute_freed_budget` в `spa_core/paper_trading/risk_gate.py`). Корневая держит
диагноз (трим происходит в АЛЛОКАТОРЕ, а не в гейте, поэтому `asked − deployed = 0` и
`freed = 0`), эта — требование свести две реализации в одну, взяв базой прод-версию.
Разводить их по разным агентам нельзя: это один файл и один money-path.

**Корневая карточка остаётся ОТКРЫТОЙ** (корзина B).
