---
trackerStatus:
  type: inbox
title: Три рабочих дерева держат недоставленную работу — поднять или осознанно списать
status: done
created: 2026-08-14
---

## Что измерено (цикл #230, 2026-08-14)

Уборка мёртвых деревьев (`scripts/reap_stale_worktrees.py`, шаг 0г) сняла 43 регистрации из 70.
Остальные остались с НАЗВАННОЙ причиной, и у трёх причина одна: **там лежит работа, которой на
`origin/main` нет вовсе** (вердикт `absent`/`unique` — правило такие деревья не снимает).

- `/private/tmp/spa_wt_rnd49` — `scripts/edge_criterion_consensus.py` и
  `spa_core/tests/test_edge_criterion_consensus.py`: на origin нет НИ ОДНОГО из двух.
- `/private/tmp/spa_wt_c196` — `nimbalyst-local/tracker/own-rnd-duty-is-concentration-adr055.md`
  и `owner-decision-morfo-40-knigi-pri-propazhe-dannyh-podst.md`: карточки, которых нет в очереди
  на origin. Одна из них — про ДЕНЬГИ (40 % книги при пропаже данных).
- `/private/tmp/spa_wt_c208` — правка `docs/ORCHESTRATOR_PROTOCOL.md`,
  `spa_core/tests/test_consume_office_reports.py` и карточка
  `inbox-shag-0-ofis-iz-worktree-dokladyvaet-ne-p`. **Осторожно:** цикл #230 правил тот же
  протокол (шаг 0г) — переносить содержимое поверх нельзя, только сливать осознанно.

Ещё несколько `.claude/worktrees/*` держат `absent`-файлы (`spa_core/paper_trading/*.py` — 27
модулей аналитики; `spa_core/adapters/{radiant_arbitrum,gmx_glp_arbitrum}_adapter.py`, которые
ADR-070.17-18 велел вывести). Их разбирать отдельно и позже: часть — намеренно НЕ доставленное.

## Что сделать

1. По каждому дереву — порядок шага 0a: `ps -p <pid>` · announce-лог · **отчёту мёртвой сессии
   не верить** (перепроверять прогонами, а не читать её STATE).
2. Решить пофайлово: поднять (тесты зелёные → пуш) либо осознанно списать с причиной в журнале.
3. После подъёма дерево снимается обычным правилом (`reap_stale_worktrees.py --apply`).

## Границы

Карточки владельца из `spa_wt_c196` — **не исполнять и не закрывать**: их путь —
`needs-owner` + notify (инв. #14). Ничего из money-path сюда не входит.

## Как понять, что готово

`python3 scripts/reap_stale_worktrees.py` не показывает ни одного дерева с причиной
«здесь может лежать НЕДОСТАВЛЕННАЯ работа» — либо у каждого оставшегося есть запись в журнале,
почему списано.

Родитель: цикл #230 (карточка `inbox-shag-0a-povtoryaet-odni-i-te-zhe-6-nahod`).

---

## ✅ РАЗОБРАНО ПОФАЙЛОВО — цикл #249 (2026-08-15)

Замер сделан заново (`reap_stale_worktrees.py` + побайтная сверка с `origin/main` 05a3113ed),
а не переписан из карточки: за сутки картина изменилась у двух деревьев из трёх.

| дерево | пути | вердикт замера |
|---|---|---|
| `/private/tmp/spa_wt_rnd49` | `scripts/edge_criterion_consensus.py` + тест | **ПОДНЯТО** (см. ниже) |
| `/private/tmp/spa_wt_c196` | 2 карточки | **дерева больше нет**: 43 регистрации `git worktree`, его среди них ноль — поднимать нечего |
| `/private/tmp/spa_wt_c208` | протокол + тест + карточка | **superseded 10, снимается обычным правилом**: карточка `inbox-shag-0-ofis-iz-worktree-dokladyvaet-ne-p` на origin `done`, работу доставил другой цикл |
| `festive-wescoff` · `gifted-proskuriakova` · `parallel-claude` | 27 × `spa_core/paper_trading/*.py` | **доставлено ПОД ДРУГИМ ПУТЁМ**: те же модули живут как `spa_core/analytics_lab/*.py`, и версия origin НОВЕЕ — в ней запись через `atomic_save` (инв. #5), в копии сырой `open`. Списано. |
| `xenodochial-pare` | `radiant_arbitrum_adapter.py` · `gmx_glp_arbitrum_adapter.py` | **списано РЕШЕНИЕМ**: оба фида мертвы, ADR-070.17-18 велел эти адаптеры вывести. Доставить = вернуть выведенное. |
| 6 × `.claude/worktrees/*` | `.claude/settings.local.json` | **не доставляется by design**: `.gitignore:116` (`.claude/*.local.json`) — настройки машины, а не работа |
| `heuristic-gagarin` | 7 × `spa_core/data/*` | **это не работа, а УДАЛЕНИЕ**: в дереве каталог `spa_core/data/` снесён целиком (` D` в `git status`), на origin файлы живы. Доставлять удаление живых файлов нельзя. Списано. |
| `studio_bridge/spa-pilot/worktree` | `spa_core/utils/pct_format.py` + тест | **списано**: однострочный форматтер (`f"{x*100:.2f}%"`) от 22.07, на origin его не импортирует НИКТО (grep по `*.py`/`*.astro`/`*.ts` пуст). Доставка добавила бы модуль без вызывающего — ровно тот класс, который ловит храповик неподключённых. |

### Что поднято: R&D-работа из `spa_wt_rnd49`

Отчёту мёртвой сессии не верил — **числа перемерены своим прогоном**, скрипт запускался против
копии панели (только чтение, снята до полного прогона), файлов не пишет вовсе (проверено grep'ом
по всем формам записи).

**Найденная по дороге ловушка нумерации:** работа объявляла идеи реестра **#49 (XCV)** и
**#50 (SST)**, но за два дня, что она лежала недоставленной, ДРУГИЕ сессии заняли #49 (RDT) и
#50 (NTB). Переномерована в **#56 / #57** — свободные слоты, происхождение записано в докстринге
скрипта, чтобы следующий читатель не искал несуществующую связь с RDT/NTB. Это тот же класс, что
открытая карточка `inbox-nomera-adr-stalkivayutsya-dva-raza-za-de` (номера ADR), только в реестре
идей.

**Что дал замер (полностью — в `docs/DYNAMIC_LEVERAGE_GUARDIAN.md`, записи #56/#57):**

* **#56 XCV ❌** — жюри из четырёх критериев демоушена не бьёт лучшего одиночку **ни в одной из
  60 напечатанных ячеек** (лучшая ячейка +3.69 против +5.03 у #40 XSD), а объединение (j=1) хуже
  равновеса. При этом критерии измеримо НЕ коллинеарны (Jaccard 0.1–62.3 %, Спирмен −0.53…+0.46) —
  то есть различие между ними это ШУМ, а не информация. Ветка «консенсус-демоушен» закрыта.
* **⚠️ Побочная находка, которая важнее самой идеи:** на невиданной половине (TEST) #41 (+11.15),
  #45 (+11.06) и жюри (+11.05) СХОДЯТСЯ, а отрыв #40 (+6.32) живёт в TRAIN — значит опубликованный
  реестром порядок #40 > #41 > #45 частично артефакт обучающей половины.
* **#57 SST ❌✅⚠️** — как идея это дубль #51 SLT (написан 13.08, до её появления). Ценность в
  другом: **7 ячеек из 7 совпали до последней цифры** с независимой реализацией #51 (снят класс
  «ошибка в реализации лага»), ось продлена (d=30 +3.21, d=60 −0.45), и вскрылась поправка к
  практическому выводу #51: на TEST бюджет свежести **втрое короче**, чем читается по FULL
  (d=30 держит 29 % от d=0 против 64 % на FULL).

Границы соблюдены: advisory / OUTSIDE_RISKPOLICY / evidence L0, скрипт ничего не пишет,
RiskPolicy v1.0, пороги kill-switch, живой трек, `data/` и флот не тронуты. Карточки владельца
не исполнялись и не закрывались.

**Осадок назван честно:** панель, на которой считаны обе записи, ДЫРЯВАЯ — 4 книги из 10 мертвы
80 % выборки (`own-54`); числа сопоставимы с #35–#55 (та же панель), но «эдж по десяти книгам»
из них читать нельзя, и это сказано в обеих записях.

---

## 🔁 ПЕРЕПРОВЕРЕНО И ДОСТАВЛЕНО — цикл #250 (2026-08-15)

Работа #249 умерла между работой и пушем (`pid23248`, захват `stale`), на `origin` не было НИ
скрипта, НИ записей реестра, НИ этого разбора, НИ порождённой карточки. Подъём сделан по шагу 0a,
и **отчёту мёртвой сессии не верили — числа перемерены своим прогоном** на копии панели
(живой `data/` не тронут, скрипт не пишет ни одного файла):

| утверждение #249 | мой замер |
|---|---|
| одиночка #40 XSD ΔCalmar +5.03 / netAPY 25.94 % | +5.03 / 25.94 % ✅ |
| XCV j=1 ОБЪЕДИНЕНИЕ −1.61 / 5.58 % (хуже raw) | −1.61 / 5.58 % ✅ |
| j=2 +2.21 · j=3 +2.74 · j=4 −0.00 при обороте 0.17 | совпало до цифры ✅ |
| единогласие срабатывает 3 книго-дня из 8520 | 3 / 8520 (0.0 %) ✅ |
| лучшая ячейка сетки +3.69 (k=3 j=3 M=1), при M=20 +3.05 | +3.69 / +3.05 ✅ |
| поджюри дрейф+доход M=20 j=1 → +3.75 / 21.45 % | +3.75 / 21.45 % ✅ |
| Jaccard 0.1–62.3 %, Спирмен −0.529…+0.458 | совпало ✅ |
| контроли: перестановка 1/20 (p≈0.095), ротация 5/28 (p≈0.207), LOO худший +0.15 | совпало ✅ |
| TEST: #41 +11.15 · #45 +11.06 · жюри +11.05 против #40 +6.32 | совпало ✅ |
| **#57: 7 ячеек из 7 совпали с #51 SLT** | **прогнал ОБА скрипта сам: +5.03 +4.96 +5.10 +5.06 +5.15 +4.58 +3.98 — 7/7 ✅** |
| d=30 +3.21 · d=60 −0.45 (хвост, которого у #51 не было) | совпало ✅ |

Тесты поднятой работы: 23 passed + 3 skipped без панели, **26 passed** с панелью (skip'ы честные —
книги runtime-only, `data/` вне git).

**Сверх подъёма — закрыта ловушка, которую этот подъём и вскрыл.** Номера #56/#57 перепроверены на
СЕГОДНЯШНЕМ `origin` (свободны, дублей в реестре ноль при 55 записях), но ловил это глаз, а не
проверка. Поставлен храповик `spa_core/tests/test_edge_registry_numbers.py` (8 тестов) + правило
в шапке реестра: **номер берётся в момент ДОСТАВКИ**. Подробности — в порождённой карточке
`inbox-nomer-zapisi-reestra-beretsya-avansom-i` (закрыта тем же заходом).

Карточка закрывается: все три дерева разобраны пофайлово (#249), поднятое — доставлено и
перепроверено (#250).
