---
trackerStatus:
  type: inbox
title: "Закрытие карточки моста не возвращается на origin: находка, снятая 25 дней назад, каждый цикл читается как живая"
status: done
created: 2026-09-04
claimed_by: cycle-89751
claimed_at: 2026-09-04T09:49:56Z
status_trail:
  - "2026-09-04T10:10:07.336998+00:00 new -> done · queue.set_status · cycle-89751"
---

## Что случилось и почему это важно

Мост находок (ADR-066) закрывает карточку в ТОМ дереве, где работает, — в прод-дереве. Пуша в
нём нет ни строкой. Карточка `inbox-kartochki-mosta-nahodok-rozhdayutsya-v-p` (08.08, `done`)
закрыла направление РОЖДЕНИЯ: карточка, рождённая в проде, на origin не попадает никогда.

**Это зеркальная половина того же класса, и она открыта.** Карточка, которая на origin УЖЕ
ЕСТЬ, закрывается мостом в проде — и закрытие наверх не возвращается ничем. На origin она
остаётся в `new` навсегда, а очередь по §3.4 читается из worktree на `origin/main`. То есть
цикл видит живой находку, которой нет.

**Замер цикла #480 (2026-09-04) на живом входе:**

| где | статус |
|---|---|
| `origin/main` | `new` (создана 2026-08-10) |
| прод-дерево | `done` |
| `data/findings_bridge_state.json`, ключ `gap:analyst_red:red_team` | `status: closed`, `last_seen: 2026-08-11T07:03:01Z` |

Находка снята **11 августа**. Замер сегодня подтверждает независимо: `red_team.json` →
`posture: NO_THREAT_OBSERVED`, `posture_reason: []`, `threat_reactor_status.kill_switch_already_active: False`,
и ключа `gap:analyst_red:red_team` в свежем `house_view_gap.json` (05:48:48Z) нет вовсе.

**Цена измерена, а не предположена:** цикл #480 взял эту карточку как открытую работу моста
(мандат велит брать их первыми), прочитал тело, поднял два gitignored-артефакта и только тогда
установил, что работа сделана 25 дней назад. Это не разовая неудача: карточка простояла бы
`new` и дальше, потому что закрывать её на origin некому по построению.

## Что от тебя нужно

Ничего от владельца — задача агента. Взявшему:

1. **Не чинить это переносом статуса из прода.** Ровно на этом отказывает доставка сегодня
   (`inbox-most-otkazyvaet-verno-no-dolg-dostavki-n`): следы расходятся двумя законными
   переходами, и перенос стёр бы чужой. Здесь перенос не нужен — на origin у этой карточки
   следа (`status_trail`) нет вовсе, но полагаться на это в общем случае нельзя.
2. **Мерить, а не переносить.** Правильная форма — тот же приём, которым закрыт частный случай
   #480: сессия ПЕРЕМЕРЯЕТ критерий по живым артефактам и закрывает карточку СВОИМ вердиктом,
   дописанным к origin-версии. Это аддитивно и ничего не стирает.
3. **Кандидат на сторожа:** ключ в `findings_bridge_state.json` со `status: closed`, чья
   карточка на `origin/main` в НЕтерминальном статусе, — механически вычислимая находка.
   Сегодня её не называет никто: шаг 0-офис печатает дрейф прод↔origin только для
   `owner-decision`, а сторож ADR-208 работает лишь по карточкам с ОБЪЯВЛЕННОЙ пробой
   (их 5 на всю популяцию — эта в их число не входила).
4. **Положительный контроль обязателен в обе стороны:** ключ `closed` + карточка открыта на
   origin ⇒ краснеет; ключ `closed` + карточка закрыта на origin ⇒ молчит. Иначе сторож
   начнёт звонить на верном состоянии — механизм, которым сторожа глохнут.

## Как понять, что готово

Есть проверка, которая по `findings_bridge_state.json` и `origin/main` называет поимённо
карточки моста, закрытые в проде и открытые наверху; на сегодняшнем входе она либо пуста,
либо называет остаток списком, а не числом.

## Что будет после

Очередь перестанет предъявлять циклам работу, снятую недели назад. Класс «инструмент пишет в
дерево, которое доставка не поднимает наверх» будет закрыт с ОБЕИХ сторон, а не с одной.

_Найдено циклом #480 при исполнении карточки `inbox-nahodka-petli-analitik-red-team-critical`
(закрыта тем же циклом по живому замеру). Направление РОЖДЕНИЯ закрыто карточкой
`inbox-kartochki-mosta-nahodok-rozhdayutsya-v-p` (08.08). ADR-081 · ADR-066 · ADR-208._

---

## Вердикт цикла #481 (2026-09-04): сделано

Критерий приёмки закрыт: проверка существует, называет поимённо и меряет, а не переносит.

**Что доставлено.**

| что | где |
|---|---|
| сторож | `spa_core/monitoring/bridge_closure_drift.py` (`scan` · `run` · `python3 -m …`) |
| проводка | `spa_core/monitoring/loop_health.py` → ключ `closure_drift` в `data/loop_health.json` |
| читатель | `scripts/consume_office_reports.py` — обязательный шаг 0-офис печатает вердикт каждый цикл |
| контроль | `spa_core/tests/test_bridge_closure_drift.py`, 24 проверки |

**Форма — та, что просит п. 2 карточки:** сторож МЕРЯЕТ (ключ моста `status: closed`
против статуса карточки на `origin/main`) и ничего не переносит. Переноса статуса нет
ни одной строкой — ровно потому, что на нём отказывает доставка
(`inbox-most-otkazyvaet-verno-no-dolg-dostavki-n`).

**Положительный контроль в ОБЕ стороны (п. 4 карточки) — и в настоящей форме вызова.**
Авария #480 воспроизведена дословно (`gap:analyst_red:red_team` →
`inbox-nahodka-petli-analitik-red-team-critical`, `closed` здесь · `new` наверху) ⇒
краснеет; та же карточка `done` наверху ⇒ молчит. Проверено обеими формами: с
подставленной сверкой И живым `run()` через настоящий git-репозиторий. Последнее —
не педантизм: работа #474 держала 16 зелёных проверок ровно потому, что настоящий
вызов был вакуумен (#475). Мутационный замер: четыре мутации (правило «закрыта
наверху» · заслон «состояние не прочитано» · сам ключ в `loop_health` · строка
читателя) краснят 1–4 теста каждая; восстановление — 24 passed.

**Третий исход обязателен и он есть.** `data/` в worktree нет ПО ПОСТРОЕНИЮ, а
`origin/main` не разрешается в песочнице без репозитория. «Находок 0» оттуда было бы
fail-OPEN — тише красной строки и потому опаснее (класс #465). Поэтому непрочитанное
состояние и несостоявшаяся сверка дают `unmeasured` С ПРИЧИНОЙ, и она печатается вслух.
Заодно снят fail-OPEN в самом `loop_health.run`: прежнее `except: state = {}` стирало
разницу между «состояния нет» и «оно пустое».

**Замер на живом входе (2026-09-04, `origin/main` 20ef7a7df):** закрытых мостом
карточек 35, сошлось 35, открытых наверху 0, нет на origin 0. Пусто — и это ИЗМЕРЕННАЯ
пустота, как и предусматривал критерий («либо пуста, либо называет остаток списком»).
Частный случай, породивший карточку, снят циклом #480; сторож написан против КЛАССА.

**Границы названы, а не забыты.** `resolved_untouched` (карточку взял человек, мост её
не закрывал) предметом не является: открытый статус наверху там законен, и краснеть на
нём значило бы глушить сторожа. Карточка, закрытая мостом и отсутствующая на ref вовсе,
идёт ОТДЕЛЬНЫМ списком `absent_on_origin` — это направление РОЖДЕНИЯ, закрытое карточкой
`inbox-kartochki-mosta-nahodok-rozhdayutsya-v-p`; сложить два списка в одно число значило
бы предъявить циклу работу, которой нет.

_Цикл #481, worktree `/tmp/spa_c481` на `origin/main` 20ef7a7df._
