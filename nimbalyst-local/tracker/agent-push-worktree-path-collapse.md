---
trackerStatus:
  type: agent-task
title: push_to_github.py молча роняет worktree-пути в basename → файлы уезжают в корень репо
status: done
priority: high
source: session-2026-07-30-cycle40
created: 2026-07-30
---

## Что случилось

Цикл #40 запушил 6 файлов из изолированного worktree (`/tmp/spa_wt_dw40/...`), передав **абсолютные**
пути, как требует правило. Инструмент отчитался `OK` по всем шести с настоящими sha — но **все шесть
легли в КОРЕНЬ репозитория** под basename'ами (`dashboard_watcher.py`, `STATE.md`, `2026-W31.md`, …),
а настоящие пути остались со старым содержимым. Обнаружено только проверкой на чистом чекауте
`origin/main` («no tests ran» вместо ожидаемых 56). Исправлено вручную: перепуш с host-путей +
6 DELETE через GitHub API (HTTP 200), origin проверен — стрэев нет.

## Корень

`push_to_github.py:31` — `PROJECT_ROOT = Path("/Users/yuriikulieshov/Documents/SPA_Claude")` жёстко
задан, а `:131-134`:

```python
try:
    repo_path = str(local.relative_to(PROJECT_ROOT))
except ValueError:
    repo_path = local.name        # ← молчаливый fallback в basename
```

Любой путь ВНЕ хост-репо (а worktree всегда вне — `/tmp/...`) проваливается в `except` и теряет
каталог. Ошибки нет, лог показывает `OK`, sha настоящая — **fail-silent в самом инструменте доставки**.

Это гарантированно повторяющийся дефект: протокол оркестратора ОБЯЗЫВАЕТ работать в изолированном
worktree (§3.4), т.е. каждый автономный цикл по умолчанию находится в условиях срабатывания.
Память `push-to-github-absolute-paths` говорила «абсолютные пути» — правда, но недостаточная:
абсолютные **worktree**-пути ломаются ровно так же, как относительные.

## Что сделать

1. Определять корень репо по факту (`git -C <файл> rev-parse --show-toplevel`), а не по константе —
   тогда worktree-путь релятивизируется правильно.
2. **Убрать молчаливый fallback:** путь вне известного корня → жёсткая ошибка с текстом
   («не могу определить путь в репо для X»), а не basename. Fail-CLOSED, инвариант #2.
3. Синхронизировать **обе копии** `push_to_github.py` (см. память `autopush-pipeline-gotchas`:
   копии в корне и в `scripts/` должны совпадать) + проверить `safe_site_push.py`, который зовёт
   пушер под собой (та же ловушка для `landing/**`).
4. Тесты: (а) host-путь → правильный repo_path; (б) worktree-путь → правильный repo_path;
   (в) путь вне любого репо → ОШИБКА, не basename; (г) относительный путь → как раньше.

## Критерий готовности

Пуш из worktree кладёт файл по настоящему пути; путь, который нельзя релятивизировать, роняет пуш
с внятной ошибкой; обе копии инструмента идентичны; тесты зелёные.

## Сделано (цикл #41, 2026-07-30)

Все четыре пункта закрыты.

1. **Корень репо определяется по факту** — новая `push_to_github.py::repo_relative_path()`:
   `git -C <каталог файла> rev-parse --show-toplevel`, а принадлежность ТОМУ ЖЕ репозиторию
   сверяется по общему git-каталогу (`rev-parse --git-common-dir`) — точный признак linked
   worktree, а не «просто какой-то git-репо».
2. **Молчаливый fallback убран** → `RepoPathError` (fail-CLOSED, инвариант #2). Текст ошибки
   называет путь и подсказывает, что делать; `push_file` отдаёт честный `FAIL` **до** любого
   сетевого вызова, batch роняет ВЕСЬ батч (частичной доставки не бывает). Без `git` в PATH
   (launchd-окружение autopush) остаётся старый путь через `PROJECT_ROOT` — хост-пути работают.
3. **Копия логики ликвидирована:** `push_to_github_batch.py` больше не имеет своей реализации —
   грузит канонический модуль по явному пути (не через `sys.path`); не загрузился → падает сразу.
   `scripts/push_to_github.py` уже был тонким шимом. `safe_site_push.py` правок не потребовал:
   он делегирует в batch, и лечится тем же фиксом.
4. **Тесты:** `spa_core/tests/test_push_repo_path.py` (+16, герметичные — настоящие git-репозитории
   и worktree в `tmp_path`, сети нет), **14 красных ДО фикса**. Плюс гейт против рецидива класса:
   исходники обоих пушеров сканируются на возврат `local.name` и на наличие своей копии функции.

**Найдено сверх карточки:** дефект бил и по САЙТУ — `landing/src/pages/index.astro` из worktree
уезжал в корень как `index.astro`, т.е. страница НЕ менялась, а в корне появлялся стрэй; именно
batch-пушер стоит под `scripts/safe_site_push.py`. Воспроизведено на чистом `origin/main` до правок.

## Проверка

Чистый `origin/main` ДО правок: `dashboard_watcher.py` / `index.astro` → **`create`** (то есть цель
на remote не существует — это и был промах). После фикса те же worktree-пути дают
`spa_core/monitoring/dashboard_watcher.py` / `landing/src/pages/index.astro` → **`skip`** (совпали
с remote побайтово). Путь вне репо: `FAIL` + exit 1, пуша нет. Owner-gate по-прежнему заворачивает
owner-gated правку сайта (guard rc=2). Накладные расходы git-определения корня — 23 мс/файл против
уже имеющихся 300 мс/файл паузы в Contents-пушере ⇒ кэш не нужен.

## Доставка (цикл #42, 2026-07-30) — подобрана осиротевшая работа

**Цикл #41 сделал правку, но НЕ доставил её и умер** (`pid31439`, последняя запись в
`data/session_changes.jsonl` — «IN WORK» в 06:50, процесс мёртв). Правка лежала только в
`/private/tmp/spa_wt_push41`; на `origin/main` дефект оставался живым
(`push_to_github.py:134` → `repo_path = local.name`), карточка стояла `backlog`. Побочно на main
от той сессии остался **пустой коммит `ef908a279` «test interlock»** (0 файлов, содержимое main не
менял) — след теста interlock'а, не откатывался.

Цикл #42 не принял отчёт #41 на веру, а **перепроверил всё сам** и доставил.

**Независимая перепроверка (не чтение кода — прогоны):**
- новый `spa_core/tests/test_push_repo_path.py` — **16 passed**; после отката ОБОИХ пушеров к
  содержимому `origin/main` тот же файл даёт **14 failed / 2 passed** ⇒ тесты действительно
  пиннят фикс, а не проходят «сами по себе»;
- **поведенческий репро дефекта** на недофиксенном коде:
  `/private/tmp/spa_wt_push41/landing/src/pages/index.astro` → `index.astro` (страница сайта НЕ
  менялась бы, в корень уезжал бы стрэй). После фикса тот же путь → `landing/src/pages/index.astro`,
  `spa_core/monitoring/dashboard_watcher.py` → полный путь, host-путь → как раньше,
  путь вне репо → `RepoPathError` (fail-CLOSED);
- `tests/test_autopush_idempotent.py` **11 passed**; срез `tests/` — **12 749 passed / 1 failed**,
  падение `test_evidence_seeded.py::test_real_days_present` **предсуществующее** (воспроизведено на
  чистой базе `/private/tmp/spa_base41` без правок: 1 failed / 13 passed) и к пушеру отношения не имеет;
- `--collect-only` по всему дереву **104 041 → 104 057 = ровно +16**; 4 ошибки сборки
  (`attic/**`, `scripts/archive/**`) присутствуют **одинаково** до и после ⇒ импортных поломок нет;
- mypy: **те же 2 предсуществующие** ошибки `no-any-return` до и после (сместились номера строк),
  новых нет; `lint_llm_forbidden` — 163 файла / 0 нарушений.

**Проверено дополнительно (в отчёте #41 этого не было):** owner-gate interlock **не обходился**
дефектом. Гейт фильтрует по ЛОКАЛЬНЫМ путям аргументов (`"landing/" in str(f)`), а не по
вычисленному `repo_path`, и срабатывает до чтения PAT и до любого сетевого вызова ⇒ worktree-путь
`/tmp/.../landing/...` он опознавал и раньше. Фикс на гейт не влияет.

**Охват одной правкой — все три пути вызова:** `scripts/push_to_github.py` — тонкий шим над корневой
копией; `scripts/safe_site_push.py` делегирует в `push_to_github_batch.py`, который теперь грузит
канонический модуль. Отдельной копии логики не осталось (гейт против рецидива — в тестах).

**Критерий готовности выполнен**, карточка закрыта (`done`).
