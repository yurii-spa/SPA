---
trackerStatus:
  type: agent
title: Сверка цикла 04.08 — первый прогон на новом коде даст БОЛЬШОЙ ребаланс, это ожидаемо (не чинить)
status: done
source: session-2026-08-03 (после ремонта прод-дерева)
created: 2026-08-03
priority: high
domain: money-path (наблюдение; вмешиваться только при расхождении с ожиданием)
---

## Зачем эта карточка

2026-08-03 прод-дерево было отремонтировано: до этого оно крутило код на 409 коммитов старше
origin, и ни один из принятых ADR не работал. Цикл **04.08 06:00 UTC** — первый прогон на реально
доставленном коде. Он сделает **крупный ребаланс**, и это НЕ поломка: это отложенный эффект четырёх
принятых решений сразу.

**Любой сессии, которая увидит большой оборот в треке: сначала прочитать это, потом реагировать.**

## Что ожидается (прогон в песочнице 03.08 15:25, живой data/ не трогался)

| | было (03.08) | ожидается (04.08) |
|---|---|---|
| книга | pendle 20k · susde 10k · extra_finance_base 5k · morpho 40k · spark_susds 5k | pendle 20k · maple 20k · morpho_blue_base 10k · morpho 40k · compound_v3 5k |
| кэш | 20 000 (20 %) | 5 000 (5 %) |
| заявленный APY | 7.15 % (в основном по литералам) | **5.57 % — по наблюдённым числам** |
| защитный слой | passed | passed (T1 45 / T2 50 / T3 0), warning `single_chain_approaching` |

Оборот ≈ **$55k за один цикл**. Причины ухода позиций:
- `susde`, `extra_finance_base` → `advisory` (инвариант 9, ADR-061);
- `spark_susds` → `gsm_not_confirmed` (инвариант 10, ADR-061);
- освободившееся уходит в наблюдаемые `maple` / `morpho_blue_base` / `compound_v3`.

**Заявленный APY УПАДЁТ с 7.15 % до 5.57 % — это не ухудшение, а прекращение приписок.** Прежние
7.15 % считались по зашитым литералам (morpho 6.5 % при реальных 3.47 %, susde 12 %, extra 8 %).

## Как сверять (по порядку)

1. `data/current_positions.json` → сравнить с колонкой «ожидается».
2. `data/allocation_rationale.json` — впервые появится вердикт тени (ADR-060 фаза 0). Он ничего не
   двигает; читать `decision_shadow.reasons` и `cash.status`.
3. `python3 -m spa_core.monitoring.deployment_drift_monitor` — должен быть WARNING (расходится только
   документация), НЕ CRITICAL. CRITICAL ⇒ прод снова разошёлся по коду.
4. `data/adapter_status.json` → `live_count` должен быть > 0. Ноль ⇒ фид не ответил (разово бывает),
   перегенерировать: `python3 -m spa_core.monitoring.adapter_status_generator --run`.

## Когда ВМЕШИВАТЬСЯ (а когда нет)

- Книга совпала, оборот ~55k → **ничего не делать**, это план.
- Книга схлопнулась в кэш / осталось 1–2 протокола → смотреть `live_count` в `adapter_status.json`:
  скорее всего фид не ответил и гейт доказанности честно всех отсёк. Это не повод ослаблять гейт.
- Защитный слой REJECTED → реальная проблема, разбирать.
- Позиции не изменились вовсе → цикл не отработал ИЛИ код снова старый (проверить сторожа дрейфа).

## Что НЕ делать

Не «чинить» падение заявленного APY и не возвращать литералы в ранжирование. Не трогать прод-дерево
без владельца. Не ослаблять гейт доказанности, чтобы вернуть протоколы в книгу.

## Закрыто 16.08

**Посылка устарела: карточка была предупреждением на КОНКРЕТНУЮ дату.**

Она просила сверить первый прогон на доставленном коде — цикл **04.08 06:00 UTC** — с
ожидаемой раскладкой и не пугаться большого оборота (≈ $55k). Сегодня **16.08**: событие
прошло 12 дней назад, предупреждать некого, сверять нечего — окно наблюдения закрыто.

Числа из таблицы «ожидается» не пропадают: они остаются в теле карточки как исторический
замер песочницы 03.08. Ничего не чинилось, статус переведён бухгалтерией волны 0
(`docs/BACKLOG_TRIAGE_2026-08-16.md`).
