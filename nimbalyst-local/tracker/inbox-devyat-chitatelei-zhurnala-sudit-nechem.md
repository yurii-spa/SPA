---
trackerStatus:
  type: inbox
title: "Девять читателей журнала: «судить нечем» — их ответ несёт поля-возрасты, а прежнее «схлопывает» было артефактом"
status: done
source: nimbalyst
created: 2026-09-16
acceptance_probe: journal_reader_census_verdict_under_injected_clock
status_trail:
  - "2026-09-16T18:18:41.734249+00:00 new -> in-progress · queue.set_status · cycle-69075"
  - "2026-09-16T18:18:42.173902+00:00 in-progress -> done · queue.set_status · cycle-69075"
---

## Что измерено

Перепись читателей журнала решений не может вынести вердикт **девяти читателям** — и это
установлено, а не предположено. Их ответ несёт поля, производные от СТЕННЫХ ЧАСОВ (у
`house_view_gap` — 33 поля `age_s`, возраст входов в десятых долях секунды), и ВСЯ разница
между стендами лежит именно в них. Сравнивать после снятия нестабильного нечего, а судить по
снятому значило бы подбрасывать монету.

Двое из девяти — `house_view_gap` и `adapter_feed_divergence` — до ADR-396 именно этим и были:
три прогона на ОДНОМ дереве и ОДНОМ дне стенда давали `collapses_to_last` **1, 0 и 2**, код не
менялся ни на байт. Прежние вердикты «схлопывает» были АРТЕФАКТОМ хеширования производных от
часов, а не наблюдением о чтении журнала. ADR-396 убрал монету и назвал исход
(`verdict_rests_on_unstable_coords`), но НЕ ответил на сам вопрос.

## Чего НЕ измерено (это и есть предмет карточки)

**Схлопывают ли эти девять день журнала.** Ответ требует инъекции часов в САМ читатель (как
`_load_evidenced_apy(..., now=)`), а не нового приёма у прибора: пока `now` берётся у машины,
любой вердикт держится на том, попали ли две пробы в одну корзину.

Отдельно — и это важнее для приказа: `house_view_gap` печатает свой ответ в офис, а он тот
самый модуль, что три месяца врал в #206. ЧТО именно из его ответа доходит до владельца или в
брифинг и меняется ли это от схлопывания — не измерено.

## Почему не этим циклом

Цикл #615 доставлял прибор (ADR-396). Инъекция часов в девять читателей — правка ЧИТАТЕЛЕЙ, и
она не уместилась бы в контрольный прогон этого цикла; выдавать чужой вердикт за свой нельзя.

## Как понять, что готово

У каждого из девяти либо есть вердикт по ИСХОДУ на трёх стендах при ИНЪЕКТИРОВАННЫХ часах,
либо названа причина, по которой инъекция невозможна. Счётчик
`unmeasured_causes.verdict_rests_on_unstable_coords` в `data/run_identity_key_price.json` при
этом убывает, а не переименовывается.

---

## Цикл #619 (2026-09-16) — заказ G28, часть 1 исполнена: часы проведены, класс 13 → 4

**ADR-401.** Посылка карточки («ответ требует инъекции часов в САМ читатель… а не нового
приёма у прибора») опровергнута замером ДО работы и записана опровергнутой, а не тихо
отброшена: **26 из 26** читателей класса УЖЕ принимали `now=`. Правки читателей не
потребовалось ни одной — не проводила часы ПРОВОДКА переписи, ни в одной из трёх ветвей
связывания зова. Это «половина инъекции» из `.claude/rules/deployment.md` #453.

**Второе расхождение с карточкой — число.** В карточке девять, в артефакте на тот час 26,
в контрольном прогоне ЭТОГО дерева 13. Разные числа потому, что артефакт написан кодом до
заказа G27. База взята у контрольного прогона, а не у артефакта: иначе G28 приписал бы себе
чужую заслугу.

**Исход** — два ПОЛНЫХ прогона переписи (785 с и 772 с) последовательно, контроль
воспроизведён мутацией проводки:

| | часы стенные | часы проведены |
|---|---|---|
| `verdict_rests_on_unstable_coords` | 13 | **4** |
| `unmeasured` всего | 59 | **50** |
| `collapses_to_last` | 3 | **3** |
| вердиктов в ОБРАТНУЮ сторону | — | **0** |

Последние две строки — главный контроль: инъекция гасит ШУМ, а не РАЗНИЦУ между стендами.
Заказу приписаны **восемь** из девяти ушедших; `riskwire` — HTTP-маршрут, инъекция до него не
доходит, ушёл прогонным шумом.

**Остаток назван, а не растворён:** три HTTP-маршрута (пина часов у партии нет по построению)
и `decision_audit_trail`, где нестабильны `snapshot_id_probe.samples[*]` — координаты НЕ от
часов. Оба вошли в заказ G29.

**Приёмка объявлена пробой** `journal_reader_census_verdict_under_injected_clock` — четыре
звена, каждое рвётся отдельно, контроль в `spa_core/tests/test_injected_clock_census_probe.py`.
Счётчик населения в артефакте — ридер, а не гейт: артефакт пишет дневной цикл, и до его
ближайшего прогона население честно отвечает «НЕ ИЗМЕРЕНО».
