---
trackerStatus:
  type: inbox
title: Проверка живости pid СКИПАЕТСЯ, когда номер занят — сторож исчезает ровно тогда, когда хост враждебен
status: done
created: 2026-09-02
---

## Что измерено (цикл #453, 02.09, worktree от `origin/main` 61d203a4f)

`spa_core/tests/test_cycle_lock_watch.py::test_pid_alive_reports_a_missing_process_as_dead`
берёт номер `4_194_303`, спрашивает у ОС `os.kill(ghost, 0)` и, если номер ЗАНЯТ, делает
`pytest.skip("номер занят на этой машине — проверка неинформативна")`.

Найдено дифференциальным замером класса (#453): при подмене обеих дверей к ОС на «любой номер
жив» этот тест не краснеет и не проходит — он ИСЧЕЗАЕТ.

## Почему это находка, а не норма

Скип **измерен и назван** — это не молчаливое ослабление, инв. #16 не нарушен, и потому чинить
его «прицепом» к чужой правке было бы неверно. Но направление слабее того, что принято в этом
же классе на соседнем файле: `test_cycle_lock_holder_liveness._dead_pid()` при невозможности
найти свободный номер **падает ГРОМКО** («предпосылка теста не обеспечена»), а не снимает
проверку. Разница важна: скип означает, что единственная проверка «`_pid_alive` отвечает
`False` на отсутствующий процесс» пропадает ровно в тот день, когда ОС ведёт себя необычно, —
то есть когда она нужнее всего. «Не измерено» снова становится неотличимо от «прошло»
(тот же довод, по которому #343 снял скип в `test_cycle_lock_refusal_verdict.py`).

Число `4_194_303` тоже стоит перемерить: оно недостижимо на macOS (`PID_MAX` 99999), но равно
`pid_max - 1` на 64-битном Linux по умолчанию, то есть на CI (`ubuntu-latest`) вполне
достижимо — это ровно та ловушка, из-за которой #453 переписал `_dead_pid()`.

## Что сделать

1. Заменить «занят ⇒ skip» на измерение свободного номера у той же двери (как в
   `_dead_pid()` после #453), с громким отказом, если свободного не нашлось.
2. Проверить положительным контролем: под враждебной ОС (обе двери отвечают «жив») тест
   обязан КРАСНЕТЬ с внятным сообщением, а не исчезать.

## Как понять, что готово

Ни один тест набора не снимает проверку живости pid скипом; при невозможности обеспечить
предпосылку прогон говорит об этом вслух. Метод замера — в `.claude/rules/deployment.md`,
раздел «ЛИЧНОСТЬ ПРОЦЕССА в тестах».

---

## ЗАКРЫТО циклом #465 (2026-09-03, worktree /tmp/spa_c465 от `origin/main` bbb425692)

**Предпосылка перемерена перед взятием** — находка жива дословно: `pytest.skip` на
строках 263 и 267, литерал `4_194_303`.

**Что сделано.**

1. `spa_core/tests/test_cycle_lock_watch.py`: скип снят, литерал снят. Введён
   `_free_pid(probe=None)` — свободный номер ИЗМЕРЯЕТСЯ у **той же двери**, которой
   потом ответит проверяемый код (`os.kill(pid, 0)`, как в `_pid_alive`; у соседнего
   `_dead_pid()` дверь другая — `ps`, потому что там другой предмет). Диапазон
   90000..99000 действителен на обеих платформах. Не нашли свободного ⇒ **громкий
   отказ** с названным счётом неизмеримых номеров, а не снятие проверки.
   `OSError` больше не читается ни как «занят», ни как «свободен».
2. `spa_core/tests/test_live_data_write_attribution.py::_ps_bin_dir`: тот же класс с
   другой стороны — «нет `ps` ⇒ skip» заменён громким отказом. Докстринг модуля прямо
   говорит, что без `ps` контроль «зеленел бы и на мёртвом pid». На macOS и
   `ubuntu-latest` ветка не берётся ни разу — штатное поведение не изменилось.

**Положительный контроль — дифференциальным замером класса** (метод
`.claude/rules/deployment.md`, «ЛИЧНОСТЬ ПРОЦЕССА в тестах»): обе двери к ОС
(`os.kill`, `ps -p`) подменены на «любой номер жив».

| дерево | обычная ОС | враждебная ОС |
|---|---|---|
| `origin/main` bbb425692 (до) | 28 passed | 27 passed, **1 skipped** — проверка ИСЧЕЗЛА |
| после правки | **31 passed** | 30 passed, **1 failed** с внятным сообщением |

**Контроль на сам сторож (мутация).** Вернул `pytest.skip` внутрь `_free_pid` ⇒
`test_free_pid_refuses_loudly_when_every_number_is_alive` покраснел: «под враждебной ОС
предпосылка обязана падать ГРОМКО, а вышла через Skipped». Контроль ловит `BaseException`
намеренно: `pytest.skip` поднимает `Skipped(BaseException)`, и `pytest.raises(AssertionError)`
дал бы исход «skipped» — сторож замолчал бы ровно от того дефекта, против которого написан.
Обратная сторона закреплена отдельно (`test_free_pid_takes_the_first_number_the_os_calls_missing`):
иначе «починка» вида «всегда падать» была бы зелёной.

**Замер класса по всему набору после правки: население 0.** Единственный оставшийся
`pytest.skip` со словом «жив» — `test_tracker_card_forms.py:100` («живая карточка
положительного контроля отсутствует в этом дереве») — к живости процессов отношения
не имеет.

**Прогоны:** адресный `test_cycle_lock_watch + test_cycle_lock_holder_liveness +
test_cycle_lock_refusal_verdict + test_live_data_write_attribution` — **99 passed,
0 skipped**; полный предписанный прогон — см. журнал W36.

### ПОДЪЁМ И НЕЗАВИСИМАЯ ПЕРЕПРОВЕРКА — цикл #466 (2026-09-03, worktree /tmp/spa_c465_78305)

Сессия #465 (`cycle-52092`) умерла между «тесты зелёные» и пушем: работа осталась только в
`/tmp/spa_c465`, на `origin/main` её не было. Шаг 0a назвал это `ОСИРОТЕЛО`, шаг 0b — `stale`.
**Отчёт мёртвой сессии на веру не принят** (порядок шага 0a) — числа перемерены заново:

| замер (мой, цикл #466) | контроль `origin/main` bbb425692 | после правки |
|---|---|---|
| адресный прогон 5 файлов | 99 passed, 0 skipped | **106 passed, 0 skipped** |
| враждебная ОС (`os.kill` = «жив» на любой номер), `test_cycle_lock_watch` | 27 passed, **1 skipped** | 30 passed, **1 failed** внятно |
| мутация «вернуть `pytest.skip` в `_free_pid`» | — | **красный**: «под враждебной ОС предпосылка обязана падать ГРОМКО, а вышла через Skipped» |

Байты сверены с деревом #465 побайтово (`cmp`), после мутаций файлы восстановлены и сверены
снова. Авторство работы — #465; доставка и перепроверка — #466.
