---
trackerStatus:
  type: agent-task
title: Тесты защиты карточек пишут в НАСТОЯЩИЙ журнал объявлений — их вердикт зависит от накопленной истории, а не от кода
status: done
priority: high
source: session-2026-08-03-cycle105
created: 2026-08-03
domain: тесты инструментов протокола (spa_core/tests/test_card_claim_guard.py, scripts/check_card_claim.py); НЕ risk, НЕ сайт, НЕ деплой
---

## Как найдено (побочная находка цикла #105, замер — не догадка)

Цикл #105 чинил `agent-claim-guard-blind-when-session-pid-is-set`. Сразу после починки
`record()` в срезе покраснел тест, который до этого был ЗЕЛЁНЫМ и которого не было ни в одном
списке падений:

```
FAILED …test_card_claim_guard.py::TestClaimAndRelease::test_release_refuses_foreign_claim_without_force
E       Failed: DID NOT RAISE ClaimError
```

Это оказалась **не регрессия правки**, а накопленное загрязнение. Причина названа по коду:
`release_card` и `claim_card` принимают `log=DEFAULT_LOG` по умолчанию, а несколько тестов
зовут их **без** `log=`:

```python
# spa_core/tests/test_card_claim_guard.py:601
def test_release_refuses_foreign_claim_without_force(self, guard, tracker):
    write_card(tracker, "agent-x", claimed_by="pid999", claimed_at=_fmt(NOW))
    with pytest.raises(guard.ClaimError):
        guard.release_card("agent-x", session="pid1", tracker_dir=tracker)   # ← log не задан
```

⇒ каждый прогон дописывает НАСТОЯЩУЮ запись в `data/session_changes.jsonl` того дерева, из
которого запущен pytest. Замерено в worktree цикла #105 — 7 записей, накопленных за 4 прогона:

```
19:13:11Z | pid1    | card=agent-x done | session_pid=66563
19:13:11Z | pid999  | card=agent-x done | session_pid=66563
19:13:28Z | pid1    | card=agent-x done | session_pid=None
…
```

Доказательство, что дело именно в накоплении, а не в правке: очистка файла → тот же тест
**1 passed**, и один прогон пишет туда ровно одну новую запись.

## Почему это стоит починить

1. **Вердикт тестов зависит от истории, а не от кода.** `release_card` строит `selves` из
   этого журнала; накопленные записи с ярлыками `pid999`/`pid1` рано или поздно делают чужой
   захват «своим», и положительный контроль защиты от столкновения #46 гаснет — молча и не в
   том прогоне, где сломали код. Это тот же класс, что и сама карточка #105: проверка,
   утверждающая то, чего не измеряла.
2. **Радиус — живой журнал координации, и важно, что CLI тут ни при чём.** Умолчания РАЗНЫЕ,
   и это надо назвать точно, иначе следующая сессия починит не то:
   * **CLI безопасен** — `main()` берёт `sibling.shared_log()[0]`
     (`check_card_claim.py:1029`), то есть журнал ГЛАВНОГО рабочего дерева. Проверено на себе:
     `claim` цикла #105 из worktree лёг именно в общий журнал хост-репо.
   * **API-умолчание — нет:** `DEFAULT_LOG = ROOT / "data" / "session_changes.jsonl"`
     (`check_card_claim.py:104`), где `ROOT` — дерево самого файла. Замерено:
     ```
     из /tmp/spa_wt_c105  → /private/tmp/spa_wt_c105/data/session_changes.jsonl
     из хост-репо         → /Users/yuriikulieshov/Documents/SPA_Claude/data/session_changes.jsonl  (390 записей)
     ```
   Тесты зовут именно API, а не CLI ⇒ прогон набора В ХОСТ-РЕПО впишет выдуманные захваты в тот
   самый журнал, который читают шаги 0a и 0b протокола. Это ровно класс карточки
   `tests-write-live-alert-state` (тест мутирует живое состояние продакшена).
   Побочно: эти два умолчания расходятся между собой — стоит решить, не должно ли API-умолчание
   тоже быть `shared_log()`, либо не быть умолчанием вовсе.
3. **Докстринг файла уже утверждает обратное** — «Тесты герметичны: свой каталог карточек и
   свой журнал в `tmp_path`». Для этих тестов это неверно.

## Что измерено про ущерб (пока его нет)

Живой `data/session_changes.jsonl` хост-репо просканирован на тестовые ярлыки
(`pid1`/`pid999`/`pid500x`/`pidOTHER`/`pid9999`/`pid777`, а также записи со словами
`agent-x`/`agent-demo`): **0 записей**. Опасность латентная и НЕ сработала — потому что
протокол §3.4 обязывает работать в worktree, и наборы гоняли там. Одного прогона набора из
`~/Documents/SPA_Claude` достаточно, чтобы это перестало быть верным.

## Acceptance criteria

- ни один тест не пишет в журнал, который не создан в `tmp_path` (проверяется, а не
  обещается: гейт, который краснеет, если `claim_card`/`release_card` зовут без `log=`);
- ни один существующий ассерт не ослаблен (инв. #16) — правится ПРЕДУСЛОВИЕ теста, а не
  ожидание; мутационный контроль на каждый затронутый тест;
- докстринг файла про герметичность становится правдой либо исправляется;
- отдельно решить, не должен ли `DEFAULT_LOG` быть обязательным аргументом для пишущих
  путей (`claim_card`/`release_card`), чтобы «забыть `log=`» стало невозможно, а не просто
  не сделано;
- запись в `docs/journal/<неделя>.md`.

**Не входит:** RiskPolicy / kill-switch / живой трек / launchd / `landing/**`.

## ~~Пока не починено — что делать сессиям~~ (снято циклом #106)

~~Не гонять `spa_core/tests/test_card_claim_guard.py` из хост-репо~~ — ограничение снято:
теперь набор физически не может выбрать журнал сам. Оставлено как история находки.

---

## ЗАКРЫТО — цикл #106 (2026-08-03)

### Что подтвердилось замером, а не пересказом

Дефект воспроизведён на чистом `origin/main` (`3cd7b9808`) **в свежем worktree, где файла
`data/session_changes.jsonl` не было вообще** — то есть создал его именно прогон набора:

```
lines_before = файла нет
pytest spa_core/tests/test_card_claim_guard.py -q  →  120 passed
lines_after  = 2
прогон 2 → 4 · прогон 3 → 6 · прогон 4 → 8      (монотонно, +2 за прогон)
```

Записи — ровно те, что описаны в карточке (`pid1` и `pid999`, карточка `agent-x`,
`card_state: done`, в `files` — пути из `pytest-of-*/tmp`).

**Уточнение к карточке (важно для понимания механизма):** без `log=` звались ЧЕТЫРЕ вызова
(`test_card_claim_guard.py:598, 604, 605, 611`), но пишут из них только ДВА — 604 падает
`ClaimError` раньше объявления, а 611 выходит по ветке «захвата не было» до `announce_claim`.
Отсюда ровно 2 записи за прогон, а не 4. Остальные два вызова журнал всё равно **читают** —
и это второй симптом, ниже.

**Второй симптом — вердикт зависит от истории, а не от кода — тоже воспроизведён, и он ЖИВОЙ
после починки цикла #105.** На неизменном коде одна и та же карточка (`claimed_by: pid999`)
даёт два разных ответа в зависимости от содержимого журнала:

```
A. журнал пуст                    → ОТКАЗ  «карточку держит pid999, а не pid1»   (тест зелёный)
B. одна запись, ярлык pid999,
   якорь текущего процесса        → НЕ ОТКАЗАЛ                                   (тест: DID NOT RAISE)
```

### Развилка «какое умолчание правильное» решена замером: правильного нет

| кандидат | куда разрешается из worktree | почему не годится |
|---|---|---|
| `DEFAULT_LOG` (было) | `/private/tmp/spa_wt_c106/data/session_changes.jsonl` | журнал, которого не видит никто, — а `announce_claim` существует ровно ради немедленной видимости захвата без пуша |
| `shared_log()` (умолчание CLI) | `/Users/…/SPA_Claude/data/session_changes.jsonl` (**394 записи, живой**) | сделало бы тестовое загрязнение строго ХУЖЕ: набор из любого worktree писал бы прямо в живой журнал координации |

⇒ у пишущего пути умолчания нет вовсе: `log` — **обязательный** keyword-only аргумент
`claim_card`/`release_card`. «Забыть `log=`» стало `TypeError` в точке вызова (fail-CLOSED).

### Сделано

1. `scripts/check_card_claim.py` — `log` обязателен у обоих пишущих путей; развилка и оба
   замера записаны в докстринг `release_card`, чтобы умолчание не вернули «за чистоту API».
2. `spa_core/tests/test_card_claim_guard.py` — 4 вызова получили `log=log` (фикстура из
   `tmp_path`); двум тестам добавлена сама фикстура. **Инв. #16: правилось ПРЕДУСЛОВИЕ, ни
   один ассерт не изменён** — тесты стали строже (герметичны), а не слабее.
3. Докстринг файла про герметичность приведён к правде + ссылка на гейт.
4. Новый гейт `spa_core/tests/test_claim_guard_writes_are_hermetic.py` (+13 тестов, AST,
   ничего не исполняет). Пиннит ДВА факта, потому что одного мало: подпись без умолчания
   (иначе умолчание вернут — и все вызовы снова легальны) И отсутствие вызовов без `log=`.
   Сканер, не нашедший ни одной из функций, считается КРАСНЫМ (fail-CLOSED).

### Приёмка

* **Гейт на нефиксенном `origin/main`: 3 failed / 10 passed** — называет все 4 нарушения
  по `файл:строка` и оба умолчания подписи. Оставшиеся 10 — положительные контроли разбора
  (доказывают, что сканер видит, а не просто краснеет).
* **A/B на одном и том же наборе:** нефиксенное дерево — 120 passed **и 2 записи в реальном
  журнале**; фиксенное — 120 passed и **файл не создан**. Набор был ЗЕЛЁНЫМ, пока наносил
  ущерб, — поэтому и нужен гейт, а не внимательность.
* **Мутации, каждая краснит ровно свою проверку:** вернуть `log=DEFAULT_LOG` в `release_card`
  (вызовы фиксенные) → 1 failed, именно `test_log_is_a_required_argument[release_card]`;
  убрать `log=` у одного вызова (подпись обязательная) → 1 failed, именно
  `test_no_call_omits_log`; откат обеих → **13/13 зелёных**.
* **Прод не изменился:** единственный не-тестовый вызывающий — CLI `main()` — журнал задавал
  явно и раньше. Сквозной смоук CLI: `check` → `СВОБОДНА` (rc 0) → `claim` → `check` из другой
  сессии → `ЗАНЯТА` (rc 1) → `release`; в журнале ровно `claim` + `done`. Живой журнал
  хост-репо за весь цикл остался 394 строки.
* `spa_core/tests/test_session_state_shared_root.py` проверен отдельно — все его вызовы уже
  задавали `log=` явно, правок не потребовал (164 passed вместе с гейтом и целевым файлом).
* mypy на изменённом файле — `Success`; `lint_llm_forbidden` 167/0; гейт сужений набора
  `test_ci_test_exclusions.py` 18 passed (набор не сужен).

**Не делал:** RiskPolicy / kill-switch / пороги / живой трек / launchd / деплой / `landing/**`
не трогал; `data/**` не публиковал; читающий путь `gather` оставлен с умолчанием намеренно —
он ничего не пишет, а из worktree отвечает «НЕ ИЗМЕРЕНО», то есть ошибается в fail-CLOSED
сторону.

**Побочное наблюдение (не чинил, чужой файл):** `tests/test_pre_commit_hooks.py:74` содержит
невалидную escape-последовательность `'\|'` — `ast.parse` в гейте честно поднимает на ней
`SyntaxWarning`. Предсуществующее, к этой карточке отношения не имеет; глушить предупреждение
в гейте не стал — это было бы маскировкой.

---

## ДОСТАВЛЕНО — цикл #108 (2026-08-04): подъём осиротевшей работы + независимая перепроверка

Работа цикла #106 **не доехала до origin**: сессия `pid85434` умерла между «сделал» и
«запушил», всё осталось в `/tmp/spa_wt_c106` на базе `3cd7b9808`, а `origin/main` ушёл на
`f23fc4fa`. Шаг 0b дал `stale`, `ps -p 85434` — пусто. Команда `claim` **корректно отказала**
на `stale` (авто-захвата чужой работы нет по построению) — владение объявлено вручную, как
предписывает шаг 0a.

**Отчёт мёртвой сессии не принят на веру — каждое его утверждение перепроверено прогоном.**

### Что подтвердилось дословно

| Утверждение #106 | Мой замер |
|---|---|
| гейт на нефиксенном origin: 3 failed / 10 passed | **совпало**, и называет все 4 нарушения по `файл:строка` + оба умолчания подписи |
| набор ЗЕЛЁНЫЙ, но пишет 2 записи за прогон, монотонно | **совпало**: 120 passed, журнал `нет файла → 2 → 4 → 6`; записи ровно `pid1`/`pid999`, карточка `agent-x`, `card_state: done` |
| после починки журнал не создаётся | **совпало**: 208 passed на четырёх файлах — файла нет |
| `test_session_state_shared_root.py` правок не требует | **совпало**: все его вызовы уже задавали `log=` явно |
| прод не меняется (CLI задаёт журнал явно) | **совпало**: сквозной смоук исполнением, см. ниже |

### Что перемерено сверх отчёта

* **Перенос чистый, а не «наложился»:** `git diff 3cd7b9808 f23fc4fa` по всем переносимым
  файлам — **пусто** (за цикл #107 они не менялись), а `test_claim_guard_writes_are_hermetic.py`
  на `f23fc4fa` **отсутствует** ⇒ доставлено действительно ничего. STATE/журнал сироты я НЕ
  переносил (их база устарела на весь цикл #107) — записи свои.
* **Мутации свои, три, каждая краснит ровно свою проверку:** вернуть умолчание в `release_card`
  → `test_log_is_a_required_argument[release_card]`; то же в `claim_card` →
  `…[claim_card]`; убрать `log=` у одного вызова → `test_no_call_omits_log`. Откат → **13/13**.
* **Покрытие каталогов измерено, а не заявлено.** Гейт объявляет `SCANNED_DIRS =
  (spa_core/tests, tests, scripts)`. Подложен зонд-нарушение в КАЖДЫЙ из трёх — гейт краснеет
  во всех трёх случаях, после уборки зондов зелёный. (Заявленное покрытие, которое никто не
  проверил, — ровно класс находок #29–#40.)
* **Полный срез CI с контролем в том же окружении, ПОСЛЕДОВАТЕЛЬНО** (не параллельно —
  цикл #107 измерил, что параллельные прогоны дают фантомные падения через фиксированные порты
  `tests/test_fund_api.py`): моё дерево **1 failed / 91240 passed**, контроль на чистом
  `origin/main` — **1 failed / 91227 passed**. Падение одно и то же предсуществующее
  (`test_telegram_guard_stays_outermost`, своя карточка
  `agent-telegram-guard-outermost-fails-only-in-full-run`), дельта **ровно +13** = 13 тестов
  нового гейта, **новых падений ноль**. `tests/` — 12962 passed / 0 failed;
  `scripts/tests + gross_of + research/cards` — 493 passed (оба совпадают с числами #107).
* **Самый наглядный замер — на полном прогоне CI:** контрольное дерево на чистом origin после
  `spa_core/tests/` **создало `data/session_changes.jsonl` с 2 записями**, моё — **файла не
  создало вовсе**. То есть загрязнение наносила не «неаккуратная отладка», а штатный прогон CI.
* `mypy_gate` **Success**, `lint_llm_forbidden` **167/0**, `pyflakes` на трёх изменённых файлах
  чисто, гейт сужений набора `test_ci_test_exclusions.py` 18 passed (набор не сужен).

### Две поправки к отчёту #106 (обе — замер, не придирка)

1. **«`check` из другой сессии → ЗАНЯТА» воспроизводится не так, как записано.** Флага
   `--session` ОДНОГО НЕ ДОСТАТОЧНО, пока в окружении жив свой якорь `SPA_SESSION_PID`:
   `check … --session pidOTHER` по карточке, захваченной секундой раньше, отвечает
   **«СВОБОДНА — всё измерено»**, потому что личность разрешается по якорю, и захват
   опознаётся как СВОЙ. Это поведение верное (свой захват и должен быть своим), но проверять
   им защиту от столкновения нельзя. Настоящая другая сессия — якорь снят из окружения
   (`env -u SPA_SESSION_PID -u SPA_SESSION_ID`) либо со своим чужим якорем — даёт
   **⛔ ЗАНЯТА** оба раза. Защита от столкновения #46 на месте, но метод её проверки в отчёте
   был бы ложноуспокаивающим — поэтому записан здесь.
2. **«Живой журнал остался 394 строки» — у меня не так и не должно быть так:** сквозной смоук
   пишущих путей через CLI по построению кладёт в общий журнал по 2 легитимные записи
   (`claim` + `done`) на прогон; у меня 399 → 405. Это не загрязнение (записи настоящие,
   именованные, разовая карточка `agent-c108-smoke` удалена в том же цикле), но утверждение
   «журнал не изменился» после смоука пишущих путей проверять нечем.

### Смоук прод-пути исполнением (подпись стала обязательной — одного mypy мало)

`check` → **СВОБОДНА** · `claim` → взята `cycle-18018`, `claimed_by` лёг в карточку ·
`check` настоящей другой сессией → **⛔ ЗАНЯТА**, и в разборе видно ОБА захвата: из
frontmatter (активность не измерима — ярлык без pid) и из объявления (**`pid18018` жив, тот же
процесс**) ⇒ вердикт честен, «не измерено» не выдаётся за «свободно» · `release` → отпущена ·
`check` → СВОБОДНА. Разовая карточка удалена, дерево чисто.

**Не делал:** RiskPolicy / kill-switch / пороги / гейты риска / живой трек
(`data/equity_curve_daily.json`) / launchd / деплой / `landing/**` не трогал ⇒
`safe_site_push.py` не требовался; `data/**` не публиковал; **ни один существующий ассерт не
изменён и не ослаблен** (инв. #16) — правились предусловия вызовов, правки строго аддитивные.
