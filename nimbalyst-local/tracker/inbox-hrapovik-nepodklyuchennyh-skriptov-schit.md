---
trackerStatus:
  type: inbox
title: Храповик неподключённых скриптов считает УПОМИНАНИЕ В КОММЕНТАРИИ за проводку — слепота стоит трёх скриптов
status: done
source: nimbalyst
created: 2026-08-12
claimed_by: pid10038
claimed_at: 2026-08-14T09:29:14Z
---

## Что нашли

`spa_core/tests/_unwired.py:49` решает, «подключён ли скрипт», простым поиском ПОДСТРОКИ
имени файла по коду:

```python
if not any(p != m and (needle_file in t or needle_mod in t) for p, t in hay):
```

Комментарий от вызова не отличается. Значит **любое упоминание имени скрипта в
комментарии молча снимает его с учёта храповика** — навсегда и без единого сигнала.

## Как нашли (на себе, цикл #205)

Разбор в шапке `scripts/run_health_check.py` честно называл его единственного вызывающего —
`run_daily_simulation` — вместе с расширением. Полный прогон покраснел второй половиной
храповика:

```
FAILED test_the_baseline_does_not_list_scripts_that_are_now_wired
AssertionError: ['run_daily_simulation'] != []
```

То есть **комментарий, объясняющий, что скрипт НЕ подключён, сделал его «подключённым»**.
Это не курьёз: так храповик теряет подопечных тихо и необратимо.

## Замер слепоты (не оценка — прогон)

Сделал сканер комментарио-слепым (для `.py` — токенайзер, для `.sh` — обрезка по `#`) и
сравнил множества:

| | скриптов без вызывающего |
|---|---|
| как сейчас (комментарий считается проводкой) | **87** |
| комментарии не считаются | **90** |

Разницу дают три скрипта, которых сегодня держит «подключёнными» ровно комментарий:

- `run_daily_simulation` (мой случай, цикл #205);
- `daily_paper_report`;
- `guardian_backtest`.

Последние два — чужие и давние: их слепота укрывала до этого замера.

## Почему НЕ починил тем же циклом

Починка добавляет три скрипта к неподключённым, и `test_no_NEW_unwired_script_appears`
законно покраснеет. Погасить это дописыванием в базу **запрещает сам храповик** своим же
текстом («НЕ добавляй в базу, чтобы погасить падение»). Значит починка неотделима от
решения по трём скриптам — подключить или обосновать поштучно, — а это отдельная работа,
не хвост чужого цикла.

Временная мера в #205 названа вслух, а не сделана молча: имя в комментарии написано без
`.py`, полная запись с путями живёт в карточке и журнале W33. С учёта никто не снят.

## Что сделать

1. Сделать сканер комментарио-слепым: `.py` — через `tokenize` (как в
   `test_critical_alerts_never_use_retired_manager.py`, там ровно этот приём и по той же
   причине), `.sh`/`.yml` — обрезка по `#`. Строковые литералы ОСТАВИТЬ: `subprocess.run
   (["python", "scripts/x.py"])` — настоящий вызов.
2. Разобрать три вскрывшихся скрипта поштучно: подключить либо обосновать в карточке.
   **В базу не дописывать.**
3. Положительный контроль: файл, где имя скрипта есть ТОЛЬКО в комментарии, обязан
   оставаться «неподключённым»; файл с настоящим вызовом — «подключённым». Мутация:
   вернуть текстовый поиск — тест краснеет.
4. Проверить обратную сторону: не считает ли сканер проводкой упоминание в docstring —
   он от комментария не отличается по последствиям.

## Границы

Только тестовый слой. RiskPolicy, kill-switch, живой трек, флот и деплой не затрагиваются.
Ослаблять храповик нельзя: починка делает его СТРОЖЕ (подопечных станет больше, не меньше).

## Как понять, что готово

Имя скрипта в комментарии больше не считается вызовом; три скрипта разобраны поштучно и
база не выросла; положительный контроль краснеет на возврате текстового поиска.

---

## ИСПОЛНЕНО — цикл #227, 2026-08-14

### Сканер стал комментарио-слепым (`spa_core/tests/_unwired.py`)

Новая функция `code_without_comments(path, text)` — и вызов из `scripts_without_caller`:

- `.py` — токенайзер, комментарии вырезаются **координатами**, не пересборкой из токенов;
- `.sh` / `.yml` / `.yaml` — обрезка по `#` **с учётом кавычек** (`"tag #1: python3 …"` не режется);
- `.plist` — XML-комментарий `<!-- … -->`;
- не разобравшийся `.py` уходит не в сырой текст, а в ту же обрезку по `#` — молчаливого
  возврата к слепоте нет;
- строковые литералы СОХРАНЕНЫ: в них живёт настоящий вызов.

**Пересборка из токенов ломала проводку — поймано своим же положительным контролем.**
Первая редакция склеивала токены разделителем, и `from scripts.<имя> import …` рвалось на
куски: живой скрипт объявлялся сиротой. Тест `test_module_import_form_is_a_call` покраснел
сразу; отсюда вырезание координатами.

### Замер разошёлся с карточкой — и это названо, а не подогнано

Карточка (12.08) обещала три скрипта: `run_daily_simulation`, `daily_paper_report`,
`guardian_backtest`. Свой замер 14.08 на `881893a3d` даёт **один**: `daily_paper_report`.
Причина — правило реестра R&D (#214) и правки между 12.08 и 14.08: `run_daily_simulation`
уже лежит в базе храповика, `guardian_backtest` держится не комментарием, а докстрингом.
Числа карточки не подтвердились; вместо повторения чужой цифры — свой прогон.

### Поштучный разбор вскрывшегося: `daily_paper_report` СПИСАН

Не «подключён» и не дописан в базу — списан, потому что он мёртв по трём независимым следам:

1. агент `com.spa.daily-paper-report` отключён с 21.06 (`…plist.disabled`, launchctl пуст);
2. ни один plist / обёртка / CI его не зовёт — единственное упоминание было тем самым
   комментарием, объяснявшим, что скрипт НЕ подключён;
3. класс схлопнут в ОДИН дневной дайджест — дословно в `spa_core/telegram/reports/__init__.py`,
   а секция Base перенесена в `daily_telegram_report` («merged from the former
   scripts/daily_paper_report.py»).

Держать его дальше — не нейтрально: у скрипта якорь трека `PAPER_START = 2026-06-12` против
доказанного 2026-06-22, и ручной запуск отправил бы владельцу ВТОРОЙ утренний отчёт с
неверным числом дней.

**Тест не выключен, а переставлен на живой путь** (инв. #16): `tests/test_daily_paper_report_base.py`
(проверял мёртвый скрипт) → `tests/test_daily_report_base_section.py` (те же четыре вопроса
заданы `daily_telegram_report`, который действительно уезжает владельцу), плюс сторож против
воскрешения дубля. Обоснование — в журнале W33.

### Что найдено по дороге и вынесено картсочками, а не починено молча

- `inbox-hrapovik-schitaet-upominanie-v-dokstring` — **обратная сторона (п. 4 карточки) измерена**:
  «подключёнными» держатся ещё 8 скриптов, и не одной слепотой, а тремя — докстринг (5),
  самоупоминание однофамильца из другого каталога (`spa_core/riskwire/day30_review.py`,
  `spa_core/audit/ots_anchor.py`), подстрочные коллизии (`perf_budget` ⊂ `dfb_perf_budget`).
  Чинить в лоб ОПАСНО: `check_tracker_drift` подключён настоящим `import check_tracker_drift`
  (`scripts/orchestrator_queue.py:198`), которого сканер не видит вовсе — докстринго-слепота
  дала бы ложную сироту, то есть красный храповик на честной работе.
- `inbox-stroka-risk-gate-dnevnogo-limita-ubytka` — у списанного скрипта была секция
  `DailyLimitsChecker` (Risk Gate PASS/WARN/HALT), которой нет в живом отчёте. Сам чекер жив
  (зовётся из `cycle_runner`), тёмной была витрина — и тёмной она стала не сегодня, а 21.06.

### Приёмка

12 новых тестов (`spa_core/tests/test_unwired_comment_is_not_a_caller.py`) + 7 переставленных;
5 мутаций краснят поимённо: снятая починка = 6 красных · пересборка из токенов = потерянный
импорт · обрезка без кавычек · plist без XML-комментариев · сырой текст на битом `.py`.
Слабая редакция проверки кавычек мутацию НЕ ловила — усилена в том же цикле.
База храповика **не выросла** (54 имени до и после), устаревших записей ноль.
