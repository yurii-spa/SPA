---
trackerStatus:
  type: inbox
title: "Шаг 0a приписывает сессии расхождение ЧУЖИХ рабочих деревьев: 12 находок из 49 ложны, и три самых объявляемых файла обречены на них навсегда"
status: done
source: nimbalyst
created: 2026-08-15
claimed_by: cycle-23548
claimed_at: 2026-08-16T02:20:41Z
---

## Что измерено (цикл #252, 2026-08-16, прод-дерево, база `origin/main` be8c5a3c2)

Шаг 0a назвал **14 находок** (5 «осиротело, окно не истекло» + 9 «НЕ ДОСТАВЛЕНО»).
Сверка руками по коммитам: **всё названное лежит на `origin`** —
`92e1bee72` (#243) · `71fd6ee39` (#246) · `05a3113ed` (#248) · `d0bf9d843` (#250) ·
`be8c5a3c2` (#251). Ни одной потерянной работы среди них нет.

**Механизм — измерен, не предположен.** `file_state()` спрашивает «расходится ли этот путь
хоть в ОДНОМ рабочем дереве», и `unseen` (строка ~1064) собирается по ВСЕМ деревьям сразу:
достаточно одного дерева, чьё содержимое не встречалось в истории `origin`, чтобы находка
осталась жить — и она приписывается КАЖДОЙ сессии, когда-либо объявлявшей этот путь,
включая ту, чьё собственное дерево совпадает с `origin` побайтно.

Пример дословно: `cycle-250` объявила `docs/DYNAMIC_LEVERAGE_GUARDIAN.md` из своего дерева и
доставила его коммитом `d0bf9d843`; её дерево `/private/tmp/spa_c250` для этого пути чистое.
Находка тем не менее напечатана — потому что тот же путь расходится в
`/Users/…/SPA_Claude`, `/private/tmp/mine_origin`, `/private/tmp/spa_c249`,
`/private/tmp/spa_wt_rnd49`.

**Замер по всему журналу (872 записи, 46 живых деревьев):** 49 находок, из них

| Что | Сколько |
|---|---|
| **ложная атрибуция** (своё дерево сессии совпадает с `origin` или его историей) | **12** |
| настоящая недоставленная работа (своё дерево расходится) | 11 |
| дерево объявления не определяется (относительный путь / дерева нет) | 26 |

## Почему это не косметика и почему само не рассосётся

Класс — «сторож отвечает не на тот вопрос»: находка честна по своему контракту
(«содержимого этих деревьев в истории `origin` нет») и ложна по вопросу, который читает
оркестратор («есть ли потерянная работа?»).

Самовоспроизводится по построению. `docs/STATE.md` расходится в **25** деревьях,
`docs/journal/2026-W33.md` — в 24, `nimbalyst-local/tracker/_BOARD.md` — в 24. Это ровно те
три файла, которые правит КАЖДЫЙ цикл и объявляет КАЖДАЯ сессия. Прод-дерево из этого списка
не убирается никогда: щит #234 запрещает его снимать, а `docs/` и `nimbalyst-local/` туда не
синкаются вовсе (#193) — то есть у трёх самых объявляемых путей источник расхождения вечен.
Уборка деревьев (#230) класс не закрывает: она снимает осадок, который набегает снова с
каждым новым циклом.

Цена уже уплачена: этот цикл потратил разбор пяти коммитов, чтобы выяснить, что поднимать
нечего. Следующий научится пролистывать раздел целиком — и настоящие 11 уедут вместе с 12.

## Что сделать

1. Судить объявление по СВОЕМУ дереву. Дерево не угадывается: абсолютный путь в самой записи
   его называет. Своё дерево совпадает с `origin` (или его содержимое уже встречалось в
   истории этого пути) ⇒ эта сессия доставила, находки по ней нет.
2. Покрытие не сужать. Расхождение в чужом дереве продолжает быть находкой — у ТОЙ сессии,
   которая объявляла путь оттуда: она проверяется тем же кодом на своём заходе.
3. Не молчать. «Доставлено своим деревом, расходятся чужие» — отдельная названная строка,
   а не тишина.
4. Дерево объявления не определяется (относительный путь, каталога нет) ⇒ поведение прежнее,
   fail-CLOSED. Никакого «не измерено ⇒ считаем доставленным».

## Границы

`scripts/check_undelivered_work.py` не ослаблять: положительный контроль обязателен —
настоящая потеря (своё дерево расходится содержимым, которого нет в истории) остаётся
находкой с прежним кодом возврата. Окно ожидания (3 ч) НЕ трогать (#233). Правило снятия
деревьев (#230) не трогать. RiskPolicy, kill-switch, живой трек, `data/`, флот, деплой,
`landing/` — не сюда.

Родитель: цикл #252 (шаг 0a). Соседи того же класса, оба закрыты и причиной ОТЛИЧАЮТСЯ:
`inbox-shag-0a-povtoryaet-odni-i-te-zhe-6-nahod` (осадок мёртвых деревьев, лечился уборкой),
`inbox-shag-0a-vechno-dokladyvaet-o-faile-kotor` (имени не существовало нигде).

---

## Исполнено — цикл #252 (2026-08-16, worktree от `origin/main` `be8c5a3c2`)

**П.1 — судим по своему дереву.** `declaring_tree()`: дерево, ИЗ КОТОРОГО объявлен путь,
разрешается тем же принципом, что `resolve_rel` (общий git-каталог + `rev-parse --show-toplevel`).
Удалённое дерево соседним НЕ подменяется — подстановка была бы той самой чужой атрибуцией;
причина «каталогов больше нет» отделена от «чужой репозиторий», как уже сделано в `resolve_rel`.

**П.2 — покрытие не сужено.** Находка остаётся и держит **прежний код возврата**; тень снимается
ТОЛЬКО при живой находке по тому же пути, и тогда обвинение достаётся сессии, которая работу
удержала. Мутация «снимать тень всегда» краснит пять тестов, включая ЧУЖОЙ существующий
`test_change_living_only_in_a_worktree_is_found` — сужение ловится и старым набором.

**П.3 — не молчим.** Новый раздел отчёта «🌲 РАСХОЖДЕНИЕ ТОЛЬКО В ЧУЖИХ ДЕРЕВЬЯХ» называет и
своё дерево, и чужие, и говорит вслух: чьи это байты — **НЕ ИЗМЕРЕНО**.

**П.4 — fail-CLOSED цел.** Дерево не определяется (относительный путь / каталога нет) ⇒ прежний,
более строгий вердикт, а причина дописана в саму находку. Мутация «не определено ⇒ доставлено»
краснит ровно свой тест.

### Живой замер до/после (последние 20 записей — то, что читает цикл)

| Раздел | origin `be8c5a3c2` | после |
|---|---|---|
| 🕳 осиротело, окно не истекло | 3 | **0** |
| ⚠️ НЕ ДОСТАВЛЕНО | 9 | **4** |
| 🌲 только чужие деревья | — | 8 |
| ❓ не измерено · код возврата | 11 · 2 | 11 · 2 |

«Поднимай руками» 12 → 4, и оставшиеся четыре — настоящие (`spa_c244`, `spa_c249`, `spa_wt_c208`,
те же, что названы в STATE). Ничего не исчезло: 12 → 4 + 8.

### Приёмка

97 passed (87 прежних + 10 новых). Контроль на чистом `origin/main` того же sha: **7 красных, из
них 5 на ПОВЕДЕНИИ**; 2 красных по `AttributeError` — юнит-тесты новой функции, поведенческим
контролем не считаются. **3 зелёные на обоих деревьях намеренно** — объявленные обратные контроли.
Мутации M1/M2/M3/M5 краснят каждая свою цель. **M4 не покрасила ничего**, и это записано в теле
теста, а не замазано: `realpath`-нормализация оставлена страховкой и мутацией НЕ доказана.

Ни один существующий тест не ослаблен, не отключён и не изменён (инв. #16 — без исключений).
Окно ожидания 3 ч (#233) и правило снятия деревьев (#230) не тронуты. RiskPolicy, kill-switch,
живой трек, `data/`, флот, `landing/`, деплой — не тронуты. Журнал — `docs/journal/2026-W33.md`.

---

## Доставлено — цикл #253 (2026-08-16, подъём осиротевшей работы)

Сессия #252 умерла между работой и пушем (`pid14140`/`pid14154` мертвы, захват `stale`); на
`origin` не было ни правки шага 0a, ни тестов, ни этой карточки. Работа поднята из
`/private/tmp/spa_c252`, базы совпали (`git diff be8c5a3c2 db59c98f6` по всем шести путям пуст).

**Отчёту мёртвой сессии не верил — перемерил сам:** 97 passed в своём дереве; на чистом
`origin/main` того же sha те же **7 красных**, разложенные по причине своим прогоном (5 —
`AssertionError` на ПОВЕДЕНИИ, 2 — `AttributeError` юнит-тестов новой функции); четыре СВОИХ
мутации, каждая красит свою цель, а мутация «сужаем покрытие» красит и ЧУЖОЙ существующий
`test_change_living_only_in_a_worktree_is_found`.

**Живой замер #253 (окно сдвинулось за 1.5 ч — таблица выше не переписана, она верна на свой
момент):** «⚠️ НЕ ДОСТАВЛЕНО» **9 → 3**, «🌲 только чужие деревья» 5, «🕳 осиротело» 6 — и эти
шесть суть недоставленная работа #252, то есть верный ответ сторожа. Каждая из пяти переехавших
строк проверена по коммиту: `71fd6ee39` (#246), `05a3113ed` (#248), `d0bf9d843` (#250) —
потерянной работы среди них нет ни одной.

Журнал: `docs/journal/2026-W33.md`, цикл #253.

---

## Доставлено — цикл #254 (2026-08-16, ВТОРОЙ подъём той же работы)

Умерла и сессия-подъёмщик: `pid75962` (#253) мёртв, захват `stale`, на `origin/main`
`f3520165e` по-прежнему нет ни правки шага 0a, ни тестов, ни этой карточки. Три захвата
подряд (`cycle-14140`, `cycle-252`, `cycle-253`) — все осиротевшие. Базы совпали: между
`db59c98f6` (дерево #253) и `f3520165e` изменён ровно один файл —
`landing/src/data/track_snapshot.json`, пересечение с работой ноль. Код в `/tmp/spa_c252` и
`/tmp/spa_c253` побайтно ОДИНАКОВ (карточка отличается только разделом #253).

**Отчётам ДВУХ мёртвых сессий не верил — перемерил своим прогоном, всё воспроизвелось:**

| Что | Отчёт #252/#253 | Мой замер |
|---|---|---|
| свой прогон файла тестов | 97 passed | **97 passed** |
| контроль на чистом `origin/main` того же sha | 7 красных (5 поведение / 2 `AttributeError`) | **7 красных, 5 `AssertionError` / 2 `AttributeError`** (разложено своим прогоном) |
| M1 «не судим по своему дереву» | красит цель | **4 failed** |
| M2 «тень снимается всегда» (сужение покрытия) | красит, вкл. чужой тест | **5 failed, среди них `TestWorktreePaths::test_change_living_only_in_a_worktree_is_found`** — проверено, что этот тест ЕСТЬ на `origin` (сужение ловится и старым набором) |
| M3 «не измерено ⇒ доставлено» | красит fail-CLOSED | **1 failed** |
| M4 «без `realpath`» | НЕ красит — страховка, мутацией не доказана | **97 passed** — подтверждаю, честно оставлено как есть |

**Живой замер #254 (прод-дерево, база `f3520165e`, сходится до строки):**

| Раздел | origin | после |
|---|---|---|
| 🕳 осиротело, окно не истекло | 6 | 6 (это и есть недоставленное #252/#253 — верный ответ) |
| ⚠️ НЕ ДОСТАВЛЕНО | **7** | **3** |
| 🌲 только чужие деревья | — | **3** |
| ❓ не измерено · код возврата | 11 · 2 | 11 · **2** |

Сальдо сходится и седьмая строка НЕ потеряна: 7 = 3 настоящих + 3 чужих + 1 тень
(`_BOARD.md` — по этому пути есть НАСТОЯЩАЯ находка в разделе 🕳, и путь остаётся напечатан
там же; снимается второе упоминание тех же байтов, а не находка).

Ни один существующий тест не ослаблен, не отключён и не изменён (инв. #16). Окно 3 ч (#233),
правило снятия деревьев (#230), RiskPolicy, kill-switch, живой трек, `data/`, флот, `landing/`,
деплой — не тронуты. Журнал: `docs/journal/2026-W33.md`, цикл #254.
