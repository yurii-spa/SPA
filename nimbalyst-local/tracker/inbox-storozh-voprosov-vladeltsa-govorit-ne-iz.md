---
trackerStatus:
  type: inbox
title: Сторож вопросов владельца говорит «не измерено» там, где ответ лежит на origin — механика (origin_view) уже есть
status: done
source: nimbalyst
created: 2026-08-17
claimed_by: cycle-273
claimed_at: 2026-08-17T09:20:33Z
---

## Что измерено (цикл #272)

`owner_decision_pending` в проде даёт **WARNING** с тремя строками `[НЕ ИЗМЕРЕНО]`:

```
card_missing:owner-decision-geit-i-allokator-schitayut-zhivoi-tvl-po
card_missing:owner-decision-vozit-li-katalog-reshenii-ob-agentah-na
card_missing:owner-decision-dolgozhivuschie-agenty-krutyat-kod-mnogo
```

Формулировка честная: «карточка закрыта, а просто не доехала в прод» и «вопрос открыт и потерян»
снаружи выглядят одинаково, поэтому вердикта нет ни в одну сторону.

**Но ответ есть, и он в одном запросе от нас.** Сверка с `origin/main` этим же циклом:

| карточка | статус на `origin/main` |
|---|---|
| `...geit-i-allokator-schitayut-zhivoi-tvl-po` | **`ingested`** — вопрос ЗАКРЫТ |
| `...vozit-li-katalog-reshenii-ob-agentah-na` | **`ingested`** — вопрос ЗАКРЫТ |
| `...dolgozhivuschie-agenty-krutyat-kod-mnogo` | файла нет и на origin |

То есть два из трёх «не измерено» — это доброкачественная ветка (дрейф прод↔origin, УРОК #193:
автосинк возит только `spa_core/`·`scripts/`·`tests/`), и она уже неделю держит сторожа в WARNING.

## Почему это стоит починить, а не терпеть

Читатель `owner_decision_pending` — обязательный шаг 0-офис. Постоянный WARNING по доброкачественной
причине — это тот самый класс «сторож отвечает не на тот вопрос»: настоящая находка того же ранга
утонет в трёх вечных строках, которые все привыкли пролистывать.

Механика для ответа **уже доставлена** и ходит рядом: `spa_core/owner_queue/origin_view.py` (#270,
очередь в версии `origin/main`, read-only, без сети, 0.05 с) — тот же модуль, которым сделана
находка H8 «очередь этого дерева НЕПОЛНА» в этом же сторожe.

## Что сделать

1. В ветке `card_missing` спросить `origin_view`: есть ли карточка на `origin/main` и какой у неё
   статус. Три исхода, каждый называется отдельно:
   * статус на origin — терминальный (`ingested`/`done`/`owner-done`) ⇒ **вопрос закрыт, карточка
     просто не доехала в прод**: это не находка, строка уходит из WARNING (но остаётся видимой
     как факт дрейфа, а не исчезает молча);
   * статус на origin — `needs-owner` ⇒ **вопрос ЖИВОЙ и потерян для нажатия** — это находка, и
     она СИЛЬНЕЕ сегодняшней («нажатие ответит "карточка исчезла"»);
   * на origin файла нет вовсе ⇒ остаётся честное `НЕ ИЗМЕРЕНО` (третий случай выше).
2. Не подмешивать карточки с origin в `pending_count` — граница #270 сохраняется намеренно.
3. `origin_view` недоступен (не git-дерево, нет ref) ⇒ **остаётся НЕ ИЗМЕРЕНО**, а не «закрыто»
   (fail-CLOSED; «не смогли посмотреть» ≠ «всё хорошо»).

## Как понять, что готово

На проде `owner_decision_pending` больше не держит WARNING из-за двух карточек, закрытых на origin,
и при этом ЛОЖНО не гасит третью. Тесты — в обе стороны: терминальный статус на origin гасит строку,
`needs-owner` на origin её УСИЛИВАЕТ, отсутствие ref оставляет `НЕ ИЗМЕРЕНО`.

## Приёмка

Положительный контроль обязателен: тест, воспроизводящий сегодняшнюю тройку (две `ingested`, одна
отсутствует), краснеет на неисправленном модуле.

---

## Исполнено — цикл #273 (2026-08-17, worktree от `origin/main` 8d2619c52)

### Что сделано

`origin_view` научился отвечать на второй вопрос — `cards_by_id(tracker_dir, card_ids, ref=)`:
статус НАЗВАННЫХ карточек на ref, одним пакетом, тем же локальным git без сети. Три исхода
различимы по построению: карточка найдена ⇒ ключ со статусом · карточки нет и на ref ⇒ ключа НЕТ
(факт, не сбой) · сверка не выполнилась ⇒ `Unmeasured` с причиной. Преамбула «с чем мы вообще
сверяемся» вынесена в общий `_locate` — две копии этой цепочки разошлись бы в том, какой случай
считать неизмеримым.

Ветка `card_missing` в `owner_decision_pending` больше не судит на месте: пропавшие карточки
собираются (ключами `dict` — заодно дедуп: переотправленная карточка давала три одинаковые строки
об ОДНОМ факте), спрашиваются у origin одним запросом и раскладываются по трём исходам ровно так,
как предписано карточкой. `pending_count` не тронут — граница #270 сохранена намеренно.

* **закрыт на origin** (`_TERMINAL_CARD_STATUS` = `ingested`/`done`/`owner-done`, список закрытый
  и поимённый) ⇒ не находка, а факт дрейфа: уходит из `unchecked`, остаётся в `closed_on_origin`
  и ПЕЧАТАЕТСЯ — и в CLI модуля, и отдельной строкой шага 0-офис. Объяснение, которого не видно,
  ничего не стоит;
* **`needs-owner` на origin** ⇒ новая находка **H9**, сильнее прежней: вопрос владельцу ОТПРАВЛЕН,
  он его видит, а нажатие отвечает «карточка исчезла». Во время остановки — CRITICAL;
* **нет на origin · незнакомый статус · сверка не выполнилась** ⇒ честное `НЕ ИЗМЕРЕНО`, и причина
  теперь названа словами («карточки тоже нет» ≠ «сверка не выполнена»).

**Пересечение H9 с H8 НЕ подавлено — сознательно.** Утверждения разные и ни одно не следует из
другого: H8 говорит «очередь дерева неполна» и считает в хвосте НЕотправленные; H9 говорит про те
же файлы ровно обратное — вопрос отправлен и владелец на него смотрит. Плюс H9 не ограничен типом
`owner-decision`, который фильтр H8 отбросил бы.

### Замер на живом проде (read-only, `--no-write`, те же данные)

| | до | после |
|---|---|---|
| `[НЕ ИЗМЕРЕНО]` | **3** | **1** |
| факт дрейфа назван | — | **2** (`ingested` на origin, поимённо) |
| ложно погашено | — | **0** |

Третья строка (`...dolgozhivuschie-agenty-krutyat-kod-mnogo`) осталась и обязана была остаться:
её нет и на origin, а «не смогли найти» — это не «вопрос закрыт». WARNING сторожа держит теперь
она одна, по делу.

### Приёмка

+12 тестов (7 в стороже, 5 в `origin_view`), прогон целевых наборов — 72 passed.
**Пять мутаций, каждая красит свою цель и только её:** список закрывающих статусов пуст ⇒ дрейф
не распознан (3 падения) · «всё, что не `needs-owner`, — закрыто» ⇒ незнакомый статус молча
погашен · «не измерено» трактуется как «закрыто» ⇒ fail-OPEN на сломанной сверке (3 падения) ·
дедуп снят ⇒ три строки об одном факте · H9 не заводится ⇒ живой потерянный вопрос молчит.
Положительный контроль — дословная тройка прода 17.08 (две `ingested`, одна отсутствует).
