---
trackerStatus:
  type: inbox
title: "Сторож кнопок объявляет «в карточке нет вариантов» там, где они есть: has_unparsed_options=False при looks_like_a_choice=True и parse_options=[]"
status: done
source: nimbalyst
created: 2026-08-22
claimed_by: cycle-85478
claimed_at: 2026-08-22T21:10:15Z
---

## Что измерено (цикл #343, 22.08, worktree `/tmp/spa_c343` от `origin/main` 9d69491d6)

Обязательный шаг 0-офис докладывает: **2 вопроса владельцу ждут ответа БЕЗ КНОПОК — ответить с
телефона нельзя**. У одного из них (`own-2026-08-19-sudba-voronki-chekapa-i-kanal-zayavok`,
priority `high`, висит с 19.08) названная причина — `no_options_in_card`:

> «в карточке не разобрано ни одного варианта — ни здесь, ни на `origin/main` (7531bcf37);
> **отказ верен**, выдумывать выбор запрещено (ADR-075)»
> remedy: «переписать вопрос перечислив варианты»

**Отказ НЕ верен, и это замерено, а не предположено.** В карточке варианты ЕСТЬ — прозой, с
рекомендацией агента, ровно по формату §2.4:

```
- **(а) Похоронить.** …
- **(б) Воскресить.** …
- **Рекомендация: (а).** …
```

Прогон настоящего парсера по этой самой карточке:

```
looks_like_a_choice   -> True     ← это ВЫБОР
parse_options         -> []       ← вариантов не разобрано ни одного
has_unparsed_options  -> False    ← ЛОЖЬ: ровно этот случай он и обязан ловить
```

### Класс — тот же, что весь #146–#343: сторож отвечает НЕ НА ТОТ вопрос

`has_unparsed_options` существует, чтобы поймать «это выбор, но разобрать варианты не вышло», —
и возвращает `False` на учебном образце этого самого состояния (`looks_like_a_choice=True` И
`parse_options=[]`). Дальше ошибка усиливается на пути к человеку:

1. **отчёт говорит о карточке то, что верно только о парсере.** «В карточке не разобрано ни
   одного варианта» — правда о нас; «отказ верен» — уже утверждение о КАРТОЧКЕ, и оно ложно;
2. **remedy зовёт сделать уже сделанное.** «Переписать вопрос перечислив варианты» — они
   перечислены с 19.08. Автор, послушавшись, переписал бы верную карточку;
3. **цена — у владельца.** Вопрос `high` третью неделю нельзя закрыть с телефона, а причина
   выглядит как его собственная недоработка.

ADR-075 («выдумывать выбор запрещено») тут НЕ нарушен и не оспаривается: варианты не выдумывать
надо, а ПРОЧИТАТЬ — они написаны. Форма `- **(а) …**` парсеру просто неизвестна.

## Что сделать

1. Научить `parse_options` форме `- **(а) …**` / `(б)` / `(в)` (буквенные метки в скобках,
   жирная головка пункта). Метку ответа для кнопки брать из самой карточки, не переименовывая.
2. **Сначала починить `has_unparsed_options`** — он сторож этого класса и обязан краснеть на
   `looks_like_a_choice=True И parse_options=[]` НЕЗАВИСИМО от того, какие формы выучены. Иначе
   следующая неизвестная форма снова уедет тихо. Положительный контроль — эта карточка.
3. Проверить, что вторая безкнопочная (`own-33-plist-marker-for-cycle-origin`,
   `card_stale_vs_origin`) — ДРУГОЙ дефект (живая копия отстала от origin, `inbox-zhivaya-kopiya-kartochki-otstala-ot-origin`), и не чинить их одним куском.
4. Владельцу по этому поводу НЕ звонить (ADR-084: своя недоставка — не повод для тревоги);
   кнопки появятся сами при следующей отправке.

## Как понять, что готово

`parse_options` на `own-2026-08-19-sudba-voronki-chekapa-i-kanal-zayavok` даёт 2 варианта с
рекомендацией (а); `has_unparsed_options` краснеет на паре «выбор есть / вариантов ноль» и это
закреплено тестом с ПОЛОЖИТЕЛЬНЫМ контролем на неисправленном коде; `buttonless_count` в
`owner_decision_pending` падает с 2 до 1, и оставшийся — про другой дефект, названный вслух.

---

## 📌 Замер цикла #347 (22.08) — карточку НЕ закрываю, поправляю одно её допущение

Пункт 4 задания («владельцу не звонить; **кнопки появятся сами при следующей отправке**»)
опирался на отправку, которой уже не будет: вопрос доставлен 22.08 09:14:44Z, а звать
владельца ради своей же недоставки запрещено (ADR-084) ⇒ `notify` по нему не пойдёт больше
никогда. Единственный оставшийся путь — штатный ремонт `heal_buttonless`, и он читал ТОЛЬКО
живую копию. Это отдельный дефект, он замерен и починен циклом #347
(`inbox-remont-knopok-ne-sprashivaet-istochnik-p`): ремонт теперь спрашивает `origin/main`,
и на НАСТОЯЩЕЙ карточке собирает «⭐ 1. Похоронить» / «2. Воскресить».

**Предмет ЭТОЙ карточки остаётся открытым** — проверено сегодня же:
форма `- **(а) …**` / `(б)` по-прежнему даёт `parse_options → 0` И `looks_like_a_choice → False`.
Конкретно `own-2026-08-19-sudba-voronki-…` перестал быть примером не потому, что парсер
научился, а потому, что карточку на `origin` переписали перечнем «Вариант 1 / Вариант 2».
Следующая карточка в форме `(а)/(б)` уедет так же тихо.

---

## ✅ Закрыто циклом #349 (22.08) — сделано всё три пункта, и авария оказалась глубже

### Что было измерено ПЕРЕД починкой

Форма из карточки, прогнанная через настоящий разбор на `origin/main` 4d9353c1c:

```
looks_like_a_choice   -> False    ← не True, как считала карточка
parse_options         -> []
has_unparsed_options  -> False
offers_ack            -> True     ← ХУЖЕ, чем «нет кнопок»
```

Карточка описывала форму «`- **(а) …**` внутри `**Решение 1 — …** Варианты:`», где слово
«Варианты» само по себе поднимало `looks_like_a_choice`. Без этого слова — а его в §2.4 никто не
требует — жирная головка пункта разрывает буквенную альтернативу `_CHOICE_TRACE_RE` двумя
звёздочками, и `looks_like_a_choice` тоже молчит. Тогда карточке с настоящим выбором
предлагается кнопка **«✅ Принято»**: нажатие закрыло бы вопрос «хоронить или воскрешать»
ответом, которого карточка не предлагала. Это прямое нарушение ADR-075, только молча и
необратимо — то есть исход дороже, чем описанный в карточке «кнопок нет».

### Что сделано

1. **Сторож починен ПЕРВЫМ** (п. 2 задания). `has_unparsed_options` спрашивает про СОСТОЯНИЕ —
   «след перечня есть, разобрано ноль» (`_UNREAD_CHOICE_RE`), — а не про одну выученную форму.
   Он больше не делит слепое пятно с подопечным: следующая незнакомая форма назовётся сама.
2. **Разбор выучил буквенную форму** (п. 1): `- **(а) Похоронить.**`, `- **(а, рекомендую)** …`,
   `- (б) …` без жирного. Метка берётся из карточки, не переименовывается.
   Попутно: рекомендация, названная буквой («**Рекомендация: (а).**» отдельным абзацем), больше
   не уезжает звездой на ПОСЛЕДНИЙ вариант — до этого звезда вставала ровно на тот вариант,
   который автор не советовал.
3. **`_CHOICE_TRACE_RE` перестал спотыкаться о жирную головку** — «✅ Принято» карточке с
   выбором больше не предлагается ни при каком написании.
4. **Причина в отчёте перестала врать** (`buttonless_reason`): у «выбор написан, а прочитать не
   смогли МЫ» теперь свой код `unreadable_options_in_card` и своё лекарство — разобрать кодом
   или разделить карточку надвое. Прежний текст «отказ верен… переписать вопрос перечнем»
   отправлял автора чинить ВЕРНУЮ карточку; тот же неверный вывод был записан и в докстринге
   модуля — исправлен там же.

### Цена расширения сторожа — ИЗМЕРЕНА, а не оценена

Прогон по ВСЕЙ живой очереди (602 карточки, `origin/main` 4d9353c1c), до и после:

| | результат |
|---|---|
| кнопки ПОЯВИЛИСЬ | **2** карточки, обе по 3 варианта, звезда на рекомендованном |
| кнопки потеряны | **0** |
| набор вариантов изменился | **0** |
| «✅ Принято» появилось там, где его не было | **0** |
| `has_unparsed_options` стал `True` | **12**, и в каждой из 12 выбор действительно написан (проверено чтением секции) |

Появились кнопки у `owner-decision-20260716-082215-k-1-22` (пилот на $1,000) и
`owner-decision-dva-agenta-ne-perezhivut-perezagruzku-ra`. **Честно: обе уже `ingested`** —
сегодняшний ОТКРЫТЫЙ набор (2 карточки `needs-owner`) дефект не задевает, у обеих варианты
разбирались и раньше. Выигрыш прогнозный: следующая карточка в этой форме не уедет тихо, а
две закрытые — доказательство на настоящих телах, а не на фикстуре.

**Граница в другую сторону сохранена и проверяется тестом:** «Вариант тот же, что вчера» по-
прежнему НЕ повод просить владельца завести дефект. Просьба завести несуществующий дефект —
такая же ложь, как «выбора не предлагали» под карточкой с выбором; поэтому сторож требует
ПЕРЕЧНЯ, а не одного слова «вариант», и к `looks_like_a_choice` (который намеренно шире и служит
другому решению) не сведён.

### Приёмка

+11 тестов (9 новых в `test_owner_decisions_lettered_options.py`, 2 в `test_buttonless_reason.py`)
плюс один усиленный. Положительный контроль на чистом `origin/main`: **7 из 9** новых краснеют,
2 зелёные с обеих сторон — это обратные контроли по построению. Четыре мутации красят каждая
свою цель: разбор без буквенной формы → 4 · сторож снова узкий → 2 · `_CHOICE_TRACE_RE` снова
слеп к жирному → 1 · рекомендация не читает букву → 1 (и падение показывает саму вредность:
`[False, True] == [True, False]`, звезда на не том варианте).

**Один тест изменён намеренно** (инв. #16, запись в `docs/journal/2026-W34.md`):
`test_a_lettered_choice_is_refused_even_though_the_narrow_check_passes` требовал
`has_unparsed_options(CARD_LETTERED) is False` как «предусловие» и `prep.keyboard is None` —
то есть закреплял слепоту разбора как свойство карточки. Проверка не ослаблена, а усилена:
прежнее требование «кнопка „Принято“ не появляется» сохранено дословно, к нему добавлено
более сильное — владелец получает НАСТОЯЩИЕ варианты. Фикстура `_WITHOUT_OPTIONS` в
`test_buttonless_reason.py` переименована в `_UNREADABLE_OPTIONS` и расширена до формы, где
разбор отказывает честно (два решения в одной карточке), а под именем «без вариантов» встала
проза без перечня.

### Пункт 3 задания

Вторая безкнопочная (`own-33-plist-marker-for-cycle-origin`, `card_stale_vs_origin`) —
ДРУГОЙ дефект, одним куском не чинилась: её код в отчёте по-прежнему `card_stale_vs_origin`,
и лечится он переносом карточки, а не разбором.
