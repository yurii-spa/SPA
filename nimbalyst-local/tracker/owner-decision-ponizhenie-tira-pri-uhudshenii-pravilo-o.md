---
trackerStatus:
  type: owner-decision
title: "Понижение тира при ухудшении: правило обещает «агент немедленно», исполнителя нет"
status: ingested
source: nimbalyst
created: 2026-09-11
status_trail:
  - "2026-09-11T20:45:32.941978+00:00 needs-owner -> ingested · queue.set_status"
owner_answered_at: 2026-09-11T21:05:00Z
owner_choice: withdrawn — владелец спросил «разве это не CIO?»; замер показал, что все пять условий уже исполняют гейты (ADR-031/053, _fundable); карточка не из трёх предметов ADR-285
answer_channel: chat-session (не бот; след записан агентом, тело содержит цитату)

---

## Что случилось и почему это важно

У нас записано, что понижение тира протокола (T1→T2, T2→T3) при ухудшении условий
делает **агент, автоматически и немедленно** — так сказано в `docs/tier_criteria.md` §5
со ссылкой на ADR-055. А в самом коде, который эти ухудшения находит
(`spa_core/analytics/tier_curator.py`), рамкой написано обратное: «никогда не меняет
тир, это только совет». Оба утверждения записаны и оба действуют. Исполнителя у слова
«немедленно» нет: сигнал печатается в лог дневного цикла и там заканчивается.

Тир — это не ярлык, это потолок вложения в один протокол: T1 — 40 % капитала, T2 — 20 %.
Поэтому «кто вправе понизить тир» — вопрос про деньги, и решать его агенту нельзя.

## Что от тебя нужно

Выбрать, что делает система, когда сама увидела ухудшение у протокола, в котором у нас
есть деньги (обвал TVL ниже порога, мёртвый фид, блокирующий риск-сигнал, ставка вне
границ политики):

**Вариант A (рекомендую).** Развести на два разных действия:
- **Тир не меняется сам** — остаётся за тобой, отдельным ADR, как сейчас.
- Но протокол **сразу перестаёт браться в новые вложения** — уже имеющаяся позиция
  держится и сокращается обычным ребалансом (никакой принудительной продажи), новые
  деньги в него не идут. Механизм для этого в системе уже есть и стоит ноль
  (`allocator.declared_ban()`, пер-пуловая заморозка по ADR-053) — его надо просто
  подключить к сигналу.

*Почему рекомендую:* защита срабатывает в тот же день, а решение о потолке остаётся
за тобой. Потерять на этом нельзя — мы перестаём ДОБАВЛЯТЬ в ухудшившийся протокол,
а не продаём в убыток.

**Вариант B.** Оставить как есть (только сигнал), но убрать из `docs/tier_criteria.md`
слова про «агент автоматически немедленно» — чтобы правило не обещало того, чего нет.
*Цена:* между «система увидела» и «ты ответил» деньги продолжают идти в ухудшившийся
протокол.

**Вариант C.** Разрешить агенту менять тир полностью автоматически.
*Не рекомендую:* это делает изменение потолка концентрации побочным эффектом обновления
фида — ровно то, против чего написана рекомендация в `docs/tier_criteria.md` §4.

## Как понять, что готово

Ты назвал вариант; на выбранном варианте `docs/tier_criteria.md` §5 и код
`tier_curator.py` говорят об одном и том же одними словами.

## Что будет после

Вариант A — пишу ADR, подключаю сигнал к заморозке от свежей аллокации через
`pre_cutover_gate` и изолированное дерево, со сквозным замером на копии живого `data/`
(без правки и с правкой, на одних входах). Вариант B — правлю документ и закрываю
противоречие текстом, кода не касаюсь. Вариант C — пишу ADR на смену тира агентом
и сторожей к нему.

Полный аудит четырёх контуров тир-жизненного цикла — `docs/TIER_LIFECYCLE_AUDIT_2026-09-11.md`.

---

## Ответ владельца (чат сессии, 2026-09-11)

> «по первой задаче разве это не ответственность CIO?»

## Инжест: вопрос снят — код уже отвечает, и отвечает не CIO, а гейты ниже него

Замер по вопросу владельца показал, что **все пять** условий, по которым куратор выносит
`DEMOTE_SIGNAL`, уже исполняются на денежном пути детерминированно, без чьего-либо решения:

| Условие деградации | Кто исполняет сегодня | Где |
|---|---|---|
| Tier-A `BLOCK` (депег / каскад / bad debt / admin-key …) | **Step 2c-pre, Analytics Blocking Gate** — цель протокола обнуляется, капитал перераспределяется | `paper_trading/cycle_gates.py:104`, ADR-031 |
| фид мёртв / stale | гейт `_fundable()` — `unevidenced`, в свежую аллокацию не берётся | `allocator/allocator.py:1064` |
| живой TVL < floor | RiskPolicy v1.0, только живой TVL | ADR-053 |
| у УДЕРЖИВАЕМОЙ позиции пропал живой TVL | заморозка cap-at-held, без forced-sell | ADR-053 |
| живой APY вне границ политики | RiskPolicy v1.0 (1 %…30 %) | `risk/policy.py` |

**Вариант A из этой карточки — уже поведение системы**, и по Tier-A даже строже A
(обнуление цели, а не только «не докладывать»). CIO стоит ВЫШЕ этих гейтов — решает,
перекладывать ли книгу, — а не вместо них; RiskPolicy остаётся единственным hard-гейтом
(инвариант #1). Tier-A путь проверен на LLM: `signal_aggregator.py` LLM не упоминает,
пороги 70/40 — константы.

**Чего автоматика по-прежнему не делает:** не меняет ЯРЛЫК тира (литерал канона
`PROTOCOL_RISK_SCORES`). Но ярлык задаёт только потолок концентрации; **эффект** деградации
(нет свежего капитала / цель обнулена) наступает и без смены ярлыка. Смена ярлыка — по ADR,
как и было.

**Вывод по ADR-285:** карточка не принадлежит ни одному из трёх предметов владельца (движение
денег здесь не меняется — гейты уже действуют), то есть это дефект очереди, а не ожидание
ответа. Снимаю сам: `needs-owner → ingested`. Остаток работы — мой и документный:
переписать `docs/tier_criteria.md` §5 так, чтобы он называл НАСТОЯЩИЙ механизм
(гейты `_fundable` / Step 2c-pre / ADR-053), а не «агент меняет тир»; дать отчёту куратора
секцию в брифинге как СЛОЙ ВИДИМОСТИ (почему протокол исключён), а не как защиту —
защита от отчёта не зависит.

Ошибка автора карточки названа в `docs/TIER_LIFECYCLE_AUDIT_2026-09-11.md` §9: вопрос
владельцу был задан о том, что код уже решил.
