---
trackerStatus:
  type: agent-task
title: Книги aggressive_lab документированы как append-only, но история пересоздаётся — susde_dn сдвинулась на −9.7%
status: done
priority: high
source: session-cycle66
created: 2026-08-01
claimed_by: cycle73
claimed_at: 2026-08-01T13:14:46Z
---

## Что измерено

Цикл #66 разбирал карточку `agent-idea21-verdict-data-drift` (расхождение опубликованного вердикта
идеи #21 с живыми данными). Гипотеза той карточки — «ряды доросли» — оказалась неверной: выравненная
сетка идентична (591 точка, 2024-06-01..2026-06-24). Расхождение полновыборочных чисел объяснилось
другим, и это НЕ домен идеи #21.

**Диф живых книг против полного бэкапа репозитория `backups/SPA_Claude_backup_2026-07-25.tar.gz`:**

| книга | строк изменилось | максимальный сдвиг |
|---|---|---|
| `susde_dn` | **853 из 853** | **−9.70%** (2026-07-05: 163150.97 → 147322.68) |
| `lrt_neutral` | 761 из 853 | −1.34% (2024-08-23) |
| `susde_spot` | 0 | — |
| `points_farm` | 0 | — |
| `eth_directional` | 0 | — |
| `pendle_pt_levered` | 0 | — |

Изменились и `hash`-поля (ряд пере-сцеплен, а не побит), то есть цепочка доказательств остаётся
формально валидной — она подтверждает целостность НОВОЙ истории, а не то, что история не менялась.

**Расхождение с документацией.** `spa_core/strategy_lab/aggressive_lab/harness.py:6` описывает файл
как «proof-chained, **append-only**, one {date,equity_usd,phase,…} per line», а `harness.py:76` —
как «Append (or REFRESH today's) point». Пересоздание всей истории ни тем, ни другим не покрыто.

**Побочное наблюдение (в ту же сторону, отдельно не проверялось):** в `susde_dn` ровно **одна**
строка имеет `phase: forward`, остальные 853 — `phase: backtest`, и между 25.07 и 01.08 эта
единственная forward-строка не добавилась к предыдущей, а ЗАМЕНИЛА её (`2026-07-25` исчезла,
появилась `2026-08-01`). Если так и задумано — форвард-трек книги не накапливается по построению.

## Почему это важно (и почему не чинил молча)

1. Любое опубликованное число, выведенное из этих книг, **невоспроизводимо задним числом**: тот же
   скрипт на той же сетке завтра даст другой ответ. Идея #21 — только один пример; сдвиг базового
   APY CORE-A составил 7.751% → 6.715%.
2. Книги `aggressive_lab` питают **3-тирный продукт** (стандинг-директива владельца: Aggressive
   ~15-20% «real paper»). Сдвиг истории на −9.7% может двигать показываемую доходность тира.
3. Это домен paper-трека и потенциально публикуемых чисел ⇒ по мандату автономного цикла —
   карточка, а не молчаливая правка.

## Что предлагается (нужна оценка, ни один вариант не выбран)

1. Установить, ЧТО именно пересоздаёт ряд (кандидат — `aggressive_lab_runner` / `harness`,
   ре-симуляция backtest-хвоста на каждом тике) и является ли это намеренным.
2. Если намеренно — привести докстринги в соответствие («backtest-часть пересчитывается»), и
   **писать в файл отпечаток ряда + версию генератора**, чтобы потребители видели смену истории.
3. Если ненамеренно — заморозить backtest-часть (пересчитывать только forward-хвост).
4. Проверить, зависит ли от этих книг что-либо ПУБЛИКУЕМОЕ (сайт / тиры / отчёты), и если да —
   отдельная карточка владельцу.

## Acceptance criteria

- [ ] причина пересоздания названа по коду (файл+функция), а не предположена;
- [ ] решено и записано: пересоздание намеренное или дефект;
- [ ] docstring `harness.py` соответствует фактическому поведению;
- [ ] потребители книг перечислены; для публикуемых — сказано, влияет ли сдвиг на их числа.

## Что НЕ делать без владельца

Трогать живой трек `data/equity_curve_daily.json`, RiskPolicy/kill-switch, публикуемые на сайте
числа доходности и наименования тиров. Если окажется, что сдвиг двигает опубликованные числа тиров
— это owner-gated (`.claude/rules/site-copy.md`), только карточкой владельцу.

## Как воспроизвести

```bash
mkdir -p /tmp/snap && cd /tmp/snap
tar xzf backups/SPA_Claude_backup_2026-07-25.tar.gz --include='*/data/aggressive_lab/*/realized_series.jsonl'
# затем сравнить equity_usd по общим датам с data/aggressive_lab/<книга>/realized_series.jsonl
```

---

## ✅ Независимо перепроверено циклом #69 (2026-08-01) + ОТВЕТ на пункт 4

Карточку создал цикл #66, который умер, не запушив. Цикл #69 поднял его работу и **перепроверил
таблицу своим кодом** (свой разбор `realized_series.jsonl`, свой диф против того же бэкапа
25.07, чтение живых книг через read-only копию `chmod a-w`). **Таблица подтвердилась дословно:**
`susde_dn` — 853 из 853 общих строк изменились, максимум **−9.70% на 2026-07-05**
(163150.97 → 147322.68); `lrt_neutral` — 761; `susde_spot` / `points_farm` / `eth_directional` /
`pendle_pt_levered` — **0**. Число строк (854) и обе границы дат не изменились ⇒ длина и границы к
этому слепы, как и сказано. Побочное наблюдение про forward тоже подтвердилось: единственная строка
`phase: forward` не добавилась, а **заменилась** (`2026-07-25` исчезла, появилась `2026-08-01`) ⇒
форвард-трек книги не накапливается.

### Пункт 4 («зависит ли от книг что-либо ПУБЛИКУЕМОЕ») — ИЗМЕРЕН, и ответ не тот, что кажется

**Да, зависит.** Публичная страница `/annual-contrast` прямо обещает: «каждая цифра прослеживается
до `data/aggressive_lab/annual_contrast.json`», а сам файл строится из этих книг —
`annual_contrast.py` штампует записи `"source": "realized_backtest_series"`.

**Но опубликованный файл НЕ сдвинулся: 0 изменившихся полей из 2002** против бэкапа 25.07.
И причина этого — НЕ то, что пересоздание безобидно: `annual_contrast.json` имеет
`generated_at = 2026-06-29T23:16:33Z` (`as_of = 2026-06-25`), то есть **не перегенерировался больше
месяца**. Опубликованные числа сегодня защищены только тем, что продюсер не запускался —
**следующая же перегенерация протянет сдвиг −9.70% в публикуемое число**. Формулировать это как
«на сайт не влияет» было бы ровно тем классом (#29/#31/#35–#38/#40), который здесь ловят: покой от
неизмерения, а не от отсутствия эффекта.

⇒ Пункт 4 закрыт с ответом «да, влияет отложенно», и по правилу самой карточки заведена
**карточка владельцу `owner-decision-knigi-aggressive-lab-perepisyvayut-svoyu`** + notify
(публикуемые числа доходности — owner-gated, `.claude/rules/site-copy.md`). Инженерные пункты 1–3
остаются здесь и владельца не ждут.

---

## ✅ ЗАКРЫТА циклом #73 (2026-08-01) — все четыре acceptance criteria выполнены

### 1. Причина названа ПО КОДУ (не предположена) — три звена, каждое измерено

| звено | где | что делает |
|---|---|---|
| полный перезапис | `harness.py::run_backtest` (`series: List[dict] = []` → `_io.atomic_write_text`) | пишет файл книги **с нуля**, стирая всё, что в нём было, включая форвард-точки |
| разрушительный дефолт | `run.py::main` — `mode = argv[0] if argv else "both"` | **без аргумента** запускается `run_backtest`, а не только тик вперёд |
| аргумент не доезжает | `scripts/agent_aggressive_lab.sh:11` — `export MODULE_ARGS=(paper)` | bash **не экспортирует списки**; `agent_template.sh` — отдельный процесс, ловит пустоту (`declare -p MODULE_ARGS` не срабатывает → `MODULE_ARGS=()`) |

**Доказательство, а не рассуждение:**
- живой лог `/tmp/spa_aggressive_lab.log`: **28 запусков** с 2026-07-05 по 2026-08-01, строка
  `exec:` **каждый раз** обрывается на `... .run ` — аргумента нет **ни разу** (`grep -c "run paper"` = **0**);
- живые артефакты: `backtest_summary.json` `generated_at 2026-08-01T01:06:24Z` (пересчёт) и
  `status.json` `2026-08-01T01:07:41Z` (тик) — то есть за один запуск отработали ОБА продюсера;
- поведение bash проверено запуском, а не по памяти: родитель после `export MODULE_ARGS=(paper)`
  видит 1 элемент, дочерний `/bin/bash` — **0**.

### 2. Решено и записано: пересоздание **намеренное**, дефект — его КАДЭНС

`run_backtest` обязан переписывать файл целиком — так задумано и так написано в коде («the
backtest fully owns the file it writes here»); для разовой перепрокрутки это правильно. Дефект в
том, что он идёт **ежесуточно по расписанию**, чего не хотел никто: сам wrapper объявляет
намерение `paper` и в комментарии пишет, что растущий форвард-трек — это то, ради чего он живёт.

### 3. Цена дефекта — измерена, и она больше, чем дрейф истории

Все **10 из 10** живых книг: 854 строки = 853 `backtest` + **ровно 1** `forward`, и дата этой
единственной строки — **сегодняшняя** (у всех десяти). Форвард-трек не «тонкий» — он **обнуляется
каждую ночь** и отстраивается до одной точки. Отсюда `min_forward_points: 0`,
`trustworthy: false`, `status: "warming_up"`, `blended_realized_apy_pct: null`,
`worst_tail_pct: null` у тиров **Balanced** и **Aggressive** в `data/tier_paper_rollup.json`:
доказанной форвард-истории у приоритетного 3-тирного продукта не может появиться в принципе,
пока это так. Это **опровергает** прежнее объяснение «тонко по построению, дозреет к ~30-му дню».

### 4. Потребители книг перечислены; для публикуемых — сказано, влияет ли

Читают `data/aggressive_lab/**` (не считая тестов и `attic/`): Lane-2 самого лаба
(`loader` · `scorecard` · `risk_metrics` · `tier_policy` · `tail_overlay` · `annual_contrast`),
рой (`swarm/{swarm_book,blend_forward,guardian_forward,funding_regime}`), `rates_desk`
(`books`/`books_series`/`realized_at_size`), API (`api/routers/{aggressive_lab,swarm}.py`),
`aggressive_lab_runner`, и скрипты `tier_paper_rollup.py` · `tier_portfolio_backtest.py` ·
`cross_desk_portfolio.py` · `guardian_backtest.py` · `refusal_gate_overlay.py` · семейство
`edge_*.py`. **Публикуемые:** `/annual-contrast` (через `annual_contrast.json`) — ответ дал цикл
#69: влияет **отложенно** (файл не пересчитывался с 29.06, следующий пересчёт протянет сдвиг
−9.70%) ⇒ карточка владельца `owner-decision-knigi-aggressive-lab-perepisyvayut-svoyu`; и числа
тиров через `tier_paper_rollup.json` — сегодня они `null`/`warming_up` **из-за этого же дефекта**.

### Что сделано в коде (аддитивно, поведение НЕ менялось)

- **Докстринг приведён к правде** (критерий 3): слова «append-only» в `harness.py` больше нет —
  описано фактическое поведение, его два следствия (невоспроизводимость задним числом;
  проof-chain пере-сцепляется и подтверждает целостность НОВОЙ истории) и измеренные числа.
  Комментарий в `run_backtest` и `run.py::main` теперь называют кадэнс-дефект явно.
- **+8 герметичных тестов** `spa_core/tests/test_aggressive_lab_series_rewrite.py` (без сети, без
  живых данных): пересчёт стирает пред-существующие точки · пересчёт уничтожает форвард-точки
  бумажного тика · `upsert_day` копит по дням (**контроль** — иначе потеря приписывалась бы не
  тому продюсеру) · `upsert_day` по-прежнему заменяет тот же день (**контроль**) · `main([])` →
  запускается пересчёт · `main(["paper"])` → **не** запускается (**контроль**) · bash-список не
  переживает `export` (механизм) · оба звена механизма всё ещё в дереве.
  **Мутационные контроли:** пересчёт заставили сохранять прошлое → краснеют ровно 2 нужных теста;
  дефолт заменили на `"paper"` → краснеет ровно тест дефолта; `upsert_day` заставили
  дублировать день → краснеет ровно тест идемпотентности. Файлы восстановлены побайтово.

### Что НЕ сделано и почему → карточка владельца

Починка обёртки `scripts/agent_aggressive_lab.sh` **не сделана**: это живой launchd-агент
(мандат автономного цикла запрещает деплой/выгрузку агентов; ADR-OWN-2026-07-repo-freeze-incident
отдаёт настройки запуска владельцу), и починка **запускает часы** на публикуемых числах тиров
(owner-gated, `.claude/rules/site-copy.md`). Заведена
**`owner-decision-agent-bumazhnyh-knig-kazhduyu-noch-stira`** (3 варианта, рекомендация — №1) +
notify. В карточку `owner-decision-knigi-aggressive-lab-perepisyvayut-svoyu` дописан ответ на её
инженерный вопрос «почему пересоздаётся»; её статус НЕ менялся.

**Не трогал:** RiskPolicy / kill-switch / пороги / гейты, живой трек `data/equity_curve_daily.json`,
launchd и деплой, публикуемые числа и наименования тиров, `landing/**`; `data/**` только читал.
