---
trackerStatus:
  type: inbox
title: "У файлов .claude/rules/ нет ни одного сторожа: 104 строки правил исчезли на шесть дней и нашлись случайно"
status: done
created: 2026-09-02
status_trail:
  - "2026-09-03T10:05:00+00:00 new -> done · queue.set_status · cycle-4071 (подъём работы #456)"
---

## Что измерено (цикл #453, 02.09)

`.claude/rules/deployment.md` потерял **104 строки** от одной доставки: коммит `ba66e1bd3`
(30.08) принёс свой пункт 8 («агентский сервер слушает петлю») поверх УСТАРЕВШЕЙ копии файла и
унёс с собой всё, что добавили после неё, — раздел «ЛИЧНОСТЬ ПРОЦЕССА в тестах» (`17a3a32b2`),
таблицу «Четыре вопроса — четыре сторожа» (файл откатился к версии «Три вопроса»), раздел
«Долгоживущий агент держит код с момента старта» и запрет проверять долгожителя запуском.

Шесть дней этих правил не существовало. При этом карточка
`inbox-test-sudit-o-hoste-literalnyi-pid-98535`, `docs/STATE.md` и журнал `2026-W34.md` хором
утверждали «класс закрыт ПРАВИЛОМ». Содержимое восстановлено #453 сверкой с историей файла.

## Почему это класс, а не единичная неаккуратность

Пропажу нашёл **не сторож, а случайность**: правило понадобилось процитировать в соседней
работе, и его не оказалось на месте. Ни один существующий механизм на неё не отвечает:

* у `docs/STATE.md` есть храповик длины (`test_state_md_length_ratchet`), у `.claude/rules/` — нет;
* `guard_entry_loss` следит за `docs/decisions/INDEX.md`, а не за правилами;
* сторож, который сверял бы правило с origin, отсутствует; `git diff` показывает пропажу
  только тому, кто её ИЩЕТ;
* класс уже известен для `docs/` (правило «дописывать поверх `git show origin/main:<path>`»),
  но на `.claude/rules/` его никто не распространил — а цена там выше: правило читают ПЕРЕД
  тем, как трогать прод.

## Что сделать (варианты, решать исполнителю — все не owner-gated)

1. Дешёвый храповик по каждому файлу `.claude/rules/*.md`: число строк и множество заголовков
   `##` могут только расти; уменьшение — красный с требованием обосновать (как у
   `frozen_date_baseline`, но на заголовках, а не на числе).
2. Проверка при доставке: `push_to_github.py` уже сверяет копию инструмента доставки — добавить
   отказ, если уезжающий `.claude/rules/*.md` **короче** origin-версии и заголовки пропадают.
   Осознанное сокращение — через явный флаг.
3. Минимум: тест, который держит перечень обязательных разделов каждого правила (по одному
   якорю на раздел), чтобы пропажа краснела на следующем же прогоне.

Вариант 2 закрывает класс у ЕДИНСТВЕННОЙ двери доставки и потому сильнее; вариант 1 дешевле.

## Как понять, что готово

Удаление любого раздела правила из уезжающей копии краснит проверку (положительный контроль —
воспроизвести именно `ba66e1bd3`: взять его версию файла и убедиться, что проверка её не
пропускает), а осознанное сокращение проходит только с явным обоснованием.

---

## Исполнено — написал цикл #456 (02.09), ПОДНЯЛ и доделал цикл #467 (03.09)

**Выбран вариант 2** (сторож у единственной двери доставки) — он сильнее варианта 1, как и
написано в карточке. `push_to_github.py`: единица смысла правила — РАЗДЕЛ (`##`/`###`),
уезжающая копия без раздела, который есть на origin, — отказ `RulesSectionLossRefused`
(fail-CLOSED). Вызов идёт диспетчером `guard_content_loss` во ВСЕХ трёх ветках расхождения,
включая `DIVERGENCE_UNMEASURED` — там и была дыра.

**Приёмка:** `spa_core/tests/test_rules_section_loss_guard.py` — 17 passed; положительный
контроль — авария `ba66e1bd3` побайтово (`spa_core/tests/fixtures/rules_section_loss/`).
Байты фикстур сверены с историей самостоятельно: sha256 обеих совпал с `git show`
(`637b55bbb` и `17a3a32b2` — предыдущее касание файла — побайтово равны, так что «версия на
origin перед доставкой» названа верно).

**Цена ошибки ПЕРЕМЕРЕНА** (вся история `.claude/rules/`: 29 коммитов, 22 пары «коммит+файл»,
где файл был и в родителе; у #456 было 16 — население выросло вместе с историей): отказов 3 —
авария `ba66e1bd3` (4 раздела) и два законных переписывания заголовка вместе с телом
(`7ed5b3da4`, `2828e2ac5`: «Три вопроса» → «Четыре вопроса»). Не гасятся молча: отказ печатает
три улики (что пропало · что появилось взамен · нетто-объём), проход — `--allow-overwrite`.

### Что добавил ПОДЪЁМ (отчёту мёртвой сессии не верил — перемерял своим прогоном)

1. **Патч #456 нельзя было принести как есть.** Пока работа лежала в мёртвом дереве, цикл #459
   завёл `pattern=` у тех же четырёх функций (реестр решений — строка таблицы) плюс `_entry_key`.
   Копия #456 подменила бы это своей редакцией с `pattern=None` — ровно та авария, против
   которой пишется этот сторож. Взято только НОВОЕ, плоскость параметра оставлена origin'ная.
2. **Третья дверь была БЕЗ ТЕСТА.** Из семи мутаций по координате шесть краснели, а «вернуть в
   ветке пере-базы `guard_entry_loss` вместо диспетчера» проходила ЗЕЛЁНОЙ: вызов в коде стоял,
   сторожа у двери не было. Закрыто `test_the_rebase_door_reaches_the_guard_too` (регрессия
   `rebase_append` + обратный контроль честной пере-базы).
3. **Три СОСЕДНИХ теста краснели на верном коде** (`test_journal_entry_loss_guard`,
   `test_entry_guard_rename_and_full_list`, `test_shared_memory_unmeasured_base`): каждый своей
   строкой искал в тексте ветки литерал `guard_entry_loss`, а ветки теперь зовут дверь. Вопрос
   сохранён целиком и усилен, реализация сведена в один `spa_core/tests/_pusher_wiring.py`
   (инвариант #16: правка теста намеренная, обоснование в коде и в журнале).
4. **Моя же первая редакция усиления оказалась УКРАШЕНИЕМ.** Второе звено («дверь доказанно
   ведёт к проверке») искало подстроку в теле диспетчера — и мутация «убрать вызов из
   диспетчера» прошла ЗЕЛЁНОЙ: имя осталось в его собственном docstring
   (`:func:`guard_entry_loss``). Проза — не вызов; звено меряется разбором AST. Итог сверки:
   5 мутаций из 5 краснеют, контроль зелёный с обеих сторон.
5. **Скип превращал «не измерено» в «прошло».** `test_is_rules_doc_matches_every_rule_in_the_tree`
   скипался при отсутствии `.claude/rules` — каталог ГИТ-ТРЕКАЕМЫЙ, его отсутствие есть находка.
   Скип снят в пользу громкого отказа (рецидивы #465/#466, `.claude/rules/deployment.md`).

**Дифференциал набора:** чистый `origin/main` da52d8ec7 — 809 passed / 0 failed на всех тестах,
касающихся пушера; после правки — 826 passed / 0 failed. Замечание «network guard was
RE-INSTALLED mid-run» воспроизводится на ЧИСТОМ origin тем же набором и к этой работе
отношения не имеет.

### Остаток #456 СНЯТ ЗАМЕРОМ, владельца не тревожим

#456 завёл карточку `owner-decision-instruktsii-po-kotorym-rabotayut-agenty` (`needs-owner`):
инструкции в прод-дереве не синхронизируются, прод держит СНЯТЫЙ запрет Sky/sUSDS и старую
команду прогона. Она осталась в мёртвом дереве и на origin не уехала. **Перемерено 03.09
из прод-дерева — предпосылка больше не верна:** `CLAUDE.md` (222 строки) и все пять
`.claude/rules/*.md` побайтово совпадают с `origin/main`. Причина названа: **ADR-214**
(«инструкции доставляются как код») включил их в синхронизацию. Карточка НЕ доставляется —
спрашивать владельца об уже решённом запрещено (ADR-084).
