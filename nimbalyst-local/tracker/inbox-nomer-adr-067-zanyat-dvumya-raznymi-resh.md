---
trackerStatus:
  type: inbox
title: Номер ADR-067 занят ДВУМЯ разными решениями — реестр показывает две строки
status: new
source: orchestrator-cycle-148
created: 2026-08-07
priority: low
---

`docs/decisions/INDEX.md` содержит **две** строки `ADR-067`, и на диске лежат два файла:

- `ADR-067-golive-blocks-on-active-gaps-only.md` — гейт go-live блокируют только активные дыры;
- `ADR-067-owner-decisions-2026-08-06-autopilot-mandate.md` — пакет решений владельца + мандат автопилота.

Оба Accepted, оба действуют, оба на них ссылаются (в т.ч. `MEMORY.md` и STATE). Найдено
циклом #148 при выборе свободного номера для ADR-069 (взят следующий за ADR-068 — коллизию
не обходил молча).

**Почему не исправлено на месте.** Перенумерация — это правка чужого принятого решения и всех
ссылок на него; сделанная походя, она ломает адресацию в памяти проекта. Нужно решить ОДИН раз,
какой из двух становится ADR-070 (и переписать ссылки), либо оставить как есть, признав, что
номер — не ключ. Молча трогать не стал.

## Критерий готовности

`grep -c '^| ADR-067' docs/decisions/INDEX.md` = 1, либо в INDEX явно записано, почему номер
делят двое. Плюс проверка, не даёт ли реестр ADR ещё коллизий.
