---
trackerStatus:
  type: agent-task
title: Тест пути стоп-крана пытается написать владельцу в живой Telegram — страж цикла #55 краснит CI на main
status: backlog
source: session-2026-07-31-cycle56
priority: high
created: 2026-07-31
---

**Как найдено (цикл #56, 2026-07-31).** На срезе CI `tests/` (команда из `test.yml`) появилась
ОШИБКА, которой не было в отчётах прошлых циклов:

```
ERROR at teardown of TestThreatReactorHeldScoping.test_reactor_activates_kill_switch_for_held_threat
spa_telegram_guard.LiveTelegramSendAttempted: 2 live Telegram API call(s) attempted in
tests/test_kill_switch_eval_path.py::TestThreatReactorHeldScoping::test_reactor_activates_kill_switch_for_held_threat
```

**Воспроизведено на ЧИСТОМ `origin/main`** (`e98cd3610`, без единой правки цикла #56):
`tests/test_kill_switch_eval_path.py` → **21 passed, 1 error**. То есть находка предсуществующая,
к работе цикла #56 отношения не имеет (та трогает только `scripts/` + свой файл тестов).

## Что не так

Это **пятый отправитель**, пойманный стражем живого Telegram (`spa_core/tests/telegram_guard.py`),
который построил цикл #55. Четырёх он назвал в отчёте (`test_lead_tracker` 39, `test_e2e_full_pipeline`
6, `test_uptime_monitor` 1 — все в `tests/`, где сеть глушится conftest'ом, плюс сам
`test_cycle_gap_monitor`, из-за которого всё и началось). **Этот — не назван.** Правдоподобное
объяснение: путь до отправки здесь условный — алерт уходит, только когда `threat_reactor`
реально активирует стоп-кран на ДЕРЖИМОМ протоколе, а это зависит от состояния фикстур/данных;
в прогоне #55 ветка могла не сработать. Проверять предположение — часть задачи.

**До владельца сообщение НЕ доходит** (страж перехватывает `urlopen`, `telegram_client` пишет
`Telegram send failed after 2 attempt(s)`), и стоп-кран пишется в `tmp`-каталог, а не в живой
`data/` — вреда проду нет. **Но шаг `tests/` в CI из-за ошибки teardown завершается ненулевым
кодом ⇒ `SPA Tests` на `main` красный.**

## Почему не починено циклом #56 молча

1. Домен стоп-крана: файл — тест пути `threat_reactor → kill_switch`. Протокол (автономный
   мандат) прямо запрещает автономно трогать risk / kill-логику; правка теста здесь требует
   понимания, ЧТО именно он пиннит, чтобы не ослабить проверку (инв. #16).
2. Один цикл — одна задача; цикл #56 поднимал осиротевшую работу #54.

## Что предлагается сделать

Ровно то, что предписывает сам страж и что цикл #55 уже применил к трём другим файлам:
**подменить отправителя в тесте** (patch `_post_message` / `_send_*` модуля) либо передать
`dry_run=True` — НЕ ослабляя ни одного ассерта о поведении стоп-крана. Проверить попутно
гипотезу «ветка условная»: если алерт уходит не всегда, то и страж ловит не всегда, и такой
тест — источник плавающего красного CI.

## Acceptance criteria

- [ ] `tests/test_kill_switch_eval_path.py` — 22 passed, 0 errors (сейчас 21 passed + 1 error);
- [ ] ни один ассерт о поведении `threat_reactor` / стоп-крана не изменён и не ослаблен (инв. #16);
      правка касается ТОЛЬКО транспорта уведомления;
- [ ] срез CI `tests/` не содержит ошибок стража (кроме предсуществующего падения
      `test_evidence_seeded`);
- [ ] записано, был ли путь отправки условным (и если да — почему цикл #55 его не увидел).

## Как понять, что готово

Команда `test.yml` по `tests/` даёт **12 771 passed / 1 предсуществующее падение / 0 ошибок**,
и это подтверждено настоящим прогоном Actions на `main`, а не только локалью.
