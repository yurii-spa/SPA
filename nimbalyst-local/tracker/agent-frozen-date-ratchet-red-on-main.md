---
trackerStatus:
  type: agent
title: Храповик литеральных дат КРАСНЫЙ на main — два новых тест-файла волны 1 (A1) вошли в закрытый класс
status: done
source: cycle-118
created: 2026-08-05
priority: high
domain: тесты / CI (НЕ money-path)
---

## Что нашли (замер 2026-08-05, цикл #118)

`spa_core/tests/test_frozen_date_ratchet.py::test_no_new_file_joins_the_frozen_date_class`
падает **на чистом `origin/main` 5440626d9**:

```
E   AssertionError: New test file(s) pin a literal date next to a freshness concept:
E       test_apy_series.py
E       test_apy_series_accumulator.py
```

Оба файла приехали волной 1 (воркстрим **A1 — линия время-рядов**, коммит `6170eb1f`).
Храповик из `.claude/rules/deployment.md` устроен так, что база может только уменьшаться:
добавить файл в `frozen_date_baseline.json`, чтобы погасить падение, **запрещено** —
чинить надо фикстуру.

## Почему это важно

1. CI на `main` снова красный — цикл #117 закрыл предыдущий красный (`ff65479c7` был зелёным
   целиком), волна 1 приземлилась после и открыла новый.
2. Класс дефекта — тот самый, ради которого храповик и заводили: тест с литеральной датой
   рядом с понятием свежести начинает падать сам по себе, когда сдвинется календарь.
   Сегодня он краснеет по храповику, а через N дней покраснел бы уже по существу и без
   всякой связи с проверяемым поведением.

## Чего цикл #118 НЕ делал и почему

Файлы принадлежат области, объявленной другой сессией (аналитика / время-ряды, волна 1→2 идёт
прямо сейчас в `/tmp/spa_wt_wave2`). Молча править чужие свежедоставленные тесты — ровно то,
от чего защищает объявление владения. Поэтому: карточка + уведомление, не тихая правка.

## Что сделать

1. Починить **фикстуру**, одним из трёх способов по протоколу (`.claude/rules/deployment.md`):
   инъекция часов `now=` (лучше) · относительные отметки `spa_core/tests/_freshness.py::ts()` ·
   `# FROZEN-DATE-OK: <причина>`, если сама дата является предметом теста.
2. **Не** трогать `frozen_date_baseline.json` — база только сокращается.
3. Прогнать `spa_core/tests/test_frozen_date_ratchet.py` и убедиться, что `main` снова зелёный.

## Критерий «сделано»

`test_no_new_file_joins_the_frozen_date_class` зелёный на свежем чекауте `origin/main`,
`frozen_date_baseline.json` не вырос.

## Закрыто 2026-08-05 (цикл #119) — ПРОВЕРЕНО, не принято на веру

Задание владельца «починить» пришло уже после того, как дефект починили: волна 3 (`d1faaf6f5`)
закрыла его **правильным способом** — не ростом базы, а объявлением предмета теста.

Измерено на чистом чекауте `origin/main d1faaf6f5`:

- `spa_core/tests/test_frozen_date_ratchet.py` — **5 passed** (падало
  `test_no_new_file_joins_the_frozen_date_class`);
- `frozen_date_baseline.json` — `count: 251`, фактических записей 251, `snapshot: 2026-08-05`
  = **база НЕ выросла** (запрет из `.claude/rules/deployment.md` соблюдён);
- оба файла получили обоснованные маркеры `# FROZEN-DATE-OK` (вариант 3 протокола, законный,
  т.к. в обоих даты — предмет проверки: выравнивание осей и идемпотентность по дате;
  в `test_apy_series_accumulator.py` часы вдобавок инжектируются через `accumulate(today=...)`);
- CI на `main @ d1faaf6f5`: **SPA Tests / SPA CI / SPA Proof Gate / SPA Lint — все success**.

Дубль-карточку не заводил (шаг 1a протокола, вердикт DONE).
