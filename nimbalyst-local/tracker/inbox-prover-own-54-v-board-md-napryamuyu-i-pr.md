---
trackerStatus:
  type: inbox
title: проверь own-54 в _BOARD.md напрямую и пришли мне её с вариантами ответа
status: done
source: telegram
created: 2026-08-19
---

## Задание (из Telegram)

проверь own-54 в _BOARD.md напрямую и пришли мне её с вариантами ответа

---
_Оркестратор: классифицируй (задача/идея/непонятно), при исполнении закрой карточку со ссылкой на порождённую работу (§6.4)._

---

## Разобрано (цикл #310) — уже сделано ТОБОЙ же, дубль не завожу

Классификация: **задача**, вердикт шага 1a — **DONE**.

`own-54-mertvye-knigi-issledovatelskoy-paneli` в доске есть и всегда была (жалоба была на бота,
который отрицал её существование — это чинилось коммитами `513a6b412` и `151244de4`: ответ про
очередь теперь читается из доски, а не «из памяти»).

Карточка тебе ушла с вариантами — и **ты на неё ответил** 19.08 в 21:51:32:
**вариант 1 — починить фиды**. То есть задание закрылось само, пока шло.

Что означает твой ответ (записан, исполняется отдельным циклом): четыре книги из десяти в
исследовательской панели не начисляли ничего 80 % времени — у них пропал источник данных, а
«данных не было» и «доходность ровно ноль» в файле выглядят одинаковым нулём. Чиним фиды и
перемеряем записи #40/#45/#54 на честной десятикнижной панели; числа реестра могут заметно
измениться — это ожидаемо. Живой трек, RiskPolicy и `data/` это не трогает: панель
исследовательская.
