---
trackerStatus:
  type: owner-decision
title: Наблюдённая угроза до тебя не доезжает, а наша собственная остановка доезжает каждый день
status: blocked
priority: medium
owner: yuriycooleshov@gmail.com
created: 2026-08-18
source: inbox-nahodka-petli-analitik-red-team-critical
---

## Что случилось и почему это важно

У нас есть аналитик-разведчик (`red_team`): он смотрит, нет ли угроз вокруг протоколов, и
ставит «постуру» — одно слово о состоянии. Дальше сверка (`house_view_gap`) превращает
тревожное слово в находку, а мост — в карточку тебе.

Замер 2026-08-18 (мой прогон, четыре состояния подряд):

| что произошло на самом деле | слово аналитика | доедет ли до тебя карточкой |
|---|---|---|
| разведка НАШЛА угрозу | `THREATS_PRESENT` | **НЕТ** |
| разведка нашла критику в симуляции атак | `CRITICAL` | да |
| мы сами остановлены выключателем | `CRITICAL` | да, каждый цикл |
| тихо | `NO_THREAT_OBSERVED` | нет (и правильно) |

То есть канал перевёрнут: **эхо нашей же остановки шумит ежедневно, а настоящая найденная
угроза молчит.** Причина техническая: карточку заводит только слово `CRITICAL`, а найденная
угроза называется другим словом (`THREATS_PRESENT`) и в лестницу тревоги не попадает.

Сам чинить не стал: лестница постур закреплена решением цикла #198, и её изменение меняет,
что именно будит владельца — это твой выбор, а не агентский.

## Что от тебя нужно

Выбрать один вариант:

- **Вариант А (рекомендация).** Поднять `THREATS_PRESENT` до уровня, который заводит карточку
  (наравне с критикой симуляции атак). Наблюдённая угроза начинает доезжать. Цена: примерно
  полдня с тестами; шума не прибавится — сегодня таких срабатываний ноль.
- **Вариант Б.** Оставить лестницу как есть, но перестать заводить карточку на эхо нашей
  собственной остановки (она и так известна и уже имеет карточку с кнопками). Тогда ежедневный
  шум уходит, но найденная угроза по-прежнему до тебя не доедет.
- **Вариант В.** Оставить как есть и записать это решением: «наблюдённая угроза не будит
  владельца» — допустимо, но это должно быть выбрано, а не получиться молчанием.

## Как понять, что готово

Есть записанный выбор, и на выбранном варианте прогон показывает: состояние
«разведка нашла угрозу» даёт (или сознательно не даёт) карточку — с тестом в обе стороны.

## Что будет после

Вариант А — правлю условие лестницы и добавляю положительный контроль в
`spa_core/tests/test_red_team_finding_loop.py` (сейчас он эту асимметрию специально НЕ
закрепляет, чтобы не узаконить кривое). Вариант Б — снимаю эхо остановки из канала находок.
Вариант В — записываю решение в карточку и закрываю тему.

---

## ДУБЛЬ — вопрос уже задан владельцу другой карточкой (сведено 18.08)

Эта карточка и `owner-decision-razvedka-krichit-critical-na-nashu-zhe-o` — **один и тот же вопрос**
разными словами: канал `red_team` заводит карточку на НАШУ СОБСТВЕННУЮ остановку и молчит на
реально наблюдённой угрозе (`THREATS_PRESENT` отсутствует в списке «красных» слов).

Заведена по недосмотру: сверка искала карточку по своему имени и не нашла существующую под другим.
Ровно тот класс, который мы весь день чиним, — «поиск по имени вместо поиска по предмету».

Ведущая карточка — `owner-decision-razvedka-krichit-critical-na-nashu-zhe-o` (там же варианты и
рекомендация чинить оба конца одной правкой). Эта переведена из `needs-owner`, чтобы владелец не
получил один вопрос дважды; содержание не удалено — замер канала в теле остаётся как второе
независимое подтверждение (эхо остановки → 1 находка, `threats_present` → 0 находок).

---

## 🚚 Перенесено на `main` циклом #324 (20.08→21.08)

Карточка родилась на ветке `origin/claude/work-status-check-xfnbew` (`6025edc8a`) и на `main` её
не было ВООБЩЕ. Перенос — по решению владельца 20.08 19:12:45Z (вариант 1, ADR-100):
разобрать карточки ветки СОДЕРЖАНИЕМ, потом ветку удалить
(задача `inbox-razobrat-52-kartochki-s-vetki-work-statu`).

**С чем сверялось на `main`:** `inbox-red-team-critical-eto-eho-ostanovki-a-ne-nahodka` (main, **`done`**).

**Почему всё-таки перенесена:** Та карточка закрыта и покрывала ТОЛЬКО половину «CRITICAL — эхо нашей же остановки».
Вторая половина — что настоящая наблюдённая угроза (`THREATS_PRESENT`) до владельца НЕ доезжает —
на `main` не записана нигде, а закрытая карточка создаёт впечатление, что вопрос разобран целиком.
Именно ради этой половины карточка и переносится.

**Владельцу пачкой НЕ отправлялась** (ADR-084: штатное и решённое не звонит владельцу) — `notify`
по ней не запускался. Код и тесты ветки не переносились: так записано в решении владельца.

