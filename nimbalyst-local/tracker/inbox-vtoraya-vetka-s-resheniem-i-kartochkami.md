---
trackerStatus:
  type: inbox
title: "Вторая ветка с решением и карточками владельца: unreadable-description-ltyucb — измерить, жива ли, и разобрать"
status: done
source: nimbalyst
created: 2026-08-20
claimed_by: cycle-328
claimed_at: 2026-08-21T06:05:49Z
---

## Что случилось

Обязательный шаг 0a (`scripts/check_undelivered_work.py`) циклом #324 назвал **ВТОРУЮ** ветку, на
которой лежат решение и карточки, отсутствующие на `main` целиком:

`origin/claude/unreadable-description-ltyucb` — 8 коммитов сверх базы, 17 уникальных путей,
последний коммит **2026-08-20**. Из них:

- **1 решение:** `docs/decisions/ADR-102-btc-cycle-backtest-archived-not-built.md`
  («BTC-движок: чемпион v0.1 принят как ЗАМЕР, реализация НЕ начинается — просадка не помещается
  под стоп-кран», Accepted);
- **3 карточки:** `own-btc-dvizhok-ne-vlezaet-pod-stop-kran` (развилка ВЛАДЕЛЬЦУ),
  `agent-pasporta-zapolneny-chastichno-ostalos-kuratoru`,
  `inbox-progon-testov-perepisyvaet-sorok-otslezhivaemyh-failov-data`;
- **новое правило** `.claude/rules/design-docs.md`, реестр зрелости `docs/MATURITY_REGISTER.md`,
  храповик `test_design_doc_status_ratchet.py`, воркфлоу `generated-docs-integrity.yml`,
  материалы `research/btc_cycle/`.

Про первую такую ветку (`work-status-check-xfnbew`, 8 решений + 52 карточки) владелец уже решил
20.08 — разобрать и удалить (`inbox-razobrat-52-kartochki-s-vetki-work-statu`, ADR-100).
**Эта — вторая, и её не карточил никто:** поиск по `main` (`ADR-102`, `ltyucb`, `btc-dvizhok`,
`unreadable-description`) даёт ноль совпадений в `nimbalyst-local/tracker/` и в `docs/`.

## Почему это НЕ то же самое, что первая ветка

Первая ветка была мёртвой (последний коммит 19.08, работа брошена). Эта — **свежая**, и один из её
коммитов (`d2e1e5c5b`) прямо говорит: «BTC-бэктест уехал с ADR-101 на ADR-102 — номер занят
мандатом автономии на main». То есть автор ветки СВЕРЯЛСЯ с `main` и, возможно, ещё работает.

Поэтому эта карточка — **не задание «разобрать и удалить»**. Слепо применить к ней рецепт первой
ветки значило бы затереть чужую живую работу. Сначала надо измерить, жива ли сессия автора.

## Что сделать

1. Измерить, идёт ли работа: возраст последнего коммита, объявления в `data/session_changes.jsonl`
   с этими путями, `ps` по объявленному долгоживущему процессу (порядок шага 0a, авто-подъёма нет).
2. **Если работа брошена** — разобрать по образцу первой ветки: 3 карточки и ADR-102 сверить
   СОДЕРЖАНИЕМ с `main`, живое перенести, дубли назвать. Отдельно проверить
   `inbox-progon-testov-perepisyvaet-sorok-otslezhivaemyh-failov-data`: она поправляет число в
   карточке `agent-test-run-dirties-tracked-fixtures` на `main` (сорок файлов вместо трёх) — если
   это верно, наша карточка занижает масштаб втрое.
3. **Если работа идёт** — не трогать, дождаться пуша автора; записать в журнал, что ветка измерена
   и оставлена намеренно.
4. Номер ADR-102 на `main` СВОБОДЕН (последний занятый — ADR-101). Если ветка не приземлится, номер
   надо либо освободить, либо занять переносом — иначе следующий цикл выдаст ADR-102 повторно
   (класс `ADR-099`: номера, взятые на ветках, `main` не видит).

## Как понять, что готово

Про ветку `unreadable-description-ltyucb` в журнале записано измерение (жива / брошена) и вытекающее
действие; если брошена — её решение и 3 карточки отнесены к трём кучам, как в первой ветке.

## ЗАМЕР 21.08 (цикл #328) — ветка ЖИВА, а сторож этого сказать не мог

**Ответ по п.1: работа ИДЁТ, ветку не трогаю.** Измерено 06:04Z:

| Что | Значение |
|---|---|
| вершина ветки | `eaeab6aaa` 2026-08-21 **05:40Z** — merge нашего же `origin/main` |
| последний коммит **автора** (`--no-merges`) | `282681925` 2026-08-20 **23:56Z** |
| последнее касание **решений и карточек** | `099c8f396` 2026-08-21 **01:56Z** |
| merge'ей базы поверх | 3 · объявлений в `session_changes.jsonl` — **0** (автор в облаке) |

Доказательство жизни — не дата, а содержимое: merge `099c8f396` дописал **25 строк** в карточку
`inbox-progon-testov-perepisyvaet-sorok-otslezhivaemyh-failov-data` — раздел «СВЕДЕНИЕ
ИСТОЧНИКОВ (дописано 21.08 после переноса на свежий `main`)», ссылающийся на **наш** коммит
`4a98fd7` (замер цикла #324). Файла на main-стороне merge'а нет вовсе ⇒ содержимое авторское.
Автор читает наш `main` и сводит с ним свою работу. Разбирать её было бы затиранием живого.

**П.2 снят замером, а не переносом.** Карточка ветки утверждала «сорок файлов» против «трёх» на
`main`; цикл #324 уже измерил живьём **83** git-tracked файла и поправил
`agent-test-run-dirties-tracked-fixtures`. Наша карточка масштаб НЕ занижает — переносить нечего,
и автор ветки сам на этот замер сослался.

**П.4 снят замером.** `adr_number.py next` → **ADR-103**, занятых 59 «союз origin/main + остальные
удалённые ветки + рабочее дерево»: номер `ADR-102` виден занятым НА ВЕТКЕ. Класс ADR-099 закрыт,
повторной выдачи не будет.

**П.3 исполнен и по букве, и сверх неё.** Развилка карточки («жива → не трогать; брошена →
разобрать») опиралась на строку шага 0a «последний коммит 2026-08-21». Строка верна и отвечает
не на тот вопрос: `remote_branch_refs` датирует ветку `committerdate` ВЕРШИНЫ, а вершину двигает
merge нашей же базы. Ветка, которую облачная сессия мержит несколько раз в сутки, выглядит
**вечно живой** — и потерянные на ней решения владельца не поднимаются НИКОГДА (fail-OPEN внутри
fail-CLOSED-сторожа, класс #226). Здесь дата ВЫБИРАЛА одно из двух противоположных действий над
чужой работой, поэтому починен сам сигнал: шаг 0a теперь печатает ОБА числа —

```
[ветка] origin/claude/unreadable-description-ltyucb (последний коммит 2026-08-21 05:40, коммитов сверх базы: 12)
    решения и карточки этой ветки не трогали с 2026-08-21 01:56 — поверх лежат 2 коммит(ов) автора,
    ни один их не касается: свежесть ВЕРШИНЫ про предмет находки НЕ ГОВОРИТ
```

Признак — **касание путей**, а не `--no-merges`: дешёвая альтернатива на этой же ветке даёт
23:56 (замороженной объявляется карточка, которую правили в 01:56) и зовёт разбирать живую
работу — та самая разрушительная сторона, от которой предостерегает эта карточка. Обратный
контроль на это стоит в тестах. Отметки печатаются с МИНУТАМИ: весь разрыв внутрисуточный,
обрезка до дня стёрла бы ровно его.

**Что осталось владельцу — ничего нового:** `own-btc-dvizhok-ne-vlezaet-pod-stop-kran` живёт на
ветке и на `main` не попадёт, пока автор не запушит. Статус ему не ставился (инв. #14), логика
стоп-крана не тронута. Если автор бросит работу, шаг 0a теперь скажет это ЧИСЛОМ, а не датой
нашего же merge'а, и находка придёт обычным путём.

## Чего делать нельзя

Удалять ветку до измерения · переносить её код (правило первой ветки; тут кода 6 файлов + тесты) ·
ставить `owner-done` карточке `own-btc-dvizhok-ne-vlezaet-pod-stop-kran` (инв. #14) ·
трогать логику стоп-крана — ADR-102 её касается, но только как ОТКАЗ строить движок.

## ➕ ПОДЪЁМ 21.08 (цикл #329) — работа #328 доставлена, приёмка перемерена

Сессия #328 умерла между работой и пушем (`ps -p 19434` и `ps -p 19448` — пусто; на `origin/main`
не было НИЧЕГО). Находку дал шаг 0a, раздел «осиротело, но окно не истекло»; шаг 0b — «старый
захват, кандидат на подъём». База осиротевшего дерева сверена с моей ДО переноса (`c0be56136`,
совпала) — урок #226.

**Отчёту мёртвой сессии не верил, и приёмка вышла сильнее заявленной:** контроль на чистом origin
того же sha 12/12 красных (сходится), а мутаций не 4, а **5** — добавлена мутация самой ПРОВОДКИ
(снятая строка `render` при живом `owner_work_last_touch` красит якорный тест ⇒ он и правда про
ПОВЕДЕНИЕ отчёта, а не про отсутствующий символ; на чистом origin это неотличимо — там красно всё).
`--no-merges` красит 5 тестов вместо 2, проглоченное «не измерено» — 2 вместо 1.

Живой замер по настоящим веткам числа #328 воспроизвёл независимо: `unreadable-description-ltyucb`
вершина 05:40 против касания 01:56, поверх 2 коммита автора; `work-status-check-xfnbew` — 02:27
против 01:33, поверх 7.

Выводы карточки НЕ пересматривались: ветка ЖИВА, не тронута; `own-btc-dvizhok-ne-vlezaet-pod-stop-kran`
статуса не получала (инв. #14).

## ➕ ВТОРОЙ ПОДЪЁМ 21.08 (цикл #330) — доставлено; приёмка перемерена в третий раз

Сессия #329 (подъём #328) тоже умерла между работой и пушем: `ps -p 19434` · `ps -p 19448` ·
`ps -p 45254` — пусто у всех трёх, на `origin/main` не было НИЧЕГО. Находку дал шаг 0a
(раздел «осиротело, но окно не истекло», 5 путей), шаг 0b — «🟡 старый захват». База
осиротевшего дерева сверена с моей ДО переноса (`c0be56136`; `git log` по `STATE.md` и журналу
от неё до `daa0f39e8` — пусто, расхождения нет).

**Отчёту мёртвых сессий не верил.** Свой прогон: **12 passed** в дереве подъёма против
**12 failed** на чистом `origin/main` того же sha. Пять мутаций, каждая убита своим тестом
(проводка `render` снята при живом замере → 1 · `--no-merges` → 2 · «не измерено» проглочено → 2 ·
счёт `sha..ref` → 1 · отметка до дня → 3); дерево после них восстановлено побайтно. Одно число
предшественника не сошлось и названо честно: `--no-merges` в моей мутации красит **2** теста, а
не 5 — мутация ставилась поверх фильтра путей, а не вместо него.

**Вывод карточки перемерен и НЕ пересмотрен: ветка ЖИВА.** Живой замер 21.08 08:49Z: вершина
`08:06`, последнее касание решений и карточек — **`07:44`** (22 минуты до замера), поверх 2
коммита автора. С момента #328 на ветке прибавилось: решений 1 → **3** (`ADR-102/103/104`),
карточек 3 → **4**. Не трогаю, `own-btc-dvizhok-ne-vlezaet-pod-stop-kran` статуса не получала
(инв. #14).
