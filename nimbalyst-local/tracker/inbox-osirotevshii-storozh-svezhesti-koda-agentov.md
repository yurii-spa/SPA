---
trackerStatus:
  type: inbox
title: "Осиротевший сторож свежести кода агентов (цикл #177) — 1136 строк лежат в /tmp, не проверены"
status: new
source: session-cycle-178
created: 2026-08-09
priority: medium
tags: [orphaned-work, monitoring, agent-health, deployment]
---

## Что случилось и почему это важно

**Цикл #177 умер, не доставив работу.** Сессия `pid21933` (старт 09.08 09:10, объявление
07:12Z) взяла карточку `inbox-dolgozhivuschie-agenty-ne-podhvatyvayut` и написала под неё
сторожа, отвечающего на ЧЕТВЁРТЫЙ вопрос доставки — тот, которого нет ни у одного
существующего монитора:

| вопрос | кто отвечает |
|---|---|
| Это тот код, который мы приняли? | `deployment_drift_monitor` |
| Способен ли флот стартовать? | `deployment_acceptance` |
| Агенты реально работают? | `agent_health_monitor` |
| **Исполняет ли ЖИВОЙ долгожитель тот код, что лежит в дереве?** | **никто** ← это |

Процесса `pid21933` больше нет (проверено `ps`), на origin ничего не приземлилось. Работа
лежит в одноразовом каталоге `/tmp/spa_wt_c177` (база — `origin/main` `55edb9f4d`):

| файл | строк | состояние |
|---|---|---|
| `spa_core/monitoring/agent_code_freshness.py` | 681 | новый, НЕ доставлен |
| `spa_core/tests/test_agent_code_freshness.py` | 455 | новый, НЕ доставлен |
| `spa_core/monitoring/agent_health.py` | +43 | правка, НЕ доставлена |
| `nimbalyst-local/tracker/agent-…-krutyat-staryi-kod.md` | 79 | **поднята #178** ✅ |

## Что сделать

**Принять и проверить код — или осознанно отказаться.** Порядок обязателен, чужой мёртвый
код на веру не поднимают:

1. Каталог `/tmp/spa_wt_c177` живёт до перезагрузки Mac — начать с копии, пока он есть.
2. Прогнать `test_agent_code_freshness.py` и убедиться, что тесты **краснеют на дереве без
   починки** (положительный контроль). Отчёта мёртвой сессии о зелёных тестах нет вообще —
   она не дошла до строки `verified`.
3. Проверить, что правка `agent_health.py` не меняет поведение существующих проверок
   (`agent_health_monitor` кормит тревоги; регресс здесь — тихая потеря пульса).
4. Полный набор + контроль на приколоченном sha, как обычно.
5. База `55edb9f4d` отстала от origin на несколько коммитов — перенести, а не копировать.

**Если код не выдержит проверки — не чинить чужой замысел молча**, а закрыть карточку
отказом с замером и завести свою. Вопрос-то настоящий: измерение руками 09.08 показало
`apiserver` с кодом от 17 июля (23 дня) и `familyfund` от 2 июля (38 дней).

## Как понять, что готово

Сторож либо на origin с зелёным полным набором и положительными контролями, либо отвергнут
записью с замером — и в обоих случаях `/tmp/spa_wt_c177` больше не единственное место, где
живёт эта работа.

## Что будет после

Появится (или будет осознанно отклонён) четвёртый сторож доставки. Вопрос владельцу про
перезапуск долгожителей уже задан отдельно —
`agent-dolgozhivuschie-agenty-krutyat-staryi-kod` (по ADR-078 это уже задача агента, не вопрос владельцу); она **не ждёт** этой карточки,
потому что факт отставания измерен независимо.

_Источник: цикл #178, шаг 0a (`check_undelivered_work.py`, код 1)._
