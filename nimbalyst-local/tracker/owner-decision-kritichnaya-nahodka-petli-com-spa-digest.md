---
trackerStatus:
  type: owner-decision
title: "Критичная находка петли: com.spa.digest_weekly работает при intent=retired"
status: ingested
source: nimbalyst
created: 2026-08-08
finding_key: "B1:zombie:com.spa.digest_weekly"
---

## Что случилось и почему это важно
Сторож петли (architecture_conformance) нашёл КРИТИЧНОЕ расхождение с архитектурой:
com.spa.digest_weekly работает при intent=retired

## Что от тебя нужно
Посмотреть находку и решить: чиним / принимаем осознанно (тогда фиксируем решение в манифесте или ADR). Рекомендация агента — чинить: критичные находки этого класса уже стоили нам молчаливых отказов.

## Как понять, что готово
Находка исчезает из data/architecture_conformance.json при следующем прогоне.

## Что будет после
Мост сам закроет эту карточку, когда находка исчезнет; сторож продолжит следить, чтобы она не вернулась.

_finding_key: `B1:zombie:com.spa.digest_weekly` · источник: architecture_conformance · ADR-066_


## Решение владельца 2026-08-09: ЗАКРЫТЬ (ложная тревога)

Находка была следствием того, что прод-дерево не получало `architecture/` (code_sync возит
только spa_core/scripts/tests) и перегенерировало конституцию из стёртой памяти. Причина
устранена сверкой курации с `origin/main`; сторож показывает **OK, 0 CRITICAL** при флоте 81.
Вопрос отпал — карточка закрыта решением владельца.

---

## Приписка цикла #171 (2026-08-09) — находка оказалась ЛОЖНОЙ

Эта карточка родилась автоматически (мост находок ADR-066). Сама находка к моменту доставки
**уже опровергнута**: сторож архитектуры судил по СТЁРТОЙ памяти прод-дерева и выдал четыре
ложных CRITICAL об исправных агентах — разобрано и починено циклом #169.

Карточка провисела в прод-дереве и **не доезжала до origin** (пуша в мосте не было ни строкой;
доставку добавил #170 — `spa_core/monitoring/card_delivery.py`). Этот цикл довозит её, чтобы
очередь на origin совпадала с реальностью, а шаг 0a перестал показывать её как потерянную.

**Закрыть её сам агент не может** сразу по двум причинам: `close_card` умеет закрывать только
`status: new`, а CRITICAL рождается сразу `needs-owner` (карточка
`inbox-most-ne-mozhet-zakryt-sobstvennuyu-criti`), и статус `owner-done` ставит только владелец
(инвариант #14).

**Рекомендация: закрыть без действий** — агент, о котором тут речь, исправен.
