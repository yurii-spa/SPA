---
trackerStatus:
  type: inbox
title: Вопрос владельца жив на origin, а в дереве та же карточка старая и закрытая — сторож считает его видимым по ИМЕНИ файла
status: done
source: nimbalyst
created: 2026-08-31
claimed_by: pid91998
claimed_at: 2026-08-31T06:53:15Z
claim_takeover_reason: захват принадлежал циклу #437b (cycle-42676), который эту карточку СОЗДАЛ; его работа доставлена, квитанция снятия /tmp/spa_c437b от 03:25:00Z перечисляет этот файл как delivered. Работа по карточке не начиналась
status_trail:
  - "2026-08-31T07:06:04.571713+00:00 new -> done · queue.set_status · cycle-91998"
---

## Что случилось и почему это важно

Сторож очереди владельца (`data/owner_decision_pending.json`) печатает строку
«очередь полна: невидимых дереву вопросов нет» и поле
`origin_queue: {count: 0, hidden: []}`. Замер 31.08 (цикл #437) показал, что эта
строка отвечает не на тот вопрос, который по ней читают.

Живой пример того же дня — `own-dashboard-razdaval-repozitorii-v-set`:

- на `origin/main` (коммит `5fd2db03a`, 30.08 22:06Z) карточка в `needs-owner` и несёт
  **новый** вопрос владельцу — что должен показывать агентский сервер;
- в прод-дереве лежала **версия от 21:51Z** — старый вопрос, в терминальном статусе.

Сторож посчитал вопрос ВИДИМЫМ, потому что файл с таким именем в дереве есть. Но
содержимое разное, статус разный, и кнопка владельца пишет ответ именно в прод-копию —
то есть ответ на новый вопрос лёг бы под текстом старого.

Это ровно тот класс, что ведётся в STATE отдельным разделом: сторож честно отвечает на
СВОЙ вопрос («есть ли файл с таким именем?») и читается как ответ на нужный («дойдёт ли
живой вопрос до владельца и туда ли попадёт его ответ?»).

Отдельно рядом стоит вторая улика того же случая: карточка на origin несёт
`owner_choice: 1` — ответ на ПРЕДЫДУЩИЙ вопрос — и одновременно `status: needs-owner` с
новым вопросом. Поле ответа пережило вопрос, к которому относилось.

## Что от тебя нужно

Автономная задача агента (hardening, не owner-gated, risk-логики не касается).

1. Мерить достижимость, а не имя файла: карточка в `needs-owner` на `origin/main`, чья
   копия в дереве **отличается содержимым** или стоит в терминальном статусе, — это
   находка, а не «видима». У сверки трекера (`check_tracker_drift`) нужное сравнение уже
   есть — здесь нужен не второй его экземпляр, а вызов (урок #47 о встроенной копии).
2. Третий исход обязателен: «не измерено» (origin недоступен) не должно читаться как
   «невидимых нет» — fail-CLOSED.
3. Решить отдельно, не молча: что делать с `owner_choice`, пережившим свой вопрос.
   Кандидат — при переводе карточки обратно в `needs-owner` с новым вопросом уводить
   старый ответ в регистр вытеснения (`SUPERSEDED_FIELDS`, ADR-160/180), а не оставлять
   его читаться как ответ на новый вопрос.
4. Положительный контроль: тест воспроизводит форму 30–31.08 (origin `needs-owner` с новым
   текстом против терминальной копии в дереве) и КРАСНЕЕТ при возврате правки. Обратный
   контроль: совпадающие копии по-прежнему НЕ находка, иначе сторож зальёт очередь шумом.

## Как понять, что готово

На форме 30–31.08 сторож называет карточку поимённо вместо `hidden: []`; на совпадающих
копиях молчит; недоступный origin даёт «не измерено», а не «чисто». Тесты зелёные.

## Что будет после

«Очередь полна» перестаёт быть утверждением о списке файлов и становится утверждением о
том, что живой вопрос владельца дойдёт до него и ответ вернётся туда, куда надо.

---

## Исполнено — цикл #439, 2026-08-31 (ADR-192)

**Пункт 1 (мерить достижимость, а не имя файла) — сделано.** `origin_view.unreachable_cards`
заменил `hidden_cards` в `_scan_origin_gap`. Исходов ТРИ, а не один, потому что чинятся они
по-разному: `absent` (файла нет) · `differs` (в дереве ДРУГОЙ текст — владелец читает не тот
вопрос) · `answer_outlived_question`. Исход печатается поимённо у каждой карточки — и в
отчёте, и в шаге 0-офис.

Деление расхождения на «дерево отстало» и «у дерева своя правка» СОЗНАТЕЛЬНО не сделано: оно
стоит обхода истории пути (замер ADR-184 — 84 с и 1041 процесс git), а на вопрос этого сторожа
не влияет. Кому нужно деление — тот зовёт сверку трекера; второй копии её доказательства здесь
нет (урок #47). Новый сторож стоит **0.49 с**.

**Пункт 2 (третий исход) — держится:** неизмеримость по-прежнему `Unmeasured` → `measured:
False` с причиной словами, и пустой список при неизмеренной сверке невозможен. Закреплено
двумя тестами.

**Пункт 3 (`owner_choice`, переживший свой вопрос) — решено НЕ молча, и решение — «называть,
но не чинить».** Замер `origin/main` 63a2f501a: живых вопросов владельца **7**, несущих
непустой `owner_choice` — **0**. Строить перенос в регистр вытеснения на популяции из нуля
значило бы угадывать форму, которой в природе нет (тот же fail-CLOSED, что в ADR-188).
Механизма нет, а ИМЯ есть: сторож называет форму отдельным исходом, и первый же случай будет
назван поимённо, а не найден глазами. Предпосылка «замер дал ноль» подлежит ПЕРЕЗАМЕРУ, а не
наследованию фразой.

**Пункт 4 (контроли).** 16 тестов, фикстуры — настоящие git-репозитории без сети, дат нет
вовсе. Ключевой — `test_the_old_guard_calls_the_same_state_visible`: на ОДНОМ входе прежний
вопрос даёт пусто, новый — находку; это доказывает, что находка новая, а не переименованная.
Обратные контроли: совпавшая байт-в-байт копия НЕ находка, закрытый на ref вопрос не находка,
чужой тип трекера не находка. **9 мутаций по координате краснеют все 9.**

**Замер эффекта по ЖИВОМУ дереву** (сухой прогон, ничего не записано): было `count: 0` и
«очередь полна» — стало **3** названные поимённо карточки (`own-dengi-stoyat-iz-za-starogo-potolka`
differs · `owner-decision-doizmerili-te-82-modulya...` absent, НИ РАЗУ не отправлялась ·
`owner-decision-knigu-perekladyvayut-22-raza...` differs). Тот же набор независимо
подтверждает `check_tracker_drift.analyze` — другой код, тот же ответ.

**Правка чужого теста НАМЕРЕННА (инвариант #16)** и записана в журнале W36: утверждение о
ПОРЯДКЕ (`issues[0]`) заменено утверждением о ПРЕДМЕТЕ и РАСШИРЕНО.

Решение: **ADR-192**.
