---
trackerStatus:
  type: agent
title: 222 теста дёргают живой фид — 9 268 отказов за прогон; набор зелёный по неверной причине
status: backlog
source: замер 2026-08-08 (полный прогон)
created: 2026-08-08
priority: high
domain: тесты / герметичность
---

## Замер

```
9 268 отказов от 222 тестов за один полный прогон
```

Сторож живой сети держит — наружу ничего не уходит, каждый вызов получает fail-CLOSED `OSError`.
Но это значит, что **222 теста проверяют путь отказа вместо того, что собирались проверять**.

Два теста дают больше половины:

| тест | отказов |
|---|---|
| `test_cycle_nav_determinism::test_nav_conservation_property` | **3 600** |
| `test_cycle_nav_determinism::test_determinism_byte_identical_output` | **1 440** |

Тест с названием «детерминизм, побайтово одинаковый вывод» четыре тысячи раз упирается в
недоступный фид.

## Почему это дороже, чем выглядит

Денег не стоит — сторож держит. Но **тест, проверяющий отказ вместо логики, зелёный по неверной
причине**: он пройдёт и тогда, когда сломается ровно то, что он должен ловить.

Это ударило по реальной работе 2026-08-06/07. Дважды подряд диагноз строился на поведении набора,
которое зависело не от кода: сначала объявлена зависимость от порядка прогона (не подтвердилась
после ЧЕТЫРЁХ полных прогонов), потом от календаря (подтвердилась, но лишь с третьей попытки).
На герметичном наборе оба вопроса решались бы за один прогон.

## Что делать

Механизм **уже есть** и описан в `.claude/rules/adapters.md`: инъекция `FakeFeed` вместо живого.
222 теста его просто не используют.

**Порядок работ — по величине, а не по алфавиту:**

1. `test_cycle_nav_determinism` — два теста, 5 040 отказов из 9 268. Больше половины проблемы
   закрывается здесь.
2. Дальше по убыванию числа отказов (список в хвосте прогона).
3. Проверять не «тест зелёный», а **что он стал проверять другое**: до правки он падал бы на
   сломанной логике? Если нет — правка бесполезна.

## Чего не делать

Не браться за все 222 разом: тесты писали разные сессии в разное время, и правка каждого требует
понять, что он НА САМОМ ДЕЛЕ проверяет. Массовая механическая замена превратит 222 честных
«проверяю не то» в 222 молчаливых «проверяю ничего».

Не глушить сторожа живой сети. Он работает правильно и именно он это расхождение и обнаружил.


---

## УТОЧНЁННЫЕ ЗАМЕРЫ 2026-08-08 (после устранения моего вклада)

**Половина проблемы уже закрыта.** Замер отключением собственного вызова показал, что
`erc4626_rate_monitor` в цикле давал **4340 отказов из 9268** — это был мой регресс от
предыдущего дня, устранён стражем `PYTEST_CURRENT_TEST` (коммит 06162c65).

```
было 9268 → стало 4928 отказов от 222 тестов
```

**Разложение оставшихся** на самом громком тесте (`test_nav_conservation_property`, 1200 отказов),
измерено поочерёдным отключением:

| источник | отказов | статус |
|---|---|---|
| `erc4626_rate_monitor` | 2400 | ✅ устранён стражем pytest |
| `adapter_status_generator` (Step 0-pre) | **400** | открыт |
| третий источник, не найден | **800** | открыт |

## Почему я остановился здесь

Правка генератора выглядит очевидной — тот же страж, одна строка. Но она требует сначала понять,
**какие тесты ЗАВИСЯТ от того, что генератор отработал**: часть набора может опираться на
свежий `adapter_status.json`, который он пишет. Угадать нельзя — можно молча превратить рабочие
проверки в пустышки, а это хуже 400 отказов.

Проверка требует полного прогона (≈8 минут) и разбора зависимых тестов.

## Что делать следующей сессии

1. Найти **третий источник** (800 отказов) — он крупнее генератора и неизвестен. Метод тот же:
   поочерёдно отключать подозреваемых в цикле и мерить.
2. Для генератора: сначала выяснить, кто из тестов опирается на его вывод, потом ставить страж.
3. Проверять не «тест зелёный», а **что он стал проверять другое**: падал бы он до правки на
   сломанной логике? Если нет — правка бесполезна.


## СЛЕД к третьему источнику (не подтверждён замером)

Отсеяно замером: `concentration_monitor` и `anomaly_detector` в сеть **не ходят** (0 обращений).

Остаётся сильный кандидат: **8 адаптеров создают `DeFiLlamaFeed()` при инициализации**, а
аллокатор инстанцирует адаптеры в классовом гейте (`_adapter_class_gate` → `is_gsm_compliant()`).
То есть каждая проверка допуска протокола может тянуть за собой обращение к фиду.

**СЛЕД ОПРОВЕРГНУТ ЗАМЕРОМ 2026-08-08.** Классовый гейт отключён (`_adapter_class_gate` всегда
возвращает «допущен») — отказов по-прежнему **1200**, без изменений. Аллокатор и инстанцирование
адаптеров НИ ПРИ ЧЁМ.

Десятая правдоподобная версия за сутки, не пережившая проверки. Записываю отрицательный результат
намеренно: он дороже догадки, потому что экономит следующей сессии ложный путь.

### Что отсеяно (проверено, не предполагается)

| кандидат | вердикт |
|---|---|
| `erc4626_rate_monitor` | ✅ был источником 2400, устранён |
| `adapter_status_generator` | ✅ источник 400, открыт |
| `concentration_monitor`, `anomaly_detector` | ❌ в сеть не ходят |
| классовый гейт аллокатора / адаптеры | ❌ **опровергнуто замером** |
| `kill_switch` в цикле | ❌ **опровергнуто замером 2026-08-08** (1200 без изменений) |
| **третий источник, ~800** | **не найден** |

### Метод, который работает

Отключить подозреваемого одной строкой → прогнать `test_nav_conservation_property` (~2 минуты) →
сравнить число отказов. Три из четырёх кандидатов отсеяны именно так.

Оставшиеся необследованные места: `kill_switch`, `alert_manager`/`telegram_manager`, и то, что
вызывается ВНУТРИ инжектированных в тест `orchestrator_fn`/`allocator`.


## ПЯТЫЙ кандидат отсеян 2026-08-08

`kill_switch` в цикле отключён — отказов по-прежнему **1200**, без изменений. Не источник.

Осталось необследованным: `alert_manager` / `telegram_manager`, и то, что вызывается ВНУТРИ
инжектированных в тест `orchestrator_fn` / `allocator` (последнее вероятнее всего: тест передаёт
свои функции, но внутри них может создаваться настоящий адаптер).

Пять отсеянных кандидатов — главная ценность этой карточки: следующая сессия не пойдёт по уже
закрытым путям. Метод, сработавший пять раз: отключить подозреваемого одной строкой → прогон
`test_nav_conservation_property` (~2 мин) → сравнить число отказов.


## ШЕСТОЙ кандидат отсеян ЧТЕНИЕМ 2026-08-08

Инжектированные в тест функции **не ходят в сеть** — проверено чтением, без прогона:
`_make_orch` явно помечен «network-free orchestrator fake» и возвращает обычные словари
(`apy_pct`, `tvl_usd`, `tvl_source: live`), ни одного адаптера не создаётся. `_make_allocator`
такой же — возвращает фиксированную раскладку.

Значит источник оставшихся ~800 отказов находится **в теле самого `run_cycle`**, среди шагов,
которые ещё не отключались поштучно.

### Полный список отсеянного (6 кандидатов)

| кандидат | как проверено | вердикт |
|---|---|---|
| `erc4626_rate_monitor` | отключение | был 2400 → устранён стражем pytest |
| `adapter_status_generator` | отключение | **источник 400, открыт** |
| `concentration_monitor`, `anomaly_detector` | grep по сетевым вызовам | не ходят в сеть |
| классовый гейт аллокатора | отключение | опровергнуто |
| `kill_switch` в цикле | отключение | опровергнуто |
| инжектированные orchestrator_fn / allocator | чтение теста | network-free по построению |

### Что осталось

~800 отказов из тела цикла. Необследованы: `alert_manager` / `telegram_manager`, шаги аналитики
и отчётности после аллокации.

Метод, сработавший шесть раз: отключить подозреваемого одной строкой (или прочитать его, если
ответ виден без прогона) → `test_nav_conservation_property` (~2 мин) → сравнить число отказов.


## ПОПРАВКА к собственному выводу + новый след 2026-08-08

**Закрыто чтением:** `alert_manager` и `telegram_manager` — **ноль** обращений к фиду.

**ПОПРАВКА.** Ранее я отсеял `concentration_monitor` по признаку «нет прямых сетевых вызовов».
Это была **проверка не того**. Замер по реестру показал:

| модуль | обращений к `ADAPTER_REGISTRY` / фиду |
|---|---|
| `concentration_monitor` | **4** |
| `policy_enforcer` | 1 (×3 места вызова) |

Оба вызываются в цикле ПОСЛЕ аллокации. Путь к сети может быть **косвенным**:
монитор → `ADAPTER_REGISTRY` → инстанцирование адаптера → 8 адаптеров создают `DeFiLlamaFeed()`
в конструкторе. Мой первый grep искал прямые вызовы и такой цепочки не видел.

**Вывод об отсеве `concentration_monitor` — НЕНАДЁЖЕН.** Его надо перепроверить ОТКЛЮЧЕНИЕМ, как
остальных, а не чтением. Иначе следующая сессия унаследует ошибку метода: «не нашёл строку» ≠
«не ходит в сеть».

### Приоритет для следующей сессии

1. **`concentration_monitor` — отключить и померить.** Четыре обращения к реестру, вызывается
   после аллокации, ~800 отказов необъяснены. Самый вероятный кандидат из оставшихся.
2. `policy_enforcer` — тем же способом.
3. Если оба чисты — искать в шагах отчётности.

### Урок метода (второй раз за сутки)

Косвенный путь не виден поиском по строкам. Отключение проверяет ПОВЕДЕНИЕ, чтение проверяет
только текст — и они дают разные ответы там, где вызов идёт через реестр или фабрику.


## ПРИОРИТЕТНЫЙ ЗАМЕР ВЫПОЛНЕН 2026-08-08 — concentration_monitor НЕ источник

`concentration_monitor` отключён в цикле — отказов по-прежнему **1200**, без изменений.
Косвенный путь через `ADAPTER_REGISTRY` не реализуется: обращения к реестру есть, но адаптеры
при этом в сеть не идут.

**Первоначальный отсев оказался верным по результату**, хотя метод (поиск по строкам) был
ненадёжным. Поправка предупреждала о риске, который в этом конкретном случае не реализовался —
но предупреждение остаётся справедливым для других модулей.

### Состояние поиска: семь кандидатов закрыто

| кандидат | как проверен | вердикт |
|---|---|---|
| `erc4626_rate_monitor` | отключение | был 2400 → **устранён** |
| `adapter_status_generator` | отключение | **источник 400 — единственный найденный** |
| классовый гейт аллокатора | отключение | не источник |
| `kill_switch` | отключение | не источник |
| `concentration_monitor` | **отключение** | **не источник** |
| `alert_manager`, `telegram_manager` | чтение | ноль обращений к фиду |
| инжектированные orchestrator_fn / allocator | чтение | network-free по построению |

**~800 отказов остаются необъяснёнными.** Все очевидные подозреваемые закрыты.

### Что это значит для следующей сессии

Перебор по одному исчерпал себя: семь кандидатов, один найденный источник (400), 800 без
объяснения. Дальше эффективнее не гадать, а **инструментировать**: поставить трассировку в
сторож живой сети, чтобы он писал СТЕК каждого отказа. Тогда источник назовёт себя сам, и
перебирать больше не придётся.

Это правка сторожа, а не цикла — она безопасна и окупится сразу.


## ИСТОЧНИК НАЙДЕН 2026-08-08 — трассировкой, за один прогон

Рекомендация предыдущего витка применена сразу: в сторож живой сети поставлена
временная запись кадра вызова. Результат первого же прогона:

| источник | отказов |
|---|---|
| `base_gas_monitor.py:306` (`_fetch_gas_gwei`) | **800** |
| `adapter_status_generator.py:212` | 400 (был известен) |

Семь раундов перебора не нашли этого. Трассировка назвала источник за один прогон.
**Вывод о методе подтверждён замером, а не рассуждением.**

### Почему сторож в модуле не сработал

Сторож там БЫЛ: `if os.environ.get("SPA_ENV") == "ci": return None`. Он смотрел на
окружение, а не на факт прогона тестов — то есть защищал CI и **не защищал машину
разработчика**, где тесты пишут и запускают.

Класс тот же, что у «не измерено ≠ измерено и равно нулю»: проверка верная, условие
слишком узкое, и потому она молчит именно там, где нужна. Мой собственный регресс с
erc4626 был из этого же класса — и лечится тем же признаком.

### Правка

`PYTEST_CURRENT_TEST` (ставит сам pytest в любом прогоне) добавлен к прежнему условию —
расширение, не замена. В проде переменной нет, поведение монитора газа не меняется.

`test_gas_monitor_hermetic.py` держит **обе** стороны. Третий тест — положительный
контроль: без обоих признаков запрос обязан уйти и цена прочитаться. Без него правка
означала бы «монитор газа больше не работает никогда», и мы разменяли бы герметичность
на неработающий стоп-кран по газу.

### Итог

**1200 → 400 отказов** в `test_nav_conservation_property`. Остаток — известный
`adapter_status_generator`, у него отдельная природа (генератор снимка по замыслу
ходит в сеть; ему нужен инжектируемый фид, а не признак прогона).

Суммарно за два витка: **9268 → ~400** в этом тесте.


## ЗАКРЫТО ПОЛНОСТЬЮ 2026-08-08 — ноль отказов

Второй источник (`adapter_status_generator`) оказался **тем же дефектом**, а не другой
природы, как я предположил витком раньше. Ровно та же конструкция: `SPA_ENV == "ci"` —
сторож, смотрящий на окружение вместо факта прогона тестов.

Моя вчерашняя оценка «генератор по замыслу ходит в сеть, ему нужен FakeFeed» была
**неверной**. Достаточно было посмотреть на сам сторож, а не рассуждать о назначении модуля.

### Почему правка не может быть ослаблением

Поход в сеть из тестов и так заканчивался `OSError`, который ловится ниже по функции
(`except (URLError, TimeoutError, OSError) → return None`). То есть наблюдаемый результат
был `None` и до правки, и после — менялось только потраченное время и 400 отказов.

`test_generator_hermetic.py` закрепляет именно **тождество**: первый тест сравнивает
результат нового пути с результатом прежнего (реальный `OSError` из сторожа), а не просто
проверяет `is None`. Плюс две стороны прода: фид читается, а сетевой сбой по-прежнему даёт
честный `None`, а не выдуманное значение.

### Итог по карточке

| | отказов в `test_nav_conservation_property` |
|---|---|
| начало | **9268** |
| после устранения моего регресса (erc4626) | 4928 → 1200 |
| после `base_gas_monitor` (800) | 400 |
| после `adapter_status_generator` (400) | **0** |

270 смежных тестов зелёные. **Карточку можно закрывать.**

### Что осталось знанием

Оба источника — один класс: **сторож, привязанный к окружению вместо факта прогона**.
Если такой появится снова, искать его надо не перебором, а трассировкой сторожа живой
сети: она назвала оба за один прогон каждый.


---

## РЕЦИДИВ 2026-08-16 — «ноль» был замером ОДНОГО теста, а не набора

Предыдущий виток закрыл карточку строкой «**0** отказов» — и это правда, но правда про
`test_nav_conservation_property`. Замер по набору говорит другое: на фиксированном срезе
дверей (143 файла, тот же срез до и после) — **1726 отказов от 56 тестов в 11 файлах**.

Это не откат предыдущей работы: оба её источника (`base_gas_monitor`, `adapter_status_generator`)
по-прежнему закрыты и в журнале отказов не появляются ни разу. Просто «ноль в одном тесте»
никогда не был ответом на вопрос карточки, а был прочитан как он. Тот же класс, что уже
дважды описан в этой карточке: **проверка отвечает на СВОЙ вопрос и читается как ответ на нужный.**

### Метод: журнал сторожа, а не перебор

Подтверждено третий раз. Сторож живой сети уже пишет каждый отказ с URL
(`network_guard._ATTEMPTS` → `archive()` → отчёт `conftest`), но отчёт печатает только топ-20.
Полный журнал снят внешним плагином-наблюдателем (только читает `archived()`, ничего не меняет),
и вся таблица дверей получена за ОДИН прогон.

**Своя ошибка по дороге, записана намеренно:** первая версия плагина вела курсор по
`archived()` и потеряла 49 тестов из 56 — собственные тесты сторожа зовут `clear_archive()`,
список схлопывается под курсором. Замер «7 записей при 56 по отчёту» и поймал это. Инструмент
измерения тоже требует положительного контроля.

### Двери (замер, срез 143 файла)

| дверь | отказов ДО | отказов ПОСЛЕ |
|---|---:|---:|
| DeFiLlama `yields.llama.fi/pools` + CoinGecko `/coins/markets` | 1190 | 266 |
| перп-фандинг (Binance/Bybit/OKX/KuCoin/Hyperliquid) через `strategy_lab/data/_http` | 155 | **0** |
| спотовые ценовые сенсоры (coingecko/coinbase/kraken/binance) | 179 | 3 |
| Fluid API · Pendle API · ethena.fi · usual.money (личные двери адаптеров) | 176 | 176 |
| сырой сокет 8.8.8.8 (проба доступности, живой тест) | 2 | 2 |
| api.anthropic.com (LLM-агент) | 1 | 1 |
| **ИТОГО** | **1726 от 56 тестов** | **471 от 26 тестов** |

Время прогона среза: **734.75 с → 771.08 с** — в пределах шума машины (на ней параллельно
работали другие агенты, load average 7–20). Герметизация здесь покупает честность, а не скорость;
скорость уже была куплена сторожем в #93.

**Ни один ассерт не тронут:** `5194 passed, 199 skipped, 344 subtests, 0 failed` — побайтово
одинаково до и после.

### Что закрыто и ЧЕМ

Двери закрыты на швах, результат которых **тождествен** отказу сторожа, поэтому это не ослабление:

1. `spa_core.adapters.config.DEFILLAMA_ENABLED` — **существующий** переключатель;
   `_fetch_pools` для выключенного фида возвращает `None` ровно там же, где его возвращает ветка
   сетевого сбоя.
2. `spa_core.strategy_lab.data._http.OFFLINE` (новый, по умолчанию `False`) — поднимает ТОТ ЖЕ
   `FetchError`, которым и так кончался отказ, но без открытия сокета. Loopback остаётся открыт
   (локальный сервер — не живая сеть, та же линия, что у `network_guard`).
3. `spa_core.feeds.defi_llama_feed.ENABLED` (новый, по умолчанию `True`) — собственная
   документированная ветка «disabled» модуля. Читается **в момент вызова**, а не в конструкторе:
   у модуля есть процессный `_SINGLETON`, и решение конструктора заморозил бы первый же тест.

Всё сведено в `spa_core/tests/live_feed_doors.py` (один источник правды, грузится по абсолютному
пути обоими conftest — та же дисциплина, что у трёх существующих сторожей) + autouse-фикстура.

**Клиентов DeFiLlama оказалось ДВА.** Закрытие только `adapters/`-клиента оставило 616 отказов
из 823 через `feeds/`-клиента. «Дверь закрыта» — это замер, а не рассуждение.

### Метка для тестов, чей ПРЕДМЕТ — транспорт (п.3)

`@pytest.mark.live_feed_transport` (зарегистрирована в `pytest.ini`) оставляет двери открытыми.
Сеть при этом НЕ возвращается: `network_guard` не тронут и отказывает как раньше — метка
разрешает только ПОПЫТКУ. Помечены ровно те, кто покраснел на замере, и ни один их ассерт не
изменён:

* `test_no_live_network_in_tests.py::TestGuardStopsTheRealFeedChain` — сквозной пин самого сторожа;
* `test_defi_llama_feed.py` (весь модуль) — 36 тестов про TTL-кеш, retry/backoff, ротацию UA,
  CoinGecko-fallback и разбор тела.

Оба покраснели, а не стали пустыми — это и есть нужный сигнал.

### Тесты, которые были зелёными по НЕВЕРНОЙ причине (названы, а не спрятаны)

1. **`test_dfb_risk_overlay.py` / `test_dfb_month2_laneA.py` — «провод пег-фида» не проверен
   ни разу.** Для LST/LRT-пулов `risk_overlay._build_risk` зовёт `UnderlyingRiskFeed.risks()`,
   та тянет 5 перп-площадок, получает пустой ряд, поднимает `FeedError` — и `except → return None`
   даёт **UNKNOWN**. То есть ветка, которую комментарий в коде называет «THE PEG-FEED WIRE (the
   UNKNOWN-grading fix)», в тестах всегда идёт по аварийному выходу. Эти тесты пройдут и тогда,
   когда починка, ради которой они написаны, сломается.
2. **`test_agents_llm_and_routing.py::test_fallback_triggers_on_api_error`** — комментарий в самом
   тесте гласит «no network in sandbox → fallback». Тест зависит от ОКРУЖЕНИЯ, а не от инъекции
   ошибки: на машине с сетью и валидным ключом он позвал бы живой Anthropic API.
3. **`test_adapter_orchestrator.py::TestCompoundV3NoFabricatedAPY::test_live_status_is_passed_through`**
   и `test_adapter_status_schema_reader.py` (compound_v3) — брали живой TVL из DeFiLlama, получали
   отказ, и `tvl_source` не становился `"live"`. Ассерты TVL не покрывают вовсе (ADR-053 требует
   именно живого TVL), так что дыра не видна по зелёному.
4. **`test_rates_desk_feeds.py::_network_available`** — честный случай: проба 8.8.8.8:53 в обход
   сторожа, чтобы live-режимный тест СКИПНУЛСЯ без egress. Не дефект, но это значит, что часть
   этого файла на CI не исполняется никогда.

### Побочная находка: рецидив клоббера сторожа

`test_aave_v3.py::TestAaveV3Adapter::test_defillama_selectors` выбивает `network_guard` из цепочки
`urlopen` (плоское присваивание вместо оборачивания) — фикстура его восстанавливает и ГРОМКО
сообщает. Ровно тот класс, ради которого раздел «RECIDIV» в
`agent-network-guard-attempts-never-reset` и писался. Воспроизводится в обоих прогонах.

### Что осталось (НЕ big-bang, по одной двери)

**471 отказ — это не одна дверь, а дюжина личных.** «DeFiLlama» оказался не одной дверью, а
**четырнадцатью**: два общих клиента (закрыты) плюс ~12 адаптеров, каждый со своей константой
`https://yields.llama.fi/pools` и своим `urlopen` (`aave_v3_base`, `aerodrome_usdc`, `compound_v3`,
`dolomite_arbitrum_usdc`, `ethena_susde`, `extra_finance_base`, `fluid_usdc`,
`fluid_arbitrum_usdc`, `gmx_research`, `gold_proxy_research`, `usual_usd0pp`, `pendle_pt`).
Они достижимы из `dfb/pool_universe` через `ADAPTER_REGISTRY`.

Правильный следующий шаг — не общая фикстура (её здесь нет), а инъекция фида в конструктор
адаптера, по одному адаптеру за итерацию. Карточку НЕ закрываю: «ноль в одном тесте» уже один
раз был прочитан как «ноль в наборе».


---

## СВЕРКА 2026-08-17 — НЕ ЗАКРЫТА, остаток подтверждён СОБСТВЕННЫМ ЗАМЕРОМ

Не поверил числу «471», а померил в этом дереве. Отказы **остались**, поэтому
карточка остаётся открытой — и это верный ответ, а не недоработка сверки.

### Замер (шесть файлов `dfb`, одна и та же команда, отличается только состояние дверей)

| прогон | отказов | тестов | время |
|---|---:|---:|---|
| двери ЗАКРЫТЫ (как в проде набора) | **462** | 21 | 2.20 с |
| двери ОТКРЫТЫ (контроль) | **1635** | 34 | 2.21 с |

Контроль снят внешним плагином-наблюдателем, который только вешает
`@pytest.mark.live_feed_transport` на все собранные тесты; кода репозитория он не
меняет. Оба прогона — `91 passed`, побайтово одинаковый результат тестов.

Дословно, закрытые двери:

```
============= live-network refusals (guard held; no call went out) =============
462 refusal(s) from 21 test(s). Each one is production code under test reaching
for a live feed and getting a fail-CLOSED OSError.
      42  test_dfb_risk_overlay.py::test_universe_deterministic_and_sorted
      21  test_dfb_risk_overlay.py::test_universe_no_fabricated_cells
      21  test_dfb_risk_overlay.py::test_toxic_pool_refused_at_any_size[size0..4]
      …
      21  test_dfb_month2_laneA.py::test_breadth_flag_on_widens_universe
```

**Что это доказывает — и чего не доказывает.**

* Общие двери РАБОТАЮТ и это измерено, а не заявлено: 1635 → 462 на одном и том же
  срезе (−72 %). Заодно подтверждена перп-дверь: `test_dfb_alerts.py` в моём
  прогоне даёт **ноль** отказов и весь срез идёт 2.2 с против 152.93 с из витка #93.
* Но остаток **не «около нуля»**: 462 отказа даёт ОДИН срез из шести файлов —
  порядок величины совпадает с записанным в карточке «471 от 26 тестов» по всему
  срезу дверей. Личные двери ~12 адаптеров, названные в разделе «Что осталось»,
  закрыты **не были**: правильный шаг — инъекция фида в конструктор адаптера, по
  одному за итерацию.
* Проверено отдельно, что закрытые двери реальны, а не «тесты перестали ходить»:
  срез из 8 адаптерных файлов (`compound_v3`, `ethena_susde`, `fluid_fusdc`,
  `pendle_pt`, `aave_v3`, `adapter_orchestrator`) даёт **0 отказов**,
  `426 passed, 95 skipped`.

### Расхождение в трекере, которое надо развести

Карточка `agent-tests-do-live-network-io` в разделе «Волна 0 триажа, 16.08»
утверждает, что эта карточка «схлопнута сюда … **переведена в `done`**». На диске
её `status` — `backlog`, и по замеру выше он верен. Читать раздел волны 0 как
доказательство закрытия нельзя.

---

## ГЕЙТ ПОСТАВЛЕН 2026-08-18 — отчёт стал отказом (п.3 закрыт), остаток перемерен

### Перемер СВОИМ прогоном (числа вчерашней сверки подтверждены)

| прогон | отказов | тестов | время | доля набора |
|---|---:|---:|---|---|
| срез шести файлов `dfb` | **462** | 21 | 2.39 с | 6 файлов |
| срез 171 файла (всё, что импортирует `dfb`/`adapters`/`feeds`/`strategy_lab`/`orchestrator`) | **512** | 33 | 4 м 25 с | **171 из 1547 файлов (11.1 %), 4715 из 96 352 тестов (4.9 %)** |

Числа «462 / 21 / 2.2 с» из сверки 17.08 воспроизведены побайтово. Полный
`spa_core/tests/` не гонялся сознательно: одна **сборка** (`--collect-only`) — 58.6 с,
96 352 теста в 1547 файлах; полный прогон стоит часы. Это не оценка, а замер.

Разложение остатка по хостам (весь срез 171 файла, полный журнал сторожа, не топ-20):

```
267 yields.llama.fi          88 api.fluid.instadapp.io     23 api-v2.pendle.finance
 22 ethena.fi                22 app.ethena.fi              22 app.usual.money
 22 api.usual.money           7 rpc.ankr.com / eth.llamarpc.com / eth.drpc.org /
                               ethereum-rpc.publicnode.com / cloudflare-eth.com / 1rpc.io
  2 api.hyperliquid.xyz       2 сырой сокет 8.8.8.8 (живая проба, by design)
```

Личные двери ~12 адаптеров (раздел «Что осталось») **по-прежнему открыты** — трогать
`spa_core/adapters/**` в этой сессии было запрещено.

### Что закрыто: п.3 — ГЕЙТ, а не отчёт

`spa_core/tests/live_feed_gate.py` + ратчет `live_feed_refusal_baseline.json`, включён в
`conftest` в тердауне того же autouse-фикстуры, что ведёт журнал отказов. Тест, чей код
полез в живой фид, теперь **краснеет по имени**, а не попадает в баннер под зелёным прогоном.

Таблица решений (одна и та же для сетевой и временной половины):

| файл теста | вердикт при отказе |
|---|---|
| в `measured_files`, у nodeid есть потолок | до потолка — можно; **больше — КРАСНЫЙ** |
| в `measured_files`, потолка нет | потолок 0 → **КРАСНЫЙ** (новый тест в измеренном файле) |
| **НОВЫЙ**: untracked или добавлен после `cutoff_epoch` | потолок 0 → **КРАСНЫЙ** — это и есть «новый тест не ходит в сеть», и это правило действует на **весь набор** |
| старый и неизмеренный | **не гейтится** — только баннер |

Последняя строка — сознательная дыра, и она названа вслух в `coverage_note`, в баннере
каждого прогона и отдельным тестом. Причина ровно та, что записана в
`.claude/rules/deployment.md` про `frozen_date_ratchet`: дефолт-деним по 89 % набора,
который никто не измерял, покрасил бы main за пределами измеренного и **научил бы
отключать гейт**. Гейт fail-CLOSED там, где есть замер, и молчит там, где замера нет —
но молчит громко.

Опт-аут — существующая метка `@pytest.mark.live_feed_transport` (та же, что держит двери
открытыми). Сеть при этом не возвращается: сторож отказывает как раньше.

### Положительный контроль (обязательный, выполнен)

`spa_core/tests/test_live_feed_gate.py`, 27 тестов, 61 с. Сквозной контроль запускает
**настоящий pytest** подпроцессом на двух сгенерированных тестах через реальный conftest
(`-p spa_core.tests.conftest`, файлы в tmp — дерево остаётся чистым):

```
ERROR at teardown of test_reaches_a_live_feed
live-feed GATE: test_probe_net.py::test_reaches_a_live_feed made 1 live-network
attempt(s) and is not allowed any — its file is NEW (untracked or added after the
baseline). The attempt was REFUSED (nothing went out), and that refusal is exactly
the problem: a test whose green depends on a failed feed call passes when the thing
it means to check is broken.
2 passed, 1 error in 0.03s
```

Тест с инжектированным `FakeFeed` — зелёный, `live-feed GATE` в выводе нет.
Живой сети в контейнере нет (`curl` к `yields.llama.fi` даёт 000) — поэтому
«не ходит в сеть» доказывается **перехватом** (сторож записал попытку), а не
недоступностью сети.

**Мутационная проверка гейта (оба контроля краснеют):**

| мутация | результат |
|---|---|
| `verdict()` всегда `None` (гейт выключен) | **5 failed** — включая сквозной «сетевой» контроль |
| `verdict()` всегда возвращает сообщение | **10 failed** — включая «ноль отказов не нарушение» и метку |
| проводка в conftest срабатывает на каждом тесте | **контроль с `FakeFeed` покраснел** |

Третья мутация нужна отдельно: контроль с `FakeFeed` не даёт ни одного отказа, поэтому
`check()` для него не вызывается вовсе — мутация самой функции его не задевает. Записано,
чтобы следующая сессия не прочла «зелёный FakeFeed» как доказательство работы гейта.

### Что закрыто: п.1 — бюджет времени (профиль + порог с меткой)

Профиль `--durations=30` по срезу 171 файла: **240.95 с всего, 125.7 с (52 %) в ТРЁХ
тестах** (62.4 с + 34.8 с + 28.6 с, два файла). Это и есть рецидив «одна медленная зона
съедает цикл» — 0.06 % тестов съедают половину прогона, и увидеть это можно было только
запустив `--durations`.

Гейт (`duration_verdict`): не запрет медленного, а запрет **необъявленного** медленного.
`@pytest.mark.slow` (уже зарегистрирована в `pytest.ini`) объявляет законно длинный тест;
измеренный долгожитель держит свой записанный потолок; всё остальное — 30 с.

Замеренная поправка, из-за которой правило именно `max(budget, cap x3)`, а не `cap x3`:
тест, профилированный на 28.6 с, в СЛЕДУЮЩЕМ прогоне занял 34.1 с под нагрузкой соседних
агентов. Потолок, который **сужает** лимит, превратил бы нагрузку машины в красные тесты —
а гейт, красный по причинам, которых код не контролирует, отключают через неделю.
Допуск x3 при этом ловит тот класс регрессий, ради которого он есть: `test_dfb_alerts.py`
ходил 152.93 с и стал 2.2 с — здесь два порядка, а не двадцать процентов.

### Приёмка (ни один ассерт не тронут)

| прогон | до гейта | с гейтом |
|---|---|---|
| срез 171 файла | `4715 passed, 218 skipped, 98 subtests` за 240.95 с | **то же самое**, exit 0, за 245.26 с |
| 90 случайных НЕизмеренных файлов + 6 файлов сторожей | — | 0 упоминаний `GATE`; 17 падений — предсуществующие (`ModuleNotFoundError: argon2`), к гейту отношения не имеют |

### Что осталось открытым (карточка НЕ закрыта)

1. **Личные двери ~12 адаптеров — 512 отказов на измеренном срезе.** Шаг прежний:
   инъекция фида в конструктор адаптера, по одному за итерацию. В этой сессии
   `spa_core/adapters/**` был вне доступа.
2. **89 % набора не измерены** (1376 файлов, 91 637 тестов). Они гейтятся только правилом
   «новый файл», старые неизмеренные отказы молчат. Расширение — это замер срезами, каждый
   срез добавляет файлы в `measured_files`; ратчет разрешает только рост охвата и только
   снижение потолков.
3. **Корень `tests/`** (соседний) своей per-test фикстуры журнала не имеет, поэтому гейт
   туда не достаёт. Отдельная работа.
4. Профиль времени снят только по измеренному срезу; про остальные 89 % набора о времени
   не известно ничего.
