---
trackerStatus:
  type: agent-task
title: "Список сетевого стража копится за весь прогон — краснит охранный тест только в полной сюите"
status: done
source: orchestrator
created: 2026-08-04
priority: high
claimed_by: cycle-55404
claimed_at: 2026-08-04T19:49:06Z
---

## Симптом

**Это ЕДИНСТВЕННЫЙ красный тест в CI на `main` прямо сейчас** — то есть весь гейт «SPA Tests»
красный из-за него одного. Прогон Actions на `a7f1ca922` (шаг «Run spa_core unit tests»,
py3.11): `1 failed, 91242 passed, 581 skipped, 1 error` —

```
FAILED spa_core/tests/test_no_live_network_in_tests.py::TestGuardIsInstalled::
       test_telegram_guard_stays_outermost
       - AssertionError: Lists differ: ['urlopen https://yields.llama.fi/pools', …] != []
```

Красные и четыре последних прогона `test.yml` на `main` (04.08, 09:35–09:46Z).

Воспроизводится локально в ПОЛНОМ прогоне `spa_core/tests/` (+ ошибка того же теста на
teardown). **В одиночку файл зелёный** — 15 passed, поэтому одиночный прогон дефект НЕ ловит.
Цикл #111 (04.08) снял контроль на чистом `origin/main`: **побайтово тот же результат**
(`1 failed, 53319 passed, 212 skipped, 1 error`, тот же тест, та же точка ~58 %), то есть
дефект предсуществующий и от правок цикла не зависит. Порядок тестов детерминирован
(`pytest-randomly` не установлен, CI гоняет с `-p no:randomly`) — это не флаки.

Побочный эффект: под `-x` сюита встаёт на 58 %, и оставшиеся 42 % не проверяются вообще.

## Корень

Тест утверждает, что телеграм-вызов принадлежит `telegram_guard`, а не сетевому стражу:

```python
self.assertEqual(_live_net.attempts(), [],
                 "the network guard swallowed a Telegram call that telegram_guard must own")
```

Но у **телеграм**-стража есть autouse-фикстура, которая делает `reset()` перед каждым тестом
(`spa_core/tests/conftest.py`), а у **сетевого** — нет. Его `_ATTEMPTS` копится за весь прогон:
к этому месту там **3897 записей** от предыдущих тестов (`urlopen https://yields.llama.fi/pools`
и т. п.). Список не пуст ⇒ ассерт падает.

Дальше — вторая половина: падение происходит ДО строки

```python
_live_tg.reset()   # «этот тест намеренно роняет телеграм-стража, съедаем запись»
```

поэтому намеренная запись остаётся, и autouse-фикстура добивает тест ошибкой на teardown
(`LiveTelegramSendAttempted: 1 live Telegram API call(s)`). **Одна причина — два симптома.**

Опасность класса уже описана в этом же файле, в докстринге соседнего класса
`TestGuardRefusesLiveNetwork`: «a refusal lands in the *live* `_ATTEMPTS` list — which then
reddens an unrelated later test». Тот случай изолировали копией модуля; общий счётчик остался.

## Чего делать НЕЛЬЗЯ

Ослаблять/скипать/сужать ассерт (инвариант #16). Он ловит настоящую вещь — порядок установки
стражей, который защищает владельца от повторения 31.07.

## Что предлагается

Симметрия с телеграм-стражем: autouse-`reset()` для сетевого стража между тестами (тогда ассерт
меряет ТОЛЬКО свой тест, как и задумано), либо сравнение не с `[]`, а со срезом, снятым в начале
теста. Первый вариант выглядит правильнее — он чинит класс, а не одно место, и заодно снимает
скрытую утечку записей между тестами.

Приёмка обязана включать **полный** прогон (в одиночку файл зелёный и до починки, поэтому
одиночный прогон ничего не доказывает) и мутацию «переставить стражей местами» — она должна
краснить тест и до, и после починки.

## Как понять, что готово

Полный `spa_core/tests/` проходит место `test_no_live_network_in_tests.py` без падения, а
подмена порядка установки стражей по-прежнему краснит `test_telegram_guard_stays_outermost`.

---

## ЗАКРЫТО циклом #115 (2026-08-04)

**Развилка карточки решена ЗАМЕРОМ, а не вкусом.** Первым делом — ответ на первый критерий
приёмки («КТО делает эти попытки, список, а не „много разных“»). Измерено **до единой правки
в репозитории**: плагин `PYTEST_PLUGINS` вне дерева, который делает `reset()` после каждого теста
и складывает попытки по `nodeid`. На чистом `origin/main` `d07714d07`:

```
2745 попыток от 102 тестов
хосты: yields.llama.fi 2153 · api.coingecko.com 183 · api.fluid.instadapp.io 88 ·
       fapi.binance.com 31 · api.bybit.com 31 · www.okx.com 31 · api-futures.kucoin.com 31 ·
       api.hyperliquid.xyz 31 · api-v2.pendle.finance 22 · ethena.fi 22 · app.ethena.fi 22 ·
       api.usual.money 22
верхние тесты: test_dfb_risk_overlay::test_universe_deterministic_and_sorted 140 ·
       test_new_endpoints_fuzz::test_corrupt_source_never_5xx 99 ·
       test_new_endpoints_fuzz::test_corrupt_captured_book_no_fabricated_carry 80 ·
       test_strategy_lab_backtest::test_determinism_two_runs_identical 74 · далее ~70 на тест
       в test_dfb_month2_laneA / test_dfb_risk_overlay
```

**Все 2745 — это ОТКАЗЫ, а не утечки:** страж поднял `OSError`, наружу не ушёл ни один вызов.
Значит вторая ветка развилки («утверждение глобальное ⇒ 2745 настоящих нарушений») не
подтвердилась: чинить нечего, а утверждение ассерта («страж не съел **мой** телеграм-вызов»)
всегда было локальным. Локальной сделана область его действия.

**Тот же прогон стал доказательством механизма ДО правки репозитория:** с per-test reset
(снаружи, плагином) полный срез дал `1 failed, 91280 passed` — целевой тест зелёный, ошибки на
teardown нет; осталось одно, постороннее падение.

### Что сделано

1. `spa_core/tests/conftest.py` — autouse-фикстура `_scope_network_guard_ledger`: `reset()` до
   теста, `archive(nodeid)` после. Симметрия с телеграм-стражем, но **намеренно НЕ гейт**: отказ
   в сети — это страж, работающий как задумано, и падать на нём значило бы покрасить 102
   ни в чём не виноватых теста.
2. `spa_core/tests/network_guard.py` — `archive()` / `archived()` / `clear_archive()`. Обнулять
   журнал молча было бы ровно классом #29 — «замер, которого никто не видит»: попытки не
   выбрасываются, а переезжают в архив с привязкой к тесту.
3. `conftest.pytest_terminal_summary` — отчёт в конце прогона: сколько отказов, от скольких
   тестов, топ-20 поимённо и **прямо сказано, сколько тестов не показано** (никаких молчаливых
   усечений). Раньше эти 2745 не читал никто.
4. `test_no_live_network_in_tests.py` — вторая половина дефекта (`ERROR at teardown`): намеренная
   телеграм-запись съедается через `addCleanup`, зарегистрированный ДО попытки, а не хвостовым
   оператором, который при падении ассерта не выполнялся.

### Приёмка

| прогон | результат |
|---|---|
| контроль, чистый `origin/main` d07714d07 | **2 failed, 91279 passed, 576 skipped, 1 error** |
| моё дерево, полный срез | **1 failed, 91293 passed, 576 skipped, 0 errors** |

Дельта passed = **ровно +14** = 13 новых тестов + сам целевой тест, ставший зелёным.
`error` 1 → 0. Оставшееся падение — **постороннее и предсуществующее**
(`test_protocol_insurance_scorer::test_missing_tvl_usd`, воспроизводится в одиночку на чистом
origin) → отдельная карточка `agent-insurance-scorer-fabricates-missing-tvl`.

**Минимальный детерминированный репро вместо 8-минутного прогона** (нашёлся при разборе):
`pytest spa_core/tests/test_dfb_alerts.py spa_core/tests/test_no_live_network_in_tests.py` —
0.42 с, оба симптома. До: `1 failed, 33 passed, 1 error`. После: `34 passed`.

**Мутации (положительные контроли), каждая прогоном:**

* удалить фикстуру целиком → краснеют **5** тестов, включая исходный
  `test_telegram_guard_stays_outermost` (дефект возвращается дословно); откат → зелено;
* переставить стражей местами (сетевой становится внешним) → краснеет **ровно**
  `test_telegram_guard_stays_outermost`, и **до фикса тоже** (проверено на отдельном чистом
  дереве origin: `1 failed, 14 passed`) ⇒ пин порядка установки стражей НЕ ослаблен;
* убрать только предварительный `reset()`, оставив `archive()` → **не краснеет**, и это
  записано честно: чистит именно `archive()`, предварительный `reset()` — подстраховка от
  попыток, сделанных вне тела теста. Мутация, которая ничего не ловит, — не контроль.

Прочее: `mypy_gate` 10 passed · `lint_llm_forbidden` 168 файлов / 0 нарушений ·
`test_ci_test_exclusions` 18 passed · pyflakes на всех изменённых файлах — чисто.

**Инвариант #16:** ни один существующий ассерт не удалён и не ослаблен — в диффе нет ни одной
удалённой строки с `assert`. Единственная правка чужого теста — перенос уже существовавшего
`_live_tg.reset()` в `addCleanup`, что делает уборку строже (работает и при падении), а не мягче.

**Не трогал:** RiskPolicy / kill-switch / пороги / гейты риска · живой трек · launchd / деплой ·
`landing/**` · `spa_core/analytics/**` · `data/**` не публиковал.

Дубль этой карточки — `agent-telegram-guard-outermost-fails-only-in-full-run` (та же причина,
описана с другой стороны), закрыт тем же изменением.
