---
trackerStatus:
  type: inbox
title: Сторож недоставляемых зависимостей краснеет на docs/research/cio_g4_repro.py — второй нарушитель за первым
status: new
source: orchestrator
created: 2026-08-20
---

## Что случилось

Сторож `test_unsynced_hard_imports` краснеет на **`docs/research/cio_g4_repro.py`**: файл лежит
ВНЕ каталогов автосинка (`spa_core/`, `scripts/`, `tests/`, `architecture/`) и на верхнем уровне
жёстко импортирует `spa_core.allocator.allocator` и `spa_core.paper_trading.risk_gate`.

Это тот же класс, что авария 17.08 с корневым `__init__.py`: в прод такой файл не приедет
**ни одним механизмом**, а значит его зависимость молча протухнет вместе с рефакторингом —
и заметит это не сторож, а следующая сессия, у которой перестанет собираться набор тестов.

**Замер (цикл #310):** падение **воспроизводится на чистом `origin/main eee01de42`**, к правкам
#310 отношения не имеет. Появилось вместе с доставкой research CIO этап-1 (`891ad092e` / `9b432abf3`).
Раньше сторож это скрывал не по доброте: он краснел на корневом `__init__.py`, и второй нарушитель
стоял за ним в очереди. Убрали первого — сразу стал виден второй. Сторож работает правильно.

## Что сделать

Разрешено ровно три исхода (сам сторож их перечисляет, и **дописывать исключение в него —
запрещено**, инв. #16):

1. **Убрать жёсткую зависимость** — импортировать `spa_core.*` внутри функции/`__main__`, а не на
   верхнем уровне. Для repro-скрипта это естественно и ничего не ломает. **Рекомендация.**
2. Перенести файл в `scripts/` (тогда он доставляется) — но это research-артефакт, не рантайм.
3. Снять файл, если repro больше не нужен (замер 19.08: **G4 не воспроизводится**).

## Критерий готовности

`python3 -m pytest spa_core/tests/test_unsynced_hard_imports.py -q` — зелено, и в базу сторожа
**ничего не дописано**.

## Почему не сделано сразу

ADR-088 (решение владельца 19.08): базовый протокол, одна безопасная задача за цикл. Цикл #310
израсходован на разбор очереди и два решения владельца. Падение пред-существующее и не ухудшает
приёмку: контроль и рабочее дерево дают одинаковые 3 failed / 108 909 passed.
