---
trackerStatus:
  type: inbox
title: Весь поддомен checkup.earn-defi.com отдаёт 404 — с главной публично ведут битые ссылки, и job site-freshness красный всегда
status: done
source: nimbalyst
created: 2026-08-14
claimed_by: cycle-15316
claimed_at: 2026-08-16T12:25:49Z
---

## Что измерено (цикл #228, 14.08)

Разбирая спам в Телеграм, прочитал логи облачного сторожа `site-freshness` — и увидел, что
этот прогон **краснеет каждые 6 часов** не из-за свежести, а из-за второй проверки в том же
job'е, `funnel_link_check`: путь конверсии с главной ведёт в 404.

Из лога прогона 167 (14.08 07:47Z), 8 страниц воронки, 54 уникальные ссылки:

```
[404] https://checkup.earn-defi.com/sample-report/   (со страницы https://earn-defi.com/)
[404] https://checkup.earn-defi.com/check/           (со страницы https://earn-defi.com/)
[404] https://checkup.earn-defi.com/                 (FUNNEL_PAGE)
[404] https://checkup.earn-defi.com/check            (CRITICAL_ROUTE)
```

404 отдаёт **весь поддомен** `checkup.earn-defi.com`, а не отдельная страница. То есть с
главной публично ведут ссылки в никуда, и ведут не первый день (тот же отказ виден в прогонах
159–167, дальше не смотрел).

## Почему это отдельная находка, а не хвост

Пока воронка красит job каждый прогон, **красный job перестаёт быть сигналом**: он красный
всегда. А это ровно тот второй канал, на который ADR-YL-011 опирается, когда тревогу в чат
слать нельзя (см. цикл #228 — маршрут недоставки таблички честности теперь уходит именно в
красный job). Сначала чинится воронка, иначе канал остаётся забит.

## Что сделать

1. Понять, жив ли сервис `checkup` (Railway) вообще: он деплоился отдельно, DNS на
   `checkup.earn-defi.com` отвечает, но все маршруты — 404.
2. Если сервис мёртв и поднимать его сейчас не время (владелец заморозил продуктовый слой в
   пользу доходности) — **честно убрать ссылки с главной**, а не оставлять публичный 404.
   Класс правки безопасный (не числа, не тиры, не legal), уезжает через `safe_site_push.py`.
3. Проверка: `python3 scripts/funnel_link_check.py` = exit 0, job `site-freshness` краснеет
   только по делу.

Смежное: карточка `agent-checkup-waitlist-fail-open-ok-true`, ключ Etherscan для прода —
`own-06-etherscan-prod-key`.

---

## Закрыто циклом #260 (2026-08-16, worktree от `origin/main` `de281d8a4`)

**Пункт 1 карточки («жив ли сервис вообще») — измерен, а не предположен.** Домен отвечает, но
отвечает не приложение: `curl -I https://checkup.earn-defi.com/check` → `HTTP/2 404`,
`server: railway-hikari`, **`x-railway-fallback: true`**. Это ответ площадки «к этому адресу не
привязан никакой сервис» — то есть 404 отдаёт не сломанный маршрут внутри приложения, а
отсутствие приложения. Поднять его без доступов владельца (Railway, плюс ключ Etherscan для
прода — `own-06`) нельзя, а продуктовый слой владельцем заморожен ⇒ **пункт 2 карточки**:
честно убрать ссылки.

**Замер до правки** (живой прогон, ровно 4 находки карточки): 8 страниц воронки, 54 уникальные
ссылки, битые — `checkup/sample-report/`, `checkup/check/` (обе с главной), `checkup/` как
FUNNEL_PAGE, `checkup/check` как CRITICAL_ROUTE.

**Что сделано.** Убраны ВСЕ 7 ссылок на снятый домен в `landing/`, каждая — на живую страницу:
| было | стало |
|---|---|
| геро-виджет «вставьте адрес» (`WalletCheck`) → `checkup/check?address=` | вход на живой `/snapshot` (200), виджет и его CSS/JS удалены |
| «Посмотреть пример анализа» (геро + нижний CTA) → `checkup/sample-report` | якорь `#sample-report` на этой же странице — иллюстративный отчёт лежал там всегда |
| шапка сайта, группа «Чекап»: 2 пункта на мёртвый домен | `/snapshot` + `#sample-report` |
| страница 404 → `checkup/check` | `/track-record` (404 больше не отправляет во второй 404) |
| `/snapshot` → «настоящий, более глубокий скан» | обещание снято, следующий шаг — живой трек |
| шелл дашборда + топбар SPA → `checkup/check` | `/snapshot` |

**Сторож (пункт 3 карточки).** `scripts/funnel_link_check.py`: снятый домен больше не объявлен
частью воронки — иначе проверка красная навсегда, а всегда-красная проверка перестаёт быть
сигналом (это и есть исходная жалоба). Но молчания это не купило: явный `DECOMMISSIONED_HOSTS`,
и ЛЮБАЯ ссылка на такой хост — находка **по построению**, с названной причиной вместо голого
кода и с прежним кодом возврата 1. Есть и второй сторож, по ИСХОДНИКАМ сайта: тест
`test_no_source_file_under_landing_links_to_a_decommissioned_host` краснеет ещё до публикации.

**Приёмка.** +12 тестов, 12 passed. Контроль на чистом `origin/main` того же sha: 7 красных /
5 зелёных, все 5 зелёных — объявленные обратные контроли (обычный 404, soft-404, сетевой
транзиент, недоступная страница воронки). 4 мутации, каждая краснит свою цель. `npm run build` —
119 страниц, 0 ошибок. `check_owner_gate.py` — **CLEAN** (ни одного числа не тронуто; в геро-строке
правлено только предложение-CTA, цифры байт-в-байт прежние).

**Осталось владельцу (не молчу, но и не делаю сам):** главная целыми секциями рекламирует
бесплатный чекап, у которого больше нет ни одной двери — карточка
`owner-decision-glavnaya-reklamiruet-besplatnyi-chekap-k` (+ notify).

**Сверх карточки (названо, чтобы не выглядело как самовольство).** Кроме 404-ссылок нашлись ТРИ
призыва «Analyze Wallet / Проверить кошелёк» (шапка ×2 на каждой странице + финальный блок главной
с дословным «вставьте публичный адрес кошелька»), ведущие на якорь `#analyze` — туда, где после
снятия виджета вставлять НЕКУДА. Это не 404, но исполнить такое указание физически нельзя ⇒
дефект, а не копирайт: уведены на `/snapshot`. Секции, которые ОПИСЫВАЮТ чекап, не тронуты —
это владельцу.
