---
trackerStatus:
  type: inbox
title: "Замер обмена «доходность ↔ концентрация» на T2 (20/25/30%) — решение владельца A, задачи не было"
status: new
source: nimbalyst
created: 2026-08-08
---

## Что случилось и почему это важно

Владелец 08.08 18:33Z ответил на карточку `own-rnd-duty-is-concentration-adr055` — **вариант A**:
признать, что главная ручка агрессивного тира — концентрация, и честно измерить её саму.
Карточка переведена в `ingested` коммитом `163046c18`. Но **задачи из решения никто не завёл** —
ни одной карточки, ни строки в KANBAN. Протокол §3.4 требует обратного: решение → задача с
критериями приёмки. Пока этого нет, решение владельца живёт только в теле закрытой карточки и
в тексте коммита, то есть в воздухе.

Решение распадается ровно на две работы; эта карточка — первая.

## Что сделать

**Advisory-замер обмена «доходность ↔ концентрация» на T2.**

- Та же реальная история 10 книг, на которой считался `duty_collapse` (реестр
  `docs/DYNAMIC_LEVERAGE_GUARDIAN.md`, записи #39–#43) — новых данных не заводить.
- Уровни на одно имя: **20 / 25 / 30 %** (20 % — сегодняшний лимит T2, опорная точка). Уровни
  взяты из примера в самой карточке; допущение зафиксировано ингестирующей сессией явно.
- **Хвост показывать РЯДОМ с доходностью обязательно** (просадка/tail) — правило сайта и
  инвариант честности: цифра доходности без хвоста в этом семействе бессмысленна, потому что
  прибавка и есть плата за концентрацию.
- Результат — таблица «уровень → доходность → хвост → оборот», карточкой владельцу.

**Границы (не нарушать):** `RiskPolicy v1.0` не меняется ни строкой; замер **advisory**, гейтом
не становится и капитал не двигает; изменение порога 20 % возможно ТОЛЬКО отдельным ADR и ТОЛЬКО
отдельным решением владельца. Живой трек не трогать, прогон — в песочнице.

## Как понять, что готово

Таблица 20/25/30 % с хвостом рядом с доходностью лежит в реестре и подана владельцу карточкой;
в коде нет ни одной новой ветки, влияющей на исполнение.

## Что будет после

Впервые появится замер цены нашего собственного лимита концентрации — вместо семи итераций
перебора признака, который сам замер назвал сноской. Дальше вопрос порога становится
предметным, но остаётся решением владельца.

_Источник: цикл #169, шаг 2 (инжест решений владельца) — decomposition решения A по
`own-rnd-duty-is-concentration-adr055`. Вторая половина решения — карточка про логирование
концентрации и duty в paper-модулях #39/#36._
