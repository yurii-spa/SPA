---
trackerStatus:
  type: inbox
title: Храповик литеральных дат снова КРАСНЫЙ на main — test_owner_order_starvation.py (приехал 26.08)
status: done
source: nimbalyst
created: 2026-08-27
status_trail:
  - "2026-08-28T23:34:18.937753+00:00 backlog -> done · queue.set_status · cycle-30331"
---

## Что случилось и почему это важно

Проверка «храповик литеральных дат» (`test_frozen_date_ratchet.py`) **красная на чистом
`origin/main`** (замер на закреплённом sha `96229c648`, 2026-08-27). Причина — файл
`spa_core/tests/test_owner_order_starvation.py`, приехавший на main 26.08 коммитом `7076dc69d`:
он закрепляет литеральную дату рядом с понятием свежести и потому вошёл в закрытый класс,
который храповик разрешает только СОКРАЩАТЬ.

Это не новая поломка кода — это сторож, честно краснеющий на верное состояние. Но пока он
красный, он **перестаёт защищать**: следующий такой же файл уже не будет отличим от этого,
и весь набор постепенно приучается к «ну оно и так красное».

Найдено попутно R&D-сессией 27.08 (реестр #81/#82). **Молча не чинилось и не скипалось**
(инвариант #16): файл принадлежит другой сессии, и правка чужого теста без разбора его
замысла — ровно то, что правило запрещает.

## Что от тебя нужно

Ничего от владельца — это задача агента. Взявшему: НЕ добавлять файл в
`frozen_date_baseline.json` (база только уменьшается — это прямо запрещено правилом).
Починить фикстуру одним из двух способов из `.claude/rules/deployment.md`:
1. **время входом** — передать `now=` И фиксированные отметки от того же якоря, затем
   пометить `# FROZEN-DATE-OK: injected-clock — <как>`;
2. **относительные отметки** — `spa_core/tests/_freshness.py::ts(hours_ago=N)`.

Если окажется, что дата ЯВЛЯЕТСЯ предметом проверки (разбор строки, golden-файл,
исторический инцидент) — пометить файл `# FROZEN-DATE-OK: <причина>`; голая пометка без
причины храповиком отвергается.

## Как понять, что готово

`python3 -m pytest spa_core/tests/test_frozen_date_ratchet.py -q` зелёный на чистом
`origin/main`, и `test_owner_order_starvation.py` по-прежнему проверяет ровно то, что
проверял (обратный контроль: снятая починка возвращает красноту).

## Что будет после

Храповик снова отличает новый нарушающий файл от старого, и класс продолжает
только сокращаться.

---

## ➕ Тот же коммит красит ВТОРОЙ храповик (замер цикла #392, 27.08)

Контрольный прогон на закреплённом sha `48c26e30f` (чистое `origin/main`, отдельное дерево)
даёт **два** падения, а не одно, и оба с одной доставки `7076dc69d`:

| проверка | падение |
|---|---|
| `test_frozen_date_ratchet.py::test_no_new_file_joins_the_frozen_date_class` | `test_owner_order_starvation.py` (предмет этой карточки) |
| `test_unwired_scripts_ratchet.py::TestRatchet::test_no_NEW_unwired_script_appears` | `check_owner_order_starvation` — новый скрипт с точкой входа, которого никто не зовёт |

Отдельной карточки не завожу намеренно: это одна доставка, один автор и одно окно починки —
второй документ про тот же коммит породил бы дубль. Но взявшему эту карточку **обязательно**
чинить обе половины: подключить `scripts/check_owner_order_starvation.py` (plist / обёртка /
вызов из цикла) либо, если он запускается руками, объяснить это здесь — и **НЕ добавлять его в
`unwired_baseline`**, ровно по той же причине, по которой нельзя трогать `frozen_date_baseline`.

Пока обе красные, `main` красный по двум сторожам сразу, и оба перестают защищать: следующий
такой же файл и следующий такой же скрипт станут неотличимы от этих.

_Замер цикла #392, попутно к ADR-150; своя работа цикла к этим падениям отношения не имеет —
набор падений на моём дереве и на чистом origin совпадает пофамильно._

---

## Сделано циклом #418 (29.08) — поднято из осиротевшего захвата, ДВА экземпляра

Карточка держалась захватом мёртвой сессии `rnd-edge-2026-08-27` (pid3045, 44.8 ч).
Подъём — по протоколу §3.4, со СВОЕЙ сверкой, а не по отчёту умершей сессии.

**Экземпляр 1 (`test_owner_order_starvation.py`) — ДОСТАВЛЕН, работа не потеряна.**
Проверено на `origin/main`: в файле стоит `# FROZEN-DATE-OK: injected-clock — …`
с причиной (строка 19), и файла НЕТ в `frozen_date_baseline.json` (276 записей).
То есть чинили фикстуру, а базу не двигали — ровно так, как карточка и требовала.

**Экземпляр 2 (`test_live_data_write_attribution.py`) — почин в этом цикле.**
Храповик на ЧИСТОМ `origin/main` 936822758 (контрольное дерево на закреплённом sha)
краснел уже на другом файле — он приехал 28.08 с циклом #417, меньше чем через
двое суток после первого. Литерал: улика-приманка

    {"updated_at": "2026-08-04T08:30:45", "count": 26}

Дата к предмету не относится ВООБЩЕ: сторож смотрит, ИЗМЕНИЛСЯ ли файл, а не что в
нём. Заменена относительной отметкой `ts(hours_ago=1)` (приём #2 правила), форма
настоящего артефакта сохранена — улика обязана быть похожа на то, что живёт в
`data/`. Ни один `assert` не тронут, база не тронута, скипов нет.

Прогоны: `test_frozen_date_ratchet.py` 23 passed (было 1 failed на чистом origin —
и это положительный контроль: сторож краснел на ВЕРНОЕ состояние);
`test_live_data_write_attribution.py` + два соседа по классу — 65 passed.

## Что этот второй экземпляр говорит о классе

Два файла за двое суток — это не невезение, а цена того, что храповик живёт ТОЛЬКО
в полном прогоне. Автор узнаёт о попадании в класс уже после того, как его работа на
`main`, и чинит её следующая сессия. Класс лечится не третьей починкой, а тем, чтобы
храповик спрашивали ДО пуша; предмет — уже не эта карточка.
