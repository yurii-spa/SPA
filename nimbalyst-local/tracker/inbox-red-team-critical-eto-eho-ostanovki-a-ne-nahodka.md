---
trackerStatus:
  type: inbox
title: "«red_team: CRITICAL» — это ЭХО нашей же остановки, а не находка разведки; причина нигде не названа"
status: done
source: cycle-195
created: 2026-08-10
tags: [investment-os, house-view-gap, adr-066, honesty]
claimed_by: cycle-18880
claimed_at: 2026-08-10T19:26:04Z
---

## Что измерено (цикл #195, шаг 0-офис)

Шаг 0-офис печатает:

> `[WARN] аналитик red_team: CRITICAL — требует реакции (карточка/решение), не пролистывания`

А сам отчёт аналитика (`data/investment_os/red_team.json`, 09:11:55Z) говорит ровно обратное:

| поле | значение |
|---|---|
| `threat_posture.clear` | **true** |
| `threat_posture.n_threats` | **0** |
| `attack_surface.critical_count` | **0** |
| `threat_posture.kill_switch_already_active` | **true** |

Причина CRITICAL — единственная: `spa_core/investment_os/agents/red_team.py:74`
`if kill_active or (critical_count > 0): posture = "CRITICAL"`. То есть разведка **не
наблюдала ничего**; она отражает факт, что мы сами остановлены — а этот факт уже известен,
уже назван в STATE и уже имеет карточку владельцу с кнопками.

## Почему это не «мелочь оформления»

Читатель получает слово `CRITICAL` от РАЗВЕДКИ и справедливо понимает его как «нашли врага».
Ни постура, ни сообщение моста не несут ПРИЧИНЫ, поэтому отличить «противник в периметре» от
«мы сами стоим» можно только открыв файл. Это ровно тот класс, на котором проект спотыкается
серией: сторож честно отвечает на СВОЙ вопрос, а читают его как ответ на нужный. Пока
выключатель активен, эта строка будет гореть каждый цикл — и приучит пролистывать именно её.

## Что предлагаю (обсуждаемо)

1. `red_team` пишет `posture_reason` — список причин (`kill_switch_active` ·
   `attack_surface_critical` · `threats_present`). **Лестницу НЕ трогать**: CRITICAL остаётся
   CRITICAL, ослабления нет — добавляется только имя причины.
2. `house_view_gap.analyst_red` переносит `posture_reason` в текст находки. Тогда
   «требует реакции» перестаёт звучать вслепую.
3. Проверить, не задваивает ли мост карточку с уже существующим вопросом владельца об
   остановке (сейчас находка висит в `waiting_hysteresis`, карточки ещё нет).

## Что НЕ делаю сейчас

Карточку-дубль по самой находке не завожу: мост `findings_bridge` уже держит
`gap:analyst_red:red_team` и заведёт её сам. Здесь — то, чего мост не увидит: **почему**
находка красная и что её текст об этом молчит.

---

## Исполнено — цикл #198 (2026-08-10)

Пункты 1 и 2 предложения сделаны, лестница НЕ тронута:

- `spa_core/investment_os/agents/red_team.py` — аналитик пишет **`posture_reason`**: список
  машинных кодов причин (`kill_switch_already_active` · `attack_surface_critical` ·
  `threats_present` · `threat_data_inconclusive` · `threat_data_missing_or_stale`). Причины
  собираются **до** лестницы, поэтому названы ВСЕ вкладчики, а не первый совпавший: иначе
  «остановка» затмила бы настоящую находку симуляции атак. `CRITICAL` остаётся `CRITICAL`,
  ослабления нет. Осторожные ветки (`UNKNOWN_CAUTIOUS`) тоже обязаны сказать почему.
- `spa_core/monitoring/house_view_gap.py` — сверка переносит причину в ТЕКСТ находки, и делает
  это для **любого** аналитика, а не только для `red_team` (сторож обязан быть шире подопечного,
  урок #197). Незнакомый код причины проходит ВЕРБАТИМ — аналитик волен назвать причину, о
  которой сверка ещё не знает. Аналитик покраснел и промолчал ⇒ это ГОВОРИТСЯ вслух
  («причина НЕ НАЗВАНА аналитиком»), а не опускается.
- **Ключ находки `gap:analyst_red:red_team` НЕ менялся** — пункт 3 предложения: смена ключа
  завела бы мостом вторую карточку на ту же находку рядом с уже существующей
  `inbox-nahodka-petli-analitik-red-team-critical`.

Живой эффект на проде (прогон read-only, без записи):

```
[WARN] аналитик red_team: CRITICAL (причина НЕ НАЗВАНА аналитиком) — требует реакции …
```

— честно: артефакт `red_team.json` в проде от 09:11Z, он старше починки. Как только аналитик
отработает заново, в строке встанет либо названная причина, либо (после снятия остановки)
находка исчезнет сама и мост закроет карточку.

Тесты: +6 в `test_red_team_agent.py`, +6 в `test_findings_bridge.py`, все — положительные
контроли, краснеют на модуле без починки. Восемь мутаций проверены поимённо (с очисткой
`__pycache__`).

Карточку закрываю: `done`.
