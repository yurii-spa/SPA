---
trackerStatus:
  type: agent-task
title: "checkpoint-7day не проходит гейт деплоя: он выходит с кодом 1, когда НАХОДИТ проблему, а не когда сломан"
status: done
source: session-2026-08-08-autopilot
created: 2026-08-08
priority: medium
tags: [agents, deployment, exit-codes, own-31]
---

## Что случилось

Владелец 2026-08-08 разрешил поставить четыре готовых агента (карточка
`own-31-desyat-agentov-v-reestre-bez-flota`, ответ «да / да»). Три пошли, **`checkpoint-7day`
гейт не пропустил**:

```
--- manual run exit=1 ---
❌ FAIL: manual run exited 1 (expected 0). NOT loading com.spa.checkpoint-7day.
```

**Гейт прав по своим правилам, и агент прав по своим — но правила разные.**
Прямой прогон `scripts/checkpoint_7day.py` показывает, что код 1 — это ВЕРДИКТ, а не поломка:

```
Days tracked:   50/30      Kill-switch: ✅ NOT triggered     GoLive: ✅ PASS
--- FAILURES ---
  ❌ gap_check: Gap in paper_evidence: 2026-06-21 → 2026-06-30 (9 days)
```

Отчёт построен полностью и корректно. Ненулевой код означает «в проверках есть красное».

## Почему нельзя просто поставить

Под launchd такой агент будет **вечно** числиться `last_exit=1`, и `agent_health` будет ровно
так же вечно писать WARN — то есть мы обменяем «агент не работает» на «агент всегда красный».
Это шум, который через неделю перестанут читать, и он замаскирует настоящую поломку.

Гасить гейт запрещено правилом доставки: «если сторож краснеет на верное состояние — чинить
сторожа (и закреплять тестом в обе стороны), а не отключать».

## Варианты

1. **Развести коды возврата (рекомендую).** Агент выходит `0`, если ОТЧЁТ ПОСТРОЕН, а красные
   проверки передаёт содержимым отчёта и уровнем алерта. Ненулевой код оставить за настоящим
   сбоем (не смог прочитать данные, не смог записать). Это приводит агента к общей конвенции
   флота, где `last_exit` означает работоспособность, а не вердикт.
2. Научить гейт различать «вердикт» и «сбой» по списку агентов-репортёров. Хуже: список нужно
   поддерживать, и он молча устареет.
3. Не ставить агента вовсе. Тогда недельная контрольная точка так и не заработает.

## Отдельно найдено (не относится к деплою, но найдено этим прогоном)

Отчёт называет **настоящий 9-дневный разрыв в `paper_evidence` (2026-06-21 → 2026-06-30)**.
Он существует независимо от того, поставим мы агента или нет, и заслуживает собственного
разбора: это трек.

**Примечание для протокола:** диагностический прогон скрипта ОТПРАВИЛ отчёт в Telegram владельцу
(скрипт шлёт при каждом запуске). Не авария, но владелец увидит внеплановое сообщение — записано,
чтобы оно не выглядело загадкой.

## Сделано 2026-08-17 (сессия #272)

Выбран **вариант 1** — развести коды возврата (он же рекомендованный):
`scripts/checkpoint_7day.py` → `EXIT_OK=0` (отчёт ПОСТРОЕН) · `EXIT_BROKEN=2` (построить не
удалось: нечего читать, упало посреди счёта) · `EXIT_VERDICT_FAIL=1` — только по явному
`--verdict-exit-code`. Вердикт НЕ ослаблен: блок FAILURES печатается, алерт через `push_policy`
уходит, а ноль при красных проверках объявляется вслух («отчёт построен», а НЕ «всё зелено»).

**Вариант 2 отклонён вместе с карточкой:** список доверенных агентов-репортёров в гейте молча
устаревает и стал бы дырой в fail-CLOSED гейте. Гейт `scripts/check_agent_before_deploy.sh`
по-прежнему отказывает на ненулевом коде — изменён только текст отказа: он НАЗЫВАЕТ обе конвенции
(«код = работоспособность» против «код = вердикт») и указывает на рабочий пример.

`check_agent_before_deploy.sh` на долгожителях НЕ запускался (правило доставки). `checkpoint-7day` —
расписанный агент: `KeepAlive` в его plist отсутствует, и это закреплено тестом через
`agent_static_probe.sh --plist-bool`, который читает ЗНАЧЕНИЕ, а не наличие ключа.

Тесты: `spa_core/tests/test_checkpoint_agent_exit_code.py` (15 проверок, положительный контроль —
версия из HEAD даёт `1` там, где требуется `0`, и `1` на отсутствующем каталоге данных, где
требуется `2`; контроли в обе стороны: гейт по-прежнему НЕ пропускает агента с кодом 2).
Намеренные правки трёх утверждений в `tests/test_checkpoint_7day.py` — обоснование в теле правок,
запись в `docs/journal/2026-W34.md` (инвариант #16).

**Осталось владельцу:** поставить агента (`launchctl bootstrap`) — гейт он теперь проходит.
Отдельно замечено, но НЕ тронуто: `StartCalendarInterval` в
`scripts/com.spa.checkpoint-7day.plist` = Month 6 / Day 19, то есть агент срабатывает раз в год,
а карточка и код говорят про НЕДЕЛЬНУЮ проверку. Расписание — вопрос деплоя, решение владельца.
