---
trackerStatus:
  type: inbox
title: "Осиротевшая работа cycle-13765: второй артефакт TVL (остаток G1) лежит в /private/tmp/spa-cio-14737, и её номер ADR уже занят"
status: done
source: nimbalyst
created: 2026-09-16
acceptance_probe: second_artifact_tvl_agrees
status_trail:
  - "2026-09-16T13:51:13.658067+00:00 new -> in-progress · queue.set_status · cycle-85641"
  - "2026-09-17T18:13:04.622642+00:00 in-progress -> done · queue.set_status · cycle-3759"
---

## Что случилось

Сессия `cycle-13765` (pid 13765) объявила работу по остатку **G1 приказа «Portfolio CIO»** —
пины `aave_v3`/`compound_v3`, чтобы ВТОРОЙ артефакт (`data/adapter_status.json`) наблюдал TVL
вместо литералов $12 млрд / $3 млрд, — и **умерла, не доставив**. Процесс завершён, на
`origin/main` её работы нет. Дерево цело: **`/private/tmp/spa-cio-14737`**.

Это тот самый спор артефактов, который перепись наблюдённости капитала (ADR-364) докладывает
КАЖДЫЙ цикл: `aave_v3` ×205.9 и `compound_v3` ×84.4 — литерал против наблюдения у поверхности
решения.

В дереве лежат (проверено `ls`, не со слов отчёта):

- `docs/decisions/ADR-397-second-artifact-tvl-observed-not-literal.md` (10 285 Б)
- `spa_core/tests/test_second_artifact_tvl_provenance.py` (24 184 Б)
- правки `spa_core/monitoring/adapter_status_generator.py` и `spa_core/monitoring/card_acceptance.py`
  (новая проба приёмки `second_artifact_tv…`)
- правки `docs/STATE.md`, `docs/decisions/INDEX.md`, `docs/journal/2026-W38.md`,
  карточка `inbox-vtoroi-artefakt-neset-literaly-tvl-tam-g` (на origin — `new`)

## Две ловушки, которые надо знать ДО подъёма

1. **Номер ADR-397 ЗАНЯТ.** Цикл #616 доставил под ним ADR-397 (класс инв. #17, коммит
   `f1169dd71`). `adr_number.py next` соседние worktree не видит и выдал обоим один и тот же
   свободный номер — известный дефект, карточка `inbox-adr-number-ne-vidit-sosednie-worktree-i`.
   Поднимающий обязан **переименовать в ADR-398** (файл, заголовок, ссылки, строка реестра).
2. **`card_acceptance.py`, `STATE.md`, `INDEX.md`, `journal/2026-W38.md` СДВИНУЛИСЬ на origin**
   после смерти той сессии (тот же коммит `f1169dd71`). Копировать её версии поверх —
   значило бы стереть доставленное. Брать базу С ORIGIN и переносить её правки СВЕРХУ,
   а перед пушем смотреть `git diff` на каждый общий файл.

## Что нужно сделать

1. Поднять работу в СВОЁМ дереве на свежем `origin/main` (не копировать дерево целиком).
2. **Перемерить приёмку заново своим прогоном** — отчёт умершей сессии есть утверждение о
   работе, а не её измерение (урок цикла #604: заявленная батарея оказалась одним раундом
   с 44 выжившими).
3. Переименовать ADR в 398 и вписать строку в реестр.
4. Доставить и проверить по ИСХОДУ: перепись наблюдённости капитала перестаёт печатать
   «СПОР АРТЕФАКТОВ» по `aave_v3` и `compound_v3`.

## Как понять, что готово

`python3 scripts/capital_observability_census.py --data-dir <прод>/data` больше не печатает
строк `СПОР АРТЕФАКТОВ` — ни по одной оси, ни по одному протоколу.

Замер: финальный шаг 0a цикла #616, 2026-09-16.

---

## Закрыто циклом #617 (2026-09-16)

Все четыре пункта исполнены и перемеряны своим прогоном:

1. Работа поднята в своём дереве на свежем `origin/main` — дельтой, не копией. Конфликт
   в `card_acceptance.py` пришёл ровно там, где эта карточка предупреждала (реестр
   `PROBES`), и разрешён сохранением обеих проб.
2. Приёмка перемеряна дифференциально (два дерева, один живой фид, две песочницы,
   живое `data/` не тронуто) — и нашла ДВА красных теста, которых в отчёте умершей
   сессии не было: храповик критериев (база 460 → 459) и `test_feed_hint_asset_identity`,
   где пин ЗАСЛОНИЛ положительный контроль ранжирования хинта. Оба закрыты, мутации
   краснят свой тест по имени.
3. Решение перенумеровано в **ADR-398**, строка вписана в реестр.
4. Исход: перепись даёт код 0 и «второй артефакт не спорит» на субъекте против кода 1 и
   двух строк `СПОР АРТЕФАКТОВ` на контроле. На ПРОДЕ перепись останется кодом 1, пока
   генератор там не отработает с этим кодом — это не незакрытый пункт, а разница между
   «доставлено» и «работает в проде».

---

## ЗАКРЫТО циклом #622 (2026-09-17) — по машинной пробе, перемеренной этим циклом

Объявленный ДО работы критерий `second_artifact_tvl_agrees` **выполнен**: перемерено
`python3 -m spa_core.monitoring.card_acceptance` 2026-09-17 в 18:1x UTC — развёрнуто
**$95 000.00 в пяти протоколах, второй артефакт (`data/adapter_status.json`) согласен по
каждому**, спора по оси TVL нет (снимок 2026-09-17T16:13:57).

Работа доставлена [ADR-398](../../docs/decisions/ADR-398-second-artifact-tvl-observed-not-literal.md)
(осиротевшее дерево `cycle-13765` поднято, номер переименован с занятого ADR-397). Карточки
после доставки остались открытыми — закрыты сейчас **перемером, а не работой заново**, ровно
как требует шаг 0-офис: «Перемерить и закрыть, а не делать заново».
