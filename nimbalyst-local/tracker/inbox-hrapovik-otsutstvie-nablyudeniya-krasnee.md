---
trackerStatus:
  type: inbox
title: Храповик «отсутствие наблюдения» краснеет на СДВИГЕ строк и зовёт это новым местом класса
status: done
source: nimbalyst
created: 2026-08-24
claimed_by: cycle-3447
claimed_at: 2026-08-25T05:41:55Z
status_trail:
  - "2026-08-25T05:51:18.324282+00:00 new -> done · queue.set_status · cycle-3326"
---

## Что случилось и почему это важно

Цикл #374 добавил 14 строк в начало `spa_core/monitoring/agent_health_monitor.py` (новая
категория расписания). Полный прогон покраснел на `test_absent_observation_ratchet.py`:

```
сигнал or_falsy: НОВЫЕ места класса «отсутствие наблюдения = благополучие» (1):
['spa_core/monitoring/agent_health_monitor.py:1170']
```

Никакого нового места не появилось. База (`spa_core/tests/absent_observation_baseline.json`)
хранит члена класса как `файл:НОМЕР СТРОКИ`, и запись `:1156` просто уехала на `:1170` —
код в этой строке побайтово тот же, что на `HEAD`. То есть **храповик краснеет на любой
вставке строк ВЫШЕ любого своего члена**, независимо от того, что вставили.

Опасность не в самом падении, а в том, к чему оно подталкивает. Сообщение говорит «НОВЫЕ
места класса», база в шапке говорит «дописывать сюда ЗАПРЕЩЕНО (инв. #16)». Сессия, которая
поверит сообщению, увидит запрет на единственное действие, которое выглядит подходящим, — и
дальше либо погасит проверку, либо потратит цикл на диагностику уже разобранного. В #374 это
стоило отдельного разбора; повторяться будет у каждого, кто вставит строку в один из 247
файлов-членов.

То же устройство у `spa_core/tests/unwired_scripts_baseline.json` (тоже якорь по номеру
строки). Остальные базы (`frozen_date`, `live_data_write`, `adr_duplicate`, `design_status`)
номерами строк не якорятся и этой болезнью не болеют.

## Что нужно сделать

1. **Якорить члена не номером строки, а содержимым** — нормализованный текст выражения
   (или его хэш) + файл. Тогда сдвиг строк ничего не значит, а настоящее НОВОЕ место
   по-прежнему ловится. Номер строки можно оставить как подсказку для человека, но НЕ как
   ключ сравнения.
2. Если п.1 дорого — минимум: **научить сообщение отличать сдвиг от появления.** Если тот же
   текст выражения уже есть в базе под другим номером в том же файле, это ПЕРЕЯКОРЕНИЕ:
   печатать «член сдвинулся 1156 → 1170, обнови координату» вместо «НОВЫЕ места класса».
3. Дописать в шапку базы, что перенос координаты существующего члена (число мест НЕ меняется)
   — не «дописывание» и запретом инв. #16 не покрыт.

## Как понять, что готово

Вставка пустой строки в начало любого файла-члена НЕ красит храповик, а добавление настоящего
нового `d.get(...) or 0` — красит (контроль в обе стороны).

## Что будет после

Храповик перестаёт срабатывать на факт редактирования файла и снова означает ровно то, что
написано на его двери, — «в дереве появился новый член класса».

---

## Разбор — цикл #378 (2026-08-25, сессия `cycle-3447`, worktree `/tmp/spa_c378` от `84fa23031`)

**Сделан п.1 (якорь по содержимому) — п.2 не понадобился, п.3 сделан, второй адрес ОТМЕНЁН замером.**

* **п.1 · якорь.** Ключ сравнения — `файл + отпечаток выражения (sha256 от ast.unparse, 12 знаков)
  + порядковый среди одинаковых`. Номер строки остался в записи подсказкой человеку и в ключ НЕ
  входит. Переякорение — команда `python3 -m spa_core.tests._absent_observation --write`, а не
  ручная правка 247 строк.
* **п.2 (различать сдвиг от появления в СООБЩЕНИИ) отпал:** после п.1 сдвиг перестал быть
  событием вообще — различать в тексте нечего.
* **п.3 · шапка базы** дополнена: перенос координаты существующего члена — не «дописывание» и
  запретом инв. #16 не покрыт.

**Критерий приёмки карточки выполнен буквально, на РЕАЛЬНОЙ аварии:** 14 пустых строк в начало
`agent_health_monitor.py` (дословно авария #374) → 20 passed; та же вставка со снятой починкой →
то самое ложное «НОВЫЕ места класса». Настоящий новый `... or 0.0` красит три теста.

**Сверх карточки — обратная половина дефекта.** Сдвиг умел не только выдумывать находку, но и
ПРЯТАТЬ её: съехавший член попадал на координату соседа, уже лежащую в базе (замер на
`apy_aggregator.py`: 329→332 при живом 332 ⇒ ложно-новых 1 вместо 2).

**Находка: класс был НЕДОСЧИТАН на 5.** Старый ключ склеивал членов на ОДНОЙ строке (вложенные
`or` — это два члена). Потолок `or_falsy` 187 → 192 **намеренно** (инв. #16: обоснование в теле
изменения + журнал W35); это не разрешение на пять мест, и проверяется отдельно — множество
координат дерева совпало со старой базой 187 в 187. Старое число не выброшено: живёт вторым
потолком `COORDINATE_CEILINGS` в своей единице, обе двери заперты порознь.

**Второй адрес карточки ОТМЕНЁН ЗАМЕРОМ, а не исполнен.** Утверждение «то же устройство у
`unwired_scripts_baseline.json` (тоже якорь по номеру строки)» неверно: оба его раздела якорятся
ИМЕНЕМ скрипта, координат ноль. Перемерены все десять баз храповиков — координата как ключ была
ровно в одной, в `absent_observation`. После цикла таких баз не осталось.

Файлы: `spa_core/tests/_absent_observation.py` · `spa_core/tests/test_absent_observation_ratchet.py`
(+8 тестов, 12→20) · `spa_core/tests/absent_observation_baseline.json` (переякорена).
