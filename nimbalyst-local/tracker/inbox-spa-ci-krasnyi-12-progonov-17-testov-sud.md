---
trackerStatus:
  type: inbox
title: "SPA CI красный 12 прогонов: 17 тестов судят о ХОСТЕ (нет launchctl на Linux, cwd=spa_core), не о коде"
status: done
source: nimbalyst
created: 2026-08-13
---

## Что измерено (цикл #221, 2026-08-13)

Воркфлоу **`SPA CI`** на `main` красный подряд как минимум 12 прогонов (11:13Z→19:38Z),
**17 падений** — и это ДРУГАЯ причина, не та, что чинилась в цикле #221 (`SPA Tests`,
непереносимый `stat`, закрыто).

Измеренная разница сред: этот job гоняет тесты не от корня репозитория, а
`cd spa_core && python -m pytest tests/` (`.github/workflows/ci.yml`). Плюс раннер — Linux,
а не macOS. Падения делятся на два семейства:

**(1) `launchctl` нет на Linux — 12 падений**, все в
`spa_core/tests/test_deploy_gate_long_lived.py`: `FileNotFoundError: 'launchctl'`. Тесты приехали
циклом #185 (статический пробник агентов). Локально на Маке они зелёные, потому что `launchctl`
здесь есть — то есть тест проверяет ХОСТ, а не поведение кода.

**(2) относительные пути ломаются под `cd spa_core` — 5 падений:**
- `test_sleeve_seeding_lock.py::test_live_modules_use_this_condition` — `FileNotFoundError:
  'spa_core/paper_trading/hy_cycle.py'` (путь от корня, а cwd — `spa_core/`);
- `test_owner_gate_approval_scope.py` (2 теста) — scope прочитан как
  `['spa_core/landing/src/pages/packages.astro']` вместо `['landing/…']`;
- `test_telegram_flood_shared.py::test_the_rate_limit_state_lives_in_the_live_tree` —
  ожидает `<repo>/data`, получает `<repo>/spa_core/data`;
- `test_deploy_gate_long_lived.py::…::test_telegram_bot_target_module_resolves`.

Что здесь ГИПОТЕЗА, а не замер: связь семейства (2) именно с `cd spa_core` выведена из текста
ошибок и текста workflow, отдельным прогоном в этот цикл НЕ перепроверялась. Диагноз в карточке —
гипотеза, пока его не измерили (урок #175).

## Что сделать

1. Перепроверить (2) прогоном `cd spa_core && python -m pytest tests/<файл>` на Маке — это
   воспроизводимо локально, Linux для него не нужен.
2. Для каждого падения назвать, кто неправ: **тест**, который судит о хосте/cwd, или **код**,
   который зависит от cwd. Если код читает пути от cwd — это находка прода (агенты стартуют из
   разных каталогов), а не удобство теста.
3. `launchctl` НЕ обходить скипом (инв. #16): поведение обязано проверяться и на Linux —
   подставлять заглушку на PATH, а не выключать проверку. Скип = потеря 12 проверок гейта деплоя,
   который и так недавно ловил настоящую аварию.
4. Закрепить положительным контролем в обе стороны.

## Границы

Прод-дерево, права и деплой агентов не трогать (`.claude/rules/deployment.md`). Пороги RiskPolicy,
kill-switch и живой трек — не сюда.

Родитель: `inbox-test-agent-template-code-sync-krasneet-t` (цикл #221).

---

## Уточнение по факту (цикл #221, вечер) — класс шире, чем выглядел

После починки `SPA Tests` (коммит `09d99443`, шаг «Run integration tests» стал зелёным) **тот же
воркфлоу `SPA Tests` продолжил краснеть на СЛЕДУЮЩЕМ шаге** — «Run spa_core unit tests», уже от
корня репозитория: **13 падений, 12 из них — `FileNotFoundError: 'launchctl'`** в
`spa_core/tests/test_deploy_gate_long_lived.py`, плюс
`TestLivePlistsTakeTheSafePath::test_telegram_bot_target_module_resolves`, который сверяется с
хост-путём `/Users/yuriikulieshov/Documents/SPA_Claude/...`.

**Что это меняет.** Семейство (1) — не особенность `cd spa_core`, как можно было подумать: оно
краснеет и от корня. Тесты просто требуют macOS-`launchctl` и хост-дерево. А ранний замер «в
`SPA Tests` всего 2 падения» был неполон по механической причине: **шаги job'а идут
последовательно, и упавший первый шаг не давал второму запуститься вовсе.** Починка первого шага
не «сломала» ничего — она открыла то, что и так было.

Поэтому починка этой карточки красит зелёным **оба** воркфлоу, а не один.

---

## Закрыто циклом #223 (2026-08-14)

Разобрано пофамильно по логам прогонов `31754118917` / `31754118930` на `main` `5763dc5c`.

**Кто был неправ — по каждому падению:**

| падение | виноват | почему |
|---|---|---|
| 12× `launchctl` | тест | страховочная уборка launchd в `tearDown`; на Linux падала ДО утверждений |
| `telegram_bot_target_module_resolves` | **пробник** | `--targets` читает обёртку по абсолютному хост-пути и в чужом дереве молча отчитывается «не python» |
| `owner_gate_approval_scope` (2) | **прод** | `_rel` домножал уже repo-relative путь на cwd ⇒ scope одобрения не совпадал ни с чем |
| `telegram_flood_shared` | **прод** | `live_root(None)` падал на `Path.cwd()` вместо дерева модуля |
| `sleeve_seeding_lock` | тест | боевой модуль открывался путём от cwd |

**Пункт 3 карточки (не обходить скипом) исполнен буквально:** ни один тест не выключен, не
сужен и не помечен skip. `launchctl` при этом заглушкой на PATH НЕ подменялся — замер показал,
что launchd в этих тестах вообще ничего не проверяет: он только УБИРАЕТ за фикстурой, и на
Linux убирать нечего по построению. Заглушка изобразила бы уборку задания, которого не может
существовать. Критерий назван платформой и закреплён тестом в обе стороны.

**Вхолостую проходившая проверка найдена по дороге:** «у каждого долгожителя разрешается цель»
на Linux не срабатывала НИ РАЗУ (нет обёртки ⇒ `python_agent: 0` ⇒ условие ложно). Теперь
`entrypoint_source` называет `missing` вслух, а тест требует непустого улова.

Правки: `scripts/agent_static_probe.sh` (только режим `--targets`, `--probe` не тронут),
`scripts/safe_site_push.py`, `spa_core/utils/live_paths.py`, 4 тест-файла (+10 тестов). Предписанный прогон: **107608 passed / 0 failed / 624 skipped** (было 107598 на origin — ровно +10, моих).
