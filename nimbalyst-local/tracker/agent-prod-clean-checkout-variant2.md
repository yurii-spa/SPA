---
trackerStatus:
  type: agent
title: Вариант 2 — перевести прод на чистый чекаут origin/main (закрыть дрейф навсегда)
status: in-progress
source: session-2026-08-03-sync-gap-followup
created: 2026-08-03
priority: high
domain: инфраструктура/деплой; запуск СРАЗУ после сверки цикла 2026-08-04
---

> **Приоритет поднят 2026-08-03:** после починки цепочки доставки дрейф прод↔origin возвращается
> после КАЖДОЙ доставки (2026-08-03 — дважды за день). До этой карточки кто-то обязан
> пересинхронизировать прод перед каждым 06:00 UTC, иначе цикл считает не принятый код.
> Предусловие: сверка большого ребаланса 04.08 (agent-cycle-reconciliation-2026-08-04) —
> не менять две вещи одновременно.

## Что случилось и почему это важно

Прод-дерево ~/Documents/SPA_Claude живёт на ветке env-setup-v3 БЕЗ общего предка с origin/main
(след пересборки после утечки PAT). Вариант 1 (файловый синк кода) — одноразовый: origin
двигается каждым циклом, дрейф возвращается за часы. Нужно радикальное решение: флот исполняет
чистый чекаут origin/main, `data/` общий.

## План (черновик аудитора, согласован с CLI-сессией)

1. Чистый чекаут origin/main рядом (например ~/Documents/SPA_prod), `data/` — общий
   (симлинк/bind), логи в /tmp/ (не ~/Documents — TCC/exit-78).
2. Полный гейт ДО переключения: pytest весь, pre_cutover_gate 17/17, d5.import.adapters,
   deployment_drift = OK против нового пути.
3. Пилот: перевести 1 некритичный агент через scripts/check_agent_before_deploy.sh, проверить
   launchd/TCC на новом пути (симлинки на macOS — отдельно протестировать, история exit-78).
4. Перевод флота пачками ≤3 через deploy-гейт, МЕЖДУ циклами; daily_cycle — последним, сразу
   после успешного 06:00-цикла (окно ~24ч). Обновить run_daily_paper_cycle.sh и все wrapper'ы
   (cd на новый путь).
5. Сохранить воркфлоу владельца: правки руками + API-push должны продолжать работать
   (либо владелец правит в новом чекауте, либо старое дерево остаётся песочницей владельца —
   решить с владельцем при переключении).
6. Старое дерево — заморозить как резерв (не удалять без владельца).
7. Автоматический пост-гейт: deployment_drift + d5.import.adapters в конце каждой доставки.

## ⚠️ Обязательное предусловие (урок 04.08, цикл не отработал в 06:00)

**На origin 67 из 69 скриптов агентов лежат как 100644 (неисполняемые)** — exec-бит выставлялся
в проде руками и через API-пуши в git не попадает. rsync/чекаут из origin применяет эти режимы
→ launchd exit 126, ВЕСЬ флот мёртв при любом перезапуске (04.08 так и легли orchestrator/
autopush/daily_cycle; цикл догнан в 11:05 вручную после chmod +x 67 файлов). Перед переводом
агентов на чистый чекаут: ПОЧИНИТЬ РЕЖИМЫ НА ORIGIN (git update-index --chmod=+x, правильный
путь) либо шаг chmod +x в процедуре чекаута; проверка исполнимости — часть гейта пилота.

## Ограничения

- Синхронизировать/чекаутить только каталогами целиком, не файлами (урок 2026-08-03:
  точечный файл сломал импорт пакета из-за новой зависимости).
- ≤3 агента за раз, каждый через гейт; трек не останавливать; data/ не копировать и не двигать.
- git reset --hard в старом дереве ЗАПРЕЩЁН (клоббер трека, прецедент).

## Исполнение (аудитор, 2026-08-04, owner full-approve)

**Реализовано как in-place автосинк вместо переезда флота** (решение аудитора: та же цель —
«доставлено = исполняется» — при нуле простоя и нуле перевода агентов; полный чекаут-переезд
остаётся опцией, если владелец захочет позже):
- `scripts/code_sync_from_origin.sh`: whole-dir синк кода, снапшот+откат при провале
  import-пробы, exec-bit safety net, статус `data/code_sync_status.json`;
- Step 0 в `run_daily_paper_cycle.sh` — синк ПЕРЕД каждым 06:00 (доставлено 7ccf0d23);
- `core.fileMode false` в прод-репо (74 mode-only файла создавали вечный псевдодрейф);
- проверено: SYNCED 5→75 файлов на живых прогонах, затем стабильный IN_SYNC/0.
- **Урок:** синк клоббернул незапушенные правки самой сессии (восстановлены из снапшота).
  Правило для интерактивных сессий в прод-дереве: СНАЧАЛА пуш, ПОТОМ синк; снапшоты
  /tmp/spa_code_presync_*.tgz — страховка.

**Остаток:**
1. Переякорить ветку прода на main — 3 команды у владельца (классификатор среды не дал
   выполнить из сессии): `git branch backup-env-setup-v3-20260804` · `git branch -f main
   origin/main && git symbolic-ref HEAD refs/heads/main` · `git reset` (mixed; рабочие
   файлы НЕ трогаются). После этого drift-сторож перестанет ругаться на ветку.
2. Наблюдение: deployment_drift money-path 0 и code_sync IN_SYNC/SYNCED ≥3 дня подряд.

## Как понять, что готово

Флот целиком исполняет чекаут origin/main; deployment_drift стабильно OK ≥3 дня подряд без
ручных синков; цикл и книга сходятся; воркфлоу владельца работает.

## Что будет после

«Доставлено на origin» = «работает в проде» становится тождеством; ежедневный 90%-аудит
меряет реальную систему; ручные пересинхронизации отменяются.

## Замер #199 (2026-08-10, не захват карточки — только факт в копилку)

Третий подтверждённый потребитель дрейфа, теперь на очереди вопросов ВЛАДЕЛЬЦУ:
`owner_decision_pending` (шаг 0-офис + `agent_health_monitor`) читает трекер рядом с
`data/`, то есть ХОСТ-дерево, куда карточки не синкаются (автосинк возит только
`spa_core/`·`scripts/`·`tests/`). В момент замера: `origin/main` — **5** карточек
`needs-owner`, хост — **4** (`own-34-kill-switch-active-13h-unnoticed` в проде нет вовсе).

Сам сторож с #199 честен относительно той очереди, которую видит, и вдобавок называет
`card_missing:*` для четырёх карточек, отправленных владельцу и отсутствующих в хост-дереве, —
то есть дрейф теперь ВИДЕН в пульсе флота. Но пока прод не на чекауте `origin/main`, число
«вопросов, ждущих владельца» в проде остаётся заниженным по построению. Родственный случай
того же класса: `inbox-prod-storozh-arhitektury-chitaet-fail-ko`.
