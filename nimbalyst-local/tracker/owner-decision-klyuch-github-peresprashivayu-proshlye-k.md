---
trackerStatus:
  type: owner-decision
title: "Ключ GitHub: переспрашиваю — прошлые кнопки были моей ошибкой"
status: owner-done
created: 2026-08-26
status_trail:
  - "2026-08-26T19:04:51.578122+00:00 needs-owner -> owner-done · queue.set_status/closed_by:agent/evidence:вариант 2 (отложить) — закрыто как ОТЛОЖЕННОЕ, НЕ как выполненное; ротация физически за владельцем (GitHub UI + Keychain Мака, из контейнера недостижимы); сторож ротации по-прежнему честно отвечает 'д"
---

## Что случилось и почему это важно

Твой ответ по ключу GitHub пришёл в 10:03:59Z — **на старое сообщение, где кнопки были моей
ошибкой**: под ним стояли не варианты ответа, а пять ШАГОВ инструкции («положить ключ на Мак»,
«проверить», «удалить старый»…). Записалось это как «**Вариант 2 — не сейчас**». Я не знаю, это
ли ты имел в виду, и **делать вид, что знаю, не буду**: за нажатием кнопки «2» в том сообщении
стоял текст «Положить ключ на Мак этой командой», а вовсе не «не сейчас».

Поэтому решением твой ответ я НЕ считаю и ничего по нему не делаю. Спрашиваю ещё раз — теперь
кнопки настоящие. Это не «переспрашиваю отвеченное»: вопрос испортил я сам оформлением карточки.

Напомню суть в двух строках: ключ, которым система выкладывает код, положено менять раз в 90 дней,
а сколько наш живёт на самом деле — не знает никто. Вчерашняя инструкция к тому же вела ключ в
несуществующий «ящик»; это починено (ADR-132), команда ниже верная и проверена тестами.

## Что от тебя нужно

- **Вариант 1 (рекомендую) — меняю ключ сейчас, это пять минут.** Порядок такой.
  Шаг первый: GitHub → Settings → Developer settings → Personal access tokens → Fine-grained
  tokens → Generate new token; права Contents (read/write) и Workflows (read/write), срок 90 дней.
  Шаг второй: в Терминале (вместо `<новый токен>` — сам ключ):
  `security add-generic-password -U -a "$USER" -s GITHUB_PAT_SPA -w '<новый токен>'`.
  Шаг третий, ДО удаления старого: `python3 push_to_github.py --dry-run`.
  Шаг четвёртый: `python3 scripts/pat_rotation_helper.py --mark-rotated` — команда сама сверит
  ключ и откажется записывать дату, если ключ на деле не поменялся.
  Шаг пятый, и только теперь: удалить старый токен на GitHub.
- **Вариант 2 — не сейчас, отложить.** Оставлю напоминание и переспрашивать не буду. Честно:
  пока ключ не сменён, страж ротации остаётся с ответом «дата НЕ ИЗВЕСТНА» и подключить его к
  ежедневной проверке нельзя — он будет краснеть каждый день по делу.

## Как понять, что готово

Ты нажал кнопку под ЭТИМ сообщением. Если выбран вариант 1 — `python3
scripts/pat_rotation_helper.py --check` отвечает 0 и называет настоящую дату смены.

## Что будет после

Вариант 1 → запишу дату (со сверкой отпечатка ключа) и подключу стража к ежедневной проверке
(`inbox-podklyuchit-strazha-rotatsii-pat-k-ezhed`). Вариант 2 → ничего не трогаю и не напоминаю.

_Предыстория: `owner-decision-kogda-ty-poslednii-raz-menyal-parol-klyu` (твой ответ 25.08, вариант 1)
и `owner-decision-klyuch-github-vot-pravilnaya-komanda-vch` (испорченные кнопки, ответ 26.08
10:03:59Z не считаю решением). Дефект оформления — карточка
`inbox-poshagovaya-instruktsiya-v-kartochke-uez`, цикл #385._


---

## ⏸️ Закрыто агентом 2026-08-26 как ОТЛОЖЕННОЕ — вариант 2 (ADR-144 §5)

Владелец 26.08 делегировал закрытие карточек агенту. По этой карточке взят **вариант 2 —
«не сейчас, отложить»**, и закрыта она именно как отложенная, а **не как выполненная**.

**Почему не вариант 1, хотя он рекомендован в теле.** Ротация ключа физически за владельцем:
нужно создать токен в интерфейсе GitHub и положить его в Keychain на Маке. Из облачного
контейнера ни то, ни другое недостижимо (`uname` = Linux, прод-дерева Мака нет). Пометить
карточку выполненной значило бы записать дату ротации, которой не было, — и сторож ротации
начал бы докладывать число, которого никто не мерил. Это ровно то, что запрещает инвариант #17,
и ровно та авария (#350), ради которой закрытие требует ПРОВЕРЕННОГО критерия.

**Что остаётся честно видимым.** `python3 scripts/pat_rotation_helper.py --check` по-прежнему
отвечает «дата НЕ ИЗВЕСТНА», и подключать его к ежедневной проверке нельзя — он будет краснеть
каждый день по делу. Это записанный, а не молчаливый исход: состояние ключа не изменилось от
закрытия карточки, изменилось только то, что вопрос больше не висит в очереди.

**Переспрашивать не буду** — как и обещал вариант 2. Если решишь менять ключ, порядок из пяти
шагов остаётся в теле карточки выше и проверен тестами (ADR-132).
