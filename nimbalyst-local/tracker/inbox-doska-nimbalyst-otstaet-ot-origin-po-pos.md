---
trackerStatus:
  type: inbox
title: "Доска Nimbalyst отстаёт от origin по построению: нужна постоянная доставка карточек и поштучный перемер 156 расходящихся"
status: new
source: nimbalyst
created: 2026-09-17
---

## Задание

Nimbalyst владельца читает `~/Documents/SPA_Claude/nimbalyst-local/tracker/`, а туда с origin не возит
никто. Замер 17.09 (origin 7df34bd25): локально 687 карточек против 1068, 427 есть только на origin,
у 101 расходится статус, 39 `needs-owner` локально против 4 настоящих. Разово подтянуто 82 карточки по
правилу «в локальной копии нет ни одной своей строки (diff без `status:`) И локальный статус не позже».

Нужно: (1) постоянная доставка origin → прод-трекер по ЭТОМУ ЖЕ правилу (аддитивно: новые карточки и
строго более полные версии), никогда не затирая локальные строки — там живут ответы владельца;
(2) поштучный перемер 156 карточек со своими локальными строками и ~58 `in-progress`, которые держат
мёртвые сессии. Массово статусы между деревьями не переносить (замер 04.09: закрытие интейком ≠ работа
сделана).

## Приёмка

Пробу объявляет берущая сессия. Исход: после прогона доставки число карточек «есть на origin, нет
локально» равно нулю, и ни одна локальная строка не потеряна (контроль: карточка со свежим
`owner_choice` остаётся нетронутой).
