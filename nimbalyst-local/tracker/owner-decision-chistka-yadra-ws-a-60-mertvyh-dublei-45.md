---
trackerStatus:
  type: owner-decision
title: "Чистка ядра (WS-A): ~60 мёртвых/дублей + 45 аналитич. модулей раздувают paper_trading — greenlight подход"
status: needs-owner
source: audit
created: 2026-07-16
---

## Что случилось и почему это важно
Аудит файлов ядра (pass 1: adapters/risk/governance/paper_trading/execution — 211 модулей) нашёл ~60
мёртвых/дублей/неподключённых. Инвариант RiskPolicy подтверждён (единственный гейт). Крупнейшее: в
`paper_trading/` лежит **~45 аналитических модулей** (Sharpe/tail-risk/Monte-Carlo…), которые построены и
покрыты тестами, но НИКУДА не подключены — money-path пакет раздут вдвое. Плюс явно-мёртвые (адаптеры,
помеченные REMOVED) и дубли. Всё в карте `docs/SYSTEM_MAP.md` (WS-A).

## Что от тебя нужно
Greenlight на подход к чистке (ничего не удаляем — всё в `attic/`, обратимо):
1. **Явно-мёртвое** (0 импортов / помечены REMOVED: gmx_glp, radiant, config, l2_adapters и т.п.) — я
   перемещаю в `attic/` сам, аккуратным проходом с ПРОГОНОМ ТЕСТОВ (рекомендую, безопасно).
2. **~45 аналитических модулей** — развязать в отдельный `spa_core/analytics_lab/` (не удалять, вынести из
   money-path), ИЛИ подключить в один tear-sheet. Это крупный рефактор — нужно твоё «ок» на направление.
3. **Дубли** (compound_v3 ×2, defillama_feed ×2) + **3 реестра адаптеров → 1** — свести, отдельным проходом.

## Как понять, что готово
Ты сказал «ок» хотя бы по п.1 и направлению п.2 (analytics_lab vs подключить).

## Что будет после
П.1 делаю сразу (attic + зелёные тесты). П.2/3 — по твоему выбору направления, инкрементально, с тестами.
RiskPolicy/kill-switch/цикл не трогаю. Каждый шаг — в манифест attic (обратимо).
