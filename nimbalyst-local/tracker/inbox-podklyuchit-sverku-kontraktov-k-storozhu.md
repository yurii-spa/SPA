---
type: inbox
title: "Подключить сверку контрактов агентов к сторожу архитектуры — когда объявлений станет большинство"
status: new
created: 2026-08-28
priority: low
source: ADR-158
---

# Подключить сверку контрактов к сторожу архитектуры

`spa_core/monitoring/artifact_contract.py` (ADR-158) умеет отвечать, совпадает ли объявленный
контракт агента с тем, что он пишет. Сейчас модуль вызывается только тестами и вручную
(`python3 -m spa_core.monitoring.artifact_contract`).

**Почему не вшит сразу — осознанно.** Из 72 агентов 53 пока без объявления. Новый сигнал
такого объёма в `architecture_conformance` (он же Архитектор, ADR-066, раз в 6 часов) — это
поток находок владельцу в первый же прогон, и сторож научит игнорировать себя раньше, чем
принесёт пользу.

**Когда подключать:** когда объявления есть у большинства (карточка
`agent-obyavlenie-produces-ostavshimsya-agentam`). Тогда `contradiction` станет редким и
значимым событием — а это ровно то, ради чего сверка написана.

**Что подключать:** вердикт `contradiction` — находка. `unmeasured` — НЕ находка (так выглядит
harness-семья, где имя артефакта собирается на лету) и в тревогу превращаться не должен;
`undeclared` — счётчик работы, а не тревога.

Учтён урок «новый модуль подключают при рождении»: здесь неподключение НАЗВАНО и обосновано,
а не забыто.
