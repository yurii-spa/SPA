---
trackerStatus:
  type: inbox
title: Шаг 0a объявляет осиротевшую регистрацию НЕИЗМЕРИМОЙ — а измерить её можно за две минуты
status: done
created: 2026-08-20
claimed_by: cycle-40264
claimed_at: 2026-08-21T01:24:57Z
---



## Что измерено (цикл #323, замер уже сделан — не выводить заново)

Шаг 0a печатал каждый цикл раздел «🧹 мёртвые регистрации рабочих деревьев (21) — каталог
остался, git-привязки нет; **сверять нечего**, но и молчать не о чем (лечится осознанным
`git worktree prune`)». Двадцать одна строка одинакового текста в выводе обязательного шага.

Утверждение «сверять нечего» **неверно**, и это выяснилось при попытке выполнить его же совет:

1. **Каталоги существуют** (`git worktree prune --dry-run` говорит «gitdir points to
   non-existent location» — сломана привязка, а не каталог). Слепое `prune` стёрло бы
   последний указатель на них.
2. В 21 скорлупе уцелело **70 настоящих файлов** вне churn: `docs/STATE.md`, `docs/journal/
   2026-W33.md`, ADR, карточки трекера, `scripts/adr_number.py`, `spa_core/monitoring/*`.
3. С `origin/main` расходится **51** из них.
4. **Решающий замер:** содержимое каждого из 51 присутствует в объектной базе репозитория
   (`git cat-file -e $(git hash-object <файл>)`) ⇒ это **старые копии**, когда-то
   закоммиченные. Никогда-не-коммитившегося содержимого — **0**.

То есть ответ был достижим тремя дешёвыми командами (`hash-object` · `rev-parse
origin/main:<путь>` · `cat-file -e`), а сторож объявлял вопрос неизмеримым.

**По результату замера регистрации сняты** (`git worktree prune`, 56 → 35), потому что цена
снятия ИЗМЕРЕНА и равна нулю, а не потому, что так советовал текст.

## Что сделать

Научить `scripts/check_undelivered_work.py` отвечать по осиротевшей регистрации, а не
объявлять её неизмеримой: для каждого пути в скорлупе — сравнение с `origin/main` и проверка
блоба в объектной базе. Три исхода, и они РАЗНЫЕ: «совпадает» · «старая копия (блоб известен)»
· **«содержимого нет в репо вообще — вот это и есть недоставленное»**. Сегодня все три
схлопнуты в одну строку «сверять нечего».

Класс тот же, что у ADR-100 (ветка — третье вместилище недоставленной работы): осиротевшая
регистрация — **четвёртое**, и на нём сторож молчит утвердительно.

## Приёмка

- положительный контроль: скорлупа с файлом, которого нет в объектной базе ⇒ находка, а не
  строка «сверять нечего»;
- обратный контроль: скорлупа со старой копией ⇒ НЕ находка (иначе сторож станет фоном);
- «блоб проверить не удалось» ⇒ `unchecked`, код 2 (fail-CLOSED не ослаблять);
- время — вход, литеральных дат в фикстурах не заводить.

## Сделано (цикл #325)

`orphan_registration_scan` в `scripts/check_undelivered_work.py`: на каждую осиротевшую
регистрацию — обход каталога (churn отсеивается ПРАВИЛОМ УБОРЩИКА, не своей копией списка),
пакетный `hash-object --stdin-paths`, сверка с `ls-tree` базы, пакетный
`cat-file --batch-check` по расходящимся блобам. Три исхода разведены: `delivered` ·
`stale` (блоб известен репо ⇒ старая копия) · `undelivered` (блоба нет ВООБЩЕ ⇒ находка,
код 1). Совет `git worktree prune` теперь идёт с ИЗМЕРЕННОЙ ценой в строке каждой скорлупы
(«цена снятия НОЛЬ» / «снимать НЕЛЬЗЯ» / «содержимое НЕ ИЗМЕРЕНО»), а не сам по себе.

**Собственная дыра, найденная по дороге и закрытая тем же заходом:** `os.walk` по умолчанию
ГЛОТАЕТ ошибки чтения (`onerror=None`) — нечитаемый подкаталог вернул бы пустой список, то
есть «файлов вне churn нет, цена снятия ноль» над содержимым, которое не прочитано вовсе.
Ровно тот fail-OPEN, ради закрытия которого написана вся проверка, только этажом ниже.

**Приёмка.** 21 тест, все краснеют на неисправленном origin; часть — на ПОВЕДЕНИИ, а не на
отсутствии символа (сквозной прогон скрипта на настоящей сломанной регистрации даёт на
origin `rc=0` и «✅ измерено полностью, всё доставлено» над файлом, которого репозиторий
не видел ни разу — байт-в-байт жалоба этой карточки). Обратные контроли: старая копия и
совпадающий с базой файл находкой НЕ становятся, скорлупа без потерь держит код 0.
Пять мутаций, все убиты своим тестом (`missing`→`stale` · снятый `onerror` · снятая
сверка числа хешей · `exit_code` без находок скорлупы · снятый `core.quotePath=false`).

Пятая мутация сначала ВЫЖИЛА, а написанный на неё тест позеленел СКИПОМ (охранник «не
переименовала ли имя ФС» спрашивал git без снятия экранирования ⇒ сам дефект читался как
нормализация ФС). Обе ошибки — мои, обе поймал прогон мутаций, обе исправлены в дереве.

Литеральных дат в фикстурах нет: разбор скорлупы про содержимое, а не про свежесть.

## Доставка (ПОДЪЁМ, цикл #326, 21.08)

Цикл #325 умер между работой и пушем: у него карточка уже стояла `done`, на `origin/main` —
`new`, работа осталась в `/tmp/spa_c325`. Поднято циклом #326 по порядку шага 0a: `ps -p 40372`
(мёртв) · объявление в журнале · **отчёту мёртвой сессии на слово не верили**. Перемерено своим
прогоном: 21 тест зелен в дереве подъёма и **21 красен на чистом `origin/main` того же sha**,
сквозной краснеет на ПОВЕДЕНИИ (`rc=0` и «✅ измерено полностью, всё доставлено» над файлом,
которого репозиторий не видел ни разу).

Из орфанного дерева взяты ТОЛЬКО код, тесты и карточки: `docs/STATE.md` и журнал у осиротевшего
дерева СВОЯ база, копировать их нельзя (урок #226). Поле `claimed_by: cycle-40264` оставлено как
провенанс — при статусе `done` захват не действует.

Побочно вскрылось, почему подъём чуть не сорвался: шаг 0b объявлял карточку ЗАНЯТОЙ, потому что
ярлык умершей сессии (`cycle-325-pid40372`) не разбирался вовсе — отдельная карточка
`inbox-shag-0a-neizmerimyi-yarlyk-derzhit-kartochku`, починена тем же заходом ПЕРЕД подъёмом.

## Доставка на origin (цикл #327, 21.08) — ТРЕТЬЯ сессия на одной карточке

#325 написал, #326 поднял, обе умерли до пуша. #327 поднял работу ОБЕИХ (в дереве #326 лежали и
`orphan_registration_scan` #325, и разбор составного ярлыка #326) и довёл до `origin/main`.
Перемерено своим прогоном, отчёты предшественников подтвердились числом: 38 passed в дереве
подъёма против 35 failed / 6 passed на чистом `origin/main` того же sha; 3 мутации, каждая красит
свой тест; живой замер шага 0a — rc 2 → 1.

**Почему подъём чуть не был запрещён правилом** (и это тот же класс, что чинит карточка): шаг 0b
объявлял её `⛔ ЗАНЯТА` — три захвата, у одного `cycle-325-pid40372` активность «не измерена»
(ярлык не разбирался). Измерение вручную (`ps -p 40372` — пусто) заняло секунду; правило же
читало неизмеримость как живого держателя. Починка едет этим же коммитом.
