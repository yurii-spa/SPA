---
trackerStatus:
  type: agent
title: 11 протоколов закрыты для капитала из-за отсутствия живого фида — вселенная выбора сужена, 10% кэша стоят под 0%
status: backlog
source: cycle-119
created: 2026-08-05
priority: high
domain: адаптеры / фиды (read-only, НЕ money-path)
---

## Что нашли (замер 2026-08-05 08:43Z, цикл #119)

`data/allocation_rationale.json` за сегодняшний цикл:

```
cash_pct 15.0 · buffer_pct 5.0 · excess_pct 10.0 · status "named_not_quantified"
attribution: blocked_protocols: aave_v3_optimism, extra_finance_base, fluid_fusdc, frax,
             scrvusd, sdai, sfrax, spark_susds, stusd, susde, wusdm   (11 шт., pct 0.0)
decision_shadow: HOLD · apy_now 6.0509 пп == apy_opt 6.0509 пп · gain 0.00 пп · no_material_legs
```

`data/current_positions.json`: `feed_coverage.live_pct = 100.0`, но покрытие считается **по 13
подключённым адаптерам**, а не по всей eligible-вселенной. Одиннадцать протоколов из списка выше
в число этих 13 не входят вообще — им нечем подтвердить доходность, и evidence-gate (ADR-061)
честно закрывает их для финансирования (`blocked_protocols → "unevidenced" | "advisory" |
"gsm_not_confirmed"`).

**Причинно-следственная связь, которую важно не потерять:** триггер доходности говорит HOLD с
выигрышем ровно 0.00 пп **не потому, что оптимум достигнут**, а потому что оптимум ищется внутри
сужённой вселенной. Расширишь наблюдаемое множество — вердикт может измениться сам, без единой
правки money-path.

## Почему это важно

10% капитала ($10 000 из $100 000 сверх обязательной 5% подушки) стоят под 0% и, по правилу
ADR-055, простой обязан быть **объяснён каждый цикл**. Сейчас он объяснён только наполовину:
система **называет** блокирующие протоколы, но не разносит проценты по причинам
(`attributed_pct 0.0`, `unexplained_pct 10.0`). То есть отчёт честен ровно настолько, чтобы
показать: адресат проблемы — не аллокатор, а покрытие фидами.

## Что сделать

1. По каждому из 11 протоколов **измерить и записать**, что именно мешает: адаптера нет вовсе ·
   адаптер есть, но не в `ADAPTER_REGISTRY` цикла · фид отдаёт `None` · протокол advisory/T3 by
   design · `sky/sUSDS` заблокирован инвариантом (GSM Pause Delay ≥ 48h — **это by design, не чинить**).
   Результат — таблица «протокол → причина → чинится/не чинится», без догадок.
2. Для чинимых — подключить живой evidenced-APY по правилу `.claude/rules/adapters.md`:
   read-only, только stdlib, `None` при недоступности фида (никаких fake-fallback), новые T2/T3 —
   `IS_ADVISORY=True`, тесты через `FakeFeed` (живую сеть не звать).
3. Отдельно — **довести атрибуцию кэша до чисел**: `named_not_quantified` → сколько процентов
   держит каждый блокирующий класс. Пока причина не разнесена по процентам, «объяснённый простой»
   остаётся наполовину декларацией.

## Границы (важно)

- **Капитал не двигать.** Работа read-only: адаптеры + отчётность. Ни одной позиции не
  перекладывать, RiskPolicy v1.0 и пороги не трогать, триггер не включать.
- Расширение вселенной само по себе позиций не двигает: аллокатор предложит ход только в
  следующем цикле и в рамках тех же потолков. Если после расширения вердикт станет ACT —
  это **money-path** и уезжает владельцу карточкой, а не исполняется молча.
- Sky/sUSDS = 0% — инвариант, из списка «починить» исключён сознательно.

## Как понять, что готово

Для всех 11 протоколов записана причина; для чинимых — живой фид в цикле (виден в
`feed_coverage.apy_sources` как `live`); `unexplained_pct` в `data/allocation_rationale.json`
меньше сегодняшних 10.0 либо разнесён по причинам с числами.

## Связанное

- Карточка владельца `owner-decision-10-deneg-stoyat-bez-raboty-prichina-naid` (его выбор:
  фиды / включение триггера / разбор morpho) — этот таск и есть вариант 1 из неё.
- `agent-allocator-yield-frozen-rootcause` (in-progress, фаза 0 SHADOW) — тот же простой с
  другой стороны: там актуатор, здесь входы.
- Инвариант «не угадывать» (fail-CLOSED) — причина блокировки, а не дефект. Чиним **наблюдаемость**,
  а не гейт.

## Замер #215 (13.08 10:19Z) — сторож офиса подтверждает этот таск числами

Шаг 0-офис принёс ДВЕ WARN-находки `house_view_gap`, и обе — ровно про то, чем этот таск и
занят («причина есть, но нигде не НАЗВАНА машиночитаемо»):

```
gap:opportunity_unnamed:moonwell_base  8.3346% (L3) — доступна книге, не держится, отказ НЕ назван
gap:opportunity_unnamed:fluid_fusdc    4.85%   (L3) — то же
источник: data/apy_ranking.json (live cycle · DeFiLlama-derived)
```

Дубль-карточку НЕ создавал (§1a, вердикт PARTIAL): `fluid_fusdc` уже в списке 11
заблокированных выше, а расширение фидов вне Ethereum — предмет ADR-076. Здесь только цифры,
которых у таска раньше не было.

**Проверено отдельно, а не принято на слово:** причина отказа для этих двух протоколов
действительно не записана НИГДЕ машиночитаемо — `grep blocked_protocols data/*.json` даёт
пусто, `data/refusal_status.json` — это домен rates_desk и этих протоколов не касается.
То есть сторож прав, а не шумит.

**Оговорка по `moonwell_base`, чтобы 8.33% никого не соблазнили:** это тот самый протокол,
который по ADR-064 завышал TVL в 190 раз ($500M против наблюдаемых $2.6M) и справедливо не
проходит порог. «Возможность» здесь — свойство ранжирования по APY, а не приглашение
разместить капитал. Отказ обязан быть НАЗВАН, но он остаётся отказом.

Пороги, RiskPolicy и лимит цепочки не трогались.

---

## Волна 0 триажа, 16.08 — что эта карточка теперь покрывает

Схлопнута сюда (кластер К7 `docs/BACKLOG_TRIAGE_2026-08-16.md`, переведена в `done`, с диска
не удалена):

- **`agent-find-feeds-for-seven-protocols`** — «найти источники данных для семи протоколов
  без фида» (решение владельца: искать дальше, из реестра ничего не удалять). Её семёрка
  `stusd` · `wusdm` · `notional_v3` · `sdai` · `scrvusd` · `frax` · `pendle_pt_susde` —
  подмножество одиннадцати из замера цикла #119 в этой карточке.
  **Не повторять уже проверенное** (поиск по DeFiLlama 05.08): `sdai` → `sparklend/SDAI`
  $44 562 @ 0 % — пылевой пул, не настоящий sDAI; `scrvusd` → `REUSD-SCRVUSD` — LP-пара,
  а не сам актив; `frax` → тот же пул, что `sfrax` (два ключа → один пул = скрытая
  концентрация). Полная таблица — в теле схлопнутой карточки.

**Корень кластера К7 переназначен.** Триаж предлагал корнем
`inbox-ozhivit-fidy-vne-ethereum-put-k-snyatiyu`, но эта карточка полнее и шире: «вне
Ethereum» по своему заглавию не покрывает `sdai`/`frax`/`susde`/`scrvusd` (Ethereum) — поэтому
`inbox-ozhivit-fidy-vne-ethereum-put-k-snyatiyu` и
`inbox-krupneishaya-pozitsiya-knigi-stoit-na-ko` (aave_v3 Ethereum на статическом TVL)
ОСТАВЛЕНЫ ОТКРЫТЫМИ как отдельная работа, а не схлопнуты.

---

## СВЕРКА 18.08 — чинимого не осталось; группа «поставить пин» ПУСТА

Офлайн-реплей `generate()` по всем одиннадцати (сети из контейнера нет, `curl` к
`yields.llama.fi` даёт `000` — числа из воспроизведённых записей фида, живой проверки НЕ было):

| группа | протоколы | вердикт |
|---|---|---|
| **уже проведены** | `aave_v3_optimism` · `extra_finance_base` · `fluid_fusdc` · `scrvusd` · `sdai` · `sfrax` · `susde` | пины стоят, `tvl_source="live"` |
| **(б) не трогать** | `spark_susds` | пул есть, но уже закреплён за `sky_susds` — оба ключа объявляют ОДИН инструмент sUSDS; пин снял бы предохранитель против двух ключей на одном контракте. Отказ уже назван машиночитаемо в `pool_match_refused` |
| **(в) пула нет вовсе, отказ верен** | `frax` · `stusd` · `wusdm` | у `frax` единственный кандидат — пул SFRAX, занятый ключом `sfrax`; у `stusd` project «angle» в фиде пуст; у `wusdm` все USDM — чужие токены (Cardano / MegaETH / Celo) |

**Новых пинов не поставлено — ставить нечего.** Всё чинимое закрыли предыдущие проходы (второй
проход 05.08, ADR-076.1 09.08). Карточка в этой части устарела.

Отдельно: из семи «уже проведённых» двое честно НЕ проходят floor живым числом
(`aave_v3_optimism` $1.74M, `extra_finance_base` $0.34M), а `susde` закрыт классовым гейтом
advisory (инвариант 9), а не фидом. То есть «закрыт для капитала» у них — верный ответ, а не
недоделка.

### Что всё-таки сделано: закрыт fail-OPEN в контроле

У шестёрки второго прохода нигде не было закреплено поведение самого ВЕРДИКТА
`tvl_floor_verdict` — только поля `tvl_source`. Между полем и гейтом стоит
`spa_core/adapters/status_reader.py:236`, и его поведение для этой шестёрки не проверял ни один
тест. Разница не косметическая: литералы незакреплённых — `frax` $100M, `stusd` $200M,
`wusdm` $400M — **все выше порога $5M**, то есть при малейшем сдвиге логики они прошли бы floor
по константе.

Добавлен класс `TestFloorVerdictRestsOnTheObservation`. Четыре мутации, все красные: снятый пин
`sdai` ⇒ 4 падения · `wusdm`, закреплённый за чужим пулом (Cardano USDM) ⇒ 3 · вердикт перестал
смотреть на `tvl_source` ⇒ 10 · `frax` на одном пуле с `sfrax` ⇒ сторож коллизий. Приёмка:
100 passed, 126 subtests.

### Расхождение, которое НЕ разрешено (назвать честно)

Реплей даёт **16 live-ключей из 34** — ровно по числу пинов, ни один не промахнулся. Число «8 из
34», ходившее в отчётах, — это состояние ПРОД-СНИМКА (частичный ответ фида или протухание), а не
таблицы пинов. Локальный `data/adapter_status.json` для сверки непригоден: он сгенерирован в
контейнере без сети (`feed_reachable: false`, все 34 ключа `static`). Нужен прод-снимок.

### Побочная находка — заведена отдельной карточкой

Под ключом `stusd` объявлены ДВА разных контракта с общим vanity-префиксом
(`…A2C3D1E5e306d3eF0a17` в адаптере и генераторе против `…A7Baa600d44da5aB5776` в
`erc4626_rate_monitor.py:64`). Подозрение — евровое хранилище под долларовым ключом; из облака
проверить нечем, гадать запрещено. Карточка `inbox-pod-klyuchom-stusd-obyavleny-dva-raznyh`.
Сегодня не течёт в деньги: `apy_pct: null`, TVL не измеряется, капитал не идёт.
