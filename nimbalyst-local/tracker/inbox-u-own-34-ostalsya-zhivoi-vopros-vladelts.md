---
trackerStatus:
  type: inbox
title: "У own-34 остался ЖИВОЙ вопрос владельцу, и он не доехал: измерить остаток и отправить по §2.4"
status: done
source: nimbalyst
created: 2026-08-10
claimed_by: cycle-272
claimed_at: 2026-08-17T08:06:40Z
---

## Что измерено (цикл #199)

Починка сторожа `owner_decision_pending` (список ждущих вопросов строится теперь по ОЧЕРЕДИ,
а не по журналу отправок) сразу вытащила на свет ровно один живой случай:

```
1 из 5 вопрос(ов) владельцу НЕ ОТПРАВЛЕНЫ НИ РАЗУ: own-34-kill-switch-active-13h-unnoticed
```

Цикл #198 не отправил `own-34` СОЗНАТЕЛЬНО: заголовок карточки — тревога об остановке 10.08,
которая уже снята решением владельца, и слать её сейчас было бы шумом. Решение верное по
заголовку, но у карточки под хроникой остался **живой вопрос владельцу**, который он не видел:

> сообщать ли в Telegram о постановке защёлки стоп-крана из ЛЮБОГО источника
> (не только внутри цикла) — агент сознательно не трогает пути уведомлений
> стоп-крана без владельца.

## Почему не сделано этим циклом

Прежде чем переписывать и отправлять карточку, надо ИЗМЕРИТЬ, что осталось от вопроса:

- отдельного отправителя «защёлка поставлена внешним агентом → Telegram p0» в коде НЕТ
  (грепом: защёлку пишет только `governance/kill_switch.py`, читают `agent_health_monitor`,
  `loop_scheduler`, `telegram/views/warnings.py`);
- НО с цикла #195 `agent_health` выводит `kill_switch_active` и поднимает CRITICAL, а
  `agent_health` **шлёт** Telegram при новых issues (`should_alert` → `push_policy`).
  То есть фраза карточки «сторож сам не напишет» может быть уже неверна — но это надо
  проверить прогоном, а не рассуждением (дедуп/кулдаун `push_policy` могут её съесть).

Отправлять владельцу вопрос, половина которого могла закрыться сама, — тот же шум, от
которого уберёгся #198.

## Что сделать

1. Измерить в песочнице: защёлка появляется от внешнего источника → доходит ли сообщение
   до Telegram на самом деле (с учётом дедупа/кулдауна `push_policy`), и как оно выглядит.
2. По результату переписать `own-34` по §2.4 (RU, четыре секции, ДВА явных варианта со
   звездой рекомендации — иначе кнопок не будет, урок #197/#198): наверху — живой вопрос,
   хроника ниже под заголовком «закрыто».
3. Отправить (`orchestrator_queue.py notify`) и проверить `buttons: true` в журнале отправок.
4. Статус карточки НЕ трогать — `needs-owner → owner-done` только владелец (инв. #14).

## Как понять, что готово

`owner_decision_pending` показывает `undelivered_count: 0` при непустой очереди, а в журнале
отправок у `own-34` есть запись с `buttons: true`.

---

## Цикл #270 (2026-08-17): приёмочный критерий выполнялся ПО СЛЕПОТЕ — корень закрыт

Карточка просила довести `undelivered_count` до нуля при непустой очереди. Замер показал, что
**он уже ноль** — и это было неправдой.

* `data/owner_decision_pending.json` в проде: `pending_count: 8`, `undelivered_count: 0`;
* на `origin/main` вопросов `needs-owner` — **девять**;
* девятая — этот самый `own-34`, и **файла в прод-дереве нет вообще** (416 карточек в дереве
  против 481 на origin, невидимо дереву 109).

Автосинк возит `spa_core/` · `scripts/` · `tests/` и НЕ возит `nimbalyst-local/tracker/`.
Поэтому `own-34` был невидим в обе стороны: в `pending` не попадал (нет файла), в `unchecked`
не попадал (обход журнала видит только ОТПРАВЛЕННОЕ, а его не отправляли ни разу). Тот же
самоподдерживающийся круг, что #199 закрыл этажом ниже.

**Сделано:** `spa_core/owner_queue/origin_view.py` (очередь в версии `origin/main`, read-only,
без сети, 0.05 с) + находка **H8 «очередь этого дерева НЕПОЛНА»** в `owner_decision_pending`
(WARNING; **CRITICAL во время остановки**), поле `origin_queue` и счётчик `queue_gap_count`
(`None` ≠ `0`), отдельная строка в шаге 0-офис. +26 тестов, 5 мутаций. Подробности — журнал
`docs/journal/2026-W34.md`, цикл #270.

**Что ОСТАЛОСЬ (карточка не закрыта):** пункты 1–3 выше — измерить в песочнице, доходит ли
до Telegram постановка защёлки стоп-крана из внешнего источника (с учётом дедупа/кулдауна
`push_policy`), переписать `own-34` по §2.4 с двумя вариантами и отправить. Не сделано
НАМЕРЕННО и по той же причине, что у #198: заголовок `own-34` — тревога об уже снятой
остановке, слать её как есть значит ответить на жалобу о спаме новым спамом.

**Приёмка второй половины не меняется**, но читать её теперь надо по новому полю: `own-34`
обязан исчезнуть из `origin_queue.hidden` (карточка доехала в прод) ИЛИ получить запись в
журнале отправок с `buttons: true`. Захват снят, статус карточки не тронут.

---

## Цикл #272 (2026-08-17): пункты 1–3 ИСПОЛНЕНЫ — измерено, переписано, отправлено

**Пункт 1 (измерить в песочнице).** Дочерний процесс, настоящие `agent_health_monitor` и
`push_policy`, временные каталоги, подменён только транспорт. Защёлка ставится «снаружи»:

* флот здоров → **сообщение доходит**, стоп-кран назван в тексте дословно;
* класс `agent_health_critical` уже `bad` по ДРУГОМУ поводу → **ноль сообщений**;
* дневной потолок Tier-1 (10) исчерпан → **ноль сообщений**;
* та же защёлка на следующем часовом прогоне → ноль (верно: тот же инцидент).

Причина двух нулей одна: `_push_via_policy` зовёт `push_critical` **без `dedup_key`** ⇒
классовый ключ без отпечатка инцидента. `threat_reactor` и дневной цикл отпечаток передают.

**Фраза карточки «сторож сам не напишет» проверена и оказалась НЕВЕРНОЙ для главного пути:**
`threat_reactor` шлёт под ключом `kill_switch` с 17.07, с отпечатком — с 05.08 (`git log -S`),
то есть до написания `own-34`. Почему 10.08 сообщение не пришло — восстановить нечем; сегодня
ключ `kill_switch` в живом `push_state.json` стоит `ok`, «вечного bad» на нём нет.

**Пункт 2 (переписать по §2.4).** Сделано: живой вопрос наверху, два варианта со звездой
рекомендации, хроника 10.08 — ниже под «✅ Закрыто». `parse_options` до отправки вернул
2 варианта с распознанной рекомендацией.

**Пункт 3 (отправить).** Отправлено `orchestrator_queue.py notify`, отметка `buttons` проверена
в журнале отправок (см. журнал `docs/journal/2026-W34.md`, цикл #272).

**Пункт 4 (статус не трогать).** Не тронут — `own-34` остаётся `needs-owner` (инв. #14).

**Что НАМЕРЕННО не делалось:** сам отправитель для стоп-крана не написан — это ровно то
решение, которое владелец оставил за собой, и в чём смысл вопроса. Карточка закрывается.
