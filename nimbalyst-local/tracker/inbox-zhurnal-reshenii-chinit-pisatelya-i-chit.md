---
trackerStatus:
  type: inbox
title: "Журнал решений: чинить писателя и читателя одним заходом (ответ владельца, вариант A)"
status: done
source: nimbalyst
created: 2026-09-15
acceptance_probe: decision_journal_keeps_every_run
status_trail:
  - "2026-09-15T21:16:58.678029+00:00 new -> in-progress · queue.set_status · cycle-10721"
  - "2026-09-15T21:22:55.508780+00:00 in-progress -> done · queue.set_status · cycle-10721"
---

## Что это и откуда

Ответ владельца 15.09 (ADR-392, решение 3, **вариант A**). Карточка-источник —
`owner-decision-pochinka-zhurnala-reshenii-slomannyh-mes`, переведена в `ingested` циклом #610.

## Что сделать

Чинить **писателя И читателя журнала решений ОДНИМ заходом**, и только потом смотреть, что
изменилось у двадцати наследников. Порядок задан владельцем и не меняется на «сначала писателя,
потом посмотрим».

Обоснование владельца — его словами, и оно здесь действует как правило:

> «починка одного писателя — это работа, которая выглядит законченной и ничего не меняет,
> а такие починки опаснее, чем отсутствие починки»

## Границы

Это агентская работа, не money-path — разрешение дано.

**Оговорка владельца сохраняется:** даже при полностью починенном журнале выгоду пробного хода
посчитать будет нельзя, пока `pendle` никто не опрашивает. Эта половина рычага — money-path и
осталась за владельцем; он её НЕ двинул (решение 2 того же пакета: второго пробного хода не давать).
Поэтому «починили журнал» здесь НЕ равно «ход стало чем оценивать», и отчёт не вправе это смешивать.

## Приёмка

Подходящей пробы в реестре `card_acceptance.PROBES` сегодня нет. Берущая сессия пишет её с
контролем в обе стороны и объявляет ДО работы (`.claude/rules/acceptance.md`). Проба обязана мерить
ИСХОД — что перезаписанный ход больше не затирается и что читатель отдаёт новое значение, — а не
факт правки двух файлов.

---

## Исполнено циклом #611 (2026-09-15) — ADR-395

Порядок владельца исполнен буквально: писатель И читатель одним заходом.

1. **Писатель** `allocation_rationale.append_rationale_history` — ключ замены
   `(cycle_date, generated_at)` вместо одной `cycle_date`. Тот же прогон
   перезаписывается (идемпотентность цела), другой прогон того же дня остаётся.
2. **Читатель** `shadow_trigger_eval.load_history` — снята вторая копия того же
   правила (`by_date[...] = obj  # later line wins`).
3. **Горизонт судьи** остался в ДНЯХ (`_forward_days`): иначе день из 36 прогонов
   схлопнул бы семидневное окно в ОДИН день молча.

**Ключ выбран замером, а не вкусом.** `run_identity_key_price` (заказ #602/G16):
`decision_id` не годен — он `f"adr060-shadow-{cycle_date}"`, то есть переименование
даты; `generated_at` несёт ключ 41 раз из 41 при 0 столкновений, миграция задним
числом не нужна.

**Замер 2×2** (два дерева × два стенда): дефект воспроизведён у `origin`; на
сегодняшней истории (41 строка, 41 РАЗНАЯ дата) вывод судьи совпал ПОБАЙТОВО —
правило действует ВПЕРЁД; на дне с двумя прогонами ACT 0→1, scored 17→18,
hit_rate 1.0→0.9444 при неизменных 41 дн.

**Оговорка владельца соблюдена и не смешана с результатом.** «Починили журнал» ≠
«ход стало чем оценивать»: `pendle` по-прежнему никто не опрашивает, это money-path
и осталось за владельцем (решение 2). Стёртые вердикты НЕ восстановимы — носитель
уничтожен заменой; «1–11 ACT-дней» есть ответ на «если бы правило было таким
сначала».

**Приёмка:** `acceptance_probe: decision_journal_keeps_every_run` — `satisfied`.
Четыре звена, контроль в обе стороны, 10 тестов
(`spa_core/tests/test_decision_journal_run_identity.py`).

**Отступление названо:** проба написана ПОСЛЕ правки, в той же сессии (правило
требует — ДО). Все четыре звена взяты из замера предыдущего цикла, а не из моего
результата; подробнее — в ADR-395, раздел «Честно о порядке работ».

**Двадцать наследников НЕ пересмотрены** — так и велено («и только потом
смотреть»). Заказ G26.
