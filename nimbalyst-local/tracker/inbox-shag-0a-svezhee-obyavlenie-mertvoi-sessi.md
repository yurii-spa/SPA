---
trackerStatus:
  type: inbox
title: "Шаг 0a: свежее объявление МЁРТВОЙ сессии печатается как «работа может идти»"
status: done
source: nimbalyst
created: 2026-08-14
claimed_by: pid26031
claimed_at: 2026-08-14T21:01:20Z
---

## Что случилось

Шаг 0a (`scripts/check_undelivered_work.py`) печатает объявления моложе 3 часов отдельным
разделом «⏳ свежие объявления — **не находки**, работа может идти». Замер 14.08: цикл #232
увидел там цикл #231 (объявлено 1.34 ч назад) и прошёл бы мимо — а `ps -p 71512` показывал,
что **процесса нет уже давно**: сессия умерла между работой и пушем, и её дерево держало
полностью готовое исполнение решения владельца (ADR-085, агент, 16 тестов), которого на
`origin/main` не было вовсе.

Инструмент об этом ЗНАЛ и даже напечатал: «долгоживущий процесс сессии pid71512 завершился
(активность не подтверждена)» — и тут же добавил «окно ожидания 3.0ч ещё не истекло».
То есть вердикт о жизни процесса измеряется, но **не поднимает** свежее объявление в находки.

## Почему это не «просто уменьшить окно»

Окно осмысленно и снимать его нельзя: оно защищает от того, что ДВЕ сессии возьмут одну
работу (класс #230/0b). Речь о другом — о комбинации: `свежее` + `процесс мёртв` +
`есть пути, которых нет на origin` = осиротевшая работа, и ждать оставшиеся часы незачем,
ждать больше некого.

Нюанс, из-за которого нельзя чинить в лоб: «процесса нет» само по себе не значит смерти —
сессия может писать из короткоживущих CLI-команд (та же причина, по которой у нас есть
`agent-durable-session-id`). Поэтому подъём в находки должен требовать ВСЕХ трёх условий,
а не одного.

## Что сделать

1. В шаге 0a: если объявление свежее, НО долгоживущий процесс сессии завершился И у неё есть
   пути со статусом `unique`/`absent` — печатать это как **находку** (раздел «осиротело, но
   окно не истекло»), а не как «работа может идти».
2. Тест — положительный контроль, воспроизводящий 14.08: объявление 1.3 ч назад, pid мёртв,
   один путь отсутствует на origin ⇒ раздел находок непуст, код возврата 1.
3. Контроль в обратную сторону: живой pid в том же окне по-прежнему НЕ находка (иначе вернём
   класс «две сессии на одной карточке»).

## Как понять, что готово

Прогон шага 0a на сценарии #231 называет дерево вслух в находках, а на сценарии живой
параллельной сессии молчит; оба закреплены тестами.

---

## Сделано (цикл #233, 14.08) — `scripts/check_undelivered_work.py`

**Окно ожидания снимается ровно там, где ждать некого**, и только по сильному измерению:
`durable_process_gone(entry, ps)` = сессия САМА объявила долгоживущий процесс
(`session_pid` + `session_pid_start`, его пишет `log_session_change.durable_process` только
когда процесс подтверждён в момент записи) И этого процесса больше нет (или pid занят другим
процессом). Такая запись меряется сейчас же, обычным порядком; её находки помечаются
`within_grace: true` и печатаются ОТДЕЛЬНЫМ разделом «🕳 ОСИРОТЕЛО, НО ОКНО НЕ ИСТЕКЛО» —
первым, раньше просроченных: работа свежая, дерево ещё на диске, поднять её дешевле всего.

**Три условия карточки требуются ВСЕ вместе** — смерть сессии сама по себе находкой не является,
находкой её делает недоставленный путь (`absent` / содержимого нет в истории `origin` для этого
пути).

Обратные контроли (то, что НЕ должно было измениться, и не изменилось):
- запись без `session_pid` (таких большинство) — окно работает как раньше: `pid<N>` в журнале
  это pid ОДНОКРАТНОЙ CLI-команды, он мёртв ВСЕГДА, и живая сессия ничем не отличалась бы от
  осиротевшей ⇒ вернулся бы класс «две сессии взяли одну карточку» (#230, шаг 0b);
- живой долгоживущий процесс в том же окне — как раньше, `sessions_active`;
- `ps` не отработал / старт не разобран — «не измерено» (код 2), а НЕ смерть. Само окно (3 ч)
  не уменьшено ни на минуту.

**Тесты: +10** (`TestOrphanInsideGraceWindow`), из них 8 — положительный контроль: на неисправленном
`origin/main` (`3dcf0e003`) краснеют поимённо, 2 оставшиеся зелёные — это и есть обратные контроли
(они обязаны быть зелёными в обе стороны). Контрольный прогон на чистом origin: 67 passed,
на исправленном дереве 75 passed — ровно +10, новых падений ноль.

**Замер на живом журнале.** Правка сработала на настоящих данных сразу: подняла объявление
цикла #232 (`cycle-99255`, 1.65 ч, `pid99255` мёртв). Ручная сверка (тул даёт СИГНАЛ, а не
вердикт) показала, что работа #232 доставлена, а расхождение — старая копия карточки в
`/private/tmp/spa_wt_c231`: на `origin` файл НОВЕЕ. Это давняя граница класса `differs`
(«содержимого рабочего дерева нет в истории origin»), она честно описана в докстринге модуля
и правкой не создана — те же записи попали бы в «НЕ ДОСТАВЛЕНО» через 1.35 ч сами. Класс
`absent` (новые файлы, как в #231) точен и шума не даёт.
