---
trackerStatus:
  type: agent-task
title: CI на main красный — два теста Spark требуют «гарантированный fallback 5.5%», который ADR-063 намеренно убрал
status: done
priority: high
source: session-2026-08-03-cycle93
created: 2026-08-03
---

## Как найдено

Не искал намеренно. Цикл #93 впервые с 31.07 довёл `spa_core/tests/` до конца
(15 мин 13 с, **91 117 passed / 2 failed**) — и эти два падения оказались единственными.
Проверены на ЧИСТОМ `origin/main` (контрольный worktree `/private/tmp/spa_wt_c93_ctl`,
без единой правки) — **воспроизводятся дословно**, то есть к изменению цикла #93 отношения
не имеют.

## Что измерено (дословно)

```
$ python3 -m pytest spa_core/tests/test_e2e_integration.py::TestAdapterBehavior -q   # чистый origin/main
FAILED …::test_spark_get_apy_pct_positive
FAILED …::test_spark_get_apy_pct_returns_float
2 failed, 10 passed in 0.13s

E   TypeError: '>' not supported between instances of 'NoneType' and 'float'
```

Тест (`spa_core/tests/test_e2e_integration.py:178`):

```python
def test_spark_get_apy_pct_positive(self):
    """SparkSusdsAdapter.get_apy_pct() > 0 (fallback 5.5% гарантирован)."""
```

Живые данные (`data/adapter_status.json`, файл **git-tracked** ⇒ на раннере CI он тот же):

```json
"spark_susds": {"apy": 5.5, "live_apy": null, "fallback_apy": 5.5, …}
```

`SparkSusdsAdapter.get_apy_pct()` → `get_apy()` → после **ADR-063** читает НАСТОЯЩУЮ схему
(`live_apy`), а не `apy`, и при `live_apy: null` честно отдаёт `None`. Тест же пиннит ровно
то поведение, которое ADR-063 назвал ложью («метка live перестала лгать; 8 адаптеров честно
отвечают „нет данных“»).

## Что не так

Это НЕ «тест устарел, поправь ассерт» и не data-зависимость — файл данных в git, значит
падение детерминированное и на CI тоже. Развилка настоящая, и она про смысл, а не про
синтаксис:

* либо тест обязан принять `None` как ЛЕГИТИМНЫЙ ответ (тогда он про контракт «float **или**
  None», и обещание «fallback 5.5% гарантирован» из докстринга удаляется как отменённое ADR-063);
* либо где-то остался потребитель, который на этот «гарантированный» 5.5 % ещё рассчитывает —
  и тогда красный тест сигнализирует о настоящей дыре, а не о себе.

**Молча не трогал (инвариант #16):** ослабить/переписать ассерт «чтобы покрасить CI зелёным» —
ровно запрещённое действие; а решить, что именно правильно, значит трогать семантику ADR-063
(домен адаптеров + owner-gated GSM-инвариант #10 рядом).

## Acceptance criteria

- перед правкой измерено, кто ещё читает `get_apy_pct()` у spark и как реагирует на `None`
  (grep по потребителям, не по имени файла — урок циклов #38/#39);
- ассерт не ослаблен: новый тест пиннит контракт ADR-063 (`float | None`) и **краснеет**,
  если адаптер вернёт зашитый литерал под видом живого числа (положительный контроль);
- докстринг «fallback 5.5% гарантирован» либо удалён как отменённый, либо подтверждён ссылкой
  на действующий источник;
- обоснование правки — в теле изменения + запись в `docs/journal/<неделя>.md` (инв. #16).

**Не входит:** RiskPolicy / kill-switch / живой трек / launchd / `landing/**`.
Sky/sUSDS = 0 % до подтверждённого GSM Pause Delay ≥ 48h (инвариант #10) — не пересматривать.

---

## ✅ Закрыто циклом #96 (2026-08-03) — подъём ОСИРОТЕВШЕЙ работы двух сессий

Карточку никто не «взял свежим»: шаг 0b дал `stale` (захват `pid7584`, процесса нет).
Шаг 0a показал, что `spa_core/tests/test_e2e_integration.py` объявляли **дважды** — `pid1763`
(цикл #92) и `pid7584` (цикл #94) — и содержимого **ни одного** из них на `origin` нет: обе
сессии починили один и тот же красный тест и обе умерли до пуша. Отчётам мёртвых сессий не
верил, всё перемерено заново.

### Развилка карточки разрешена ИЗМЕРЕНИЕМ, а не выбором

Вопрос стоял так: тест устарел — или он сигналит о настоящем потребителе, который ещё
рассчитывает на «гарантированные 5.5 %»? Проверено grep'ом по вызовам `.get_apy_pct(`
(по потребителям, не по имени файла):

* `spa_core/adapters/adapter_registry.py:155` — единственный внешний потребитель, и он
  **уже** обрабатывает `None` явно (`if val is not None`), после чего честно уходит на
  `canonical_apy_pct`;
* внутри самого spark каждый путь fail-CLOSED и уже согласован с ADR-063: `is_eligible()`
  → `False`, `health_check()` → `"degraded"`, `vs_morpho_gap()` → `None`,
  `get_yield_info()` пробрасывает `None`;
* `DEFAULT_APY_PCT` у spark **не читает ни одна строка кода** — только докстринги.

⇒ Потребителя, зависящего от подстановки, НЕТ. Красный тест — про себя, а не про дыру.
(Соседний `morpho_steakhouse_adapter` действительно возвращает литерал 6.5 % без `None`, но
это ДРУГОЙ, уже отслеживаемый домен — `agent-apy-evidence-provenance` +
`owner-decision-allokator-ranzhiruet-puly-na-vydumannom-tvl`; в этом цикле не трогал.)

### Что сделано

* тесты `test_e2e_integration.py` строят адаптер на **объявленном** входе
  (`data_dir` с герметичным JSON), а не читают окружающий `data/adapter_status.json`;
* **инвариант #16 соблюдён механически:** в диффе против origin **ноль** удалённых строк
  с `assert` — те же `assertIsInstance(float)` / `assertGreater(0.0)`, правки строго
  аддитивные (+74 / −8, где −8 — только докстринги и подмена хелпера);
* **два положительных контроля** (`..._is_none_without_data`, `..._is_none_when_live_apy_null`),
  второй воспроизводит побайтово живую форму блока (`"apy": 5.5` рядом с `"live_apy": null`) —
  ровно тот вход, на котором краснел `main`;
* **контроль доказан мутацией, а не заявлен:** возврат fake-fallback в `get_apy()` краснит
  ровно эти два теста (`AssertionError: 5.5 is not None`), после отката — снова зелено;
* докстринги адаптера (модуль / класс / `get_apy`), обещавшие «fallback = 5.5 %», приведены
  к действующему поведению; сама константа **сохранена** (удаление публичного атрибута шире
  задачи), но помечена «НЕ подстановка» со ссылкой на тест, который покраснеет при возврате.

Проверка: целевой файл 62 passed / 1 skipped; смежные (`test_spark_susds_adapter`,
`test_adapter_status_schema_reader`, `test_allocator_evidence_gate`) 163 passed;
mypy на изменённом файле чисто; `lint_llm_forbidden` 166/0.
RiskPolicy / kill-switch / пороги / живой трек / launchd / `landing/**` — не трогал.
