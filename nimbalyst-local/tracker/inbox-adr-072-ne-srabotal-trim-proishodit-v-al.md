---
trackerStatus:
  type: inbox
title: "ADR-072 не сработал: трим происходит в АЛЛОКАТОРЕ, не в гейте — кэш всё ещё 25%"
status: new
source: nimbalyst
created: 2026-08-08
priority: high
---

Замер после цикла 08.08 06:00: кэш 25% (20% сверх буфера, 451 bps/год forgone). Причина найдена: below-median трим (maple 20→10) и TVL-freeze (morpho 20→5) применяются ВНУТРИ аллокатора — гейт получает уже 75k, поэтому мой предохранитель «перераздаём только срезанное ГЕЙТОМ» (asked−deployed=0) даёт freed=0. Фикс: перераздавать бюджет, срезанный ЛЮБЫМ защитным тримом (гейт + пост-оптимизаторные тримы аллокатора), НЕ трогая честный cap-bound остаток оптимизатора; отличать по факту трима (was_capped/below_median/tvl_frozen), а не по разнице сумм. Повторный проход гейта обязателен (инвариант 1). Первая версия заполняла до буфера и уронила 6 cycle-фикстур — нужен третий вариант: явный сигнал «сколько срезали защиты».

## Замер 08.08 (живой прогон аллокатора, read-only)

Модель в проде — **`risk_adjusted`**, НЕ optimized_yield (моя первая реплика диагноза считала
оптимизатор — отсюда неверная гипотеза «оптимизатор дал 95%»). Ноты живого прогона:

- `Веса ограничены cap'ами по тирам (T1≤40%, T2≤20%)`
- `MP-011: суммарный T2 срезан до 50% (излишек перераспределён в headroom T1 либо остался кэшем)`
- `ADR-061 evidence gate ON` — не допущены advisory (`extra_finance_base`, `susde`) и
  `gsm_not_confirmed`
- `ADR-053`: у 5 пулов TVL статический (неверифицированный) → свежий капитал не идёт
- `MP-011 TVL-floor $5M` исключил `moonwell_base`

Итог два дня подряд: target = aave 40k + pendle 20k + maple 10k + morpho 5k = 75k, кэш 25%.
`compound_v3` (T1, 3.3%, live TVL $37.6M, прошёл evidence gate) получает НОЛЬ, хотя
`_fill_remainder` при risk_adjusted обязан заполнять headroom T1.

**Следующий шаг (для свежей сессии):** инструментировать САНДБОКС-прогон cycle_runner
(живой data/ не трогать!) с печатью target на каждом шаге: после аллокатора → после
`_apply_risk_policy_gate` → после `apply_analytics_blocking_gate` → после derisk/kill →
финал. Найти шаг, который реально режет 95→75 (или доказать, что аллокатор сам отдаёт 75
и `_fill_remainder` не срабатывает — тогда чинить его). Только после этого править код:
перераздача уже написана (ADR-072, risk_gate.py) и ждёт правильного сигнала «сколько срезали».

## РАЗГАДКА (sandbox-прогон 08.08, ADR-072 РАБОТАЕТ)

Механизм исправен: перераздача сработала и предложила yearn_v3 + euler_v2. **Повторный гейт
её отклонил — и правильно:**

```
yearn_v3: Chain concentration on ethereum after trade 91.0% > single-chain limit 90.0%
euler_v2: Chain concentration on ethereum after trade 95.0% > 90.0%
euler_v2: After trade, cash buffer 5.0% < minimum 5.0%
```

**Настоящая причина простоя кэша — НЕ лень аллокатора, а лимит концентрации по цепочке
(ADR-062: одна цепочка ≤90%).** ВСЕ 8 живых кандидатов сидят на Ethereum (поле `chain` в
снимке = None у всех восьми — отдельный дефект наблюдаемости), книга уже 89–91% на Ethereum,
поэтому размещать свободные $20k буквально НЕКУДА, не нарушив диверсификацию.

Что из этого следует (порядок работ):

1. **Атрибуция кэша ЛЖЁТ**: пишет `unexplained_deployable` $20k / 451 bps «fundable headroom
   under every cap», хотя причина названа гейтом — `chain_concentration_bound`. Починить
   наименование (ADR-055: кэш обязан быть объяснён ЧЕСТНО, а не «непонятно почему»).
2. **Перераздача должна фильтровать по chain-headroom ДО предложения** (не предлагать
   заведомо отвергаемое) — и, если после фильтра кандидатов нет, честно записывать
   «cap-bound, разместить нечего».
3. **Граничный случай:** `cash buffer 5.0% < minimum 5.0%` — строгое сравнение отвергает
   ТОЧНОЕ равенство порогу. Проверить эпсилон (это money-path, не пороги менять, а сравнение).
4. **ГЛАВНОЕ ДЛЯ ДОХОДНОСТИ (карточка владельцу):** чтобы деньги работали, нужны живые
   кандидаты на ДРУГИХ цепочках. Сейчас Base/Arbitrum/Polygon-адаптеры отсеяны: у 5 пулов
   TVL статический (ADR-053), у `morpho_blue_base`/`silo_arbitrum` аномальный APY 0.00%,
   `moonwell_base` отфильтрован TVL-floor, `aerodrome_base` — глубина $1M. Это и есть путь
   к снятию простоя: расширить покрытие ЖИВЫХ фидов вне Ethereum, а не «заставить аллокатор».
5. **Дефект наблюдаемости:** `chain` у всех адаптеров снимка = None — chain-лимит считается
   где-то по другому источнику; если бы он считался по снимку, лимит был бы слеп.

## Доставлено (cloud-сессия, мандат владельца 20.08): «третий вариант» — явный сигнал

`AllocationResult.protective_trims` — дельта КАЖДОГО защитного шага измерена по
факту (сумма весов до − после): `t2_total_cap`, `t3_total_cap`; срез > 0 →
строка в notes «Защитные тримы аллокатора срезали в кэш X% (…) — сигнал для
перезаполнения (ADR-072)». Перезаполнителю больше не нужно вычитать суммы на
глаз. Приёмка: +3 теста (срез 30% при 4×T2 без якоря назван числом; два якоря
впитывают излишек → сигнала НЕТ, ложной тревоги нет; сумма срезов ≤ кэша);
аллокаторный набор 506 против контроля 503 на чистом origin = ровно +3, 0 регрессий.
Остаток карточки: подключить сигнал в redistribute_freed_budget (freed +=
protective_trims) — за циклом, это money-path решение о размере перераздачи.
