---
trackerStatus:
  type: owner-decision
title: "Чистка: разрешить удалять мёртвые МОДУЛИ из ядра (не только скрипты)?"
status: ingested
source: audit
created: 2026-07-16
---

## Что случилось и почему это важно
Чистку скриптов сделал (49 в archive). Перешёл к мёртвым Python-МОДУЛЯМ ядра. Предохранитель
справедливо отделил: удалять код пакета (spa_core/…) серьёзнее, чем скрипты, и просит твоё явное «да»
именно на это. Я уже проверил и забэкапил в `attic/` два заведомо-мёртвых адаптера (gmx_glp, radiant —
в коде помечены REMOVED, 0 ссылок, adapter-тесты зелёные 2978). Оригиналы пока НЕ удалял.

Важно: аудит-вердикт «мёртвое» я НЕ применяю слепо — надёжная перепроверка показала, что 13 из 15
«мёртвых» кандидатов на самом деле где-то используются (напр. posture, data_trust_monitor). Двигаю
ТОЛЬКО реально-безопасные, по одному, с прогоном тестов.

## Что от тебя нужно
Одно «да»: **разрешаешь удалять из ядра заведомо-мёртвые модули** (после проверки 0-ссылок + зелёных
тестов, всё в attic обратимо)? Тогда завершу перенос этих двух и продолжу остальные так же — без
переспросов на каждый.
Варианты: (а, рекомендую) да, удаляй мёртвые модули по моему протоколу (проверка+тесты+attic);
(б) только бэкап в attic, оригиналы НЕ удалять (оставлять дубли); (в) модули пока не трогать.

## Как понять, что готово
Ты ответил а/б/в.

## Что будет после
(а): завершаю gmx_glp+radiant + иду по остальным верифицированным, тесты после каждого батча.

## Ответ владельца (2026-07-16, в чате) → инжест
Да, удалять мёртвые модули по протоколу (делаю).
