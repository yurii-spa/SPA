---
trackerStatus:
  type: agent
title: Аудит всего проекта 2026-08-02 — три параллельных трека починки (координация, НЕ дублировать)
status: in-progress
source: session-2026-08-02-owner-audit
created: 2026-08-02
priority: high
domain: координация параллельных сессий; risk-gate + analytics + CI
---

> **КООРДИНАЦИОННАЯ КАРТОЧКА.** Владелец запустил полный аудит проекта и по его итогам —
> три параллельные интерактивные сессии починки. Фоновым сессиям (почасовой оркестратор и др.)
> эти области НЕ брать, пока карточка in-progress: работа уже идёт, коллизия = двойная работа
> (прецедент 2026-07-30). Перед взятием смежных карточек — `scripts/check_card_claim.py` +
> `python3 scripts/log_session_change.py --tail`.

## Что делаем

Полный аудит проекта (money-path, аналитика/тесты, продукт/процессы) тремя агентами-аудиторами
+ сверка жёстких цифр. Итоговая оценка владельцу выдана. По трём главным находкам запущены
отдельные сессии-фиксеры (чипы), работают в изолированных worktree в /tmp.

## Что нашли (главное)

1. **TVL-floor был декоративным**: `risk_gate.py` подставлял $20M при отсутствии живого TVL;
   ~22 адаптера отдают константу `TVL_USD` вместо живых данных (см. также
   `agent-tvl-floor-passes-on-literals`).
2. **Аналитический слой протокол-слепой**: no-arg fallback в `signal_aggregator._invoke` —
   модули считали на demo-данных, все 8 протоколов получали одинаковый скор 6.7/mult 1.433.
   Advisory-only, в hard-гейт RiskPolicy НЕ попадало (проверено).
3. **CI SPA Tests красный** на origin/main (Proof Gate и LLM-lint зелёные).
4. Прочее (не в работе, кандидаты на карточки): 9 страниц /admin без авторизации; STATE.md 10×
   больше собственного лимита; ADR в трёх папках; клон-фабрика 38 файлов
   `..._gross_of_<X>_base_gap_analyzer.py` (~30k строк = одна формула).

## Что фиксим и статус (на 2026-08-02 ~22:15 UTC)

- **TVL fail-closed** — ✅ доставлено на origin (22:01, «TVL provenance in the allocator»,
  ADR-062/ADR-053-side). Литерал больше не считается проверкой.
- **Аналитика фаза 2** — ✅ доставлено (21:55, коммит f6ec5e35c): 12/12 Tier-A подключены к
  протокол-контексту через `_protocol_facts.py`, Tier-B массовая починка 81 модуля.
  Остаток: 68 failed / 48 dormant / 203 unchecked.
- **CI red** — 🔄 в процессе: серия фиксов (18:51, 22:12 — `tests/test_signal_aggregator.py`),
  ждём зелёный прогон.

## Что дальше (закреплено владельцем)

Шаги 2–3 по аналитике возьмёт сессия-аудитор ПОСЛЕ завершения текущей сессии-фиксера:
(2) переподключение остатка Tier-B по корзинам failed/dormant/unchecked (адаптеры под
протокол-контекст, удаление только через карточку); (3) рефакторинг клон-фабрики 38 файлов в
одну параметризованную `analyze_fee_gap(...)` + конфиги, все 38 анализов сохранить.
Гайд по починке модулей — в памяти сессии и `scripts/audit_protocol_blindness.py`
(только в sandbox-checkout: прогоны грязнят data/*.json и spa.db).

## Директива владельца (2026-08-03)

**Цель — ~90% слоя должно РАБОТАТЬ, а не спать.** Повторный аудит — каждый день после дневного
цикла (свежие data/*.json), первый — 2026-08-03. Метрики: % протокол-чувствительных модулей
аналитики (сейчас ~9%) и % агентов флота, делающих реальную работу. Незадействованный код —
либо подключить, либо честно списать через карточку (не молча).

## Как понять, что готово

CI зелёный + шаги 2–3 доставлены на origin + доля работающего слоя ≥90% (или остаток явно
списан владельцем) → карточка переводится в done.

---

## Доставка карточки и сверка её статуса (автономный цикл #95, 2026-08-03 ~04:00 UTC)

**Карточки не было на `origin/main`.** Её нашёл шаг 0a: сессия `pid6859` объявила её
2026-08-02T22:18:07Z и умерла, не доставив (`[отсутствует] … на origin/main файла нет`).
Файл найден в осиротевшем worktree `/tmp/spa_wt_c94` и перенесён сюда **побайтово, без единой
правки** — всё выше этой черты написано владельческой сессией аудита, ниже — только замер.

Замер статуса на момент доставки (иначе координационная карточка `in-progress` запирает три
области бессрочно — класс «нечитаемый вердикт, который некому снять»):

- **все три сессии-фиксера измеримо мертвы** (`ps -p` пусто для `pid6859`, `pid2867`, `pid1763`,
  `pid7584`); ни одна из областей сейчас никем не удерживается;
- **трек 1 (TVL fail-closed)** — на origin, подтверждено;
- **трек 2 (аналитика фаза 2)** — на origin: `f6ec5e35c` и `19666683a` присутствуют в истории;
- **трек 3 (CI red)** — по-прежнему КРАСНЫЙ: `SPA Tests` / `SPA CI` на `49d0d1122` = failure
  (`spa_core/tests/test_e2e_integration.py`, spark APY против убранного ADR-063 fallback).
  Для него уже есть карточка `agent-spark-apy-tests-assert-a-fallback-adr-063-removed`
  (взята сессией `pid7584`, которая умерла; окно свежести захвата истекает 05:27Z —
  цикл #95 её по шагу 0b НЕ брал).
- **найден ЧЕТВЁРТЫЙ красный, о котором аудит не знал** — `SPA CI-Lite` красный с
  2026-08-02T19:14Z (mypy на `allocator.py:244`, внесено ADR-061). Это и есть задача цикла #95;
  карточка `agent-task-ci-lite-krasnyi-s-02-08-mypy-na-allocato`.

Статус карточки НЕ меняю (`in-progress` остаётся): её критерий «готово» задан владельцем и
включает шаги 2–3 по аналитике, которые не сделаны.

---

## Ежедневный повторный аудит #1 — 2026-08-03 (после дневного цикла 06:00 UTC)

Замер read-only. Tier-A и Tier-B прогнаны `scripts/audit_protocol_blindness.py` в **чистом
sandbox-worktree от `origin/main` 5255a62d4** (живой `data/` и `spa.db` не трогались).

### Главное: доставленная починка НЕ РАБОТАЕТ в проде

Прод-дерево `~/Documents/SPA_Claude` (из него `run_daily_paper_cycle.sh` запускает цикл) —
ветка **`env-setup-v3`**, HEAD `508c8912b` от **2026-07-15**, отстаёт от `origin/main` на
**411 коммитов**. Файла `spa_core/analytics/_protocol_facts.py` в нём **физически нет**.

Доказано живыми данными, а не рассуждением — `data/analytics_signals_advisory.json`
(сгенерирован сегодня 07:48Z самим прод-агентом `com.spa.analytics_tier_b`):

| | ожидалось после фазы 2 | фактически в проде сегодня |
|---|---|---|
| composite_risk_0_100 | 50.0 (честное «не знаю») | **6.7 — одинаково у всех протоколов** |
| risk_multiplier | 1.0 | **1.433** |
| confidence | 0 | 0.3198 |
| modules_ok | 200 | 166 |

Это байт-в-байт **до-фиксовая сигнатура**, названная в находке №2 этой карточки. То есть
фаза 2 живёт только на origin; на живом треке аналитика по-прежнему протокол-слепая.

Разрыв «origin → прод» уже вскрыт и закартирован параллельной сессией:
`agent-deployment-drift-guard` (critical, in-progress) + монитор `deployment_drift_monitor.py`
(коммит `acf8c9a37`). **Здесь не дублирую** — фиксирую последствие для метрики аудита:
пока разрыв не закрыт, любой процент «работающего слоя» относится к origin, а не к рантайму.

### Метрика «% работающего слоя» (на origin/main, строгий дифференциальный тест)

| Tier | Модулей | sensitive | blind_constant | blind_equal | failed | dormant | unchecked |
|---|---|---|---|---|---|---|---|
| A (blocking) | 12 | **10** | 0 | 2 | 0 | 0 | 0 |
| B (advisory) | 519 | **12** | 150 | 38 | 68 | 48 | 203 |

**Итого доказано протокол-чувствительных: 22 из 738 модулей `spa_core/analytics/` = 3.0 %.**
В рантайме — **0 %** (см. выше).

Динамика со вчера (тем же инструментом, из `docs/ANALYTICS_PROTOCOL_BLINDNESS.md`):

- Tier-B `sensitive`: **0 → 12** (+12) — первый реальный сдвиг;
- Tier-B `failed`: 149 → 68 (−81) — те самые «81 починенный модуль»;
- но из этих 81 **только 12 действительно различают протоколы**; остальные перешли
  в `dormant` (17 → 48) и `blind_equal` (0 → 38), т.е. перестали падать, но информации
  не несут;
- `blind_constant` 150 и `unchecked` 203 — **без движения**;
- Tier-A 10 sensitive / 2 blind_equal — без изменений.

**Про «~9 %» во вчерашней записи.** Строгим тестом это не воспроизводится: 9 % ≈ доля модулей,
которые перестали падать. Честная метрика для директивы владельца — доля тех, чей сигнал
измеримо зависит от протокола: **3.0 %**. Дальше в карточке считаю только её.

### Флот

`data/agent_health.json` (07:42Z): 69 агентов, **69 OK**, 0 WARN / 0 CRIT, все `loaded=true`;
свежесть логов < 1 ч — 32, 1–24 ч — 29, `log_age=None` — 8 (по ним судить нельзя, только по
state-файлу). Ненулевой `last_exit`: `apiserver` (−15), `rtmr_sense` (−15), `telegram_bot` (1).
`data/cycle_health.json` — HEALTHY (цикл 06:00Z, equity 100 712.66, +0.0156 %).
`data/system_health.json` — **overall CRITICAL**, домен `d5_code_integrity` CRITICAL
(остальные: 4× WARNING, 3× OK/INFO).

Честная рамка: 100 % «здоровья» флота здесь означает лишь «процессы живы и не падают».
Все 69 агентов исполняют код от 15.07 — работу они делают, но не ту, что доставлена.

### CI на origin/main

`SPA Tests` и `SPA CI` — **failure** на `acf8c9a37` и `6dfa1f39b`; `SPA Proof Gate` и
`SPA Lint — LLM forbidden` — success. Осталось одно падение
(`test_telegram_guard_stays_outermost`), его прямо сейчас держит цикл #97 по карточке
`agent-telegram-guard-outermost-fails-only-in-full-run` — **не трогаю**.

### Что двигает к 90 % дальше (по стоимости шага)

1. **Закрыть разрыв «origin → прод»** — без этого метрика измеряет мёртвый код.
   Решение владельца (обновление прод-дерева), карточка `agent-deployment-drift-guard`.
2. **`unchecked` 203** — самая крупная и самая дешёвая корзина: у модулей просто нет
   entrypoint'а, принимающего protocol-контекст. Либо адаптер, либо честное списание карточкой.
3. **`blind_constant` 150** (132 из них — константа `0.0`) — кандидаты №1 на списание:
   формально «ok», информации ноль.
4. **`failed` 68 + `dormant` 48** — точечная починка, дороже пункта 2.
5. **Клон-фабрика 38 файлов** `..._gross_of_<X>_base_gap_analyzer.py` — не начата.

### Что застряло

Шаги 2–3 по аналитике (переподключение остатка Tier-B, рефакторинг клон-фабрики) с 02.08
не начаты — все автономные циклы #95–#97 уходили на красный CI. Статус карточки
**не меняю** (`in-progress`): критерий «готово» задан владельцем, до 90 % далеко (3.0 %).

---

## Ежедневный повторный аудит #2 — 2026-08-07 (после дневного цикла 06:00 UTC)

Замер read-only. **Все три тира** прогнаны `scripts/audit_protocol_blindness.py` в чистом
клоне от `origin/main` **162c2e8f6** в `/tmp` (живой `data/` и `spa.db` не трогались).
Живые числа флота/сигналов взяты через live-API `:8765` (`/api/live/data/<файл>`), потому что
у этой сессии посреди прогона **пропал доступ на чтение к `~/Documents/SPA_Claude`** (см. ниже).

### Метрика «% работающего слоя» — строгий дифференциальный тест

| Класс | A (12) | B (479) | C (180) | Всего | % от 736 |
|---|---:|---:|---:|---:|---:|
| **sensitive** (сигнал зависит от протокола) | 10 | 105 | 0 | **115** | **15.6 %** |
| `blind_equal_wide_ok` (равенство доказано на 32 протоколах — честный проход) | 2 | 19 | 0 | 21 | 2.9 % |
| `unchecked` | 0 | 140 | 162 | 302 | 41.0 % |
| `blind_constant` | 0 | 84 | 9 | 93 | 12.6 % |
| `blind_equal` | 0 | 83 | 0 | 83 | 11.3 % |
| `dormant` | 0 | 48 | 4 | 52 | 7.1 % |
| `failed` | 0 | 0 | 5 | 5 | 0.7 % |

**Работает = 115 (15.6 %)**, с честными `wide_ok` — **136 (18.5 %)**. Честно списанных
(решением/карточкой) — **0**: `dormant` это «не подключён», а не «списан».

**Динамика с 03.08: 3.0 % → 15.6 %** (22 → 115 sensitive, +93 модуля). Рост настоящий и
почти весь в Tier-B (12 → 105); методика та же, тест тот же.

**Tier-C по-прежнему ровно 0 sensitive** — и это главное, что нужно понять правильно.
Против замера #131 (9 ok / 103 unchecked / **64 failed** / 4 dormant) сегодня
9 / **162** / **5** / 4: 59 модулей перестали падать, но **не заработали** — они переехали
из `failed` в `unchecked` с причиной «entrypoint requires non-mapping input, adapter supplies
Mapping». Это движение внутри корзины «не работает», в метрику оно не идёт.

**Живой Tier-C честен**: в `data/analytics_report_full.json` ключа
`_meta.protocol_differentiation` НЕТ вовсе (= UNCHECKED), а все 8 протоколов несут
идентичный `avg_score` 20.56 при `modules_ok` 9. Число к протоколу не относится — в
«работающие» не засчитано.

**Живой Tier-B/A дифференцирован** (в отличие от 02.08): `analytics_signals_blocking.json`
(A, 12/12 ok) даёт 5 разных скоров 41.9…67.5 с разными `triggered_by`;
`analytics_signals_advisory.json` (B, 479) — 8 разных `composite_risk` 35.48…40.75 с
разными `top_contributors`. Старый провал «все 6.7 / mult 1.433» не воспроизводится.

**82 публичных модуля не входят НИ В ОДИН тир** (738 публичных `.py` в `spa_core/analytics/`
против 671 в реестре тиров). Они не измеряются вообще — ни «работают», ни «не работают».
Это 11 % знаменателя, и до сегодня их никто не считал.

### Флот

`data/agent_health.json` (08:43Z): **77 агентов, 76 OK, 1 WARNING, 0 CRITICAL = 98.7 %**
(`com.spa.telegram_bot`, `last_exit=1`, перезапущен). `log_age_min = null` **у всех 77** —
судить можно только по state-файлам, не по логам. `system_issues`: fleet parity DRIFT
(26 orphan-plist, 1 declared-not-running) и «capital-efficiency LAZY: 10 % капитала простаивает
UNEXPLAINED».
`data/cycle_health.json` — HEALTHY (цикл 06:00Z, equity 100 778.48, +0.0157 %).
`data/deployment_acceptance.json` (09:21Z) — **OK**, 77/77 entrypoints исполняемы.
**GoLive 29/29, `ready: true`** (в CLAUDE.md ещё записано 27/29 — снимок устарел).

### Три находки, которых не было вчера

1. **CI на main красный с 02:21Z по одной строке.** `SPA CI` / `SPA Tests` падают на
   **сборе тестов**, не на логике: `scripts/edge_cross_sectional_demotion.py:476` —
   `f"{'k \\ M':>8s}"`, обратный слэш внутри выражения f-строки. Это **синтаксис Python 3.12**
   (PEP 701); матрица CI включает **3.11**, где это `SyntaxError`. Ошибка сбора отравляет весь
   прогон — падают `tests/test_edge_cross_sectional_demotion.py` и
   `tests/test_edge_capital_recycling.py`, дальше `Interrupted: 2 errors during collection`.
   Доставлено R&D-сессией #40/#41 в 02:21Z; локально она гоняла 3.12 и видела зелёное.
   Починка — одна строка (вынести `'k \\ M'` из выражения). **Я её не делаю**: мандат этой
   задачи — замер, а доступ к дереву у меня в этой сессии рвётся (см. ниже), и недоставленный
   пуш хуже отсутствующего.
2. **`d5.deployment.drift` CRITICAL — почти наверняка ложная тревога от гонки.**
   `system_health` сгенерирован в **06:00:01.766**, `code_sync_from_origin` завершился в
   **06:00:09.875** (`SYNCED`, 47 файлов, origin 766238822). Сторож измерил дерево **за 8 секунд
   до** синка, который сам же и делает его верным, и назвал 3 money-path файла
   (`rebalance_economics.py`, `cycle_gates.py`, `cycle_runner.py` — ровно поставка цикла #144
   в 04:04Z). До следующих суток d5 никто не пересчитывает, поэтому весь день
   `system_health.json` держит `overall: CRITICAL` и утверждает, что в проде крутится
   непроверенная risk-логика. Класс — сторож честно отвечает на свой вопрос в неверный момент.
   **Прямо подтвердить не смог**: sha256 хост-дерева не читается (см. ниже). Отдельный
   `data/deployment_drift.json` устарел на сутки (06.08 02:51, `money_path_files: []`).
3. **Сессия потеряла доступ на чтение к прод-дереву посреди работы.** `~/Documents/SPA_Claude`
   отдаёт `Operation not permitted` на **чтение содержимого** (даже `cat CLAUDE.md`, даже
   `git log`), при этом `ls` работает, а **запись в `data/` работает**. Состояние мигающее.
   Флот не пострадал (агенты и live-API читают нормально), это касается только интерактивных/
   фоновых сессий — но любая сессия, поймавшая это, тихо ослепнет, а её `git`-проверки
   выродятся в пустые строки (у меня первая же сверка sha256 дала три ложных `DIFF`,
   потому что `shasum` вернул пустоту, а сравнение этого не заметило).

### Что двигает к 90 % дальше (по стоимости шага)

1. **`unchecked` 302 (41 %)** — самая большая и самая дешёвая корзина. В Tier-C она разложена
   на две названные причины: 103 «нет entrypoint под protocol-контекст» + 59 «entrypoint хочет
   не-Mapping, адаптер даёт Mapping». Вторая — механическая, один адаптер закрывает 59 модулей.
2. **Tier-C целиком (180, 27 % реестра, 0 % работает)** — сейчас это самый большой мёртвый груз.
3. **`blind_constant` 93 + `blind_equal` 83** — кандидаты №1 на честное списание карточкой:
   формально считаются, информации о протоколе ноль.
4. **82 модуля вне тиров** — сначала внести в реестр, иначе метрика структурно неполна.
5. **Клон-фабрика 38 файлов** `..._gross_of_<X>_base_gap_analyzer.py` — не начата (с 02.08).

### Что застряло

Tier-C с 06.08 не сдвинулся по существу (0 sensitive), карточку
`inbox-tier-c-pyat-nastoyaschih-otkazov-agregat` держит `cycle-87477` с 06.08 18:43 — сутки.
Статус карточки **не меняю** (`in-progress`): до 90 % далеко (15.6 %).

---

## Ежедневный повторный аудит #3 — 2026-08-26 (после дневного цикла 17:16 UTC)

Замер read-only. Все три тира прогнаны `scripts/audit_protocol_blindness.py` в чистом
sandbox-worktree от `origin/main` **feb9cd477** (живой `data/` и `spa.db` не трогались,
`SPA_DATA_DIR` уведён в песочницу). Живые числа флота/сигналов взяты через live-API `:8765`
(`/api/live/data/<файл>`), потому что у этой сессии **снова** пропал доступ на чтение к
`~/Documents/SPA_Claude` — ровно тот же класс, что 07.08 (см. находку 4).

**Между аудитами #2 и #3 прошло 19 суток: с 07.08 ежедневный аудит не запускался ни разу.**
Это и есть предмет карточки `inbox-u-ezhednevnogo-audita-90-net-storozha-on` (держит
`cycle-367` с 24.08) — здесь не дублирую, фиксирую как причину, по которой «динамика со вчера»
на самом деле «динамика за 19 дней».

### Метрика «% работающего слоя» — строгий дифференциальный тест

| Класс | A (12) | B (479) | C (180) | Всего | % от 736 | Δ с 07.08 |
|---|---:|---:|---:|---:|---:|---:|
| **sensitive** (сигнал зависит от протокола) | 10 | 106 | 0 | **116** | **15.8 %** | **+1** |
| `blind_equal_wide_ok` (равенство доказано на 32 протоколах — честный проход) | 2 | 19 | 0 | 21 | 2.9 % | 0 |
| `unchecked` | 0 | 140 | 162 | 302 | 41.0 % | 0 |
| `blind_constant` | 0 | 84 | 9 | 93 | 12.6 % | 0 |
| `blind_equal` | 0 | 82 | 0 | 82 | 11.1 % | −1 |
| `dormant` | 0 | 48 | 4 | 52 | 7.1 % | 0 |
| `failed` | 0 | 0 | 5 | 5 | 0.7 % | 0 |

**Работает = 116 (15.8 %)**, с честными `wide_ok` — **137 (18.6 %)**.
**Честно списанных решением владельца — по-прежнему 0.**

**За 19 суток слой сдвинулся на ОДИН модуль** (115 → 116 sensitive; один модуль переехал из
`blind_equal` в `sensitive`). Все остальные корзины — бит в бит те же числа, что 07.08.
Это не «медленный рост», это остановка: с 03.08 по 07.08 было +93 модуля за 4 дня, с 07.08
по 26.08 — +1 за 19 дней.

**Про `_tier_c_writeoff.WRITTEN_OFF` (9 записей, «константа 0.0 на всех протоколах аудита»).**
Это МАШИННАЯ разметка, сгенерированная самим аудитом (`AUDIT_GENERATED_AT`), а не решение
владельца. Директива требует «либо подключить, либо честно списать через карточку (не молча)» —
карточки нет, поэтому в «честно списанные» эти 9 не засчитываю.

### Tier-C: живой прогон сам признаёт, что число к протоколу не относится

`data/analytics_report_full.json` (03:00Z, tier C, 180 модулей) теперь несёт
`_meta.protocol_differentiation` — и вердикт в нём **`NONE`**, дословно:

> `avg_score=20.56 одинаков у всех 8 протоколов И у несуществующего контрольного ⇒ модули не читают ctx['protocol']`

`control_modules_ok: 9`, `distinct_avg_scores: 1`. То есть несуществующий контрольный протокол
получает тот же скор, что и настоящие. В «работающие» не засчитано — и это **улучшение
честности отчётности** против 07.08, когда ключа не было вовсе (тогда пришлось выводить
`UNCHECKED` вручную). Сама работоспособность Tier-C не изменилась: 0 sensitive, как и 06.08.

### Живой Tier-A/B дифференцирован — старый провал не воспроизводится

- `analytics_signals_blocking.json` (A, 12/12 ok, 17:17Z): 5 протоколов, 5 РАЗНЫХ скоров
  41.86 … 67.46, разные `triggered_by` (`admin_key_control`, `systemic_risk_contagion`,
  `withdrawal_queue`).
- `analytics_signals_advisory.json` (B, 479 модулей, 17:57Z): 8 протоколов, 8 РАЗНЫХ
  `composite_risk` 35.79 … 40.47, разные `top_contributors`, `modules_ok` 77…93.

Сигнатура провала 02.08 («все 6.7 / mult 1.433») не воспроизводится ни в одном файле.

### Цепочка «origin → рантайм»: код доехал, но три агента его не исполняют

Прямая проверка (пока доступ на чтение ещё был): `spa_core/analytics/_protocol_facts.py` и
`_protocol_blindness.py` в прод-дереве **байт-в-байт совпадают с `origin/main`**
(`git hash-object` против `git rev-parse origin/main:<путь>`). Разрыв аудита #1 закрыт.

Но `agent_health.system_issues` вскрывает разрыв ДРУГОГО класса — «доставлено, но НЕ
исполняется»: долгоживущие KeepAlive-процессы держат старый код и сами новый не подхватят
никогда:

| Агент | Код в процессе | Разрыв |
|---|---|---|
| `com.spa.apiserver` | 17 июля | **39.7 суток** |
| `com.spa.familyfund` | 2 июля | **32.4 суток** |
| `com.spa.rtmr_sense` | 7 августа | ~19 суток |

Перезапуск — решение владельца (правило доставки, п. 6). Для метрики аналитики это не
критично (тир-агенты одноразовые, сигналы сегодняшние), но `d5.deployment.drift` = CRITICAL
в `system_health.json` **на этот раз НЕ ложная тревога** (в отличие от 07.08, где это была
гонка со синком): три агента названы поимённо, с датами.

### Флот

`data/agent_health.json` (18:04Z): **80 агентов, 78 OK, 2 WARNING, 0 CRITICAL = 97.5 %**.
`log_age = null` у **9** — по ним судить можно только по state-файлам, не по логам.
`system_issues` сверх трёх агентов выше: fleet parity DRIFT (**28 orphan-plist**,
2 declared-not-running) и «capital-efficiency LAZY: 5.0 % капитала простаивает UNEXPLAINED,
~22 bps/год упущено».

`data/cycle_health.json` — **HEALTHY** (цикл 17:16Z, equity 101 078.55, +0.0161 %), но
`evidence_vs_curve` = WARNING: **17 из 48 дат расходятся**, максимум $215.99 (own-32).
`data/system_health.json` (18:00Z) — **overall CRITICAL**: 1 CRITICAL (`d5.deployment.drift`),
3 WARNING (`d1.equity.count` 64 дня против ожидаемых ~66; `d1.equity.dates` 2 разрыва по 2 дня;
`d_riskwire.no_fork` — 10 измерений пулов без `seed_proof_hash`), 40 OK.

Честная рамка прежняя: 97.5 % «здоровья» означает «процессы живы», а не «делают доставленную
работу» — три агента из таблицы выше формально OK и при этом исполняют код месячной давности.

### CI на `origin/main`

Последний **завершённый** прогон — `e6fdd76f0` (17:58Z): `SPA Tests` **success**, `SPA CI`
**success**, `SPA Proof Gate` success, `SPA Lint — LLM forbidden` success. На `feb9cd477`
и `878d3c21e` (18:39Z / 18:50Z, поставки цикла #387) `SPA Tests` / `SPA CI` ещё `in_progress`.
**CI зелёный.**

### Четыре находки, которых не было в аудите #2

1. **Ежедневный аудит молчал 19 суток** (записи #1 03.08, #2 07.08, следующая — эта). Директива
   владельца требует ежедневного замера; фактическая частота — раз в три недели. Карточка уже
   есть (`inbox-u-ezhednevnogo-audita-90-net-storozha-on`, `cycle-367` с 24.08).
2. **15 записей реестра тиров указывают на несуществующие модули.** `ALL_MODULES` = 671, из них
   у **15 нет файла** в `spa_core/analytics/`. Вместе с 82 публичными модулями вне тиров это
   значит: знаменатель врёт в обе стороны одновременно — 82 модуля не измеряются вовсе,
   15 «модулей» измеряются, хотя их нет.
3. **Три тест-файла живут только в прод-дереве и никогда не попадали на origin** —
   `spa_core/analytics/gross_of/test_defi_protocol_vault_performance_fee_gross_of_{lp_amm_fee_drag,
   lst_peg_slippage,referral_affiliate_fee}_analyzer.py` (951 + 951 + 805 строк). CI их не
   гоняет НИ РАЗУ. Синхронизация каталогами лишнее не удаляет, поэтому они могут жить там
   бесконечно — класс «тест, которого нет в приёмке».
4. **Сессия снова ослепла посреди работы, и это воспроизводимо.** `~/Documents/SPA_Claude`
   отдаёт `Operation not permitted` на ЧТЕНИЕ (файлы, `listdir`, `git`), при этом **ЗАПИСЬ в
   `data/` работает** — проверено пробой файла. Ровно та же асимметрия, что 07.08. Два аудита
   из двух поймали это состояние; за 19 суток по нему не заведено ни карточки, ни сторожа.
   Опасность именно в асимметрии: `git`-сверки вырождаются в пустые строки и дают ложные
   вердикты, а запись при этом продолжает идти — сессия может доставить работу, которую
   не смогла проверить. Карточка заводится этим аудитом:
   `inbox-sessiya-teryaet-dostup-na-chtenie-prod-de`.

### Что двигает к 90 % дальше (по стоимости шага) — список НЕ изменился с 07.08

1. **`unchecked` 302 (41 %)** — самая большая и самая дешёвая корзина, и она названа поимённо:
   242 модуля (140 в B + 103 в C) = `no context-accepting entrypoint`; 59 (C) = `entrypoint
   requires non-mapping input, adapter supplies Mapping`. Вторая причина механическая —
   один адаптер закрывает 59 модулей за раз.
2. **Tier-C целиком** (180 модулей, 27 % реестра, 0 % работает) — крупнейший мёртвый груз;
   собственный вердикт прогона `NONE` это подтверждает.
3. **`blind_constant` 93 + `blind_equal` 82 = 175 (24 %)** — кандидаты №1 на честное списание
   карточкой владельцу. Машинная разметка на 9 из них уже есть, решения владельца нет ни по
   одному.
4. **82 модуля вне тиров + 15 призраков в реестре** — сначала починить знаменатель.
5. **Клон-фабрика 38 файлов** `..._gross_of_<X>_base_gap_analyzer.py` — не начата с 02.08.

### Что застряло

**Всё.** Ни один из пяти пунктов списка «что дальше» не сдвинулся за 19 суток; корзины
`unchecked` / `blind_constant` / `dormant` / `failed` совпадают с 07.08 бит в бит. Карточку
`inbox-tier-c-pyat-nastoyaschih-otkazov-agregat` держит `cycle-87477` с **06.08 18:43** —
захват висит 20 суток (у ярлыка нет живого pid ⇒ по новому правилу цикла #387 такой захват
не стареет никогда и запирает карточку).

Статус карточки **не меняю** (`in-progress`): критерий «готово» задан владельцем — 90 %,
фактически 15.8 %. До порога перевода в done далеко.

---

## ⚠️ Столкновение нумерации (замечено 2026-08-28)

Секции «#3» две, и написаны они РАЗНЫМИ сессиями: на origin лежала «#3 — 26.08», а в хост-дереве
жили «#3 — 20.08» и «#4 — 27.08», которые на origin так и не уехали. Ничего не переписываю и не
выбрасываю: обе ветки истории сохранены ниже в хронологическом порядке дат замера, нумерация
дальше ведётся по ДАТЕ, а не по счётчику. Класс уже известен — карточки и общие документы,
дописанные в хост-дереве, надо перед отправкой класть на origin, иначе следующая сессия либо
затрёт чужую работу, либо (как здесь) заведёт вторую ветку одного и того же журнала.

---

## Ежедневный повторный аудит — 2026-08-20 (после дневного цикла 07:32 UTC) · секция жила только в хост-дереве, на origin её не было

Замер read-only. Все три тира прогнаны `scripts/audit_protocol_blindness.py` в чистом
sandbox-worktree от `origin/main` **2be8f768d** (живой `data/` и `spa.db` не трогались).

### Главное: за 13 суток метрика не сдвинулась НИ НА ОДИН модуль

| Класс | A (12) | B (479) | C (180) | Всего | % от 736 | Δ с 07.08 |
|---|---:|---:|---:|---:|---:|---:|
| **sensitive** | 10 | 105 | 0 | **115** | **15.6 %** | **0** |
| `blind_equal_wide_ok` | 2 | 19 | 0 | 21 | 2.9 % | 0 |
| `unchecked` | 0 | 140 | 162 | 302 | 41.0 % | 0 |
| `blind_constant` | 0 | 84 | 9 | 93 | 12.6 % | 0 |
| `blind_equal` | 0 | 83 | 0 | 83 | 11.3 % | 0 |
| `dormant` | 0 | 48 | 4 | 52 | 7.1 % | 0 |
| `failed` | 0 | 0 | 5 | 5 | 0.7 % | 0 |

**Работает = 115 (15,6 %)**, с честными `wide_ok` — **136 (18,5 %)**. Честно списанных — **0**.
Совпадение с замером #2 (07.08, origin `162c2e8f6`) **поклассовое, число в число**.

**Причина найдена и она не в аналитике.** С 07.08 на `origin/main` легло **576 коммитов**,
из них `spa_core/analytics/` тронули **3**, и ни один не про протокол-чувствительность:
`a10ace02d` (граница времени на модуль), `0e96a2efd` (развод `ADAPTER_REGISTRY`),
`3a1baf58a` (казна 2 % TVL — отказ вместо выдумки). Слой не деградировал — его просто
никто не двигал. Работа этих 13 суток ушла в сайт, стоп-кран, гигиену тестов и R&D-эджи.

**Сам ежедневный аудит тоже остановился.** Последний announce «Ежедневный аудит 90 %» —
**2026-08-05**, последняя секция в этой карточке — **07.08**. Тринадцать суток задача не
измеряла ничего. Это отдельный отказ: директива владельца от 03.08 требует ежедневного замера,
и его отсутствие никто не заметил — потому что сторожа у самого аудита нет.

### Цепочка origin → рантайм ЦЕЛА

`git diff origin/main -- spa_core/analytics/` — **пусто**; `_protocol_facts.py` в рабочем дереве
побайтово равен origin (`b8b7710d1`). Разрыва 03.08 больше нет, `code_sync_from_origin` работает.

### Живые сигналы честны и различают протоколы

- `analytics_signals_blocking.json` (Tier-A, 09:32 local, 12/12 ok): 4 протокола, разные скоры
  41,9…49,3 с разными `triggered_by`.
- `analytics_signals_advisory.json` (Tier-B, 09:50 local, 479 модулей): 8 протоколов,
  `composite_risk` 35,58…39,72, разные `top_contributors`. Старый провал «все 6,7 / mult 1,433»
  не воспроизводится.
- **Tier-C честно признаёт свою слепоту**: `analytics_report_full.json` →
  `_meta.protocol_differentiation.verdict = **NONE**`, «avg_score=20.56 одинаков у всех
  8 протоколов И у несуществующего контрольного». В «работающие» не засчитано — верно.

### Флот: 78/78 живы по пульсу, но трое исполняют код месячной давности

`data/agent_health.json` (09:51 local): **78 агентов, 78 OK, 0 WARNING, 0 CRITICAL**,
`overall_status: WARNING` — из-за `system_issues`, не из-за агентов. `log_age_min = null` у всех 78
(судить можно только по state-файлам).

Три долгоживущих `KeepAlive`-агента **доставлены, но не исполняются** — сами новый код не
подхватят никогда, перезапуск — решение владельца (правило доставки, п. 6):

| Агент | Код от | Разрыв |
|---|---|---:|
| `com.spa.apiserver` | 17 июля | **33,5 сут** |
| `com.spa.familyfund` | 2 июля | **32,4 сут** |
| `com.spa.rtmr_sense` | 7 августа | **12,9 сут** |

Итого честно: **75 из 78 = 96,2 %** исполняют актуальный код. Ещё в `system_issues` —
fleet parity DRIFT (26 orphan-plist, 2 declared-not-running) и «capital-efficiency LAZY:
5,0 % капитала простаивает UNEXPLAINED (~25 bps/год)». `cycle_health` — **HEALTHY**
(цикл 07:32Z, 0,59 ч назад). `system_health` — WARNING (0 CRITICAL, 6 WARNING, 37 OK),
GoLive **29/29**.

### CI на origin/main — ЗЕЛЁНЫЙ

На `2be8f768d` успешны все шесть: `SPA Tests`, `SPA CI`, `SPA Proof Gate`,
`SPA Lint — LLM forbidden`, `SPA Numbers Lint`, `owner-gate`. Красный CI, тянувшийся
с 02.08, закрыт.

### Что застряло (и держит 90 % на месте)

1. **Tier-C (180 модулей, 0 sensitive)** — карточку
   `inbox-tier-c-pyat-nastoyaschih-otkazov-agregat` держит `cycle-87477` **с 06.08, 14 суток**;
   `ps -p 87477` — **пусто, сессия мертва**. Захват никем не снят, карточку никто не возьмёт.
   Родственная `inbox-tier-c-171-iz-180-modulei-ne-otvechayut` висит `in-progress` без движения.
2. **`unchecked` 302 (41 %)** — самая дешёвая корзина, в Tier-C разложена на две названные
   причины (103 «нет entrypoint» + 59 «entrypoint хочет не-Mapping»). Не начата.
3. **`blind_constant` 93 + `blind_equal` 83** — кандидаты №1 на честное списание карточкой.
   За 13 суток списано ноль.
4. **82 публичных модуля вне всех тиров** (671 в реестре против 738 публичных файлов) —
   не измеряются вообще. Подтверждено сегодня повторно.
5. **Клон-фабрика** — по-прежнему **36 плоских файлов** `*_gross_of_*_base_gap_analyzer.py`
   в `spa_core/analytics/` плюс 19 в подпакете `gross_of/`. Рефакторинг не начат с 02.08.

Статус карточки **не меняю** (`in-progress`): 15,6 % против цели 90 %. Инвариант №14 соблюдён.

---

## Ежедневный повторный аудит — 2026-08-27 (после дневного цикла 07:11 UTC) · секция жила только в хост-дереве, на origin её не было

Замер read-only. Все три тира прогнаны `scripts/audit_protocol_blindness.py` в чистом
sandbox-worktree от `origin/main` **393fa22f0** (живой `data/` и `spa.db` не трогались).

### Главное: впервые с 03.08 честное списание перестало быть нулём

| Класс | A (12) | B (479) | C (180) | Всего | % от 736 | Δ с 20.08 |
|---|---:|---:|---:|---:|---:|---:|
| **sensitive** | 10 | 106 | 0 | **116** | **15,8 %** | **+1** |
| `blind_equal_wide_ok` | 2 | 19 | 0 | 21 | 2,9 % | 0 |
| `unchecked` | 0 | 140 | 162 | 302 | 41,0 % | 0 |
| `blind_constant` | 0 | 84 | 9 | 93 | 12,6 % | 0 |
| `blind_equal` | 0 | 82 | 0 | 82 | 11,1 % | −1 |
| `dormant` | 0 | 48 | 4 | 52 | 7,1 % | 0 |
| `failed` | 0 | 0 | 5 | 5 | 0,7 % | 0 |

**Сдвиг за 7 суток — ровно один модуль**, и он назван: `protocol_insurance_scorer`
(коммит `b53958044`, цикл #366) выбрасывал измеренное страховое покрытие и объявлял его нулём —
`blind_equal` → `sensitive`, различных `risk_score` 1 → 10.

**Честное списание состоялось (ADR-133, коммит `f1a5ef14e`, решение владельца 25.08, вар. 1А+2А).**
Реестр `spa_core/analytics/_tier_c_writeoff.py` — **сгенерирован из JSON аудита, не набран руками**:
`WRITTEN_OFF` 9 (с измеренной причиной каждого) · `UNKNOWN_FROZEN` 162 · `DORMANT` 4 · `FAILED` 5,
пересечения пусты. В рабочем дереве побайтово = origin.

**Две честные прочтения метрики — цифра зависит от того, считать ли «заморожено как не знаем»
распоряжённым:**

- **консервативно** (работает ИЛИ списано): 116 + 21 `wide_ok` + 9 списанных = **146 / 736 = 19,8 %**
  (было 18,5 % на 20.08);
- **широко** (плюс 162 замороженных решением владельца): **308 / 736 = 41,8 %**.

Рекомендация: считать headline-метрикой консервативную. Заморозка — честная запись «мы про них
не знаем», но это не работа; в 90 % её засчитывать нельзя без отдельного слова владельца.

### Tier-C: слепота перестала выдавать число — это видно на живом артефакте

`analytics_report_full.json` → `_meta.protocol_differentiation.verdict`: **`NONE` (20.08) →
`UNCHECKED` (27.08)**, `protocols_with_responding_modules = 0`, `control_avg_score = null`,
причина названа словами. Прежнее `avg_score = 20,56` — одно число до сотой у `aave_v3` и `maple` —
больше не публикуется. Списание доехало в прод и видно на выходе, а не только в коде.

### Цепочка origin → рантайм ЦЕЛА

`git diff origin/main -- spa_core/analytics/` — только 3 **нетронутых локальных тест-файла**
(`gross_of/test_*`), которых нет на origin. `_protocol_facts.py` побайтово = origin (`b8b7710d1`),
`_tier_c_writeoff.py` и `signal_aggregator.py` — тоже. Разрыва 03.08 нет.

### Живые сигналы честны и различают протоколы

- **Tier-A** (`analytics_signals_blocking.json`, 10:11 local, 12/12 ok): 5 протоколов,
  скоры 41,86 / 48,71 / 49,27 / 67,46 / 67,46, разные `triggered_by`.
- **Tier-B** (`analytics_signals_advisory.json`, 10:57 local, 479 модулей): 8 протоколов,
  `composite_risk` **35,82 … 40,49**, `modules_ok` 77–93. Старый провал «все 6,7 / mult 1,433»
  не воспроизводится.

### CI на origin/main — РЕГРЕССИЯ: снова красный

На 20.08 все шесть workflow были зелёные. Сейчас:

- **`SPA Tests` и `SPA CI` — failure** (последние прогоны: `6145ccca6` 07:34Z, `4032a218d` 07:13Z).
  Семь падающих тестов, все класса гигиены/храповиков, **аналитики среди них нет**:
  `test_architecture_manifest.py` (3 — `com.spa.orchestrator2`: plist в репо есть, в манифесте нет,
  это ночная установка двух агентов владельцем), `test_frozen_date_ratchet.py`,
  `test_session_anchor_proves_nothing.py`, `test_tracker_board_matches_cards.py`,
  `test_unwired_scripts_ratchet.py`.
- **На свежайшем `393fa22f0` `SPA Tests` и `SPA CI` не запускались вообще** — сработали только
  три workflow из шести. Это отдельный класс «CI молча не запустился», по нему уже есть карточка
  `owner-decision-ci-molcha-ne-zapustilsya-na-pr-zelenaya`. Текущее состояние main тестами
  **не измерено**.

### Флот: 82 агента, но «живой пульс» ≠ «делает свежую работу»

`data/agent_health.json` (11:07 local): **82 агента, 80 OK, 2 WARNING** (`com.spa.monthly_statement`,
`com.spa.source_discovery`), 0 CRITICAL; `overall_status: WARNING` из-за `system_issues`.
`log_age_min = null` у 10 — судить можно только по state-файлам.

Три долгоживущих `KeepAlive`-агента доставлены, но исполняют старый код (перезапуск —
решение владельца, правило доставки п. 6):

| Агент | Разрыв на 20.08 | Разрыв сегодня |
|---|---:|---:|
| `com.spa.apiserver` | 33,5 сут | **40,3 сут** |
| `com.spa.familyfund` | 32,4 сут | 32,4 сут |
| `com.spa.rtmr_sense` | 12,9 сут | **19,6 сут** |

Честно: по пульсу **80/82 = 97,6 %**; за вычетом трёх, исполняющих код месячной давности, —
**77/82 = 93,9 %**. Разрыв `apiserver` и `rtmr_sense` за неделю **вырос на 7 суток** — сам не
починится по построению.

`cycle_health` — **HEALTHY** (цикл 07:11Z, 0,98 ч назад; equity 101 095,54, +0,0165 %), но
`evidence_vs_curve` WARNING: 18 из 49 дат расходятся, максимум $215,99 (own-32).
`system_health` — WARNING (3 WARNING: `d1_data_pipeline`, `d5_code_integrity`, `d_riskwire`).
`system_issues` также называет: fleet parity DRIFT (**29** orphan-plist, было 26; 2
declared-not-running) и простой 5,0 % капитала UNEXPLAINED (~24 bps/год).

### Что застряло (и держит 90 % на месте)

1. **Все 7 захваченных карточек держат МЁРТВЫЕ сессии** — `ps -p` пусто для 87477, 367, 28258,
   6697, 27502, 96657, 8630. Среди них Tier-C (`cycle-87477` с **06.08, 21 сутки**) и сторож
   самого аудита (`cycle-367` с 24.08). Захват никем не снят ⇒ карточки никто не возьмёт.
   Это самый дешёвый разблокиратор из всех перечисленных.
2. **Tier-B `unchecked` 140 + `blind_constant` 84 + `blind_equal` 82 + `dormant` 48 = 354 модуля**
   не тронуты с 07.08. **Прецедент для них уже создан** — ADR-133 сделал ровно это для Tier-C
   реестром, сгенерированным из аудита. Повторение приёма на Tier-B даёт
   146 → **500 / 736 = 68 %** за одну доставку.
3. **83 публичных модуля вне всех тиров** — 754 публичных файла против 671 в реестре тиров.
   Неделю назад было 67; корпус растёт быстрее, чем его меряют, **знаменатель метрики дрейфует**.
4. **Клон-фабрика** — 36 плоских `*_gross_of_*_base_gap_analyzer.py` + 17 в подпакете
   `gross_of/`. Не тронута с 02.08.
5. **Сторож аудита есть, но мерит не то.** `data/analytics_90pct_status.json` (создан 24.08,
   цикл #367) — **FRESH**, `age_hours` 21,5, и честно отказывается публиковать метрику
   (`metric_90pct: null`, «отметка разметки покрывает только Tier B — частичный замер метрикой
   не называем»). Но он следит за свежестью РАЗМЕТКИ, а не за тем, что ежедневный аудит
   состоялся: секций #4/#5/#6 за 21–26.08 в этой карточке нет — **пропуск шести суток он не
   покраснел**.

Статус карточки **не меняю** (`in-progress`): 19,8 % против цели 90 %. Инвариант №14 соблюдён.
## Ежедневный повторный аудит — 2026-08-28 (после дневного цикла 07:54 UTC)

Замер read-only. Все три тира прогнаны `scripts/audit_protocol_blindness.py` в чистом
sandbox-worktree от `origin/main` **5428b0337** (`SPA_DATA_DIR` уведён в песочницу, живой `data/`
и `spa.db` не трогались).

### Главное: за сутки метрика не сдвинулась ни на один модуль — но не деградировала

| Класс | A (12) | B (479) | C (180) | Всего | % от 736 | Δ с 27.08 |
|---|---:|---:|---:|---:|---:|---:|
| **sensitive** (сигнал зависит от протокола) | 10 | 106 | 0 | **116** | **15,8 %** | **0** |
| `blind_equal_wide_ok` (равенство доказано на 32 протоколах — честный проход) | 2 | 19 | 0 | 21 | 2,9 % | 0 |
| `unchecked` | 0 | 140 | 162 | 302 | 41,0 % | 0 |
| `blind_constant` | 0 | 84 | 9 | 93 | 12,6 % | 0 |
| `blind_equal` | 0 | 82 | 0 | 82 | 11,1 % | 0 |
| `dormant` | 0 | 48 | 4 | 52 | 7,1 % | 0 |
| `failed` | 0 | 0 | 5 | 5 | 0,7 % | 0 |

Сумма по корзинам = **671** и это ровно размер реестра тиров (`get_tier_counts()` = A 12 · B 479 ·
C 180) — то есть замер накрыл реестр целиком, без «потерявшихся» модулей.

- **консервативно** (работает ИЛИ списано владельцем): 116 + 21 `wide_ok` + 9 списанных =
  **146 / 736 = 19,8 %** — как 27.08, **Δ = 0**;
- **широко** (плюс 162 замороженных решением владельца): **308 / 736 = 41,8 %**.

**Спор между секциями 26.08 и 27.08 разрешён в пользу 27.08 — проверено по источнику.** Секция
26.08 отказалась засчитывать девять `WRITTEN_OFF` как честное списание («карточки нет,
разметка машинная»). Карточка есть: `docs/decisions/ADR-133-tier-c-nine-written-off-162-frozen.md`
на origin, статус Accepted, **решение владельца 2026-08-25 «все одобряю», варианты 1А + 2А**,
исходная карточка названа в шапке (`owner-decision-spisat-180-fonovyh-modulei-tier-c-ili-ch.md`).
Машинная разметка здесь — способ ИСПОЛНЕНИЯ owner-решения, а не замена ему. Поэтому headline —
19,8 %, а не 18,6 %.

### Знаменатель 736 не совпадает ни с одним живым числом — и это надо решать

| Что считаем | Сколько |
|---|---:|
| Реестр тиров (`ALL_MODULES`) | **671** |
| Публичных `.py` в `spa_core/analytics/` на диске (без `test_*`, без `_*`) | **754** |
| Знаменатель метрики, заданный директивой 03.08 | **736** |

**83 модуля не входят ни в один тир** — они не измеряются вообще, ни в какую корзину не попадают
и на метрику не влияют. Неделю назад их было 82, 20.08 — 67: корпус растёт быстрее, чем его
меряют. Пока знаменатель зафиксирован на 736, метрика систематически ЗАВЫШЕНА относительно
честного `146 / 754 = 19,4 %`. Разница сегодня мала, но дрейф односторонний и накапливается.

### Живые сигналы: честны, протоколы различают

- **Tier-A** (`analytics_signals_blocking.json`, 07:54Z, 12/12 ok): 5 протоколов, скоры
  41,86 / 44,28 / 48,71 / 49,27 / 67,46, разные `triggered_by`.
- **Tier-B** (`analytics_signals_advisory.json`, 07:27Z, 479 модулей): 8 протоколов,
  `composite_risk` **35,92 … 40,49**, `modules_ok` 77–93. Старый провал «все 6,7 / mult 1,433»
  не воспроизводится.
- **Tier-C** (`analytics_report_full.json`, 03:00Z, 171 модуль после списания):
  `_meta.protocol_differentiation.verdict` = **`UNCHECKED`**, `protocols_with_responding_modules: 0`,
  `control_avg_score: null`, причина словами. Число, не относящееся к протоколу, не публикуется —
  списание доехало в прод и видно на выходе.

### Цепочка origin → рантайм ЦЕЛА

`git diff origin/main -- spa_core/analytics/` — три файла, все три **локальные тесты**
(`gross_of/test_*`), которых нет на origin; ни одного случая «код на origin, а в проде нет».
`deployment_acceptance` прогнан вручную: **OK** — 85 точек входа, 0 сломанных, 0 провальных
импортов, 72 агента импортируют свою цель, артефакты свежие.

### Флот: 79/82 OK, а разрыв «исполняют старый код» ЗАКРЫЛСЯ — перезагрузкой, не деплоем

`data/agent_health.json` (07:31Z): **82 агента, 79 OK, 3 WARNING, 0 CRITICAL = 96,3 %** по пульсу
(`launchctl` знает 85 меток). Три WARNING разобраны поимённо:

| Агент | Что говорит | Что это на самом деле |
|---|---|---|
| `com.spa.monthly_statement` | `log missing (never ran?)` | месячный, ещё не наступал срок |
| `com.spa.telegram_milestone` | `log missing (never ran?)` | суточный, лога нет — **не измерено** |
| `com.spa.reboot_verify` | `last_exit=1` | гонка на старте, разбор ниже |

**Разрыв трёх `KeepAlive`-агентов исчез.** 27.08: `apiserver` 40,3 сут · `familyfund` 32,4 сут ·
`rtmr_sense` 19,6 сут старого кода. Сегодня все трое стартовали заново (01:27–01:30 local) —
потому что **хост перезагрузился** (`uptime` = 9 ч 09 мин, загрузка ~01:03). Разрыв закрыт
СЛУЧАЙНО, а не доставкой: механизма, который перезапускает долгоживущего агента после обновления
кода, как не было, так и нет. Следующий разрыв начнёт копиться с сегодняшнего дня.

**`reboot_verify` рухнул на гонке, и красный код выхода остался висеть.** Проверка стартовала в
23:27:47Z — через 100 секунд после загрузки, когда `apiserver` ещё не поднялся (он стартовал в
01:30 local = 23:30Z). `apiserver /api/live/ping: 000` ⇒ `verdict=ATTENTION`, `rc=1`. Через шесть
минут проверку прогнали повторно, и `data/fleet_reboot_status.json` (23:33:11Z) уже говорит
**`verdict: HEALTHY`, `apiserver_ping_code: "200"`, `still_down: 0`, `exit78: 0`**; я проверил
живьём — `127.0.0.1:8765/api/live/ping` отвечает **200**. То есть аварии не было, а WARNING во
флоте держится залипшим кодом выхода первого прогона. Класс: **сторож проверяет сервисы раньше,
чем launchd успевает их поднять**, и сам себя не перепроверяет.

`cycle_health` — **HEALTHY** (цикл 07:54Z, 0,2 ч назад; equity 101 111,76, +0,0167 %), но
`evidence_vs_curve` WARNING: **19 из 50** дат расходятся (было 18 из 49), максимум $215,99 (own-32).
`system_health` — WARNING (6 WARNING): `d1.adapter.apy_range` (scrvusd 0,46 %), `d1.equity.count`
(66 дней трека при ожидаемых ~68), `d1.equity.dates` (2 разрыва: перед 20.07 и перед 28.07),
`d2.defillama.deviation` (**aave_v3: хранимые 12,49 % против живых 4,96 %, +60 %**),
`d5.deployment.drift` (712 файлов вне синхронизируемых каталогов — помечено самим сторожем
«Reference only», это штатно) и `d_riskwire.no_fork` (10 измерений без `seed_proof_hash`).
`system_issues`: fleet parity DRIFT (**29** orphan-plist, 2 declared-not-running) и простой
5,0 % капитала UNEXPLAINED (~24 bps/год).

### CI на origin/main — КРАСНЫЙ, и падений стало вдвое больше

`SPA Tests` и `SPA CI` — **failure** на четырёх последних завершённых прогонах подряд
(`cca1d6f7e`, `ada950420`, `e96e86cb8`, `e18d43675`). Последний полный прогон:
**16 failed / 97 495 passed** (было 7 падений 27.08). На свежайшем `5428b0337` оба ещё
`in_progress`. Остальные четыре workflow (`owner-gate`, `SPA Lint — LLM forbidden`,
`SPA Proof Gate`, `SPA Numbers Lint`, «генерируемые документы») — **success**.

**Аналитики среди падений нет ни одного.** Все 16 — гигиена, храповики и дедуп:

| Семейство | Шт. | Суть |
|---|---:|---|
| `test_prescribed_run_matches_ci.py` | **8** | CI гейтит `research/cards`, `scripts/tests`, `spa_core/analytics/gross_of`, `tests` и задаёт `SPA_ENV=ci`, `PYTHONHASHSEED=0` — предписанная в CLAUDE.md команда прогона не называет ни каталогов, ни переменных |
| `test_telegram_*` (flood / outbound_guard ×2 / selfheal_spam) | 4 | окно дедупа не считает повтор повтором |
| `test_doc_drift` | 1 | живой документ (CLAUDE.md:89) всё ещё подаёт правило Sky/sUSDS = 0 % как ДЕЙСТВУЮЩЕЕ |
| `test_knowledge_graph_connectivity` | 1 | `.claude/rules/design-docs.md` недостижим из графа правил |
| `test_session_anchor_proves_nothing` | 1 | announce записал таймер как якорь сессии |
| `test_unwired_scripts_ratchet` | 1 | два новых неподключённых скрипта: `build_knowledge_graph`, `tracker_counts` |

Восемь падений первого семейства — прямое следствие того, что предписанная команда прогона
отстала от того, что CI на самом деле гейтит. Это ровно тот класс, который уже стоил восьми
красных коммитов подряд (цикл #189).

### Находка дня: у `deployment_acceptance` НЕТ расписания

Правило `.claude/rules/deployment.md` (написано по разбору 04.08) требует прогонять
`deployment_acceptance` **до и после ЛЮБОГО изменения дерева** и называет три сторожа, ни один из
которых не заменяет другой. Проверил проводку каждого:

| Сторож | Кто его зовёт | Свежесть артефакта |
|---|---|---|
| `deployment_drift_monitor` | `system_health_monitor._probe_deployment_drift` — ежедневно 06:00 | проверка свежая; отдельный `data/deployment_drift.json` — **16 суток** (12.08) |
| `deployment_acceptance` | **никто** — ни plist, ни шаг цикла, ни workflow | артефакта нет вовсе |
| `agent_health_monitor` | агент, 07:31Z | свежий |

`grep` по `scripts/`, `launchd/`, `.github/`, `~/Library/LaunchAgents/` даёт единственное
упоминание `deployment_acceptance` — комментарий в `scripts/cycle_analytics_audit.py` про коды
возврата. Вызова нет ни одного. Сторож, отвечающий на вопрос «способен ли флот вообще
стартовать», работает ровно тогда, когда о нём вспомнит живая сессия. Сегодня я его прогнал
вручную — **OK**, но это не проводка. Заведена карточка.

Сам ежедневный аудит 90 %, наоборот, **подключён**: `scripts/agent_orchestrator.sh` шаг 1в зовёт
`scripts/cycle_analytics_audit.py` настоящим `subprocess.run` (ADR-130), а
`data/analytics_90pct_status.json` — **FRESH** (`age_hours` 5,43) и честно отказывается публиковать
число (`metric_90pct: null`), потому что разметка покрывает только Tier B.

### Что застряло (и держит 90 % на месте)

1. **Захваченные карточки держат мёртвые сессии — их стало БОЛЬШЕ.** Девять карточек заняты
   держателями с числовым pid, и `ps -p` пуст у **всех девяти**: `cycle-87477` (Tier-C, с 06.08 —
   **22 суток**), `cycle-367` (сторож самого аудита), `cycle-28258`, `cycle-6697`, `cycle-27502`,
   `cycle-96657`, `cycle-8630`, `pid54980`, `pid6764`. 27.08 их было семь. Ещё пять держит
   `interactive-session-2026-08-27` — по имени метки живость **не измеряется** (это не pid), и
   отдельного способа проверить нет; засчитывать их в мёртвые не стал. Снятие мёртвых захватов
   остаётся самым дешёвым разблокиратором: без него Tier-C никто не возьмёт.
2. **Tier-B: `unchecked` 140 + `blind_constant` 84 + `blind_equal` 82 + `dormant` 48 = 354 модуля**
   не тронуты с 07.08 — 21 сутки. Прецедент готов: ADR-133 сделал ровно это для Tier-C реестром,
   сгенерированным из аудита, и решение владельца по нему уже есть. Повторение приёма на Tier-B
   даёт **500 / 736 = 68 %** за одну доставку и одну карточку владельцу.
3. **83 модуля вне тиров** — знаменатель дрейфует, метрика структурно неполна.
4. **Клон-фабрика** — 36 плоских `*_gross_of_*_base_gap_analyzer.py` + 17 в подпакете `gross_of/`.
   Не тронута с 02.08. Сегодня она же засветилась в CI: `spa_core/analytics/gross_of` — один из
   четырёх каталогов, которые CI гейтит, а предписанная команда не называет.

### Что двигает к 90 % дальше (по стоимости шага)

1. **Снять девять мёртвых захватов** — минуты, разблокирует Tier-C и сторожа аудита.
2. **Tier-B по образцу ADR-133** — одна карточка владельцу + один сгенерированный реестр:
   19,8 % → **68 %**. Это единственный шаг, который в одиночку приближает к цели.
3. **Внести 83 внетировых модуля в реестр** — иначе даже 68 % будут посчитаны не от всего корпуса.
4. **Починить `SPA Tests`** — 16 падений, аналитики среди них нет, но пока main красный,
   любая доставка едет вслепую.

Статус карточки **не меняю** (`in-progress`): 19,8 % против цели 90 %. Инвариант №14 соблюдён.

## Ежедневный повторный аудит — 2026-08-29 (после дневного цикла 07:49 UTC)

### Главное: вторые сутки подряд метрика не сдвинулась ни на один модуль

Три тира прогнаны в чистом sandbox-worktree от `origin/main` (`SPA_DATA_DIR` уведён в песочницу,
живой `data/` и `spa.db` не тронуты). Числа **побайтово те же, что 28.08**:

| корзина | 27.08 | 28.08 | 29.08 | Δ за сутки |
|---|---|---|---|---|
| sensitive | 116 | 116 | **116** | 0 |
| blind_equal_wide_ok | 21 | 21 | **21** | 0 |
| unchecked | 302 | 302 | **302** | 0 |
| blind_constant | 93 | 93 | **93** | 0 |
| blind_equal | 82 | 82 | **82** | 0 |
| dormant | 52 | 52 | **52** | 0 |
| failed | 5 | 5 | **5** | 0 |

Сумма 671 = весь реестр тиров (A 12 · B 479 · C 180). Метрика:
**116 + 21 + 9 честно списанных Tier-C = 146 / 736 = 19,8 %**. Δ = **0** вторые сутки.

Это не деградация, но и не работа: за двое суток слой аналитики не получил ни одной правки,
двигающей метрику. Все доставки 28–29.08 (циклы #419–#422) ушли в сторожей ответа владельца,
пульс петли и предписанный прогон — ни одна не касалась протокол-слепоты.

### Живые сигналы: честны, протоколы различают

`data/analytics_signals_blocking.json` (Tier A, 07:49): 12 модулей, **все 12 ok**.
`data/analytics_signals_advisory.json` (Tier B, 09:27): 479 модулей, 95 ok / 140 unchecked /
48 dormant / 166 blind / 30 unsourced. Восемь протоколов дают **восемь разных** composite-risk:
aave_v3 35,84 · compound_v3 36,48 · spark_susds 37,22 · morpho_blue 37,59 · euler_v2 39,44 ·
yearn_v3 39,70 · maple 40,19 · pendle 40,29. Старая авария «у всех 6,7 / mult 1,433» не вернулась.

**Tier-C (`analytics_report_full.json`, 03:00) честно отказывается:** вердикт
`protocol_differentiation = UNCHECKED`, причина — «ни один Tier-C модуль не ответил ни для одного
протокола — различать нечего». В «работающие» не засчитан. Списано 9, unchecked 162, dormant 4,
failed 5.

### Цепочка origin → рантайм: ЦЕЛА для кода, РАЗОРВАНА для CLAUDE.md

`spa_core/analytics/_protocol_facts.py` на месте, `git diff origin/main -- spa_core/analytics/`
не содержит ни одного удаления — то есть на origin нет ничего, чего нет в рантайме. Обратный
разрыв есть, но безобидный: три **тестовых** файла в `spa_core/analytics/gross_of/`
(`test_..._lp_amm_fee_drag`, `..._lst_peg_slippage`, `..._referral_affiliate_fee`, 2707 строк)
живут в рабочем дереве и **отсутствуют на origin**. Модулей реестра это не касается,
знаменатель 736 не двигает.

**А вот `CLAUDE.md` разошёлся по существу.** На origin (строка 146) предписанная команда прогона
уже исправлена циклом #422:

```
SPA_ENV=ci PYTHONHASHSEED=0 python3 -m pytest tests/ spa_core/tests/ scripts/tests/ spa_core/analytics/gross_of/ research/cards/ -q --tb=short -p no:randomly
```

В хост-дереве `CLAUDE.md` по-прежнему несёт старое `python3 -m pytest spa_core/tests/ -v`.
`code_sync_from_origin.sh` возит только `spa_core/scripts/tests/architecture` — `CLAUDE.md` в этот
список не входит **by design**. Значит каждая сессия, стартующая с хоста, читает инструкцию,
которая на два каталога и две переменные отстаёт от того, что гейтит CI. Ровно тот отказ, который
цикл #422 и чинил.

### Флот: 79 агентов, 76 OK, 3 WARNING

`agent_health.json` (09:33): overall WARNING, healthy 76 / warning 3 / critical 0.

* `com.spa.monthly_statement` — «log missing (never ran?)», категория monthly;
* `com.spa.reboot_verify` — `last_exit=1` (тот же залипший код выхода с 28.08: гонка с apiserver
  на старте, повторный прогон вчера дал HEALTHY/200);
* `com.spa.source_discovery` — лог протух 34,1 ч при пороге 26 ч.

`log_age_min = null` у **всех 79** записей — поле не заполняется ни для одного агента, включая
здоровые; «свежесть лога» как признак сегодня не измеряется вовсе (WARNING у трёх выставлен по
другим признакам). `system_health.json` (06:00): overall WARNING — d1 pipeline, d2 connectivity,
d5 code_integrity, d_riskwire. `cycle_health.json`: **HEALTHY**, разрыв цикла 1,82 ч при пороге 26,
equity 101 123,06 (+0,011 % за сутки), stale-файлов нет; `evidence_vs_curve` — WARNING.
`fleet_parity_status: DRIFT`, `capital_efficiency: WARNING` (простой сверх буфера 0,05 %).

**Здоровье флота: 76 / 79 = 96,2 %.**

### CI на origin/main — КРАСНЫЙ, падений стало 18 (было 16)

Последний завершённый `SPA Tests` (sha `a6af3c496`, 07:31): **18 failed / 97 708 passed**.
Прогоны на `40f382675` и `67740ba98` ещё идут. **Аналитики среди падений НЕТ** ни одной.

Восемь из 18 — тот самый `test_prescribed_run_matches_ci`, и он покраснел на коммите, который
**старше** доставки #422; на origin `CLAUDE.md` уже исправлен, так что эти восемь должны погаснуть
на идущих прогонах. Остальные десять — `test_doc_drift` (sUSDS-правило подано как действующее в
живом документе), `test_knowledge_graph_connectivity` (`.claude/rules/design-docs.md` недостижим),
четыре телеграм-сторожа дедупликации, `test_session_anchor_proves_nothing`,
`test_state_journal_coverage_audit`, `test_tracker_board_matches_cards`,
`test_unwired_scripts_ratchet`.

### Находка дня: у `deployment_acceptance` по-прежнему НЕТ вызова — и вчерашняя проверка едва не дала ложное «починено»

Вчера я завёл карточку `inbox-storozh-sposoben-li-flot-startovat-nikem`. Сегодня наивный `grep`
по имени показал «вызывающий: `scripts/cycle_analytics_audit.py`» — и это было бы ложное
«подключено». В `cycle_analytics_audit.py` имя встречается **только в docstring**:
«КОДЫ ВОЗВРАТА (как у `deployment_acceptance`, чтобы не заводить второй словарь)». Это ссылка на
соглашение, а не вызов.

Полный обход дерева `origin/main` даёт: ни одного plist, ни одного шага цикла, ни одного workflow.
Все живые упоминания — тесты, проза карточек и один cross-import **вспомогательной функции** из
`deployment_drift_monitor`. Сторож, отвечающий на вопрос «способен ли флот вообще стартовать»,
запускается ровно тогда, когда о нём вспомнит сессия. Прогнал вручную сегодня — **OK, exit 0**:
82 entrypoints исполнимы, 0 сломанных, 69 агентов импортируют свою цель, 0 просроченных
артефактов, 13 точек входа честно названы «вне разбора». Карточка остаётся открытой.

**Урок для следующего аудита:** проверять проводку не совпадением имени, а формой вызова
(`-m <модуль>` / `import` / `subprocess`) — иначе документация про сторожа читается как сторож.

### Ратчет непроводки покраснел: три новых скрипта никто не зовёт

`test_unwired_scripts_ratchet` называет три новых имени: `agent_quarantine`,
`build_knowledge_graph`, `tracker_counts`. Проверено по форме вызова на origin:
`build_knowledge_graph` **вызов имеет** (`scripts/update_system_briefing.py`), а
`agent_quarantine` и `tracker_counts` — **ноль вызывающих**. Это ровно директива владельца
«90 % должно работать»: новый инструмент рождается неподключённым. `agent_quarantine`
особенно заметен — рядом лежит карточка владельца `own-36-partiya-agentov-v-karantin`
(«девять агентов не читает никто»), то есть инструмент под уже поставленный вопрос написан
и не подключён.

### Очередь владельца: origin говорит 0, хост говорит 20 — расходятся ПОЛНОСТЬЮ

Замер по карточкам (не по доске), обе стороны:

* **origin/main `96f012e16`: 754 карточки, `needs-owner` = 0**, мёртвых захватов 7
  (`pid85035`, `cycle-28258`, `cycle-81141`, `cycle-51123`, `pid43119`, `cycle-352`, `pid66130`),
  живых 0, ещё 3 держат метки `interactive-session-*` (по метке живость не измеряется);
* **хост: 536 карточек, `needs-owner` = 20**, захватов с pid нет вовсе, 5 меток
  `interactive-session-2026-08-27`.

Проверил каждую из 20: **все 20 существуют на origin и все 20 там `ingested`** — то есть владелец
на них уже ответил, ответы инжестированы, а хост-копии просто никогда не получили обновления
(`nimbalyst-local/` не синхронизируется by design). Реальная очередь владельца — **пустая**,
и это хорошая новость.

Но цена есть: `CLAUDE.md` велит каждой сессии читать `_BOARD.md` **первым**, а хостовая доска
показывает 20 фантомных «ждёт владельца» и 15 захватов при 5 реальных в карточках. Отсюда же
красный `test_tracker_board_matches_cards` в CI. Мёртвых захватов, кстати, стало **7 против 9**
вчера — на два меньше.

### Что застряло (и держит 90 % на месте)

1. **Tier-B: `unchecked` 140 + `blind_constant` 84 + `blind_equal` 82 + `dormant` 48 = 354 модуля**
   не тронуты с 07.08 — **22 суток**. Прецедент готов: ADR-133 сделал ровно это для Tier-C
   реестром из аудита, решение владельца по нему есть. Повторение приёма даёт **500 / 736 = 68 %**
   за одну доставку и одну карточку.
2. **Семь мёртвых захватов на origin**, среди них `cycle-87477` (Tier-C, с 06.08 — **23 суток**)
   и `cycle-367` (сторож самого аудита). Пока захват висит, Tier-C никто не возьмёт.
3. **83 модуля вне тиров** — знаменатель структурно неполон.
4. **Клон-фабрика** — 36 плоских `*_gross_of_*_base_gap_analyzer.py` + 17 в `gross_of/`,
   не тронута с 02.08.
5. **`deployment_acceptance` без проводки** — вторые сутки, карточка открыта.

### Что двигает к 90 % дальше (по стоимости шага)

1. **Tier-B по образцу ADR-133** — одна карточка владельцу + один сгенерированный реестр:
   19,8 % → **68 %**. Единственный шаг, который в одиночку приближает к цели.
2. **Снять семь мёртвых захватов на origin** — минуты, разблокирует Tier-C и сторожа аудита.
3. **Внести 83 внетировых модуля в реестр** — иначе и 68 % будут посчитаны не от всего корпуса.
4. **Подключить `deployment_acceptance`, `agent_quarantine`, `tracker_counts`** — три сторожа/
   инструмента, написанных и не позванных.
5. **`CLAUDE.md` на хосте отстаёт от origin** — сессии читают инструкцию прогона, которая не
   совпадает с CI. Либо возить `CLAUDE.md` синхронизацией, либо не держать в нём команду,
   которая обязана совпадать с workflow.

Статус карточки **не меняю** (`in-progress`): 19,8 % против цели 90 %. Инвариант №14 соблюдён.

---

## Ежедневный аудит 90 % — 2026-08-30 (замер в песочнице от `origin/main` db022323f)

### Метрика «% работающего слоя» — строгий дифференциальный тест

| Класс | A (12) | B (479) | C (180) | Всего | % от 672 | Δ с 29.08 |
|---|---:|---:|---:|---:|---:|---:|
| **sensitive** (сигнал зависит от протокола) | 10 | 106 | 0 | **116** | **17,3 %** | **0** |
| `blind_equal_wide_ok` (честный coarse-проход) | 2 | 19 | 0 | 21 | 3,1 % | 0 |
| `unchecked` | 0 | 140 | 162 | 302 | 44,9 % | 0 |
| `blind_constant` | 0 | 84 | 9 | 93 | 13,8 % | 0 |
| `blind_equal` | 0 | 82 | 0 | 82 | 12,2 % | 0 |
| `dormant` | 0 | 48 | 4 | 52 | 7,7 % | 0 |
| `failed` | 0 | 0 | 5 | 5 | 0,7 % | 0 |

Сумма корзин = **671** = `get_tier_counts()` целиком. Знаменатель — **672** по ADR-165
(671 реестр + `bts_exit_monitor`; 82 внетировых исключены с измеренной причиной).

**Исполнено сегодня: 116 + 21 + 9 (Tier-C, ADR-133) = 146 / 672 = 21,7 %** (по старому
знаменателю 736 — 19,8 %). **Δ = 0 модулей третьи сутки подряд.**

### Главное: ответ владельца получен 12 часов назад и НЕ ИСПОЛНЕН — это одна строка

29.08 в 21:01Z владелец ответил по обеим карточкам Tier-B (инжест 30.08 00:59Z):

| Карточка | Ответ | Что это значит |
|---|---|---|
| `owner-decision-tier-b-84-modulya-otvechayut-odinakovo-d` | `owner_choice: 1` | **списать 84** — перестать исполнять, убрать числа из советующего сигнала |
| `owner-decision-tier-b-esche-82-modulya-razlichii-ne-pok` | `owner_choice: 3` | **доизмерить и вернуться с полной уликой** — НЕ списание, а заказ на работу |

Реестр `spa_core/analytics/_tier_b_writeoff.py` на origin (`WRITTEN_OFF` 84 ·
`BLIND_ACROSS_WIDE` 82 · `UNKNOWN_FROZEN` 140 · `DORMANT` 48 = 354 имени), сгенерирован
замером 10:26Z — то есть **за 10 часов ДО ответа владельца**. Его собственная шапка говорит
прямо: «Файл НИЧЕГО не исполняет и ничего не отключает сам по себе. Прекращение исполнения —
отдельное решение владельца и **отдельная строка в `signal_aggregator`**, как это было с Tier-C».

**Этой строки нет.** Полный обход дерева `origin/main` по имени `_tier_b_writeoff` даёт: два
тест-файла, ADR-165, INDEX, журнал W35 и генератор. **Ноль вызывающих в рантайме.** Для
сравнения — Tier-C позван: `signal_aggregator.py:92` импортирует `TIER_C_WRITTEN_OFF`,
строка 903 отсеивает списанные и пишет каждому статус `written_off` с причиной.

Живой сигнал подтверждает, что решение не исполняется: `data/analytics_signals_advisory.json`
(30.08 07:28Z) по-прежнему гоняет все **479** модулей, `blind: 166`.

**Цена бездействия: +84 модуля = 230 / 672 = 34,2 %.** Доступно СЕГОДНЯ, без единого нового
замера, по уже полученному ответу владельца. Это единственный шаг, который сейчас двигает
метрику вообще.

**Ловушка для того, кто возьмёт.** `spa_core/tests/test_tier_b_writeoff.py` (строки 21–22)
держит границу «Tier-B-реестр НЕ ДОЛЖЕН быть подключён к агрегатору **до ответа владельца**»
и краснеет при молчаливом подключении. Ответ пришёл ⇒ **предпосылка сторожа истекла**.
Обновлять сторожа надо В ТОМ ЖЕ изменении (инвариант №16: обоснование в теле + запись в
журнал), иначе исполнение прямого решения владельца покрасит CI и будет выглядеть как
нарушение. Известный класс: «предпосылка отложенной починки протухает».

### 82 модуля списывать НЕЛЬЗЯ

Владелец выбрал вариант 3 — «сначала доизмерить». Потолок **46,4 %** (INDEX уже поправил
вчерашние «68 %», которые смешивали списанное с замороженным) достижим только после этого
доизмерения и **нового** ответа владельца. Засчитывать 82 в работающие сейчас — подлог.

### Tier-C: живой вердикт честный — UNCHECKED

`data/analytics_report_full.json` (30.08 03:00Z, 171 модуль):
`_meta.protocol_differentiation.verdict = **UNCHECKED**`, причина — «ни один Tier-C модуль не
ответил ни для одного протокола — различать нечего». В работающие не засчитано, `avg_score`
к протоколу не относится.

Песочничный замер `--tier C`: **9** `blind_constant` · **162** `unchecked` · **4** `dormant` ·
**5** `failed`, sensitive = **0**. Против замера цикла #131 (9 / 103 / 64 / 4): `failed`
64 → 5 — но освободившиеся ушли не в работу, а в `unchecked` (103 → 162). **Работающих не
прибавилось ни одного.**

### Живые сигналы честны, протоколы различают

- **Tier-A** (`analytics_signals_blocking.json`, 06:09Z, 12/12 ok): 5 протоколов —
  `aave_v3` WARN 56,8 · `maple`/`fluid_fusdc`/`fluid_usdc` WARN 67,5 · `spark_susds` OK 35,3.
- **Tier-B** (`analytics_signals_advisory.json`, 07:28Z, 479 модулей, ok 94): 8 протоколов,
  **8 различных** скоров 36,07…40,62, mult 1,0938…1,1393. Старый провал (все 6,7 / mult 1,433)
  **не воспроизводится** — слой действительно различает протоколы.

### Цепочка origin → рантайм: ЦЕЛА

Все пять новых файлов слоя (`_tier_b_writeoff.py`, `_tier_c_writeoff.py`,
`_protocol_blindness.py`, `scripts/generate_tier_writeoff.py`,
`scripts/audit_untiered_analytics.py`) есть в прод-дереве и **байт-в-байт равны origin**.
Единственное расхождение в `spa_core/analytics/` — 3 локальных `test_*_gross_of_*.py`
в `gross_of/`, которых нет на origin (незапушенная локальная работа, метрики не касается).

### Флот и здоровье

- **`agent_health.json`** (07:35Z): **76 OK / 2 WARNING / 1 CRITICAL из 79 = 96,2 % здоровых**;
  общий статус CRITICAL из-за одного агента.
  - CRITICAL `com.spa.source_discovery` — лог протух 2,3 суток (порог 2,2);
  - WARNING `com.spa.monthly_statement` — лога нет вовсе («ни разу не запускался»);
  - WARNING `com.spa.reboot_verify` — `last_exit=1`.
- **`system_health.json`** (06:00Z) — WARNING: 0 CRITICAL / 6 WARNING / 39 OK. Из
  `system_issues`, что относится к директиве 90 %:
  - **fleet parity DRIFT: 29 orphan-plist + 5 declared-not-running** — заявленный состав
    флота и живой расходятся на 34 позиции;
  - **28,7 % капитала простаивает НЕОБЪЯСНЁННО, ~143 bps/год** — прямое нарушение ADR-055
    («кэш сверх 5 %-буфера ОБЯЗАН быть объяснён каждый цикл»);
  - `com.spa.familyfund` работает кодом от 28.08 при дереве от 29.08 — разрыв 1,5 суток;
  - `portfolio_health` 62,1/100 (порог 70).
- **`cycle_health.json`** — HEALTHY: gap 2,0 ч, equity 101 127,87 (+0,0076 %). Одно WARNING:
  `evidence_vs_curve` — 21 из 52 дат расходятся, максимум $215,99 (own-32).

### CI на origin/main — SPA Tests КРАСНЫЙ

Пять последних **завершённых** прогонов подряд `failure`: `e53ce497`, `804c9e93`, `c009c9b0`,
`02153e1e`, `48bbd571`. На `e874c24e` прогон ещё идёт. Остальные workflow на `db022323f`
зелёные (Lint LLM-forbidden · генерируемые доки · Proof Gate).

### Мёртвые захваты: 9 на незакрытых карточках (вчера 7)

`cycle-352`, `cycle-51123`, `cycle-81141`, `cycle-84821` (все `new`);
`interactive-session-2026-08-27` ×2, `interactive-session-2026-08-28`, `pid43119`, `pid66130`
(все `in-progress`). Всего захватов на origin 161, живых 3 (все `cycle-663`) — остальные
на карточках `done`/`ingested`, где захват по правилу уже не действует.

### Что двигает к 90 % дальше (по стоимости шага)

1. **Исполнить полученный ответ владельца по 84 модулям** — одна строка в
   `signal_aggregator` + обновление истёкшего сторожа `test_tier_b_writeoff.py`.
   **21,7 % → 34,2 %.** Единственный шаг, доступный без новых замеров и без новых вопросов.
2. **Доизмерить 82 модуля** (вариант 3 владельца) и вернуться с полной уликой — потолок 46,4 %.
3. **Tier-C: 162 `unchecked`** — «позвать нечем». Пока не разобрано, 24 % знаменателя мертвы.
4. **Снять 9 мёртвых захватов** — минуты.
5. **Починить красный `SPA Tests` на main** — пятый завершённый прогон подряд.
6. **28,7 % необъяснённого простоя капитала** — нарушение ADR-055, к аналитике отношения не
   имеет, но это деньги.

Статус карточки **не меняю** (`in-progress`): 21,7 % против цели 90 %. Инвариант №14 соблюдён.

---

## 2026-08-31 — движение по указанию владельца «двигайся к 90» (доставлено)

### Доставлено (4 коммита, все файлы сверены байт-в-байт на origin)

1. **ADR-189** (`b9115475` + `e38d6ece`) — **ответ владельца по Tier-B ИСПОЛНЕН**.
   Реестр `_tier_b_writeoff.py` лежал на origin с 10:26Z 29.08 — за 10 часов ДО ответа
   владельца — и по собственной шапке ничего не отключал. Строки в `signal_aggregator`
   не было; решение жило ровно в карточке. Метрика **21,7 % → 34,2 %**.
   Решающий контроль: сигналы ПОБАЙТНО тождественны, 93/93 исполняемых,
   `blind 166 → written_off 84 + blind 82`. Это бухгалтерия, не изменение сигнала.
2. **main зелёный** (`9c566c02`) — два предсуществующих красных храповика документов
   (подтверждены контролем на пришпиленном `e49641f0e` поимённо, регрессий 0):
   храповик дат (маркер `injected-clock`, обоснован ЗАМЕРОМ — файл даёт 9 passed под
   часами +365 дней) и покрытие хроники (`MISSING 2 → 0`).
3. **ADR-190** (`1dda0fd9`) — **перепись, закрывающая вопрос о 90 %** + карточка владельцу.

### Главное: инженерного пути к 90 % НЕТ (измерено, а не предположено)

Месяц мерили метрику и ни разу не спросили, достижимы ли 90 % вообще. Все планы —
включая мой вчерашний аудит — исходили из того, что остаток это работающий код без
проводки. **Предположение ложно:**

| Путь | Измеренный прирост |
|---|---:|
| честная проводка (3 `WIRABLE` из 659, все три уже работают) | **0** |
| проводка «непроверяемых» (302, `WIRABLE` среди них) | **0** |
| починка 20 неверных привязок в реестре (датакласс вместо анализатора) | **0** |
| передача профиля 163 модулям с предметным аргументом | **запрещено** |

Последнее — самое важное: соблазн выглядит как очевидная починка, но подсунутый профиль
даёт **правдоподобно различающееся число о неизмеренном**, и метрика скакнула бы на лжи.
Запрет записан в самом инструменте пригодности; его пример — анализатор спреда, дающий
60/51/36 по трём протоколам при НУЛЕВОМ покрытии своих ключей.

### Исполнено прямое указание владельца от 29.08 (вариант 3)

Владелец велел «сначала доизмерить те 82 и вернуться с полной уликой». Сделано: второй,
более строгий инструмент подтвердил `BLIND` у **71 из 82**; у 11 вердикта нет (5 `RAISES`,
4 `SHAPE_NOT_PROBED`, 2 `NO_SCORE`) — записано как НЕ ИЗМЕРЕНО, а не «работают». Сила
улики названа точно: подтверждение на другом ВХОДЕ, не независимый метод.
Карточка `owner-decision-doizmerili-te-82-modulya-kak-ty-prosil-7` — 3 кнопки,
рекомендация вариант 1.

### Лестница к 90 % (остаток берётся решениями владельца, не кодом)

| Шаг | % | Чем берётся |
|---|---:|---|
| сегодня | **34,2 %** | доставлено |
| исключить 139 «не модулей сигнала» из знаменателя (приём ADR-165) | 43,2 % | замер, **не прогресс** |
| + списать 71 подтверждённо слепых | 56,5 % | **ответ владельца** (карточка отправлена) |
| + списать 52 нечитаемых и 5 падающих | 67,2 % | **ответ владельца** |
| до 90 % от 533 | 90 % | остаётся разобрать **122** |

### Что НЕ сделано намеренно

- Знаменатель не исправлен: сначала нужен реестр 139 имён с измеренной причиной
  (как `_untiered_census.py`), иначе исключение держится на прогоне, а не на файле.
- 20 неверных привязок не починены — измеренный эффект ноль.
- Ни один модуль не списан без владельца (инвариант №14).

Статус карточки **не меняю** (`in-progress`): 34,2 % против цели 90 %.

---

## 2026-08-31 08:45 UTC — ежедневный аудит после дневного цикла (независимая перепроверка)

Замер сделан **заново**, в чистом worktree из `origin/main 4acf8008e` (`/tmp/spa_audit_0831`),
все три тира, живой `data/` не тронут. Это не пересказ ADR-189/190 — это контроль их числа.

### Метрика: 34,3 % (230 из 671). Вчера 21,7 %

| Тир | всего | protocol-sensitive | wide_ok | blind_equal | blind_constant | unchecked | dormant | failed |
|---|---:|---:|---:|---:|---:|---:|---:|---:|
| A | 12 | 10 | 2 | 0 | 0 | 0 | 0 | 0 |
| B | 479 | 106 | 19 | 82 | 84 | 140 | 48 | 0 |
| C | 180 | **0** | 0 | 0 | 9 | 162 | 4 | 5 |

Числитель = 137 работающих (A 12 + B 125) + 93 честно списанных (B 84 + C 9) = **230**.
Цифра ADR-189 (230/672 = 34,2 %) **подтверждена** независимым прогоном; расхождение
знаменателя на 1 — в счёте, не в существе.

### Главное, что нельзя смазать: работающих модулей не прибавилось НИ ОДНОГО

Прирост +12,6 п.п. за сутки целиком дала **бухгалтерия** — исполнение ответа владельца по
84 модулям (ADR-189). Число реально протокол-чувствительных модулей — **137, ровно как вчера**.
Метрика выросла, система считать лучше не стала. Это ровно то, о чём предупреждает ADR-190:
дальше метрику двигают решения владельца, а не код.

### Tier-C: живой вердикт самого прогона — `UNCHECKED`

`data/analytics_report_full.json` → `_meta.protocol_differentiation.verdict = "UNCHECKED"`,
причина дословно: «ни один Tier-C модуль не ответил ни для одного протокола — различать нечего».
Sandbox-прогон согласен: `sensitive = 0`, 162 из 180 `unchecked`. **27 % знаменателя мертвы**
и в числитель не входят — ни как работающие, ни как списанные.

### Цепочка origin → рантайм по аналитике ЦЕЛА (проверено эффектом, не датой файла)

`data/analytics_signals_advisory.json` записан 07:28:35Z и уже несёт
`module_status.counts.written_off = 84` — счёт, который умеет производить ТОЛЬКО код ADR-189.
Значит `signal_aggregator` в проде исполняет новый код. Производители сигналов —
`agent_analytics_tier_b.sh` / `agent_analytics_tier_c.sh`, отдельные короткоживущие агенты.

### Разрыв «доставлено ≠ исполняется» — есть, но НЕ в аналитике (три долгоживущих агента)

`agent_health` называет три процесса с `KeepAlive`, исполняющих старый код и неспособных
подхватить новый никогда:

| агент | код от | разрыв | самый новый непринятый файл |
|---|---|---:|---|
| `com.spa.rtmr_sense` | 28.08 | 2,5 сут | `spa_core/telegram/buttonless_audit.py` |
| `com.spa.apiserver` | 29.08 | 1,9 сут | `spa_core/analytics/signal_aggregator.py` |
| `com.spa.familyfund` | 28.08 | 1,5 сут | `spa_core/family_fund/api/app.py` |

Строка `signal_aggregator.py` у apiserver выглядит как провал аналитики — **это не так**:
сигналы пишут другие агенты (см. выше), apiserver их только отдаёт наружу. Задет API/дашборд,
не метрика. Перезапуск — решение владельца (правило доставки, п. 6), сам не двигаю.

### Флот: 97,4 % по пульсу — но пульс это не работа

76 агентов: **74 OK / 2 WARNING / 0 CRITICAL** (`com.spa.monthly_statement` — лога нет,
никогда не запускался; `com.spa.reboot_verify` — `last_exit=1`). Ни одного незагруженного,
логов старше суток нет. Рядом тот же монитор говорит `fleet parity DRIFT`:
**7 osirotевших plist + 6 объявленных, но не запущенных**. Здоровым считается тот, кто
подаёт признаки жизни, а не тот, чей продукт кто-то читает (ср. `own-36-partiya-agentov-v-karantin`).

### CI на main: «зелёный» пока НЕ подтверждён CI

Последний **завершённый** прогон `SPA Tests` на main — `b9115475` (06:27Z), **failure**,
упал job `test (3.11)`, шаг «Run spa_core unit tests». Это коммит ADR-189, ДО починки.
Прогоны для 7 более новых коммитов, включая `9c566c02` (где чинили два храповика),
висят `queued`/`in_progress`. `SPA Proof Gate` за утро отменён 4 раза подряд — каждый
следующий пуш вытесняет предыдущий прогон. За утро ~15 пушей; CI физически не успевает
дать вердикт ни одному. **Запись «main зелёный» держится на локальном прогоне, не на CI.**

### Деньги (к аналитике не относится, но это трек)

`capital-efficiency LAZY`: 17,4 % капитала простаивает необъяснённо после атрибуции,
≈148 bps/год упущено. Портфель $101 149, cash $22 368 при буфере 5 %. Нарушение ADR-055.

### Что двигает к 90 % дальше

Инженерных шагов не осталось — это доказано ADR-190. Очередь целиком у владельца:

1. **Ответ на `owner-decision-doizmerili-te-82-modulya-kak-ty-prosil-7`** — списание 71
   подтверждённо слепых. **34,3 % → ~56 %.** Карточка **ни разу не отправлена владельцу**
   (`owner_decision_pending` называет её прямо) — это узкое место сегодня, а не код.
2. Реестр 139 «не модулей сигнала» с измеренной причиной → знаменатель 533 (замер, не прогресс).
3. Tier-C 162 `unchecked` — «позвать нечем»; пока не разобрано, 27 % знаменателя мертвы.
4. Перезапуск трёх долгоживущих агентов (решение владельца).
5. Дать CI успеть: пушить пачками, а не по коммиту каждые 15 минут.

Мелочь на память: в рабочем дереве три файла `spa_core/analytics/gross_of/test_*_gross_of_*.py`
есть локально и отсутствуют на origin — незапушенные тесты клон-семейства, не разрыв доставки.

Статус карточки **не меняю** (`in-progress`): 34,3 % против цели 90 %. Инвариант №14 соблюдён.

### ПОПРАВКА к секции выше (08:50 UTC) — владелец ответил, пока шёл замер

Строка «карточка `owner-decision-doizmerili-te-82-modulya…` ни разу не отправлена, это узкое
место» **устарела через 20 минут после того, как была написана**. Владелец ответил кнопкой
в **08:33:24Z** (`owner_choice: 3`, via telegram) — мой замер снят в 08:12Z.

Ответ лежал только в живом дереве; на origin карточка оставалась `needs-owner` без поля.
След доставлен названным маршрутом `owner_answer_delivery` (провенанс 3) — теперь
`owner_choice: 3` виден на origin. Статус на origin **не двигаю** (инвариант №14).

**Выбран вариант 3 — не списывать ничего, сначала починить те 11.** Следствия, которые надо
понимать буквально:

- **Метрика остаётся 34,3 % и это решение, а не затык.** Лестница «34,3 % → ~56 %» через
  списание 71 модуля **отменена владельцем**. Планировать от неё больше нельзя.
- Работа, которую ответ открыл: доизмерить 11 модулей без вердикта — 5 `RAISES`,
  4 `SHAPE_NOT_PROBED`, 2 `NO_SCORE`. После этого решение по всем 82 принимается на полной улике.
- Пункт 1 в списке «что двигает к 90 %» выше читать как **закрытый**; актуальный первый шаг —
  эти 11.

Побочно измерено при доставке: `telegram_owner_decisions.json` записи об отправке этой карточки
**не содержит**, хотя ответ пришёл `via: telegram`. То есть журнал отправок и факт отправки
разошлись — сам по себе он не доказывает «владелец не видел» (класс
`card-not-on-origin-is-not-owner-never-saw-it`, здесь — обратная сторона того же).

---

## 2026-08-31 12:05 UTC — разбор 11 по ответу владельца (вариант 3) → ADR-194, коммит `93364afb`

Владелец велел «сначала починить те 11». Разбор пошёл не туда, куда предполагалось, и это
оказалось важнее самих 11.

### Гипотеза, отвергнутая СВОИМ ЖЕ контролем до написания реестра

Первая версия: 11 — «не модули сигнала», их предмет не протокол (портфель, позиции,
транзакция, маршрут, оракул, ряд чисел). Объясняла все 11 и уже собиралась в реестр по
образцу `_untiered_census.py`.

Прогон критерия **по всей популяции**: из 137 работающих **34 (25 %)** объявляют вход как
`position`/`positions`/`portfolio`/`oracles`. У пяти из 11 есть работающие близнецы с ТЕМ ЖЕ
именем параметра и той же аннотацией: `positions` — 10, `context` без аннотации — **28**,
`profile` — 1. Реестр на этом критерии был бы ложью в 34 случаях. Отброшен.

### Находка: у подтверждающего инструмента 7,8 % ложных BLIND

Прогон ВТОРОГО инструмента по 115 модулям, которых ПЕРВЫЙ под РЕАЛЬНЫМ входом агрегатора
измеряет протокол-различающими:

| вердикт на эталоне | шт |
|---|---:|
| `SHAPE_NOT_PROBED` | 37 |
| `UNCOVERED` | 31 |
| `COVERAGE_UNMEASURED` | 25 |
| **`BLIND`** | **9** |
| `RAISES` | 8 |
| `NO_SCORE` | 3 |
| `WIRABLE` | 2 |

**Ложный `BLIND`: 9 из 115 = 7,8 %**, разбросы у них 63,3 · 30,1 · 15,0 · 5,0 · 2,5 · 0,42 —
не пограничные случаи. Списание 71 (вариант 1) похоронило бы **5–6 работающих** модулей, и
узнать было бы неоткуда: списанный модуль не исполняется и себя не опровергнет.

**Ни один вердикт «нет ответа» не является свойством модуля:** `SHAPE_NOT_PROBED` получают 37
работающих, `RAISES` — 8, `NO_SCORE` — 3. То есть **4 из «одиннадцати» стоят в одной корзине с
37 модулями, которые крутятся в проде каждый день, — в них чинить нечего.**

Вывод узкий, инструмент не обвиняется: он и первый кормят модуль РАЗНЫМИ входами, разные числа
законны. Но `BLIND` от него **не устанавливает**, что модуль не читает протокол в проде.

### Что это значит для выбора владельца

Вариант 3 («не списывать ничего») оказался верным — но по причине, которой на карточке не было.
Дело не в том, что 11 не измерены, а в том, что **инструмент, выдавший остальные 71 вердикта,
имеет измеренную цену ошибки**. Отдельно: `defi_protocol_yield_after_tax_drag_analyzer` числится
`sensitive` из-за разброса 7,1·10⁻¹⁵ — числитель 137 завышен минимум на 1.

### Доставлено

`scripts/audit_instrument_error_rate.py` (новый постоянный замер) · тесты (8) ·
проводка в суточный `cycle_analytics_audit` · база храповика непроводки уменьшена на 1 ·
ADR-194 + реестр + STATE + журнал. 6 мутаций по координате краснеют все 6; 163 passed;
единственное красное (`test_no_NEW_unwired_script_appears`, 4 ЧУЖИХ скрипта) подтверждено
контролем на пришпиленном `db9e8d385` имя-в-имя, регрессий 0.

Полный прогон НЕ запускался осознанно: два уже идут в чужих деревьях, третий заморил бы оба.

### Метрика

**34,3 % не двигается.** Изменилась не доля работающих, а знание о том, чего стоит инструмент,
которым её собирались двигать. Статус карточки не меняю (`in-progress`), инвариант №14 соблюдён.
