---
trackerStatus:
  type: agent
title: Аудит всего проекта 2026-08-02 — три параллельных трека починки (координация, НЕ дублировать)
status: in-progress
source: session-2026-08-02-owner-audit
created: 2026-08-02
priority: high
domain: координация параллельных сессий; risk-gate + analytics + CI
---

> **КООРДИНАЦИОННАЯ КАРТОЧКА.** Владелец запустил полный аудит проекта и по его итогам —
> три параллельные интерактивные сессии починки. Фоновым сессиям (почасовой оркестратор и др.)
> эти области НЕ брать, пока карточка in-progress: работа уже идёт, коллизия = двойная работа
> (прецедент 2026-07-30). Перед взятием смежных карточек — `scripts/check_card_claim.py` +
> `python3 scripts/log_session_change.py --tail`.

## Что делаем

Полный аудит проекта (money-path, аналитика/тесты, продукт/процессы) тремя агентами-аудиторами
+ сверка жёстких цифр. Итоговая оценка владельцу выдана. По трём главным находкам запущены
отдельные сессии-фиксеры (чипы), работают в изолированных worktree в /tmp.

## Что нашли (главное)

1. **TVL-floor был декоративным**: `risk_gate.py` подставлял $20M при отсутствии живого TVL;
   ~22 адаптера отдают константу `TVL_USD` вместо живых данных (см. также
   `agent-tvl-floor-passes-on-literals`).
2. **Аналитический слой протокол-слепой**: no-arg fallback в `signal_aggregator._invoke` —
   модули считали на demo-данных, все 8 протоколов получали одинаковый скор 6.7/mult 1.433.
   Advisory-only, в hard-гейт RiskPolicy НЕ попадало (проверено).
3. **CI SPA Tests красный** на origin/main (Proof Gate и LLM-lint зелёные).
4. Прочее (не в работе, кандидаты на карточки): 9 страниц /admin без авторизации; STATE.md 10×
   больше собственного лимита; ADR в трёх папках; клон-фабрика 38 файлов
   `..._gross_of_<X>_base_gap_analyzer.py` (~30k строк = одна формула).

## Что фиксим и статус (на 2026-08-02 ~22:15 UTC)

- **TVL fail-closed** — ✅ доставлено на origin (22:01, «TVL provenance in the allocator»,
  ADR-062/ADR-053-side). Литерал больше не считается проверкой.
- **Аналитика фаза 2** — ✅ доставлено (21:55, коммит f6ec5e35c): 12/12 Tier-A подключены к
  протокол-контексту через `_protocol_facts.py`, Tier-B массовая починка 81 модуля.
  Остаток: 68 failed / 48 dormant / 203 unchecked.
- **CI red** — 🔄 в процессе: серия фиксов (18:51, 22:12 — `tests/test_signal_aggregator.py`),
  ждём зелёный прогон.

## Что дальше (закреплено владельцем)

Шаги 2–3 по аналитике возьмёт сессия-аудитор ПОСЛЕ завершения текущей сессии-фиксера:
(2) переподключение остатка Tier-B по корзинам failed/dormant/unchecked (адаптеры под
протокол-контекст, удаление только через карточку); (3) рефакторинг клон-фабрики 38 файлов в
одну параметризованную `analyze_fee_gap(...)` + конфиги, все 38 анализов сохранить.
Гайд по починке модулей — в памяти сессии и `scripts/audit_protocol_blindness.py`
(только в sandbox-checkout: прогоны грязнят data/*.json и spa.db).

## Директива владельца (2026-08-03)

**Цель — ~90% слоя должно РАБОТАТЬ, а не спать.** Повторный аудит — каждый день после дневного
цикла (свежие data/*.json), первый — 2026-08-03. Метрики: % протокол-чувствительных модулей
аналитики (сейчас ~9%) и % агентов флота, делающих реальную работу. Незадействованный код —
либо подключить, либо честно списать через карточку (не молча).

## Как понять, что готово

CI зелёный + шаги 2–3 доставлены на origin + доля работающего слоя ≥90% (или остаток явно
списан владельцем) → карточка переводится в done.

---

## Доставка карточки и сверка её статуса (автономный цикл #95, 2026-08-03 ~04:00 UTC)

**Карточки не было на `origin/main`.** Её нашёл шаг 0a: сессия `pid6859` объявила её
2026-08-02T22:18:07Z и умерла, не доставив (`[отсутствует] … на origin/main файла нет`).
Файл найден в осиротевшем worktree `/tmp/spa_wt_c94` и перенесён сюда **побайтово, без единой
правки** — всё выше этой черты написано владельческой сессией аудита, ниже — только замер.

Замер статуса на момент доставки (иначе координационная карточка `in-progress` запирает три
области бессрочно — класс «нечитаемый вердикт, который некому снять»):

- **все три сессии-фиксера измеримо мертвы** (`ps -p` пусто для `pid6859`, `pid2867`, `pid1763`,
  `pid7584`); ни одна из областей сейчас никем не удерживается;
- **трек 1 (TVL fail-closed)** — на origin, подтверждено;
- **трек 2 (аналитика фаза 2)** — на origin: `f6ec5e35c` и `19666683a` присутствуют в истории;
- **трек 3 (CI red)** — по-прежнему КРАСНЫЙ: `SPA Tests` / `SPA CI` на `49d0d1122` = failure
  (`spa_core/tests/test_e2e_integration.py`, spark APY против убранного ADR-063 fallback).
  Для него уже есть карточка `agent-spark-apy-tests-assert-a-fallback-adr-063-removed`
  (взята сессией `pid7584`, которая умерла; окно свежести захвата истекает 05:27Z —
  цикл #95 её по шагу 0b НЕ брал).
- **найден ЧЕТВЁРТЫЙ красный, о котором аудит не знал** — `SPA CI-Lite` красный с
  2026-08-02T19:14Z (mypy на `allocator.py:244`, внесено ADR-061). Это и есть задача цикла #95;
  карточка `agent-task-ci-lite-krasnyi-s-02-08-mypy-na-allocato`.

Статус карточки НЕ меняю (`in-progress` остаётся): её критерий «готово» задан владельцем и
включает шаги 2–3 по аналитике, которые не сделаны.

---

## Ежедневный повторный аудит #1 — 2026-08-03 (после дневного цикла 06:00 UTC)

Замер read-only. Tier-A и Tier-B прогнаны `scripts/audit_protocol_blindness.py` в **чистом
sandbox-worktree от `origin/main` 5255a62d4** (живой `data/` и `spa.db` не трогались).

### Главное: доставленная починка НЕ РАБОТАЕТ в проде

Прод-дерево `~/Documents/SPA_Claude` (из него `run_daily_paper_cycle.sh` запускает цикл) —
ветка **`env-setup-v3`**, HEAD `508c8912b` от **2026-07-15**, отстаёт от `origin/main` на
**411 коммитов**. Файла `spa_core/analytics/_protocol_facts.py` в нём **физически нет**.

Доказано живыми данными, а не рассуждением — `data/analytics_signals_advisory.json`
(сгенерирован сегодня 07:48Z самим прод-агентом `com.spa.analytics_tier_b`):

| | ожидалось после фазы 2 | фактически в проде сегодня |
|---|---|---|
| composite_risk_0_100 | 50.0 (честное «не знаю») | **6.7 — одинаково у всех протоколов** |
| risk_multiplier | 1.0 | **1.433** |
| confidence | 0 | 0.3198 |
| modules_ok | 200 | 166 |

Это байт-в-байт **до-фиксовая сигнатура**, названная в находке №2 этой карточки. То есть
фаза 2 живёт только на origin; на живом треке аналитика по-прежнему протокол-слепая.

Разрыв «origin → прод» уже вскрыт и закартирован параллельной сессией:
`agent-deployment-drift-guard` (critical, in-progress) + монитор `deployment_drift_monitor.py`
(коммит `acf8c9a37`). **Здесь не дублирую** — фиксирую последствие для метрики аудита:
пока разрыв не закрыт, любой процент «работающего слоя» относится к origin, а не к рантайму.

### Метрика «% работающего слоя» (на origin/main, строгий дифференциальный тест)

| Tier | Модулей | sensitive | blind_constant | blind_equal | failed | dormant | unchecked |
|---|---|---|---|---|---|---|---|
| A (blocking) | 12 | **10** | 0 | 2 | 0 | 0 | 0 |
| B (advisory) | 519 | **12** | 150 | 38 | 68 | 48 | 203 |

**Итого доказано протокол-чувствительных: 22 из 738 модулей `spa_core/analytics/` = 3.0 %.**
В рантайме — **0 %** (см. выше).

Динамика со вчера (тем же инструментом, из `docs/ANALYTICS_PROTOCOL_BLINDNESS.md`):

- Tier-B `sensitive`: **0 → 12** (+12) — первый реальный сдвиг;
- Tier-B `failed`: 149 → 68 (−81) — те самые «81 починенный модуль»;
- но из этих 81 **только 12 действительно различают протоколы**; остальные перешли
  в `dormant` (17 → 48) и `blind_equal` (0 → 38), т.е. перестали падать, но информации
  не несут;
- `blind_constant` 150 и `unchecked` 203 — **без движения**;
- Tier-A 10 sensitive / 2 blind_equal — без изменений.

**Про «~9 %» во вчерашней записи.** Строгим тестом это не воспроизводится: 9 % ≈ доля модулей,
которые перестали падать. Честная метрика для директивы владельца — доля тех, чей сигнал
измеримо зависит от протокола: **3.0 %**. Дальше в карточке считаю только её.

### Флот

`data/agent_health.json` (07:42Z): 69 агентов, **69 OK**, 0 WARN / 0 CRIT, все `loaded=true`;
свежесть логов < 1 ч — 32, 1–24 ч — 29, `log_age=None` — 8 (по ним судить нельзя, только по
state-файлу). Ненулевой `last_exit`: `apiserver` (−15), `rtmr_sense` (−15), `telegram_bot` (1).
`data/cycle_health.json` — HEALTHY (цикл 06:00Z, equity 100 712.66, +0.0156 %).
`data/system_health.json` — **overall CRITICAL**, домен `d5_code_integrity` CRITICAL
(остальные: 4× WARNING, 3× OK/INFO).

Честная рамка: 100 % «здоровья» флота здесь означает лишь «процессы живы и не падают».
Все 69 агентов исполняют код от 15.07 — работу они делают, но не ту, что доставлена.

### CI на origin/main

`SPA Tests` и `SPA CI` — **failure** на `acf8c9a37` и `6dfa1f39b`; `SPA Proof Gate` и
`SPA Lint — LLM forbidden` — success. Осталось одно падение
(`test_telegram_guard_stays_outermost`), его прямо сейчас держит цикл #97 по карточке
`agent-telegram-guard-outermost-fails-only-in-full-run` — **не трогаю**.

### Что двигает к 90 % дальше (по стоимости шага)

1. **Закрыть разрыв «origin → прод»** — без этого метрика измеряет мёртвый код.
   Решение владельца (обновление прод-дерева), карточка `agent-deployment-drift-guard`.
2. **`unchecked` 203** — самая крупная и самая дешёвая корзина: у модулей просто нет
   entrypoint'а, принимающего protocol-контекст. Либо адаптер, либо честное списание карточкой.
3. **`blind_constant` 150** (132 из них — константа `0.0`) — кандидаты №1 на списание:
   формально «ok», информации ноль.
4. **`failed` 68 + `dormant` 48** — точечная починка, дороже пункта 2.
5. **Клон-фабрика 38 файлов** `..._gross_of_<X>_base_gap_analyzer.py` — не начата.

### Что застряло

Шаги 2–3 по аналитике (переподключение остатка Tier-B, рефакторинг клон-фабрики) с 02.08
не начаты — все автономные циклы #95–#97 уходили на красный CI. Статус карточки
**не меняю** (`in-progress`): критерий «готово» задан владельцем, до 90 % далеко (3.0 %).

---

## Ежедневный повторный аудит #2 — 2026-08-07 (после дневного цикла 06:00 UTC)

Замер read-only. **Все три тира** прогнаны `scripts/audit_protocol_blindness.py` в чистом
клоне от `origin/main` **162c2e8f6** в `/tmp` (живой `data/` и `spa.db` не трогались).
Живые числа флота/сигналов взяты через live-API `:8765` (`/api/live/data/<файл>`), потому что
у этой сессии посреди прогона **пропал доступ на чтение к `~/Documents/SPA_Claude`** (см. ниже).

### Метрика «% работающего слоя» — строгий дифференциальный тест

| Класс | A (12) | B (479) | C (180) | Всего | % от 736 |
|---|---:|---:|---:|---:|---:|
| **sensitive** (сигнал зависит от протокола) | 10 | 105 | 0 | **115** | **15.6 %** |
| `blind_equal_wide_ok` (равенство доказано на 32 протоколах — честный проход) | 2 | 19 | 0 | 21 | 2.9 % |
| `unchecked` | 0 | 140 | 162 | 302 | 41.0 % |
| `blind_constant` | 0 | 84 | 9 | 93 | 12.6 % |
| `blind_equal` | 0 | 83 | 0 | 83 | 11.3 % |
| `dormant` | 0 | 48 | 4 | 52 | 7.1 % |
| `failed` | 0 | 0 | 5 | 5 | 0.7 % |

**Работает = 115 (15.6 %)**, с честными `wide_ok` — **136 (18.5 %)**. Честно списанных
(решением/карточкой) — **0**: `dormant` это «не подключён», а не «списан».

**Динамика с 03.08: 3.0 % → 15.6 %** (22 → 115 sensitive, +93 модуля). Рост настоящий и
почти весь в Tier-B (12 → 105); методика та же, тест тот же.

**Tier-C по-прежнему ровно 0 sensitive** — и это главное, что нужно понять правильно.
Против замера #131 (9 ok / 103 unchecked / **64 failed** / 4 dormant) сегодня
9 / **162** / **5** / 4: 59 модулей перестали падать, но **не заработали** — они переехали
из `failed` в `unchecked` с причиной «entrypoint requires non-mapping input, adapter supplies
Mapping». Это движение внутри корзины «не работает», в метрику оно не идёт.

**Живой Tier-C честен**: в `data/analytics_report_full.json` ключа
`_meta.protocol_differentiation` НЕТ вовсе (= UNCHECKED), а все 8 протоколов несут
идентичный `avg_score` 20.56 при `modules_ok` 9. Число к протоколу не относится — в
«работающие» не засчитано.

**Живой Tier-B/A дифференцирован** (в отличие от 02.08): `analytics_signals_blocking.json`
(A, 12/12 ok) даёт 5 разных скоров 41.9…67.5 с разными `triggered_by`;
`analytics_signals_advisory.json` (B, 479) — 8 разных `composite_risk` 35.48…40.75 с
разными `top_contributors`. Старый провал «все 6.7 / mult 1.433» не воспроизводится.

**82 публичных модуля не входят НИ В ОДИН тир** (738 публичных `.py` в `spa_core/analytics/`
против 671 в реестре тиров). Они не измеряются вообще — ни «работают», ни «не работают».
Это 11 % знаменателя, и до сегодня их никто не считал.

### Флот

`data/agent_health.json` (08:43Z): **77 агентов, 76 OK, 1 WARNING, 0 CRITICAL = 98.7 %**
(`com.spa.telegram_bot`, `last_exit=1`, перезапущен). `log_age_min = null` **у всех 77** —
судить можно только по state-файлам, не по логам. `system_issues`: fleet parity DRIFT
(26 orphan-plist, 1 declared-not-running) и «capital-efficiency LAZY: 10 % капитала простаивает
UNEXPLAINED».
`data/cycle_health.json` — HEALTHY (цикл 06:00Z, equity 100 778.48, +0.0157 %).
`data/deployment_acceptance.json` (09:21Z) — **OK**, 77/77 entrypoints исполняемы.
**GoLive 29/29, `ready: true`** (в CLAUDE.md ещё записано 27/29 — снимок устарел).

### Три находки, которых не было вчера

1. **CI на main красный с 02:21Z по одной строке.** `SPA CI` / `SPA Tests` падают на
   **сборе тестов**, не на логике: `scripts/edge_cross_sectional_demotion.py:476` —
   `f"{'k \\ M':>8s}"`, обратный слэш внутри выражения f-строки. Это **синтаксис Python 3.12**
   (PEP 701); матрица CI включает **3.11**, где это `SyntaxError`. Ошибка сбора отравляет весь
   прогон — падают `tests/test_edge_cross_sectional_demotion.py` и
   `tests/test_edge_capital_recycling.py`, дальше `Interrupted: 2 errors during collection`.
   Доставлено R&D-сессией #40/#41 в 02:21Z; локально она гоняла 3.12 и видела зелёное.
   Починка — одна строка (вынести `'k \\ M'` из выражения). **Я её не делаю**: мандат этой
   задачи — замер, а доступ к дереву у меня в этой сессии рвётся (см. ниже), и недоставленный
   пуш хуже отсутствующего.
2. **`d5.deployment.drift` CRITICAL — почти наверняка ложная тревога от гонки.**
   `system_health` сгенерирован в **06:00:01.766**, `code_sync_from_origin` завершился в
   **06:00:09.875** (`SYNCED`, 47 файлов, origin 766238822). Сторож измерил дерево **за 8 секунд
   до** синка, который сам же и делает его верным, и назвал 3 money-path файла
   (`rebalance_economics.py`, `cycle_gates.py`, `cycle_runner.py` — ровно поставка цикла #144
   в 04:04Z). До следующих суток d5 никто не пересчитывает, поэтому весь день
   `system_health.json` держит `overall: CRITICAL` и утверждает, что в проде крутится
   непроверенная risk-логика. Класс — сторож честно отвечает на свой вопрос в неверный момент.
   **Прямо подтвердить не смог**: sha256 хост-дерева не читается (см. ниже). Отдельный
   `data/deployment_drift.json` устарел на сутки (06.08 02:51, `money_path_files: []`).
3. **Сессия потеряла доступ на чтение к прод-дереву посреди работы.** `~/Documents/SPA_Claude`
   отдаёт `Operation not permitted` на **чтение содержимого** (даже `cat CLAUDE.md`, даже
   `git log`), при этом `ls` работает, а **запись в `data/` работает**. Состояние мигающее.
   Флот не пострадал (агенты и live-API читают нормально), это касается только интерактивных/
   фоновых сессий — но любая сессия, поймавшая это, тихо ослепнет, а её `git`-проверки
   выродятся в пустые строки (у меня первая же сверка sha256 дала три ложных `DIFF`,
   потому что `shasum` вернул пустоту, а сравнение этого не заметило).

### Что двигает к 90 % дальше (по стоимости шага)

1. **`unchecked` 302 (41 %)** — самая большая и самая дешёвая корзина. В Tier-C она разложена
   на две названные причины: 103 «нет entrypoint под protocol-контекст» + 59 «entrypoint хочет
   не-Mapping, адаптер даёт Mapping». Вторая — механическая, один адаптер закрывает 59 модулей.
2. **Tier-C целиком (180, 27 % реестра, 0 % работает)** — сейчас это самый большой мёртвый груз.
3. **`blind_constant` 93 + `blind_equal` 83** — кандидаты №1 на честное списание карточкой:
   формально считаются, информации о протоколе ноль.
4. **82 модуля вне тиров** — сначала внести в реестр, иначе метрика структурно неполна.
5. **Клон-фабрика 38 файлов** `..._gross_of_<X>_base_gap_analyzer.py` — не начата (с 02.08).

### Что застряло

Tier-C с 06.08 не сдвинулся по существу (0 sensitive), карточку
`inbox-tier-c-pyat-nastoyaschih-otkazov-agregat` держит `cycle-87477` с 06.08 18:43 — сутки.
Статус карточки **не меняю** (`in-progress`): до 90 % далеко (15.6 %).

---

## Ежедневный повторный аудит #3 — 2026-08-26 (после дневного цикла 17:16 UTC)

Замер read-only. Все три тира прогнаны `scripts/audit_protocol_blindness.py` в чистом
sandbox-worktree от `origin/main` **feb9cd477** (живой `data/` и `spa.db` не трогались,
`SPA_DATA_DIR` уведён в песочницу). Живые числа флота/сигналов взяты через live-API `:8765`
(`/api/live/data/<файл>`), потому что у этой сессии **снова** пропал доступ на чтение к
`~/Documents/SPA_Claude` — ровно тот же класс, что 07.08 (см. находку 4).

**Между аудитами #2 и #3 прошло 19 суток: с 07.08 ежедневный аудит не запускался ни разу.**
Это и есть предмет карточки `inbox-u-ezhednevnogo-audita-90-net-storozha-on` (держит
`cycle-367` с 24.08) — здесь не дублирую, фиксирую как причину, по которой «динамика со вчера»
на самом деле «динамика за 19 дней».

### Метрика «% работающего слоя» — строгий дифференциальный тест

| Класс | A (12) | B (479) | C (180) | Всего | % от 736 | Δ с 07.08 |
|---|---:|---:|---:|---:|---:|---:|
| **sensitive** (сигнал зависит от протокола) | 10 | 106 | 0 | **116** | **15.8 %** | **+1** |
| `blind_equal_wide_ok` (равенство доказано на 32 протоколах — честный проход) | 2 | 19 | 0 | 21 | 2.9 % | 0 |
| `unchecked` | 0 | 140 | 162 | 302 | 41.0 % | 0 |
| `blind_constant` | 0 | 84 | 9 | 93 | 12.6 % | 0 |
| `blind_equal` | 0 | 82 | 0 | 82 | 11.1 % | −1 |
| `dormant` | 0 | 48 | 4 | 52 | 7.1 % | 0 |
| `failed` | 0 | 0 | 5 | 5 | 0.7 % | 0 |

**Работает = 116 (15.8 %)**, с честными `wide_ok` — **137 (18.6 %)**.
**Честно списанных решением владельца — по-прежнему 0.**

**За 19 суток слой сдвинулся на ОДИН модуль** (115 → 116 sensitive; один модуль переехал из
`blind_equal` в `sensitive`). Все остальные корзины — бит в бит те же числа, что 07.08.
Это не «медленный рост», это остановка: с 03.08 по 07.08 было +93 модуля за 4 дня, с 07.08
по 26.08 — +1 за 19 дней.

**Про `_tier_c_writeoff.WRITTEN_OFF` (9 записей, «константа 0.0 на всех протоколах аудита»).**
Это МАШИННАЯ разметка, сгенерированная самим аудитом (`AUDIT_GENERATED_AT`), а не решение
владельца. Директива требует «либо подключить, либо честно списать через карточку (не молча)» —
карточки нет, поэтому в «честно списанные» эти 9 не засчитываю.

### Tier-C: живой прогон сам признаёт, что число к протоколу не относится

`data/analytics_report_full.json` (03:00Z, tier C, 180 модулей) теперь несёт
`_meta.protocol_differentiation` — и вердикт в нём **`NONE`**, дословно:

> `avg_score=20.56 одинаков у всех 8 протоколов И у несуществующего контрольного ⇒ модули не читают ctx['protocol']`

`control_modules_ok: 9`, `distinct_avg_scores: 1`. То есть несуществующий контрольный протокол
получает тот же скор, что и настоящие. В «работающие» не засчитано — и это **улучшение
честности отчётности** против 07.08, когда ключа не было вовсе (тогда пришлось выводить
`UNCHECKED` вручную). Сама работоспособность Tier-C не изменилась: 0 sensitive, как и 06.08.

### Живой Tier-A/B дифференцирован — старый провал не воспроизводится

- `analytics_signals_blocking.json` (A, 12/12 ok, 17:17Z): 5 протоколов, 5 РАЗНЫХ скоров
  41.86 … 67.46, разные `triggered_by` (`admin_key_control`, `systemic_risk_contagion`,
  `withdrawal_queue`).
- `analytics_signals_advisory.json` (B, 479 модулей, 17:57Z): 8 протоколов, 8 РАЗНЫХ
  `composite_risk` 35.79 … 40.47, разные `top_contributors`, `modules_ok` 77…93.

Сигнатура провала 02.08 («все 6.7 / mult 1.433») не воспроизводится ни в одном файле.

### Цепочка «origin → рантайм»: код доехал, но три агента его не исполняют

Прямая проверка (пока доступ на чтение ещё был): `spa_core/analytics/_protocol_facts.py` и
`_protocol_blindness.py` в прод-дереве **байт-в-байт совпадают с `origin/main`**
(`git hash-object` против `git rev-parse origin/main:<путь>`). Разрыв аудита #1 закрыт.

Но `agent_health.system_issues` вскрывает разрыв ДРУГОГО класса — «доставлено, но НЕ
исполняется»: долгоживущие KeepAlive-процессы держат старый код и сами новый не подхватят
никогда:

| Агент | Код в процессе | Разрыв |
|---|---|---|
| `com.spa.apiserver` | 17 июля | **39.7 суток** |
| `com.spa.familyfund` | 2 июля | **32.4 суток** |
| `com.spa.rtmr_sense` | 7 августа | ~19 суток |

Перезапуск — решение владельца (правило доставки, п. 6). Для метрики аналитики это не
критично (тир-агенты одноразовые, сигналы сегодняшние), но `d5.deployment.drift` = CRITICAL
в `system_health.json` **на этот раз НЕ ложная тревога** (в отличие от 07.08, где это была
гонка со синком): три агента названы поимённо, с датами.

### Флот

`data/agent_health.json` (18:04Z): **80 агентов, 78 OK, 2 WARNING, 0 CRITICAL = 97.5 %**.
`log_age = null` у **9** — по ним судить можно только по state-файлам, не по логам.
`system_issues` сверх трёх агентов выше: fleet parity DRIFT (**28 orphan-plist**,
2 declared-not-running) и «capital-efficiency LAZY: 5.0 % капитала простаивает UNEXPLAINED,
~22 bps/год упущено».

`data/cycle_health.json` — **HEALTHY** (цикл 17:16Z, equity 101 078.55, +0.0161 %), но
`evidence_vs_curve` = WARNING: **17 из 48 дат расходятся**, максимум $215.99 (own-32).
`data/system_health.json` (18:00Z) — **overall CRITICAL**: 1 CRITICAL (`d5.deployment.drift`),
3 WARNING (`d1.equity.count` 64 дня против ожидаемых ~66; `d1.equity.dates` 2 разрыва по 2 дня;
`d_riskwire.no_fork` — 10 измерений пулов без `seed_proof_hash`), 40 OK.

Честная рамка прежняя: 97.5 % «здоровья» означает «процессы живы», а не «делают доставленную
работу» — три агента из таблицы выше формально OK и при этом исполняют код месячной давности.

### CI на `origin/main`

Последний **завершённый** прогон — `e6fdd76f0` (17:58Z): `SPA Tests` **success**, `SPA CI`
**success**, `SPA Proof Gate` success, `SPA Lint — LLM forbidden` success. На `feb9cd477`
и `878d3c21e` (18:39Z / 18:50Z, поставки цикла #387) `SPA Tests` / `SPA CI` ещё `in_progress`.
**CI зелёный.**

### Четыре находки, которых не было в аудите #2

1. **Ежедневный аудит молчал 19 суток** (записи #1 03.08, #2 07.08, следующая — эта). Директива
   владельца требует ежедневного замера; фактическая частота — раз в три недели. Карточка уже
   есть (`inbox-u-ezhednevnogo-audita-90-net-storozha-on`, `cycle-367` с 24.08).
2. **15 записей реестра тиров указывают на несуществующие модули.** `ALL_MODULES` = 671, из них
   у **15 нет файла** в `spa_core/analytics/`. Вместе с 82 публичными модулями вне тиров это
   значит: знаменатель врёт в обе стороны одновременно — 82 модуля не измеряются вовсе,
   15 «модулей» измеряются, хотя их нет.
3. **Три тест-файла живут только в прод-дереве и никогда не попадали на origin** —
   `spa_core/analytics/gross_of/test_defi_protocol_vault_performance_fee_gross_of_{lp_amm_fee_drag,
   lst_peg_slippage,referral_affiliate_fee}_analyzer.py` (951 + 951 + 805 строк). CI их не
   гоняет НИ РАЗУ. Синхронизация каталогами лишнее не удаляет, поэтому они могут жить там
   бесконечно — класс «тест, которого нет в приёмке».
4. **Сессия снова ослепла посреди работы, и это воспроизводимо.** `~/Documents/SPA_Claude`
   отдаёт `Operation not permitted` на ЧТЕНИЕ (файлы, `listdir`, `git`), при этом **ЗАПИСЬ в
   `data/` работает** — проверено пробой файла. Ровно та же асимметрия, что 07.08. Два аудита
   из двух поймали это состояние; за 19 суток по нему не заведено ни карточки, ни сторожа.
   Опасность именно в асимметрии: `git`-сверки вырождаются в пустые строки и дают ложные
   вердикты, а запись при этом продолжает идти — сессия может доставить работу, которую
   не смогла проверить. Карточка заводится этим аудитом:
   `inbox-sessiya-teryaet-dostup-na-chtenie-prod-de`.

### Что двигает к 90 % дальше (по стоимости шага) — список НЕ изменился с 07.08

1. **`unchecked` 302 (41 %)** — самая большая и самая дешёвая корзина, и она названа поимённо:
   242 модуля (140 в B + 103 в C) = `no context-accepting entrypoint`; 59 (C) = `entrypoint
   requires non-mapping input, adapter supplies Mapping`. Вторая причина механическая —
   один адаптер закрывает 59 модулей за раз.
2. **Tier-C целиком** (180 модулей, 27 % реестра, 0 % работает) — крупнейший мёртвый груз;
   собственный вердикт прогона `NONE` это подтверждает.
3. **`blind_constant` 93 + `blind_equal` 82 = 175 (24 %)** — кандидаты №1 на честное списание
   карточкой владельцу. Машинная разметка на 9 из них уже есть, решения владельца нет ни по
   одному.
4. **82 модуля вне тиров + 15 призраков в реестре** — сначала починить знаменатель.
5. **Клон-фабрика 38 файлов** `..._gross_of_<X>_base_gap_analyzer.py` — не начата с 02.08.

### Что застряло

**Всё.** Ни один из пяти пунктов списка «что дальше» не сдвинулся за 19 суток; корзины
`unchecked` / `blind_constant` / `dormant` / `failed` совпадают с 07.08 бит в бит. Карточку
`inbox-tier-c-pyat-nastoyaschih-otkazov-agregat` держит `cycle-87477` с **06.08 18:43** —
захват висит 20 суток (у ярлыка нет живого pid ⇒ по новому правилу цикла #387 такой захват
не стареет никогда и запирает карточку).

Статус карточки **не меняю** (`in-progress`): критерий «готово» задан владельцем — 90 %,
фактически 15.8 %. До порога перевода в done далеко.
