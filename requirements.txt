# SPA repo root — top-level requirements (proxies spa_core/requirements.txt
# plus optional add-ons for the execution layer).
#
# Install: pip install -r requirements.txt
-r spa_core/requirements.txt

# ─── FEAT-004 Phase 3 (Sprint v3.9 / SPA-V39-001) ────────────────────────────
# eth-account is REQUIRED only when running live writes (dry_run=False AND
# SPA_EXECUTION_MODE=live). The aave_v3_adapter.py lazy-imports it via
# _require_eth_account() — missing dependency degrades to a structured
# {"status": "FAILED", "reason": "eth_account not installed ..."} so the
# paper-trading engine never crashes. Pin >=0.10 because we rely on the
# sign_transaction(...) EIP-1559 (type=2) support that landed in 0.10.0.
eth-account>=0.10.0
