# earn-defi.com — SPA (Smart Passive Aggregator)
# A paper-stage DeFi stablecoin-yield & risk research desk.
# The site is PUBLIC: search engines and AI agents are welcome to read, index, and audit
# the entire site. Everything here is honestly framed — research decisions with verdicts +
# evidence levels, NOT live offerings or investment advice (see /disclaimer, /yield-lab).
# Machine-readable index for agents: https://earn-defi.com/llms.txt

# AI / LLM crawlers (GPTBot, ClaudeBot, PerplexityBot, Google-Extended, CCBot, etc.) are
# explicitly WELCOME — owner decision for a transparency-first product. Not blocked.
User-agent: *
Allow: /
Disallow: /api/
# Operator Console — internal, no auth yet, not for indexing (belt-and-braces with the
# per-page noindex meta on admin={true} pages).
Disallow: /admin
# Desk Cockpit — internal operator home screen (UX-05, refined): not a public-proof page, so it
# should not be indexed as a standalone result. The public proof pages (rates-desk, rwa-backstop,
# exit-nav, due-diligence, structural-desk, monitoring, readiness, system, yield-lab, annual-contrast)
# are DELIBERATELY left indexable — they are the funnel's proof surface, not operator internals.
Disallow: /cockpit
# Q-OWN-04 (owner: /dashboard is the ONE public dashboard): /board/* (DFB risk board) is an operator
# surface too — /dashboard is the canonical public one.
Disallow: /board

Sitemap: https://earn-defi.com/sitemap.xml
