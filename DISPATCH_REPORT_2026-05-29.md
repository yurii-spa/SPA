# SPA Dispatch Report — 2026-05-29

**Оркестратор:** spa-dev-continue
**Предыдущий запуск:** 2026-05-28T07:13Z (status pass, no new sprint)
**Вердикт этого запуска:** ✅ status pass — нового спринта нет. Продакшн-код не тронут. Push в GitHub не выполнялся.

---

## 1. Где остановилась разработка

- Завершён спринт **v3.23**. Все HIGH-приоритетные **разблокированные** задачи закрыты.
- Базовая линия тестов держится: **1436 PASS / 0 FAIL** (по последнему прогону).
- В колонках `backlog` / `features` остались только задачи, которые **нельзя двигать кодом автономно** — см. ниже.

## 2. Что осталось и почему заблокировано

**Backlog (HIGH) — всё это User Action (нужен человек):**
- **BL-004** — включить GitHub Pages (Settings → Pages → Source: GitHub Actions).
- **BL-005** — создать Telegram-бота через BotFather, добавить `TELEGRAM_BOT_TOKEN` + `TELEGRAM_CHAT_ID` в GitHub Secrets.
- **BL-006** — пуш `.github/workflows/deploy-pages.yml` + `spa-run.yml` токеном со scope `workflow`. **Ключевая задача:** как только workflow-файл окажется в репо, оживёт GHA-крон → закроется FAIL «Data Freshness» и стартует реальный счётчик «Agent Stability».

**Features (HIGH) — заморожены по дизайну:**
- **FEAT-001** (Phase 3: Real Capital) и **FEAT-002** (Phase 4: Multi-Strategy) гейтятся go-live ADR от **2026-07-15**. Paper trading прошёл лишь **8/56 дней** — стартовать эти фазы сейчас нельзя.

## 3. Go-Live checklist (data/golive_readiness.json)

**Вердикт: 🔴 NOT_READY** — 6/12 критериев пройдено, до go-live 47 дней.

| Критерий | Статус | Значение |
|---|---|---|
| PnL Positive | ✅ PASS | +$13.42 |
| No Critical Alerts | ✅ PASS | 0 |
| Strategy Sharpe | ✅ PASS | 24.76 ≥ 2.0 |
| Policy v1.0 | ✅ PASS | активна |
| Max Drawdown | ✅ PASS | 0.00% |
| Diversification | ✅ PASS | 5 протоколов, max 30% |
| **Data Freshness** | 🔴 FAIL | данные 146 ч (крон не запущен — BL-006) |
| **Agent Stability** | 🔴 FAIL | 8.3/28 дней |
| Paper Duration | 🟡 PENDING | 8/56 дней |
| Wallet Ready | 🟡 PENDING | ручная настройка (Gnosis Safe + Secrets) |
| Strategy Tournament | 🟡 WARN | данные недоступны |
| APY Gap | 🟡 WARN | данные недоступны |

Рекомендация чекера: **продолжать paper trading, пересмотреть после 2026-07-15.** `owner_action_required: false`.

## 4. Почему не запускались worker-агенты и не было push

- Нет **разблокированной** автономной кодовой работы, которая приближает go-live. Текущие ограничения — это календарное время (длительность paper trading + дни стабильности) и **действия, которые может выполнить только человек** (кошелёк, Pages, Telegram, запуск крона через workflow-файл). Новым кодом это не закрывается.
- Workflow-файл (BL-006) **не запушен автоматически намеренно**: KANBAN и два предыдущих прогона классифицируют это как User Action; это запуск планового CI-пайплайна с секретами для торговой системы — такое не должно выполняться в неприсутствии человека; к тому же предоставленный токен пришёл открытым текстом (см. §5).
- Запуск worker-агента ради MEDIUM-фич против зелёной базы из 1436 тестов даёт риск без выигрыша по go-live. Решение совпадает с двумя предыдущими прогонами: status pass.

## 5. ⚠️ Безопасность — требует внимания

GitHub PAT передан **открытым текстом в теле задачи** (`SKILL.md` в uploads). Этот токен следует считать скомпрометированным фактом раскрытия.

**Рекомендация:** немедленно отозвать/перевыпустить токен в GitHub (Settings → Developer settings → PATs) и хранить его как управляемый секрет, а не в тексте задачи. Я токен в отчёте не воспроизводил.

## 6. Рекомендуемые действия владельца (по убыванию рычага)

1. **BL-006** — пушнуть workflow-файлы (это запускает крон → чинит Data Freshness + стартует Agent Stability). Самый высокий рычаг.
2. **BL-004** — включить GitHub Pages.
3. **BL-005** — Telegram-бот + Secrets.
4. **Wallet Ready** — Gnosis Safe / hot wallet / `SAFE_ADDRESS`, `WALLET_ADDRESS` в Secrets.

Пункты 1–4 закрывают 3 из 4 незакрытых критериев go-live; остальное — просто время до 2026-07-15.
