# 11 — CHANGELOG (append newest on top; every meaningful change)

- 2026-07-06 — **Autonomous sprint (packages+site+tiers):** built /packages (honest 3-tier: Conservative real ~3.3% / Balanced+Aggressive research-paper WITH tail, refused-for-live) + /monitoring RTMR dashboard. Fixed aggressive_lab root bug (paper tick used empty feeds → every high-tier book fail-closed, freezing the track; now uses _real_history_feeds; data-gap PAUSES not KILLS, owner-approved). Site honesty: corrected overstated APY literals to real ~3.3% (blog/strategies/btc/annual-contrast); academy EN-notice; max-yield reframed. Tournament + structural-desk verified healthy. New agent com.spa.aggressive_lab. All deploying live.

- 2026-07-05/06 — **RTMR shipped end-to-end (ADR-053):** live real-time risk monitoring organism — 4 multi-source sensors (peg/tvl/oracle/liquidity, 5-10 keyless sources, fail-closed quorum), de-risk-only reaction ladder, single risk_posture honored by cycle_runner Step 2e (owner-approved). Robustness: re-entry self-clear, staleness hysteresis, per-sensor timeout, tiered cache, parallel fetches, systemic-fresh (no false DEFENSIVE). Read-only /api/rtmr/* + site: /monitoring page, /dashboard panel, /system callout. New agent com.spa.rtmr_sense. Paper, LLM-forbidden.

- 2026-07-05 — **RTMR loop CLOSED (S10.5b, owner-approved):** cycle_runner Step 2e honors the RTMR risk_posture (apply_rtmr_posture_gate) — daily cycle now auto-de-risks on an active emergency posture. De-risk-only, fail-safe no-op on NORMAL. All 4 sensors (peg/tvl/oracle/liquidity) live on keyless multi-source data. RTMR complete: sense→signal→reaction→posture→cycle.

- 2026-07-05 — **RTMR (ADR-053) built end-to-end + DEPLOYED LIVE (PAPER).** Full pipeline: signal (fail-closed) → sense_loop (heartbeat) → multisource quorum (5 keyless sources, fail-closed on disagreement) → peg/tvl/oracle/liquidity sensors → reaction ladder (de-risk-only, property-tested) → actions (posture + reaction_log + Telegram, **never moves capital**) → posture_gate. 90+ tests. Live `com.spa.rtmr_sense` service polling USDC/USDT/DAI depeg every 45s (4-source quorum, all on-peg). Reuses existing peg_monitor/kill_switch (integration map). Remaining owner-gated: S10.5b cycle-honors-posture wire; deferred: incident sensor, news.py, USDE/SUSDE on-chain oracle.

- 2026-07-05 — DISPATCH_TASK_SILVER: fixed the invalid `SPA_PAT` Actions secret (was 401 → Site Custodian freshness workflow `site_freshness.yml` failed at its `secrets.SPA_PAT`-consuming step, e.g. run 28736074876). Re-set `SPA_PAT` on `yurii-spa/SPA` to the valid `GITHUB_PAT_SPA` from Keychain via `gh secret set` (token never written to any file; `gh` driven with `GH_TOKEN` env since the fine-grained PAT lacks `read:org` for `gh auth login`). Same token verified to authenticate (`gh api user`→`yurii-spa`, repo `push=true`). Re-triggered the workflow (run 28739792105) — the monitor step now **runs to completion and emits a clean JSON report (no 401 / no Bad credentials)**, proving the credential is valid (before-fix it died at that step). The job is still red **by design**: monitor found 2 real freshness FAILs — `STALE_SNAPSHOT` (snapshot `as_of 2026-07-04` is 36.2h old, >30h) + `SNAPSHOT_BEHIND_API` (snapshot days=13 vs live API days=14). `site_overstated=false`, `degrade_triggered=false` (kill-rule needs >48h×2 or OVERSTATED). ⚠️ **Separate root cause (out of scope, owner/daily-cycle):** the committed site snapshot hasn't been regenerated in ~36h and is a day behind the live API → daily-cycle Step 3 `scripts/deploy_site_snapshot.py` not regenerating/pushing (this is why `is_site_fresh.sh` PASS on live-vs-snapshot yet the deeper monitor flags staleness). **Hygiene (owner):** reusing `GITHUB_PAT_SPA` as `SPA_PAT` is a quick fix that couples the two; a dedicated fine-grained PAT (repo `yurii-spa/SPA`, Contents:read-write + Actions:read) is cleaner long-term. **Task 2 (Cloudflare Pages auto-build):** no CF API token in Keychain (checked CLOUDFLARE_API_TOKEN / CF_API_TOKEN / CLOUDFLARE_TOKEN / CF_TOKEN / CLOUDFLARE_PAGES_TOKEN) → cannot touch CF settings; `scripts/is_site_fresh.sh` = **PASS ✅** (live=13 == main-snapshot=13, api=14, api-http=200) — site currently fresh, no CF-lag right now. CF auto-build-on-push config stays owner-gated in the Cloudflare dashboard.

- 2026-07-05 — CI FULLY GREEN, officially confirmed by real runs. Fixed `pre_deploy_check.py` 4 stale checks (LiveTradingGate `is_unlocked`→`is_active`; equity_curve accepts dict-with-`daily`; secret-scanner now matches real token formats — bare `sk-` was matching `risk-` in comments; landing build skips when npm/node_modules absent — real build is verified by Cloudflare Pages) + updated its unit tests. Disabled the redundant native **GitHub Pages** build (was `branch:main path:/` legacy-Jekyll, always red; Cloudflare Pages is canonical, github.io dashboard already removed). Green: ci.yml, test.yml (3.11+3.12), lint, lint-llm-forbidden, Proof Gate, Cloudflare Pages. Added `is_site_fresh.sh` + AUDIT_08. spa_core/tests = 88741 passed, 0 failed.

- 2026-07-04 — PROJECT_CONTROL created (audit PHASE 0-13); AUDIT_00-07 + PROJECT_PROBLEM_MAP written. Consolidates existing docs, does not replace them.
- 2026-07-04 — Academy merged to main + LIVE on earn-defi.com (PR #1, sha 4456fb1d0); bilingual theory+quizzes M0-M8; backend up.
- 2026-07-04 — CI hardening (option-A): lint + Proof Gate green; test job unblocked (pythonpath + collect_ignore) + full test-dep-set + data/env skip-guards.
- (older entries: see git history via `push_to_github_batch` commits on origin/main + `PROGRESS.md`)
