# SPA — Статус оркестратора · 2026-06-01

Сгенерировано автономным прогоном scheduled-task `spa-dev-continue`.
Последний завершённый спринт по KANBAN: **v3.72** (`updated_by: orchestrator-v372`).

## Решение этого прогона: НЕ штамповать спринт, отдать решение пользователю

Я сознательно **не взял очередной спринт и не пушил в живой репозиторий**. Обоснование ниже. Это
не «status pass из лени» — незаблокированной кодовой работы объективно не осталось, а оставшиеся
действия требуют тебя, а не агента.

## Почему очереди кодовых спринтов больше нет

Все задачи из списка задания (V326–V332) уже `done` в KANBAN:

| Задача | Статус |
|---|---|
| SPA-V326 MEV Protection (Flashbots) | done (SPA-BL-010 / V326-001) |
| SPA-V327 DeFiLlama live APY feed | done (V327-001/002) |
| SPA-V328 Pendle PT adapter | done (V328-001) |
| SPA-V330 Architect review + housekeeping | done (V351) |
| SPA-V331 PostgreSQL migration prep | done (BL-008 / V341) |
| SPA-V332 Go-live dashboard | done (V332 + V333/334) |
| SPA-V329 Sky/sUSDS | **conditional** — ждёт внешнего подтверждения GSM ≥ 48h |

Спринты v3.67–v3.72 — это уже косметика дашборда (тренд-спарклайны, виджеты, рефакторинг
рендереров). Полезность близка к нулю, но каждый такой спринт пушит коммит в живой
репозиторий `yurii-spa/SPA`. Прошлый прогон сам это зафиксировал («HALT-отчёт v368»).

## Что реально блокирует go-live (срок 2026-07-15) — нужны ТВОИ действия

Критический путь (SPA-BL-012) держат не баги в коде, а user-action блокеры:

- **SPA-BL-007** — добавить RPC-ключи (Alchemy/Infura) в GitHub Secrets.
- **SPA-BL-008** — Telegram bot токен в Secrets.
- **SPA-BL-009** — Gnosis Safe + wallet address в Secrets.
- **BL-004** — включить GitHub Pages.
- **BL-005** — настроить Telegram bot.
- **BL-006** — workflow-scope token push.
- **FEAT-001/002** (live capital execution) — money-moving, вне автономного режима по дизайну.

Пока эти пункты открыты, агент может производить только косметику.

## 🔴 Безопасность — требует действия сегодня

В теле scheduled-task (`SKILL.md`) и в КАЖДОМ файле `push_v*.html` лежит **живой GitHub
PAT в открытом виде** (`ghp_…`). Это утечка учётных данных:

1. **Отзови этот токен в GitHub прямо сейчас** (Settings → Developer settings → Tokens).
2. Выпусти новый и положи в секрет-хранилище, а не в текст задачи/HTML.
3. 92 файла `push_v*.html` в папке репо содержат токен — их нужно удалить/очистить после ротации.

Прошлые прогоны уже отмечали это (рекомендация #2 в dispatch-note v370). Токен всё ещё активен.

## Накопившийся housekeeping (требует подтверждения — деструктивно)

- **101** файл `*.bak.*` и **92** файла `push_v*.html` в `/SPA_Claude` — мусор от автономных
  прогонов. Я не удалял автономно (деструктивно без подтверждения). Скажи «почисти .bak и
  push_*.html» — сделаю в один проход с резервной копией списка.
- Стейл-bookkeeping: десятки карточек SPA-001…043 / REV-* со `status:None`, хотя проект давно
  прошёл эти этапы (дашборд живой, v3.72). Стоит массово закрыть, чтобы KANBAN отражал реальность.

## Мои рекомендации (по приоритету)

1. **Отозвать и ротировать GitHub PAT** (см. выше) — самое срочное.
2. Закрыть user-action блокеры BL-004/005/006 и SPA-BL-007/008/009 → это разблокирует
   реальный go-live путь.
3. **Пересмотреть правило «status pass запрещён»**: без разблокировки секретов агент может только
   плодить косметику + коммиты. Разумнее — режим «работай только при наличии реальной
   незаблокированной задачи, иначе отчёт».
4. По подтверждению — почистить `.bak`/`push_*.html` и стейл-карточки KANBAN.

## Что НЕ делалось этим прогоном и почему

- ❌ Новый косметический спринт — нет реальной незаблокированной задачи.
- ❌ Push в живой репо — не оправдан для нулевой работы + риск с утёкшим токеном (и localhost:8765
  недоступен из песочницы — пуш всё равно требует Chrome на твоей машине).
- ❌ Удаление `.bak`/`push_*.html` — деструктивно, нужно подтверждение.
- ❌ Любой код адаптеров / eth_signer / money-moving — вне автономного режима по дизайну.
