# SPA — эскалация орк-агента (цикл v3.75, 2026-06-01)

## Кратко
Спринт сознательно **не запущен**. Это не сбой — это обоснованный status-hold. За 8 циклов подряд (v368→v375) процесс выродился: реальной разблокированной код-работы нет, а единственный способ пушить требует повторно экспонировать **утёкший GitHub PAT**. Я не делаю ни того, ни другого автономно.

## 🔴 Самое срочное — отозвать утёкший токен
`ghp_REVOKED_LEAKED_N31r` лежит в plaintext в **71 файле** на диске (`push_v*.html`) и в теле самой scheduled-task. Он **не отозван 8 циклов**.

1. GitHub → Settings → Developer settings → Personal access tokens → **Revoke**.
2. Перевыпустить новый токен, положить в keychain / секрет-хранилище.
3. **Не** вставлять токен обратно в task-файл или HTML.

## Почему нет код-работы
| Категория | Статус |
|---|---|
| HIGH backlog BL-004/005/006, SPA-BL-007/008/009, SPA-BL-012 | **действия пользователя**, не код (секреты, GitHub Pages, Telegram, go-live) |
| SPA-BL-010 (MEV) | ✅ done |
| SPA-BL-011 (feed-health) | governance-freeze — трогать запрещено |
| FEAT-001 / FEAT-002 | live money-moving — вне автономного режима, после go-live (2026-07-15) |
| SPA-V326…V332 | ✅ всё done с ~v3.52 |

Остаётся только косметика фронтенда, которую к тому же нельзя запушить.

## Почему нет пуша
`push_*.html → localhost:8765 → Chrome` требует встроить живой утёкший PAT в HTML. Отказался. **Замените механизм** на `git push` с токеном из env/keychain.

## Тех-долг на диске (уборка — по вашему подтверждению, деструктивно)
71 файл с утёкшим PAT · 78 `push_v*.html` · 107 `.bak.*` · `httpserver.log` ≈ 6.9 МБ.

## Рекомендация
Приостановить scheduled-task до: (1) отзыва PAT, (2) замены механизма пуша, (3) закрытия SPA-BL-012. И пересмотреть правило «status pass запрещён» — без разблокировки оно генерирует только мусор.

## Что сделано в этом цикле
Только bookkeeping: запись HALT в `SPA_sprint_log.md`, метаданные в `KANBAN.json` (`sprint_completed` НЕ изменён — спринт не выполнен), бэкапы `.bak.v375`. Код не менялся, пуш не выполнялся.
