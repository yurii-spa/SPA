#!/bin/bash
# SPA Push v8.72
# MP-1226: DeFiProtocolVaultPerformanceFeeGrossOfBoostFeeBaseGapAnalyzer  (91 tests)
#
# SECURITY: PAT is read from macOS Keychain (service: GITHUB_PAT_SPA).
# DO NOT embed PAT or any credentials in this file.
#
# Usage: bash ~/Documents/SPA_Claude/scripts/push_v872.sh

set -e

COMMIT_MSG="feat(v8.72): MP-1226 DeFiProtocolVaultPerformanceFeeGrossOfBoostFeeBaseGapAnalyzer (91 tests) | advisory/read-only yield_quality Tier-B (weight 0.5) | a vault routes its emission/reward position through an EXTERNAL BOOST PROVIDER / boost aggregator (Convex/Aura/StakeDAO/Yearn veToken locker) that amplifies the vault's gauge emissions with pooled veToken voting power (boosted CRV/BAL/etc.) and charges a recurring PLATFORM / BOOST FEE each period = boost_rate*yield*intervals (a PERCENTAGE cut of the boosted reward yield skimmed by the boost provider before the remainder reaches the depositor -- e.g. Convex's ~17% platform fee on boosted CRV/CVX, Aura's platform fee on boosted BAL/AURA); the depositor's economically realized yield is net_of_boost_fee = gross_yield - boost_fee. The vault charges its PERFORMANCE fee on the GROSS yield (before netting the boost fee), not on the NET-OF-BOOST-FEE yield the depositor actually realized -- so the depositor pays a performance fee on the slice of yield the boost fee already erased (a fee-on-boost-fee / fee-base inflation). metrics: fee_frac=clamp(performance_fee_pct/100,0,1); gross_yield_pct REQUIRED finite>0 else INSUFFICIENT_DATA; net_of_boost_fee_yield_pct (default 0, may be < gross, may be negative); boost_fee_consumed_yield_pct=max(0,gross-net); fee_charged_pct=fee_frac*max(0,gross); fair_fee_pct=fee_frac*max(0,net); fee_on_boost_fee_gap_pct=max(0,fee_charged-fair); fee_on_boost_fee_fraction=clamp(gap/fee_charged,0,1) scale-free classification basis; net_return_after_fee/net_return_fair; realization_ratio=clamp(net_after_fee/net_fair,0,1) with edge for non-positive fair. classification CLEAN_NET_OF_BOOST_FEE_BASE(<=0.05)/MILD_FEE_ON_BOOST_FEE_GAP(<=0.20)/MODERATE_FEE_ON_BOOST_FEE_GAP(<=0.50)/SEVERE_FEE_ON_BOOST_FEE_GAP(>0.50 OR net-negative)/INSUFFICIENT_DATA. flags CLEAN_NET_BASE / NET_NEGATIVE_AFTER_FEE / FEE_ON_BOOST_FEE / FULL_FEE_ON_BOOST_FEE / HIGH_BOOST_FEE (boost_fee_cost_pct>=10) / GAP_FROM_OVERRIDE. score=clamp(70*realisation + 30*(1-fee_on_boost_fee_fraction),0,100); INSUFFICIENT->0. HIGHER score = the performance fee was charged on the net-of-boost-fee base (gross ~= net), the fee was effectively fair. recommendation TRUST_FEE_STRUCTURE/MINOR_FEE_ON_BOOST_FEE/DEMAND_NET_OF_BOOST_FEE_BASE/AVOID_FEE_ON_BOOST_FEE; grade A-F. override path: direct fee_on_boost_fee_gap_pct + positive fee_charged_pct + positive gross -> taken verbatim (negative->magnitude) -> geometry (net_of_boost_fee/boost_fee_consumed/fair)->None, GAP_FROM_OVERRIDE flag, geometry-only flags suppressed, realization_ratio anchored to (1-fee_on_boost_fee_fraction). aggregate cleanest_vault/worst_boost_fee_gap_vault/avg_score/net_negative_count/position_count; pure stdlib, atomic ring-buffer log (data/vault_performance_fee_gross_of_boost_fee_base_gap_log.json, cap 100), no inf/NaN, read-only/advisory | DISTINCT axis: the performance-fee BASE being gross-of-boost-fee -- distinct from the other gross_of_* perf-fee base-gap modules (protocol_revenue_share=an INTERNAL cut the vault's OWN protocol takes into its own treasury, HERE a SEPARATE EXTERNAL veToken-locking boost aggregator skimming a platform fee on the AMPLIFIED emissions it supplies; harvest_bounty=per-harvest caller reward to a keeper, HERE a standing platform fee on boosted emissions regardless of harvest; referral_fee=affiliate/frontend cut paid OUTWARD to the UI that onboarded the deposit, HERE the boost provider's fee for amplifying emissions; validator_commission=standing node-operator commission on staking rewards, HERE a boost-provider platform fee on gauge/liquidity-mining emissions with no validator; management_fee=AUM-based, HERE a PERCENTAGE of yield; bridge_fee=cross-chain transport, HERE boost amplification; cost/borrow_cost/rebalancing_cost/exit_slippage/funding_cost/insurance_premium/slashing_loss/reserve_contribution/impermanent_loss/bad_debt_socialization -- none is a recurring PERCENTAGE platform/boost fee paid to an external veToken-locking boost aggregator); from hwm/volatility_tax/crystallization_frequency/hurdle_rate_gap/catch_up_clause_gap | registry Tier-B yield_quality weight 0.5 (B 480->481, ALL 672->673) | self-authored sprint: no type=code&status=ready task in KANBAN (backlog: agent_infra needing git/launchd/keychain on Mac + USER ACTION/P0-P2; features P3; ideas LOW), orchestrator chose the topic after a non-overlap scan of the gross_of_* family (the recurring percentage platform/boost fee paid to an external veToken-locking boost aggregator existed nowhere as a performance-fee base layer), added MP-1226 to KANBAN.json done and took it into work; updated KANBAN sprint_completed v8.72, sprint_current v8.73, done_count 918->919 + done MP-1226, appended sprint_log, created this push script | architect review: last completed before this run v8.71 ends in 1 -> NOT 0/5 -> review NOT required; backlog scanned manually: no type=code&status=ready, agent_infra tasks need Mac infra, no architectural regressions (module strictly additive, isolated in analytics/, registry parses with no dups) | STRICTLY READ-ONLY (SPA-BL-011): risk/execution/monitoring/allocator/cycle_runner.py/golive_checker.py untouched by this sprint"

FILES="/Users/yuriikulieshov/Documents/SPA_Claude/spa_core/analytics/defi_protocol_vault_performance_fee_gross_of_boost_fee_base_gap_analyzer.py
/Users/yuriikulieshov/Documents/SPA_Claude/spa_core/tests/test_defi_protocol_vault_performance_fee_gross_of_boost_fee_base_gap_analyzer.py
/Users/yuriikulieshov/Documents/SPA_Claude/data/vault_performance_fee_gross_of_boost_fee_base_gap_log.json
/Users/yuriikulieshov/Documents/SPA_Claude/spa_core/analytics/_module_registry.py
/Users/yuriikulieshov/Documents/SPA_Claude/KANBAN.json
/Users/yuriikulieshov/Documents/SPA_Claude/sprint_log.md
/Users/yuriikulieshov/Documents/SPA_Claude/scripts/push_v872.sh"

# PAT resolution: Keychain -> env GITHUB_PAT_SPA -> env SPA_GITHUB_PAT -> ~/.github_pat
PAT=$(security find-generic-password -s GITHUB_PAT_SPA -w 2>/dev/null || echo "")
[ -z "$PAT" ] && PAT="${GITHUB_PAT_SPA:-${SPA_GITHUB_PAT:-}}"
[ -z "$PAT" ] && [ -f ~/.github_pat ] && PAT=$(cat ~/.github_pat)
[ -z "$PAT" ] && { echo "PAT not found — run: bash setup_pat.sh"; exit 1; }

cd ~/Documents/SPA_Claude
echo "SPA Push v8.72 — MP-1226 + tests + registry + KANBAN + sprint_log"
python3 push_to_github.py --files $FILES --message "$COMMIT_MSG" --pat "$PAT"
echo "Push v8.72 complete!"
