#!/bin/bash
# SPA Push v8.71
# MP-1225: DeFiProtocolVaultPerformanceFeeGrossOfBridgeFeeBaseGapAnalyzer  (91 tests)
#
# SECURITY: PAT is read from macOS Keychain (service: GITHUB_PAT_SPA).
# DO NOT embed PAT or any credentials in this file.
#
# Usage: bash ~/Documents/SPA_Claude/scripts/push_v871.sh

set -e

COMMIT_MSG="feat(v8.71): MP-1225 DeFiProtocolVaultPerformanceFeeGrossOfBridgeFeeBaseGapAnalyzer (91 tests) | advisory/read-only yield_quality Tier-B (weight 0.5) | a cross-chain vault transports its capital and/or harvested rewards across chains each epoch via a bridge (Stargate/LayerZero/Across/canonical rollup bridge) and pays a recurring BRIDGE FEE (cross-chain transport fee / messaging fee / LP bridge cut) each period = bridge_rate*yield*intervals (a PERCENTAGE cut of the depositor's yield skimmed by the bridge protocol when the harvested rewards / rebalanced liquidity are transported back to the deposit chain before the remainder reaches the depositor -- e.g. a multi-chain yield aggregator that bridges each harvest home); the depositor's economically realized yield is net_of_bridge_fee = gross_yield - bridge_fee. The vault charges its PERFORMANCE fee on the GROSS yield (before netting the bridge fee), not on the NET-OF-BRIDGE-FEE yield the depositor actually realized -- so the depositor pays a performance fee on the slice of yield the bridge fee already erased (a fee-on-bridge-fee / fee-base inflation). metrics: fee_frac=clamp(performance_fee_pct/100,0,1); gross_yield_pct REQUIRED finite>0 else INSUFFICIENT_DATA; net_of_bridge_fee_yield_pct (default 0, may be < gross, may be negative); bridge_fee_consumed_yield_pct=max(0,gross-net); fee_charged_pct=fee_frac*max(0,gross); fair_fee_pct=fee_frac*max(0,net); fee_on_bridge_fee_gap_pct=max(0,fee_charged-fair); fee_on_bridge_fee_fraction=clamp(gap/fee_charged,0,1) scale-free classification basis; net_return_after_fee/net_return_fair; realization_ratio=clamp(net_after_fee/net_fair,0,1) with edge for non-positive fair. classification CLEAN_NET_OF_BRIDGE_FEE_BASE(<=0.05)/MILD_FEE_ON_BRIDGE_FEE_GAP(<=0.20)/MODERATE_FEE_ON_BRIDGE_FEE_GAP(<=0.50)/SEVERE_FEE_ON_BRIDGE_FEE_GAP(>0.50 OR net-negative)/INSUFFICIENT_DATA. flags CLEAN_NET_BASE / NET_NEGATIVE_AFTER_FEE / FEE_ON_BRIDGE_FEE / FULL_FEE_ON_BRIDGE_FEE / HIGH_BRIDGE_FEE (bridge_fee_cost_pct>=10) / GAP_FROM_OVERRIDE. score=clamp(70*realisation + 30*(1-fee_on_bridge_fee_fraction),0,100); INSUFFICIENT->0. HIGHER score = the performance fee was charged on the net-of-bridge-fee base (gross ~= net), the fee was effectively fair. recommendation TRUST_FEE_STRUCTURE/MINOR_FEE_ON_BRIDGE_FEE/DEMAND_NET_OF_BRIDGE_FEE_BASE/AVOID_FEE_ON_BRIDGE_FEE; grade A-F. override path: direct fee_on_bridge_fee_gap_pct + positive fee_charged_pct + positive gross -> taken verbatim (negative->magnitude) -> geometry (net_of_bridge_fee/bridge_fee_consumed/fair)->None, GAP_FROM_OVERRIDE flag, geometry-only flags suppressed, realization_ratio anchored to (1-fee_on_bridge_fee_fraction). aggregate cleanest_vault/worst_bridge_fee_gap_vault/avg_score/net_negative_count/position_count; pure stdlib, atomic ring-buffer log (data/vault_performance_fee_gross_of_bridge_fee_base_gap_log.json, cap 100), no inf/NaN, read-only/advisory | DISTINCT axis: the performance-fee BASE being gross-of-bridge-fee -- distinct from the other gross_of_* perf-fee base-gap modules (cost=FIXED gas/destination-gas/keeper TX flat amount that does NOT scale with yield, borrow_cost=lending-market loan interest, rebalancing_cost=swap turnover WITHIN a chain, exit_slippage, funding_cost=perpetual funding carry, reserve_contribution, impermanent_loss, bad_debt_socialization, insurance_premium, slashing_loss, validator_commission=standing validator commission on staking rewards, harvest_bounty=per-harvest caller reward, referral_fee=affiliate/frontend cut paid OUTWARD to the UI that onboarded the deposit, protocol_revenue_share, management_fee -- none is a recurring PERCENTAGE cross-chain bridge fee paid to a bridge protocol for transporting value BETWEEN chains); key contrast vs gross_of_rebalancing_cost (there swap turnover WITHIN a chain, here CROSS-CHAIN transport BETWEEN chains) and vs gross_of_referral_fee (there an affiliate/frontend referral cut, here the bridge protocol's transport fee) and vs gross_of_cost (there the FIXED destination-gas leg of bridging, here the PERCENTAGE bridge fee that scales with yield); also distinct from protocol_defi_cross_chain_bridge_fee_analyzer/MP-1095 (that computes the TRUE COST of bridging -- fee+destination gas+time cost+net APY impact -- for routing decisions; HERE it is the performance-fee BASE inflation from charging the fee on the gross pre-bridge-fee yield); from hwm/volatility_tax/crystallization_frequency/hurdle_rate_gap/catch_up_clause_gap | registry Tier-B yield_quality weight 0.5 (B 479->480, ALL 671->672) | self-authored sprint: no type=code&status=ready task in KANBAN (backlog: agent_infra needing git/launchd/keychain on Mac + USER ACTION/P0-P2; features P3; ideas LOW), orchestrator chose the topic after a non-overlap scan of the gross_of_* family (13 existing erosion layers; the recurring percentage cross-chain bridge fee paid to an external bridge protocol existed nowhere as a performance-fee base layer), added MP-1225 to KANBAN.json done and took it into work; updated KANBAN sprint_completed/sprint_current v8.70->v8.71 (sprint_current v8.72) + done MP-1225, appended sprint_log, created this push script | architect review: last completed v8.70 ends in 0 -> review DUE; spa_core.dev_agents.architect requires anthropic (LLM agent, unavailable in Linux sandbox) -> performed MANUALLY: no type=code&status=ready in backlog, agent_infra tasks need Mac infra, no architectural regressions (module strictly additive, isolated in analytics/, registry parses with no dups) | STRICTLY READ-ONLY (SPA-BL-011): risk/execution/monitoring/allocator/cycle_runner.py/golive_checker.py untouched by this sprint"

FILES="/Users/yuriikulieshov/Documents/SPA_Claude/spa_core/analytics/defi_protocol_vault_performance_fee_gross_of_bridge_fee_base_gap_analyzer.py
/Users/yuriikulieshov/Documents/SPA_Claude/spa_core/tests/test_defi_protocol_vault_performance_fee_gross_of_bridge_fee_base_gap_analyzer.py
/Users/yuriikulieshov/Documents/SPA_Claude/data/vault_performance_fee_gross_of_bridge_fee_base_gap_log.json
/Users/yuriikulieshov/Documents/SPA_Claude/spa_core/analytics/_module_registry.py
/Users/yuriikulieshov/Documents/SPA_Claude/KANBAN.json
/Users/yuriikulieshov/Documents/SPA_Claude/sprint_log.md
/Users/yuriikulieshov/Documents/SPA_Claude/scripts/push_v871.sh"

# PAT resolution: Keychain -> env GITHUB_PAT_SPA -> env SPA_GITHUB_PAT -> ~/.github_pat
PAT=$(security find-generic-password -s GITHUB_PAT_SPA -w 2>/dev/null || echo "")
[ -z "$PAT" ] && PAT="${GITHUB_PAT_SPA:-${SPA_GITHUB_PAT:-}}"
[ -z "$PAT" ] && [ -f ~/.github_pat ] && PAT=$(cat ~/.github_pat)
[ -z "$PAT" ] && { echo "PAT not found — run: bash setup_pat.sh"; exit 1; }

cd ~/Documents/SPA_Claude
echo "SPA Push v8.71 — MP-1225 + tests + registry + KANBAN + sprint_log"
python3 push_to_github.py --files $FILES --message "$COMMIT_MSG" --pat "$PAT"
echo "Push v8.71 complete!"
