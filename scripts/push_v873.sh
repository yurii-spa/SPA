#!/bin/bash
# SPA Push v8.73
# MP-1227: DeFiProtocolVaultPerformanceFeeGrossOfWithdrawalFeeBaseGapAnalyzer  (91 tests)
#
# SECURITY: PAT is read from macOS Keychain (service: GITHUB_PAT_SPA).
# DO NOT embed PAT or any credentials in this file.
#
# Usage: bash ~/Documents/SPA_Claude/scripts/push_v873.sh

set -e

COMMIT_MSG="feat(v8.73): MP-1227 DeFiProtocolVaultPerformanceFeeGrossOfWithdrawalFeeBaseGapAnalyzer (91 tests) | advisory/read-only yield_quality Tier-B (weight 0.5) | a vault's strategy realizes yield only by periodically REDEEMING/ROTATING out of an underlying protocol that charges an explicit recurring PERCENTAGE WITHDRAWAL / REDEMPTION / UNSTAKE FEE on every realization (LST/LSD instant-unstake fee on each exit, lending/money-market redemption fee, algo-stable/structured-product withdrawal cut, exit/redemption fee a meta-vault pays its underlying vault each harvest when redeeming shares) = withdrawal_rate*yield*intervals (a PERCENTAGE cut of the realized yield skimmed by the underlying redemption fee before the remainder reaches the depositor); the depositor's economically realized yield is net_of_withdrawal_fee = gross_yield - withdrawal_fee. The vault charges its PERFORMANCE fee on the GROSS yield (before netting the withdrawal fee), not on the NET-OF-WITHDRAWAL-FEE yield the depositor actually realized -- so the depositor pays a performance fee on the slice of yield the withdrawal fee already erased (a fee-on-withdrawal-fee / fee-base inflation). metrics: fee_frac=clamp(performance_fee_pct/100,0,1); gross_yield_pct REQUIRED finite>0 else INSUFFICIENT_DATA; net_of_withdrawal_fee_yield_pct (default 0, may be < gross, may be negative); withdrawal_fee_consumed_yield_pct=max(0,gross-net); fee_charged_pct=fee_frac*max(0,gross); fair_fee_pct=fee_frac*max(0,net); fee_on_withdrawal_fee_gap_pct=max(0,fee_charged-fair); fee_on_withdrawal_fee_fraction=clamp(gap/fee_charged,0,1) scale-free classification basis; net_return_after_fee/net_return_fair; realization_ratio=clamp(net_after_fee/net_fair,0,1) with edge for non-positive fair. classification CLEAN_NET_OF_WITHDRAWAL_FEE_BASE(<=0.05)/MILD_FEE_ON_WITHDRAWAL_FEE_GAP(<=0.20)/MODERATE_FEE_ON_WITHDRAWAL_FEE_GAP(<=0.50)/SEVERE_FEE_ON_WITHDRAWAL_FEE_GAP(>0.50 OR net-negative)/INSUFFICIENT_DATA. flags CLEAN_NET_BASE / NET_NEGATIVE_AFTER_FEE / FEE_ON_WITHDRAWAL_FEE / FULL_FEE_ON_WITHDRAWAL_FEE / HIGH_WITHDRAWAL_FEE (withdrawal_fee_cost_pct>=10) / GAP_FROM_OVERRIDE. score=clamp(70*realisation + 30*(1-fee_on_withdrawal_fee_fraction),0,100); INSUFFICIENT->0. HIGHER score = the performance fee was charged on the net-of-withdrawal-fee base (gross ~= net), the fee was effectively fair. recommendation TRUST_FEE_STRUCTURE/MINOR_FEE_ON_WITHDRAWAL_FEE/DEMAND_NET_OF_WITHDRAWAL_FEE_BASE/AVOID_FEE_ON_WITHDRAWAL_FEE; grade A-F. override path: direct fee_on_withdrawal_fee_gap_pct + positive fee_charged_pct + positive gross -> taken verbatim (negative->magnitude) -> geometry (net_of_withdrawal_fee/withdrawal_fee_consumed/fair)->None, GAP_FROM_OVERRIDE flag, geometry-only flags suppressed, realization_ratio anchored to (1-fee_on_withdrawal_fee_fraction). aggregate cleanest_vault/worst_withdrawal_fee_gap_vault/avg_score/net_negative_count/position_count; pure stdlib, atomic ring-buffer log (data/vault_performance_fee_gross_of_withdrawal_fee_base_gap_log.json, cap 100), no inf/NaN, read-only/advisory | DISTINCT axis: the performance-fee BASE being gross-of-withdrawal-fee -- distinct from the other gross_of_* perf-fee base-gap modules (exit_slippage=a ONE-OFF MARKET-DRIVEN price impact on a single principal exit against AMM/orderbook depth, HERE an EXPLICIT DETERMINISTIC protocol-set PERCENTAGE redemption fee on EVERY recurring realization; management_fee=AUM-based % of principal over time, HERE % of realized yield at withdrawal; protocol_revenue_share=the protocol's INTERNAL cut of yield to its treasury, HERE a posted redemption fee on the value being withdrawn; rebalancing_cost=swap turnover WITHIN the position, HERE the posted redemption fee on EXITING the position not an AMM swap; cost=FIXED gas/keeper-TX not scaling with yield, HERE a PERCENTAGE of yield; boost_fee/bridge_fee/harvest_bounty/referral_fee/validator_commission/borrow_cost/funding_cost/insurance_premium/slashing_loss/reserve_contribution/impermanent_loss/bad_debt_socialization -- none is a recurring percentage withdrawal/redemption/unstake fee charged by the underlying on each realization); also distinct from defi_protocol_vault_withdrawal_fee_decay_analyzer (that prices a TIME-DECAYING withdrawal-fee SCHEDULE for the hold-time/exit-timing decision, HERE the performance-fee BASE inflation); from hwm/volatility_tax/crystallization_frequency/hurdle_rate_gap/catch_up_clause_gap | registry Tier-B yield_quality weight 0.5 (B 481->482, ALL 673->674) | self-authored sprint: no type=code&status=ready task in KANBAN (backlog: agent_infra needing git/launchd/keychain on Mac + USER ACTION/P0-P2; features P3; ideas LOW), orchestrator chose the topic after a non-overlap scan of the gross_of_* family (15 existing erosion layers; a recurring percentage withdrawal/redemption/unstake fee charged by the underlying protocol on each realization existed nowhere as a performance-fee base layer, separated from one-off exit_slippage and the withdrawal_fee_decay time-schedule analyzer), added MP-1227 to KANBAN.json done and took it into work; updated KANBAN sprint_completed v8.73, sprint_current v8.74, done_count 919->920 + done MP-1227, appended sprint_log, created this push script | architect review: last completed before this run v8.72 ends in 2 -> NOT 0/5 -> review NOT required; backlog scanned manually: no type=code&status=ready, agent_infra tasks need Mac infra, no architectural regressions (module strictly additive, isolated in analytics/, registry parses with no dups) | STRICTLY READ-ONLY (SPA-BL-011): risk/execution/monitoring/allocator/cycle_runner.py/golive_checker.py untouched by this sprint"

FILES="/Users/yuriikulieshov/Documents/SPA_Claude/spa_core/analytics/defi_protocol_vault_performance_fee_gross_of_withdrawal_fee_base_gap_analyzer.py
/Users/yuriikulieshov/Documents/SPA_Claude/spa_core/tests/test_defi_protocol_vault_performance_fee_gross_of_withdrawal_fee_base_gap_analyzer.py
/Users/yuriikulieshov/Documents/SPA_Claude/data/vault_performance_fee_gross_of_withdrawal_fee_base_gap_log.json
/Users/yuriikulieshov/Documents/SPA_Claude/spa_core/analytics/_module_registry.py
/Users/yuriikulieshov/Documents/SPA_Claude/KANBAN.json
/Users/yuriikulieshov/Documents/SPA_Claude/sprint_log.md
/Users/yuriikulieshov/Documents/SPA_Claude/scripts/push_v873.sh"

# PAT resolution: Keychain -> env GITHUB_PAT_SPA -> env SPA_GITHUB_PAT -> ~/.github_pat
PAT=$(security find-generic-password -s GITHUB_PAT_SPA -w 2>/dev/null || echo "")
[ -z "$PAT" ] && PAT="${GITHUB_PAT_SPA:-${SPA_GITHUB_PAT:-}}"
[ -z "$PAT" ] && [ -f ~/.github_pat ] && PAT=$(cat ~/.github_pat)
[ -z "$PAT" ] && { echo "PAT not found — run: bash setup_pat.sh"; exit 1; }

cd ~/Documents/SPA_Claude
echo "SPA Push v8.73 — MP-1227 + tests + registry + KANBAN + sprint_log"
python3 push_to_github.py --files $FILES --message "$COMMIT_MSG" --pat "$PAT"
echo "Push v8.73 complete!"
