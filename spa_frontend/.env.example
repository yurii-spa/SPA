# Development: Vite proxy handles /api → localhost:8000
# No VITE_API_URL needed in dev (proxy is configured in vite.config.ts)

# Production (если API деплоится отдельно):
# VITE_API_URL=https://your-api-server.com
