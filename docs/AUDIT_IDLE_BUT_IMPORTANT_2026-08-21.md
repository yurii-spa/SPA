# Аудит «построено, но без дела» — 2026-08-21

> Рабочий документ (ненумерованный). Метод — четыре ЗАМЕРА, не мнение:
> `docs/MATURITY_REGISTER.md` (уровни зрелости) · `architecture/manifest.json` (флот: 81 active / 1 designed / 7 retired) ·
> поиск вызывающих по каждому подозреваемому пакету · ратчеты (`unwired_scripts_baseline.json`: **67** скриптов без вызывающего;
> `design_status_baseline.json`: **45** документов без строки статуса). Открытых карточек в трекере — **148**.
> Итог: не «что ещё построить», а **что уже построено/куплено и простаивает** — и что из этого брать в бэклог.

## A. Простаивает и ВАЖНО — брать в бэклог (приоритет по эффект×готовность, AI1 гл. 06)

| # | Что простаивает | Замер | Почему важно | Карточка |
|---|---|---|---|---|
| 1 | **Мост origin → рантайм** | прод-дерево отставало на 665 коммитов (#310); доставленный код не работает в проде | КОРЕНЬ половины проблем ниже: всё «влитое» не доезжает | `agent-origin-runtime-sync-gap` (in-progress) — поднять приоритет |
| 2 | **Сторож-автоперезапуск telegram_bot** | бот завис ДВАЖДЫ за 21.08; маячок протухает → владелец слепнет к вопросам (кнопки пропадают) | путь решений владельца рвётся молча; чинится за час (сторож маячка + kickstart) | **НОВАЯ** `agent-telegram-bot-watchdog` |
| 3 | **Долгожители крутят старый код** | `apiserver` — код 28-дневной давности | фикс, влитый месяц назад, в проде не существует | `agent-dolgozhivuschie-agenty-krutyat-staryi-kod` (backlog) |
| 4 | **site_freshness на Маке** | единственный `designed` агент манифеста; plist есть, не загружен; включение упёрлось в №1 | сайт без локального сторожа свежести | `agent-vklyuchit-site-freshness-posle-adr-098` (backlog, после №1) |
| 5 | **Живые фиды для аллокатора** | 11 протоколов закрыты без live-фидов; fake-fallback в 15 адаптерах; расхождение путей данных (aave 4.80% против 2.36%) | капитал раскладывается по хуже подтверждённым числам, чем мог бы | `agent-blocked-protocols-need-live-feeds` · `agent-fake-fallback-v-15-adapterah` · `agent-allocator-yield-frozen-rootcause` |
| 6 | **spa_core/redteam/** — 1021 строк, тесты есть | plist написан, агента НЕТ среди загруженных; единственный вызывающий — сам в unwired-базе | состязательная ротация против proof-chain rates_desk написана и не бегает НИ РАЗУ | **НОВАЯ** `agent-vklyuchit-redteam-rotation` |
| 7 | **Внутридневной paper-актор** | ADR-104 честно называет его «следующий шаг, свой ADR»; сенсоры 5-мин есть, актора нет | защита реагирует между циклами, а РЕБАЛАНС только раз в сутки | **НОВАЯ** `agent-intraday-paper-actor` |
| 8 | **Флот-паритет: 26 plist-сирот** | сторож паритета не запускался 25 суток | манифест ↔ реальный флот расходятся молча | `agent-fleet-parity-guard-never-scheduled` (blocked — разблокировать) |

## B. Простаивает, но НЕ брать (осознанно, с причиной)

| Что | Замер | Почему НЕ брать сейчас |
|---|---|---|
| `family_fund/` вторая половина (KYC, вывод средств, регистрация — 6 модулей без вызывающих) | API-кабинет живой, онбординг мёртв | **Заблокировано инвариантом #8**: внешний капитал закрыт до legal-clearance. Строить онбординг раньше права его включить — риск solicitation |
| `strategy_lab/liquidator/` (625 строк, 1 тест) | вызывающий — строка документации | эффект low; бизнес-линия не выбрана владельцем |
| `spa_core/compliance/` (1018 строк, **0 тестов**) | единственный L3 в реестре; один приватный импорт | сначала решение владельца: достроить минимум (отчёт аудита) или заархивировать. НЕ достраивать молча |
| Investment Committee · Builder OS · схема БД | L2, эффект low | реестр прямо говорит: оставить документами |
| BTC-движок | L2, эффект high | ПРОЕКТ ПОДГОТОВКИ (ADR-102): просадка −25% NAV не проходит под HARD_KILL −10%. Решение — за владельцем, карточка есть |

## C. Модули-сироты внутри ЖИВЫХ пакетов → скормить `agent-cleanup` (не новая карточка)

`swarm/rank_demotion_forward.py` (19 КБ, ни агента, ни импортёра, ни теста) ·
`rates_desk/{promotion_rates, capacity_sizing, fair_value (двойник живого fair_value_engine), exit_liquidity_validation, rate_floor_recal}.py` ·
`aggressive_lab/{tier_policy, tail_overlay}.py` + `aggressive_lab_runner.py` (агент зовёт template, не раннер) ·
`rwa_backstop/onchain_nav.py` · `gross_of/sequencer_tip_config.py` + 3 приблудных `test_*.py` внутри пакета ·
67 скриптов из unwired-базы (может только уменьшаться).

По каждому — решить: подключить или удалить; молча оставить нельзя (это и есть болезнь «неотличимо от работающего»).

## D. Методологическое (для будущих аудитов)

`gross_of/` по grep выглядит мёртвым, но живёт через `_module_registry.py` + importlib; агенты зовут модули через
`agent_template.sh MODULE=`. **Любой поиск мёртвого кода в этом репо обязан резолвить реестр аналитики и
template-индирекцию** — иначе объявит живую подсистему мёртвой.
