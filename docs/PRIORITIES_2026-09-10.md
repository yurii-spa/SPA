# Матрица приоритетов — 2026-09-10

> **Статус:** L4 · владелец: @yurii · приёмка: каждая строка Q1 либо закрыта, либо имеет
> карточку с измеренным критерием; повторный замер — на следующей ревизии.

Заказ владельца 10.09: «проверь весь скоп задач, даже те, что говорил на потом, прокрути
их по матрице Эйзенхауэра и приступай». Ниже — весь скоп, разложенный **по замеру**, а не
по ощущению. Срочность здесь означает «блокирует другое», важность — «двигает доходность,
доказуемость или деньги».

## Q1 · срочно И важно — делать сейчас

| Что | Замер | Состояние |
|---|---|---|
| **CI: шаг 2 задания** | 12.09: полный прогон на `7f3772d4` дал 101 885 зелёных и 8 падений (было 22 утром); все восемь закрыты, 92 зелёных на `10a96d41`. «Эффект порядка» оказался НЕ порядком: четыре телеграм-теста краснели от ДЛИТЕЛЬНОСТИ прогона (ADR-348) | **закрыто 12.09** |
| два контроля money-path 29.08 | вердикт решала ВЕРСИЯ ИНТЕРПРЕТАТОРА (`sum()` в CPython 3.12 стал компенсированным): прод 3.13 воспроизводит аварию, CI 3.11 — нет | **закрыто 10.09** |
| свои скрипты без вызывающего | 2 из 2 подключены; `adr_number` удалён из базы (база УМЕНЬШИЛАСЬ) | **закрыто 10.09** |
| пять ЧУЖИХ скриптов без вызывающего | разобраны поимённо (ADR-342): двое были ЖИВЫ (слепота детектора к корню репо; ручной инструмент со своим реестром-продуктом), у графа знаний мёртв был ПРОДУКТ, счётчик карточек подключён к брифингу, коррелятор в attic — гипотезу опроверг замер | **закрыто 11.09** |
| шаги 3–4 задания CI | это `Run scripts/ gate tests` и `Pre-deploy checks`; они ПРОПУСКАЮТСЯ после падения шага 2 — но и после починки не исполнились бы: замер 12.09 — 14 прогонов в работе, **166 в очереди**, тяжёлый идёт 1,5–4 ч, пуш добавляет пять. Вердикт о ВЕРШИНЕ main не приходил вовсе. Прописана группа `concurrency` (отмена только для пушей) + суточный прогон | **причина закрыта 12.09**, ждёт разгрузки очереди (~полсуток) |
| три вопроса по автоторговле | ждут владельца 2+ суток; без них money-path не двигается | **за владельцем** |

## Q2 · важно, НЕ срочно — то, ради чего всё остальное

| Что | Почему важно | Замер |
|---|---|---|
| **независимое наблюдение книги** (ADR-286 §1 шаг 3) | пока его нет, трек доказуем только нашим же кодом | ADR-257: 10 кандидатов, **0 независимых** |
| **своя история цен** (earn-defi D-24/D-39) | снимает лицензию Coin Metrics NC с шеи навсегда | отложено решением владельца до этапа C |
| перевод 32 страниц на провенанс | механизм построен (ADR-315), остаток в храповике | 32 из 140; база может только убывать |
| откуда считать публичный трек двух книг | публичное число — предмет №2 владельца | **за владельцем** |

## Q3 · срочно, НЕ важно — делегировать или закрыть дёшево

| Что | Замер |
|---|---|
| `_BOARD.md` разошёлся с карточками | **закрыто 11.09** — пересобран из набора карточек origin (расхождение было 2 карточки, обе мои) |
| `STATE.md` несёт непокрытую хронику | 1 строка; переносится в журнал |
| дедуп Telegram (4 теста) | **закрыто 12.09, и гипотеза этой строки была НЕВЕРНА.** Загрязнителя не существует: якорь времени вычислялся на СБОРЕ набора, субъект спрашивает часы при ВЫПОЛНЕНИИ, и в полуторачасовом прогоне запись «120 секунд назад» старше получасового окна. Полдня ушло на деление набора пополам — уликой была ФОРМА падений (все четыре «ждали True, получили False»), а не их совместность (ADR-348) |
| Obsidian показывает состояние недельной давности | одна минута, **за владельцем** |
| удалить оставшийся файл с токеном | одна команда, **за владельцем** |

## Q4 · ни срочно, ни важно — НЕ делать

| Что | Почему нет |
|---|---|
| переселение 18 форкастеров в линию время-рядов | косметика архитектуры; не двигает ни доходность, ни доказуемость |
| переселение 13 оптимизаторов в советники аллокатора | то же |
| переселение 9 трекеров в слой отчётности | то же |

Три эти карточки остаются в бэклоге и не берутся, пока Q1 и Q2 не пусты. Это решение
записано здесь, чтобы не пересматривать его каждый цикл заново.

## Что НЕ входит в матрицу и почему

**Перезапуск `com.spa.apiserver` и `com.spa.rtmr_sense`** (код от 8 и 5 сентября,
KeepAlive-долгожители) — не приоритет, а действие владельца: правило доставки, п. 6.
**Создание репозитория `yurii-spa/earn-defi` и Telegram-канала** — то же: агент физически
не может, PAT repo-scoped.

## Ревизия 2026-09-11 (вечер)

Закрыто за день: **ADR-340** (тир шага псевдонимов пула), **ADR-341** (паспорт «трогает
execution» — структурой, а не подстрокой), **ADR-342** (пять чужих скриптов), **ADR-344**
(инвариант #17 исчез из конституции на 16 суток; класс «не измерено = ноль» разобран до конца —
185 мест, база 192→148 и 60→52). Плюс гейт mypy (шаг 1 CI), фикстура буквенных вариантов,
7 файлов храповика дат, деплой снимка сайта (моя регрессия ADR-315 + настоящий дефект: флаг
перезаписи распространялся на второй артефакт), контроль edge-паритета (падал исключением там,
где обязан отказывать).

**Следующее по матрице:** Q2 «независимое наблюдение книги» (ADR-286 §1 шаг 3) — пока его нет,
трек доказуем только нашим же кодом.

## Ревизия 2026-09-12 (ночь)

Закрыт **весь Q1, кроме того, что за владельцем**. Шаг 2 зелёный: восемь падений полного
прогона разобраны поимённо — два теста тождества (механизм сменён с `is` на происхождение
кода: в полном прогоне модуль грузится второй раз, и объекты РАЗНЫЕ при честном
переиспользовании), четыре телеграм-дедупа (**ADR-348**), доска трекера (пересобрана),
`haystack_origin_census` (восстановлен ADR-346 — потеря проводки была МОЕЙ доставкой,
записано в журнал своими словами).

**Главный урок ночи — про то, как я мерил, а не про то, что чинил.** Строка Q3 про дедуп
несла ГИПОТЕЗУ («общее состояние»), и я полдня проверял её делением набора, вместо того
чтобы прочитать форму падений: все четыре были «ждали True, получили False», то есть
смещение однонаправленное, а это подпись часов, а не загрязнителя. Гипотеза в таблице
приоритетов читается как замер и ведёт работу за собой; впредь такие строки помечаю как
гипотезу явно.

**Q1 «шаги 3–4» переформулирован по замеру.** Две недели строка звучала как загадка. Никакой
загадки: шаги пропускаются после падения шага 2 (обычное поведение job'а), а вердикт о
вершине не приходил, потому что очередь Actions была заперта собой. Починка — группа
`concurrency` с отменой ТОЛЬКО для пушей плюс суточный прогон вне этой группы; одна лишь
отмена сменила бы молчание очереди на молчание отмен. Отменить 161 уже стоящий прогон не
вышло: 403, у токена нет прав на Actions — затор разовый и разгрузится сам.

**Следующее по матрице:** Q2 «независимое наблюдение книги» (ADR-286 §1 шаг 3).
