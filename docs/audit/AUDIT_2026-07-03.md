# SPA — Full System Audit (2026-07-03)

**Method:** 6 parallel read-only auditor agents (frontend / backend-core / research+docs / agents+ops+DR /
push+deploy+tracking+git / tests+CI) + direct live-evidence verification (launchctl, scoped pytest,
origin raw-fetch, proof-chain). All findings are real-file evidence; unknowns flagged, not fabricated.

## Headline verdict

**Healthy for the paper stage. Zero active security/correctness defects.** The deterministic money path is
fail-closed on every numeric input, atomic on every track-state write, and the two-tier kill ladder is
provably single-source across governance + execution. 47 launchd agents run exit-0 with zero exit-78
exposure. No secrets are tracked in git. The scoped suite is 101,339 passed / 2 failed. Proof-chain
`verify_spa.py data/` → VERDICT OK.

The issues below are **latent hazards, deploy redundancy, doc/state drift, and two self-undermining items**
(a publicly-broken "verify the verifier" sidecar; leaked-adjacent files stranded on the public remote).

## Live evidence (verified this audit)

| Signal | Value |
|---|---|
| launchd fleet | **47 agents, all exit 0/−**, zero exit-78, agent_health crit=0 (WARNING = 3 weekly agents) |
| Scoped tests | `pytest spa_core/tests tests research --ignore=.claude` → **101,339 passed / 2 failed / 573 skipped** |
| Proof-chain | `verify_spa.py data/` → **VERDICT: OK**; decision_index 38 rows; OTS anchor live |
| Secrets in git | **0** real-format tokens tracked (PAT Keychain-only) |
| Remote hygiene | `cabinet/node_modules` = **133 entries on origin**; `.claude/settings.local.json` = **HTTP 200 on raw** |
| Cockpit | on origin/main (200) + live `/cockpit` 308 — **deployed, not broken**; only untracked locally |
| Git model | local HEAD on `fix/static-numbers`, drifts from origin (API-push-to-main model) |

## P0 / P1

| # | domain | problem | fix |
|---|---|---|---|
| **P1** | frontend | `og-image.png` + `favicon.png` missing but `Layout.astro` references them site-wide → broken social cards + favicon 404 on every page | add real assets, or drop the png refs |
| **P1** | frontend/deploy | `_headers`+`_redirects` are Cloudflare-only, but `deploy-landing.yml` targets **GitHub Pages** (ignores them) → any GH-Pages traffic served without CSP/HSTS/www→apex | **owner decision:** pick one host; disable GH-Pages workflow if CF is canonical |
| **P1** | ops/DR | `restore_drill_status.json` clobbered by a pytest run (writes to live `data/`; `all_ok:false` fake evidence) → poisons R8 resilience rollup | isolate test to `tmp_path` + conftest guard + regen status (**safe fix**) |
| **P1** | ops/DR | R8 rollup + drills have **no launchd agent** → `resilience_status.json` stale 6d, DR posture drifting dormant | add `com.spa.resilience` via deploy-gate, or daily-cycle Step 4 |

> **Downgraded:** the frontend agent flagged untracked Cockpit as P0 "broken витрина" — live verification shows
> cockpit IS on origin + serving. Real residual = **P2** (untracked-local drift; `cockpit/` subdir could
> collapse on a future git-status-derived push — push by name).

## P2

**Backend-core** (latent / deliberate, none block paper):
- `paper_trading/risk_gate.py:262` injects synthetic **$20M TVL** when the live feed is missing, then approves
  — fail-open live gate (can't tell "feed down" from "TVL crashed"). Fix before real-capital cutover.
- `execution/adapter_status.py:400` — **non-atomic** state write (violates FORBIDDEN #4); route through `atomic_save`.
- Adapter `get_apy()` returns **decimal** while the money path uses **percent** — 100× foot-gun for new callers.
- `paper_trading/engine.py:139` imports `execution` inside a paper module (boundary hygiene; no live leak).

**Research / docs:**
- `scripts/verify_spa.SHA256` **stale** (`9befddc6` vs actual `bbc4853a`) → documented `shasum -c` fails today (**safe fix**).
- `prompts/claude_code/master_prompt.md` dangling `ADR-001..007` (renamed to `ADR-YL-`); 3 more docs w/ dead refs.
- `strategy_lab_config.json` `maple_syrup.doc` "9-12%" vs `apy_pct 4.7` (contradictory string).
- `centrifuge_drop` gets an 8% forward advisory curve while its verdict is WATCH/opacity-held (refusal-first tension).

**Push / deploy / tracking:**
- `CURRENT_STATE.md` internally contradictory (26/29 & 27/29; days 9/7/12; truth 27/29, 12/30) — **safe fix**.
- GitHub-Pages vs Cloudflare **double-build** of `landing/` (source of past 404-with-200) — owner decision.
- `push_v9999_design_update.sh` PENDING does `git push HEAD:main` from a non-main branch → perpetual retry-reject.
- **API push cannot DELETE** → `cabinet/node_modules` (133) + `.claude/settings.local.json` stranded on remote.

**Ops:** offsite = same disk (SPOF, owner-flagged); 3 weekly agents WARNING (StartCalendarInterval miss on sleep).

## P3 / cosmetic

Hardcoded `27/29` on due-diligence/trust/methodology (drift from snapshot); synthetic-data pages
(cockpit-kit/board) indexable + in sitemap; `_headers` dead routes + `/dashboard` not in no-cache; nav
JS-gated (no-JS unreachable); dead modules with TODO-remove; `.disabled` plists (doc says "deleted"); KANBAN
count drift (33 vs 35 adapters, sprint v10.4<current); 3 stray `.claude/worktrees` duplicates.

## Verified CLEAN (positive)

Money-path fail-closed + atomic + single-source kill ladder; **0 LLM** in risk/exec/governance; advisory
discipline (all 5 sleeves `is_advisory:true`, config drift-free, no track leak); OTS Bitcoin anchor live;
0 tracked secrets (PAT Keychain-only); push scripts collapsed root↔scripts to a shim; 409/rate-limit handled;
47 agents exit-0 zero exit-78; RETIRED-6 dormant; sitemap↔routes exact 75/75; proof/DD pages server-rendered.

## Remediation split

**Safe (autonomous, verify local==main first):** verify_spa.SHA256 regen · restore_drill test-isolation +
conftest guard · CURRENT_STATE auto-derive · hardcoded 27/29→snapshot · og/favicon · dangling ADR-refs ·
maple doc-string.

**Owner-gated:** remote-DELETE node_modules + settings.local (Contents-API delete) · GitHub-Pages-vs-CF host ·
cockpit push-by-name · risk_gate $20M fallback (pre-cutover) · real offsite SPOF · centrifuge 8% curve.

*Ops/research-layer audit. No RiskPolicy / go-live-track / execution change performed. Safe fixes tracked in
follow-up commits `fix(audit-2)`.*
