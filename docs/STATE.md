# SPA — STATE (текущее состояние проекта)

> Живой файл состояния. Оркестратор читает в начале цикла и обновляет в конце.
> Живые оперативные цифры — `docs/SYSTEM_BRIEFING.md` (auto, 30 мин). Здесь — фокус,
> задачи, решения, вопросы. **Максимум ~150 строк.**
>
> **Хроника циклов живёт в `docs/journal/<неделя>.md`, а НЕ здесь.** Сюда пишется только то,
> что меняет ответ на вопрос «что делать дальше». Предел закреплён храповиком
> `spa_core/tests/test_state_md_length_ratchet.py` (длина только вниз) — так же, как храповик
> литеральных дат: жёсткий лимит покраснел бы сразу и его бы отключили. Снимок файла до
> сокращения (1920 строк) — [`docs/journal/STATE-archive-2026-08-07.md`](journal/STATE-archive-2026-08-07.md).

## 🎯 Текущий фокус

- **Paper-трек добран и продолжается: 46/30 evidenced-дней** (anchor 2026-06-22). Владелец выбрал
  путь **D → B** (сначала укрепление, потом крошечный реальный пилот) — `docs/POST_PAPER_TEST_PLAN.md`.
  Гонки к деньгам нет; цель — стабильная система, а не быстрый go-live.
- **ADR-067 отработал (shadow ADR-060 день 1/30 · intraday-DD ADR-068 · архив исходов)** — дальше
  накопление эвиденса временем. **Петля ADR-066 закрыта; ложные 4 CRITICAL сторожа архитектуры
  СНЯТЫ (#169):** курация (`intent`) читается с `origin/main`, где решения и живут, механика
  остаётся локальной, расхождение НАЗЫВАЕТСЯ (проверка B6). Сторож стал строже, а не зеленее —
  3 замаскированных WARN «не переживёт ребут» вышли наружу. Остаток owner-gated: возить ли
  `architecture/` в прод-дерево (карточка заведена, рекомендация — не возить).
- **Владелец ответил 08.08 ДВАЖДЫ (1/1/A/A+C, затем 1/1/1/все15/да-да/да/1/А) — ВСЁ ИСПОЛНЕНО.**
  Детали в журнале W32. **Не переделывать:** подстановки APY убраны у ВСЕХ 36 адаптеров (храповик
  `adapter_fake_fallback_baseline.json` ПУСТ) · замок цикла спрашивает живость держателя ·
  `scripts/` синкается перед КАЖДЫМ запуском агента · ключ owner-gate починен · **ADR-074 ПРИНЯТ**,
  paper-модуль с ДВУМЯ руками построен · 6 агентов выведены, 3 из 4 поставлены (флот 77→81).
  Замер **#46**: лимит 20 % на имя стоит в максимуме Calmar ⇒ **ADR на пороги НЕ нужен**.
- **Телеграм — рабочее место владельца (ADR-069 + ADR-075).** Тревоги И решения приходят с
  вариантами из самой карточки (§2.4); нажатие = решение владельца, инв. #14 не ослаблен.
  Владелец в отпуске 08.08–18.08, связь только через ТГ.

## 📊 Оперативный снимок (дрейфует — истина в SYSTEM_BRIEFING)

_Источник — живой `docs/SYSTEM_BRIEFING.md` на **2026-08-08 10:44 UTC**._

| Поле | Значение |
|---|---|
| GoLive | ✅ **29/29 READY** — блокеров нет (ADR-067: две невосстановимые дыры 19.07/27.07 остаются в отчёте, но не блокируют бессрочно) |
| Трек | **46/30 evidenced** (anchor 2026-06-22); дальше по пути D → B, решение за владельцем |
| Portfolio | $100 791.39 (+0.79 % за 46 дней) · deployed 90 % · cash 10 % · ожидаемый APY 6.81 % · policy compliant |
| Позиции | `morpho_steakhouse` 40 % · `pendle` 20 % · `maple` 15 % · `compound_v3` 15 % (T1 55 / T2 35 / cash 10) |
| Агенты | 77 загружено, **76 OK / 1 WARN / 0 CRIT** (`daily_cycle` exit=2 — это ОТКАЗ замка, не авария, см. #161/#164) |
| Система | CRITICAL — `d6.health`; `d5.deployment.drift` тоже CRIT (#159: точка входа `com.spa.orchestrator` разошлась с origin) |
| Офис | 20/20 прочитано, НЕ ПРОЧИТАН 0 · conformance после #169 **0 CRITICAL / 4 WARN** (курация с origin) · chief **YELLOW** |

**Незакрытое:** fleet parity DRIFT (26 orphan-plist) · `d6_risk_gates` **CRITICAL** (не отказ
RiskPolicy — она compliant, а `health_score < 70`) · **LAZY 5 %** (~40 bps/год; ADR-076).

## 📋 Активные задачи

- [идёт временем] **Эвиденс взвода ADR-060** — 30 дней наблюдения, `net > 0`, hit-rate ≥ 60 %
  (пороги = мандат владельца, ADR-067). Сейчас день 1/30, `ready_to_arm: false`.
- [очередь] **~31 открытая карточка inbox** (`inbox-*`, `new`) — Tier-C 171/180 · 25 модулей
  `COVERAGE_UNMEASURED` · внедрения ADR-070/076 · ложный CRITICAL сторожа архитектуры (#168).
- [#168, ПОДНЯТО ИЗ /tmp] **У порога TVL стало ОДНО определение** (`spa_core/risk/tvl_floor.py`) —
  работа #165 умерла в worktree, поднята и перепроверена. Вторая половина (гейт не знает про
  ADR-064) — money-path, ждёт владельца.
- [ждёт владельца] **6 карточек `needs-owner`** (замер #169 из worktree от origin) — очередь ниже.
  Самое дорогое: 8 внерасписанных запусков цикла (#164, КТО звал — обёртка не пишет) и единое
  определение «живого TVL» (#168).

### 🔧 Сторожа очереди и памяти — десять циклов подряд, один класс дефекта

«Сторож отвечает не на тот вопрос». Подробности — `docs/journal/2026-W32.md`.

- [#146/#147/#149] **Шаг 0b запирал карточки НАВСЕГДА** (`rc=2`→`rc=1`) · **очередь показывала
  сделанное и прятала новое** (260 против 281, скрыты 3 вопроса владельца; теперь сверка с
  origin) · **шаг 0a морил очередь шумом** (18 ложных «НЕ ИЗМЕРЕНО», код 2→1).
- [#150–#154] **16 записей журнала объявлены утраченными, лёжа на месте** · **общая память
  уезжала вслепую** (ЗАГОЛОВОК, не ТЕЛО) · **защита была у карточек, но не у ЦИКЛА**.
- [#169] **Сторож архитектуры судил по СТЁРТОЙ памяти прода** — 4 ложных CRITICAL об исправных
  агентах. Курация — durable-запись в git, механика — факты хоста; граница уже была проведена
  генератором манифеста, сторож её игнорировал.
- [#170] **Карточка петли не доезжала до очереди владельца**: пуша в мосте не было ни строкой —
  из рождённых в рантайме доставлено **0 из 4** (7 «доехавших» из 11 уехали пушем разработчика).
  Доставка — `card_delivery.py`: пачкой, fail-CLOSED, квитанция и при отказе. Попутно ветка
  разбора моста в ОБЯЗАТЕЛЬНОМ шаге 0-офис была мертва (`findings_bridge.json` не производит
  никто) ⇒ `deferred` не печатался вопреки манифесту. Открыто карточкой: мост не способен
  закрыть свою же CRITICAL (`needs-owner` ≠ `new`) ⇒ ложная тревога вечна.
- [#157/#159/#163/#160] **Храповик дат метил ОБРАЗЦОВЫЙ паттерн** (детектор НЕ тронут) · **точка
  входа launchd числилась КОСМЕТИКОЙ** · **сетевой страж принимал ЧУЖОЙ маркер за свой** · **у
  тревоги был вход и не было ВЫХОДА** (`kill_switch` висел в «плохо» с 04.07 — беззвучно).
- [ЗАКРЫТО 08.08] **Замок дневного цикла спрашивает, ЖИВ ли держатель** (владелец, вар. 1); «не
  измерено» не блокирует навечно. `cycle_lock_watch` остаётся — он про ДРУГОЙ вопрос (8
  внерасписанных запусков), карточка ОТКРЫТА.
- [#167] **У номеров ADR не было распределителя** ⇒ 08.08 столкновения ДВАЖДЫ (073, 076).
  `adr_number.py next` мерит занятость СОЮЗОМ origin и дерева, интерлок пушера (код 7) ловит.

## 🗂️ Последние решения (одной строкой → ADR)

> Полный реестр — [`docs/decisions/INDEX.md`](decisions/INDEX.md). Здесь только последние;
> подробности каждого цикла — в `docs/journal/`, не тут.

- **ADR-076** (08.08, вариант 1) — **оживить фиды вне Ethereum**: кэш стоит не от лени, а
  потому что размещать НЕКУДА — книга на 90 %-лимите цепочки, живой TVL вне Ethereum есть у
  ОДНОГО протокола; у 4 Aave (base/arb/op/polygon) APY живой, а TVL — константа ⇒ ADR-053/064
  их справедливо не пускают. Пороги НЕ меняются: меняется, что система НАБЛЮДАЕТ. 3 карточки.
- **ADR-073 / ADR-075** — перераздача срезанного бюджета соблюдает лимиты сети · решения
  владельца приходят в Телеграм с вариантами (варианты ЧИТАЮТСЯ из карточки, инв. #14 цел).
- **ADR-069 / ADR-068 / ADR-067 / ADR-066** — кнопки под алертом · intraday-просадка 5 мин
  (пороги 5/10 % не менялись, изменилась ЧАСТОТА) · пакет решений владельца 06.08 ·
  контур архитектурного соответствия и замыкание петли решений (фазы 0–4, ЗАКРЫТ).
- **ADR-065 / ADR-064 / ADR-063** — Sky/sUSDS WL → T1 (инв. 10 выполнен, GSM 48.00 ч on-chain) ·
  личность пула по UUID DeFiLlama как условие TVL-доказательства · адаптеры читают реальную
  схему `adapter_status.json`, `DEFAULT_APY_PCT` убран.
- **ADR-062 / ADR-061 / ADR-060** — покрытие защитного слоя · гейт доказанности APY ·
  триггер ребаланса по улучшению доходности (раскатка shadow → armed).
- **ADR-048 / 050 / 053 / 054 / 055** — two-tier kill-switch (SOFT −5 % / HARD −10 % inclusive) ·
  RiskPolicy в governance-слой · RTMR sense-loop · авторитет kill-switch · Head-of-Investment слой.
- **ADR-YL-011 / ADR-YL-012 / ADR-OWN-2026-07** — Site Custodian · SPA Swarm (advisory) ·
  пакеты owner-решений (в т.ч. авто-шип сайта под owner-gate).

## ❓ Открытые вопросы владельцу

**Источник правды — трекер, не этот файл:** `nimbalyst-local/tracker/{own,owner-decision}-*.md`
со статусом `needs-owner`; обзор — `_BOARD.md`. Дубль списка здесь запрещён: прошлая копия
разошлась с очередью (9 против 19), а расходящийся дубль хуже пустоты.

```bash
python3 scripts/orchestrator_queue.py list --type owner-decision --status needs-owner --json
```

**Сейчас: 4** (замер #167: 8 запусков цикла · «живой TVL» гейт vs аллокатор · 7-Day Checkpoint ·
табличка честности не доезжает до сайта). Гонять ТОЛЬКО из worktree на `origin/main` — из
хост-дерева ответ неполный. Отвечать `needs-owner → owner-done`; **только владелец** (инв. #14).

## ⏸️ Отложено до MVP 2–3 (не потерять)

- **P3-стабы Yield Lab** (`docs/23,24,26,39–43`, ветка `yield-lab-scaffolding`) — «TODO: expand
  at MVP 2-3»; разворачивать при MVP 2–3, не раньше.
- **Остаток ROADMAP v2** — `docs/ROADMAP_2MONTH_EISENHOWER_v2.md`: открыты Q2-7, Q2-11/12, Q2-13,
  Q2-14, Q2-16, Q2-19, Q3-7; owner-gated Q1-5/6, Q2-3/4/5 → карточки `own-*`. **НЕ подхватывать
  автоматически** — приоритеты выбирает владелец.

## 👁️ Наблюдение

- **Резервный монитор сессий:** `claude-code-kanban` → http://localhost:4455 (read-only над
  `~/.claude`) — видит headless-сессии. Идёт ли цикл сейчас — `orchestrator_cycle_lock.py status`.

## 🔗 Ориентиры

- Инварианты: `CLAUDE.md` + `.claude/rules/`. Решения: `docs/decisions/INDEX.md`. Живой статус:
  `docs/SYSTEM_BRIEFING.md`. Журнал: `docs/journal/`. Карта: `docs/SYSTEM_MAP.md`. Идеи (не
  действовать): `docs/ideas/`. Черновики правил: `docs/rules-draft/`.
- Протокол оркестратора: `docs/ORCHESTRATOR_PROTOCOL.md`. Очередь: `nimbalyst-local/tracker/`.

> **ADR-070 (07.08): очередь владельца разобрана** (22 решения). Приоритеты: кэш в работу
> (ADR-072/073) · 3 paper-трека · аудит-как-код (ADR-071). Открыто 15 карточек внедрения.
