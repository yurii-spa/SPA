# SPA — STATE (текущее состояние проекта)

> Живой файл состояния. Оркестратор читает в начале цикла и обновляет в конце.
> Живые оперативные цифры — `docs/SYSTEM_BRIEFING.md` (auto, 30 мин). Здесь — фокус,
> задачи, решения, вопросы. **Максимум ~150 строк.**
>
> **Хроника циклов живёт в `docs/journal/<неделя>.md`, а НЕ здесь.** Сюда — только то, что меняет
> ответ на «что делать дальше». Предел держит храповик `test_state_md_length_ratchet.py` (только
> вниз). Снимок до сокращения (1920 строк) — [архив](journal/STATE-archive-2026-08-07.md).

## 🎯 Текущий фокус

- **Paper-трек добран и продолжается: 46/30 evidenced-дней** (anchor 2026-06-22). Владелец выбрал
  путь **D → B** (сначала укрепление, потом крошечный реальный пилот) — `docs/POST_PAPER_TEST_PLAN.md`.
  Гонки к деньгам нет; цель — стабильная система, а не быстрый go-live.
- **ADR-067 отработал (shadow ADR-060 день 1/30 · intraday-DD ADR-068 · архив исходов)** — дальше
  накопление эвиденса временем. **Петля ADR-066 закрыта; ложные 4 CRITICAL сторожа архитектуры
  СНЯТЫ (#169):** курация (`intent`) читается с `origin/main`, механика остаётся локальной,
  расхождение НАЗЫВАЕТСЯ (B6). Сторож стал строже, а не зеленее. Остаток owner-gated: возить ли
  `architecture/` в прод-дерево (рекомендация — не возить).
- **Владелец ответил 08.08 ДВАЖДЫ — ВСЁ ИСПОЛНЕНО** (детали в журнале W32). **Не переделывать:** подстановки APY убраны у ВСЕХ 36 адаптеров (храповик ПУСТ) · замок цикла спрашивает живость держателя · `scripts/` синкается перед КАЖДЫМ запуском · ключ owner-gate починен · **ADR-074 ПРИНЯТ** · флот 77→81. Замер **#46**: лимит 20 % = максимум Calmar ⇒ ADR не нужен.
- **Телеграм — рабочее место владельца (ADR-069 + ADR-075).** Тревоги И решения приходят с вариантами из самой карточки (§2.4); нажатие = решение владельца, инв. #14 не ослаблен. Владелец в отъезде до ~19.08 — работаем по мандату **ADR-078**, связь только через ТГ.

## 📊 Оперативный снимок (дрейфует — истина в SYSTEM_BRIEFING)

_Источник — живой `docs/SYSTEM_BRIEFING.md` на **2026-08-09 05:44 UTC**._

| Поле | Значение |
|---|---|
| GoLive | ✅ **29/29 READY** (ADR-067: дыры 19.07/27.07 в отчёте, но не блокируют бессрочно) |
| Трек | **47/30 evidenced** (anchor 2026-06-22); дальше путь D → B, решение за владельцем |
| Portfolio | $100 803.24 (+0.80 % за 47 дней) · deployed 90 % · cash 10 % · APY 6.18 % · compliant |
| Позиции | `aave_v3` 40 % · `pendle` 20 % · `maple` 20 % · `compound_v3` 10 % (T1 50 / T2 40) |
| Агенты | загружен 81, **78 OK / 0 WARN / 0 CRIT** из 78 |
| Система | WARNING — OK 38 / WARN 5 / CRIT 0 (`d1_data_pipeline`, `d5_code_integrity`, `d_riskwire`) |
| Офис | 20/20 прочитано, НЕ ПРОЧИТАН 0 · conformance **OK 0/0/0** · chief **YELLOW** · с #176 у каждого артефакта виден ВОЗРАСТ |

**Незакрытое:** fleet parity DRIFT (26 orphan-plist) · **LAZY 5 %** (~22 bps/год; ADR-076) · доставка карточек моста честна (`IDLE`, 0 на origin) · **долгожители крутят код недельной давности** (#177/#178, задача агента по ADR-078).

## 📋 Активные задачи

- [идёт временем] **Эвиденс взвода ADR-060** — 30 дней наблюдения, `net > 0`, hit-rate ≥ 60 % (пороги = мандат владельца, ADR-067). Сейчас день 1/30, `ready_to_arm: false`; при всех трёх — взвод АВТОМАТИЧЕСКИЙ (ADR-078).
- [очередь] **43 открытые карточки inbox** (`inbox-*`, `new`) — Tier-C 171/180 · 25 модулей `COVERAGE_UNMEASURED` · внедрения ADR-070/076 · оживление фидов вне Ethereum.
- [#168, ПОДНЯТО ИЗ /tmp] **У порога TVL ОДНО определение** (`spa_core/risk/tvl_floor.py`); вторая
  половина (гейт не знает про ADR-064) — money-path, ждёт владельца.
- [#171, НЕ ПУБЛИКОВАТЬ] **Balanced — пустая книга:** `0` строк с `equity > 0` из 40 при 920 циклах
  с 22.06 ⇒ «идёт paper-трек» про Balanced на сайте запрещено (инв. #8). Карточка
  `inbox-sbalansirovannyi-trek-nol-profinansirova` — проверить фактом.
- [первым же циклом] **Долгожители крутят старый код:** `apiserver` — от 17.07 (23 дня),
  `familyfund` — от 02.07 (38 дней), перемерено #178 (`ps -o lstart`). Вопрос #177 к владельцу
  **переклассифицирован в задачу агента** — ADR-078 прямо разрешает флот. Перезапуск отдельным
  циклом с `deployment_acceptance` до/после: `agent-dolgozhivuschie-agenty-krutyat-staryi-kod`.
- [очередь] **`needs-owner` = 0, `owner-done` = 0, promotions = 0** (замер #178 из worktree,
  374 карточки). Владелец в отпуске до ~19.08; **ADR-078 — мандат автономии**: архитектура,
  код, флот, paper-книги, приоритеты, сайт БЕЗ новых чисел — решаю сам; пороги RiskPolicy,
  kill-switch, реальный капитал, числа/тиры/legal — по-прежнему нет. **Пустой бэклог = находка.**
- [#174, закрыто] **Офис рекомендовал числа, которых никто не наблюдал** (`apy_aggregator` вёл
  `apy or fallback_apy`). Остаток: тот же `or` глотает наблюдённый НОЛЬ — `inbox-nablyudennyi-nol-*`.

### 🔧 Сторожа очереди и памяти — один класс: «сторож отвечает не на тот вопрос»

Подробности каждого случая — `docs/journal/2026-W32.md`.

- [#178, закрыто] **Вердикт сверки с origin не доезжал до того, кто его читает.** Шаг 2 предписан
  как `list … --json` (контракт — stdout), а сторож #147 говорил ТОЛЬКО stderr-прозой. Замер на
  прод-дереве: шаг 2 выдал **2 карточки `owner-done`, обе на origin давно `ingested`** — этот же
  цикл начал разбирать их заново. Теперь `origin_check` есть у КАЖДОЙ карточки в JSON (нет сверки
  ⇒ `unmeasured`), а «кто новее НЕ измерено» для `diverged` **доизмеряется** статусом на origin;
  карточка не выбрасывается — сторож называет, решает сессия. **Причина осталась** (бот пишет в
  хост-дерево, инжест — на origin; `owner_choice` в git не попадает): `inbox-otvet-vladeltsa-*`.
- [#176, закрыто] **Шаг 0-офис печатал ПУСТОТУ вместо содержимого файлов** — ТРЕТИЙ рецидив в
  ОДНОМ файле (`consume_office_reports.py`): ветки читали поля, которых производитель не пишет.
  «Проверить все ветки» сделано ИЗМЕРЕНИЕМ (`_READ_SCHEMA` сверяется с живым документом), не осмотром.
- [#146–#170, все закрыты] шаг 0b запирал карточки навсегда · очередь прятала новое · шаг 0a
  морил шумом · общая память уезжала вслепую · защита была у карточек, но не у ЦИКЛА · точка входа
  launchd числилась косметикой · у тревоги не было ВЫХОДА · сторож архитектуры судил по СТЁРТОЙ
  памяти прода · карточка петли не доезжала до очереди владельца (0 из 4).
- [#172, закрыто] **Вопрос владельца не закрывался НИКОГДА** (`close_card` знал только `new`).
  **Не видели потому, что подделка очереди в тестах писала `new` всем.**
- [#171, закрыто] **Одобрение владельца не исполнялось НИЧЕМ** — обход owner-gate был порван в
  ПЯТИ местах. **Урок:** у починки, которую не провели сквозь весь путь, нет причины работать.
- [#173, закрыто] **Приёмка деплоя судила одно дерево, а отвечала про другое** ⇒ `OK` про непроверенный прод (ложная ТИШИНА).
- [#171, НЕ починено] **Статусы карточек переписываются сами** при любой записи в файл; кто пишет —
  НЕ измерено: `inbox-statusy-kartochek-vladeltsa-perepisalis`.
- [#175, закрыто — класс ВЫВЕРНУЛСЯ] **Соврал не сторож, а ЖАЛОБА на него.** Монитор прав по
  контракту, лгали два ассерта «а сегодня ли?»; починка «по описанию» внесла бы ровно ту дыру,
  которой боялись. **Диагноз в карточке — гипотеза, а не измерение.**

## 🗂️ Последние решения (одной строкой → ADR)

> Полный реестр — [`docs/decisions/INDEX.md`](decisions/INDEX.md). Здесь только последние;
> подробности каждого цикла — в `docs/journal/`, не тут.

- **ADR-078** (09.08) — **мандат 10-дневной автономии** (до ~19.08): архитектура/код/тесты, флот
  агентов, paper-книги, приоритеты, R&D и сайт БЕЗ новых чисел — решаю сам, без карточки. НЕЛЬЗЯ:
  пороги RiskPolicy, kill-switch, реальный капитал, числа/тиры/legal, активация HoI, секреты.
  Yield-триггер ADR-060 взводится АВТОМАТИЧЕСКИ при всех трёх критериях. Пустой бэклог = находка.
- **ADR-076** (08.08, вар. 1) — **оживить фиды вне Ethereum**: кэш стоит не от лени, а размещать НЕКУДА (книга на 90 %-лимите цепочки, живой TVL вне Ethereum у ОДНОГО протокола). Пороги НЕ меняются.
- **ADR-073 / 075** — перераздача срезанного бюджета соблюдает лимиты сети · решения владельца приходят в Телеграм с вариантами (читаются из карточки, инв. #14 цел).
- **ADR-069 / ADR-068 / ADR-067 / ADR-066** — кнопки под алертом · intraday-просадка 5 мин
  (пороги 5/10 % не менялись, изменилась ЧАСТОТА) · пакет решений владельца 06.08 ·
  контур архитектурного соответствия и замыкание петли решений (фазы 0–4, ЗАКРЫТ).
- **ADR-065 / 064 / 063** — Sky/sUSDS WL → T1 (инв. 10 выполнен, GSM 48.00 ч on-chain) · личность
  пула по UUID DeFiLlama как условие TVL-доказательства · адаптеры читают реальную схему
  `adapter_status.json`, `DEFAULT_APY_PCT` убран.
- **ADR-062 / 061 / 060** — покрытие защитного слоя · гейт доказанности APY · триггер ребаланса
  по улучшению доходности (раскатка shadow → armed).
- **ADR-048 / 050 / 053 / 054 / 055** — two-tier kill-switch (SOFT −5 % / HARD −10 % inclusive) ·
  RiskPolicy в governance-слой · RTMR sense-loop · авторитет kill-switch · Head-of-Investment слой.
- **ADR-YL-011 / YL-012 / OWN-2026-07** — Site Custodian · SPA Swarm (advisory) · пакеты
  owner-решений (в т.ч. авто-шип сайта под owner-gate).

## ❓ Открытые вопросы владельцу

**Источник правды — трекер, не этот файл:** `nimbalyst-local/tracker/{own,owner-decision}-*.md`
со статусом `needs-owner`; обзор — `_BOARD.md`. Дубль списка здесь запрещён: прошлая копия
разошлась с очередью (9 против 19), а расходящийся дубль хуже пустоты.

```bash
python3 scripts/orchestrator_queue.py list --type owner-decision --status needs-owner --json
```

**Сейчас: 0** (замер #175 из worktree на `origin/main`; там же `owner-done` 0, promotions 0 —
прежняя цифра «7» осталась от #171 и разошлась с очередью). Гонять ТОЛЬКО из worktree на
`origin/main`. Отвечать `needs-owner → owner-done`; **только владелец** (инв. #14).

## ⏸️ Отложено до MVP 2–3 (не потерять)

- **P3-стабы Yield Lab** (`docs/23,24,26,39–43`, ветка `yield-lab-scaffolding`) — «TODO: expand
  at MVP 2-3»; разворачивать при MVP 2–3, не раньше.
- **Остаток ROADMAP v2** — `docs/ROADMAP_2MONTH_EISENHOWER_v2.md`: открыты Q2-7, Q2-11/12, Q2-13,
  Q2-14, Q2-16, Q2-19, Q3-7; owner-gated Q1-5/6, Q2-3/4/5 → карточки `own-*`. **НЕ подхватывать
  автоматически** — приоритеты выбирает владелец.

## 👁️ Наблюдение

- **Монитор сессий:** `claude-code-kanban` → http://localhost:4455 (видит headless); идёт ли цикл — `orchestrator_cycle_lock.py status`.

## 🔗 Ориентиры

- Инварианты: `CLAUDE.md` + `.claude/rules/`. Решения: `docs/decisions/INDEX.md`. Живой статус:
  `docs/SYSTEM_BRIEFING.md`. Журнал: `docs/journal/`. Карта: `docs/SYSTEM_MAP.md`. Протокол:
  `docs/ORCHESTRATOR_PROTOCOL.md`. Очередь: `nimbalyst-local/tracker/`. Идеи (НЕ действовать):
  `docs/ideas/`, `docs/rules-draft/`.

> **ADR-070 (07.08): очередь владельца разобрана** (22 решения). Приоритеты: кэш в работу
> (ADR-072/073) · 3 paper-трека · аудит-как-код (ADR-071). 15 карточек внедрения.
