# SPA — STATE (текущее состояние проекта)

> Живой файл состояния. Оркестратор читает в начале цикла и обновляет в конце.
> Живые оперативные цифры — `docs/SYSTEM_BRIEFING.md` (auto, 30 мин). Здесь — фокус,
> задачи, решения, вопросы. **Максимум ~150 строк.**
>
> **Хроника циклов живёт в `docs/journal/<неделя>.md`, а НЕ здесь.** Сюда пишется только то,
> что меняет ответ на вопрос «что делать дальше». Предел закреплён храповиком
> `spa_core/tests/test_state_md_length_ratchet.py` (длина только вниз) — так же, как храповик
> литеральных дат: жёсткий лимит покраснел бы сразу и его бы отключили. Снимок файла до
> сокращения (1920 строк) — [`docs/journal/STATE-archive-2026-08-07.md`](journal/STATE-archive-2026-08-07.md).

## 🎯 Текущий фокус

- **Paper-трек добран и продолжается: 46/30 evidenced-дней** (anchor 2026-06-22). Владелец выбрал
  путь **D → B** (сначала укрепление, потом крошечный реальный пилот) — `docs/POST_PAPER_TEST_PLAN.md`.
  Гонки к деньгам нет; цель — стабильная система, а не быстрый go-live.
- **ADR-067 отработал (shadow ADR-060 день 1/30 · intraday-DD ADR-068 · архив исходов)** — дальше
  накопление эвиденса временем. **Петля ADR-066 закрыта; ложные 4 CRITICAL сторожа архитектуры
  СНЯТЫ (#169):** курация (`intent`) читается с `origin/main`, где решения и живут, механика
  остаётся локальной, расхождение НАЗЫВАЕТСЯ (проверка B6). Сторож стал строже, а не зеленее —
  3 замаскированных WARN «не переживёт ребут» вышли наружу. Остаток owner-gated: возить ли
  `architecture/` в прод-дерево (карточка заведена, рекомендация — не возить).
- **Владелец ответил 08.08 ДВАЖДЫ (1/1/A/A+C, затем 1/1/1/все15/да-да/да/1/А) — ВСЁ ИСПОЛНЕНО.**
  Детали в журнале W32. **Не переделывать:** подстановки APY убраны у ВСЕХ 36 адаптеров (храповик
  `adapter_fake_fallback_baseline.json` ПУСТ) · замок цикла спрашивает живость держателя ·
  `scripts/` синкается перед КАЖДЫМ запуском агента · ключ owner-gate починен · **ADR-074 ПРИНЯТ**,
  paper-модуль с ДВУМЯ руками построен · 6 агентов выведены, 3 из 4 поставлены (флот 77→81).
  Замер **#46**: лимит 20 % на имя стоит в максимуме Calmar ⇒ **ADR на пороги НЕ нужен**.
- **Телеграм — рабочее место владельца (ADR-069 + ADR-075).** Тревоги И решения приходят с
  вариантами из самой карточки (§2.4); нажатие = решение владельца, инв. #14 не ослаблен.
  Владелец в отпуске 08.08–18.08, связь только через ТГ.

## 📊 Оперативный снимок (дрейфует — истина в SYSTEM_BRIEFING)

_Источник — живой `docs/SYSTEM_BRIEFING.md` на **2026-08-08 21:44 UTC**._

| Поле | Значение |
|---|---|
| GoLive | ✅ **29/29 READY** (ADR-067: дыры 19.07/27.07 в отчёте, но не блокируют бессрочно) |
| Трек | **46/30 evidenced** (anchor 2026-06-22); дальше путь D → B, решение за владельцем |
| Portfolio | $100 787.99 (+0.79 % за 46 дней) · deployed 90 % · cash 10 % · APY 5.44 % · compliant |
| Позиции | `morpho_steakhouse` 40 % · `pendle` 20 % · `maple` 15 % · `aave_v3` 15 % (T1 55 / T2 35) |
| Агенты | загружен 81, **78 OK / 0 WARN / 0 CRIT** из 78 |
| Система | WARNING — OK 38 / WARN 5 / CRIT 0 (`d1_data_pipeline`, `d5_code_integrity`, `d_riskwire`) |
| Офис | 20/20 прочитано, НЕ ПРОЧИТАН 0 · conformance **OK 0/0/0** · chief **YELLOW** |

**Незакрытое:** fleet parity DRIFT (26 orphan-plist) · **LAZY 5 %** (~25 bps/год; ADR-076) ·
доставка карточек моста в отчёте **НЕ ИЗМЕРЕНА** (нет блока `delivery` — код #170 в прод-дереве
свежий, но отчёт писал старый).

## 📋 Активные задачи

- [идёт временем] **Эвиденс взвода ADR-060** — 30 дней наблюдения, `net > 0`, hit-rate ≥ 60 %
  (пороги = мандат владельца, ADR-067). Сейчас день 1/30, `ready_to_arm: false`.
- [очередь] **~31 открытая карточка inbox** (`inbox-*`, `new`) — Tier-C 171/180 · 25 модулей
  `COVERAGE_UNMEASURED` · внедрения ADR-070/076 · ложный CRITICAL сторожа архитектуры (#168).
- [#168, ПОДНЯТО ИЗ /tmp] **У порога TVL стало ОДНО определение** (`spa_core/risk/tvl_floor.py`) —
  работа #165 умерла в worktree, поднята и перепроверена. Вторая половина (гейт не знает про
  ADR-064) — money-path, ждёт владельца.
- [#171, ОПУБЛИКОВАНО ВЛАДЕЛЬЦЕМ] **Сайт: статусы тиров «идёт paper-трек»** уехали в live
  (`39adb9d6f`, интерактивное «да, обновить») — включая Balanced, см. ниже.
- [#171, НЕ ПУБЛИКОВАТЬ] **Balanced — пустая книга:** `0` строк с `equity > 0` из 40 и `0` из 41
  при 920/931 циклах с 22.06 (засев лёг в шапку, строка на дату пишется один раз) ⇒ «идёт
  paper-трек» про Balanced на сайте запрещено (инв. #8). Карточка
  `inbox-sbalansirovannyi-trek-nol-profinansirova` — **проверить фактом, а не поверить**.
- [ждёт владельца] **7 карточек `needs-owner`** — очередь ниже. Самое дорогое: 8 внерасписанных
  запусков цикла (#164) и единое определение «живого TVL» (#168).

### 🔧 Сторожа очереди и памяти — один класс: «сторож отвечает не на тот вопрос»

Подробности каждого случая — `docs/journal/2026-W32.md`.

- [#146–#167, все закрыты] шаг 0b запирал карточки навсегда · очередь прятала новое (260 против
  281) · шаг 0a морил шумом · 16 записей журнала «утрачены» лёжа на месте · общая память уезжала
  вслепую · защита была у карточек, но не у ЦИКЛА · храповик дат метил образцовый паттерн ·
  точка входа launchd числилась косметикой · сетевой страж принимал чужой маркер за свой ·
  у тревоги не было ВЫХОДА · у номеров ADR не было распределителя.
- [#169/#170] **Сторож архитектуры судил по СТЁРТОЙ памяти прода** (4 ложных CRITICAL) ·
  **карточка петли не доезжала до очереди владельца** (из рождённых в рантайме 0 из 4; доставка
  `card_delivery.py`). Открыто: мост не закрывает свою же CRITICAL (`needs-owner` ≠ `new`).
- [#171] **Одобрение владельца не исполнялось НИЧЕМ.** Обход owner-gate порван в ПЯТИ местах;
  две половины «починены» 08.08, механизм всё равно был мёртв: читалось `card.frontmatter`,
  которого у `Card` нет (есть `fields`) ⇒ scope пуст всегда · шаблон трейлера не брал
  `owner-decision-…`, то есть id, который гейт САМ и генерирует · `safe_site_push` не писал
  `approves:`. Починены 3 звена как доделка решения владельца (вариант А); защита не ослаблена.
  **Урок:** у починки, которую не провели сквозь весь путь, нет причины работать.
- [#171, НЕ починено] **Статусы карточек переписываются сами** при любой записи в файл карточки
  (два случая за цикл; живой вопрос владельцу закрылся сам). Кто пишет — НЕ измерено, карточка
  `inbox-statusy-kartochek-vladeltsa-perepisalis`.

## 🗂️ Последние решения (одной строкой → ADR)

> Полный реестр — [`docs/decisions/INDEX.md`](decisions/INDEX.md). Здесь только последние;
> подробности каждого цикла — в `docs/journal/`, не тут.

- **ADR-076** (08.08, вар. 1) — **оживить фиды вне Ethereum**: кэш стоит не от лени, а размещать
  НЕКУДА (книга на 90 %-лимите цепочки, живой TVL вне Ethereum у ОДНОГО протокола; у 4 Aave APY
  живой, TVL — константа ⇒ ADR-053/064 их не пускают). Пороги НЕ меняются. 3 карточки.
- **ADR-073 / 075** — перераздача срезанного бюджета соблюдает лимиты сети · решения владельца
  приходят в Телеграм с вариантами (читаются из карточки, инв. #14 цел).
- **ADR-069 / ADR-068 / ADR-067 / ADR-066** — кнопки под алертом · intraday-просадка 5 мин
  (пороги 5/10 % не менялись, изменилась ЧАСТОТА) · пакет решений владельца 06.08 ·
  контур архитектурного соответствия и замыкание петли решений (фазы 0–4, ЗАКРЫТ).
- **ADR-065 / 064 / 063** — Sky/sUSDS WL → T1 (инв. 10 выполнен, GSM 48.00 ч on-chain) · личность
  пула по UUID DeFiLlama как условие TVL-доказательства · адаптеры читают реальную схему
  `adapter_status.json`, `DEFAULT_APY_PCT` убран.
- **ADR-062 / 061 / 060** — покрытие защитного слоя · гейт доказанности APY · триггер ребаланса
  по улучшению доходности (раскатка shadow → armed).
- **ADR-048 / 050 / 053 / 054 / 055** — two-tier kill-switch (SOFT −5 % / HARD −10 % inclusive) ·
  RiskPolicy в governance-слой · RTMR sense-loop · авторитет kill-switch · Head-of-Investment слой.
- **ADR-YL-011 / YL-012 / OWN-2026-07** — Site Custodian · SPA Swarm (advisory) · пакеты
  owner-решений (в т.ч. авто-шип сайта под owner-gate).

## ❓ Открытые вопросы владельцу

**Источник правды — трекер, не этот файл:** `nimbalyst-local/tracker/{own,owner-decision}-*.md`
со статусом `needs-owner`; обзор — `_BOARD.md`. Дубль списка здесь запрещён: прошлая копия
разошлась с очередью (9 против 19), а расходящийся дубль хуже пустоты.

```bash
python3 scripts/orchestrator_queue.py list --type owner-decision --status needs-owner --json
```

**Сейчас: 7** (#171: + правка сайта Aggressive `…owner-gat-3`; было 4 — 8 запусков цикла ·
«живой TVL» гейт vs аллокатор · 7-Day Checkpoint · табличка честности). Гонять ТОЛЬКО из
worktree на `origin/main`. Отвечать `needs-owner → owner-done`; **только владелец** (инв. #14).

## ⏸️ Отложено до MVP 2–3 (не потерять)

- **P3-стабы Yield Lab** (`docs/23,24,26,39–43`, ветка `yield-lab-scaffolding`) — «TODO: expand
  at MVP 2-3»; разворачивать при MVP 2–3, не раньше.
- **Остаток ROADMAP v2** — `docs/ROADMAP_2MONTH_EISENHOWER_v2.md`: открыты Q2-7, Q2-11/12, Q2-13,
  Q2-14, Q2-16, Q2-19, Q3-7; owner-gated Q1-5/6, Q2-3/4/5 → карточки `own-*`. **НЕ подхватывать
  автоматически** — приоритеты выбирает владелец.

## 👁️ Наблюдение

- **Монитор сессий:** `claude-code-kanban` → http://localhost:4455 (видит headless);
  идёт ли цикл — `orchestrator_cycle_lock.py status`.

## 🔗 Ориентиры

- Инварианты: `CLAUDE.md` + `.claude/rules/`. Решения: `docs/decisions/INDEX.md`. Живой статус:
  `docs/SYSTEM_BRIEFING.md`. Журнал: `docs/journal/`. Карта: `docs/SYSTEM_MAP.md`. Идеи (не
  действовать): `docs/ideas/`. Черновики правил: `docs/rules-draft/`.
- Протокол оркестратора: `docs/ORCHESTRATOR_PROTOCOL.md`. Очередь: `nimbalyst-local/tracker/`.

> **ADR-070 (07.08): очередь владельца разобрана** (22 решения). Приоритеты: кэш в работу
> (ADR-072/073) · 3 paper-трека · аудит-как-код (ADR-071). 15 карточек внедрения.
