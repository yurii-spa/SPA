# SPA — STATE (текущее состояние проекта)

> Живой файл состояния. Оркестратор читает в начале цикла и обновляет в конце.
> Живые оперативные цифры — `docs/SYSTEM_BRIEFING.md` (auto, 30 мин). Здесь — фокус,
> задачи, решения, вопросы. **Максимум ~150 строк.**
>
> **Хроника циклов живёт в `docs/journal/<неделя>.md`, а НЕ здесь.** Сюда — только то, что меняет
> ответ на «что делать дальше». Предел держит храповик `test_state_md_length_ratchet.py` (только
> вниз). Снимок до сокращения (1920 строк) — [архив](journal/STATE-archive-2026-08-07.md).

## 🎯 Текущий фокус

- **Paper-трек добран и продолжается: 46/30 evidenced-дней** (anchor 2026-06-22). Владелец выбрал
  путь **D → B** (сначала укрепление, потом крошечный реальный пилот) — `docs/POST_PAPER_TEST_PLAN.md`.
  Гонки к деньгам нет; цель — стабильная система, а не быстрый go-live.
- **ADR-067 отработал (shadow ADR-060 день 1/30 · intraday-DD ADR-068 · архив исходов)** — дальше
  накопление эвиденса временем. **Петля ADR-066 закрыта; ложные 4 CRITICAL сторожа архитектуры
  СНЯТЫ (#169):** курация (`intent`) читается с `origin/main`, механика остаётся локальной,
  расхождение НАЗЫВАЕТСЯ (B6). Сторож стал строже, а не зеленее. Остаток owner-gated: возить ли
  `architecture/` в прод-дерево (рекомендация — не возить).
- **Владелец ответил 08.08 ДВАЖДЫ — ВСЁ ИСПОЛНЕНО** (детали в журнале W32). **Не переделывать:** подстановки APY убраны у ВСЕХ 36 адаптеров (храповик ПУСТ) · замок цикла спрашивает живость держателя · `scripts/` синкается перед КАЖДЫМ запуском · ключ owner-gate починен · **ADR-074 ПРИНЯТ** · флот 77→81. Замер **#46**: лимит 20 % = максимум Calmar ⇒ ADR не нужен.
- **Телеграм — рабочее место владельца (ADR-069 + ADR-075).** Тревоги И решения приходят с вариантами из самой карточки (§2.4); нажатие = решение владельца, инв. #14 не ослаблен. Владелец в отъезде до ~19.08 — работаем по мандату **ADR-078**, связь только через ТГ.
- ✅ **Остановка 10.08 СНЯТА решением владельца** (13:45Z, вариант 1) и исполнена: цикл идёт,
  книга размещена (deployed 90 %, cash 10 %). Кворум EB-02 поднят 3 → 4 его же решением
  (**ADR-079**) — причина (живой APY против литеральной константы) НЕ вылечена, названа честно.

## 📊 Оперативный снимок (дрейфует — истина в SYSTEM_BRIEFING)

_Источник — живой `docs/SYSTEM_BRIEFING.md` + `data/current_positions.json` на **2026-08-10 17:37 UTC**._

| Поле | Значение |
|---|---|
| GoLive | ✅ **29/29 READY** (восстановление подтверждено, `own-34`) |
| Трек | **48/30 evidenced** (anchor 2026-06-22); **три «доказанных» дня — с ПУСТОЙ книгой** (28.07, 29.07, 10.08) — вопрос владельцу `own-32` отвечен, исполнение в очереди |
| Portfolio | $100 816.53 · deployed **90 %** · cash 10 % · compliant · цикл 17:26Z HEALTHY |
| Позиции | восстановлены после снятия остановки (`aave_v3` · `pendle` · `maple` · `compound_v3`) |
| Агенты | загружен 81, 78 OK из 78 |
| Система | WARNING — OK 38 / WARN 5 / CRIT 0 (`d1_data_pipeline`, `d5_code_integrity`, `d_riskwire`) |
| Офис | 20/20 прочитано, НЕ ПРОЧИТАН 0 · conformance **OK 0/0/0** · chief **YELLOW** · с #176 у каждого артефакта виден ВОЗРАСТ |

**Незакрытое:** fleet parity DRIFT (26 orphan-plist) · **LAZY** (ADR-076; с #186 у кэша ЕСТЬ куда встать вне Ethereum — три Aave по UUID проходят floor живым TVL, Optimism честно НЕ проходит) · **долгожители крутят код недельной давности** (#177/#178, задача агента по ADR-078) · **расхождение двух путей данных** (`aave_v3` 4.80 % против 2.36 %; `pendle` 14.01 % при отсутствии живого замера вовсе).

## 📋 Активные задачи

- [идёт временем] **Эвиденс взвода ADR-060** — 30 дней наблюдения, `net > 0`, hit-rate ≥ 60 % (пороги = мандат владельца, ADR-067). Сейчас день 1/30, `ready_to_arm: false`; при всех трёх — взвод АВТОМАТИЧЕСКИЙ (ADR-078).
- [очередь] **52 открытых карточки inbox на `origin`** (`inbox-*`, `new`; замер #200) — Tier-C 171/180 · 25 модулей `COVERAGE_UNMEASURED` · внедрения ADR-070/076 · **у `aave_v3` (Ethereum, 40 % книги) TVL «не измерено»: константа $12B против наблюдаемых $179.9M, пул в фиде есть**. В ПРОДЕ очередь своя (46 после чистки #200) — она с `origin` не синкается.
- [#207, закрыто] **Наблюдённый 0 % APY больше не подменяется литералом** (`apy_aggregator`, цепочка `or` глотала ноль): `stusd`/`wusdm` печатались 6.0/5.0 % и стояли ВКЛАДЧИКАМИ комнаты в тревоге о простое — фантом убран, начисление Engine B/C замерено НЕ изменившимся; офис отсеивает нулевой пул по своей причине. Второй экземпляр класса не найден.
- [#168, ПОДНЯТО ИЗ /tmp] **У порога TVL ОДНО определение** (`spa_core/risk/tvl_floor.py`); вторая половина (гейт не знает про ADR-064) — money-path, ждёт владельца.
- [#171, НЕ ПУБЛИКОВАТЬ] **Balanced — пустая книга:** `0` строк с `equity > 0` из 40 при 920 циклах с 22.06 ⇒ «идёт paper-трек» про Balanced на сайте запрещено (инв. #8); карточка `inbox-sbalansirovannyi-trek-nol-profinansirova`.
- [первым же циклом] **Долгожители крутят старый код** (`apiserver` 23,3 сут · `familyfund` 32,4; сторож `agent_code_freshness`) — перезапуск отдельным циклом с `deployment_acceptance` до/после: `agent-dolgozhivuschie-agenty-krutyat-staryi-kod`.
- [#191–#201, ЗАКРЫТО — журнал W33] Карточки без КНОПОК · кнопка вела в НИКУДА · у ОСТАНОВКИ не было
  сторожа «есть ли путь вверх?» · сторож «вопросы владельцу» видел только ОТПРАВЛЕННОЕ · недоступность
  классификатора приходила как ВЕРДИКТ (11.08 — **44 фантомных вопроса владельцу**, прод вычищен) ·
  `divergent_days` рос три дня без читателя (**18/54, худшее $215.99**), читатель — `SYSTEM_BRIEFING`. Сам `own-32` (два писателя кривой) НЕ чинился: money-path. **#206 не брал — держит мёртвая сессия, окно 3 ч не истекло.**
- [#193, УРОК] **Хост стухает** (автосинк возит только `spa_core/`·`scripts/`·`tests/`) — очередь, `docs/` и `CLAUDE.md` читать из worktree на `origin/main`.
- [#202→#205, ЗАКРЫТО и ПРОВЕРЕНО В ПРОДЕ] **Доставка карточек моста.** Пушер судил по базе рабочей
  копии, а карточка моста в HEAD прод-дерева не попадает НИКОГДА ⇒ обновление отбивалось навсегда
  (**ADR-080**, там же `--allow-overwrite` — флаг КОМАНДЫ); повтора при этом не было — три
  застрявшие (уже `closed`) не поехали бы НИКОГДА (**ADR-081**, долг). **#205 дождался живого
  прогона 19:03:18Z:** квитанция нового образца, `delivery.debt.count = 0`, шаг 0-офис ПОЛНОСТЬЮ
  зелёный. Обещание «зеленеет только измеренным нулём» подтверждено продом, а не рассуждением.
- [очередь] **`needs-owner` = 5** (возврат после жёсткой остановки · смысл мягкой ступени 5 % ·
  два дня трека с пустой книгой · `own-33` · `own-34`); `owner-done` = 0, promotions = 0,
  inbox `new` = 50 (замер #205). Четыре ответа владельца 10.08 исполнены и `ingested` (инв. #14).
  Владелец в отпуске до ~19.08; **ADR-078 — мандат автономии**: архитектура, код, флот,
  paper-книги, приоритеты, сайт БЕЗ новых чисел — решаю сам; пороги RiskPolicy, kill-switch,
  реальный капитал, числа/тиры/legal — нет. **Пустой бэклог = находка.**
### 🔧 Один класс на всю очередь: «сторож отвечает не на тот вопрос»

Хроника каждого случая (#146–#195) — `docs/journal/2026-W32.md` и `2026-W33.md`. Здесь только
то, что меняет ответ на «что делать дальше»:

- **Правило класса.** Зелёный ответ сторожа на СВОЙ вопрос никогда не есть ответ на нужный.
  Проверять эффект, а не исходник; сторожа писать ШИРЕ подопечного (иначе он его эхо, #197);
  ложный отказ опаснее пропуска, но и «не измерено» навсегда — тоже отказ.
- [#205] **Третий «критическое через отставленный канал»** (`run_health_check`: `p0` в `TelegramManager`
  + ЛОЖНЫЙ «cooldown active») → `push_policy`; вместо теста на файл — храповик по дереву (AST, база
  ПУСТА). Тревоги не терялось: корень неподключён, жив `cycle_gap_monitor`. **Две новые карточки:**
  гэп офиса сравнивает имена протоколов с КОРТЕЖАМИ (обе ветки мертвы, `moonwell_base` 8.33 % и
  `fluid_fusdc` ложно «недостижимы») · храповик неподключённых считает КОММЕНТАРИЙ за проводку (87→90).
- **Осознанно красное:** `edge_exposure_depth`; `test_risk_scoring_completeness` — закрыт владельцем (#197).
- **Приёмка.** Полный прогон командой CI (`spa_core/tests/ tests/ scripts/tests/ spa_core/analytics/gross_of/
  research/cards/`), контроль на чистом `origin/main` с ТЕМ ЖЕ sha, мутации с проверкой якоря (#192, #197).

## 🗂️ Последние решения (одной строкой → ADR)

> Полный реестр — [`docs/decisions/INDEX.md`](decisions/INDEX.md). Здесь только последние;
> подробности каждого цикла — в `docs/journal/`, не тут.

- **ADR-078** (09.08) — **мандат 10-дневной автономии** (до ~19.08): архитектура/код/тесты, флот
  агентов, paper-книги, приоритеты, R&D и сайт БЕЗ новых чисел — решаю сам, без карточки. НЕЛЬЗЯ:
  пороги RiskPolicy, kill-switch, реальный капитал, числа/тиры/legal, активация HoI, секреты.
  Yield-триггер ADR-060 взводится АВТОМАТИЧЕСКИ при всех трёх критериях. Пустой бэклог = находка.
- **ADR-081** (12.08, #204) — у провалившейся доставки карточек есть **долг**, повторяемый каждым
  прогоном до попадания на `origin`; восстанавливается и из последней квитанции, `IDLE` при непустом
  долге запрещён, непригодный должник снимается и НАЗЫВАЕТСЯ. Читатель — шаг 0-офис.
- **ADR-080** (12.08, #202→#203) — доставка карточек моста **переносит правку на прочитанный
  `origin`**: перенос только доказуемый, ответ владельца отменяет закрытие, непрочитанный — ждёт.
- **ADR-079** (10.08, вар. 3 владельца) — **кворум EB-02 поднят 3 → 4** после остановки на
  минимально возможном кворуме (три протокола, ни одного отказа фида). ЧЕСТНО: причину — сравнение
  живого APY с литеральной константой — это НЕ чинит, кворум 4 будет достигнут тем же дрейфом;
  ценой стала слепота сторожа к аварии, задевшей ровно 3 протокола. Порог 500 бп не менялся.
- **ADR-076** (08.08, вар. 1) — **оживить фиды вне Ethereum**: кэш стоит не от лени, а размещать НЕКУДА (книга на 90 %-лимите цепочки, живой TVL вне Ethereum у ОДНОГО протокола). Пороги НЕ меняются.
- **ADR-073 / 075 / 069 / 068 / 067 / 066** — перераздача срезанного бюджета соблюдает лимиты
  сети · решения владельца приходят в Телеграм с вариантами (инв. #14 цел) · кнопки под алертом ·
  intraday-просадка 5 мин (пороги 5/10 % не менялись, изменилась ЧАСТОТА) · пакет решений
  владельца 06.08 · контур архитектурного соответствия (фазы 0–4, ЗАКРЫТ).
- **ADR-065 / 064 / 063** — Sky/sUSDS WL → T1 (инв. 10 выполнен, GSM 48.00 ч on-chain) · личность
  пула по UUID DeFiLlama как условие TVL-доказательства · адаптеры читают реальную схему
  `adapter_status.json`, `DEFAULT_APY_PCT` убран.
- **ADR-062 / 061 / 060** — покрытие защитного слоя · гейт доказанности APY · триггер ребаланса
  по улучшению доходности (раскатка shadow → armed).
- **ADR-048 / 050 / 053 / 054 / 055** — two-tier kill-switch (SOFT −5 % / HARD −10 % inclusive) ·
  RiskPolicy в governance-слой · RTMR sense-loop · авторитет kill-switch · Head-of-Investment слой.
- **ADR-YL-011 / YL-012 / OWN-2026-07** — Site Custodian · SPA Swarm (advisory) · пакеты
  owner-решений (в т.ч. авто-шип сайта под owner-gate).

## ❓ Открытые вопросы владельцу

**Источник правды — трекер, не этот файл:** `nimbalyst-local/tracker/{own,owner-decision}-*.md`
со статусом `needs-owner`; обзор — `_BOARD.md`. Дубль списка здесь запрещён: прошлая копия
разошлась с очередью (9 против 19), а расходящийся дубль хуже пустоты.

```bash
python3 scripts/orchestrator_queue.py list --type owner-decision --status needs-owner --json
```

**Сейчас: 5** (замер #199; `owner-done` 0, promotions 0). Остановка прода СНЯТА решением
владельца 13:45Z и исполнена — книга размещена, цикл ok. **`pending`-сторож с #199 считает по
ОЧЕРЕДИ** (а не по журналу отправок) и называет неотправленные вопросы; в проде он видит
хост-очередь, которая короче origin, — это дрейф прод↔origin, а не сторож. Гонять
ТОЛЬКО из worktree на `origin/main`: с хоста список НЕПОЛНЫЙ (карточки не синхронизируются, #193).
Отвечать `needs-owner → owner-done`; **только владелец** (инв. #14).

## ⏸️ Отложено до MVP 2–3 (не потерять)

- **P3-стабы Yield Lab** (`docs/23,24,26,39–43`, ветка `yield-lab-scaffolding`) — разворачивать
  при MVP 2–3, не раньше.
- **Остаток ROADMAP v2** — `docs/ROADMAP_2MONTH_EISENHOWER_v2.md`: открыты Q2-7, Q2-11/12, Q2-13, Q2-14, Q2-16, Q2-19, Q3-7; owner-gated Q1-5/6, Q2-3/4/5. **НЕ подхватывать автоматически.**

## 👁️ Наблюдение

- **Монитор сессий:** `claude-code-kanban` → http://localhost:4455 (видит headless); идёт ли цикл — `orchestrator_cycle_lock.py status`.

## 🔗 Ориентиры

- Инварианты: `CLAUDE.md` + `.claude/rules/`. Решения: `docs/decisions/INDEX.md`. Живой статус:
  `docs/SYSTEM_BRIEFING.md`. Журнал: `docs/journal/`. Карта: `docs/SYSTEM_MAP.md`. Протокол:
  `docs/ORCHESTRATOR_PROTOCOL.md`. Очередь: `nimbalyst-local/tracker/`. Идеи (НЕ действовать):
  `docs/ideas/`, `docs/rules-draft/`.
