# Journal · 2026-W29 (2026-07-13 … 2026-07-19)

> Append-only. Один файл на ISO-неделю. Оркестратор дописывает в конце цикла.

## 2026-07-15

- **ENV_SETUP_BRIEF_v3 запущен** (ветка `env-setup-v3`, мягкая заморозка — торговый цикл и трек
  не трогаем).
- **Этап 0 (заморозка):** проанализированы писатели файлов; выбрана мягкая заморозка (изоляция
  ветка + селективные коммиты + не создавать `push_v*.sh`); autopush подтверждён no-op. Одобрено владельцем.
- **Этап 1 (память проекта):** CLAUDE.md сжат 608→148 строк (инварианты сохранены); созданы
  `.claude/rules/{risk-engine,site-copy,adapters}.md`, `docs/STATE.md`, `docs/journal/`,
  `docs/decisions/` формализован (шаблон + INDEX + backfill ADR-YL-011 / ADR-048 / ADR-OWN-2026-07),
  `docs/ideas/`, `docs/rules-draft/`, `board/{owner,inbox}/`. Auto-memory ✅, `claude 2.1.210`.
- **Уточнение владельца к Этапу 6.3 (записано, реализуется в Этапе 6):** оркестратор при разборе
  inbox классифицирует каждое сообщение на 3 класса:
  1. **ЗАДАЧА** (есть конкретное действие) → карточка с acceptance-критериями (In Progress);
  2. **ИДЕЯ** (мысль / наблюдение / «а что если») → сохранить в `docs/ideas/` с датой,
     **задачу НЕ создавать** (согласуется с «идея ≠ инструкция»);
  3. **НЕПОНЯТНО** → карточка `Needs Owner` с уточняющим вопросом.
  Telegram-бот после обработки шлёт короткий ответ: «создал задачу: …» / «записал как идею» /
  «есть вопрос — смотри карточку». (Дополняет исходный 6.3: ветка «конфликт с инвариантами» → тоже `Needs Owner`.)
- **Этап 3 (протокол оркестратора):** `spa_core/owner_queue/` (детерминированный парсер/лист/смена
  статуса карточек, запрет `owner-done` для агентов, Telegram-нотификация) + CLI
  `scripts/orchestrator_queue.py` + 7 тестов (зелёные) + `docs/ORCHESTRATOR_PROTOCOL.md` (упорядоченный
  цикл: STATE→Inbox→ingest owner-done→ADR→STATE/journal→notify) + INERT агент
  `com.spa.orchestrator` (wrapper с arming-gate `SPA_ORCHESTRATOR_ARMED`, plist НЕ загружен до Этапа 8).
- **Амендмент §2.4 (owner):** карточки — по-русски, человеческие НАЗВАНИЯ, 4 секции
  (Что случилось / Что от тебя нужно / Как понять что готово / Что будет после). Закреплён в
  CLAUDE.md #15; все 5 карточек переписаны.
- **Найдена и убита петля own-06 (Etherscan):** задача возвращалась не из-за незакрытого ключа, а
  из-за ежечасного `defi-checkup-build-cycle`, который докладывал блокер БЕЗ проверки реальности.
  Проверка вживую: `checkup.earn-defi.com` → `report.approvals.status = scanned` — ключ работает.
  Фикс: LOOP GUARD в SKILL.md цикла (проверять approvals вживую перед докладом; owner-действия — один
  раз карточкой, не свободным текстом каждый час). own-06 → `ingested`.
- **Этап 4 (наблюдение):** развёрнут `com.spa.cc-kanban` (KeepAlive, PID жив, exit 0, слушает
  **http://localhost:4455**), read-only claude-code-kanban над `~/.claude`; добавлен в
  install_all_agents.sh. Обход pre-existing npm-cache EACCES через dedicated cache
  `~/.cache/cckanban-npm`. §4.2 проверено: Nimbalyst видит только свои сессии (`ai_sessions`=1 vs
  165 внешних транскриптов) → headless наблюдаем через 4455; Nimbalyst — интерактив/очередь/аппрувы.
  Кастомные UI не строил (§4.3).
- **Этап 6 (Inbox):** трекер-тип `.nimbalyst/trackers/inbox.yaml` (new→in-progress→done, source).
  Три входа заданий, все → `inbox-*` карточки: (1) карточка Nimbalyst; (2) заметка в `inbox/` (Obsidian)
  → `orchestrator_queue.py ingest-notes`; (3) Telegram-бот `/task <текст>` ИЛИ **голосовое** →
  офлайн-расшифровка `whisper large-v3-turbo` (ru/uk auto), `source: voice`. Хелперы: `create_card` /
  `ingest_notes` в `spa_core/owner_queue/` + CLI (`create`, `ingest-notes`); `spa_core/telegram/inbox_intake.py`;
  intercept в `bot.py::_handle_inbox_intake` (owner-only, fail-safe) + `/task` в setMyCommands.
  Voice e2e проверен: синтезированное RU → opus → whisper = точная расшифровка за ~19с (холодный старт).
  14 тестов зелёные. Протокол §3.1 обновлён (ingest-notes + классификация + §6.4 close-with-links).
- **Этап 7 (Obsidian-база знаний):** vault = корень репо (редактор над markdown, без плагинов).
  `.obsidian/` → `.gitignore`. Промоушен `#promote`: `scan_promotions` + CLI `promotions` +
  протокол Шаг-1b (идея/черновик → правило/ADR/задача, метка `#promoted`). Routing-правило в CLAUDE.md
  дополнено (промоушен + фикс stale `board/`→`nimbalyst-local/tracker/`). Док `docs/OBSIDIAN_VAULT.md`
  (структура + список Excluded files + мобильный захват через Telegram/Nimbalyst). 12 тестов зелёные.
- **Этап 8 (SMOKE-TEST ПРОЙДЕН):** оркестратор создал TEST needs-owner карточку + Telegram-уведомление
  (доставлено); владелец перевёл в owner-done в Nimbalyst → **статус записан обратно в файл** (files-first
  writeback подтверждён); владелец прислал **голосовое** → whisper-расшифровка → inbox-карточка `source:voice`
  (+ `/task` ранее). Прогон цикла: owner-done→`ingested` + ADR-TEST + STATE/journal; 2 inbox → классифицированы
  (ЗАДАЧА, acceptance-критерии) → `done` (§6.4). Старый `OWNER_DECISIONS_NEEDED.md` → **RETIRED**.
  **ENV_SETUP_BRIEF_v3 завершён.** Открыто владельцу: вооружить `com.spa.orchestrator` (INERT) + мерж/пуш ветки.

## 2026-07-15 (продолжение — agent cleanup исполнение)
- **own-21 п.1 исполнен:** выгружены 3 retired-агента (digest_weekly, tier1_digest, weekly_backup):
  bootout + plist'ы убраны (бэкап data/retired_plists_backup/) + install-блоки удалены из install_all_agents.sh.
  self_heal не оживит (RETIRED_LABELS, self_heal.py:110). Fleet 57→54.
- **own-21 п.2 исполнен:** novel-edge-rnd переподчинён — в ~/.claude/scheduled-tasks/novel-edge-rnd/SKILL.md
  добавлен блок «НОВЫЙ ПРОТОКОЛ» (announce/no-silent-test/owner-gated-cards/read-STATE/deploy-by-card).
- **Автономный цикл ВКЛЮЧЁН (owner-approved):** `com.spa.orchestrator` armed (governed autonomy) —
  headless claude каждые 3ч под протоколом (очередь+hardening+мелкие фичи; owner-gated→карточки; тесты
  не трогать молча; announce ownership; тесты до пуша). Сухой прогон прошёл (собрал `/status` команду).
  Wrapper: `--dangerously-skip-permissions` (headless); механизм проверен (claude launch+auth OK).
  Добавлен в install_all_agents.sh (+фикс `\\`→`\` в cc-kanban блоке). Пересмотр кадэнса ~17.07 (own-23).
- **CI-фикс (rule #16, намеренная смена теста с обоснованием):** `tests/test_telegram_bot_v2.py::
  test_handle_update_routes_correctly` ожидал `/status` → Home-панель, но `/status` стал настоящей
  командой (сводка). Ассерт обновлён на `"Статус SPA"`. Fallback-на-Home для НЕизвестных команд по-прежнему
  покрыт `test_unknown_command_shows_help`. Урок: CI гоняет и `tests/`, и `spa_core/tests/` — гонять обе.
- **Событийный интейк Inbox (owner-approved):** `com.spa.inbox_watch` (WatchPaths на inbox/) →
  `agent_inbox_intake.sh` → `spa_core/owner_queue/intake.py`. Мгновенно (не раз в 3ч): классификация
  (ask_router/Claude) + карточка/идея/вопрос + Telegram-ответ; ЖЁСТКО без кода/пушей/исполнения.
  Debounce 60с + atomic-mkdir lock (macOS без flock) + coord-log + fail-safe. «срочно»→kickstart полного цикла.
  Бот-сообщения — уже in-process (_classify_route). E2E проверено: дроп заметки → WatchPaths → карточка in-progress
  + notify. Записано в AGENT_REGISTRY + ORCHESTRATOR_PROTOCOL. Фидбек владельца (нечитаемые ID) → задача own-readable-ids.
- **Дашборд фикс №1 (owner: «пустые прочерки при открытии»):** нашёл в браузере — при сбое/начале
  поллинга `DashboardLive.jsx` делал `setFacts(null)` → все поля мигали в «—» + красный «0/24».
  Фикс: (A) не стирать facts при транзиентном сбое (держим last-good, бейдж честно «снимок»);
  (B) скрыть «0/24» chip во время начального `connecting`. Astro build зелёный (116 стр). Улучшает
  и preview, и живой /dashboard (общий island) — строгое улучшение, без даунсайда. Данные загружаются за ~5с.
- **Дашборд фикс №1b (скелетоны):** карточки денег при `connecting` показывали пустые «—» ~5с.
  Добавил `loading`-проп в `Metric` → пульсирующий скелетон вместо «—». Проверено вживую (превью):
  на открытии скелетоны, затем живые числа, красного «0/24» нет.
- **Дашборд структура по важности** (owner: «по важности материала должно быть структурированно»):
  пересобрал overview — ТИР 1 «Бумажный портфель» (деньги) поднят наверх с ведущим teal-оформлением;
  ринг «Путь к go-live» опущен ниже обычной панелью. Порядок: деньги → go-live прогресс →
  критерии+защита → day-30 → флот. Astro build зелёный (116 стр), задеплоено, проверено в браузере.
- **ADR backfill (автономный цикл оркестратора, 2026-07-15):** очередь чистая (нет new inbox /
  owner-done / promotions) → по автономному мандату взял одну безопасную docs-задачу. Выписал 3
  ADR-файла, помеченных в `INDEX.md` как «backfill TODO» (решения действуют, отдельных файлов не было):
  **ADR-050** (RiskPolicy→governance-слой; API auth HMAC/`SPA_API_KEY`; exec-bypass закрыт),
  **ADR-053** (RTMR sense→react sense-loop, детерминированный/fail-closed/LLM-forbidden, paper-advisory),
  **ADR-YL-012** (SPA Swarm 5-слойный рой, advisory, charter `docs/SWARM_ARCHITECTURE.md`). Каждый —
  запись УЖЕ принятого решения из CLAUDE.md/кода/чартера, без изменения risk-логики. INDEX-строки
  теперь ссылаются на файлы. doc-drift 11 pass. Работал в изолированном worktree.
  **ГРАБЛИ (пойманы+исправлены):** `push_to_github.py` считает `repo_path = relative_to(PROJECT_ROOT)`;
  файлы из `/tmp`-worktree (вне PROJECT_ROOT) сколлапсировали в BASENAME → легли в КОРЕНЬ репо.
  Удалил 4 неверных root-файла через API DELETE, перепушил из реальных `docs/decisions/` путей.
  Верифицировано на origin: 3 ADR → 200, INDEX-ссылки на месте, root → 404 (чисто).

## 2026-07-15 (вечер) — промоушен дашборда + owner-gate авто-шип (attended-сессия владельца)
- **Дашборд-shell промоутлен на живой /dashboard** (own-19): `dashboard.astro` принял `DashboardSPAApp`
  (app-режим, широкий DeBank-shell), как одобрено на preview; legal сохранён, числа динамические.
  Собрано в изолированном worktree (build exit 0), проверено в браузере на живом сайте. ГРАБЛИ: локальное
  дерево было на 820 коммитов позади origin (deployed preview использует НОВЫЙ `DashboardSPAApp.jsx`, не
  legacy `DashboardShell.astro`) — работал против origin через worktree.
- **Owner-gate авто-шип сайта построен + активирован** (ADR-OWN-2026-07-autoship, владелец явно выбрал
  «полный авто-шип»): `scripts/check_owner_gate.py` (классы A-E, кастодиан-эквивалентность, approval-bypass)
  + `scripts/safe_site_push.py` (gated→карточка+notify) + hard interlock в `push_to_github*.py` (активен при
  `SPA_AUTONOMOUS=1`, fail-closed) + `.github/workflows/owner-gate.yml` + `tests/test_owner_gate_check.py`
  (15 зелёных) + `docs/OWNER_GATE.md`. Red-team: подмена числа тира 20%→30% → GATED (exit 2); layout-правка
  → CLEAN; автономный gated-пуш → BLOCKED. `agent_orchestrator.sh` теперь ставит `SPA_AUTONOMOUS=1` + мандат
  пуша сайта через `safe_site_push.py`. Честный риск (кооперативный гейт) зафиксирован в ADR.
- **Этап 3 (armed-цикл) само-доказан:** оркестратор УЖЕ отработал автономный цикл (18:52→18:58, backfill
  ADR-050/053/YL-012 + STATE/journal + push) — protocol end-to-end подтверждён.

## 2026-07-15 (ночь) — Telegram-задачи: custodian FAILs (owner-approved)
- **Разобрал 2 задачи из Telegram** (событийный интейк создал карточки локально).
- **FAIL #1 SNAPSHOT_BEHIND_API — ложное срабатывание, ИСПРАВЛЕНО (owner-approved вариант «а»,
  правило #16):** сначала сам НЕ чинил (audit-механизм инв.#8) — завёл карточку владельцу
  `owner-decision-20260715-212059-apy`; владелец выбрал «а». Убрал apy-ногу из SNAPSHOT_BEHIND_API в
  `site_freshness_monitor.py`: она сравнивала СТАБИЛЬНОЕ трековое APY снапшота (4.742, от якоря) с
  ВОЛАТИЛЬНЫМ дневным API apy_today (6.654) — категориальная ошибка (генератор намеренно не берёт
  дневное). Реальная несвежесть по-прежнему ловится: evidenced_days leg + STALE_SNAPSHOT (проверено
  evaluate(): apy-differ→clean, days-differ→SNAPSHOT_BEHIND_API). Обоснование в коде.
- **FAIL #2 MISSING_ASOF — ИСПРАВЛЕНО (owner-approved):** добавил метку «as of <дата>» (`sl-asof`,
  data-ru «на дату …») в заголовок кривой капитала на главной; статически рендерится из snapshot.as_of,
  JS флипает на «live» при ответе API. Worktree-build зелёный (116 стр), в dist есть «as of 2026-07-15».
  Мой owner-gate линтер: CLEAN (as-of дата — не gated класс). Верификация в браузере после CF-деплоя.
- **Задача 2 (спека уведомлений DeFi Checkup)** — заведена как задача-карточка, ждёт исполнения.

## 2026-07-15 (ночь, автономный цикл оркестратора) — inbox-569xl: спека уведомлений DeFi Checkup
- **Контекст (шаги 0–2):** прочитал STATE + INDEX + SYSTEM_BRIEFING + свежий `session_changes.jsonl`.
  Очередь: `ingest-notes` пусто, inbox `new` пусто, owner-done пусто, promotions пусто. Две карточки с
  малформед-frontmatter (без top-level `status:`) от событийного интейка: `owner-decision-…-apy` (уже
  исполнена pid67426, оставил владельцу) и **inbox-569xl** (живая открытая ЗАДАЧА §6.3).
- **Автономный мандат → взял ОДНУ безопасную задачу:** исполнил inbox-569xl (owner-задача из Telegram) —
  написал **`docs/CHECKUP_NOTIFICATIONS_SPEC.md`** (214 стр). Дизайн-документ (НЕ код, НЕ owner-gated):
  каналы e-mail(Resend)+Telegram, классы триггеров CRITICAL/IMPORTANT/WATCH/RESOLVED/DATA-STALE + пороги
  `$M_low/mid/high` по $-материальности (max абсолют/относит.), deeplink-схема
  `/check/<wallet_ref>?report&focus=<reason_code>#anchor`, weekly digest (пустая неделя — тоже шлём:
  сигнал живости + fail-closed), шаблоны TG/e-mail (вердикт-банд как в карточках, honest-tone,
  no-solicitation, никаких score/вероятностей/execute-кнопок/PII), бэклог push/webhooks (до PVA/B2B) с
  обоснованием, §8 owner-gates (RESEND_API_KEY/own-07, дефолт-пороги/own-16, отдельный TG-бот, кадэнс).
  Всё чтит honesty-инварианты чекапа (`docs/CHECKUP_REPORT_10OF10.md`).
- **Дисциплина:** объявил владение (`log_session_change.py`, pid69383). Тесты: doc-drift **11 passed,
  1 skipped** против origin/main worktree + мой файл (GITHUB_ACTIONS=true). Локальное падение
  `test_narrative_docs_match_golive_state` — чистый артефакт локального дрейфа (мой рабочий
  `data/golive_status.json` real_track_days=24 двигает живой цикл, а committed CURRENT_STATE.md=14/30);
  против **origin committed** (real_track_days=13) тест ЗЕЛЁНЫЙ — **живой трек/CURRENT_STATE.md не трогал**
  (инвариант). Пуш: `push_to_github.py` (docs/, не landing/) — sha b8a3b5a5, проверено на origin по пути
  (без basename-collapse).
- **§6.4:** карточку inbox-569xl закрыл в `done` (дополнил frontmatter недостающей `status:` строкой) со
  ссылкой на порождённую спеку; запушено (sha 84fa80c4). Очередь чиста.
- **Ничего в карточки не ушло** (спорного/owner-gated в этом цикле не возникло). Открытые owner-gates по
  сборке уведомлений уже описаны в §8 спеки — отдельная карточка появится при `#approved`.

## 2026-07-16 (ночь, автономный цикл оркестратора) — hardening: owner-gate Class-B fail-open закрыт
- **Контекст (шаги 0–2):** прочитал STATE + INDEX + SYSTEM_BRIEFING + свежий `session_changes.jsonl`.
  Очередь: `ingest-notes` пусто, inbox `new` пусто, owner-done пусто, promotions пусто. Owner-decision
  карточки (own-07/08/11/13/14/15/16…) — все `needs-owner`, ждут владельца (не трогаю).
- **Автономный мандат → взял ОДНУ безопасную задачу (hardening):** починил **fail-open в owner-gate**
  (`scripts/check_owner_gate.py`, Class B — запечённые числа доходности). Line-level dynamic-suppressor
  (`_RE_YIELD_NUMBER.search(text) and not _RE_DYNAMIC.search(text)`) подавлял ЛЮБОЕ запечённое число,
  если где-то на ТОЙ ЖЕ строке был динамический токен (`{snap.x}`, `props.`). Пример:
  `Fixed 30% net APY — live {snap.paper_apy_pct}%` → `snap.` глушил Class-B → запечённое «30% net APY»
  уезжало в live само (нарушение fail-CLOSED). Подтверждено эмпирически до правки.
- **Fix (строго fail-CLOSED):** подавление теперь per MATCH SPAN, не per line — динамический токен
  глушит число, только если он в пределах `_DYNAMIC_WINDOW=6` символов от процента. Изменение может лишь
  ДОБАВИТЬ owner-карточку, никогда не пропустить — безопасное направление для security-гейта. Соседний
  `{apy}%`/`.toFixed()%` по-прежнему exempt (span окно); контроли (`test_dynamic_yield_read_is_clean`,
  `plain baked 25%`) не регрессируют.
- **Тесты:** +2 регрессионных (`test_baked_literal_with_dynamic_token_same_line_gates_B` — fail-open
  closer; `test_dynamic_token_adjacent_to_percent_still_exempt`). Полный `tests/test_owner_gate_check.py`
  **17 passed** (было 15) под `GITHUB_ACTIONS=true`, в изолированном worktree из origin/main и на main-repo.
- **Дисциплина:** объявил владение (`log_session_change.py`); worktree из **origin/main** (локальный HEAD
  508c8912b — другая линия, файлов owner-gate не содержал); пуш **КОДА** через `push_to_github.py` (не
  landing/ → обычный путь, не safe_site_push) по абсолютным путям; проверено на origin — верные пути,
  root чистый (без basename-collapse), fix+тест присутствуют, 17 зелёных против origin-content.
- **Не трогал:** RiskPolicy/kill/risk-логику, живой трек (`data/equity_curve_daily.json`), деплой/агентов,
  сайт (`landing/`); тесты не ослаблял (только добавил). Ничего в карточки не ушло (спорного не возникло).

## 2026-07-16 (автономный цикл оркестратора) — hardening: owner-queue dead-letter (карточки без статуса)
- **Контекст (шаги 0–2):** прочитал STATE + INDEX + SYSTEM_BRIEFING + свежий `session_changes.jsonl`.
  Очередь пуста: `ingest-notes` пусто, inbox `new` `[]`, owner-done `[]`, promotions `[]`. Два inbox-card
  `in-progress` (dashboard-debank pid41813 / readable-card-ids) — принадлежат другим сессиям, не трогал.
- **Находка при осмотре очереди:** owner-decision карточка `owner-decision-20260715-212059-apy` имела
  **пустой статус** (нет поля `status:` во frontmatter) → невидима во ВСЕХ фильтрах, включая owner-очередь
  `needs-owner`. При этом работа по ней исполнена (owner ответил в чате, apy-нога кастодиана убрана + as-of
  метка, pid67426, commit e01e042f). Карточка-«письмо мёртвых».
- **Корневая причина:** `spa_core/owner_queue/queue.py::create_card` писал `status:` только `if status`.
  При `status=None` (CLI `orchestrator_queue.py create` без `--status`, либо бот) карточка рождалась БЕЗ
  поля статуса. Хуже: `set_status` такую карточку не чинил — `raise ValueError("no top-level 'status:'…")`.
  Постоянный dead-letter: невидим И неисправим инструментом.
- **Fix (fail-safe, hardening):**
  1. `create_card` теперь ВСЕГДА пишет строку статуса: `effective_status = status or _DEFAULT_STATUS.get(
     tracker_type, "new")` (owner-decision→`needs-owner`, inbox→`new`, неизвестный тип→`new`). owner-done
     по-прежнему запрещён (проверка в начале create_card, инвариант #14).
  2. `set_status` при отсутствии строки `status:` теперь ВСТАВЛЯЕТ её последней строкой frontmatter (перед
     закрывающим `---`) вместо raise — сломанные карточки становятся исправимыми. owner-done по-прежнему
     отклоняется (`OwnerDoneForbidden`).
- **Данные:** починил реальную карточку `212059-apy` через обновлённый `set_status` (дог-фудинг) → статус
  `needs-owner`, теперь видна владельцу для финального owner-done (я owner-done НЕ ставил, инвариант #14).
- **Тесты:** +3 регрессионных в `spa_core/tests/test_owner_queue.py` (`…_without_status_defaults_by_type`,
  `…_always_emits_top_level_status_line`, `…_repairs_status_less_card`). `test_owner_queue` + `test_inbox_intake`
  **18 passed** (было 15); смежный набор (owner_queue/telegram/intake/promotions) **705 passed / 7 skipped**.
- **Дисциплина:** объявил владение (`log_session_change.py`); изолированный worktree из **origin/main**
  (локальный main дрейфует 836 позади — тесты гоняю против чистого origin); пуш **КОДА** через
  `push_to_github.py` (не `landing/` → обычный путь) по абсолютным путям; проверено на origin —
  `_DEFAULT_STATUS` + always-status + repair присутствуют, карточка `status: needs-owner` на origin.
- **Не трогал:** RiskPolicy/kill/risk-логику, живой трек (`data/equity_curve_daily.json`), деплой/агентов,
  сайт (`landing/`); тесты не ослаблял (только добавил). Спорного не возникло — карточек `needs-owner` не заводил.

---


## 2026-07-16 · Автономный цикл (env-setup-v3, #3) — читаемые имена карточек (inbox-task-readable-card-ids)

**Контекст:** очередь пуста (inbox `new` / owner-done / promotions — 0), НО в `inbox` висела задача
`inbox-task-readable-card-ids` в статусе `in-progress` с 2026-07-15 (создана событийным интейком, который
код НЕ исполняет — ждал полный цикл). Взял её как безопасную не-owner-gated задачу.

**Задача (фидбек владельца):** ID карточек вида `inbox-20260715-160918-note` нечитаемы; хочет читаемые имена
(`inbox-dobavit-knopku-naverh.md`), таймстамп — только collision-суффикс.

**Корневая причина:** `_slug` в `queue.py` резал не-ASCII → **русские** заголовки схлопывались в `note`,
и таймстамп оставался единственным различителем → отсюда уродливые `…-<stamp>-note`.

**Fix (`spa_core/owner_queue/queue.py`):**
1. Добавлен Cyrillic→Latin транслит (`_CYR_TRANSLIT` + `_translit`), вызывается в `_slug` до ASCII-фильтра.
   «Добавить кнопку наверх» → `dobavit-knopku-naverh`. **Это внутренние имена файлов, НЕ site-copy** —
   транслит в оффере запрещён (`.claude/rules/site-copy.md`, память `ru-copy-quality-no-translit`); здесь
   он именно то, что владелец просит в теле задачи. Различие явно закомментировано в коде.
2. Имя файла карточки = `<type>-<slug>.md`, **таймстамп из имени убран** (жил лишь для уникальности, делал
   ID непрозрачными). Дата осталась в frontmatter-поле `created:`. Коллизия → читаемый суффикс `-2`/`-3`.
3. Бонус: идея-файлы `docs/ideas/` (`intake.py` использует тот же `_slug`) тоже стали читаемыми.

**Про `/status`:** он и так показывает счётчики по статусам (не ID); `notify.py` предпочитает title —
уродливые таймстамп-ID владельцу больше нигде не всплывают. Отдельной правки бота не потребовалось.

**Существующие карточки НЕ переименовывал** — только форвард-изменение (переименование ломало бы ссылки
на них в STATE/journal/git). Новые пойдут читаемыми.

**Тесты (§16 — только добавил/обновил под acceptance, ничего не ослаблял):** обновил `test_create_card_roundtrips`
(ассертил старый таймстамп-формат → теперь `inbox-prover-dashbord-na-telefone.md`, обосновано: это acceptance
задачи) + 3 новых (`…_readable_slug_no_timestamp`, `…_collision_gets_readable_suffix`, `…_fallback_when_nothing_survives`).
`test_owner_queue` **18 passed**; смежный детерминированный набор (owner_queue/inbox_intake/ask_router/status_summary)
**29 passed**.

**Дисциплина:** объявил владение (`log_session_change.py`); ветка `env-setup-v3` (изолированная от main),
файлы под эксклюзивным владением; пуш **КОДА** через `push_to_github.py` по абсолютным путям — проверено на
origin/main (`_CYR_TRANSLIT`/`_translit` + обновлённые ассерты присутствуют). Карточка закрыта `done` со
ссылкой на результат (§6.4).

**Не трогал:** RiskPolicy/kill/risk-логику, живой трек (`data/equity_curve_daily.json`), деплой/агентов,
сайт (`landing/`). Спорного не возникло — карточек `needs-owner` не заводил.

## 2026-07-16 (novel-edge R&D, автономный цикл) — Идея #15: Kelly KODS (Kelly Optimal Dynamic Sizing)

- **Директива:** standing owner directive «continuously invent + BACKTEST new DeFi yield/risk edge ideas, honestly».
  Прочитал реестр `docs/DYNAMIC_LEVERAGE_GUARDIAN.md` (#1–#14). Kelly Criterion (f* = (μ − rf)/σ²) не тестировался
  ни в одной предыдущей идее — структурно иной класс (непрерывный, включает ожидаемую доходность μ).
- **Реализация:** `scripts/edge_kelly_dynamic_sizing.py` — детерминированный stdlib-only скрипт, IS_ADVISORY=True.
  Каузальная оценка μ и σ² только на исторических данных (нет look-ahead). Свип 24 комбинаций:
  alpha ∈ {0.1,0.25,0.5,1.0} × lookback ∈ {10,20,30} × max_risky ∈ {0.25,0.40}.
  Данные — `fixtures.py` (synthetic, L0-evidence, label 'backtest', 2024-07-01–2026-05-31, 699 trading days).
- **Структурная находка (α-degeneracy):** В фикстуре σ²≈0 вне кризисных окон → f_kelly → ∞ → всегда
  обрезается до max_risky. В кризис после первого дня μ_rolling < 0 → f_kelly < 0 → 0%. Множитель alpha
  не влияет: применяется к уже обрезанному или нулевому значению. Kelly редуцирует к бинарному on/off сигналу.
  Все четыре alpha дают идентичные числа (ожидаемо, задокументировано честно).
- **Результаты (лучший: alpha=0.1, lookback=10, max_risky=0.40, bt-только, L0):**
  APY = 5.05% · maxDD = 1.11% · Calmar = 4.55 vs DDO #9 = 3.68 vs static #3 = 2.03 (BEAT baseline).
  OOS (last 100 days): Calmar 52.35 vs DDO 32.68.
- **Механизм:** 0%-де-риск в кризисе (Kelly → f=0) сохраняет капитал лучше, чем DDO DEFEND=5% sUSDe.
  lookback=10 даёт более быстрое восстановление vs 21-дневное DDO harvest-окно.
- **Следующий шаг (Hybrid #12):** Kelly 0%-де-риск (DEFEND) + DDO 40% harvest (HARVEST).
  Конвергенция идей #11 (CPPI+DDO) и #15 (Kelly). Требует нового скрипта + ADR перед live.
- **Тесты pytest:** 88 776 passed / 39 failed / 608 skipped. 39 failing — ВСЕ pre-existing,
  не связаны с нашим кодом (test_academy_auth: bcrypt; test_rates_desk_*: модель; test_tier1_*: KeyError).
  Aggressive_lab тесты — зелёные.
- **Коммиты:** `bc8bb8b` (novel-edge #15) + `6766195` (data sync) → pushed to main.
- **Реестр:** запись #15 добавлена в `docs/DYNAMIC_LEVERAGE_GUARDIAN.md`.
- **Не трогал:** RiskPolicy, kill-switch, живой трек, деплой агентов, сайт (`landing/`). IS_ADVISORY=True.

## 2026-07-16 — чистка WS-A фаза 1 (owner greenlight)
- **49 одноразовых скриптов → `scripts/archive/`** (push_*.sh, install one-shots, .command, migrate/backfill,
  .plist.disabled). Все проверены на 0 ссылок (31 из 80 кандидатов оказались упомянуты другими стелыми
  скриптами — оставил, консервативно). Копии запушены, оригиналы удалены новым `scripts/github_delete.py`
  (Contents-API delete для обратимых attic-move). Живой surface (push_to_github, install_all_agents, CI-гейты,
  agent-wrappers) цел. Недеструктивно — всё в archive/, в attic/MANIFEST.md.

## 2026-07-16 — hardening: регресс-тесты history_check (§6.6 дедуп-путь) [автономный цикл #4]
- **Контекст:** очередь пуста (inbox new / owner-done / promotions = 0), needs-owner карточки ждут
  владельца. По автономному мандату взял ОДНУ безопасную hardening-задачу.
- **Находка:** новый модуль `spa_core/owner_queue/history_check.py` (160 строк, вкорячен pid11335,
  §6.6 — ОБЯЗАТЕЛЬНАЯ проверка истории перед созданием карточки) на origin имел **0 тестов**. Это
  критический дедуп-путь: если парсинг вердикта молча сломается, шаг деградирует до «всегда NEW» →
  снова плодятся дубли карточек (ровно то, что модуль призван предотвращать) — без краша, тихо.
- **Сделано:** `spa_core/tests/test_history_check.py` (+21 тест, subprocess замокан — без реального
  `claude`, офлайн/детерминированно): `is_duplicate` (только DONE/IN_PROGRESS/REJECTED подавляют
  карточку; PARTIAL/NEW/unknown/lowercase — нет), парсинг (first-token upper-case, dash-clause на
  строке вердикта, многострочное тело-ответ, raw сохраняется), **fail-safe→NEW** на: пустой ввод
  (без вызова subprocess), non-zero exit, исключение (Timeout/OSError), пустой stdout, мусор-вердикт;
  + smoke `_build_history_context` (строка, ≤max_chars, переживает пустой репо) + проверка, что текст
  задания реально доходит до промпта.
- **Тесты:** 21/21 зелёных; смежный owner_queue-набор 51/51 (test_owner_queue + test_inbox_intake +
  test_owner_blockers + test_history_check).
- **Пуш:** код `spa_core/tests/test_history_check.py` через `push_to_github.py` → verified на origin
  (верный путь `spa_core/tests/`, 184 строки, без basename-collapse, root чист). STATE+journal — docs.
- **Не трогал:** RiskPolicy/kill-switch/risk-логику, живой трек (`data/equity_curve_daily.json`),
  деплой/агентов, сайт (`landing/`). Тесты только ДОБАВИЛ (инвариант #16 не нарушен). owner-done не ставил.

## 2026-07-16 · Автономный цикл #5 (env-setup-v3) — DRY-фикс дубля `_slug` в intake

- **Контекст:** очередь пуста (inbox new/in-progress=0, owner-done=0, promotions=0; все `needs-owner`
  ждут владельца — не трогал, инвариант #14). По автономному мандату взял ОДНУ безопасную hardening-задачу.
- **Найдено (реальный баг):** `spa_core/owner_queue/intake.py` держал СОБСТВЕННУЮ копию `_slug` БЕЗ
  Cyrillic→Latin транслита. Цикл #3 (readable-card-ids) починил транслит только в `queue._slug`, а дубль
  в intake остался — поэтому русский заголовок ИДЕИ схлопывался в `docs/ideas/<дата>-note.md` (opaque),
  ровно тот дефект, что владелец просил устранить для карточек. Классика «reporting module name
  collisions» — параллельная забытая копия.
- **Сделано:** удалил локальный `_slug` (и осиротевший `import re`), intake теперь переиспользует
  каноническую `queue._slug` через существующий lazy-import блок (DRY — две реализации больше не
  разъедутся молча). Поведение имён файлов идей = ровно `queue._slug`.
- **Тесты:** новый `spa_core/tests/test_owner_intake.py` (+2 регресс-теста на `run_note_intake`,
  idea-ветка): (1) русский заголовок → читаемое транслит-имя, НЕ `-note.md`; (2) имя файла идеи ==
  `queue._slug(title)` (guard против повторного расхождения). Claude/telegram-леги замоканы на
  source-модулях (lazy-import → патч берётся при вызове), без реального `claude`/бота. 2/2 + смежный
  набор 53/53 (owner_queue + inbox_intake + history_check + owner_blockers) зелёные.
- **Пуш:** код `intake.py` + `test_owner_intake.py` через `push_to_github.py` → verified на origin
  (верные пути, root чист, без basename-collapse). STATE+journal — docs.
- **Не трогал:** RiskPolicy/kill-switch/risk-логику, живой трек (`data/equity_curve_daily.json`),
  деплой/агентов, сайт (`landing/`). Тесты только ДОБАВИЛ (инвариант #16 не нарушен). owner-done не ставил.

## 2026-07-16 · Автономный цикл #6 (env-setup-v3) — регресс-тесты `build_agent_registry` (SSOT флота)

- **Контекст:** очередь пуста (inbox new/in-progress=0, owner-done=0, promotions=0; все `needs-owner`
  ждут владельца — не трогал, инвариант #14). По автономному мандату взял ОДНУ безопасную hardening-задачу.
  **Сознательно диверсифицировал** от `owner_queue` (циклы #2–#5 подряд правили его) — выбрал соседний
  критичный, но НЕпокрытый модуль.
- **Найдено (пробел покрытия):** `scripts/build_agent_registry.py` (детерминированный SSOT всего
  launchd-флота 58 агентов; кормит И память, И визуальный дашборд `/admin/agents` через
  `spa_core/api/routers/agents.py`) имел **0 выделенных тестов** на origin — только `test_api_agents.py`
  косвенно дёргал его через router. Нетривиальная логика без сети под тестом: три класса флагов проблем
  (retired-но-загружен · загружен-но-НЕ-reboot-safe · в-`~/Library`-но-не-загружен=drift), допуск
  `last_exit` (0 и SIGTERM `-15` чистые, остальное флагается), форматирование расписания
  (StartInterval ч/м/с · StartCalendarInterval single/list+Weekday · KeepAlive · WatchPaths).
- **Сделано:** `spa_core/tests/test_build_agent_registry.py` (+17 тестов). Модуль грузится по файловому
  пути через `importlib.util.spec_from_file_location` — ровно как router в рантайме (`scripts/` не пакет).
  Тесты **герметичны/офлайн**: `_launchctl`/`_retired` замоканы, `_LAUNCH_DIR` перенаправлен в tmp с
  фейковыми plist — не зависят от того, крутится ли SPA на хосте. Покрыто: `_schedule` (8 форм),
  clean-agent без проблем, каждый из 3 классов флагов, retired+не-загружен=чисто+вне role-rollup,
  last_exit −15/0 чистые vs ненулевой флагается, unknown short→role `other`, форма/rollup реестра.
  **Модуль НЕ менял — только добавил тесты** (инвариант #16 соблюдён).
- **Тесты:** 17/17 новых + смежный `test_api_agents.py` 21/21 (в изолированном worktree из origin/main,
  затем в основном репо-пути). Зелёные до пуша.
- **Пуш:** `test_build_agent_registry.py` через `push_to_github.py` (валидированную копию положил под
  PROJECT_ROOT перед пушем → `relative_to` даёт верный путь) → verified на origin (203 строки по
  `spa_core/tests/`, root чист, без basename-collapse). STATE+journal — docs.
- **Не трогал:** RiskPolicy/kill-switch/risk-логику, живой трек (`data/equity_curve_daily.json`),
  деплой/агентов, сайт (`landing/`). owner-done не ставил.

### 2026-07-16 · автономный цикл #7 (inbox-cadence: задача уже была сделана — §6.6 DONE)

- **Контекст:** разбор очереди. `ingest-notes` пусто; owner-done=0; promotions=0. Одна `new` inbox-карточка
  из Telegram: **«автономный оркестратор должен был быть раз в 1 час, а не раз в 3 часа»**
  (`inbox-avtonomnyi-arkestrator-dolzhen-byl-byt-r`).
- **Классификация:** ЗАДАЧА (конкретное действие — сменить кадэнс автономного цикла 3ч → 1ч).
- **Шаг 1a — проверка истории (§6.6) → DONE.** `history_check` + прямая верификация:
  - origin-коммит `6f5a26504` — `chore(orchestrator): cadence 3h→1h (owner 2026-07-16: больше работы)`;
  - `launchd/com.spa.orchestrator.plist` на origin: `StartInterval = 3600` (1 час), комментарий «owner 2026-07-16»;
  - загруженный агент подтверждён живым: `launchctl print gui/$(id -u)/com.spa.orchestrator` → `run interval = 3600 seconds`.
  Т.е. запрос владельца **уже исполнен** предыдущей сессией; кода/деплоя — ничего нового не требуется
  (агент уже перезагружен и живёт с интервалом 1ч). Дубль-задачу НЕ создавал.
- **Bookkeeping (то, что оставалось «в воздухе»):**
  - inbox-карточка: дописан разбор + результат, закрыта `done` через `orchestrator_queue.py set-status` (§6.4);
  - `docs/STATE.md`: фокус-строка и «Обновлено» поправлены (кадэнс 3ч → **каждый час**), добавлена строка решения #7;
  - `own-23-autonomy-cadence-recheck` (needs-owner, recheck ~17.07): аннотирована — владелец фактически ответил
    «ежечасно» этой задачей, менять нечего; статус оставлен `needs-owner`, финальный owner-done **за владельцем**
    (инвариант #14 — агент не ставит owner-done);
  - Telegram-ответ владельцу по-человечески (HTML): «уже сделано — раз в час, live, коммит 6f5a26504».
- **Не трогал:** RiskPolicy/kill-switch/risk-логику, живой трек (`data/equity_curve_daily.json`),
  деплой/выгрузку агентов (сам плейст НЕ менял — он уже был 3600), сайт (`landing/`), тесты. owner-done не ставил.
- **Пуш:** только docs/tracker-bookkeeping (STATE + journal + карточки) — кода нет. Announce записан в `session_changes.jsonl`.

## Автономный цикл #8 (2026-07-16) — hardening: PARTIAL-сигнал больше не теряется в intake

- **Очередь пуста** (inbox new / owner-done / promotions = 0; застрявших in-progress нет; needs-owner
  карточки ждут владельца — не трогаю). Взял ОДНУ безопасную задачу (§Автономный мандат): hardening,
  диверсифицировал от чистого owner_queue — реальный **баг корректности** в живом event-driven intake.
- **Баг:** `spa_core/owner_queue/intake.py` — `partial_note` ставится (вердикт `PARTIAL` из
  `history_check`, §6.6/§1a), но **НИГДЕ не читается** (dead variable). Протокол §1a требует: при PARTIAL
  карточку СОЗДАТЬ, но пометку «похоже на …, проверь» вписать **и в тело карточки, и в Telegram-ответ**.
  Сейчас сигнал PARTIAL молча терялся во ВСЕХ трёх ветках (task / idea / unclear) — владелец не видел,
  что задание похоже на уже существующее.
- **Фикс:** ввёл `partial_body` (блок в persisted-тело) и `partial_tg` (суффикс в notify), вплетены
  во все 3 ветки: idea → в файл `docs/ideas/` + TG; unclear → в тело owner-карточки + TG; task → в тело
  inbox-карточки (её увидит полный цикл) + TG. NEW/DONE/IN_PROGRESS/REJECTED — без изменений.
- **Тесты:** +4 регресс-теста в `spa_core/tests/test_owner_intake.py` (task/idea/unclear несут пометку в
  body+TG; NEW не добавляет ложной пометки). `_wire` теперь captures `_notify` и изолирует `Q.TRACKER_DIR`
  в tmp (unclear-ветка иначе писала бы реальную owner-карточку). 6/6 файла + 45/45 смежных (owner_queue +
  history_check + intake) зелёные.
- **Пуш КОДА** через `push_to_github.py`. ГОТЧА СРАБОТАЛА И ПОЧИНЕНА: /tmp-worktree → basename-collapse,
  файлы легли в КОРЕНЬ (root/intake.py, root/test_owner_intake.py), верные пути остались старыми. Скопировал
  фикс в реальные пути под PROJECT_ROOT, перепушил, удалил 2 корневых дубля через API. Верифицировано на
  origin: `intake.py` содержит `partial_tg`, тест содержит `_PARTIAL_RESP`+4 теста, root 404 чист.
- **Не трогал:** RiskPolicy/kill-switch/risk-логику, живой трек, деплой/агентов, сайт (`landing/`).
  Тесты только добавил (инвариант #16). owner-done не ставил.

---

## 2026-07-16 — Автономный цикл #9 (hardening: adapters, fail-CLOSED)

**Контекст.** Очередь пуста (`ingest-notes` — нет заметок; inbox new/in-progress = 0; owner-done = 0;
promotions = 0). 20 карточек `needs-owner` ждут владельца — не мои, не трогал. Автономный мандат →
одна безопасная hardening-задача. **Сознательно диверсифицировал** от `owner_queue` (циклы #2–#8 подряд
его правили) → домен адаптеров (read-only, разрешённый).

**Найденный баг (разведка + верификация чтением кода).** `spa_core/adapters/ethena_susde_adapter.py::_norm_apy`
нормализует APY эвристикой `>1.0 → percent, иначе decimal` (строка 132), а out-of-band результат затем
**молча клампится** `_clamp` до правдоподобных 50% (строка 238). Следствие: обвал доходности до <1% у
percent-источника (DeFiLlama `apy` однозначно percent) мис-масштабируется в ~90% → кламп 50% → показывается
как «живой здоровый APY», и advisory-anomaly (`apy < 3%`, строка 242) **не срабатывает** — коллапс скрыт
именно когда важен. Молчаливый кламп фабрикует число → прямое нарушение инварианта #2 (fail-CLOSED,
refusal-first) и философии `apy_contract.py` (out-of-band → fail closed, не угадывать).

**Правка (минимальная, безопасная).** НЕ трогал неоднозначную percent/decimal-эвристику (её нельзя
разрешить оффлайн без знания единиц источника Ethena). Добавил в `_norm_apy` fail-closed проверку
диапазона: нормализованный APY вне `[MIN_APY, MAX_APY]` (0…0.50) → `None` (источник пропускается, система
fail-close'ится на next-source / cached-stale) вместо фабрикации 50%. Также `bool`/`inf` теперь тоже fail
closed (было: `bool` проходил как 1.0/0.0; `inf` не ловился). `@staticmethod`→`@classmethod` для доступа
к `cls.MIN_APY/MAX_APY`. **Для нормальной полосы 0–50% — ноль изменений** (12.0→0.12, 0.12→0.12).

**Held-позиция не затронута.** Портфельная `susde` обслуживается ДРУГИМ адаптером `SusdeAdapter` (T3,
MP-460), а не `EthenaSusdeAdapter` (T2, `ethena_susde`). Money-path риск ещё ниже.

**Тесты.** Класс имел **0 тестов**. `spa_core/tests/test_ethena_susde_adapter.py` (+17): unit-нормализация
(percent/decimal/границы 0.0 и 50%), **ключевой регресс** `percent_collapse_below_one_pct_fails_closed`
(0.9→None, было→фейк 50%), out-of-band/negative/NaN/inf/bool/non-numeric → None, property «результат всегда
в полосе»; e2e `fetch()` (healthy→live; out-of-band primary→cached-stale, НЕ клампленый live 50%; defillama
fallback; in-band 2%→anomaly True; apy никогда не NaN/out-of-band). 17/17 + 242 смежных (registry/manifests/
kill_switch/vportfolio) + 116 classifier зелёные.

**Пуш КОДА** через `push_to_github.py` (не сайт — owner-gate не задействован). Basename-collapse готча
обойдена заранее: правил в `/tmp`-worktree, скопировал в пути под PROJECT_ROOT, пушил их. Верифицировано на
origin: адаптер содержит `out-of-band → fail-closed` + `@classmethod`, тест 17 функций, **root чист** (нет
basename-дублей).

**Не трогал:** RiskPolicy/kill-switch/risk-логику, живой трек (`data/equity_curve_daily.json`),
деплой/агентов, сайт (`landing/`). Тесты только добавил (инвариант #16). owner-done не ставил.

## 2026-07-16 — Автономный цикл #10 (hardening: safe_site_push owner-notify)

**Очередь пуста** (inbox new / owner-done / promotions = 0). По автономному мандату взял ОДНУ безопасную
hardening-задачу. Сознательно диверсифицировал от `owner_queue`/адаптеров (циклы #2–#9) → `scripts/`.

**Цель.** `scripts/safe_site_push.py` — единственный санкционированный путь авто-шипа сайта в live
(ADR-OWN-2026-07-autoship). На origin — **0 выделенных тестов**. Это script (не risk/governance/gates, не
site-copy, не деплой) — в scope автономного мандата.

**Баг (реальный латентный, воспроизведён эмпирически).** На GATED (owner-gated) site-правке функция
`_route_to_owner_card` ОБЯЗАНА (а) создать `needs-owner` карточку и (б) уведомить владельца в Telegram
(инвариант #8, human-in-the-loop). Но вызов был `create_card(card_type="owner-decision", …)`, а сигнатура —
`create_card(tracker_type=…)` (нет параметра `card_type`) → `TypeError` при КАЖДОМ gated-push, пойманный
внешним `except` → печаталось «FAILED to create owner card», карточка **не создавалась**, владелец **не
уведомлялся**. Push оставался fail-closed (не уезжал), но владелец был слеп к застрявшей owner-gated
правке — весь смысл маршрутизации к владельцу молча терялся. Заодно: строка `path = getattr(card,"path",…)
or getattr(card,"name","")` брала `Path.name` (голый basename) → `orchestrator_queue.py notify <basename>`
→ `load_card(basename)` не резолвит против `TRACKER_DIR` → `FileNotFoundError` (второй, замаскированный баг).

**Фикс (не трогает решение гейта).** `create_card` возвращает полный `Path` (queue.py:331). Заменил
`card_type`→`tracker_type` и использую возвращённый `Path` напрямую, передавая `str(card_path)` в notify.
Ветки CLEAN / GATED / ERROR (rc 0/2/1) не тронуты — fail-closed на push сохранён.

**Тесты (+4, класс имел 0).** `spa_core/tests/test_safe_site_push.py`: (1) gated → создаётся ровно 1
`needs-owner` карточка (реальный `create_card` в tmp-`TRACKER_DIR`) + notify вызван ровно раз с ПОЛНЫМ
абсолютным путём (не basename) — **проверил: до фикса этот тест падает именно с `TypeError create_card()
got an unexpected keyword argument 'card_type'`**, после — зелёный; (2) guard-error rc=1 → fail-closed,
ноль subprocess/карточек; (3) clean rc=0 → делегирует batch-push с `SPA_SITE_PUSH_VERIFIED=1`; (4) нет
`landing/`-файлов → guard пропущен, push всё равно с маркером. Guard/push/Telegram застаблены (оффлайн).
4/4 + 49 смежных (owner_queue/intake/history_check) зелёные.

**Пуш КОДА** через `push_to_github.py`. **Basename-collapse готча сработала** (память
`deploy-untracked-dir-collapse`/`push-to-github-absolute-paths`): первый пуш абсолютными путями из
`/tmp/spa_wt_safesitepush/…` схлопнул оба файла в root (`safe_site_push.py`, `test_safe_site_push.py` = 200,
`scripts/safe_site_push.py` НЕ обновлён, `spa_core/tests/…` = 404). Исправил: скопировал фикс в главный
репо на верные пути, перепушил абсолютными путями ПОД PROJECT_ROOT, удалил 2 root-дубля через GitHub API
(DELETE 200/200). Финал на origin: `scripts/safe_site_push.py` 200 (содержит `tracker_type`, нет
`card_type`), `spa_core/tests/test_safe_site_push.py` 200, root-дубли 404/404.

**Не трогал:** RiskPolicy/kill-switch/risk-логику, живой трек (`data/equity_curve_daily.json`),
деплой/агентов, site-copy (`landing/`). Тесты только добавил (инвариант #16). owner-done не ставил.
STATE обновлён (снимок трек-дней 24→25 под SYSTEM_BRIEFING).

## 2026-07-16 — чистка WS-A фаза 2 (мёртвые модули, owner-approved «по протоколу»)
- **5 мёртвых модулей → attic** (adapters gmx_glp+radiant [REMOVED-marked]; monitoring posture_gate+adapter_watchdog;
  alerts protocol_report) + их ПОСВЯЩЁННЫЕ тесты (инвариант #16). Оригиналы удалены через github_delete.py.
- **УРОК (важный):** аудит-вердикт «мёртвое» нельзя применять слепо. Fuzzy-греп по имени модуля даёт ложные
  срабатывания (общие слова: config/signal/posture; одноимённые файлы в разных папках: 2×adapter_watchdog).
  Надёжный протокол: (1) PRECISE import-check (реальные `from spa_core.<sub>.<mod> import`, не слово в тексте);
  (2) тест посвящён ли модулю (не трогать тест, покрывающий ЖИВОЙ код — напр. whitelabel_api-тест тянет
  scoring_engine → HOLD); (3) import-smoke + `pytest --collect-only` (ловит осиротевшие импорты за 8с — это
  РЕШАЮЩИЙ быстрый гейт для переноса мёртвого; полный сьют по-файлово >2мин, непрактично на каждый батч).
- **Регрессия фазы-1 поймана+исправлена:** архивация `migrate_json_to_sqlite.py` осиротила `test_json_to_sqlite.py`
  (скрипт-чек фазы-1 исключал tests/ — пробел). Тест перенесён в archive вслед за миграцией. collect: 102593 clean.
- HOLD (реально используются, аудит переоценил): data_trust_monitor (tournament_engine), posture (cycle_gates/rtmr),
  whitelabel_api (тест тянет живое). Pass-3 дал ещё кандидатов (portfolio/ мёртвый остров и др.) — следующими батчами.

## 2026-07-16 — Автономный цикл #11 (hardening: owner-notify path регресс-тесты)

**Очередь пуста** (inbox new / owner-done / promotions = 0). По автономному мандату взял ОДНУ безопасную
hardening-задачу. Сознательно диверсифицировал от `owner_queue`-slug/intake, адаптеров, `safe_site_push`
(циклы #2–#10) → соседний НЕпокрытый файл того же human-in-the-loop контура: `spa_core/owner_queue/notify.py`.

**Цель.** `notify.py` — Telegram-хоп, который держит владельца в петле (инвариант #8): при создании
owner-gated site-правки или любой `needs-owner` карточки он уведомляет владельца. На origin покрыт лишь
ОДНИМ happy-path тестом на `build_message` (`test_owner_queue.py`). Непокрыты именно те ветки, что и
защищают инвариант. Это НЕ risk/kill/gates/site-copy/деплой — тесты в scope мандата.

**Что покрыл (+9 тестов, `spa_core/tests/test_owner_notify.py`, файл новый):**
- `build_message` — repo-relative путь для in-repo карточки (владелец находит файл) + basename-fallback
  для out-of-repo карточки (напр. `/tmp`-worktree путь: `resolve().relative_to(repo_root)` бросает
  `ValueError` → голое имя файла, БЕЗ краха) + HTML-escape заголовка (`<`/`&` не утекают в HTML parse-mode)
  + предпочтение amended-заголовка §2.4 «## Что от тебя нужно» со срезкой list-маркера.
- `notify_needs_owner` — `dry_run=True` строит сообщение НЕ трогая бота (бот-фабрика бросает, если вызвана);
  и главное — **инвариант «уведомление НИКОГДА не роняет оркестратор»** (docstring-контракт): бот бросает
  на `send_message` / бросает в `__init__` (нет creds) / возвращает falsy — все три ветки МОЛЧА проглочены,
  `notify_needs_owner` возвращает msg без исключения; happy-path шлёт через бота с `parse_mode="HTML"`.
- Герметичность: реальный Keychain-бот НЕ импортируется — фейковый `spa_core.telegram.bot` инжектится в
  `sys.modules` (ленивый импорт внутри функции честит подмену). `build_message`-ветки — прямым построением
  `Card` (не читает диск); send-ветки — через файл в `tmp_path`.

**Тесты.** 9/9 новых + 64 смежных (owner_queue/intake/history_check/inbox/safe_site_push/telegram-single-authority)
зелёные в worktree на `origin/main`. Модуль `notify.py` НЕ менял — только добавил тесты (инвариант #16).

**Пуш КОДА** через `push_to_github.py` абсолютными путями ПОД `PROJECT_ROOT` (worktree на `/tmp` → скопировал
в главный репо на верный путь `spa_core/tests/`, чтобы `relative_to(PROJECT_ROOT)` дал верный repo-path, а не
basename-collapse в root — память `push-to-github-absolute-paths`). STATE/journal взяты из worktree на
`origin/main` (локаль на 969 коммитов позади — редактировать локальные склоббербы origin).

**Не трогал:** RiskPolicy/kill-switch/risk-логику, живой трек (`data/equity_curve_daily.json`),
деплой/агентов, site-copy (`landing/`). Тесты только добавил. owner-done не ставил.

## 2026-07-16 — Автономный цикл #12 (hardening: CLI-обёртка протокола регресс-тесты)

**Очередь пуста** (inbox new / owner-done / promotions = 0; `ingest-notes` — нет свободных заметок). По
автономному мандату взял ОДНУ безопасную hardening-задачу. Сознательно диверсифицировал от **внутренностей**
`owner_queue` (queue/intake/history_check/notify/safe_site_push — циклы #2–#11) и адаптеров → на уровень
**точки входа**: `scripts/orchestrator_queue.py`.

**Цель.** `scripts/orchestrator_queue.py` — детерминированный stdlib-CLI, через который LaunchAgent-оркестратор
(`com.spa.orchestrator`) КАЖДЫЙ цикл дёргает весь протокол: `list` / `set-status` / `create` / `ingest-notes` /
`promotions` / `notify`. Молчаливый слом ЗДЕСЬ (неверный exit-code, потерянный `owner-done`-refusal, битый JSON)
корраптит петлю owner-queue, а на origin — **0 выделенных тестов**. Это project-management CLI (LLM здесь разрешён),
НЕ risk/kill/gates/site/деплой → тесты в scope мандата.

**Что покрыл (+17 тестов, `spa_core/tests/test_orchestrator_queue_cli.py`, файл новый):** сквозь `main(argv=…)` —
- `list` — JSON-контракт `_card_dict` (ключи, на которые завязаны дашборд/вызовы), фильтр по type+status, human-«no matching cards»;
- `set-status` — happy-path (exit 0, строка статуса переписана), **инвариант #14: `owner-done` → REFUSED exit 2, карточка не тронута**, отсутствующий файл → ERROR exit 1;
- `create` — файл создан + путь напечатан (дефолт-статус по типу), **`owner-done` → REFUSED exit 2 (файл не создан)**, repeatable `--field k=v` во frontmatter, `--body-file`;
- `ingest-notes` — пусто → «(no loose notes…)», заметка (русская) → карточка в трекере;
- `promotions` — JSON и human-ветки через мок `scan_promotions`;
- `notify` — **`--check` строит сообщение и НИКОГДА не идёт в send-путь** (real-send замокан бросать), обычный send репортит «OK: notified» с верным path+dry_run=False;
- `main([])` → `SystemExit` (argparse required subcommand).

**Герметичность/оффлайн:** `queue.TRACKER_DIR`/`INBOX_NOTES_DIR` перенаправлены на tmp (CLI резолвит трекер через
эти module-глобалы); реальный Telegram-бот/Keychain НЕ трогается — send-путь `notify` замокан на CLI-уровне
(`cli.notify_needs_owner`). Модуль загружен по файловому пути (`importlib.spec_from_file_location`, как
`test_build_agent_registry.py`), т.к. `scripts/` — не пакет. `owner-done` нигде не пишется (инвариант #14).

**Тесты.** 17/17 новых + 75 суммарно смежных (owner_queue/intake/notify/history_check/safe_site_push) зелёные в
worktree на `origin/main`. CLI-модуль НЕ менял — только добавил тесты (инвариант #16).

**Пуш КОДА** через `push_to_github.py` абсолютными путями ПОД `PROJECT_ROOT` (worktree на `/tmp` → файл скопирован
в главный репо на верный путь `spa_core/tests/`, чтобы `relative_to(PROJECT_ROOT)` дал repo-path, а не
basename-collapse в root — память `push-to-github-absolute-paths`). STATE/journal — из worktree на `origin/main`
(локаль на ~969 коммитов позади).

**Не трогал:** RiskPolicy/kill-switch/risk-логику, живой трек (`data/equity_curve_daily.json`),
деплой/агентов, site-copy (`landing/`). Тесты только добавил. owner-done не ставил.

---

## 2026-07-16 · Автономный цикл #13 (hardening: ask_router fail-safe)

**Контекст.** Очередь пуста (inbox new / owner-done / promotions / loose-notes = 0; `ingest-notes` без
свободных заметок). По автономному мандату (§«управляемая автономия») — ОДНА безопасная hardening-задача.
Диверсифицировал от owner_queue-внутренностей / адаптеров / CLI / router (циклы #2–#12 их покрыли).

**Что сделал.** Регресс-покрытие непокрытых **fail-safe веток** `spa_core/telegram/ask_router.py::classify_and_answer`
— это ЖИВОЙ классификатор каждого сообщения владельца в Telegram (вопрос / задача / непонятно), общий для
бота (`_classify_route`) и event-driven intake. Документированный контракт «любая ошибка → `unclear`» был
покрыт лишь наполовину (5 тестов: question/task/unclear/OSError-failsafe/malformed→answer). Непокрытыми
оставались ветки, регрессия в которых = спурьозные карточки или сфабрикованный ответ на живом owner-пути:

- **rc≠0** (claude non-zero exit — rate-limit / auth / crash) даже при непустом stdout → должно fail-safe в
  `unclear`, а НЕ отдать stale-stdout как ответ (проверяю, что «старый кэш» не утекает в ответ);
- **пустой stdout** → `unclear` («Пустой ответ…»);
- **whitespace-only stdout** (strip → пусто) → `unclear`, не «пустой question»;
- **`subprocess.TimeoutExpired`** (самый вероятный реальный сбой медленного headless-claude) → generic
  `except` ловит → `unclear`;
- **UNCLEAR без второй строки** → дефолтный уточняющий вопрос (`rest.strip() or …`), не пустой промпт;
- **QUESTION с пустым телом** → плейсхолдер `(пустой ответ)`, не blank;
- **регистро-/пробел-устойчивость маркера** — `head = first.strip().upper()`: `task` / `Task` / ` TASK ` /
  `task\n` все → `task`; lowercase `question` → сохраняет тело как ответ. LLM-казинг недетерминирован.

+8 регресс-тестов (`spa_core/tests/test_ask_router.py`: 5 → 13). Модуль `ask_router.py` **НЕ менял** — только
тесты (инвариант #16). Замечено (НЕ чинил молча — поведенческое изменение, риск ложных карточек):
`TASK: <текст>` на одной строке классифицируется как `question` (head≠`TASK`) — если владелец подтвердит,
что это баг, завести карточку; сейчас не трогаю логику.

**Тесты.** 13/13 (файл) + 37 смежных (ask_router / inbox_intake / owner_intake / bot_commands) зелёные в
worktree на `origin/main`. Baseline 5/5 до правки зелёный. Модуль untouched (`git diff --stat` пуст).

**Пуш КОДА** через `push_to_github.py` абсолютными путями (готча basename-collapse — файл на верный путь
`spa_core/tests/`). STATE/journal — из worktree на `origin/main`.

**Не трогал:** RiskPolicy/kill-switch/risk-логику, живой трек (`data/equity_curve_daily.json`),
деплой/агентов, site-copy (`landing/`). Тесты только добавил. owner-done не ставил.

## 2026-07-16 — Q11 ветка yield-lab-scaffolding (owner: разобраться детально)
Разведка: ветка ЛОКАЛЬНАЯ, форк 30.06, 980 коммитов позади main. Вся research-работа (Maple-HY non-Ethena
9-11%, tokenized-credit screen, ADR-YL-009, non_ethena_ladder) УЖЕ в main в более полном виде (main
tokenized_credit 4399B + «Huma DEEP DD» vs branch 2225B). Уникально-ценного = 0 (6719 из 7671 файлов =
закоммиченный cabinet/node_modules; остальное — дубли старых тестов + пустой init). Слияние откатило бы
~528 файлов / ~2 недели main (PROJECT_CONTROL, owner-gate, CI-workflows). ВЕРДИКТ: archive-as-tag → не
мержить. Сделал локальный тег archive/yield-lab-scaffolding (история сохранена), origin/main нетронут.

## 2026-07-16 — Q2 analytics_lab (доведено до зелёного) + bootstrap-пункт (единая доска карточек)

**Q2 (owner-approved: вынести аналитику в analytics_lab/).** Предыдущий bulk-скрипт упал на середине
переписывания импортов (IndexError на root-level пути) → 14 collect-ошибок, дерево красное. Доделал руками:
27 модулей уже в `spa_core/analytics_lab/`; переписал импорты по всему дереву `spa_core.paper_trading.<mod>`
→ `spa_core.analytics_lab.<mod>` ТОЛЬКО для 27 перенесённых имён (проверил: смешанных import-строк нет;
`concentration_analytics` и др. НЕ перенесённые — не тронуты; `risk_contribution` продолжает импортить
`paper_trading.concentration_analytics` корректно). Плюс обновил захардкоженные пути в hygiene-тестах
(`spa_core/paper_trading/<mod>.py` → `analytics_lab/`) и logger-лейблы. Итог: `--collect-only` **102 608
собрано, 0 ошибок**; целевые тесты **821 passed / 78 skipped / 330 subtests**; entry-points (cycle_runner,
api.server, risk.policy, kill_switch, analytics_lab) импортируются чисто. Инвариант #16 соблюдён — тесты
переехали с модулями (пути обновлены, ничего не заглушено).
_Готча:_ мой rewrite-скрипт случайно зацепил `.claude/worktrees/*` (относительный путь обошёл фильтр) —
косметические правки в 3 заброшенных worktree; классификатор верно заблокировал `git checkout` там
(возможна работа параллельных сессий) — не трогаю, в основной пуш не входят.

**Bootstrap (owner-директива при 98% контекста: по умолчанию читать историю решений + все карточки в
одном месте, чтобы новое окно не было «новым сотрудником»).** Ядро уже было (CLAUDE.md §1 + memory
`session-bootstrap-read-first`). Добавил недостающий кусок — **единый обзор карточек одним файлом:**
- `scripts/build_tracker_board.py` (stdlib, атомарная запись) → `nimbalyst-local/tracker/_BOARD.md`:
  все 56 карточек по типу+статусу, вверху секция «🔴 ЖДЁТ ВЛАДЕЛЬЦА» (сейчас 3: own-07 ключи писем,
  own-17 capacity-число, own-21 чистка агентов).
- Регенерация встроена в `orchestrator_queue.py` (create / set-status / ingest-notes → `_rebuild_board()`,
  best-effort, не роняет мутацию) → доска всегда свежая.
- Указатели на `_BOARD.md` добавлены в CLAUDE.md §1 и memory (читать первым, не открывать 56 файлов).

**Не трогал:** RiskPolicy/kill-switch/risk-логику, живой трек, деплой/агентов, site-copy. owner-done не ставил.

## 2026-07-16 — Q6 уведомления о заявках (крупные → instant Telegram, прочие → дайджест)

Owner Q-OWN-16: крупные/B2B заявки — мгновенный пинг, остальные — в дайджест. До сих пор ВСЕ заявки
(`/api/pilot/request` → `interest._notify_owner_telegram`) демотились в дайджест через
`push_policy.enqueue_digest`. Сделал:
- **push_policy.py:** новый whitelist-key `pilot_request` + `ONESHOT_KEYS`. Заявки — повторяющиеся
  one-shot события; edge-trigger (push на ok→bad, потом «still bad → silent») заглушил бы 2-ю заявку.
  One-shot путь: всегда пуш под daily-ceiling, БЕЗ persistent bad-state → каждая крупная пингуется;
  потолок 10/сутки сохранён (флуд сворачивается в одно «more events»).
- **interest.py:** детерминированный `_is_material_lead()` — поля суммы нет (zero-PII), «крупная/B2B»
  по сигналам: домен email не из бесплатных провайдеров (B2B/фонд/family-office) ∨ source=early_access ∨
  tier=aggressive. Material → `push_critical("pilot_request",…)` (instant), прочие → `enqueue_digest`
  (как было). Единая Telegram-власть соблюдена (только через push_policy), fail-safe, zero-PII на /admin.
- **ADR-OWN-2026-07-lead-pings** + INDEX. Тесты: +3 one-shot в `test_push_policy` (16), новый
  `test_lead_ping_routing` (8: классификатор + маршрутизация + never-raises). Срез 92/92 зелёный.

**Не трогал:** RiskPolicy/kill-switch, живой трек, деплой, site-copy. owner-done не ставил.

## 2026-07-16 — Q4/Q5 числа/нейминг сайта (owner-gated, owner-decided)

**Q4 — убрать расшифровку «Smart Passive Aggregator».** На рендерящихся .astro НЕ было (там «SPA —» =
«SPA это…»). Литеральная расшифровка жила в публично-отдаваемых `landing/public/robots.txt` (комментарий)
+ `landing/public/llms.txt` (первая строка описания для AI-агентов) + `landing/package.json` description +
внутреннем `LANDING_BUILD_STATUS.md`. Заменил на «SPA» везде; grep подтверждает 0 остатков; package.json
валиден. Запушено в live (CF Pages). Изменения не-компилируемые (статик public + метаданные), honesty-числа
не тронуты. own-08 ingested.

**Q5 — Conservative «до 6%» заголовком + ~3.3% realized рядом.** VERIFY-before-building: проверил — УЖЕ
реализовано консистентно прежними работами (tier_bands.json единый источник band-строк; UX-14 убрал
волатильный дневной LIVE-бейдж-число; 6mo-M1 #7 консолидация). «up to 6% / до 6%» заголовок + «~3.3%
realized · 0.0% drawdown» рядом на index (hero + comparison-bar + tier-cards), packages, SiteHeader-nav
(из единого источника), strategies/conservative. Band читается как потолок/потенциал, реализованное
~3.3% показано фактом рядом — honesty соблюдён. Оставшиеся «2-6%» в коде = комментарии про УЖЕ
исправленный баг + BTC borrow-утилизация (не Conservative APY). Расхождений заголовка Conservative нет —
числа НЕ выдумывал, лишних правок не делал. own-14 ingested. (own-17 fundability-capacity — отдельное
needs-owner, число за владельцем, не Q5.)

**Не трогал:** RiskPolicy/kill-switch, живой трек, деплой-конфиг, tier APY-значения, tier_bands.json.

## 2026-07-16 (продолжение — автономный цикл #14)

- **Hardening (автономный цикл env-setup-v3, #14):** очередь пуста (inbox/owner-done/promotions/
  loose-notes = 0) → hardening. Сознательно диверсифицировал от owner_queue/адаптеров/CLI/router
  (циклы #2–#13) в домен **monitoring**: покрыл `spa_core/monitoring/data_freshness_monitor.py` —
  self-детектор устаревания данных site/monitoring-контура (классификатор FRESH/STALE/MISSING по
  per-type порогам mtime). На origin было **0 выделенных тестов**; если его граничная логика врёт,
  молча устаревший фид не репортится. +12 герметичных регресс-тестов (`spa_core/tests/
  test_data_freshness_monitor.py`, файл новый; инжект `clock`/`thresholds`/`file_map`, все файлы под
  `tmp_path`, запретных файлов — включая живой трек — НЕ трогает):
  - **STALE/FRESH граница строгая** (`age_sec > max_age_sec`): ровно на пороге → FRESH, +1с → STALE
    (порог/`now` запинены к реальному mtime → иммунно к округлению mtime ФС);
  - **два пути MISSING**: замапленный файл отсутствует на диске **и** неизвестный `data_type`
    (`_resolve_path`→None, `path:None`) — оба дают MISSING;
  - **`is_fresh()` три-состояние**: `None` (неизвестный/до `check_all`) vs `False` (STALE/MISSING) vs
    `True` (FRESH) — чтобы `None` не путался с `False`;
  - **консистентность summary/списков**: счётчики == длинам списков, каждый тип ровно в одном ведре
    (partition), `stale_count`/`missing_count`, метаданные чека (threshold_sec/label/path), label-
    fallback `"<n>s"` для незамапленного типа.
  Модуль НЕ менял (только тесты, инвариант #16). 13/13 новых + 5261 смежных зелёные (в worktree
  1 сбой `test_tier1_e2e::test_status_health_invariant` — data-dependent e2e, в worktree нет
  gitignored live `data/`; на основном дереве с живыми данными **passed** — не мой change, файл
  чисто аддитивный, не трогаю чужой тест). Пуш кодом+STATE+journal через `push_to_github.py` из
  PROJECT_ROOT (без basename-collapse). НЕ трогал risk/kill/трек/site/агентов; owner-done не ставил.

## 2026-07-16/17 — Автономный цикл #15 (hardening: owner-gate interlock регресс-тесты)

- **Hardening (автономный цикл env-setup-v3, #15):** очередь пуста (inbox/owner-done/promotions/
  loose-notes = 0) → hardening. Диверсифицировал от owner_queue/адаптеров/CLI/router/monitoring
  (циклы #2–#14) → **safety-critical fail-CLOSED gate** `scripts/check_owner_gate.py` — интерлок,
  который не даёт автономному оркестратору авто-шипнуть OWNER-GATED правку сайта в live (сайт
  push==live, CF Pages билдит `landing/` на каждый push). Гейт классифицирует изменение по классам
  A–E (A solicitation · B числа доходности · C нейминг тиров + расшифровка «SPA» · D legal/disclaimer ·
  E удаление honesty-токенов L0–L6/refused-for-live) и заворачивает в owner-карточку. На origin —
  **0 выделенных тестов**, при этом молчаливая регрессия здесь = **fail-OPEN**: owner-gated число
  уехало бы в прод мимо владельца (нарушение инварианта #8 + site-copy rule).
- **+34 герметичных регресс-теста** (`spa_core/tests/test_check_owner_gate.py`, файл новый; модуль
  грузится по файловому пути через `importlib`, т.к. `scripts/` — не пакет):
  - `_changed_paths_and_hunks` — парсинг diff-hunk'ов (+/- нумерация строк, пропуск `/dev/null`-таргета);
  - `_scan_free_text` классы A–E: solicitation EN+RU (только на `+`; удаление — де-риск, НЕ гейтит),
    baked yield-число, **КЛЮЧЕВОЙ per-span dynamic-window fail-OPEN регресс** (далёкий `{snap.x}` НЕ
    подавляет запечённое «30% net APY» на той же строке — старый line-level суппрессор подавлял →
    fail-open; соседний `{snap.x}` вплотную — по-прежнему exempt), non-canon «SPA»-расшифровка гейтит /
    canon exempt, legal-path гейтит ЛЮБОЕ изменение (оба знака) и короткозамыкает A/B/C, удаление
    honesty-токена гейтит / добавление — нет;
  - `_tier_bands_violations` / `_track_snapshot_violations` — field-diff структурного JSON (число→B,
    нейминг→C, исчезновение evidence→E, добавление evidence — не гейт; exempt короткозамыкает;
    walk вложенных `_pct`/`_usd`, non-number поля игнорятся);
  - `_snapshot_is_custodian_equivalent` — custodian-exemption не подделываема (без data-канона → False,
    fail-closed, а не бросок);
  - **end-to-end через одноразовый `git init` repo** (git-range mode): чистая layout-правка → shippable;
    baked число → GATED; **owner-approval bypass** только валидным `Owner-Approved: own-NN` трейлером
    при карточке `owner-done` с покрывающим `approves:` scope (монкипатч `list_cards`, никакой реальный
    card-store/Telegram/Keychain не тронут, `owner-done` не пишется — инвариант #14); non-owner-done
    карточка / отсутствие трейлера → bypass НЕ даётся.
- Модуль `check_owner_gate.py` **НЕ менял** (только тесты, инвариант #16). 34/34 новых зелёные;
  `--collect-only` всей сюиты чист (90 032 собрано, 0 ошибок импорта).
- **Наблюдение (НЕ чинил молча, инвариант #16 / scope=одна задача за цикл):** на origin/main
  предсуществующе КРАСНЫ 3 теста `spa_core/tests/test_orchestrator_queue_cli.py`
  (`test_create_writes_file_and_prints_path`, `test_create_parses_repeatable_extra_fields`,
  `test_create_reads_body_file`) — `create` дёргает авто-реген `_BOARD.md`, который пишет в РЕАЛЬНЫЙ
  `nimbalyst-local/tracker/` и затем не находит карточку в монкипатченном tmp `TRACKER_DIR` →
  `FileNotFoundError` (test-isolation баг в тесте цикла #12, не в проде). Падают идентично БЕЗ моего
  файла (проверено на чистом основном дереве) — не мой регресс. Оставляю сигнал будущему hardening-циклу
  (не owner-gated, тест-гигиена; не карточка).
- Пуш кодом+STATE+journal через `push_to_github.py` из PROJECT_ROOT (без basename-collapse), verified
  на origin. НЕ трогал risk/kill/трек/site/агентов; owner-done не ставил.

## 2026-07-16/17 — AAA продуктовый слой: Шаги 1-3 + КЛОББЕР-ФИКС (честно)

Owner включил режим авто-продвижения («не стой и не жди часами») → прописал в memory
`decide-dont-ask-continue` (НИКОГДА не заканчивать ход вопросом-разрешением; авто-переход к следующему
пункту). Взялся за AAA.

**Скоуп (субагент, read-only)** → `docs/AAA_PRODUCT_LAYER_ACTIVATION_PLAN.md` (staged-план, swarm как
шаблон активации). **ОШИБКА субагента+моя:** он сканировал ЛОКАЛЬНОЕ дерево и пропустил, что прошлая
сессия УЖЕ построила интегрированный CMO-слой на **origin** (`spa_core/cmo/{honesty_gate,draft_store,
template_rewriter,pipeline}.py` + `api/routers/cmo.py` approve/reject + server.py). Локальное дерево
его не имело (дрейф origin>local).

**Что сделал:**
- **Шаг 2 (genuinely new, конфликтов нет):** `spa_core/investment_os/harness.py` — база продуктовых
  аналитиков: feed fail-closed→UNKNOWN, evidence L0-L6, LLM number-gate (несорсенное число→fallback),
  advisory emit+proof. 16 тестов. Импортит `_extract_numbers` из cmo.honesty_gate (есть в prior).
- **Шаг 1/3 — КЛОББЕР:** я по ошибке создал+запушил `cmo/honesty_gate.py` дублем с API `check` вместо
  prior `check_draft` → сломал `template_rewriter` (импортит `check_draft`). Нарушил правило «сверяйся
  с origin ПЕРЕД правкой».
- **ФИКС:** восстановил prior `honesty_gate.py` + его тест (406 строк, ±2% толеранс, disclaimer-группы,
  RU, «не является офертой» уже в дисклеймерах). `editorial_agent.py` переписал в тонкий ЖИВОЙ RUNNER,
  делегирующий в prior `pipeline.run_pipeline` (читает track_ledger+refusal-log→facts→rewrite→gate→
  DraftStore). НЕ дублирует rewrite/gate — это недостающие живой агент + launchd, которых у прошлой
  сессии не было. Задеплоил `com.spa.cmo_editorial` через gate (sandbox exit 0, живой трек hash не
  тронут, ≤3, /tmp логи). Живой прогон: draft `cmo_20260716_000` создан (status draft, gate→честный
  dry-fallback), НИКОГДА не публикует (flow B).
- 116 тестов зелёные (prior honesty_gate + draft_store + мой runner + harness). Reconciliation на origin.

**УРОК (в memory):** ВСЕГДА `git show origin/main:<path>` перед созданием файла в cmo/ или где возможен
дрейф. Субагент-скоуп ОБЯЗАН сверяться с origin, не только локально.

**Не трогал:** RiskPolicy/kill/трек/execution/деплой-сайта. CMO публикацию НЕ делал (owner-gate). owner-done не ставил.

## 2026-07-17 — AAA Phase 2: три аналитика на harness (по одному, origin-check каждый)

Owner «продолжай Phase 2». Строю аналитиков на `investment_os/harness` — каждый: feed fail-closed→UNKNOWN,
evidence L0-L6, advisory emit+proof. Origin-check ПЕРЕД каждым (урок применён — поймал коллизию имени теста).
- **Шаг 7 — Stablecoin Yield** (`agents/stablecoin_yield.py`): ранжирует T1/T2 из `apy_ranking.json` по
  risk-adjusted APY, T3 exotic исключён+посчитан (refused-for-live), evidence L3/L4, feed stale→UNKNOWN. 7 тестов.
- **Шаг 8 — Reporting** (`agents/reporting.py`): surfaces свой evidenced трек (`track_ledger`) + day30-review
  readiness, evidence L6, ничего не выдумывает. НЕ mtime-гейтит трек (freshness = last_evidenced_date в теге —
  чтобы во время time-gated ожидания не читалось UNKNOWN). Distinct от `agents/reporting_agent.py` (Telegram —
  НЕ трогал, поймал коллизию теста → назвал `test_investment_os_reporting.py`). 4 теста.
- **Шаг 10 — Market Regime** (`agents/market_regime.py`): RESHAPE — потребляет `market_regime.json` (yield) +
  `swarm/funding_regime.json` (funding GREEN/YELLOW/RED), НЕ пересчитывает; combined_posture = most-cautious
  (fail-safe); одна сторона stale→UNKNOWN-тег, обе→UNKNOWN. 6 тестов. Не дублирует существующие продюсеры.
Все live --check работают (stablecoin: 29 пулов; regime: STABLE; reporting: 19/30 трек). 33 теста зелёные.
Пока код+тесты; деплой батчем ≤3 через gate + read-only роутер `investment_os` — следующий шаг.
**Не трогал:** RiskPolicy/kill/трек/execution/деплой-сайта/live-apiserver. owner-done не ставил.
