# Журнал — неделя 2026-W39

## Автономный цикл оркестратора #656 (21.09) — G59 п. 2 приказа «Portfolio CIO»: канал имени был одноязычным (ADR-437)

**Шаг 0a-ГОЛОД.** Голодающий приказ «Portfolio CIO»
(`inbox-task-portfolio-cio-dynamic-capital-alloc`), 721 ч, 436 циклов мимо — взят ПЕРВЫМ.
Предмет задан не очередью, а хвостом ADR-436: стоячий приказ несёт блок «ЗАКАЗ», и
следующая строка ряда названа там дословно.

**Занятость ИЗМЕРЕНА, а не предположена.** Последний claim по карточке — `cycle-72304`
(#655), `card_state=done`, коммит `8a9b3481` на origin; pid 72304 мёртв по `ps` (пусто).
Живых держателей нет. Осиротевший `pytest` pid 79605 ЖИВ, cwd `/private/tmp/spa_c655` —
это предписанный прогон #655, ЧУЖОЙ контроль: не тронут, и своего полного прогона рядом
не запускал, пока он идёт (два полных прогона морят друг друга ядрами).

**Журнал заявок прочитан ДО первой правки** (шаг 1 протокола не отменяется голодом):
предмет G59 свободен, соседей по файлам нет.

### Ответ заказу

Заказ: «сколько ЧИСЕЛ стои́т в семи таблицах вне канала и сколько совпадает с порогом,
уже известным переписи; нет совпадений — канал верно узок, есть — молчит граница канала».

Ответ — `OUT_OF_CHANNEL_SILENT`, и держится он на ОДНОЙ паре:
`docs/allocation_logic_explicit.md:282` «Base-цепочка | до 20 % только после go-live»
против `spa_core/risk/policy.py:162` `BASE_CHAIN_CAP = 0.20` и витрины
`chain_caps.base_chain_pct: 20.0`. Один порог, три копии, ADR-025 назван обеими
сторонами. Число в `docs/**` НЕ правлено: соседство с порогом RiskPolicy — предмет №1
границы ADR-285, уехало карточкой
`owner-decision-potolok-base-tsepochki-zapisan-v-treh-mestah`.

### Главное: первый ответ был ЛОЖНЫМ, и ложным о самом приборе

Честно измеренный первый ответ — `NARROW`, «канал верно узок». Он был бы утверждением
о ПРИБОРЕ: канал имени спрашивал у строки английский идентификатор (`base_chain_pct`),
а `docs/` и `.claude/rules/` написаны по-русски — «Base-цепочка». Класс ровно тот же,
что у ADR-431 («ноль был формой вопроса») и ADR-434 («не объявлена никем» было выводом
о канале), и поймать его удалось только потому, что память проекта держит это
рассуждение отдельной записью.

Проверено дифференциально, а не рассуждением: карта синонимов пуста ⇒ `NARROW`/0,
карта на месте ⇒ `SILENT`/1. **Вердикт переворачивается картой**, и обе стороны
закреплены тестами; третий контроль показывает, что карта НЕ красит соседний русский
абзац с процентом.

### Своя частота ошибки — измерена, а не предположена

Голых совпадений по величине **106 из 269**. Докладывать это число значило бы отвечать
на вопрос «встречается ли такая цифра»: порог `var_horizon_days = 7` равен номеру
седьмой строки таблицы, `$1M`/`$10k` — размеры проб красной команды, `0 %` — запрет
на протокол. Обогащение имени над голой величиной **×0.046**, то есть называние порога
и равенство цифры здесь почти антикоррелированы.

### Две свои ошибки, найденные приёмкой

1. **Имя без единицы объявлялось находкой.** Первая редакция дала шесть названных пар;
   пять — голые `30` против `min_paper_days_before_live`, у которого различающий токен
   ровно один (`paper`), часть чисел — обломки ISO-дат. Достаточным свидетельством стало
   имя **и** единица вместе; пара «имя без единицы» не выброшена, а ушла в свой канал —
   иначе нижняя граница ответа пропала бы молча. Названных **6 → 1**.
2. **Ветка слепоты была НЕДОСТИЖИМА.** `constitution_values` возвращает `({}, причина)`,
   а спрашивал я `if shelf is None`. Объявлена слепота, у которой нет ни одного
   исполнения. Чинено чтением ПРИЧИНЫ, контроль в обе стороны: витрина-не-JSON ⇒
   слепота названа; пустая витрина без причины ⇒ НЕ названа.

### Батарея нашла ещё две дыры — в тестах, и обе настоящие

Первый заход: **20 убито, 2 выжили**.

* «нулевой знаменатель ⇒ `enrichment = 1.0`» пережил проверку «число ЛИБО причина» —
  то есть сочинённое число на нулевом знаменателе проходило как ответ. Это инв. #17
  наизнанку. Закрыто отдельным тестом: строк, называющих порог, ноль ⇒ обогащение
  обязано быть `None` с причиной.
* «`TABLE_CHANNEL_DOCS` → `TABLE_CHANNEL_RULES`» пережил проверку проводки: она
  спрашивала только строковые литералы, а имя канала — `Name`-узел. Координата
  отвечала бы на ЧУЖОЙ вопрос (внутри объявленного канала молчания границы нет по
  построению). Закрыто проверкой имени узла в обе стороны.

Второй заход после усиления: **22/22 убито, выживших нет**, дерево сверено по sha после
каждой мутации.

### Приёмка

34 теста нового файла, **все 34 красны** на чистом `origin/main` `8a9b3481` (контроль в
отдельном дереве; sha предмета на контроле побайтно равен origin — `0099f37f8`, сверка
по `rev-parse`, а не `diff --stat`). Секция отчёта вынесена ЗА блок координаты формы и
закреплена тестом: вложенная внутрь его `else`, она молчала бы у любого дерева, где
форма не измерена, — ошибка размещения, которую соседний сторож поймал у #655.
Литеральных дат-якорей и литеральных pid в файле нет; две ISO-даты в теле суть ПРЕДМЕТ
теста (отсев обломков), а не фикстура времени.

**Обязательные шаги цикла:** офис — код 0 из прод-каталога данных (**из worktree код 3 =
НЕ ИЗМЕРЕНО**, и это не «всё хорошо»: артефакты пишет флот в прод-дерево, в git их нет
по построению); `cycle_analytics_audit` — код 0 (в окне такта, 18.76 ч при 30 ч);
`book_second_record` — **код 1**, оба расхождения прежние и уже карточенные (дыра T008
23.08 на $14 999.88; переименование `fluid_usdc`→`fluid_fusdc` T034);
`capital_observability_census` — обе оси **100.00 %**, второй артефакт не спорит;
`build_site_numbers --if-due` — публикация не назначена (1 из 7 дн);
`import_time_anchor_bombs` — код 0, бомб нет.

**НЕ тронуты:** пороги RiskPolicy v1.0, стоп-кран, живой трек, `landing/`,
`POLLED_ADAPTERS`, аллокатор, деплой агентов. Ни один тест не ослаблен и не отключён
(инв. #16): все правки тестов — усиления, и каждая названа выше.

## Цикл #657 — заказ G60 пп. 1–2: одноязычие кусается там, где у токена есть слово в языке (ADR-438)

Взята первой стоячая карточка приказа владельца «Portfolio CIO»
(`inbox-task-portfolio-cio-dynamic-capital-alloc`), заказ **G60 пп. 1–2** —
хвост ADR-437.

**Вопрос заказа.** ADR-437 нашёл одноязычие у ОДНОЙ координаты и предположил,
что тем же больны `subject_witness` (ADR-417) и канал объявления именем
(ADR-434). Проверено замером, а не согласием.

**Ответ: посылка верна наполовину, и половина измерена.** Население — ВСЕ ТРИ
оси переписи (219 пар, 285 модулей), а не выборка, на которой одноязычие
нашлось (урок ADR-436). `subject_witness` сменил вердикт **0** раз; ноль НЕ
вырожден по языку — 158 из 285 модулей написаны по-русски, — он вырожден по
КАРТЕ: различающие токены основы имени файла закрыты целиком лишь у **1**
модуля. Канал имени, наоборот, назвал **59** пар, из них подтверждены
величиной **4**. Причина разницы названа: карта переводит ПОНЯТИЕ, а имя
модуля и идентификатор в кавычках понятием не являются и едут в русский текст
латиницей как есть.

**Цена расширения измерена контролем:** 59 на 15 объявлениях против 147 на 160
контрольных абзацах = ×4.28, тогда как дословный канал ADR-434 на том же
дереве даёт ×6.4. Двуязычие по токенам различает ХУЖЕ дословного, и это число.

**Находка цикла.** Из 4 подтверждённых пар руками проверены все 4; две
настоящие, и обе — вторая копия `RiskPolicy.min_cash_pct = 0.05`:
`spa_core/governance/churn_damper.py:60` (`_MIN_CASH_PCT`) и
`spa_core/reporting/performance_attributor.py:56` (`CASH_BUFFER_PCT = 5.0`).
Обе сознаю́тся в происхождении своим же комментарием, и НИ ОДНОЙ не было в
документе переписи ДО этой координаты (замер на `4eb054307`: 0 вхождений) — имена разные, а правило
называет порог по-русски. Две пары ложны одинаково: равное число называет
разные пороги (класс ADR-418), поэтому подтверждение величиной объявлено
фильтром обозримости (59 → 4), а не доказательством.

**Полнота карты (п. 2)** получила величину: из 35 известных переписи порогов
закрыты целиком 16, частично 10, не закрыты вовсе 9; различающих токенов без
русского соответствия — 13 из 27 (`base`, `derisk`, `hard`, `l2`, `pnl`,
`soft`, `start`, `t1`, `t2`, `t3`, `unrealized`, `var`, `z`).

**Что НЕ сделано намеренно.** Копии не правятся — порог RiskPolicy на денежном
пути есть предмет №1 границы ADR-285; заведена карточка владельцу
`owner-decision-pyat-protsentov-kesha-zapisany-v-dvuh-me (дополнением, а не второй карточкой о том же пороге)` с тремя вариантами и
рекомендацией. Карта не дополнена ни словом: дополнить значило бы сдвинуть тот
самый замер, который недостачу и обнаружил.

**Приёмка.** 55 тестов нового файла; 53 красны на чистом `origin/main`
`4eb054307` (sha предмета на контроле побайтно равен origin — `c5102a550`).
Зелёных два, и это объявлено ДО прогона: оба закрепляют поведение ADR-437,
которое рефакторинг обязан был сохранить. Батарея мутаций по координатам: 33
мутации, убито 31. Первый заход оставлял ДЕВЯТЬ выживших — семь были
настоящими дырами тестов и закрыты усилением (полнота против пересечения у
обоих знаменателей, «текст есть, но не прочитан», частичное закрытие карты,
контроль как объявление, проводка общего индекса, латинские токены врозь, пара
со свидетелем на другой стороне). Две оставшиеся выжившие — сторожа,
ДОКАЗАННО редундантные сегодня: свидетель предмета срабатывает только на
дословном имени, значит все токены основы стоя́т в тексте латиницей, и канал
токенов физически не может ответить `bilingual`. Редундантность закреплена
двумя тестами по формам (с проверкой `checked > 0`: пустой обход доказал бы не
редундантность, а отсутствие сцен), и названа вслух в ADR, а не спрятана в
проценте убитых.

**Вторая копия правила НЕ заведена.** Правило сверки токенов вынесено в
`_token_evidence` и подаётся картой-аргументом; копия его внутри переписи «одно
правило — две копии» была бы ровно тем предметом, который перепись ищет.

**Обязательные шаги цикла.** Офис — прочитано 97, не прочитано 0.
`cycle_analytics_audit` — код 0 (такт не подошёл). `import_time_anchor_bombs` —
код 0. `capital_observability_census` — обе оси 100 %. `build_site_numbers
--if-due` — код 0, внутри недели. `book_second_record` — **код 1, расхождения
НАЗВАНЫ**: дыра в записи T008 (2026-08-23, $94 999.88 → $80 000.00) и
переименование ключа T034 (`fluid_usdc` → `fluid_fusdc`); оба — уже известные
классы (ADR-350 и родня), новых не появилось.

**НЕ тронуты:** пороги RiskPolicy v1.0, стоп-кран, живой трек, `landing/`,
`POLLED_ADAPTERS`, аллокатор, деплой агентов, пределы `MAX_DECLARING_*`,
население `decision_surfaces`. Ни один тест не ослаблен и не отключён
(инв. #16): все правки тестов — усиления против переживших мутаций, и каждая
названа выше.

**Предписанный полный прогон.** 119 177 passed · 39 failed · 682 skipped
(2:30:42). Ни одно падение не моё, и это ЗАМЕР, а не убеждение: все 39 лежат в
21 файле, и те же 21 файл, прогнанные ОДНОЙ И ТОЙ ЖЕ командой на чистом
`origin/main` `4eb054307` и на моём дереве, дали **поимённо одинаковые 21
падение** — пересечение по именам полное, разности нет ни в одну сторону.
Разница 39 против 21 — соседская (население прогона другое), и она одинаково
действует на обе стороны. Граница метода названа: полного контрольного прогона
на origin (ещё 2,5 ч) я не делал, дифференциал снят по файлам-носителям. Класс
«красные от данных» уже числится карточкой `agent-ci-data-dependent-red-tests`.

**Карточка не заведена — заведено ДОПОЛНЕНИЕ.** Проверка очереди перед
созданием показала, что о том же пороге уже стои́т карточка от 19.09
(`owner-decision-pyat-protsentov-kesha-zapisany-v-dvuh-me`, пара `MIN_CASH` в
лимитах бэктеста). Вторая карточка об одном и том же числе была бы дефектом
очереди, а не вопросом: владелец увидел бы два «пять процентов кэша» и не понял
бы, чем они отличаются. Поэтому население вопроса дописано в существующую
карточку (было две копии, стало четыре, и у двух новых разная природа —
денежный путь против отчётности), а созданная было вторая карточка удалена до
пуша.

## Цикл #658 (21.09): заказ G61 п. 1 — точность канала есть ИНТЕРВАЛ, а правило читает ИМЯ (ADR-439)

Взят первым как голодающий приказ. Занятость ИЗМЕРЕНА до работы: последний
claim — `cycle-46419` (#657), `card_state=done`, pid мёртв по `ps`; живых
держателей нет. Журнал заявок прочитан ДО первой правки.

**Заказ был неисполним по построению, и это выяснилось раньше разметки.**
Канал выдавал `pairs` — только те срабатывания, которым величина уже нашла
подтверждение. Остальные 55 из 59 существовали как счётчик. Разметить число
нельзя; первой правкой стала запись населения целиком, обе стороны.

**Правило записано ДО разметки** константой в коде, применяется машиной
единообразно, свободных параметров нет. Ответ на `b2c8f4896`: 59 размечено,
настоящих 4, ложных 5, не решено 50 ⇒ **точность в [6,8 %, 91,5 %]**. Ширину
держит `UNDECIDED`, и это ответ заказа, а не уклонение: независимого свидетеля
хватило на 9 срабатываний из 59.

**Самое важное — правило измерено САМО, и оно не выдержало.** Контроль
размечался тем же правилом: обогащение `GENUINE` даёт ×3,88 на абзац
объявления и ×0,91 на срабатывание — два знаменателя отвечают противоположно,
и оба напечатаны. Метка при этом предсказывается размером набора токенов, а не
текстом: все 15 `GENUINE` обеих сторон имеют ровно один различающий токен, все
5 `ARTEFACT` — два. На четырёх парах, где у ADR-438 есть мнение глаз, разметка
расходится с ним на одной: `MAX_CAPITAL_PCT` зовётся настоящей по величине
`0.2`, пришедшей из «20 % T2» того же абзаца, то есть из ДРУГОГО порога, — и та
же пара помечена `GENUINE` на контрольной стороне, в абзаце про строку статуса
документа.

**Правило НЕ переписано после того, как разметка увидена.** Это не оставленная
работа, а условие заказа: мерка, выбранная после работы, не меряет ничего. Всё
добавленное после первого прогона — диагностика, и метки от неё не сдвинулись
ни одной.

**Своя ошибка, найденная батареей:** во всех сценах имя несло ЧИСЛО, поэтому
потеря записи за отсевом `value is None` была невидима, и мутация «писать
только подтверждённые величиной» — та самая прежняя редакция производителя —
пережила первый заход. Закрыто сценой с нечисловым именем; на живом дереве
такие имена составляют большинство населения.

**Две мутации первого захода были НЕ-ОПЕРАЦИЯМИ** (вставленный `pass`;
переименованный ярлык при сохранённом числе). Их выживание — свойство мутации,
а не тестов, и это названо, а не зачтено в убитые.

**Одна ветка снята как доказанно редундантная** — проверка «назван полный путь»
в `_module_named`: внутри собственного пути основа всегда обрамлена `/` и `.`.
Снятие проверено замером, все четыре числа живого дерева не сдвинулись.
Выжившая мутация сужения оставлена намеренно: её редундантность зависит от
фильтра соседнего производителя, то есть это контракт, а не украшение, и
доказана она тестом по формам с `checked > 0`.

Числа в `docs/**` и `.claude/rules/**` не правлены, пороги RiskPolicy не
тронуты, `decision_surfaces` не выросли ни строкой. Заказ **G62** — в хвосте
ADR-439.

---

## Цикл #659 (21.09) — заказ G62 п. 1: цена «двух токенов» (ADR-440)

**Взято:** стоячий приказ владельца «Portfolio CIO» первым (голодающий). Занятость
ИЗМЕРЕНА до работы: последний захват `cycle-20812` (#658) — `card_state=done`, pid
20812 и 46419 мертвы по `ps`. Дерево `/tmp/spa_c659` от `de7e1790e`.

**Обязательные приборы цикла.**

| шаг | код | что сказал |
|---|---|---|
| 0-офис (`consume_office_reports`) | 0 | прочитано 97, не прочитано 0; красные строки — уже стоящие заказы, новых карточек не породили |
| 1в `cycle_analytics_audit` | 0 | аудит мерили 29.1 ч назад при такте 30 ч — прогон не нужен |
| 1г `import_time_anchor_bombs` | 0 | бомб нет (задержка 95 мин вердиктов не сдвинула) |
| 1д `book_second_record` | **1** | расхождения НАЗВАНЫ (ниже) |
| 1е `capital_observability_census` | 0 | обе оси **100 %** ($95 000 из $95 000, 5 протоколов) |
| 1е `build_site_numbers --if-due` | 0 | внутри такта: со дня публикации прошёл 1 день из 7 |

**Вторая запись о деньгах, код 1 — оба расхождения известные, не новые:**
`ДЫРА В ЗАПИСИ T008 (23.08): $94 999,88 → $80 000,00` и
`ПЕРЕИМЕНОВАНИЕ T034 (11.09): fluid_usdc → fluid_fusdc, суммы равны`. Оба уже несут
карточки (`agent-dve-zapisi-o-dengah-*`, класс переименования ключа); новой дыры за
сутки не появилось, и это главное, что прибор обязан был сказать.

**Работа.** Заказ G62 п. 1 требовал назвать цену требования ДВУХ различающих токенов
ЧИСЛОМ и прямо запрещал вводить порог до этого. Кандидат прочитан двумя способами,
потому что заказ поставил его под заголовком «третий свидетель», а спросил словами
фильтра, — и это разные меры: сужение W1 убавляет НАСЕЛЕНИЕ, свидетель W4 убавляет
`UNDECIDED`.

Ответ: сужение теряет **4 настоящих из 4**, не снимает **ни одной** из 5 ложных и не
решает **ни одного** из 50 `UNDECIDED`; доля ложных 8 % → 71 %. В валюте книг — **3
пары из 4**, включая обе настоящие вторые копии `RiskPolicy.min_cash_pct`. Контроль
(147 срабатываний) уничтожается целиком, потому что все они одно-токенные, — значит
и право говорить о точности исчезает вместе с ним.

**Интервал при сужении становится УЖЕ — и это ловушка.** Записано отдельной слепотой
в самом приборе: узость берётся из убыли знаменателя, а не из решённых `UNDECIDED`.
Сторож, у которого «точность выросла» значит «предмет кончился», отвечает не на тот
вопрос — тот же класс, что «зелёный ответ на СВОЙ вопрос не есть ответ на нужный».

**Третий свидетель.** Кандидат им не является: у всех 23 имён, встреченных в двух и
более разных абзацах, его вердикт один и тот же — он читает ИМЯ. Форма искомого
свидетеля теперь названа (обязан читать АБЗАЦ) и ушла заказом G63 вместе с двумя
вопросами о том, не является ли одно-токенность контроля свойством ВЫБОРКИ.

**Инв. #16:** ни один тест не ослаблен. Две правки тестов этого цикла — УСИЛЕНИЯ, и
обе вызваны батареей: (1) пять отсутствий давали три причины, тест требовал лишь
различности причин — теперь причины разведены в коде и тест требует названных слов;
(2) ветка `isinstance(depth, bool)` снята как ДОКАЗАННО редундантная (`bool` есть
подкласс `int`, оба его значения ниже порога), и вместо неё стои́т подтест на оба
значения. Живые числа от снятия не сдвинулись ни на единицу.

**Приёмка:** 37 тестов · 37 из 37 красны на чистом origin `de7e1790e` · батарея
мутаций 28/28 убито, выживших нет, дерево сверено по sha после каждой · 2 мутации
проводки и 2 мутации отчёта в их числе · соседи 294 passed · храповики 76 passed ·
pyflakes чист.

## Автономный цикл оркестратора #660 (21.09) — G63 п. 1 приказа «Portfolio CIO»: третий свидетель абзаца существует (ADR-441)

**Шаг 0a-ГОЛОД.** Голодающий приказ «Portfolio CIO»
(`inbox-task-portfolio-cio-dynamic-capital-alloc`), 732,7 ч, 440 циклов мимо — взят
ПЕРВЫМ. Предмет задан хвостом ADR-440: заказ G63 п. 1.

**Занятость ИЗМЕРЕНА, а не предположена.** Журнал заявок прочитан до первой правки:
последняя запись — `cycle-659` (11:43Z), её предмет G62 п. 1 уже на origin коммитом
`4b1a82cd7`. Живой `pytest` pid 87004, cwd `/private/tmp/spa_c659_full` — это
предписанный прогон #659, ЧУЖОЙ контроль. Не тронут; своего полного прогона рядом не
запускал, пока он идёт (два полных прогона морят друг друга ядрами и фабрикуют падения).

### Ответ заказу

Кандидаты объявлены ДО разметки константой `WITNESS_RULE`, и под ту же мерку поставлены
ДЕЙСТВУЮЩИЕ свидетели W2 и W3 — это прямое требование заказа («кандидат без измеренной
частоты ошибки в правило не входит: W2 сегодня ровно таков»).

| кандидат | решает `UNDECIDED` | ошибка на контроле |
|---|---|---|
| `adr_cited` | **33 из 50 (66 %)**, 5 абзацев | **11 из 147 (7,5 %)**, 2 абзаца |
| `unit_named` | 29 из 50 (58 %) | 36 из 147 (24,5 %) |
| `executor_dir_named` | 24 из 50 (48 %) | 27 из 147 (18,4 %) |
| **W2** (действующий) | 0 по построению | **0 из 147** при охвате 1 из 201 |
| **W3** (действующий) | 0 по построению | **11 из 63 (17,5 %)**, не спрошен на 112 из 201 |

**Третий свидетель искомой формы СУЩЕСТВУЕТ — и он точнее того, что в правиле уже
стои́т.** Знаменатели у двух частот разные (147 против 63), и это названо, а не
усреднено.

**Находка, которой заказ не называл: критерий ADR-440 необходим, но НЕ достаточен.**
Действующий W2 получает ровно тот вердикт (`VERDICT_FROM_NAME_ALONE`), которым ADR-440
приговорил кандидата двух токенов. Дословно это значило бы «W2 читает имя» — и это ложь
по построению: имени W2 на вход не получает. Причина измерена: он срабатывает 1 раз из
201. Постоянный вердикт и отсутствие различительной силы выглядят одинаково; различает
их ОХВАТ, и он теперь печатается рядом с вердиктом.

**Порог НЕ введён** (`applied: false`), ни один вердикт переписи не изменён: допуск в
правило решает ИЗМЕРЕННОСТЬ частоты, а не её величина — порог на величину был бы выбран
после того, как его исход увиден (запрет G62).

**Сняты две вторые копии** — предмет самой переписи (ADR-417): разрешение координаты
абзаца (`paragraph_at`) и проверка независимости (`independence_verdict`). Поведение
соседних координат сохранено ПОБУКВЕННО и замерено: старая копия с `origin/main` и новая,
запущенные на ОДНОМ дереве, дали побайтово одинаковые `bilingual_name_precision` и
`token_depth_price`.

**Инв. #16:** ни один тест не ослаблен. Единственная правка набора — УСИЛЕНИЕ, вызванное
батареей: «не спросили» в знаменателе частоты ошибки не меняло ни одного вердикта, а у
действующего W3 таких строк 84 из 147 — дыра была не умозрительной. Одна мутация первого
захода не применилась (координата встречалась дважды) и была ПЕРЕМЕРЕНА с контекстом, а
не зачтена неприменимой.

**Шаг 1в (ADR-130):** аудит протокол-слепоты дал код 1 (не мерили 30,5 ч при такте 30 ч);
обновлённый `spa_core/analytics/_protocol_blindness.py` уехал ЭТИМ же пушем.
**Шаг 1д (ADR-350):** код 1, расхождения названы и они прежние — дыра в записи T008
(2026-08-23, −$14 999,88) и переименование ключа T034 (`fluid_usdc` → `fluid_fusdc`);
новых расхождений нет. **Шаг 1е:** перепись капитала — обе оси 100 %, второй артефакт не
спорит; витрина чисел сайта — внутри недельного такта, ничего не переписано.

**Приёмка:** 59 тестов · 59 из 59 красны на чистом origin `4b1a82cd7` (падают на
импорте — контроль доказывает НОВИЗНУ, не поведение, и это сказано вслух) · батарея
мутаций **36/36 убито**, выживших нет, дерево сверено по sha после каждой, батарея
гонялась в одноразовом дереве · соседи 333 passed · храповики 120 passed · pyflakes
чист · бомб якоря времени нет.

### Вторая находка цикла #660: CI на `main` НИЧЕГО не мерил

Проверка «тесты зелёные до пуша» упёрлась в то, что предписанный прогон соседа (#659,
pid 87004) шёл рядом, а два полных прогона морят друг друга. Вместо своего второго
прогона я прочёл CI — и он оказался не инструментом, а молчанием.

**Замер.** Job `test (3.11)` воркфлоу «SPA Tests» умирает на СБОРЕ: 8 ошибок в
`tests/cartographer/*`, ни один тест не запускается. Причина одна и точная —
`scripts/cartographer/portal_render.py:1561`, f-строка с переносом внутри `{...}`
(PEP 701, валидно только с Python 3.12): `SyntaxError: unterminated string literal`.
`fail-fast` снимает job 3.12 на 16 %. **Красная галочка означала «не мерили», а не
«тесты упали»** — тот же класс, что «зелёная галочка означала „не проверяли“»
(owner-decision `...-ci-molcha-ne-zapustilsya-na-pr-zelenaya`), только с другой стороны.
Состояние воспроизведено на ДВУХ коммитах подряд (`4b1a82cd7` и `4f0e91889`), то есть
это не случайность моего пуша.

**Население класса измерено, а не предположено:** компиляция всего дерева
интерпретатором `/usr/bin/python3` (3.9.6) — **4536 файлов собираются, отказ ровно
один**, и прочих отказов ноль (у меры нет примесей). Отдельно проверено, что
`ast.parse(..., feature_version=(3, 11))` этот класс **НЕ ловит** ни в одной из двух
форм PEP 701, — сторож на нём был бы украшением.

**Починено одной строкой:** выражение поднято из f-строки в локальную переменную.
Правка доказанно безвредна: вывод `_investments` сверен со СТАРОЙ копией (из `HEAD`)
на пяти сценах, покрывающих все формы поля (`None` · не словарь · пустой словарь ·
пустой `detail` · непустой `detail`), — расхождений 0, включая совпадение ИСКЛЮЧЕНИЯ
на сцене «не словарь». После правки на 3.9 собирается 4537 файлов, отказов 0; тесты
картографа 827 passed, 3 skipped.

**Сторож НЕ написан — и это решение, а не задел.** Класс требует своей меры с
положительным контролем и своего храповика; заведена карточка
`inbox-ci-na-main-nichego-ne-meril-job-python-3` с замером внутри. Довешивать
проверку к однострочной починке значило бы получить украшение вместо сторожа.

## Цикл #661 (2026-09-21) — заказ G64 п. 1: цена допуска свидетеля, в валюте пар (ADR-442)

**Что взято и почему.** Голодающий приказ владельца «Portfolio CIO» (734,9 ч, 441 цикл мимо)
взят первым, как велит шаг 0a-ГОЛОД. Журнал заявок прочитан ДО работы: последняя запись —
`cycle-660` (13:40Z), её предмет G63 п. 1 уже на origin (`310ad604b`), держателя G64 нет.
Рядом живы два чужих полных `pytest` (pid 48786, 69328) и чужая сессия-исполнитель по другой
карточке — не тронуты; своего полного прогона рядом не запускал (два полных прогона морят
друг друга ядрами).

**Обязательные шаги цикла.** 0-офис из ПРОД-дерева — прочитано 97, не прочитано 0, код 0;
красные строки в нём принадлежат уже заведённым заказам (G15/G19/G44/G45/G46/§45/§46) и
новых карточек не требуют. Аудит аналитики — код 0 (мерили 2,13 ч назад при такте 30 ч).
Вторая запись о деньгах (`--data-dir` прода) — код 1, ОБА расхождения известные и
поимённо перечисленные: дыра T008 23.08 (−$14 999,88, ADR-350) и переименование T034
`fluid_usdc→fluid_fusdc` (ADR-335). Новой дыры нет. Перепись наблюдённости капитала —
обе оси **100 %** ($95 000 в 5 протоколах), второй артефакт не спорит, код 0. Витрина
чисел сайта `--if-due` — внутри недели (1 день из 7), ничего не переписано, код 0.
Якорь времени на импорте — бомб нет.

**Работа.** Новая координата `witness_admission_price` отвечает заказу G64 п. 1: цена
допуска кандидата считается в ПАРАХ (в том, что канал доносит до книг), а не в
срабатываниях разметки. Числа — в ADR-442 и в карточке; порог НЕ введён (`applied: false`).

**Главное, что стои́т запомнить.** Заказ спрашивал «сколько пар прибавляет правило
`GENUINE`», и в самой формулировке молча принято, что пары книг и метка `GENUINE` — одно
множество. Их 4 и 4, а пересечение — **3**. Если бы координата считала прибыток от числа,
не спросив множества, она выдала бы верное число при неверном основании. Это ровно класс
ADR-418 («равное число называет разные предметы»), найденный у САМОЙ базы измерения.

**Своя ошибка, найденная и названная.** Первый прогон батареи мутаций объявил выжившими
семь мутаций; две из них на руках убивались. Причина — не тест: батарея удаляла каталоги
`__pycache__` ВО ВРЕМЯ обхода `rglob`, обходчик терял часть, и мутация того же размера не
доезжала до прогона. Исправлено (перечень материализуется до удаления, байткод не пишется
вовсе); после починки — 7 настоящих дыр, все закрыты УСИЛЕНИЕМ тестов, итог **38 из 38
убито, выживших нет**. Ни одна мутация не ослаблена.

**Чужое красное, воспроизведённое и НЕ чинимое здесь.** `test_python_git_ref_provenance.py::
TestRatchet::test_real_tree_matches_the_frozen_baseline` падает и на моём дереве, и на
ЧИСТОМ `origin/main` `310ad604b` тем же прогоном: предмет вырос на 6 —
`scripts/cartographer/snapshot.py:176,192,193,195` и `scripts/check_owner_gate.py:242,248`.
Это чужая доставка и уже заведённая карточка
(`inbox-predpisannyi-progon-daet-39-padenii-prot`), новой не завожу — дубль в очереди
топит настоящее.

**Тесты.** 39 новых (все 39 красны на чистом origin — контроль на НОВИЗНУ, не на поведение,
и это сказано вслух); соседи по оси 473 passed; потребители переписи и `scripts/tests/`
462 passed; храповики 474 passed, 11 skipped при единственном чужом падении выше.
`pyflakes` чист. Полный предписанный прогон рядом с двумя чужими не запускался намеренно.

## Цикл #662 (21.09) — заказ G65 п. 1: каждый читатель переписи берёт СВОЮ базу, а одно слово называет две (ADR-443)

**Как взят.** Голодающий приказ «Portfolio CIO» (736,8 ч, 442 цикла мимо) — первым, до
разбора очереди. Журнал заявок прочитан ДО работы: последняя запись по карточке —
`cycle-661-pid86141` с `card_state: done`, её предмет G64 п. 1 уже на origin (`30e565548`);
процесс 86141 мёртв (проверено `ps`, не «похоже, что»), карточка свободна. Рядом с моим
деревом чужих полных прогонов не было; своё — worktree на `origin/main` `5f81f1d8f`.

**Что спрашивал заказ и что вышло.** ADR-442 измерил расхождение у самой базы (пары книг ≠
`GENUINE`) и сказал, чего не спрашивал: КТО из читателей какую базу берёт. Спрошены все
трое поимённо — и **ни один не берёт ту пару баз, о которой спрашивал заказ**. Секция
отчёта читает все ТРИ (9 чисел: 7 названо верно, 2 многозначно, 1 без имени). Мост находок
берёт своё ТРЕТЬЕ — `counts.two_copies`, население самой переписи, — и это единственное
число, которое он печатает вообще. Шаг 0-офис не берёт НИКАКОЙ: он ввозит `format_report`
производителя, и своей базы у него нет по построению.

**Находка, которой заказ не называл.** Токен «пар» стои́т вплотную к числам ДВУХ
непересекающихся населений в одном и том же отчёте: `two_copies` (пары «сторож ×
исполнитель», стр. 6017) и `channel.pairs` (пары канала имени, стр. 6825). Различить их
этим словом читатель не может ничем. Это вторая копия правила снаружи прибора — копия не
кода, а СЛОВАРЯ. И доезжает она до оркестратора именно через ДЕЛЕГАТА: у шага 0-офис своей
базы нет, поэтому он несёт чужое многозначное имя дальше без изменений.

**Где проходит граница замера и объявления — сказано вслух.** Объявлены координаты баз,
токены и реестр читателей. ИЗМЕРЕНА смежность: токен, вставший рядом с двумя базами,
многозначен по замеру. Бралась бы многозначность из объявления — «пар» краснел бы всегда, и
мера отвечала бы на свой вопрос вместо нужного; сторож держит обе стороны.

**Три ловушки, обойдённые заранее.** (1) Координатой значения считается ПОСЛЕДНЯЯ
применённая: объединение ключей цепи приписало бы базе книг каждое поле каждой пары — форма
«контейнер привязкой не является» из правила доставки. (2) Чужое чтение в замер не попадает:
у моста соседняя перепись печатает свой `pairs`, и без вопроса о происхождении прибор
выдумал бы находку ровно того класса, который ищет. (3) Имя — текст в окне 40 символов
вплотную к числу: брать прогон целиком значило бы объявить многозначными все семь чисел
строки разом.

**Батарея мутаций нашла шесть дыр, и все шесть закрыты УСИЛЕНИЕМ тестов.** Первый заход:
28 убито из 34. Выжившие и что они означали: окно имени, расширенное вдесятеро, не краснило
НИ ОДИН тест — мой тест мерил окно через саму константу, то есть судил по собственной мерке
(закрыто сценой с АБСОЛЮТНЫМ расстоянием 120 символов); порядок «делегирование важнее
чтения» не проверялся, потому что ни одна сцена не читала И не делегировала одновременно;
приём многобазовой координаты был непроверяем при нынешнем реестре (закрыто подменой
реестра на время замера — иначе ветка осталась бы сторожем без зубов); неподвижная точка
не проверялась цепью, записанной обратно порядку обхода; многозначность-из-объявления не
отличалась от многозначности-из-замера; порядок проверок вердикта (AMBIGUOUS раньше CROSS)
ничем не держался. Второй заход — **34 из 34 убито, выживших нет**. Ни одна мутация не
ослаблена, дерево восстановлено и сверено по sha после каждой.

**Обязательные приборы цикла.** Шаг 0-офис: прочитано 97, не прочитано 0.
`cycle_analytics_audit` — код 0 (аудит мерили 4,1 ч назад при такте 30 ч).
`capital_observability_census` (приёмка G1): **обе оси 100 %** ($95 000 из $95 000), второй
артефакт не спорит. `build_site_numbers --if-due` — внутри недели, публикация не назначена
(1 день из 7). `book_second_record` — **код 1, расхождения НАЗВАНЫ**: дыра в записи T008
(23.08, $94 999,88 → $80 000,00) и переименование ключа T034 (`fluid_usdc` → `fluid_fusdc`).
Оба уже карточные (`inbox-dyra-v-zapisi-o-dengah-23-08-kniga-poter`,
`inbox-strazh-perezapisi-schitaet-pereimenovani`) — новых не завожу, дубль в очереди топит
настоящее.

**Тесты.** 45 новых, все красны на чистом `origin/main` `5f81f1d8f` — падают на СБОРКЕ,
то есть контроль доказывает НОВИЗНУ, а не поведение, и это сказано вслух. `pyflakes` чист.

**Чужое красное, перемеренное на ЧИСТОМ дереве и НЕ чинимое здесь.**
`test_python_git_ref_provenance.py::TestRatchet::test_real_tree_matches_the_frozen_baseline`
падает и у меня, и на чистом `origin/main` `5f81f1d8f` (контрольный worktree, рабочее
дерево пусто по `git status`) — предмет вырос на 6: `scripts/cartographer/snapshot.py:176,
192, 193, 195` и `scripts/check_owner_gate.py:242, 248`. Тот же текст, что у цикла #661 на
`310ad604b`, то есть доставка чужая и живёт дальше. Карточка уже есть
(`inbox-predpisannyi-progon-daet-39-padenii-prot`); второй не завожу.

**Прогоны.** Соседи по оси переписи — 680 passed. Потребители и `scripts/tests/` — 447
passed. Храповики и провенанс — 690 passed, 10 skipped при единственном чужом падении выше.
Полный предписанный прогон здесь не запускался: он идёт 113 тыс. тестов и рядом с чужими
сессиями морит их ядрами; вместо него взяты четыре набора, покрывающие тронутое.

## Автономный цикл оркестратора #665 (22.09) — G67 п. 1 приказа «Portfolio CIO»: класс невидимого потребителя померен у ВСЕХ 85 переписей (ADR-445)

**Шаг 0a-ГОЛОД.** Голодающий приказ «Portfolio CIO»
(`inbox-task-portfolio-cio-dynamic-capital-alloc`), 745.7 ч, 445 циклов мимо — взят ПЕРВЫМ.
Предмет задан хвостом ADR-444 (блок «Заказ G67»), а не очередью.

**Занятость ИЗМЕРЕНА, а не предположена.** Шаг 0b из worktree дал `❓ НЕ ИЗМЕРЕНО`
(53 захвата, известная слепота к меткам без pid) — разобрано руками: ПОЗДНЕЙШЕЕ
объявление по карточке — `cycle-664-pid66315`, `card_state=done` (22.09 00:38Z), то есть
захват снят по построению; `ps -p 66315` и `ps -p 5433` пусты, процессов, связанных с
`/tmp/spa_c66*`, нет. Живой полный прогон в системе ОДИН и он ЧУЖОЙ — pid 20654, cwd
`/Users/yuriikulieshov/studio-os-scratch/final-candidate`, к нашему дереву отношения не
имеет (проверено `lsof -d cwd`, а не по имени команды).

**Обязательные приборы цикла.** Шаг 0-офис из ПРОД-дерева — прочитано 97, не прочитано 0 ·
`cycle_analytics_audit` — код 0, FRESH (12.9 ч при такте 30 ч) · `capital_observability_census`
(прод-`data/`) — обе оси 100 %, второй артефакт не спорит · `build_site_numbers --if-due` —
внутри недели (2 дн из 7), ничего не переписано · `import_time_anchor_bombs` — код 0,
бомб нет · `book_second_record` (прод-`data/`) — код 0, расхождения СТОЯЧИЕ и названы:
дыра записи T008 (2026-08-23, $-14 999,88) и переименование ключа T034
(`fluid_usdc`→`fluid_fusdc`). Owner-done — 0, промоушенов — 0.

### Ответ заказу

Переписей 85 · отрисовщик разрешён 57 · **население зовущих ЗАНИЖЕНО у 43** (75.4 %
разрешённых) · мест 50 (кода 42, тестов 8) · **шаг 0-офис невидим соседу у 42 переписей**.
Класс, найденный ADR-444 у одной переписи, не аномалия, а норма — и почти весь он есть
один файл: тот самый единственный читатель, который доносит числа переписей до
оркестратора.

Соседний прибор не сломан. Его ноль означает «зовущих нет», а читается как «потребителей
нет», и на 42 переписях второе ложно.

### Три решения, каждое ради того, чтобы число не соврало

1. **Отрисовщик ИЗМЕРЕН, а не взят словарём.** ADR-444 объявил `("report",
   "format_report")` для ОДНОГО производителя; перенести словарь на 85 значило бы завести
   вторую копию правила в СЛОВАРЕ — класс, который эта же перепись ищет у других. Правило
   спрашивает у самого модуля: отрисовщик — функция, чей результат он САМ печатает. Имена
   вывелись сами: `format_report` 42 · `report` 12 · `_lines` 2 · `render` 1, и **словарь
   ADR-444 не назвал бы три переписи** (`apy_composition`, `capital_evidence_coverage`,
   `owner_answer_delivery`).
2. **Третий исход РАЗДЕЛЁН на два рода.** «У модуля нет ни одной текстовой функции» (11 —
   печатает встроенно, отдавать нечего, класс не применим ПО ПОСТРОЕНИЮ) против «текстовые
   функции есть, печать их не решает» (17 — граница нашего правила). Слить их значило бы
   выдать первое за второе.
3. **Цена границы названа ЧИСЛОМ, а не оговоркой.** Из 17 отдают текстовую функцию
   потребителю без зова `run` — **4**, среди них `rate_observation_census` (его
   `format_report` ввозит тот же шаг 0-офис). Поэтому ответ есть **интервал [43, 47]**, а
   не точное 43.

**Обратная сторона измерена и НЕ в нашу пользу:** занижено ДО НУЛЯ — **0**. У каждой из 43
сосед видит хотя бы одного зовущего. Класс есть занижение населения, а не его обнуление;
сказать «43 переписи невидимы» было бы преувеличением на порядок тяжести, и ноль стои́т в
отчёте рядом с 43 именно поэтому.

### Приёмка

35 тестов, у каждого обратная сторона. Контроль на новизну переснят ФИНАЛЬНОЙ редакцией
(sha `9cb7b7ce`, урок #663) на пине `89d73c083`: все 35 умирают на СБОРКЕ — это контроль
про НОВИЗНУ, а не про поведение, и сказано вслух. Поведение доказывает батарея: **26 из 26
убито**, выживших нет, дерево восстановлено и сверено по sha после КАЖДОЙ мутации; среди
них две мутации ПРОВОДКИ и четыре по отчёту.

**Три дыры первого захода батареи закрыты УСИЛЕНИЕМ тестов, ни одна мутация не ослаблена**
(инв. #16): (1) печатаемая ВВЕЗЁННАЯ функция объявлялась нашим отрисовщиком — без проверки
«имя объявлено ЗДЕСЬ» потребитель чужой переписи стал бы нашей находкой; (2) форма
`import a.b.c as x` не проверялась вовсе — правило знало две формы ввоза из трёх, то есть
повторяло слепоту соседа на соседней оси; (3) тест «перепись себе не потребитель» СЦЕНЫ НЕ
СТРОИЛ — файл себя не ввозил, и снятие защиты не меняло ничего (украшение, а не контроль).

Проводка `measure()` меряется ФОРМОЙ ЗОВА, а не наличием имени: полный `measure` на живом
дереве идёт 80 с, а на одноразовом стенде отказывает по предпосылкам СОСЕДНИХ частей
(`NotMeasured` от `_guard_files`, затем от порогов RiskPolicy) — то есть тест мерил бы не
своё. Секция отчёта при этом читается на НАСТОЯЩЕМ замере стенда, не на выдуманном
документе.

Соседи и храповики — 188 зелёных (`consumer_registry_completeness`, `consumer_base_naming`,
`census_stage_root_contract`, `absent_observation`, `frozen_date`, `injected_clock`,
`state_md_length`, `inbox_acceptance`, `prescribed_run_matches_ci`).

**Заказ G68** — в хвосте ADR-445: (1) занижение измерено по ПОВЕРХНОСТИ, а вред — нет:
меняет ли оно хоть один вывод, который на населении зовущих кто-нибудь строит;
(2) остаток границы правила печати — 13 переписей из 17, про которые не сказано ничего,
кроме имён кандидатов; (3) цена самой починки у соседа, спрошенная ДО правки, а не после.

## R&D-итерация novel-edge-rnd (22.09) — заказ #110 исполнен ЦЕЛИКОМ: барьер реактивного переключателя не переносится на реальную панель, он ЗАМЕНЯЕТСЯ пошлиной (записи реестра #111, #112)

**Что делалось.** Одна автономная итерация стандинг-директивы «novel-edge R&D». Реестр читался
ИЗ ORIGIN (`git show origin/main:docs/DYNAMIC_LEVERAGE_GUARDIAN.md`) — рабочая копия прод-дерева
отстаёт и держит НЕДОСТАВЛЕННЫЕ записи другой сессии под номерами #108/#109 (CDMS-REAL/CDMS-ASYM),
о чём origin-реестр сам предупреждает в «ЗАМЕЧАНИИ О НУМЕРАЦИИ» внутри #109. Свободные номера на
момент доставки — #111 и #112.

**Гипотезы (обе — исполнение чужих заказов, а не свободное изобретение).**

1. **#111 BISS-REAL** — заказ #110 п. 1: перемерить ORACLE-потолок бинарного переключателя
   INVEST/DEFEND на РЕАЛЬНОЙ панели (`data/aggressive_lab`, 10 книг × 852 дня, только блок
   `phase="backtest"`). `scripts/edge_biss_real_panel.py`.
2. **#112 XLS** — заказ #110 п. 2 И заказ #19 (EWVM), лежавший с 20.07 с пометкой **DATA-BLOCKED**:
   существует ли НЕ-реактивный (ведущий) сигнал по внешнему фиду. Данные нашлись и выровнены:
   `funding_deep.json` 755 дней + `prices_deep.json`. `scripts/edge_exogenous_leading_switch.py`.

**Вердикты — ОБА ОТРИЦАТЕЛЬНЫЕ, и в этом главная ценность итерации.**

- **Критерий приёмки #110 неизмерим в том виде, в каком он записан.** «Доля от ORACLE-потолка > 50 %»
  требует названного оракула; на реальной панели списка окон нет, поэтому потолок приходится
  выводить, и одно и то же число пробегает **от +2.7 % до +246 %** по четырём выведенным оракулам.
  ORACLE-WIN вообще не потолок: причинный 1DAY обгоняет его на 146 %. Второй половине критерия
  («≤ 10 % ложных в штиль») не назван ЗНАМЕНАТЕЛЬ, а их два, и они расходятся на порядок —
  #19 объявляла бюджет на одном, #110 на другом.
- **«73.5 % улучшения неизвлекаемо из-за дня 1» — свойство фикстуры.** На реальных окнах доля
  дня 1 = 1.5 %…81.1 % и является функцией ДЛИНЫ окна (121-дневное снижение отдаёт в первый день
  1.5 %). Латентность на реальной панели барьером НЕ является.
- **Барьер заменяется двумя жёсткими.** (1) Наряд: 38.7 % дней EW отрицательны, поэтому 1DAY сидит
  в защите 97.9 % дней и отдаёт защите 97.4 % штилевых — против ≤ 10 % критерия. (2) **Пошлина:
  при канонических 96 bp даже ORACLE-DAY (идеальное дневное предвидение, 440 переключений) даёт
  Calmar −0.568 против 3.295 у «ничего не делать».** Потолок класса опускается НИЖЕ нуля, и прибор
  печатает «NOT MEASURED — oracle does not beat EW», а не ноль. Бюджет: ORACLE-DAY **23.8 bp**,
  1DAY 22.3 bp, ORACLE-WIN2 **314.1 bp** — вся разница в СЧЁТЧИКЕ переключений (440 против 12).
- **Ведущий экзогенный сигнал существует, но экономически пуст.** Реактивный 1DAY информативен про
  онсет в **0 из 4** окон (`ALREADY-IN` — он просто не выходит из защиты), funding-z — в **4 из 4**
  с медианой **+5 дней**. Но: (а) сдвинутая по оси копия того же сигнала даёт 3/4 с медианой +4 ⇒
  **опережение не отличимо от совпадения на четырёх окнах** (предел выборки, назван вслух);
  (б) бюджет funding-правил **0.0–1.0 bp** — независимое подтверждение измеренных #22 **1.1 bp**
  на ДРУГОМ механизме (там гейт одной ноги, здесь переключатель всего рукава). Лучшая по TRAIN
  экзогенная клетка — та, которая **не срабатывает ни разу**.
- `ETHVOL1.5` проходит критерий #110 по ложным срабатываниям (fp|calm 7.2 % ≤ 10 %) и всё равно
  проигрывает нулю почти вдвое: критерий по FP необходим и НЕ достаточен.

**Три собственные ошибки, найденные до публикации и записанные в реестр как часть результата.**

1. **Прибор опережения мерил НАРЯД и называл это ОПЕРЕЖЕНИЕМ.** Первая версия искала первый день
   полосы, в который сигнал НАХОДИЛСЯ в защите; для 1DAY с нарядом 97.7 % это левый край полосы
   для каждого окна, и прибор печатал «+10 дней опережения» правилу, которое никогда не выходит из
   защиты. Исправлено переходом INVEST→DEFEND и третьим исходом `ALREADY-IN`; колонка наряда теперь
   стоит прямо в таблице опережения, чтобы подмена была видна глазами.
2. **Контроль механизма был бы ИНЕРТЕН в очевидной форме.** Положительный контроль «поверни ручку
   `n_recover` и клетка обязана сдвинуться» не работает через `B111.N_RECOVER = N + 7`: дефолт
   параметра привязан в момент определения функции, глобальная подмена до него не доходит, и
   контроль остался бы зелёным, ВЫГЛЯДЯ поворотом ручки. Ручка повёрнута НА ВЫЗОВЕ; в тесте это
   записано для следующего читателя.
3. **Правило окна датировало снижение с первого ПЛОСКОГО дня.** Бегущий максимум обновлялся по `>`,
   поэтому на точно плоском отрезке он не двигался. Найдено тестом на плоской сцене, исправлено на
   `>=`; на реальной панели состав окон от правки не изменился (точных равенств на максимуме нет) —
   то есть правка чинит класс, а не подгоняет числа.

**Приборы.** Оба скрипта — advisory / OUTSIDE_RISKPOLICY, stdlib-only, LLM_FORBIDDEN, читают панель
и фиды READ-ONLY, ничего не сайзят и не гейтят. Симулятор #111 прогоняется на фикстуре #110 и
воспроизводит ВСЕ семь опубликованных клеток; расхождение = ОТКАЗ, не предупреждение. У каждого
отказа названа причина (инв. #17): «потолок бесконечен» / «оракул не обгоняет ноль» / «правило не
срабатывает ни разу» — три разных исхода, ни один не печатается нулём.

**Приёмка.** `spa_core/tests/test_edge_biss_real.py` — **32 теста, все зелёные**; каждый —
положительный контроль на что-то, что было неверно хотя бы один раз (включая контроль самого
контроля). `spa_core/tests/test_swarm_*.py` — 104 зелёных. Сторожа, реагирующие на ДОБАВЛЕНИЕ
файлов (номера реестра, frozen-date, injected-clock, absent-observation, unwired-scripts,
unused-import, prescribed-run) прогнаны отдельно на чистом дереве origin.

**Красное, что НЕ моё и намеренно не тронуто (инв. #16).** В прод-дереве
`test_frozen_date_ratchet.py` краснит на `test_cdms_real_panel.py` — файл недоставленной работы
другой сессии, лежащий только в рабочей копии. На origin его нет, и на чистом дереве сторож зелён.
Чужую фикстуру не правил. Также на момент доставки в системе шли ДВА полных предписанных прогона
других сессий (pid 20654, 37614) — третий starve'ил бы все три, поэтому вместо него прогнана
адресная группа сторожей, и это сказано здесь прямо, а не выдано за полный прогон.

**Чего НЕ делалось и почему.** Paper-модуль в рой НЕ строился и агент НЕ заводился: оба вердикта
отрицательные, строить нечего — по директиве модуль строится только при устойчивом OOS-позитиве.
Капитал не двигался, `data/equity_curve_daily.json` и RiskPolicy v1.0 не тронуты, живой
`cycle_runner` не запускался. Карточка владельцу не заводилась: ни один из трёх предметов ADR-285
не задет (денег нет, публичных чисел нет, необратимого нет).

**Заказы, оставленные следующей записи.** (1) «Какая пошлина у НАС на самом деле при $100k» —
ветка #91 (AST); весь класс мерился в Calmar, а связывает его СЧЁТЧИК переключений. (2) Существует
ли ПРИЧИННЫЙ детектор окна с ≤ 20 переключениями за два года и какой у него recall. (3) ORACLE-DAY
при 0 bp даёт Calmar 22.3 против 3.3 — информации в панели много, но извлекается она не
выключателем; мерить сайзером (ветка #73/#74 ACL). (4) Население окон — четыре; любое утверждение
об опережении на нём недоказуемо, нужна ось по книгам ОТДЕЛЬНО (десятки эпизодов) — и это другой
вопрос: опережение ЧЬЕГО онсета.

---

## Цикл #666 (2026-09-22) — заказ G68 п. 1: занижение повсеместно, а у ИСХОДА доходит до двух тестов (ADR-446)

**Взято первым — голодающий приказ владельца** «Portfolio CIO» (747.3 ч в `in-progress`,
448 циклов мимо). Занятость ИЗМЕРЕНА, а не принята на веру: позднейшее объявление по карточке —
`cycle-25582` (02:16:44Z) с `card_state=done`, захват снят по построению; из 53 захватов в журнале
все осиротели либо стары. Два живых полных предписанных прогона в системе — ЧУЖИЕ: cwd
`studio-os-scratch/final-candidate` (pid 20654) и `/tmp/spa_c665` (pid 37614), проверено
`lsof -a -d cwd`, а не по имени команды.

**Обязательные шаги цикла.** Офис: прочитано **97**, НЕ ПРОЧИТАН **0** (из прод-дерева — из
worktree тот же шаг лжёт по построению, `data/` в `.gitignore`). Аудит аналитики — код 0 (мерили
14.6 ч назад при такте 30 ч, прогон не нужен). Вторая запись о деньгах — код 1 с
`--data-dir` **прод-дерева**: `ДЫРА T008 −$14 999.88` и `ПЕРЕИМЕНОВАНИЕ T034
fluid_usdc→fluid_fusdc`; оба названы поимённо в ADR-350/ADR-335, новых дыр нет. Из worktree
умолчание даёт 7 ходов вместо 34 и «конец не сошёлся» — не находка, а замороженный канон.
Перепись наблюдённости капитала — **100 % по обеим осям** ($95 000 в 5 протоколах), второй
артефакт не спорит. Витрина чисел сайта — публикация не назначена (2 дн. из 7). Бомб импортного
якоря нет (код 0).

**Что измерено по заказу.** Полей у соседа `census_consumer_census` — **116**; от расширения
населения на 52 измеренных места ADR-445 сдвинулось **4**: `consumers.by_name`,
`counts.consumers_by_name_calls` (80 → 131), `counts.consumers_by_name_files` (3 → 13) и текст
`findings`. **Полей вердикта объявлено до замера 5, сдвинулось 0** — то есть `overall` и
`counts.critical/warn/unchecked`, ровно то, что мост печатает оркестратору, остаются
тождественными, и решение читателя вердикта измениться НЕ МОЖЕТ. Утверждение получено ДВУМЯ
прогонами соседа, а не рассуждением.

Читателей — **6** по пяти дорогам; чтений **44**: решают 25 · печатают 12 · НЕ РАЗОБРАНО 7.
Решают по сдвинувшемуся полю **2**, и оба — канал теста (`test_census_consumer_census.py`,
`test_invisible_consumer_scale.py`); до оркестратора печать теста не доезжает. Единственный
читатель, чьё решение прибор способен ПЕРЕСЧИТАТЬ, — `consumer_registry_completeness` — дал ТО
ЖЕ решение. Итог: занижение повсеместно (43 из 57 переписей) и при этом у исхода почти
безвредно; оба утверждения верны разом.

**Прибор нашёл СВОЮ слепоту того же класса, и это правка, а не оговорка.** Первый заход искал
читателей четырьмя дорогами и доложил 4 — без `rule_second_copy_census.py`, то есть без модуля,
внутри которого живут оба читателя, чьё решение и есть предмет заказа: он достаёт соседа
`importlib.import_module(NEIGHBOUR_CENSUS)` и ни одной формой `import` его не ввозит. Добавлена
пятая дорога `ROAD_DYNAMIC`. Ещё две дыры того же захода закрыты УСИЛЕНИЕМ правила: ввоз `as`
переименовывает отрисовщик (сверка шла по имени ЧИТАТЕЛЯ и теряла шаг 0-офис — ту самую
находку ADR-444), и обёртка вокруг чтения считалась вторым чтением (`print(doc["counts"]
["critical"])` ехало в отчёт дважды).

**Приёмка.** 64 теста, у каждого обратная сторона. Контроль новизны переснят ФИНАЛЬНОЙ редакцией
(sha `3905252f2dd3`) на пине `031e645cf`: все умирают на СБОРКЕ — контроль про НОВИЗНУ, а не про
поведение, и сказано вслух. Поведение доказано батареей: **38 мутаций по координате, 38 из 38 убито**, дерево
сверялось по sha после КАЖДОЙ. Первый заход батареи оставил **5 выживших**, и все пять закрыты
УСИЛЕНИЕМ тестов, ни одна мутация не ослаблена (инв. #16):

1. сцена «тело ветки не решает» до `ast.If` не доходила вовсе — разбор обрывался на присваивании;
   заменена на форму, которая доходит, и добавлен контроль самого контроля (цепочка родителей
   обязана содержать `ast.If`);
2. зов ПЕРЕИМЕНОВАННОГО ввезённого искателя не проверялся ни одной сценой;
3. отказ «масштаб не измерен» ловился ВТОРЫМ сторожом, и причина совпадала подстрокой — снятие
   первого проходило незамеченным;
4. не было обратной стороны у ответа: читатель, решающий по НЕсдвинувшемуся полю, в находку
   попадать не должен;
5. счётчик чувствительности к форме не проверялся на записи реестра БЕЗ объявленной области —
   а область и есть весь его смысл.

**Сторож класса поймал МОЙ ЖЕ дефект на доставке.** `test_absent_observation_ratchet` покраснел
на форме `counts = doc.get("counts") or {}` в проекции решения: документ БЕЗ счётчиков становился
неотличим от документа с ПУСТЫМИ, то есть два РАЗНЫХ решения сравнивались как одно (инв. #17).
Починено у ПИСАТЕЛЯ — чтение через `observed(...)` плюс три признака наблюдённости, — а не
дописыванием в базу сторожа (это запрещено прямо); добавлен тест на обе стороны и 38-я мутация
батареи, возвращающая прежнюю форму.

**Слой.** ADVISORY: порог не введён, ни один вердикт переписи не изменился, `POLLED_ADAPTERS`,
пороги RiskPolicy v1.0, стоп-кран, живой трек и `landing/` не тронуты. Карточка владельцу не
заводилась: ни один из трёх предметов ADR-285 не задет.

**Порядок шагов назван явно, а не выдан за спешку.** Доставка идёт ПОСЛЕ адресных прогонов и
своей батареи, но ДО конца полного предписанного прогона — порядок из замера #417 (пять циклов
подряд потеряли работу внутри собственного полного прогона, между «тесты зелёные» и пушем).
На момент пуша в системе шли ДВА чужих полных прогона (pid 20654 — `studio-os-scratch/final-candidate`,
pid 37614 — `/tmp/spa_c665`), и третий ползёт вдвое медленнее. Прогнано до пуша: 64 своих теста ·
соседи `test_invisible_consumer_scale` / `test_consumer_registry_completeness` /
`test_census_consumer_census` / `test_consumer_base_naming` — **141 зелёный** · сторожа,
реагирующие на ДОБАВЛЕНИЕ файлов (frozen-date, injected-clock, absent-observation,
inbox-acceptance, design-doc-status, state-md-length, prescribed-run, site-numbers-shelf,
unused-import) — **130 зелёных после починки инв. #17** · `adr_number check` код 0 ·
`import_time_anchor_bombs` код 0 · батарея 38/38. Полный прогон идёт в ТОМ ЖЕ дереве, но
запущен ПОСЛЕ последней правки и дерево до конца прогона не трогается; его итог дописывается
сюда отдельной строкой.

**Заказ G69** — в хвосте ADR-446 (цена пересчёта каждого читателя · цена предела в одно
связывание · цена починки против измеренного вреда). G68 действует в остатке (пп. 2, 3).

---

## Цикл #667 (2026-09-22) — заказ G69 п. 1: у остатка была цена и был бюджет, и не было ЧАСТНОГО (ADR-447)

Взят первым как голодающий приказ «Portfolio CIO» (749.5 ч, 449 циклов мимо). Шаг 0b дал
**НЕ ИЗМЕРЕНО** (код 2) — известная слепота к меткам без pid: четыре древних идентификатора
(`cycle-74714`, `cycle-491`, `cycle-28734`, `cycle-3512`) возрастом 266–408 ч. Занятость
перемерена руками: свежих захватов в окне 3 ч нет ни одного, последнее объявление сегодня
в 04:15Z снимало захват. Карточка свободна.

### Что спрашивал заказ и что найдено

ADR-446 оставил исход `not_executed` с ценой **35 с** и назвал его остатком. Заказ требовал
померить цену пересчёта КАЖДОГО читателя и сверить с тактом ступени.

**Обе объявленные цены оказались ВЕРНЫ** — 21 против измеренных 19.11 и 35 против 32.32 за
прогон, ошибка под десять процентов. Такт ADR-446 тоже называл прямо («35 с при такте ступени
6 ч»). То есть у остатка были оба числа — и он всё равно стоял.

**Не хватало не числа, а ЧАСТНОГО.** Ни одна строка прибора не делила цену на бюджет, поэтому
«дорого» оставалось суждением сессии, а не величиной, и оспорить его было нечем. Замер 22.09:
полный пересчёт **83.75 с** при такте **21600 с** ⇒ запас **257.9×** при объявленных ДО замера
**10×**.

### Доставлено (автономно, не money-path): ADR-447

`recompute_cost_budget` — координата, у которой цена ИЗМЕРЯЕТСЯ часами вокруг настоящего
пересчёта, а бюджет ЧИТАЕТСЯ у объявителя (`architecture/manifest.json`, расписание агента,
производящего артефакт переписи). Вписать «6 ч» числом значило бы завести вторую копию правила
— ровно тот класс, который эта перепись ищет у других; сторож этого — тест, требующий
отсутствия литерала `21600` в исходнике.

**Остаток закрыт оплатой, и оплата принесла знание:** решение `invisible_consumer_scale` от
расширения населения **не меняется** (43 занижённых переписи, верхняя граница 47, обнулённых 0,
роды находок те же). Прежде на этом месте стояло молчание с ценником. Счётчики координаты вреда:
`readers_not_executed` 1 → **0**, `readers_decision_unchanged` 1 → **2**.

**Оплата не была бы замером, если бы расширение до читателя не доходило.** Положительный
контроль на голом стенде: расширение подставляется соседской `find_by_name_consumers`, а именно
её этот читатель и спрашивает про `seen`; перепись без зовущих `by_name` попадает в
`blanked_to_zero`, расширение её оттуда убирает — решение МЕНЯЕТСЯ. Обратная сторона: то же
место, та же форма, названа ДРУГАЯ перепись — решение стои́т.

### Две своих ошибки, найденные ДО публикации

1. **Сверка претензии шла с ПОЛНОЙ ценой, а не с ценой прогона.** У второго читателя это 64.64 с
   против объявленных 35 — разница 46 %, и она прошла бы под допуском 50 % как «подтверждено».
   Подтверждение было бы удачей: 35 с объявлялись как цена ОДНОГО прогона, а мерились два.
   Переведено на `cost_s_per_run`, обе стороны закреплены тестом (80 с при двух прогонах —
   подтверждена, при одном — опровергнута).
2. **Чистый документ брался по ключу читателя**, и на стенде им оказалась заглушка расширения, а
   не документ. Источник чистого документа стал ОБЪЯВЛЕННЫМ полем спецификации (`clean_from`); у
   двух читателей он разный, отсюда два основания цены (`widened_run_only` против `both_runs`),
   которые нельзя молча складывать.

### Намеренные правки чужих тестов (инв. #16) — обоснование

Два теста `spa_core/tests/test_neighbour_population_harm.py` изменены осознанно, обоснование
продублировано в теле каждого:

1. `test_the_unpaid_reader_is_a_THIRD_outcome_with_a_price` закреплял ОСТАТОК как свойство
   прибора («неоплаченный ровно один»). Остаток оплачен замером ⇒ утверждение перевёрнуто вслед
   за замером, а не ослаблено: рядом ДОБАВЛЕНЫ два теста обратной стороны — неисполнимый
   читатель всё ещё уходит в третий исход с объявленной ценой, и спецификация с неизвестным
   именем проекции тоже.
2. `test_the_verdict_of_the_neighbour_does_not_move` читал `harm["status"] == "MEASURED"` как
   «вердикт соседа не сдвинулся». Это подмена координаты: статус говорит о решении ЧИТАТЕЛЕЙ, и
   со второго оплаченного читателя стал их различать. Проверяется теперь то, о чём тест всегда и
   был, — ОБЪЯВЛЕННЫЕ поля вердикта. По той же причине в
   `test_the_same_widening_elsewhere_leaves_the_decision_alone` общий статус заменён на точную
   проверку: он говорит о ЛЮБОМ читателе, а тест — про одного, и перестал бы различать ровно то,
   ради чего написан.

Ни одно утверждение не снято и не сужено; во всех трёх случаях добавлена обратная сторона.

### Приёмка

**49 новых тестов** (`test_recompute_cost_budget.py`) + **11** в соседнем наборе, у каждого
обратная сторона. **Батарея мутаций по КООРДИНАТЕ: 22 из 22 убито, выживших нет** — среди них
три ПРОВОДКИ (зов снят из `measure`, ключ снят из документа, раздел снят из отчёта), возврат
формы `doc.get("counts") or {}` в новую проекцию (инв. #17), возврат остатка (`execute: False`)
и снятие бегуна. Дерево восстанавливалось и сверялось по sha после КАЖДОЙ мутации; батарея шла
в ОТДЕЛЬНОМ дереве `/tmp/spa_c667_bat`, рабочее не мутировалось ни разу.

Первый заход батареи дал одного «выжившего» — и им оказалась МОЯ ЖЕ мутация, написанная так,
что она ничего не меняла (`"__unused": 0` вместо `execute: False`). Записано как есть: мутация,
не меняющая поведения, не есть дыра в приёмке, но и засчитывать её в убитые нельзя.

Обязательные шаги цикла: `consume_office_reports` — прочитано 97, не прочитано 0 ·
`cycle_analytics_audit` код 0 (аудит мерили 16.7 ч назад при такте 30 ч) · `book_second_record`
код 1 — расхождение НАЗВАНО: дыра в записи T008 (23.08, $94 999.88 → $80 000.00) и
переименование ключа T034 (`fluid_usdc` → `fluid_fusdc`), оба известны и держатся отдельными
карточками · `capital_observability_census` код 0 (обе оси 100 %) · `build_site_numbers --if-due`
код 0 (внутри недели, 2 дн из 7) · `import_time_anchor_bombs` код 0 · `adr_number next` = 447.

**Слой.** ADVISORY: порог не введён, ни один вердикт переписи не изменился, `POLLED_ADAPTERS`,
пороги RiskPolicy v1.0, стоп-кран, живой трек и `landing/` не тронуты. Карточка владельцу не
заводилась: ни один из трёх предметов ADR-285 не задет.

**Заказ G70** — в хвосте ADR-447 (цена читателей канала теста, чья неизмеримость объявлена
причиной, но причина не померена · собственный шум измеренной цены против объявленного запаса ·
вторая копия цены, оставленная «для сверки»). G69 действует в остатке (пп. 2, 3).
