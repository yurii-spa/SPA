# OWNER DECISIONS NEEDED — накопительный список вопросов

> Автономные циклы (roadmap-ship + UI/UX) складывают сюда всё, что требует твоего
> решения (owner-gated: числа / наименования / legal / инфра / нарратив / секреты).
> Ты отвечаешь пачкой когда удобно — прямо здесь (пиши ответ под вопросом в блоке
> **ОТВЕТ:**), либо в чате со ссылкой на ID. Я перечитываю файл каждый виток и
> исполняю ответы. **Не удаляй вопросы — я помечаю их RESOLVED сам после исполнения.**

Обновлено: 2026-07-12 (ночной автономный прогон).

---

## 🔴 ВЫСОКИЙ ПРИОРИТЕТ (разблокируют конверсию/деньги)

### Q-OWN-01 · Бизнес-email для точки контакта  ✅ RESOLVED 2026-07-12
**Контекст:** `/pilot` — терминал воронки (я развёл туда трафик с packages/conservative),
но у него НЕТ способа связаться. Единственный контакт на сайте — твой личный
`yuriycooleshov@gmail.com` на selling-страницах (выглядит непрофессионально для
аллокатора). Это **главный рычаг** — превращает готовую воронку в реальную точку конверсии.
**Что нужно:** канонический бизнес-email (напр. `pilot@earn-defi.com` / `research@earn-defi.com`).
Как только дашь — подключу его на `/pilot` и заменю личный Gmail на selling-страницах.
**ОТВЕТ:** РЕШЕНО — построена ФОРМА заявки на /pilot (email+сообщение) → owner Telegram + счётчик в /admin (commit 0f6231eb). Реальный mailbox pilot@earn-defi.com НЕ создан (нужен mail-провайдер+MX в твоём CF — Cloudflare Email Routing, 5 мин, форвард на Gmail); форма работает БЕЗ него. Скажи, если хочешь ещё и mailto-адрес.

### Q-OWN-02 · FAQ противоречит paper-стадии (честностный риск)  ✅ RESOLVED 2026-07-12
**Контекст:** `landing/src/pages/faq.astro` описывает продукт как **уже принимающий деньги**:
«minimum investment amount», «withdrawals processed within 1 business day», «no lock-up»,
«fee structure after KYC». Это прямо противоречит позиции везде на сайте («paper / advisory /
НЕ оферта, внешний капитал закрыт до legal-clearance»). Читается как solicitation действующего
фонда — то, чего ты избегаешь через `/pilot` «not an offer».
**Варианты:**
- (A) Переписать 4 ответа FAQ под paper-стадию («исследовательский paper-трек; коммерческие
  условия — после go-live + legal review»). Я могу сделать честно, без выдумывания чисел.
- (B) Подтвердить, что продукт реально принимает капитал сейчас (тогда надо согласовать с
  остальным сайтом — но это меняет всю честностную рамку и требует legal).
**Рекомендация:** (A). Скажи «FAQ вариант A» — исполню.
**ОТВЕТ:** РЕШЕНО (вариант A) — FAQ minimum/lock-up/fees переписаны под честную paper-стадию (commit c46b809d): «research paper-track, не открыт для внешнего капитала, условия публикуются прозрачно на go-live, не оферта».

---

## 🟡 СРЕДНИЙ ПРИОРИТЕТ (инфра / нарратив)

### Q-OWN-03 · CF Access на /admin (утечка бизнес-данных)  ✅ RESOLVED 2026-07-12
**ОТВЕТ (owner):** «уже сделано давно, вход по моей почте». **ПРОВЕРЕНО ВЖИВУЮ 2026-07-12:** `/admin`
и `/admin/funnels` → **HTTP 302 → `restless-pond-...cloudflareaccess.com` (Cloudflare Access «Sign in»)**.
Admin закрыт CF Access по owner-email. Прежняя оценка «без auth» была НЕВЕРНОЙ — я смотрел код, а auth
стоит на CF-edge ДО origin (в коде не виден). **Разблокирует Phase 3 (admin-auth prereq satisfied).**
**Контекст (historic):** `/admin/*` в коде без auth — защита на CF-edge (не в коде).

### Q-OWN-04 · Консолидация дашбордов (page sprawl, UX-36)
**Контекст:** три параллельных дашборда: `/dashboard` (canonical), `/cockpit/*` (Desk Cockpit),
`/board/*` (DFB DeFi Risk Board). Дублируют функции, размывают IA. Какой canonical публичный,
а остальные — за `/admin` (операторские)?
**Рекомендация:** `/dashboard` публичный canonical; `/cockpit/*` + `/board/*` → операторские
(убрать из публичного sitemap, как я сделал с cockpit-kit). Подтверди — исполню.
**ОТВЕТ:**

### Q-OWN-05 · Публиковать ли per-sleeve forward-вердикты  ✅ RESOLVED 2026-07-12
**ОТВЕТ:** «скрыть до улучшения» — per-sleeve вердикты убраны с публичной /fundability (флагман BELOW_FLOOR больше не светится); aggregate-честность сохранена; реверсится из git.
**Контекст:** `forward_analytics.json` считает честные per-sleeve вердикты
(BEATS_FLOOR / THIN_TRACK / **BELOW_FLOOR**) + дни до вердикта. Сейчас нигде не показаны.
Флагманский `rates_desk_fixed_carry` sleeve = **BELOW_FLOOR** (пока не бьёт RWA-floor).
Показывать это публично (сильный честностный сигнал, но признаёт недоработку флагмана) или
держать внутренним до улучшения трека?
**ОТВЕТ:**

### Q-OWN-08 · Аббревиатура «SPA» расшифровывается 3 способами (branding-дрейф)  ✅ RESOLVED 2026-07-12
**Контекст:** на публичном сайте «SPA» раскрывается по-разному:
- **«Smart Passive Aggregator»** — на legal-страницах (`/disclaimer`, `/risk-disclosure`) +
  это канон в `CLAUDE.md` (внутренний источник истины). Доминирует (6×).
- **«Stablecoin Preservation Algorithm»** — в `/faq` (2×).
- **«Stablecoin Portfolio Algorithm»** — в блог-посте 2026-06-18 (2×).
Даже два «Stablecoin …» варианта расходятся одним словом (Preservation vs Portfolio) — это дрейф,
а не сознательный выбор. Для honesty-first бренда 3 расшифровки на одном сайте выглядят небрежно.
**Рекомендация:** выровнять ВСЁ к **«Smart Passive Aggregator»** (совпадает с legal-страницами +
CLAUDE.md-каноном). Это НЕ изобретение имени — приведение отклонений к уже-каноническому.
Скажи «SPA = Smart Passive Aggregator везде» — исполню (faq + блог → канон). Либо назови другое
единое публичное имя, если «Stablecoin Preservation Algorithm» — сознательный маркетинг-бренд.
**ОТВЕТ:**

---

## 🟢 НИЗКИЙ ПРИОРИТЕТ (secrets / прод-конфиг на твоей стороне)

### Q-OWN-06 · ETHERSCAN_API_KEY на прод (checkup approvals)
**Контекст:** в DeFi Checkup сканирование approvals показывает «not scanned» в проде
(Etherscan V2 keyless retired; твой `ETHERSCAN_API_KEY` не доходит до прод-сервиса Railway).
Approvals — сигнал риска #1 (drain-риск). Fallback на Alchemy `eth_getLogs` хрупок на китах.
**Что нужно:** проверить, что `ETHERSCAN_API_KEY` выставлен на правильном Railway-сервисе+env
и передеплоить. Инфра на твоей стороне.
**ОТВЕТ:**

### Q-OWN-07 · Retention secrets (wallet-watch, Q2-3)
**Контекст:** wallet-watch retention-петля (главный PMF-рычаг: run-once tool → monitoring habit)
собрана в коде, но требует `WALLET_REF_SALT` + `RESEND_API_KEY` на прод Railway + cron.
**Что нужно:** выставить секреты + подтвердить cron. Инфра на твоей стороне.
**ОТВЕТ:**

---

## ✅ УЖЕ РЕШЕНО (для истории, не требует действий)

- **Tier naming** → Conservative / Balanced / Aggressive (везде). ✅ 2026-07-11
- **APY display** → «up to {max}%» везде как на главной (Aggressive capped up-to-20%);
  реализованный ~3.3% — как честный контекст. ✅ 2026-07-12
- **UX-26** — evidenced live книга = тир Conservative. ✅
- **URL rename** → /strategies/{conservative,balanced,aggressive} (стары­е → redirect-stubs). ✅
- **Homepage** → ведёт реальным работающим тиром, не doom-агрессивом (хвост показан в drawdown-ячейке). ✅

### Q-OWN-11 · /pilot «человек» + invest@ mailbox (SELL SPRINT M9/UX-17)
**Контекст:** brief M9 — /pilot должен вести живым человеком: «поговори с тем, кто это построил». Нужно: твоё отображаемое имя (или псевдоним) + опц. фото + ссылка на календарь (Calendly и т.п.). Плюс: создать `invest@earn-defi.com` (или подобный), чтобы убрать личный Gmail с продающих страниц (UX-17). Копию /pilot чиню сейчас; человека/почту добавлю по твоему ответу.
**ОТВЕТ:**

### Q-OWN-12 · Admin auth (эскалация Q-OWN-03; блокирует Phase 3)  ✅ RESOLVED 2026-07-12 (CF Access live, see Q-OWN-03) — Phase 3 UNBLOCKED
**Контекст:** `/admin/*` без авторизации, а Phase 3 хочет спрятать туда ещё чувствительные операторские страницы. Нельзя двигать их под /admin без auth. Выбор: (A) Cloudflare Access на /admin (твоя инфра, ~15 мин) — рекомендую; (B) серверный shared-token gate (я могу построить). Phase 3 заблокирован, пока не выберешь.
**ОТВЕТ:**

### Q-OWN-13 · Early-access list M7 — подтверди framing
**Контекст:** brief M7 — превратить наши gate-критерии (30 дней + vol-event) в scarcity: waitlist «Aggressive выйдет в live только после проверки — вступи, чтобы первым получить отчёт о валидации» + номер позиции («You're #23»). Переиспользую /api/pilot/request с тегом source=early_access. Честно (не фабрикуем). Рекомендую: ДА. Подтверди framing.
**ОТВЕТ:**

### Q-OWN-14 · Conservative headline = «~3.3% realized (track-to-date)»
**Контекст:** brief §2/§8 — сейчас три поверхности показывают три разных числа (up-to-6/12/20 vs 15-25 vs spot ~4.4% vs ~3.3%) — это убивает доверие. Фикс: везде вести числом **«~3.3% realized (track-to-date)»** (стабильное годовое с начала трека, НЕ волатильный дневной spot), а «up to 6%» — как band-контекст, не headline. Сохраняет твоё решение от 11.07 про «up to {max}%». Рекомендую: ДА. Подтверди.
**ОТВЕТ:**

## Q-OWN-15 — push git tag `verifier-v1.2` (проверка верификатора)
**Что:** я перепинил публичный верификатор на реальный SHA `f560aed8` (manifest v1.2 + verify.astro). Я умею пушить только файлы, НЕ git-теги. Download-ссылка сейчас указывает на `main` (работает, но не immutable).
**Действие владельца:** `git tag verifier-v1.2 <commit b03a72af> && git push origin verifier-v1.2` — тогда появится immutable-ссылка на верификатор.
**Влияние:** низкое (main-ссылка + SHA-пин уже дают целостность; тег — это «золотой» immutable reference для скептика). Не срочно.

## Q-OWN-16 — мгновенный Telegram-пуш о лиде (сейчас через дайджест)
**Что:** заявки с /pilot и early-access (M7) я перевёл с прямого `telegram_client.send` на
санкционированную единую Telegram-власть (`push_policy` digest queue) — иначе тест
`test_no_rogue_telegram_senders` держит CI красным (архитектура: один push-authority).
**Эффект сейчас:** лид попадает в твой ежедневный дайджест (не мгновенный отдельный пинг).
**Если хочешь мгновенный пинг на каждую заявку:** нужно завести Tier-1 whitelist-key `pilot_request`
в `spa_core/telegram/push_policy.py` (ADR/owner-решение — это меняет push-политику). Скажи «да,
мгновенный» → заведу key. По умолчанию оставляю дайджест (заявка не теряется).

## Q-OWN-17 — двойной capacity-артефакт на /fundability (framing)
**Что:** на /fundability теперь два измеренных capacity-числа рядом (оба реальны/детерминированы, разного scope):
- `portfolio_capacity.json` (комбинированный портфель, все sleeve'ы) = **~$65k/год над флором, 22 fundable-книги**
- `n_book_capacity.json` (rates-carry книга ОТДЕЛЬНО, стек по одному рельсу) = **плато ~$5k/год к 23 книгам, marginal последней книги $9**
Я заскоупил n-book label явно («rates-carry книга ОТДЕЛЬНО... НЕ комбинированное число выше»), чтобы не читалось как противоречие.
**Вопрос владельцу:** какое число — «заголовочное» на /fundability? Показывать оба (как сейчас, комплементарно — единичный thesis имеет низкий структурный потолок → нужен масштаб/декорреляция) ИЛИ только одно? Это framing/messaging-решение, не изменение чисел.
**Влияние:** низкое-среднее (оба честны; вопрос акцента). Не срочно — текущий scoped-показ безопасен.

## Q-OWN-18 — ❌ FALSE ALARM (снято 2026-07-12) — CF был исправен, ошибка в МОЁМ методе проверки
**Разрешено:** владелец проверил CF Deployments — все деплои ✅ зелёные, ни одного failed/quota. CF деплоил нормально ВСЁ время. Мой ложный вывод «не деплоит» = баг проверки: курлил `/refusals` без `-L` → ловил пустое тело 308-редиректа (Astro trailing-slash), плюс `?cb=` ломал роутинг sub-страниц → ложные «старая версия». Правильная проверка (`curl -L` + trailing slash, без query) показывает: всё было live. Никаких действий от владельца НЕ требовалось. Извинения за напрасную проверку дашборда. Исходный (ошибочный) текст ниже сохранён для истории.

### (исходный ошибочный флаг)
## Q-OWN-18 (WRONG) — CF Pages перестал деплоить (БЛОКЕР landing-деплоев) ⚠️
**Симптом:** коммиты `0586adee` + `173c702c` (мои 2 moat-surface'а) на origin/main HEAD >1 часа, локальные `npm run build` зелёные, но **earn-defi.com их не показывает** — последний задеплоенный коммит `88a081bc`. Сайт НЕ заморожен (88a081bc live, HTTP 200), но CF встал на новейших.
**Диагностика с Mac (сделана):** не CDN-кэш (cache-buster + cf-cache-status:DYNAMIC), не freshness-guard (все prebuild через `|| true`), коммиты подтверждённо на origin/main. Причина — **на стороне CF** (невидима отсюда).
**Вероятная причина:** CF Pages **build-quota cap** (free tier ~500 build/мес — за эту длинную сессию десятки landing-пушей, каждый = отдельная CF-сборка) ИЛИ зависшая/упавшая CF-сборка.
**Действие владельца:** открой **Cloudflare Pages dashboard → earn-defi project → Deployments** — посмотри статус сборок после 88a081bc (failed / queued / quota-exceeded). Если quota — апгрейд плана или подожди сброса; если failed — пришли лог сборки.
**Влияние:** ВЫСОКОЕ для landing — пока CF не деплоит, НИ ОДИН новый landing-пуш не появится на сайте (мои surface'а + любые будущие). Я приостановил landing-пуши до разблокировки. API/checkup/CI не затронуты.

## Q-OWN-19 — Phase 1 (DeBank dashboard shell) требует браузерной проверки — как действовать?
**Контекст:** Phase 0 (продающий слой) завершён. Следующее — Phase 1 (spec 03): пересобрать /dashboard из узкой колонки в DeBank/Zapper-shell (sidebar + topbar + широкая 12-колоночная сетка). Это ровно то, что ты просил («пересобрать дашборды в современный формат как DeBank»).
**Проблема:** приёмка Phase 1 (spec §7) требует **браузерной/devtools-проверки**: sidebar/topbar/сетка без горизонтального скролла на 1280/1440/1920, мобильный bottom-bar, и критично — «заблокировать API-хост в devtools → каждая из 24 панелей деградирует индивидуально» (что polling живого дашборда не сломан). Из моего окружения я делаю только curl — **визуально и polling проверить не могу**. Ранее ты отклонил браузерный navigate.
**Риск:** re-shell ЖИВОГО /dashboard без визуальной+polling-проверки может незаметно сломать главный дашборд (island DashboardLive я НЕ переписываю, только оборачиваю — JS-polling не должен сломаться, но CSS-раскладка непроверяема).
**Варианты (выбери):**
1. **Дать браузер-доступ** (разрешить mcp navigate на earn-defi.com) → я сам проверю раскладку+polling.
2. **Я собираю shell + оборачиваю /dashboard, ТЫ смотришь глазами** → если раскладка кривая, откачу мгновенно (island цел, откат = 1 коммит).
3. **Собрать shell на превью-роуте** `/dashboard-preview` сначала → ты одобряешь → потом меняю живой.
**Рекомендую (2)** — быстрее всего, island не трогается, откат тривиален. Пока не ответишь — Phase 1 не начинаю вслепую (не рискую главным дашбордом), иду по другим не-gated пунктам.

**✅ РАЗБЛОКИРОВАНО через вариант 3 (preview-роут) — 2026-07-12, commit `6a957fe6`.**
Собрал shell на отдельном превью-роуте, ЖИВОЙ `/dashboard` НЕ тронут:
- **Смотри вживую:** **https://earn-defi.com/dashboard-preview** (noindex; вверху жёлтый баннер «PREVIEW»).
- Что там: `DashboardShell` (спрятываемый sidebar 256/64 + sticky topbar + KPI-полоса + 12-колоночная сетка) оборачивает **тот же самый** island `DashboardLive` (`client:load`, те же `initialFacts`, НИ строки JSX внутри не менял — spec §3 Step 1 «оборачиваем, не переписываем»). RTMR-монитор ушёл в правый rail. Токены — из существующего `Layout :root`, новых цветов нет.
- **Что проверить глазами (spec §7):** (a) sidebar+topbar+KPI+сетка без горизонтального скролла на 1280/1440/1920; (b) кнопка-сворачивание sidebar (256⇄64, состояние помнится); (c) на телефоне sidebar → горизонтальная полоса сверху, rail под main; (d) **главное — polling жив:** вкладки дашборда обновляются каждые ~15с, при мёртвом фиде панель индивидуально сереет (не ломает соседей).
- **Твой вердикт:** нравится → **скажи «промоутим»**, и я переношу shell на живой `/dashboard` одной правкой (`dashboard.astro` начинает использовать `DashboardShell`, island тот же). Не нравится/криво → скажи что не так или «удаляй» → сношу 2 файла (`shell/DashboardShell.astro` + `dashboard-preview.astro`), **нулевой след** на остальном сайте.
- Что НЕ вошло в превью (Step 2, после одобрения): вынос живых KPI из island в топбар shell, section-nav → якоря sidebar. Сейчас KPI-полоса — из committed-снимка (`track_snapshot.json`), честно помечена «снимок · трек-на-дату».

## Q-OWN-20 — Авто-агент research-changelog (Q2-14): новый агент vs встроить в существующий?
**Контекст:** Генератор публичного changelog'а (`scripts/generate_research_changelog.py`) полностью готов, детерминирован, идемпотентен (пишет `landing/src/data/changelog.json`, который рендерят `/changelog` + RSS). Проверено: сейчас корректно НЕ создаёт дубль (трек = 19 дней, сигнатура не менялась). Но он **ни к какому агенту не подключён** — «публичный heartbeat» появляется только если запустить руками. Новая запись возникает, когда трек продвигается на день (ежедневный цикл 06:00 UTC).
**Проблема:** чтобы heartbeat был автоматическим, нужно (а) регулярно запускать генератор И (б) запушить обновлённый `changelog.json` (это **закоммиченный source-файл**, не runtime-`data/`) в origin, чтобы CF пересобрал сайт. Автопуш закоммиченного source-файла из агента = зона push-автоматики/флита (твой домен, риск клоббера параллельной работы) — не трогаю без твоего решения.
**Варианты (выбери):**
1. **Отдельный агент** `com.spa.research_changelog` (weekly): соберу wrapper+plist по каноничному стандарту, прогоню `check_agent_before_deploy.sh` (exit 0, лог создан), добавлю в `install_all_agents.sh` — **загрузишь ты** (`launchctl bootstrap` через gate). Мамкин-пуш через стандартный `push_v*.sh`→autopush.
2. **Встроить в существующий** `com.spa.golive_freshness` (уже крутится 6h, уже пишет readiness/owner_blockers): добавлю один детерминированный шаг генерации + постановку файла в стандартную autopush-очередь. Без нового агента/плиста — меньше поверхности флита. Нужна твоя отмашка, т.к. трогает push-поток закоммиченного файла.
3. **Оставить ручным** — генерирую по требованию (напр. когда прошу отчёт), без авто-heartbeat.
**Рекомендую (2)** — переиспользование вместо размножения агентов; ты только подтверждаешь push-поток. Код генератора уже готов и протестирован в любом случае.

## Q-OWN-21 — Контраст text-muted / text-faint не проходит WCAG AA (a11y, дизайн-токены)  ✅ RESOLVED 2026-07-12
**ОТВЕТ:** «Да, осветли» — применил рекомендованные шейды (muted #6B7280→#868D99 5.52:1, faint #565E6D→#727A88 4.26:1) в `Layout.astro` (commit dd0abea6) + синхронизировал `SITE_DESIGN_SYSTEM.md`. Оба проходят WCAG AA, иерархия затемнения сохранена.

**Контекст:** замерил реальный контраст токенов текста против фонов (`Layout.astro:141/144`). Порог WCAG AA = **4.5:1** для body-текста, **3:1** для крупного/UI.

| Токен | Цвет | vs bg-base #0A0C10 | vs bg-surface #11141A | Вердикт |
|---|---|---|---|---|
| text-primary | #E8EAF0 | 16.3 | 15.3 | ✅ |
| text-secondary | #A6ADBB | 8.7 | 8.2 | ✅ |
| **text-muted** | #6B7280 | 4.05 | **3.81** | ⚠ проходит 3:1, **НЕ проходит 4.5 body** (используется в 95 файлах) |
| **text-faint** | #565E6D | 3.00 | **2.83** | ❌ **не проходит даже 3:1** на surface (79 файлов) |
| accent | #5B8DEF | 6.1 | 5.7 | ✅ |

**Проблема:** text-faint используется НЕ только декоративно — на нём живёт **ключевая honesty-микрокопия** («paper · tail shown · для live отклонён», «нельзя проверить в блокчейне», «auditable block-by-block · 0.0% drawdown»). Эта копия — суть продукта (раскрытие хвоста), и она на **проваленном контрасте** → часть пользователей (слабое зрение, яркий свет, дешёвые экраны) её не прочитает.
**Почему флагаю, а не чиню сам:** это твоя тщательно настроенная дизайн-система (`SITE_DESIGN_SYSTEM.md`) с намеренной иерархией затемнения primary>secondary>muted>faint. Подъём muted/faint до AA **сплющит** эту иерархию — эстетический trade-off, который решать тебе, не мне (правило «дизайн/числа — owner-gated»).
**Рекомендованные значения (замерены, минимальный подъём до AA):** text-muted #6B7280 → **#868D99** (base 5.86 / surface **5.52**:1 — чистый body-AA); text-faint #565E6D → **#727A88** (base 4.52 / surface **4.26**:1 — почти body-AA, уверенно >3:1). Оба заметно светлее текущих, но иерархия primary>secondary>muted>faint сохраняется (secondary #A6ADBB = 8.2 остаётся выше muted 5.5 выше faint 4.3). Могу применить в 1 правку (только 2 значения токенов в `Layout.astro`, весь сайт подхватит) — **скажи, и сделаю**, либо дай свои шейды.
**Побочная находка (doc-drift):** `SITE_DESIGN_SYSTEM.md:180` говорит text-faint = **#444B57** (это ещё хуже, ~2.3:1), а код использует #565E6D. Токен-таблицу дока стоит синхронизировать с кодом при этом же решении.
