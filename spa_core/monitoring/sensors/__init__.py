"""RTMR (ADR-053) sensors package. Deterministic, read-only, off-chain, LLM-forbidden
(except a future advisory news.py, currently deferred). Each sensor emits RiskSignal via
the multi-source quorum helper."""
# LLM_FORBIDDEN
