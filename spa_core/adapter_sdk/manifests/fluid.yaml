# Fluid (Instadapp) lending — DeFiLlama yields project slug: "fluid-lending".
# NOT covered by the existing file adapters — declarative-only protocol.
name: fluid
defillama_protocol_id: fluid-lending
chains:
  - Ethereum
symbols:
  - USDC
  - USDT
tier: T2
cap: 0.15
exit_latency:
  hours: 0.0          # liquid lending position, same-block withdrawal
  profile: instant
quality_gates:
  min_tvl_usd: 1000000
  stable_only: true
  max_apy_pct: 30.0
