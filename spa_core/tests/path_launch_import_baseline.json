{
  "_why": "Скрипты, которые импортируют пакет репозитория на ВЕРХНЕМ УРОВНЕ модуля и при этом НЕ могут его импортировать, когда файл запускают ПО ПУТИ (launchd и обёртки флота зовут именно так: sys.path[0] = каталог scripts/, рабочий каталог в sys.path не попадает). База может ТОЛЬКО УМЕНЬШАТЬСЯ. Дописывать сюда файл, чтобы погасить падение, ЗАПРЕЩЕНО — тот же порядок, что у frozen_date_baseline.json и site_numbers_baseline.json.",
  "_measured": "2026-09-12, цикл #571, ADR-347. Население: 88 скриптов с импортом пакета репо на верхнем уровне; не импортируются 10; не измерен 1 (scripts/archive/test_json_to_sqlite.py — падает на `import scripts`, а это ДРУГОЙ класс, и выдавать его за наш нельзя).",
  "_live_exposure": "Единственный член класса, который запускал ЖИВОЙ агент флота (scripts/find_defillama_sources.py, com.spa.source_discovery), ПОЧИНЕН 13.09 по решению владельца (ADR-366): бутстрап корня стоит ДО импортов, и `export PYTHONPATH` в обёртке перестал быть единственной опорой. Оставшиеся девять живыми агентами не запускаются. База может ТОЛЬКО уменьшаться.",
  "cannot_import_when_launched_by_path": [
    "scripts/archive/db_migrate.py",
    "scripts/archive/migrate_json_to_sqlite.py",
    "scripts/archive/regenerate_rates_decision_log.py",
    "scripts/build_dependency_map.py",
    "scripts/fund_api_server.py",
    "scripts/generate_changelog.py",
    "scripts/module_health_report.py",
    "scripts/s11_backtest_90d.py",
    "scripts/strategy_integration_test.py"
  ]
}
