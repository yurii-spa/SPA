"""SPA Swarm — the reflexive carry-harvester agent architecture (docs/SWARM_ARCHITECTURE.md).

Layer packages live here. Block 1 (L2 position guardians, forward paper) is
`guardian_forward.py`. Everything in this package is ADVISORY / paper-only / OUTSIDE_RISKPOLICY:
it never moves capital, never touches the go-live track, and RiskPolicy v1.0 stays the sole
hard execution gate. Deterministic, stdlib-only, fail-CLOSED. LLM FORBIDDEN.
"""
# LLM_FORBIDDEN
