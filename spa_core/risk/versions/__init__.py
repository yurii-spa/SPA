# spa_core/risk/versions/
# Frozen snapshots of RiskConfig for each released policy version.
# These files are READ-ONLY historical records — do not import them as active policy.
# To rollback: copy the desired version's dataclass values back into policy.py RiskConfig defaults.
